def максимум_или_ноль(элементы):
    if not элементы:
        return 0
    return max(элементы)

# Примеры
print(максимум_или_ноль([3, 9, 4]))  # Вывод: 9
print(максимум_или_ноль([]))        # Вывод: 0
