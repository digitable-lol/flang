(module "Есть элемент"
  (define (есть-элемент элементы значение)
    (not (null? (filter (lambda (x) (= x значение)) элементы))))

  (display (есть-элемент '(1 2 3) 2))
  (newline)
  (display (есть-элемент '(1 2 3) 9))
  (newline))
