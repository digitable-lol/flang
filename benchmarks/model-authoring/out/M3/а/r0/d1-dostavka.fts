class Отправление:
    вес: number
    срочная: boolean

function Стоимость_доставки(отправление: Отправление): number {
    var стоимость = 100
    if (отправление.вес >= 10) {
        стоимость += 50
    }
    if (отправление.срочная) {
        стоимость += 200
    }
    return стоимость
}
