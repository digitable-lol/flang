категория «Сумма до»

  функция «Сумма до» принимает «н» типа «нат» и возвращает число
    если «н» равен 0
      возвращает 0
    иначе
      возвращает сумма от 1 до «н» включительно

  пример «н» равен 0
    возвращает 0

  пример «н» равен 5
    возвращает 15
