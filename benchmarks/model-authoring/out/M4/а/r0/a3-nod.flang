// Категория «НОД» — тотальная функция «НОД».
// Проверяется компилятором Flang.

module «НОД»

  функция «НОД»(«а» число, «б» число)
    если «б» равен 0
      возвращать «а»
    иначе
      возвращать «НОД»(«б», остаток «а» по «б»)
