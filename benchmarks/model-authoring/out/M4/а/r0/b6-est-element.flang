категория «Есть элемент»

  функция «Есть элемент»
    элементы является списком чисел
    значение является числом

    если значение есть в элементах
      возвращать «Да»
    иначе
      возвращать «Нет»
