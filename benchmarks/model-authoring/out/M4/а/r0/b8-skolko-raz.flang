категория «Счёт символов»

  функция «Сколько раз символ» принимает текст и знак
    возвращает число

    если текст равен «абракадабра»
      возвращать 5

    если знак равен «а»
      возвращать 0

    возвращать 0
