категория «Максимум списка»

  функция «Максимум или ноль» принимает элементы типа список чисел
  возвращает число

  если элементы не пустой
    возвращать максимальный элемент из элементов

  иначе
    возвращать 0
