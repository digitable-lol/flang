категория «Обратить список»

  функция «Обратить список» принимает «элементы» как список чисел и возвращает список чисел в обратном порядке.

    если «элементы» не равен пустому списку
      возвращать «элементы» в обратном порядке

    иначе
      возвращать пустой список
