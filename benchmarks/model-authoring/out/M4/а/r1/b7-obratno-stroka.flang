категория «Обратная строка»

  функция «Обратно» принимает «текст» как строку
    возвращает строку
      если «текст» равен «абв»
        возвращает «вба»
      если «текст» равен пустой строке
        возвращает пустую строку
