категория «Длина списка»

  функция «Длина списка» принимает элементы как список чисел
  возвращает число — сколько в нём элементов

  для каждого элемента в элементах
    если элемент равен 0
      продолжить
    иначе
      увеличить результат на 1
