категория «Компьютерные науки»

  макрос «Максимум списка»
    функция «Максимум или ноль»
      принимает «элементы» как список чисел
      возвращает число
      если «элементы» не равны пустому списку
        возвращает максимум «элементы»
      иначе
        возвращает 0
