категория «Только чётные»

  функция «Только чётные» принимает «элементы» как список чисел
  возвращает список чисел
  где если элемент делится на 2 без остатка то возвращаем его

  пример [1, 2, 3, 4] даёт [2, 4]
  пример пустой список даёт пустой список
