категория «Есть элемент»

  функция «Есть элемент»
    параметры «элементы» является списком чисел
    параметр «значение» является числом
    возвращает признак

    если «значение» в «элементы»
      возвращать «Да»
    иначе
      возвращать «Нет»
