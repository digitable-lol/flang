категория «Обратная строка»

  функция «Обратно» принимает «текст» как строка
    возвращает строку
    если «текст» не равен пустой строке
      возвращает обратную строку «текст»
    иначе
      возвращает пустую строку
