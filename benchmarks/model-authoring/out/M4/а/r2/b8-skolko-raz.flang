категория «Счёт символов»

  функция «Сколько раз символ»
    текст является строкой
    знак является строкой
    возвращает число

    если текст равен «абракадабра»
      возвращать 5
    если текст равен пустой строке
      возвращать 0
