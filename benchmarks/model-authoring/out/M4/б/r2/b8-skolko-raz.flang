категория «Счёт символов»

  объект «Сколько раз символ»
    принимает текст
    принимает знак
    возвращает число
    пример «Символ «а»»
      дано текст равен «абракадабра»
      ожидается 5
    пример «Символ не входит»
      дано текст равен «»
      ожидается 0
