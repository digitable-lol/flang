модуль «Максимум списка»

  экспортирует «Максимум или ноль»
    принимает элементы: список числа
    возвращает число
    пример «Список 3 9 4»
      дано элементы равно [3, 9, 4]
      ожидается 9
    если элементы не пустой
      то максимум элементов
    иначе
      то 0
