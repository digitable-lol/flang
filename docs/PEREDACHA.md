# Передача: состояние flang на 16 августа 2026

Этот файл написан для того, чтобы работу можно было продолжить **с другой учётной
записи и без доступа к переписке, в которой она велась**. Здесь состояние, пути,
ветки, чего не хватает и что делать дальше.

Ход размышлений — почему решили так, что померили, что оказалось ложным — лежит
рядом в `docs/zettel/` (65 заметок, начните с `README.md`). Этот файл про
**состояние**, тот каталог про **знание**.

---

## 1. Где что лежит

| Что | Путь |
|---|---|
| Рабочий каталог | `/home/a/projects/flang` |
| Удалённый | `github.com/digitable-lol/flang` |
| База знаний, источник правды | `docs/zettel/` (в дереве) |
| Копия базы для всех пользователей машины | `/srv/flang-znanie/` |
| Навык, заставляющий базу пополнять | `.claude/skills/flang-zettel/SKILL.md` |
| Резервная копия всех веток до правок | бандл `REZERV-do-perepisi.bundle` в рабочем каталоге сессии |
| Точка возврата `main` | ветка `backup/main-do-sliyaniya-15aug` на GitHub |

**Пуш с этой машины напрямую не работает** — нет учётных данных GitHub. Путь
такой: `git bundle create` → `scp` на `sbxa` в `/tmp/` → `sudo cp` в `/home/m/` и
`chown m:m` → `sudo -u m git fetch <бандл>` → `sudo -u m git push origin`. Через
`/tmp` обязательно: у пользователя `m` нет прав читать домашний каталог `a`.

**Подпись коммитов** настроена в конфигурации репозитория:
`the-homeless-god <zimtir@mail.ru>`. Рабочие копии подхватывают её сами.

**Локаль в системе сломана** — перед `node` ставьте `LC_ALL=C.UTF-8`.

---

## 2. Что сделано за 15–16 августа

### Слой доказательств

| | было 15-го утром | стало |
|---|---|---|
| Утверждений высказано | 30 | **110** |
| Доказано ядром | 28 | **89** |
| Отвергнуто / нарушено | 0 | **0** |
| Решающих правил в ядре | 3 | 3–5 (по ветке) |
| Аксиом | **0** | **0** |

- **Индукция по встроенным типам** (`work/indukciya-vstroennyh`): носителей принципа
  стало три — объявленная сумма, встроенный список, `признак`. Замер, дававший
  **0 из 20** обычных функций библиотеки, даёт **7 из 20**.
- **Предусловие `требует`** (`work/trebuet`): условных утверждений было **ноль**,
  потому что сказать «если» было нечем. Проверяет **вызывающий**, поэтому цена в
  напечатанном коде — **ноль байт**, измерено побайтово во всех восьми целях.
- **Шесть дыр в ядре** (`work/pravda-po-primeru`): ядро печатало «доказано обо ВСЕХ
  входах» там, где прогон находит контрпример. Три нашёл сам агент. Цена починки
  нулевая — ни одно доказательство корпуса не потерялось.
- **56 доказанных лемм** (`work/lemmy`), ядро при этом не тронуто ни строкой.
- **Три формы тела** (`work/formy-tela`, 16 августа вечером): ядро строило посылки
  индукции только с ветвей `разбор` — **58 тел из 208** в `flang/stdlib`. Теперь
  ещё и со свёртки по параметру (**96 из 208**), цель с `если` делится по
  значению условия, а `по свойству` перестало быть заглушкой и сличает
  постусловие по месту вызова. На двадцати функциях замера: **2 содержательных
  из 20 стало 4**. Отчёт — `docs/formy-tela.md`.

### Скорость, память, процессы

- **Память**: 1655 МиБ → 3,5 МиБ на сортировке слиянием (473×); кубический рост
  на сортировке вставками стал линейным (`work/region-na-vyzov`).
- **Процессы**: динамическое порождение работает, **4 млн процессов**, 1768 байт
  на процесс (у Erlang 2648), 2,03 млн сообщений/с при миллионе процессов (у BEAM
  1,53 млн) (`work/porodit`).
- **Скорость**: медленнее Python в 1,4×, Node в 3,3×, ручного C в 8,6×. **Цена
  доказуемости — 2,5 %**: что-то измеримое платит 71 функция из 2799
  (`work/zamer-skorosti`).

### Язык

- **Точное десятичное** (`work/exact-decimal`) — цена **нулевая**, тела печатаются
  побайтово так же, как на обычном числе.
- **Тип `вес`** = отрезок [0, +∞] (`work/beskonechnost`) — законная арифметика с
  «недостижимо».
- **Пять свойств** (`work/zakony`): идемпотентность, коммутативность,
  дистрибутивность, частичный порядок, монотонность.
- **Равенство морфизмов и естественные преобразования** (`work/morphism-equality`) —
  первый закон, краснеющий на честной программе.
- **Модули как объекты категории** (`work/moduli-kak-obekty`) — интерфейс модуля это
  категория, связь между модулями это функтор.

### Самораскрутка

Семь целей печати из восьми написаны на самом flang (JavaScript оставлен
намеренно). Связка поверхность↔ядро перенесена (`work/self-obligations`) и
**подключена к рабочему пути** (`work/bez-etalona`): обязательства в
`flang check --proof` считает слой на самом flang. Что ещё держит реализацию на
JavaScript — раздел 5 и `docs/vybros-etalona.md`.

### WebAssembly

Девятая цель печати **не нужна**: наш C собирается в wasm без единой правки,
94 программы из 94, расхождений 0 (`work/wasm-cherez-c`).

---

## 3. Ветки: что с ними делать

**Сборка сделана.** Ветка `work/svodka3` собрала в себя всё, что несло работу:
38 слияний, красный список прогона совпадает с красным списком `main` строка в
строку (8 проверок, все чужие: sha256 формулы Homebrew и семь строк
`self-proof.test.mjs`). Ведомость пересчитана ОДНИМ прогоном по собранному
дереву, а не сложена из веток.

Остались неслитыми вот эти, и у каждой довод, а не «потом разберёмся».

### Не сливать: работа есть, но текстом она не переносится

| ветка | коммитов | довод |
|---|---|---|
| `work/indukciya-vstroennyh` | 36 | второе ядро, выросшее от той же точки: 55 конфликтов, разные подписи `свести` и `нормализовать`, разные наборы правил. Слияние текста здесь — написание третьего ядра. Разобрано в `docs/zettel/dva-yadra-ne-slivayutsya-tekstom.md` |
| `work/list-inductive` | 10 | тот же носитель принципа (встроенный список), что и у ветки выше, и тот же конфликт |
| `work/self-emit-js` | 9 | побайтовое совпадение не переживает собранное дерево: рантайм уехал на 179 строк в трёх местах, одно из них — замена. Догонка ~550 строк |

### Не сливать: измерено, что работы нет

| ветка | коммитов | довод |
|---|---|---|
| `work/goal-shapes` | 1 | отрицательный результат: заказ на «пять видов цели» проверен и НЕ подтвердился — граница у всех пяти зависит от переменной индукции. Вывод стоит того, чтобы его записать, но кодом он ничего не меняет |
| `work/kernel-byproperty` | 8 | правило «по свойству» в дереве уже стоит и сверено близнецом |
| `work/claims-core` | 7 | утверждения ядра в дереве уже есть — приехали с `work/claims-stdlib` и `work/lemmy` |

### Выбросить (доводы в `docs/vetki-2026-08-15.md`)

`pc`, `fix/seed-grid-one-seed`, `backup/depth-pre-rebase`, `work/flang-rest`,
`bench/model-authoring`, `work/self-check`, `work/lib-measure`.

### Спросить владельца

`surface-per-file` (2 коммита) — архитектурная развилка: поверхность как
объявление файла. `probe/wasm-v-brauzere` (1) — на её вопрос ответила
`work/wasm-cherez-c`, уже влитая.

### Разобрать отдельно, работа большая

`measure/messages-on-bench` (8 коммитов, 569 файлов) — замер авторства моделей.
Сырьё замера (16 520 строк) в корпус пускать нельзя: оно нарочно сломанное, и
репозиторные прогоны по `.flang` от него краснеют на 41 проверке. Вливать —
только вместе с именованным исключением в сборщике файлов.

`work/cons` (5 коммитов, 61 файл) — приписывание в начало встроенной формой.
Работа полезная и живая, но трогает библиотеку целиком и требует своего прогона.

---

## 4. Что делать дальше — по отдаче

### 4.1. Вычислять замкнутую цель — **СДЕЛАНО**

Ход написан и влит: `свести` получила пятый довод — вычислитель — и спрашивает
его после трёх правил и только при их отказе. Решающих правил по-прежнему три,
`АКСИОМЫ` пусты. Ведомость называет ход своим словом («доказано вычислением
замкнутой цели»).

Долг при нём назван: у близнеца на flang этого хода НЕТ, и написать его значит
дать близнецу вычислитель языка, а не переписать правило.

### 4.2. Сличение по вызову — **СДЕЛАНО**

`по свойству «имя»` инстанцирует названное постусловие по месту вызова
(`flang/src/proofterm.mjs`, `поСвойству`). До этого правило существовало на
поверхности и отказывало всегда.

Долг при нём назван: правило стоит на том, что постусловие либо доказано ядром,
либо проверяется на каждом возврате. Снять проверку с НЕДОКАЗАННОГО постусловия
нельзя, не сломав правило.

### 4.3. Список недостающего — десять пунктов с измеренными отказами

`docs/lemmy-otchet.md` на ветке `work/lemmy`. Первые пять по отдаче: вычислять
замкнутую цель; не проверять доказанное постусловие в рантайме; переписка «поле
известной записи»; ветвь `случай любое` как запасная; теоремы в близнеце связывания.

### 4.4. Скорость — две правки дают 1,8× в среднем

1. **Флаг оптимизации при сборке — две строки** (`flang/src/emit/c.mjs:825`,
   `flang/self/emit-c.flang:3211`). Даёт 1,03–1,60×.
2. **Не печатать проверку типа там, где тип доказан** — 150–250 строк. Оценка
   поднята с 60 честно: проверка типов не отдаёт типы узлов вовсе, придётся
   добавить. Даёт 1,09–3,15×.

Вместе 1,8× среднее, до 4,95× на арифметике. Отчёт — `docs/zamer-skorosti.md`.

### 4.5. Разборщик на flang — зазор ЗАКРЫТ, 148 слов из 148

Этот пункт больше не верен дважды. `flang/self/parser.flang` разбирает **все 148
слов таблицы лексера**, включая `требует`, `обеспечивает` и прочие слова слоя
доказательства, теоркат, процессы, `план` — и `в монаде`. Список пробелов —
исполняемый, в `self-parser.test.mjs` (`ПРОБЕЛЫ_РАЗБОРА`), и он **пуст**.

Последнее слово закрыто не продукцией: узел `inMonad` живёт до конца разбора,
где его заменяет разворачивание. Близнецу нужен был отдельный слой — он написан,
`flang/self/monad-expand.flang`. Улика — настоящий файл дерева
`flang/examples/monad/order-total.flang`, вставший в корпус побайтовой сверки.

Четыре поверхности диагностик тоже сделаны: сообщение цитирует слова ТОЙ
поверхности, на которой написан файл. Сверено на 18 отказах руками и 168
обрезках настоящих нерусских файлов — расхождений 0.

Числа и изъятия — `docs/vybros-etalona.md`, §3.5.

### 4.6. Модули как объекты — доделать ~1900 строк

Из них ~970 — **старые долги, не этой работы**: функтор никогда не проверялся на
самом flang, категорные объявления в связывателе на flang не переносятся ни одно.

### 4.7. Пакеты и распространение — решение принято, работа не начата

`docs/modulnost-i-pakety.md` (ветка `work/modulnost-issledovanie`, 1058 строк).
Unison установлен и измерен, а не пересказан.

**Рекомендация:** хеш содержимого как идентичность внутри, имена и версии как
интерфейс снаружи; базу данных вместо файлов не делать.
Шаг 1 — ~770 строк, неделя, **оправдан не пакетами, а кешом доказательств**.
Шаг 2 — ~1370 строк, **синтаксис языка не меняется вовсе**.

---

## 5. Чего не хватает до цели

### Реализацию на JavaScript выбрасываем — решение владельца, работа начата

Перемерено на ветке `work/bez-etalona` 16 августа. Раскладка целиком, с планом
работ и ценой, — `docs/vybros-etalona.md`. Кратко:

1. **Обязательства из рабочего пути вынуты.** `flang check --proof` считает их
   слоем `flang/self/obligations.flang`; реализация на JavaScript живёт только в
   проверках, как второе мнение. Ведомость до и после совпала побайтово на 230
   программах.
2. **Мост между двумя представлениями данных переехал в дерево** —
   `flang/src/bridge.mjs`; зовёт слой `flang/src/self.mjs`.
3. **Ядро доказательств переключить нельзя:** зазор 12 программ из 249, назван
   поимённо. Не хватает четырёх правил: начальная алгебра встроенного списка,
   принцип по отрезку `нат`, точные десятичные, вычисление замкнутой цели.
   Последнее — уже не «написать вычислитель», а связать ядро с
   `self/interpret.flang`, который написан и сверен.
4. **Ведомость переключить нельзя:** у близнеца нет раздела утверждений вовсе,
   это 7 красных проверок и ~410 строк переноса.
5. **Близнеца нет вовсе у 14 711 строк из 53 030**, а не у 29 646 из 50 926:
   прежнее число устарело вдвое. Без близнеца: цель печати в JavaScript,
   процессы, ввод-вывод, языковой сервер, командная строка, девять законов.

### Процессы: до миллиона одновременно активных

Одна стенка — очередь готовых, **измерена**: около 170 мкс на переключение при
миллионе, то есть 5900 переключений в секунду. Миллион молчащих или коротко
живущих — держим уже сейчас.

Это единственный пункт, требующий **решения, а не работы**: порядок в очереди
готовых — часть наблюдаемого поведения, и любая структура с другим порядком
уронит побайтовую сверку.

### Открытые вопросы владельца

- тег `v0.5.0` не создан. Перед ним надо починить `publish-npm.yml` — он запускает
  тесты **раньше** сборки архива и упадёт;
- форма «приписать в начало» — 46 файлов, 4 поверхности;
- несовпадение единицы шага — печатаемый шаг в 40–50 раз грубее;
- `Digitable` и `qa-scheduler` в истории коммитов — это владелец или нет.

---

## 6. Как здесь принято работать

Это не стиль, а то, чем ловятся ошибки. За двое суток **семь раз** нашлась
проверка, которая продолжала зеленеть, перестав сравнивать.

- **Улика ДО, числом.** Не «не реализовано», а «расходится на 1020 из 7888».
- **Снять правку и посмотреть, что покраснеет.** Если ничего — правка ничего не
  держит: либо тест не тот, либо правка не нужна.
- **Подделка.** На каждое новое правило — программа, пытающаяся доказать ложь.
  Она обязана быть отвергнута, и **код отказа обязан её назвать**.
- **Побайтовая сверка**, а не «похоже»: те же значения, те же коды отказов, те же
  тексты, те же счётчики шагов.
- **Контрольная сумма в замере скорости**: все сборки обязаны дать одинаковый
  ответ, иначе замерена скорость чего-то другого.
- **Отрицательный результат ценен.** «Сделали, вышло ноль, вот разложение почему»
  экономит чужую работу больше, чем описание успеха. Так закрылись две работы за
  15 августа.
- **Красный список сравнивать `diff`-ом полных списков**, не на глаз.
- **Числа сторожей прозы сводить правкой прозы**, а не сторожа.

Известные чужие красные на сегодня: 14 файлов не грузятся без собранного `dist/`
(в среде нет `tsc`), sha256 формулы Homebrew, семь проверок `self-proof.test.mjs`
(близнец ведомости не печатает раздел утверждений).

**Часть тестов подвисает под нагрузкой** (`emit-elixir`, `emit-csharp` видели на
599 и 832 с). Подвисание — не красный; прогоняйте отдельно и говорите об этом.

---

## 7. Что нельзя делать

- **Расширять ядро наугад.** Дважды за сутки измерением показано, что угадывание не
  работает: поиск доказательств нашёл ноль, потому что искать нечем; правила по
  условиям `если` дали ноль, потому что нужный факт всегда в запрещённой ветви.
- **Заводить аксиомы.** `АКСИОМЫ` в `flang/src/proofterm.mjs` обязан оставаться
  пуст, и это стоит отдельным тестом.
- **Подключать Z3 как судью.** Как оракула — можно и полезно: поиск предлагает,
  ядро перепроверяет. Как источник истины — нет: это отдаёт доверие двумстам
  тысячам строк чужого кода и убивает то, ради чего язык существует.
- **Сливать с ослабленной сверкой.** Если сверка перестала сравнивать, она врёт
  молча.
- **Считать «доказано» равным «правильно».** Доказательство говорит, что код
  соответствует спецификации. Оно ничего не говорит о том, выражает ли
  спецификация ваше намерение. Это единственная причина, по которой формальные
  методы за пятьдесят лет не захватили индустрию.

---

## 8. Условие, при котором это станет важным

Не набор фич. Одно измеримое условие:

> **Доказательство должно стоить дешевле, чем тесты, которые оно заменяет.**

Сегодня в мире доказательство функции стоит в 5–20 раз дороже тестов, поэтому
доказывают только ядра ОС, криптографию и авионику.

**Наш замер этого числа: 0 из 20** (`docs/zamer-tseny-dokazatelstva.md`, ветка
`work/zamer-tseny`). Тесты: 390 строк, 7 мин 49 с, **4 настоящие ошибки**.
Доказательства: 196 строк, 9 мин 39 с, **0 принятых**.

Повторён 16 августа на голом `main` (`docs/zamer-tseny-2.md`): **2 из 20**
содержательных, и узкое место названо со знаменателем — 72 % библиотеки написано
формами, к которым индукцию прицепить нечем.

После работы по формам тела (`work/formy-tela`, `docs/formy-tela.md`) — **4 из
20**, и узкое место переехало в третий раз: мешает теперь форма УТВЕРЖДЕНИЯ
(связь результата со входом), а не форма тела. Замер надо повторять: это
линейка, по которой видно, движемся ли мы к цели или наращиваем фичи.
