# ADR-0030. Печатник доказывает КАЖДЫЙ СВОЙ ЗАПУСК, а не себя целиком

Состояние: **принято владельцем 13 сентября 2026** — словом «сделай что нужно» в ответ на перечень того, чего языку не хватает до формально-доказуемого (протокол перевода и сличитель — задача 1401, замер CompCert — 1402).
Дата: 11 сентября 2026.
Зависит от: [ADR-0022](0022-a-type-fact-travels-as-a-derivation.md) (факт едет
выводом, который переигрывают).

## 1. Дыра, названная числом

11 сентября прибор `sh scripts/доказуемость.sh` впервые напечатал **ДОКАЗУЕМ**:
625 из 651 обязательств набора переиграны независимой программой, 401 подделка
не принята.

**Но доказательство привязано к `flang check`, и только к нему.** Владелец
спросил прямо: если он пишет программу и собирает её, доказан ли собранный код.
Ответ — нет, и цепочка рвётся так:

| шаг | что доказано |
|---|---|
| `flang check` | постусловия верны, функции завершаются; независимая программа это переиграла |
| `flang run` | ничего: вычислитель внутри двоичного не доказан |
| `flang emit --target c` и сборка | ничего: **никто не проверяет, что напечатанный C соответствует исходнику** |

Показано прогоном на разборе `п.flang`: `check --proof` даёт «доказано 1»,
независимая программа отвечает «НЕ ПРОВЕРЕНО — запись не противоречит исходнику,
но доказательством это не является», называя место на слове ядра. Разрыв
печатника при этом даже не участвует: он лежит ЗА пределами того, что запись
описывает вовсе.

## 2. Как с этим живут другие — проверено по источникам

| система | что с разрывом |
|---|---|
| **Coq** | извлечение в OCaml/Haskell **не доказано** и числится доверенным куском |
| **Lean 4** | ядро проверяет доказательства; компилятор, делающий исполняемый файл, — отдельная непроверенная программа |
| **Idris** | сборка через чужие рантаймы, не доказана |
| **CompCert** | разрыв ЗАКРЫТ: компилятор C доказан в Coq, теорема — собранный код ведёт себя как исходный. Годы работы команды |
| **CakeML** | разрыв закрыт до машинного кода, в HOL4. Тоже годы |

**Вывод: у распространённых средств разрыв такой же, как у нас.** Закрыли его
единицы, и очень дорого. Это не оправдание, а обстановка.

## 3. Два способа закрыть, и почему выбран второй

### 3.1. Доказать печатник целиком (путь CompCert)
Один раз доказывается теорема «для любой программы напечатанный код ведёт себя
как исходный». Сильнее всего и дороже всего: требует семантики исходного языка,
семантики цели и доказательства соответствия. Для десяти целей печати — десять
раз. **Отвергнуто как первый шаг:** цена несоразмерна, и до неё нужна логика,
которой у нас нет (ADR-0026 только принят).

### 3.2. Проверять КАЖДЫЙ ЗАПУСК (проверка перевода) — ПРИНЯТО
Печатник выдаёт не только код, но и **протокол перевода**: что во что перешло и
по какому правилу. Посторонняя программа берёт исходник, напечатанный код и
протокол — и сличает. Не сошлось — отказ.

**Почему это ложится на нас лучше всего: у нас УЖЕ такая схема.** Компилятор
печатает запись доказательства, независимая программа её переигрывает. Здесь та
же конструкция второй раз, только про перевод, а не про логику. Мы не изобретаем
подход, а применяем свой же.

Плата названа честно: доказывается не печатник, а конкретный перевод. Печатник
может оставаться кривым — но кривой перевод будет пойман на выходе, а не
разойдётся молча.

## 4. Решение

1. **Печатник печатает протокол перевода**, как ядро печатает запись
   доказательства.
2. **Заводится вторая независимая программа-сличитель**, которая переигрывает
   этот протокол. Она не видит печатника, как нынешняя не видит компилятора.
3. **К ней заводится набор подделок**, как у нынешней: подложный протокол,
   подменённая функция в напечатанном коде, потерянная ветвь, переставленные
   доводы. Ни одна не должна приниматься.
4. **Начинаем с ОДНОЙ цели — C.** Она несёт релиз и самосборку; прочие девять
   ждут, пока приём себя покажет.
5. **Доля перевода меряется отдельным прибором** и в долю доказательств НЕ
   смешивается: это разные утверждения, и сливать их в один процент значит врать.

## 5. Что мы теряем

- Печатник обязан печатать больше, и печать станет дороже — насколько, не
  измерено.
- Доверенная база растёт на вторую программу-сличитель. Её саму придётся
  стеречь подделками, как нынешнюю.
- Проверка перевода **не доказывает печатник**: назавтра он может напечатать
  другую чушь, и она будет поймана заново, но не предотвращена.
- Девять целей печати остаются без проверки на неопределённый срок.

## 6. Что отвергнуто

- **Доказать печатник целиком сейчас** — §3.1: цена несоразмерна, логики нет.
- **Считать, что сборка C ничего не портит** — это и есть нынешнее положение,
  и оно неявное. Решение делает его явным и временным.
- **Смешать долю перевода с долей доказательств** — разные утверждения.

## 7. Как проверить, что решение исполнено

1. Программа печатается в C, сличитель говорит «сошлось», подделка протокола —
   «не сошлось». Оба прогоном.
2. Подмена одной функции в напечатанном коде ловится. Прогоном.
3. Прибор доли перевода печатает своё число ОТДЕЛЬНО от доли доказательств.
4. Набор подделок сличителя под храповиком, как нынешний.

## 8. Где не уверен

- Форма протокола перевода не спроектирована вовсе.
- Не измерено, сколько правил перевода в печатнике C и сколько из них
  сличаемы дёшево.
- Неизвестно, справится ли сличитель с оптимизациями печати, если они есть.

## 9. Дешёвая победа, которую едва не упустили: CompCert закрывает разрыв НИЖЕ нас

CompCert доказывает переход **C → машинный код**; CakeML — **ML → машинный
код**. Наш разрыв другой: **flang → C**. Их теоремы к нашей паре языков не
прикладываются, и «взять их код» нельзя — там нет того, что нам нужно.

**Но обратное верно и полезно.** Сегодня после печати в C стоят ДВА места
доверия: наш печатник и чужой компилятор C. Если напечатанное собирать
CompCert, второе место закрывается его теоремой, и остаётся одно — наше.

Это не требует от нас ничего, кроме поддержки CompCert как компилятора C в
сборке. **Стоит проверить отдельно и дёшево**, не дожидаясь сличителя:
собирается ли напечатанный C под CompCert вовсе (он берёт подмножество C99 и
строг к расширениям). Если да — половина разрыва закрывается чужой работой.

Заведено задачей.

**Замер снят (задача 1402, 14 сентября 2026, CompCert 3.18 через opam, ствол
`bdce1f75`).** Напечатанный C **собирается**. Всё дерево — 302 программы, из них 255
печатаются в C, и 253 из 255 собирает и cc, и CompCert; две оставшиеся не собирает никто —
ошибка печатника (модуль «100 doors», задача 3984). Сам компилятор (35 МБ C) CompCert
собирает тоже. Собранное CompCert отвечает **байт в байт** как собранное `cc`: 11 475
примеров и 56 987 граничных вызовов на 248 программах, у компилятора — ведомости 157
файлов, AST 302 файлов, печать в четыре цели, `check --proof`. **Расхождений 0**, а
отрицательный контроль (cc с `-ffast-math`) расходится у 82 из 90 программ. Мешают не
печатник, а четыре кириллических имени в рантайме (`flang_runtime.c`, `flang_repl.c`) и
пять ключей ccomp (`-fstruct-passing -flongdouble -Wno-c11-extensions -Wno-unknown-pragmas
-Wno-literal-range`): без них не собирается ни одна программа (0 из 255). Оговорка к
«половине разрыва»: с `-fstruct-passing` замена структур по значению указателями делается
проходом `cparser/StructPassing.ml` (600 строк OCaml, двойника на Coq нет) **до**
проверенной части CompCert. Наш C передаёт `fl_value` по значению всюду, поэтому теорема
ложится только на то, что после этого прохода. Поддержка CompCert как выбираемого
компилятора — задача 3467.

## 10. Ход работы (задача 1401)

### Ш0. Замер до правки — 14 сентября 2026

Снято двоичным `b3216a8f` (flang 0.7.19, собран `make -C bootstrap` из
семени ствола на `25690222`), печатником `flang/self/emit-c.flang` того же
коммита.

| что | число | чем снято |
|---|---|---|
| меток места в напечатанном C (`#line`) | **0** | `grep -c '#line'` по выводу `emit --target c` на 47 примерах |
| где печатник читает место узла (`span`) | **нигде**: одно упоминание — обход, который `span` пропускает | `grep '"span"' flang/self/emit-c.flang` |
| узлов выражения у файла `flang/proof/examples/*.flang` | медиана **31** (от 6 до 234), всего **2647** на 47 файлов | `flang ast`, узлы с полями `kind` и `span` в телах и обещаниях, без примеров и типов |
| строк C модуля | медиана **196**, всего **12 163** | `emit --target c --no-check`, файл модуля без рантайма |
| строк C на узел | **4,60** по сумме, медиана **6,38** (от 3,00 до 15,50) | деление двух строк выше; в строки модуля входят шапка и диспетчер, поэтому у малых файлов на узел приходится больше |

Вывод замера: соответствия «узел → строки C» печатник не знает вовсе — ни
меток, ни чтения места. Протокол перевода приходится заводить с нуля, и место
каждого узла он берёт из `span` разбора, которое печатник до сих пор
выбрасывал. Правил печати в закрытом списке — **40**
(`flang/translation/PRINT-RULES.tsv`).

### Форма протокола перевода

Ключ — `flang emit --target c --протокол ФАЙЛ` (латиницей `--protocol`), только
у цели `c`. Без ключа печать зовётся прежней точкой входа, и C обязан выйти
байт в байт прежним. С ключом хозяин подаёт печати путь исходника и его sha256
(`«Напечатать связанное с протоколом»` в `flang/self/bootstrap/compiler.flang`),
печать кладёт в ответ файл `<модуль>.protocol`, хозяин пишет его по названному
пути.

Протокол — построчный текст:

```
протокол перевода 1
исходник <путь>
отпечаток256 <sha256 исходника>
модуль «<имя>»
файл <имя>.c
функция «Имя» …                                   ← блок функции
  узел <вид> строка L столбец C правило «имя»[ дополнение]
    … вложенные узлы в порядке печати …
    часть <метка> строк N
    <N строк C как напечатаны>
    значение <выражение C, которым узел отдан родителю>
  конец узла
конец узла
граница входа / план / шапка модуля              ← блоки модуля, частью «текст»
конец протокола
```

Вид узла — `kind` разбора, место — его `span`, правило — имя из закрытого
списка `flang/translation/PRINT-RULES.tsv`. Части и узлы идут в порядке
печати, и он совпадает с порядком текста C, кроме трёх раскладок, которые
сличитель знает по имени: часть `гашение` (снятие неиспользованных имён)
решается после тела, а стоит перед ним; у блока функции части `шапка`,
`гашение`, `пролог` стоят перед телом; `шапка модуля` печатается последней,
когда таблица констант полна, а стоит в файле первой.

Протокол ведёт то же «Состояние», что считает временные имена: два поля,
`«след»` и `«протокол»`. При выключенном следе каждая функция протокола отдаёт
состояние нетронутым — отсюда обещание «без ключа байт в байт».

### Ш1. Ключ у хозяина и печатник до перепечатки семени — 14 сентября 2026

Правка печатника в двоичный не попадёт до перепечатки семени, поэтому печать
проверена двумя обходными дорогами, обе названы в `docs/zettel/`:

* **хозяин** (`flang/src/emit/c/flang_repl.c`) — «горячей» сборкой: семя
  ствола плюс шапка и исходник хозяина, `make` в каталоге вне дерева;
* **печатник** (`flang/self/emit-c.flang`) — компилятором, напечатанным в
  JavaScript (`emit flang/self/bootstrap/compiler.flang --target js --no-check
  --max-steps 200000000 --runtime flang/src/emit/js`, около 10 минут и
  11–12 ГиБ один раз), дальше `node flang_cli.js ./compiler_flang.js` с
  вопросом `{"fn":"Печать в C от исходников с протоколом",…}`.

| проверка | итог |
|---|---|
| ключ на нынешнем семени | код 1 и слова «этот двоичный протокола перевода не печатает — семя ещё не перепечатано» |
| ключ у цели `js` / пустое имя файла | код 2, отказ словами |
| хозяин без ключа против обычного двоичного | **47 из 47** примеров `flang/proof/examples` — все файлы вывода совпали `cmp` |
| печать через JavaScript (печатник ствола) против обычного двоичного | **47 из 47** совпали `cmp` — дорога через JavaScript печатает тот же C |

В унаследованной правке найдено и исправлено четыре беды, каждая ловилась
только прогоном: хозяин не собирался (`EMIT_PROTOCOL_SUFFIX` стоял ниже
функции, которая его зовёт); две новые функции печатника совпали именем с
чужими — «Закрыть блок» с лексером, «Место узла» с проверкой типов (имена
функций едины на всё замыкание компилятора), третья — «Хвост после» — с
файлами вне замыкания; печать в Go строит то же «Состояние» пятьюдесятью
записями, и без двух новых полей замыкание не проходило проверку типов.
Печать в Go протокола не ведёт: поля в ней стоят мёртвыми, `нет` и `""`.

### Ш2. Протокол на 47 примерах `flang/proof/examples` — 14 сентября 2026

Дорогой через JavaScript (Ш1), печатник этой ветки:

| что | число |
|---|---|
| печать с протоколом | **47 из 47**, код 0 |
| печать без протокола против печатника ствола | **47 из 47** — все файлы совпали `cmp` |
| размер протокола | 1458137 байт на 664204 байт C модуля — в **2.20 раза** больше самого C: протокол несёт C целиком, по частям |

### Ш3. Сличитель — `flang/translation/matcher.c`

Один файл C99 (1636 строк), кроме libc ничего; печатника и flang не видит.
Переигрывает протокол так: SHA-256 исходника против шапки протокола; C,
собранный из частей протокола по раскладке правил, против файла байт в байт;
на месте каждого узла в исходнике стоит его слово (сняты замером слов на 47
примерах); каждое правило с сличением — по своей форме строк C, по значениям
детей; блоки и правила — только из закрытого списка; у каждой функции
исходника есть блок, у вызова — столько доводов, сколько параметров в
«принимает». Три исхода — «СОШЛОСЬ» (код 0), «НЕ ПРОВЕРЕНО» с перечнем
непереигранных правил (код 3), «НЕ СОШЛОСЬ» со строкой C или местом в исходнике
(код 1); неизвестное имя правила или блока — «ОТКАЗ» (код 1).

| честные пары, 47 примеров | число |
|---|---|
| «СОШЛОСЬ» | **14** |
| «НЕ ПРОВЕРЕНО» | **33** |
| «НЕ СОШЛОСЬ» | **0** |
| узлов переиграно правилом | **3349** |
| узлов принято на слово | **257**: «случай» 80, «литерал» 63 (строки с иными экранированиями, составные), «свёртка» 40, «разбор» 31, «операция-вызовом» 13 (операции через помощника, кроме равенства), «хвост-возврат» 10, «список» 8 (пустой), «отфильтровать» 4, «отобразить» 3, «арифметика-числом» 3 (mod), «хвост-если» 2 |

Что именно переигрывается у каждого правила и что принято на слово —
столбцы «сличение» и «доверие» `flang/translation/PRINT-RULES.tsv`; они
описывают сличитель как он есть, а не как задуман.

### Ш4. Подделки — `sh flang/translation/run.sh` (`./ярлык перевод:проверка`)

16 опытов, все сошлись с ожиданием: сверка закрытого списка (TSV против
`RULES[]`, с отрицательным контролем — подменённое имя в копии даёт красное);
две честные пары (код 0 и код 3 с названным непереигранным); тринадцать
подделок, каждая одной правкой — подменённый фрагмент протокола, изменённая
строка C при прежнем протоколе, пропущенный узел (место в исходнике названо),
протокол от другого исходника, подменённая функция в C и протоколе заодно,
потерянная ветвь «иначе», переставленные доводы, правило не из закрытого
списка, место узла сдвинуто на знак, замолчанная функция, оборванный протокол,
лишняя строка в C, выпавший довод (C и протокол заодно), подложный блок. Ни одна
не принята.

### Ш5. Покрытие узлов протоколом

Узел исходника покрыт, если в протоколе есть узел того же вида на том же месте
(вид и место — из `flang ast`; тройки «вид, строка, столбец» без повторов).

| набор | покрыто |
|---|---|
| `flang/proof/examples/*.flang`, 47 файлов | **2639 из 2643** (99,8 %) по виду; непокрыто — `apply` 4. Все четыре на своих местах стоят в протоколе вызовом «применить N»: дефункционализация меняет вид узла, а не место. По месту — **2643 из 2643** |

### Ш1б. Вторая дорога — толкование — дала тот же байт

Печать того же печатника толкованием исходников `flang/self` семенем ствола
(`bootstrap/flang run`, 11 мин 03 с, пик 19,8 ГиБ) на
`flang/proof/examples/traffic-light.flang` выдала `svetofor.c`,
`svetofor.h` и `svetofor.protocol`, совпавшие с печатью через JavaScript
знак в знак. Две независимые дороги до перепечатки согласны. Зонд в ствол не
положен: он новый файл `flang/self`, а число файлов там цитируют девять мест
прозы. Чтобы повторить — положить рядом с `flang/self/bootstrap/compiler.flang`
файл с текстом ниже и звать
`bootstrap/flang run <зонд> --function "«Печать пробы 1401»" --max-steps 2000000000 --args '{"путь": …, "текст": …, "отпечаток": …}'`:

```
модуль «Translation protocol probe»
  использует «Печать в C»
  использует «Compiler flang»
  использует «Связывание flang»

функция «Настройки пробы 1401»
  возвращает «Настройки»
  запись «Настройки» с «путь» равным "" и «есть путь» равным нет и «база» равным 1 и «предел глубины» равным 10000 и «предел шагов» равным 1000000 и «прогонщик» равным нет и «рантайм заголовок» равным "" и «рантайм исходник» равным "" и «исходник прогонщика» равным "" и «оболочка» равным нет и «исходник оболочки» равным "" и «типы входа» равным пустой список и «поля входа» равным пустой список и «варианты входа» равным пустой список и «параметры входа» равным пустой список и «планировщик заголовок» равным "" и «планировщик исходник» равным ""

тотальная функция «Файл пробы 1401»
  принимает «файл»: «Файл»
  возвращает строка
  соединить ["=====", («файл».«путь»), "=====\n", («файл».«содержимое»)] по ""

тотальная функция «Файл модуля пробы 1401»
  принимает «файл»: «Файл»
  возвращает признак
  (не ((«файл».«путь») начинается с "flang_")) и притом (не ((«файл».«путь») равен "Makefile"))

функция «Печать пробы 1401»
  принимает «путь»: строка, «текст»: строка, «отпечаток»: строка
  возвращает строка
  пусть «исходники» равно [(запись «Исходник» с «путь» равным «путь» и «текст» равным «текст»)]
  пусть «итог» равно («Печать в C от исходников с протоколом» от «исходники» и «путь» и («Настройки пробы 1401») и «путь» и «отпечаток»)
  соединить (отобразить (отфильтровать («итог».«файлы») где «файл» → («Файл модуля пробы 1401» от «файл»)) как «файл» → («Файл пробы 1401» от «файл»)) по ""
```
