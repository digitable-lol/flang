# Опись проверок: цель, код возврата, время, причина

Снято 25 августа 2026 на коммите `a3a18a3f` (ствол `b125519e` плюс взятие
задачи 0001), двоичным `flang 0.6.2`, собранным из семени этого же дерева
(`make -C bootstrap`). Это опись, а не починка: ничего по её ходу не чинилось
нарочно, чтобы список показывал то состояние, которое есть.

Целей в `ярлыки.flang` — 82. Каждая звалась как её зовёт человек:
`./ярлык <имя>`. Имена целей переписаны кириллицей 25 августа (задача 0030);
опись снята уже по новым именам.

## Чем эта опись отличается от «CI зелёный / CI красный»

Красное здесь двух разных пород, и их нельзя складывать:

* **красное по делу** — чинится работой над деревом;
* **красное из-за отставшего семени** — двоичный собран из `bootstrap/`, а
  `bootstrap/` не перепечатывали. Правка в `flang/self/**` и `flang/core/**`
  лежит в исходнике и в собранной программе не работает. Такое красное
  перепечаткой снимается, и работать над ним сейчас нечего.

Семя на момент описи отстало на **41 расхождение** (`./ярлык семя:быстро`):
40 файлов плюс строка о том, что печать зовётся иначе — было
«предел-шагов 1000000000», стало «предел-шагов 4000000000».

Двадцать целей из восьмидесяти двух объявлены в `ярлыки.flang` как «Ярлыки о
доказательствах». Перед ними стоит заслон `scripts/seed-freshness.sh`: пока
семя отстало, они **отказываются судить** и отвечают кодом 1, не запуская
проверки вовсе. Это не поломка, а заслон, поставленный нарочно, — иначе они
печатали бы зелёное про дерево, которого нет.

Поэтому каждая из этих двадцати снималась **дважды**:

* как её увидит человек и CI — с заслоном;
* с ключом обхода `SEMYA_OTSTALO_ZNAYU=1` — чтобы узнать, красна ли она **сама
  по себе** или только из-за семени.

Второй прогон — единственное, что отделяет «чинить руками» от «ждать
перепечатки», и ради него опись и снималась.

## Числа

| | сколько |
|---|---|
| целей в `ярлыки.flang` | 82 |
| зелёных (код 0) | 26 |
| красных | 48 |
| — красны ПО ДЕЛУ (чинятся работой над деревом) | 34 |
| — красны ИЗ-ЗА ОТСТАВШЕГО СЕМЕНИ (снимутся перепечаткой) | 9 |
| — красны ЗАСЛОНОМ, а сами по себе не установлены (с ключом обрыв) | 5 |
| не уложились в отведённый срок — состояние не установлено | 7 |
| не запускались | 1 |

Одна цель — `раскрутка` — не запускалась нарочно: это сама перепечатка семени,
она ПИШЕТ в `bootstrap/`, а задание прямо запрещает трогать `bootstrap/**`. На
соседней учётной записи в это время шла перепечатка, и второй такой прогон
машину бы уронил.

## Опись поимённо

Столбец «код» — что вернул `./ярлык <имя>`. Столбец «с ключом» заполнен только
у тех двадцати целей, перед которыми стоит заслон свежести семени: это их код
при `SEMYA_OTSTALO_ZNAYU=1`, то есть их настоящее состояние. «обрыв» — прогон
не уложился в отведённый ему срок и снят; в столбце времени назван сам срок.
Время — стенные секунды одного прогона, замок ворот в них входит.

| # | цель | код | время | с ключом | причина у красного |
|---|---|---|---|---|---|
| 1 | `сборка` | 0 | 1 с |  |  |
| 2 | `ярлыки` | 0 | 6 с |  |  |
| 3 | `лицензии:проверка` | 1 | 3 с |  | нет SPDX-заголовка в `flang/scripts/сличить-двух-сводителей.py` — 1 нарушение на 73 файла |
| 4 | `утверждения:проверка` | 1 | 3 с |  | 3 протухших утверждения о недостаче: `flang/cat/SPEC.md:1065`, `flang/self/parser.flang:5899` и `docs/ci-inventory.md:58` — прежняя редакция ЭТОЙ страницы, которая цитировала беду дословно и тем её удваивала. После переписывания страницы переспрошено: **2**, и эта страница в списке больше не числится |
| 5 | `подсчёты:проверка` | обрыв | 240 с, переспрос 659 с |  | не уложилась ни в 4 минуты, ни в 11 (обходит дерево; своего срока в строке ярлыка нет) |
| 6 | `коды:проверка` | 1 | 6 с |  | ПЕРЕСНЯТО 29 августа: сторож ПОБЕЖАЛ — предел обойдён задачей 9350, до того он не сверял ничего. Нарушений 5 → 2: остались две ПЕРЕМЕННЫЕ СРЕДЫ, принятые за коды отказа (`FLANG_ZAMER`, `FLANG_PROBE_MAX_DEPTH`) |
| 7 | `коды:подлог` | **0** | 36 с |  | ЗЕЛЁН с 29 августа: «сторож кодов краснеет на подложенном коде и молчит, когда его убрали; страница вернулась знак в знак» |
| 8 | `печать:проверка` | 3 | 2 с |  | FLANG_PROPERTY — нарушено собственное свойство сторожа «программа улика: длина постоянной» |
| 9 | `октет:проверка` | 3 | 2 с |  | `ярлыки.flang` зовёт bash-скрипт через `sh`; в dash `${BASH_SOURCE[0]}` — «Bad substitution», путь собирается как `//bootstrap/flang` |
| 10 | `октет:порча` | 3 | 1 с |  | то же: bash-скрипт запущен через `sh` |
| 11 | `умения:проверка` | 1 | 2 с | 1 | СЕМЯ: с ключом «ЯДРО отстало — якоря «прибавление к ГРАНИЦЕ…» в `proof-kernel.flang` нет», разошлось мест 1 |
| 12 | `умения:улики` | 1 | 2 с | 1 | СЕМЯ: с ключом FLANG_UNKNOWN_NAME — старый двоичный не знает типа «неотрицательное» в `flang/proof/map/abilities.flang` |
| 13 | `закон:проверка` | 1 | 71 с |  | суд убит сигналом SIG9 на `flang/stdlib/datetime.flang` — вердикта нет |
| 14 | `имена:проверка` | 1 | 1048 с |  | 870 НОВЫХ нарушений имён; всего 16930 мест долга в 212 файлах из 282 просмотренных |
| 15 | `память:проверка` | 0 | 12 с |  |  |
| 16 | `правила:проверка` | 1 | 5 с |  | нарушений 5: две печати без имени поверхности, одна поверхность без улики, две протухшие записи (FLANG_INTERNAL, FLANG_STEP_LIMIT) |
| 17 | `узел:цели` | 1 | 122 с |  | полный узел работает на 7 целях из 8: `elixir` — ответ с узла опоры не поменял состояния «Счётчика» |
| 18 | `времянки:проверка` | 3 | 3 с |  | FLANG_UNKNOWN_PLAN: планов в файле два («Временных каталогов не осталось», «Проба»), а `--plan` в строке ярлыка не назван |
| 19 | `занятые:проверка` | 1 | 15 с |  | FLANG_ZANYATOE_NECHEM: прогонщик ответил 0 раз на 1 вопрос о цели c — `output/storozh-zanyatogo.json` нет |
| 20 | `слова:проверка` | 1 | 17 с |  | бед 16: подделка занятых слов не печатается ни в c, ни в go, ни в rust, ни в java; модули `System` и `StringBuilder` собираются, хотя имена заняты словом цели |
| 21 | `подделки:проверка` | 1 | 3 с | 1 | СЕМЯ: с ключом нарушений 44, и 42 из них — «правило … НЕ РАБОТАЕТ»: честные половины правил ядра есть в исходнике и отсутствуют в семени |
| 22 | `запись:проверка` | 1 | 2 с | обрыв 600 с | с заслоном код 1; с ключом не уложилась в 10 минут — собственное состояние не установлено |
| 23 | `сведение:проверка` | 1 | 2 с | обрыв 600 с | с заслоном код 1; с ключом не уложилась в 10 минут — собственное состояние не установлено |
| 24 | `срок:проверка` | 0 | 68 с |  |  |
| 25 | `связь:печать` | 0 | 11 с |  |  |
| 26 | `выпуск:проверка` | 0 | 4 с |  |  |
| 27 | `столкновения:проверка` | 1 | 5 с |  | столкновений 52 на замыкании из 905 файлов: одно имя объявлено в двух модулях библиотеки («Цифра 16», «Степень», «Ход поиска», «Шаг поиска» и другие) |
| 28 | `семя:быстро` | 1 | 2 с |  | СЕМЯ: расхождений 41 — это и есть его работа, он краснеет ровно об отставшем семени |
| 29 | `столкновения:пересчёт` | обрыв | 240 с, переспрос 780 с |  | не уложилась ни в 4 минуты, ни в 13 (её собственная шапка обещает тридцать) |
| 30 | `сращивание:проверка` | 3 | 6 с |  | FLANG_PROPERTY — нарушено собственное свойство сторожа «путь к файлу долга — тридцать семь знаков» |
| 31 | `поиск:различающий` | 1 | 2 с | 0 | СЕМЯ: с ключом ЗЕЛЕНА — красна только заслоном |
| 32 | `сколько-верим` | 1 | 2 с | 1 | потолок пробит: ядро решений было 3372 строк, стало 4461 (+1089); достижимо было 5967, стало 7428 (+1461) |
| 33 | `спеки:проверка` | 1 | 2 с | 1 | СЕМЯ: с ключом FLANG_UNKNOWN_NAME — старый двоичный не знает типа «неотрицательное» |
| 34 | `спеки:подлог` | 1 | 3 с | 1 | СЕМЯ: с ключом расхождений 2 из 14 подложенных, и на чистом дереве «бед в системе спек: 30» — те же FLANG_UNKNOWN_NAME от старого двоичного |
| 35 | `доказательства:ведомость` | 1 | 2 с | обрыв 600 с | с заслоном код 1; с ключом не уложилась в 10 минут — собственное состояние не установлено |
| 36 | `доказательства:свод` | 1 | 2 с | обрыв 600 с | с заслоном код 1; с ключом не уложилась в 10 минут — собственное состояние не установлено |
| 37 | `слово:проверка` | 1 | 2 с | 1 | нарушений 3: долги ядра закрыты, а записи о них остались в каталоге |
| 38 | `слово:подлог` | 1 | 2 с | 1 | СЕМЯ: не сработала 1 порча из-за того, что ведомость не сложилась — FLANG_UNKNOWN_NAME от старого двоичного |
| 39 | `доказательства:пустота` | 1 | 2 с | обрыв 600 с | с заслоном код 1; с ключом не уложилась в 10 минут — собственное состояние не установлено |
| 40 | `пустота:проверка` | 1 | 2 с | 1 | 14 файлов библиотеки не названы ни в «Наших», ни в «Отложенных»: aes, automaton, crl, der, ecdsa, math-classics, math-classics-lists, registry, rsa, sqlite, tls, x25519, x509, образцы |
| 41 | `пустота:перепись` | 1 | 2 с | 1 | то же: те же 14 файлов библиотеки не расписаны по спискам |
| 42 | `пустота:порча` | 1 | 2 с | 1 | краснеет вслед за `пустота:проверка` — те же 14 нерасписанных файлов |
| 43 | `доказанное:проверка` | 1 | 2 с | 3 | FLANG_PROPERTY — нарушено собственное свойство сторожа «длина имени файла пола» функции «Файл пола» |
| 44 | `доказанное:порча` | 1 | 2 с | 1 | краснеет вслед за `доказанное:проверка`: то же нарушенное свойство «длина имени файла пола» |
| 45 | `доказательства:20` | 1 | 3 с | 0 | СЕМЯ: с ключом ЗЕЛЕНА — красна только заслоном |
| 46 | `слово:занятость` | 2 | 1 с |  | это не проверка, а измеритель: без довода-слова отвечает подсказкой и кодом 2 |
| 47 | `слово:занятость:проверка` | 0 | 79 с |  |  |
| 48 | `ссылки:проверка` | 0 | 29 с |  |  |
| 49 | `указатель:печать` | 0 | 10 с |  |  |
| 50 | `путь:проверка` | 0 | 3 с |  |  |
| 51 | `словарь:полнота` | 0 | 5 с |  |  |
| 52 | `вкладка:проверка` | 1 | 33 с |  | хозяин вкладки отстал от словаря: из 20 поручений 4 без ветки (чтение октетов, запись октетов, удаление файла, временный каталог) |
| 53 | `браузер:собрать` | 0 | 4 с |  |  |
| 54 | `браузер:стенд` | обрыв | 100 с |  | это не проверка, а стенд: поднимает сервер на 127.0.0.1:8908 и не завершается — обрыв по построению |
| 55 | `браузер:проба` | 2 | 2 с |  | без `TMPDIR` отказывается начинать. С `TMPDIR=<каталог>` — код 1 за 42 с: Chrome/151 поднялся, но на экране не дождались «набор:» |
| 56 | `справка:проверка` | 0 | 5 с |  |  |
| 57 | `дарвин:проверка` | 0 | 2 с |  |  |
| 58 | `формула:проверка` | 1 | 6 с |  | архива `flang-0.6.2-c.tar.gz` нет ни в `output/`, ни рядом — sha256 формулы Homebrew сверить нечем |
| 59 | `установка:npm` | 1 | 74 с |  | шаг 13: `flang emit primer.flang --target js` из поставленного пакета вернул код 2, а ждали 0 |
| 60 | `сайт` | 0 | 13 с |  |  |
| 61 | `сайт:проверка` | 0 | 13 с |  |  |
| 62 | `контраст:проверка` | 0 | 6 с |  |  |
| 63 | `словарь` | 0 | 6 с |  |  |
| 64 | `словарь:проверка` | 0 | 7 с |  |  |
| 65 | `поверхности:прогон` | 0 | 2 с |  |  |
| 66 | `поверхности:проверка` | 0 | 2 с |  |  |
| 67 | `поверхности:подлог` | 0 | 3 с |  |  |
| 68 | `числа` | обрыв | 241 с |  | не уложилась в 4 минуты (своего срока в строке ярлыка нет) |
| 69 | `числа:проверка` | обрыв | 240 с |  | не уложилась в 4 минуты (своего срока в строке ярлыка нет) |
| 70 | `журнал` | 0 | 3 с |  |  |
| 71 | `журнал:проверка` | 1 | 2 с |  | правило видимости не знает путей: `.rabdir`, `.reader-pages.txt`, `.sborka.kod`, `.tmpdir`, `ROADMAP.md`, `bootstrap-vysokiy`, `flang/examples`, `tasks`, `ярлык`, `ярлыки.flang` и ещё четыре |
| 72 | `журнал:страница` | 0 | 11 с |  |  |
| 73 | `журнал:страница:проверка` | 1 | 2 с |  | `changelog.md` не совпадает с печатью из `changelog.json` — страницу правили руками |
| 74 | `выпуски:страница` | 3 | 16 с |  | FLANG_RECURSION_LIMIT: «Шаг разметки json» исчерпала 10000000 шагов на глубине вызовов 4 |
| 75 | `выпуски:страница:проверка` | 3 | 16 с |  | то же: FLANG_RECURSION_LIMIT в «Шаг разметки json» |
| 76 | `раскрутка` | не гоняли |  |  | ЗАПУСКАТЬ БЫЛО НЕЛЬЗЯ: это сама перепечатка семени, она ПИШЕТ в `bootstrap/`; задание запрещает трогать `bootstrap/**`, и на соседней учётной записи в это время шла перепечатка |
| 77 | `раскрутка:проверка` | обрыв | 301 с |  | не уложилась в 5 минут: печатает точку раскрутки целиком, замер в её же шапке — 18 м 50 с и пик 24,1 ГиБ на одной цели C |
| 78 | `точка:проверка` | обрыв | 300 с |  | не уложилась в 5 минут: то же самое другим путём — печать двоичным, по шапке 18 минут и 24 ГиБ |
| 79 | `строки:проверка` | 0 | 2 с |  |  |
| 80 | `сторожа:проверка` | 0 | 4 с |  |  |
| 81 | `тесты` | 1 | 208 с |  | 31 проверка прошла, 7 провалено — все семь в стороже жаргона: «ledger» и «ведомост» в `README.md` и `README.ru.md`, «эталон» и «побайтов» в `docs/site/browser-app.ru.md`, «byte-for-byte» в `docs/site/browser-app.md` и далее по страницам сайта |
| 82 | `тесты:по-ssh` | 1 | 1 с |  | не задан `FLANG_REMOTE` — нужна чужая машина с восемью тулчейнами и доступом по ключу |

## Красное поимённо по породам

**Красны из-за отставшего семени — 9.** Перепечатка (`раскрутка`, задача 0003)
снимает их без единой правки в дереве:

`умения:проверка`, `умения:улики`, `подделки:проверка`, `семя:быстро`,
`поиск:различающий`, `спеки:проверка`, `спеки:подлог`, `слово:подлог`,
`доказательства:20`.

Из них две — `поиск:различающий` и `доказательства:20` — с ключом обхода
**зелены**: они красны ровно заслоном и больше ничем. Остальные семь с ключом
тоже красны, но текст отказа называет виновником старый двоичный: то якоря
правила ядра в `proof-kernel.flang` нет, то FLANG_UNKNOWN_NAME на типе
«неотрицательное», которого семя не знает.

**Красны заслоном, а сами по себе не установлены — 5.** С ключом обхода каждая
не уложилась в десять минут:

`запись:проверка`, `сведение:проверка`, `доказательства:ведомость`,
`доказательства:свод`, `доказательства:пустота`.

**Красны по делу — 34.** Это те, что чинятся работой над деревом. Три из них
красны не о дереве, и это стоит знать до того, как браться:

* `слово:занятость` (код 2) — это измеритель, а не проверка: без довода-слова
  он и обязан отвечать подсказкой;
* `тесты:по-ssh` — нужна чужая машина с восемью тулчейнами и доступом по ключу;
* `браузер:проба` — нужен `TMPDIR` и живой Chrome.

## Что чинить первым

**`коды:подлог` починен 29 августа 2026 и больше не первый в этом списке.**
Прежний диагноз — «он единственный красен тем, что НЕ ЗАМЕТИЛ подлога, молчит
там, где обязан кричать» — оказался неверен, и вот чем.

Сторож кодов не молчал: он не добирался до просмотра ВОВСЕ. Вывод `git grep`
перерастал предел хозяина в 1 МиБ, и сторож отвечал отказом «смотреть нечем».
Проверка подлога видела ответ без подложенного имени и читала это как молчание.
Предел обойдён задачей 9350 — сторож спрашивает имена кодов, а не строки, — и
первая половина подлога сразу стала проходить.

Вторая половина после этого краснела уже по своей причине, и опять не по вине
сторожа. Сняв подлог, проверка требует, чтобы имя исчезло из ответа, а ищет его
простым вхождением по ВСЕМУ ответу. Имя же было названо прозой ЭТОЙ САМОЙ
СТРАНИЦЫ — в строках, где описан отказ. Сторож честно называл его и на чистом
дереве, с местом. Круг замкнулся: страница, заведённая описывать красноту
сторожа, эту красноту и держала.

Круг разорван: имена, которых сторож не видит по устройству, проза больше не
называет голым словом. Прогон — код 0.

**Что осталось хрупким.** Проверка подлога по-прежнему сверяет голое имя по
всему ответу: любое будущее упоминание подкладываемого кода в любой странице
снова сделает её красной, и снова с ложным доводом «сторож смотрит в свою
память». Чинить надо сверку — сличать имя ВМЕСТЕ С МЕСТОМ подлога; разделитель
измерен: на чистом дереве `docs/what-blocks-1-0.md` в ответе сторожа не
встречается ни разу, с подлогом встречается.

Дальше по убыванию отдачи:

1. **Три цели красны одной причиной** — `пустота:проверка`, `пустота:перепись`,
   `пустота:порча`: четырнадцать файлов библиотеки не расписаны ни в «Наших»,
   ни в «Отложенных». Одна правка списка снимает три красных. Их было семь на
   прошлой описи — список отстаёт быстрее, чем его правят.
2. **Две цели красны одной строкой** — `октет:проверка` и `октет:порча`:
   `ярлыки.flang` зовёт `sh scripts/bad-octet-guard.sh`, а у скрипта
   `#!/usr/bin/env bash` и `${BASH_SOURCE[0]}` внутри. `/bin/sh` здесь — dash,
   и он отвечает «Bad substitution». Правится словом `bash` вместо `sh` в
   строке ярлыка.
3. **Четыре цели красны собственным нарушенным свойством**, а не тем, что они
   стерегут: `печать:проверка`, `сращивание:проверка`, `доказанное:проверка` и
   вслед за ним `доказанное:порча`. FLANG_PROPERTY — это отказ самой программы
   сторожа, до всякой проверки дерева.
4. **Семь проверок из `тесты` — жаргон**: все семь об одном, и правится это
   прозой, а не кодом.
5. **Семь целей не уложились в срок**, и у пяти из них нет своего `--timeout` в
   строке ярлыка, хотя их собственные шапки называют минуты: `подсчёты:проверка`,
   `столкновения:пересчёт` (шапка обещает тридцать минут), `числа`,
   `числа:проверка`, а также дорогие `раскрутка:проверка` и `точка:проверка`.
   Пока срока нет, они в CI либо висят, либо режутся чужим сроком.

## Чем прогон был ограничен, чтобы числа читались правильно

* Разом шло **не больше двух** прогонов: на соседней учётной записи шла
  перепечатка семени, и мешать ей было нельзя.
* Каждый прогон — через `PAMYAT=45G CHISLO=2 POROG=60G
  /srv/flang-rabota/vorota/flang-vorota`. Свободной памяти на машине всё время
  было 350–380 ГиБ, очереди у ворот не возникало: замок брался сразу, и время
  в таблице — время самой цели.
* Срок на цель: 4 минуты в основном заходе, 10 минут в переспросе с ключом,
  5 минут у двух дорогих печатей. Цель, не уложившаяся в него, помечена
  «обрыв» — это НЕ приговор проверке, это значит, что состояние не установлено.
  Двум обрывам срок потом добавили — `подсчёты:проверка` не уложилась и в 11
  минут, `столкновения:пересчёт` не уложилась и в 13.
* Дерево после каждой цели возвращалось к чистому (`git checkout --`), чтобы
  печатающий ярлык (`сайт`, `числа`, `журнал`, `словарь`, `указатель:печать`)
  не зеленил идущую следом сверку своей же печатью.

### Ворота ломают три цели, и это НЕ их беда

Ворота ставят `ulimit -v` (RLIMIT_AS). Для flang это безопасно, для чужой
оснастки — нет, и в шапке самих ворот так и написано: «Для НИХ такой предел
ставить нельзя — сборка упадёт на ровном месте». Замерено здесь же:

* `узел:цели` под воротами — «работает на 6 целях из 8», csharp падает с
  «Out of memory» от Roslyn; **без ворот — 7 из 8**, и не сходится только elixir;
* `слова:проверка` под воротами — бед 18 (четыре из них — тот же Roslyn);
  **без ворот — бед 16**;
* `тесты` под воротами — 28 прошло / 10 провалено, три лишних падения на csharp;
  **без ворот — 31 / 7**, и все семь в стороже жаргона.

В таблице у этих трёх стоят числа **без ворот**: под воротами они мерят предел
адресного пространства, а не себя.

## Рядом с описью: `ярлыки.flang` и то, что гоняет GitHub, — разные списки

Считано по `.github/workflows/*.yml` того же коммита (`binary.yml`,
`build-macos.yml`, `ci.yml`, `install-path.yml`, `pages.yml`,
`publish-npm.yml`):

* **Именем `./ярлык <цель>` workflow не зовёт ни одной цели.** Четыре
  упоминания ярлыков в файлах workflow — это текст в комментариях
  (`сторожа:проверка`, `установка:npm`, `дарвин:проверка`, `формула:проверка`),
  а не вызов.
* Зовутся те же файлы напрямую: у **28 целей из 82** файл-исполнитель из строки
  ярлыка упомянут хоть в одном workflow. У остальных 54 — нет.
* **Заслон свежести семени в workflow не стоит.** Он живёт в оболочке `ярлык`, а
  workflow зовёт сторожей напрямую. То есть двадцать целей, которые рукой
  сегодня отказываются судить, в GitHub судят молча — ответом старого семени.

## Что осталось недоснятым и почему

Недоснятыми остались семь «обрывов» и одна незапускавшаяся цель — каждая
названа в таблице своей строкой со своим сроком. Срок на всю опись был 150
минут; растянуть его на дорогие печати (`раскрутка:проверка` — 19 минут,
`точка:проверка` — 18 минут, `установка:npm` — свой срок 40 минут) значило бы
не снять остальные восемьдесят.
