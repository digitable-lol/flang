# Опись проверок: цель, код возврата, причина

Снято 24 августа 2026 на коммите `ed580f56`, двоичным `flang 0.6.2`, собранным
из семени этого же дерева (`make -C bootstrap`). Это опись, а не починка: ничего
по её ходу не чинилось нарочно, чтобы список показывал то состояние, которое
есть.

Целей в `ярлыки.flang` — 82. Каждая звалась как её зовёт человек: `./ярлык <имя>`.

## Чем эта опись отличается от «CI зелёный / CI красный»

Красное здесь двух разных пород, и их нельзя складывать:

* **красное само по себе** — чинится работой над деревом;
* **красное из-за отставшего семени** — двоичный собран из `bootstrap/`, а
  `bootstrap/` не перепечатывали. Правка в `flang/self/**` и `flang/core/**`
  лежит в исходнике и в собранной программе не работает. Такое красное
  перепечаткой снимается, и работать над ним сейчас нечего.

Семя на момент описи отстало на **38 файлов** (`sh scripts/raskrutka.sh --bystro`,
«расхождений 39» — 38 файлов плюс строка о том, что печать зовётся иначе:
было «предел-шагов 1000000000», стало «предел-шагов 4000000000»).

Двадцать целей из восьмидесяти двух объявлены в `ярлыки.flang` как «Ярлыки о
доказательствах». Перед ними стоит заслон `scripts/svezhest-semeni.sh`: пока
семя отстало, они **отказываются судить** и отвечают кодом 1, не запуская
проверки вовсе. Это не поломка, а заслон, поставленный нарочно, — иначе они
печатали бы зелёное про дерево, которого нет.

Поэтому каждая из этих двадцати снималась **дважды**:

* как её увидит человек и CI — с заслоном;
* с ключом обхода `SEMYA_OTSTALO_ZNAYU=1` — чтобы узнать, красна ли она **сама
  по себе** или только из-за семени.

Второй прогон — единственное, что отделяет «чинить руками» от «ждать
перепечатки», и ради него опись и снималась.

## Числа

| | сколько |
|---|---|
| целей в `ярлыки.flang` | 82 |
| зелёных (код 0) | 25 |
| красных | 40 |
| — красны САМИ ПО СЕБЕ (чинятся работой) | 29 |
| — красны ИЗ-ЗА ОТСТАВШЕГО СЕМЕНИ (снимутся перепечаткой) | 11 |
| не уложились в отведённый срок — состояние не установлено | 12 |
| не запускались | 5 |

## Опись поимённо

Столбец «код» — что вернул `./ярлык <имя>`. Столбец «с ключом» заполнен только
у тех двадцати целей, перед которыми стоит заслон свежести семени: это их код
при `SEMYA_OTSTALO_ZNAYU=1`, то есть их настоящее состояние. «обрыв» — прогон
не уложился в отведённый ему срок и снят.

| # | цель | код | с ключом | причина у красного |
|---|---|---|---|---|
| 1 | `сборка` | 0 |  |  |
| 2 | `ярлыки` | 0 |  |  |
| 3 | `license:check` | 1 |  | нет SPDX-заголовка в `flang/scripts/сличить-двух-сводителей.py` — 1 нарушение на 73 файла |
| 4 | `claims:check` | 1 |  | проза уверяет, что формы «символ по коду» в языке нет, а лексер отдаёт её ключевым словом |
| 5 | `counts:check` | обрыв |  | не уложилась за 4 минуты (обходит дерево; своего срока в команде нет) |
| 6 | `codes:check` | 0 |  |  |
| 7 | `codes:podlog` | 1 |  | сторож не запустился: двоичный напечатал больше 1 МиБ, и подлог читать было нечем |
| 8 | `pechat:check` | 3 |  | FLANG_PROPERTY — нарушено собственное свойство сторожа «программа улика: длина постоянной» |
| 9 | `oktet:check` | 3 |  | `ярлыки.flang` зовёт bash-скрипт через `sh`; в dash `${BASH_SOURCE[0]}` — «Bad substitution», путь к двоичному собирается как `//bootstrap/flang` |
| 10 | `oktet:porcha` | 3 |  | то же: bash-скрипт запущен через `sh` |
| 11 | `umeniya:check` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 12 | `umeniya:uliki` | 1 | 0 | с ключом ЗЕЛЕНА: 28 утверждений из 28 доказаны, сетка 0 — красна только заслоном |
| 13 | `zakon:check` | 1 |  | суд убит SIG9 на `flang/stdlib/datetime.flang` — вердикта нет. Не предел памяти: тот же ответ и при 40 ГиБ, и при 99 ГиБ |
| 14 | `names:check` | обрыв |  | не уложилась за 4 минуты (обходит все программы дерева; своего срока в команде нет) |
| 15 | `memory:check` | 0 |  |  |
| 16 | `rules:check` | 1 |  | 5 протухших записей: FLANG_INTERNAL, FLANG_STEP_LIMIT и ещё три числятся за двоичным, а в его замыкании их нет |
| 17 | `uzel:celi` | 1 |  | цель elixir не собралась: печать узла даёт предупреждения-ошибки в напечатанном коде цели elixir |
| 18 | `tmp:check` | 3 |  | планов в файле два («Временных каталогов не осталось», «Проба»), а `--plan` в команде ярлыка не назван — FLANG_UNKNOWN_PLAN |
| 19 | `occupied:check` | 0 |  |  |
| 20 | `slova:check` | 1 |  | 4 беды: напечатанные модули `Class`, `Def` (python) и `False` (elixir) собираются, хотя имена заняты словом цели |
| 21 | `poddelki:check` | 1 | 1 | нарушений 27 — все на честных половинах правил ядра, которых нет в семени: подделка при неработающем правиле ничего не меряет |
| 22 | `zapis:check` | 1 | обрыв | с заслоном код 1; с ключом не уложилась за 15 минут — состояние не установлено |
| 23 | `svedenie:check` | 1 | обрыв | с заслоном код 1; с ключом не уложилась за 15 минут — состояние не установлено |
| 24 | `srok:check` | 0 |  |  |
| 25 | `svyaz:pechat` | 1 |  | 2 расхождения: `flang/conc/svyaz.js` и `flang/conc/planirovshchik.js` разошлись со своими исходниками на flang |
| 26 | `release:check` | 0 |  |  |
| 27 | `collisions:check` | 1 |  | одно имя объявлено в двух модулях: «Шаг поиска» в `flang/stdlib/tls.flang` и `flang/stdlib/образцы.flang` |
| 28 | `semya:bystro` | 1 |  | семя отстало на 38 файлов — это и есть его работа: он краснеет ровно об этом |
| 29 | `stolknoveniya:check` | обрыв |  | не уложилась за 4 минуты (её собственная шапка обещает тридцать) |
| 30 | `srashchivanie:check` | обрыв |  | не уложилась за 4 минуты |
| 31 | `poisk:razlichitelnyy` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 32 | `verit:skolku` | 1 | 1 | потолок пробит: ядро решений было 3372 строк, стало 4357 (+985); достижимо было 5967, стало 7324 (+1357) |
| 33 | `spec:check` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 34 | `spec:podlog` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 35 | `proof:ledger` | 1 | обрыв | с заслоном код 1; с ключом не уложилась за 15 минут — состояние не установлено |
| 36 | `proof:svod` | 1 | обрыв | с заслоном код 1; с ключом не уложилась за 15 минут — состояние не установлено |
| 37 | `slovo:check` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном (сторож слов починен сегодня, 06b06f8a) |
| 38 | `slovo:podlog` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 39 | `proof:pustota` | 1 | обрыв | с заслоном код 1; с ключом не уложилась за 15 минут — состояние не установлено |
| 40 | `pustota:check` | 1 | 1 | 7 файлов библиотеки не названы ни в «Наших», ни в «Отложенных»: reestr, rsa, sqlite, tls, x25519, x509, образцы |
| 41 | `pustota:perepis` | 1 | 1 | то же: те же 7 файлов библиотеки не расписаны по спискам |
| 42 | `pustota:porcha` | 1 | 1 | краснеет вслед за `pustota:check` — те же 7 нерасписанных файлов |
| 43 | `dokazannoe:check` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 44 | `dokazannoe:porcha` | 1 | 1 | порча НЕ замечена: сторож ответил кодом 0, а обязан был 1 — сторож не ловит того, ради чего заведён |
| 45 | `proof:20` | 1 | 0 | с ключом ЗЕЛЕНА — красна только заслоном |
| 46 | `word:occupancy` | 2 |  | это не проверка, а измеритель: без довода-слова отвечает подсказкой и кодом 2 |
| 47 | `word:occupancy:check` | 0 |  |  |
| 48 | `links:check` | 0 |  |  |
| 49 | `ukazatel:pechat` | 0 |  |  |
| 50 | `path:check` | 0 |  |  |
| 51 | `slovar:check` | 0 |  |  |
| 52 | `vkladka:check` | 1 |  | хозяин вкладки отстал от словаря: из 20 поручений 4 без ветки (чтение, запись, удаление файла, временный каталог) |
| 53 | `web:sobrat` | 0 |  |  |
| 54 | `web:stend` | обрыв |  | это не проверка, а стенд: поднимает сервер на 127.0.0.1:8908 и не завершается — 4 минуты, обрыв по сроку |
| 55 | `web:proba` | не гоняли |  | не запускалась: поднимает настоящий Chrome; прогоны остановлены до конца перепечатки семени |
| 56 | `manpage:check` | 0 |  |  |
| 57 | `darwin:check` | 0 |  |  |
| 58 | `formula:check` | 1 |  | архива `flang-0.6.2-c.tar.gz` нет ни в `output/`, ни рядом — sha256 формулы Homebrew сверить нечем |
| 59 | `ustanovka:npm` | не гоняли |  | дорогая: собирает тарбол, ставит его в пустой каталог и СВЕРХ ТОГО собирает вторую установку способом brew (`make` целиком), затем гоняет обе по одному списку вызовов. Свой срок в команде — 40 минут. |
| 60 | `site` | 0 |  |  |
| 61 | `site:check` | 0 |  |  |
| 62 | `kontrast:check` | 0 |  |  |
| 63 | `glossary` | 0 |  |  |
| 64 | `glossary:check` | 0 |  |  |
| 65 | `surfaces:run` | 0 |  |  |
| 66 | `surfaces:check` | 1 |  | числа страницы разошлись с прогоном: не хватает строк про `examples/surfaces/factorial.eo.flang` (3128) и `examples/surfaces/factorial.zh.flang` (3083) |
| 67 | `surfaces:podlog` | 1 |  | краснеет вслед за `surfaces:check`: после отката страницы сторож обязан молчать, а отвечает кодом 1 |
| 68 | `numbers` | обрыв |  | не уложилась за 4 минуты |
| 69 | `numbers:check` | обрыв |  | не уложилась за 4 минуты |
| 70 | `changelog` | 0 |  |  |
| 71 | `changelog:check` | 1 |  | журнал отстал от истории на 258 коммитов |
| 72 | `changelog:page` | 0 |  |  |
| 73 | `changelog:page:check` | 1 |  | страница вливаний отстала от истории |
| 74 | `releases:page` | 3 |  | FLANG_RECURSION_LIMIT: «Одиночный знак json» исчерпала 10000000 шагов на глубине 5 |
| 75 | `releases:page:check` | 3 |  | то же: FLANG_RECURSION_LIMIT в «Одиночный знак json» |
| 76 | `bootstrap` | не гоняли |  | ЗАПУСКАТЬ БЫЛО НЕЛЬЗЯ: это сама перепечатка семени, она ПИШЕТ в `bootstrap/`. Последняя попытка 24 августа: 3 часа 02 минуты, пик 227 ГиБ, код 1 (FLANG_RECURSION_LIMIT — «Суд ядра о программе» съел 4000000001 виток из 4000000000). Семя от неё не обновилось. |
| 77 | `bootstrap:check` | не гоняли |  | дорогая: печатает точку раскрутки целиком, чтобы сверить. Замер в её же шапке — 18 м 50 с и пик 24,1 ГиБ на одной цели C. |
| 78 | `tochka:check` | не гоняли |  | дорогая, то же самое другим путём: печать двоичным, 18 минут и 24 ГиБ. |
| 79 | `stroki:check` | 0 |  |  |
| 80 | `storozha:check` | 0 |  |  |
| 81 | `тесты` | 1 |  | 31 проверка прошла, 7 провалено — все семь в стороже жаргона: «ledger» и «ведомость» на пяти страницах (`docs/site/cli.md`, `docs/site/cli.ru.md`, `docs/site/kak-dokazat.md`, `docs/site/kak-dokazat.ru.md`, `packaging/flang.1`) |
| 82 | `test:remote` | 1 |  | не задан `FLANG_REMOTE` — нужна чужая машина с восемью тулчейнами и доступом по ключу |

## Что чинить первым

Если чинить только одно — **`dokazannoe:porcha`**. Он единственный красен тем,
что НЕ ЗАМЕТИЛ подлога: «сторож ответил кодом 0, а обязан был 1». Остальные
тридцать девять красных честно называют беду; этот молчит там, где обязан
кричать, а сторож, который не ловит того, ради чего заведён, хуже отсутствующего
— на него смотрят и успокаиваются.

Дальше по убыванию отдачи:

1. **Три цели красны одной причиной** — `pustota:check`, `pustota:perepis`,
   `pustota:porcha`: семь файлов библиотеки (`reestr`, `rsa`, `sqlite`, `tls`,
   `x25519`, `x509`, `образцы`) не расписаны ни в «Наших», ни в «Отложенных».
   Одна правка списка снимает три красных.
2. **Две цели красны одной строкой** — `oktet:check` и `oktet:porcha`:
   `ярлыки.flang` зовёт `sh scripts/storozh-negodnogo-okteta.sh`, а у скрипта
   `#!/usr/bin/env bash` и `${BASH_SOURCE[0]}` внутри. `/bin/sh` здесь — dash,
   и он отвечает «Bad substitution». Правится словом `bash` вместо `sh` в строке
   ярлыка. То же место стоит проверить у `scripts/odinakovye-obyavleniya.sh` и
   `scripts/stolknoveniya-celey.sh` — там тот же `BASH_SOURCE`.
3. **Семь целей — жаргон**: `тесты` красны семью проверками, и все семь об одном
   — слова «ledger» и «ведомость» на пяти страницах. Правка прозы, не кода.
4. **Шесть целей не уложились в срок**, и у пяти из них нет своего `--timeout` в
   строке ярлыка, хотя их собственные шапки называют минуты: `counts:check`,
   `names:check`, `stolknoveniya:check` (шапка обещает тридцать минут),
   `srashchivanie:check`, `numbers`, `numbers:check`. Пока срока нет, они в CI
   либо висят, либо режутся чужим сроком.

## Чем прогон был ограничен, чтобы числа читались правильно

* Разом шёл **ровно один** прогон; каждый — через
  `PAMYAT=<40G|99G> CHISLO=1 POROG=200G /srv/flang-rabota/vorota/flang-vorota`.
* Срок на цель: 4 минуты в первом заходе, 15 минут в переспросе. Цель, не
  уложившаяся в него, помечена «обрыв» — это НЕ приговор проверке, это значит,
  что состояние не установлено.
* `zakon:check` переспрошен при 99 ГиБ вместо 40 — ответ тот же (SIG9), значит
  дело не в пределе памяти.
* Дерево после каждой цели возвращалось к чистому (`git checkout -- .`), чтобы
  печатающий ярлык (`site`, `numbers`, `changelog`, `glossary`) не зеленил
  идущую следом сверку своей же печатью.
* `web:stend` числится в «обрыве» по построению: это не проверка, а стенд —
  он поднимает сервер и не завершается никогда.

## Рядом с описью: `ярлыки.flang` и то, что гоняет GitHub, — разные списки

Считано по `.github/workflows/*.yml` того же коммита:

* **61 ярлык из 82 не зовёт ни один workflow.** Опись выше — про то, что человек
  может позвать рукой, а не про то, что проверяется на каждом пуше.
* **11 проверок, которые workflow зовёт, ярлыком не являются вовсе**: среди них
  `flang/проверки/обход-примеров.sh`, `flang/проверки/обход.sh`,
  `flang/proof/сверка-правил.sh`, `scripts/progonshchik-korpusa.flang`,
  `scripts/komandy-cherez-ssylku.flang`, `scripts/pechat-i-primery.flang`,
  `scripts/proverka-pered-progonom.flang`, `scripts/granica-vhoda.flang`,
  `scripts/oblast-v-celi-c.flang`, `scripts/vypusk-v-c.flang` и
  `flang/self/parser.flang` под `check`.
* **Заслон свежести семени в workflow не стоит.** Он живёт в оболочке `ярлык`,
  а workflow зовёт сторожей напрямую — `bootstrap/flang io
  flang/scripts/poddelki-yadra.flang` и подобным. То есть двадцать целей,
  которые рукой сегодня отказываются судить, в GitHub судят молча. Отдельная
  работа `semya` там всё же есть и краснеет об отставшем семени — но она одна, а
  ответы сторожей рядом с ней остаются от старого семени.

## Что осталось недоснятым и почему

Прогоны остановлены координатором в 19:35: на соседней учётной записи шла
перепечатка семени (2 ч 35 мин, 207 ГиБ), свободной памяти оставалось 150 ГиБ
при 74 ГиБ занятого swap. Недоснятыми остались двенадцать «обрывов» и пять
незапускавшихся целей — каждая названа в таблице своей строкой.

Отдельно про **`flang/проверки/ведомость-примеров.txt`**: ярлыком она не
печатается ни одним, но её сверяет обходчик примеров из `.github/workflows/dvoichnyy.yml`. На
момент описи в ней **774 строки при 1094 файлах `.flang` в дереве**, и последний
раз её переписывали содержательно **20 августа** (`1c1df904`); правка 24 августа
(`08ba3cf5`) только переименовала пути после переноса примеров — 135 строк
взамен 135, ни одного нового файла.

