# Курс по flang для портала Digitable

Четырнадцать глав учебного курса, готовых к заливке на
`courses.digitable.life`. **Ничего не опубликовано** — публикация решается
владельцем.

## Что здесь лежит

| Файл | Что это |
| --- | --- |
| `00-overview.md` … `13-where-next.md` | 14 глав курса, с front matter в формате Hugo |
| `check.flang` | проверка кода курса, **написанная на flang** |
| `check-report.md` | её вывод: какие блоки вынуты и какие прошли |
| `scratch/` | рабочий каталог проверки; `.gitkeep` держит его в дереве |

## Куда это заливать

Портал — Hugo 0.93, приватный репозиторий `github.com/digitable-lol/courses`.

**Целевой адрес:** `content/post/flang-kurs/NN-slug.md` → страницы вида
`https://courses.digitable.life/post/flang-kurs/00-obzor/`.

**Почему новый трек, а не дополнение старого.** На портале уже есть трек
`flang` — «flang — полный язык поверх FTS», 14 материалов, обновлён 07.08.2026,
входная страница `/post/flang/00-overview/`. Это **разбор языка по исходникам на
конкретный коммит**, а не учебный курс, и он устарел: его первая глава сама
говорит, что три её принципиальных утверждения развернулись в
противоположную сторону за сутки.

Слить два в один нельзя — у них разный жанр. Предложение: оставить старый трек
как срез с пометкой даты, а новый поставить основным входом. Решение за
владельцем.

## Чего в этих файлах проверить нельзя, и это надо сделать руками

Репозиторий портала при подготовке курса **был недоступен** (нет ключа для
`git@github.com:digitable-lol/courses.git`, `gh` не авторизован). Всё, что
известно об устройстве портала, снято с **живых страниц** —
`https://courses.digitable.life/courses/` и `/post/flang/00-overview/`.

Отсюда: **front matter в главах — предположение, а не факт.** Поля `title`,
`description`, `date`, `lastmod`, `tags` на страницах портала видны и совпадают
по смыслу; поля `categories` и то, чем задаётся раздел каталога
(«03 · Языки и парадигмы») и порядок глав («Глава 1 из 14»), — **не видны из
собранного HTML**. Перед заливкой сверьте шапку с любой главой существующего
трека `content/post/flang/` и приведите к ней.

Что известно точно, потому что видно на собранных страницах:

- карточка трека в каталоге показывает название, описание, число материалов,
  оценку времени и дату обновления;
- хлебные крошки: `Курсы / 03 · Языки и парадигмы / <название трека> / Глава N из M`;
- под заголовком главы — время чтения, число слов, дата и дата обновления;
- метки трека `flang` на портале: `#flang`, `#fts`, `#языки программирования`,
  `#тотальность`, `#кодогенерация`.

## Как проверяется код курса

**Проверка написана на flang.** `check.flang` — это план (`flang io`), который
перечисляет каталог, читает каждую страницу, вынимает из неё блоки с оградой
```` ```flang ````, кладёт каждый во временный файл и зовёт на нём `flang check`.

```bash
cd docs/course
flang io check.flang
```

Итог плана — число не прошедших блоков; отчёт пишется в `check-report.md`.

**Блоки вынимаются из готовой страницы, а не из черновика рядом.** Читатель
видит страницу — значит проверять надо её.

**Две ограды, и разница между ними — контракт:**

- ```` ```flang ```` — **целая программа**, которую можно скопировать и
  запустить. Обязана пройти `flang check` с кодом 0;
- ```` ``` ```` без имени языка — отрывок: одна строка постусловия, скелет
  `разбор`а, вывод команды. Проверке не подлежит и в неё не попадает.

**Блок, который обязан отказать**, помечается комментарием внутри самого блока:

```
// ОЖИДАЕТСЯ ОТКАЗ: …
```

Тогда проверка требует **ненулевой** код возврата, а нулевой считает провалом.
Без этого правила главы, показывающие настоящие отказы компилятора (минус ноль,
несходящиеся примеры), нельзя было бы держать проверенными.

Последний прогон — в `check-report.md`: **15 блоков, не прошло 0** (27 августа
2026, дерево `d363b1e5`).

**И честная запись про эту проверку: она не работала.** Каталог, куда она пишет
блоки, звался `vremenno/` и остался лежать по старому пути `docs/kurs/vremenno/`,
когда сам курс переехал в `docs/course/`. С того дня план отвечал
`FLANG_IO_WRITE` на первом же блоке — то есть **не проверял ничего**. Поймано
прогоном 27 августа 2026; каталог переехал к курсу и назван по-английски —
`scratch/`.

Заодно покраснел и собственный пример `check.flang`: функция «Это страница»
отсеивала отчёт по началу имени `proverka`, а отчёт давно зовётся
`check-report.md`. Отсеивается теперь по полному имени.

Сама проверка тоже проверяется:

```bash
flang check docs/course/check.flang
```

```
модуль «Проверка курса»: функций 26, из них с доказанным завершением 26; типов 6
docs/course/check.flang: проверено — разбор, типы, завершаемость, ядро и примеры; замечаний нет
```

## Подсветка кода на портале

**Решение: печатать подсветку заранее тем же лексером, каким язык читает свои
файлы.** Ни грамматики chroma, ни набора правил не заводится.

Довод не вкусовой. Подсветка, написанная отдельными правилами (регулярками,
списком слов в теме), — это **второе описание языка**, и оно расходится с первым
в тот день, когда в язык добавят слово. Словарь языка ведёт 149 понятий на
четырёх поверхностях записи; ни один список, переписанный руками, первого
пополнения не переживёт.

Инструмент уже есть в дереве и ничего нового писать не надо —
`docs/site/podsvetka.mjs` зовёт настоящий `tokenize` из лексера языка. Прогон по
страницам этого курса:

```
страниц 14; блоков-кандидатов 94 (помечено flang 17, без метки 77);
целых программ 18; окрашено 18; текст сверен знак в знак 18 из 18
```

Восемнадцать блоков окрашиваются, остальные 77 — вывод команд и отрывки —
остаются серыми, и это правильно: блок, который лексер прочтёт «на всякий
случай», покрасится случайно совпавшими словами.

**Роли подсветки уже совпадают с портальными.** Классы `tok-slovo`, `tok-imya`,
`tok-stroka`, `tok-chislo`, `tok-kommentariy` розданы не заново: сопоставление
взято у Digitable как есть (`--course-syn-*` из `courses-v2.css`) — ключевое
слово синий, имя циан, строка зелёный, число оранжевый, комментарий приглушённый.
Шестой роли портала («объявление», фиолетовый) соответствия нет: лексер flang
такого вида токена не выдаёт, и придумывать его подсветке нельзя.

**Урок про контраст соблюдён.** Карточка кода тёмная в обеих темах, и цвет на
карточке берётся только из палитры карточки. Замер по дереву языка:

```
Контраст подсветки: палитра Digitable, карточка кода тёмная в обеих темах;
замеров 18 (мест темы 3 × цветов текста 6), норма 4.5:1, ниже нормы 0;
худший — ключевое слово 7.94:1.
```

**Чего решить нельзя без репозитория портала:** как именно готовый HTML попадает
в страницу Hugo — сырой блок в markdown (нужен `unsafe = true` у goldmark) или
шорткод. Оба пути рабочие, выбор — за тем, кто увидит `config.toml`.

## Откуда взяты числа курса

Правило курса: **ни одного числа и ни одного вывода команды по памяти.** Всё
переснято 27 августа 2026 на дереве `d363b1e5`, выпуск `0.6.2`.

| Что | Чем снято |
| --- | --- |
| строка `--version`, справка `--help`, все расшифровки `check`/`test`/`run`/`emit` | двоичный `/srv/…/w-predely/bootstrap/flang` от 23 августа 16:00 UTC, через ворота |
| 24 063 функции, 19 694 тотальных, 1 045 утверждений, 407 доказано, 150 понятий | `docs/site/numbers.json`, последний пересчёт — коммит `252606e8` |
| размеры напечатанного в восемь целей | `flang emit … --target …`, восемь прогонов подряд |
| ответы восьми целей | сборка и запуск восьми напечатанных программ, .NET 10.0.110 |
| 82 задачи LeetCode, десятка самых коротких | `ls`, `wc -l`, счёт строк `пример` |
| 42 файла и 31 523 строки библиотеки <!-- СНЯТО 2026-09-02 файлов flang/stdlib/*.flang = 42 --><!-- СНЯТО 2026-09-02 строк-в flang/stdlib/*.flang = 31523 --> | `ls flang/stdlib/*.flang`, `cat … \| wc -l` |
| 641 заметок, 197 примеров, 139 файла доказательств <!-- СНЯТО 2026-09-03 файлов docs/zettel/*.md = 641 --><!-- СНЯТО 2026-09-06 файлов examples/*.flang = 197 --><!-- СНЯТО 2026-09-06 файлов flang/proof/*.flang = 139 --> | `ls`, `find` |
| 2 из 20 — цена доказательств | `docs/benchmark-proof-cost-2.md`, замер 16 августа |

**Чего переснять не удалось, и это надо знать.**

| что | почему |
| --- | --- |
| `node flang/scripts/proof-ledger.mjs` по всему дереву | под воротами `node` не запускается вовсе: ворота ставят `ulimit -v`, а V8 резервирует адресное пространство впрок. Числа взяты из `docs/site/numbers.json` — с названным коммитом пересчёта |
| цена доказательств «сегодня» | `docs/benchmark2/tools/measure20.mjs` и `reasons.mjs` удалены вместе с реализацией на JavaScript. Отчёты остались, перемерить нечем |
| значения выражений в оболочке (`4`, `42`) | у доступного двоичного нет рядом `libcompiler_flang.a`, и оболочка отвечает «проверено» вместо значения. Курс показывает то, что видел |
| числа подсветки и её контраста | `docs/site/podsvetka.mjs` зовёт лексер через двоичный, а измеритель — через `node` |

**Уклад, которого не было и который завёлся из-за этого опоздания.** Расшифровка
прогона на странице — это **замер**, такой же, как число или время сборки, и
после каждого выпуска его надо **переснимать**, а не подправлять. Курс на сайт не
попадает, подставлять `{{выпуск.версия}}` там некому, поэтому у каждой главы
теперь первой строкой после шапки стоит объявление замера, невидимое в разметке
и читаемое машиной:

```
<!-- замер: версия 0.6.2 · дерево d363b1e5 · сверено 2026-08-27 -->
```

Прошлая пометка — «перед заливкой прогоните заново» — была адресована человеку.
Она пролежала **четыре выпуска**: `0.5.1`, `0.6.0`, `0.6.1`, `0.6.2`, — пока
четыре строки курса называли версию `0.5.0`.

## Курс на сайт документации не попадает — и это стоит решить

```bash
grep course docs/site/build.mjs
```

Пусто, код возврата 1. Сайт документации языка собирается по списку
`docs/site/sitemap.mjs`, и ни одной главы курса в этом списке нет: курс лежит в
дереве, но публикуется отдельно, на портал Digitable.

**Надо ли туда попадать — решает владелец.** Довод «за» один и он сильный: всё,
что вне сборки сайта, не сверяет никто. Именно поэтому четыре строки курса
называли версию `0.5.0` четыре выпуска подряд, а сайт всё это время показывал
верную: у страниц сайта версия не набирается, а подставляется из
`docs/site/numbers.json`.

**Что для этого нужно — по списку, снято чтением `docs/site/build.mjs` и
`docs/site/sitemap.mjs`:**

1. **Строки в карте сайта.** Раздел в `РАЗДЕЛЫ` и по строке на главу:
   `{ адрес: 'kurs-00.html', имя: '…', из: 'docs/course/00-overview.md' }`.
   Английская пара для страниц вне `docs/site/` не обязательна — она берётся по
   наличию, а у курса её нет.
2. **Снять шапку Hugo.** Блок `---` в начале каждой главы предназначен порталу;
   сборка сайта заголовок берёт из первого `#`, а шапку покажет как текст.
3. **Переписать внутренние ссылки.** В главах они написаны как
   `02-first-program.md`; сборка **отказывает** на ссылке в никуда, а не
   предупреждает.
4. **Заменить набранную версию подстановкой.** На странице сборки сайта место
   версии одно — `{{выпуск.версия}}`. Объявление замера, которое стоит в главах
   сейчас, для страниц сборки не нужно и вредно: подстановка не протухает.
5. **Вписать курс в проверку слов.** `docs/jargon.json` перечисляет поверхности
   поимённо; `docs/course/*.md` в этот список добавлен этой же работой, и три
   десятка внутренних слов из глав вычищены.
6. **Решить про подсветку.** `docs/site/podsvetka.mjs` красит блоки настоящим
   лексером языка — тем же, каким компилятор читает свои файлы.

Пункты 2 и 3 несовместимы с заливкой на портал Digitable **в том же файле**:
порталу шапка нужна, а ссылки нужны портальные. Значит либо курс живёт в одном
месте, либо у него появляется печать под вторую цель. Это и есть решение, которое
надо принять до, а не после.

## Что делать перед заливкой

1. сверить front matter с главой существующего трека `content/post/flang/`;
2. решить, как заводится раздел каталога и порядок глав (данные трека, вес,
   или что там принято);
3. выбрать способ вставки подсветки (сырой HTML или шорткод) и прогнать
   `docs/site/podsvetka.mjs` по страницам;
4. прогнать сборку Hugo и посмотреть на битые ссылки: **внутренние ссылки в
   главах написаны как `02-first-program.md`** и при заливке обязаны стать
   портальными адресами;
5. прогнать `flang io check.flang` ещё раз — числа и выводы в курсе привязаны
   к дереву, и на новом дереве часть из них поедет;
6. переснять расшифровки прогонов и переписать строку объявления замера в каждой
   главе. **Не подправить числа, а переснять**: разница между этими двумя
   действиями и есть разница между замером и выдумкой.
