# Семья «строка по знакам»: склейка разложения (Тожд3) и свёртка по знакам строки (Инд4)

Задача [7359](../../../../../../tasks/completed/7359-strings-have-no-induction-principle.md),
основание — [ADR-0040](../../../../../../adr/0040-a-simplifier-rewrites-by-proved-equalities-and-prints-every-step.md)
§2.5. Строки ведомости `Тожд3` и `Инд4`, леммы «Тожд3», «Инд4», «Инд4-знак» и
встречные примеры «Тожд3-со-связкой-ложна», «Инд4-чужой-список-ложна» в
`flang/proof/lean/Rules.lean`, искажения 60 и 61 в `Trap.lean`.

## Что стережётся

* **Тожд3** — `соединить (разложить Т на символы) по ""` есть `Т`. Связка — ровно
  литерал пустой строки, внутри — разложение ровно одного довода. В записи — ход
  `переписать формой ⟨…⟩ = ⟨Т⟩ законом «Склейка разложения»`, и сверщик
  пересобирает правую сторону сам (`форма_законом`).
* **Инд4** — тело функции `свёртка (разложить Т на символы) …`, где `Т` —
  строка-параметр: принцип свёртки по числу витков, элемент витка объявлен знаком
  (строкой длиной 1), пройденный кусок — списком знаков, а в цели `Т` заменяется
  склейкой пройденного куска. Свёртка по знакам ДРУГОЙ строки и свёртка по кускам
  `разделить` принципа по строке не получают.

## Файлы

Записи — печать ядра ветки: компилятор ветки напечатан в JavaScript (ADR-0030
§10), семя правил Тожд3 и Инд4 не знает до перепечатки.

| файл | что | ядро ветки | семя `0.7.19` | сверщик на записи |
|---|---|---|---|---|
| `join-of-decomposition.flang` | «Символы»: `(соединить результат по "") равен текст` | доказано (Тожд3) | сетка 2 | 0: развернуть → переписать формой законом «Склейка разложения» → тождество |
| `last-character.flang` | «Последний знак»: `(длина результат) не больше 1` над свёрткой по знакам | доказано индукцией (Инд4) | сетка 2 | 3: узел fold в домене «не больше» вне приёма, как у списка |
| `join-with-separator-forgery.flang` | ЛОЖНОЕ: знаки, склеенные через дефис, — исходная строка | объявлено, не доказано | объявлено, не доказано | 0, впустую: доказанным не числится ничего |
| `last-piece-forgery.flang` | ЛОЖНОЕ: последний кусок `разделить` не длиннее знака | сетка 1 | сетка 1 | 0: доказан только шаг |
| `other-string-forgery.flang` | ЛОЖНОЕ: свёртка по знакам `текст ++ "а"` считает длину `текст` | объявлено, не доказано | объявлено, не доказано | 0, впустую |

## Порча — код 1 с причиной (`run.sh`)

| порча | причина в ответе сверщика |
|---|---|
| в записи ход склейки разложения со связкой «-» | «в цели … нет терма» |
| в исходнике цель со связкой «-», запись честной программы | «в цели … нет терма» |
| то и другое вместе: закон склейки разложения на склейке со связкой — ловушка 60 у сверщика | «левая сторона … не той формы, какой закон требует» |
