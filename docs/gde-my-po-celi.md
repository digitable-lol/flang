# Где мы по цели: замер 22 августа 2026

Это не мнение о языке и не пересказ прежних отчётов. Это замер: цель разобрана
на проверяемые составляющие, и против каждой стоит число, добытое прогоном на
этом дереве. Где числа нет — так и написано «не мерено», а не заполнено
догадкой.

Дерево замера: ветка `b/ocenka-do-celi` от `github/main` = `2f1238f3`.
Двоичный — `bootstrap/flang` 0.6.2, собран здесь же (`make -C bootstrap -j4`).
Все прогоны через ворота памяти (`/srv/flang-rabota/vorota/flang-vorota`).

---

## Три разных слова, которые здесь не смешиваются

Весь документ держится на этом различии, и без него числа не значат ничего.

- **ДОКАЗАНО** — утверждение обо ВСЕХ входах, установленное ядром. В ведомости
  это вердикт `proved`.
- **ПРОВЕРЕНО** — утверждение о конечном числе входов: примеры, сетка значений,
  прогон на живом сервере. В ведомости это `сетка N`. Это НЕ доказательство.
- **ОБЪЯВЛЕНО** — сказано словами и не подтверждено ничем: ни ядром, ни сеткой.
  В ведомости это `declared`.

Четвёртое, и оно отдельно: **ПУСТОЕ ДОКАЗАННОЕ** — обещание, которое переживает
замену тела заглушкой той же подписи. Слово «доказано» о нём законно, знания в
нём нет.

---

## Цель дословно

Из `/srv/flang-rabota/CEL-CELIKOM.md`, словами владельца:

> формально-доказуемый функциональный язык со статической сильной типизацией и
> своей альтернативой OTP/BEAM, пригодный для написания любых сервисов;
> всё написано на flang — включая библиотеки, фронтенд и бэкенд.

Из `docs/zettel/goal-of-the-language.md` — та же цель, сказанная изнутри дерева:

> **один язык, на котором и доказывают, и пишут сервисы, и который при этом
> читается**. Планка, которую называет владелец — «чтобы можно было хоть код для
> ракет писать, и самое важное — в доступном синтаксисе, зная теоркат и
> формальную логику».

И условие, при котором цель вообще что-то значит
(`docs/zettel/condition-for-the-revolution.md`):

> **Доказать функцию должно стоить дешевле, чем написать на неё тесты.**

## Цель, разобранная на проверяемые составляющие

| № | составляющая | чем проверяется | проверяется ли прогоном |
|---|---|---|---|
| 1 | функциональный | неизменяемость, отделённые эффекты | да, косвенно (`flang check`, план как значение) |
| 2 | статическая сильная типизация | отказ до запуска с местом и причиной | да |
| 2б | доказанное завершение | ведомость: носитель обещания «тотальная» | да |
| 3 | формально-доказуемый | ведомость: доказано / сетка / объявлено | да |
| 3б | доказанное непусто | подмена тела заглушкой | да |
| 3в | ядру можно верить | подделки + число строк ядра решений | частично: подделки — прогон, «верить» — не доказательство |
| 4 | альтернатива OTP/BEAM | узел, надзор, перезапуск на восьми целях | да |
| 5 | пригоден для любых служб | БД, HTTP, TLS, файлы, память | да |
| 6 | всё на flang | счёт файлов JavaScript | да |
| 7 | доказать дешевле, чем протестировать | замер цены на 20 функциях | да |
| 8 | «доступный синтаксис» | — | **НЕТ. Прогоном не проверяется вовсе** |
| 9 | «код для ракет» | — | **НЕТ. Это не утверждение, а планка** |

Пункты 8 и 9 — часть цели, у которой измерителя нет и, по-видимому, быть не
может. Это тоже результат: их нельзя ни закрыть, ни отчитаться по ним числом.

---

# Часть 3. Чего ядру не хватает — ОДИН список

Это сводка каталогов отказов, собранных пятью исполнителями 21–22 августа по
пяти долям библиотеки, плюс то, что нашлось в остальных заходах. Источник
каждой строки назван. **Числа мои только там, где сказано «мой прогон»;
остальные — чужие замеры, перепроверять их я не стал, потому что каждый стоит
часов прогонов.**

## Складывать можно, и вот почему

Доли не пересекаются по файлам:

| доля | файлы | обещаний в доле |
|---|---|---:|
| числа и логика | `numbers`, `logic` | 31 |
| множества и словарь | `sets`, `dictionary` | 37 |
| списки | `lists`, `strlists` | 58 |
| хеши | `sha1`, `sha256`, `hmac`, `kdf` | 88 |
| функции высшего порядка | `higher-order`, `optional`, `result` | 61 |
| **охвачено каталогами** | **11 файлов из 26** | **275** |

Значит одно и то же умение, названное в двух долях, закрывает РАЗНЫЕ обещания —
их числа складываются. Внутри одной доли складывать нельзя: одно обещание ждёт
двух умений сразу.

## Список, упорядоченный по числу обещаний

«Закроет» — обещаний, которые доказались бы от одного этого умения. Где
написано «в паре» — само по себе даёт ноль.

| # | чего не хватает ядру | закроет | откуда число |
|---:|---|---:|---|
| 1 | **решать равенство ВЫЧИСЛЕНИЕМ, а не сличением записей** | 23 | доля «списки» |
| 2 | **modus ponens по охране**: охранённый факт после разреза цели | 23 | доля «ВПО» |
| 3 | **пример под свободным именем** — обоснование «по построению» | 18 | доля «хеши» |
| 4 | **новые виды цели**: конъюнкция равенств о полях записи, «строго больше» | 18 | доля «хеши» |
| 5 | **посылка индукции для тел, которые не `разбор` и не `свёртка`** (вызов, встроенная форма, `отфильтровать`, `пусть`) | 15 | доля «списки» |
| 6 | **встроенные формы в позиции цели** (`элемент N в` 11 · `содержит` 9 · `пусто` 5 · `соединить … по …` 4) | до 11 | доля «списки» |
| 7 | **брать названное `по свойству` постусловие НЕПРОЗРАЧНЫМ фактом**, не разворачивая тело | 10 | доля «хеши» |
| 8 | **`теорема «имя» функции «Ф»`** — теорема цепляется только по имени, при совпадении `FLANG_PROOF_AMBIGUOUS` | 10 | доля «хеши» |
| 9 | **обобщение утверждения по накопителю у свёртки** | 9 (+4) | доля «списки» |
| 10 | **охрана цели про `длина`/`пусто` должна выбирать ветвь `разбор`** того же списка | 9 | доля «ВПО» |
| 11 | **алгебра остатка и деления в правиле «равно»** | 6 | доля «числа» |
| 12 | **конъюнкция в ВЫВОДЕ по половинам** | 5 | доля «списки» |
| 13 | **свёртка против списка** (принцип, знающий, что виток идёт по элементам) | 5 | доля «ВПО» |
| 14 | **уравнения встроенных форм на конструкторах списка + принцип свёртки силы индукции** | 5 в паре | доля «множества» |
| 15 | **неотрицательность по построению через `остаток от`** | 5 | доля «хеши» |
| 16 | **порядок и арифметика**: `х больше 0 ⟹ х не равен 0`, `Е плюс 0 ≡ Е` | 4 | доля «ВПО» |
| 17 | **правило меры списка должно читать ВЫЗОВ в шаге свёртки** | 3 | доля «множества» |
| 18 | **порядок с границей-термом** (вычитание в границе) | 3 | доля «списки» |
| 19 | **разбор цели по `разбор`** (вложенный разбор второго списка) | 3 | доля «списки» |
| 20 | **допущение индукции при другой подстановке остальных параметров** | 3 | доля «списки» |
| 21 | **дно терма из названного допущения** (`Т не меньше К`, К>0) и дно произведения | 3 | доля «числа» |
| 22 | **принцип по свёртке для цели «равно длине» при ЧИСЛОВОМ накопителе** | 2 | доля «множества» |
| 23 | **«разбор цели по условию» должен читать условие-ВЫЗОВ** | 2 | доля «множества» |
| 24 | **принцип по свёртке, знающий что элемент витка ВЗЯТ ИЗ СПИСКА** | 2 в паре | доля «множества» |
| 25 | **разбор случаев по значению скаляра** (`признак`, знак числа) | 2 | доля «числа» |
| 26 | **инвариант свёртки, связывающий накопитель СО СПИСКОМ** | 2 (+2) | доля «числа» |
| 27 | **посылка рефлексивности «Т не есть не-число»** вместо «Т не меньше 0» | 2 | доля «числа» |
| 28 | **принцип индукции по `число`** | 2 | доля «списки» |
| 29 | **равенство конструкторов суммы при свободном поле** | 2 | доля «ВПО» |
| 30 | **мера накопителя свёртки с началом-ТЕРМОМ** (правило требует ноль) | 1 | доля «множества» |
| 31 | **правило порядка под запись «Е не меньше Г»** | 1 в паре | доля «множества» |
| 32 | **читать посылку цели как факт** (база с противоречивым условием) | 1 | доля «списки» |
| 33 | **разложение цели-конъюнкции и цели-дизъюнкции** | 0 в одиночку, **необходимо для 8** | доля «множества» |
| 34 | **транзитивность порядка через названный автором средний терм** | 0 (+2) | доля «числа» |
| 35 | **индукция по мере у нетотальной функции** | 0 (+2) | доля «числа» |
| 36 | **ex falso из противоречия допущения с условием ветви тела** | 0 (+1) | доля «числа» |
| 37 | **сложение литерала слева у известного порядка** | 0 (+1) | доля «числа» |
| 38 | **правило конъюнкции цели** (`А и притом Б` половинами) | **0 замерено**, соучаствует 4 | доля «числа», парный замер |

Сумма столбца «закроет» по первым десяти строкам — **146 обещаний**, и это
меньше двухсот семидесяти пяти охваченных: остальное упирается либо в пары,
либо в границы, которые правкой ядра не снимаются.

## Пять находок, которые в этот столбец не помещаются, а стоят дороже него

1. **★★★ «Доказано обо ВСЕХ входах» стоит на посылке, которая НЕ доказана.**
   Доля «хеши»: семь прибавок из восьми выведены из посылок, стоящих на сетке.
   Ядро принимает постусловие вызванной функции допущением независимо от того,
   доказано ли оно, и итог УСЛОВНЫМ не помечает. Печать при этом разницу знает
   (у недоказанной посылки проверка при работе остаётся) и человеку о ней
   молчит. Это не «не хватает правила» — это то, из-за чего слову «доказано»
   нельзя верить до конца.
2. **★★ Спека обещает то, чего в языке нет.** `flang/proof/SPEC.md` показывает
   промежуточный факт `затем` ВНУТРИ случая индукции — ровно там, где он нужен.
   Двоичный такую запись не разбирает вовсе (`FLANG_PARSE`). Заперт весь класс
   «шагу индукции нужна лемма о вызванной функции».
3. **★★ Ядро не приводит допущения к одному виду.** `если (а не меньше 1) то …`
   — сетка; зеркальное `если (1 не больше а) то …` — ДОКАЗАНО. Переворот
   исходников проверен и дал ноль: упирается в другое. Мой прогон грепом по
   стволу: запись `не меньше К` при К ≠ 0 стоит в `обеспечивает`/`требует`
   библиотеки **104 раза**; в позиции допущения (внутри `если … то`) мой греп
   насчитал **12**. Цифра «38 мест» из сводного каталога моим определением не
   воспроизводится — считалось, видимо, иначе.
4. **★★ Отвергнутая теорема убивает уже готовое доказательство.** Доля
   «множества»: из 35 прогонов, где теорема приписана к УЖЕ доказанному, вердикт
   потерян в 23. И отвергнутая теорема роняет `flang check` целиком — ведомость
   не печатается вовсе.
5. **★ Граница ЯЗЫКА, а не ядра.** Постусловие не умеет говорить о
   функции-аргументе (доля «ВПО», 3 обещания) и не умеет назвать добавочную
   переменную-порог («список упорядочен» невыразимо). Правилами ядра это не
   чинится — нужна поверхность, которой нет.

## Доли, каталогов по которым НЕТ

`strings`, `utf8`, `tree`, `numtree`, `hashmap`, `datetime`, `json`, `base64`,
`http`, `provod`, `postgres`, `redis`, `scram` — 15 файлов библиотеки из 26.
Известно про них только по итоговым коммитам:

- `http`+`provod` — написано 75 теорем, принято ядром **2**; дословные отказы
  разложились в три кода: `FLANG_PROOF_STEP` 67, `FLANG_PROOF_INDUCTION_STEP`
  31, `FLANG_PROOF_INDUCTION_BRANCH` 24 (мой подсчёт по
  `/srv/flang-rabota/b-dok-set-062/*otkaz*.txt`);
- `tree` — прибавка 1, и упиралась она не в правило, а в ЗАХВАТ ИМЕНИ
  связывателем цели;
- `utf8` — прибавка 3 разнятием составных целей на половины, из них
  содержательны 2;
- `scram` — 4 обещания сняты с «объявлено» теоремой, ссылающейся на постусловие
  вызываемого;
- `json`+`base64` — теоремы не прибавили **ничего**.

**Это и есть первая дыра замера: по 15 файлам из 26 никто не знает, чего именно
не хватает.**
---

# Часть 5. Самоприменение — НЕ СХОДИТСЯ НА СТВОЛЕ

Мой прогон, ствол `2f1238f3`, дерево чистое (`git status` пуст):

```
$ sh scripts/raskrutka.sh --bystro
строки рантайма flang/src/emit/c: проверяю, что все литералы закрыты
  все литералы закрыты
ВХОДЫ ПЕЧАТИ РАЗОШЛИСЬ С СЕМЕНЕМ.

  • flang/self/parser.flang: изменён после перепечатки
  • flang/src/emit/c/flang_cli.c: изменён после перепечатки

расхождений 2.
код возврата 1
```

**Ноль означал бы порядок. Здесь единица.**

## Когда сломалось — прогоном по истории, а не догадкой

Сверял так: для каждого коммита первой ветви беру sha256 файла из дерева и то же
имя в `scripts/otpechatok-semeni`; совпали — «ок».

| коммит | `parser.flang` | `flang_cli.c` |
|---|---|---|
| `908f61a3` Слияние `u/perepis-serdca` | ок | ок |
| `8c805685` Слияние `u/processy-iz-yazyka` | **РАЗОШЁЛСЯ** | ок |
| `ba389ad5` Слияние `u/progonshchiki-ne-molchat` | разошёлся | **РАЗОШЁЛСЯ** |
| `35663391` … `2f1238f3` (ещё пять слияний) | разошёлся | разошёлся |

Оба раза потеряло **слияние**: дерево взяло правку из ветки, отпечаток — из
ствола. С тех пор ствол пережил **семь слияний**, и ни одно не покраснело — хотя
сторож стоит в CI (`.github/workflows/dvoichnyy.yml`, строка 177, зовёт ровно
`sh scripts/raskrutka.sh --bystro`).

## Чем это подтверждается независимо от отпечатка

Слово `поручить` вернули в язык коммитом `fe547684`. Мой греп:

```
поручить в flang/self/parser.flang        7
поручить в bootstrap/compiler_flang.c     1
```

То есть **собранный двоичный работает по старым правилам**: правка в исходнике
компилятора есть, в семени её нет. Это ровно то, о чём предупреждает сам
сторож — «оно соберётся и будет работать по СТАРЫМ правилам».

## Что при этом ПРАВДА

- Семя есть, и оно самодостаточное: `make -C bootstrap -j4` собрал двоичный
  0.6.2 без единого предупреждения при `-Wall -Wextra -Werror -pedantic`, node
  не понадобился (мой прогон).
- Семя — **7 файлов, 26 543 354 байта** (мой счёт `wc -c` по `bootstrap/*.c
  *.h Makefile`). В `README.ru.md` стоит «7 файлов, 24 074 977 байт» — проза
  отстала от дерева на 2,5 МБ.
- Отпечаток входов печати покрывает **42 файла** (`scripts/otpechatok-semeni`,
  48 строк минус 6 строк шапки).
- Компилятор печатает сам себя **в одну цель — C**. Восемь целей печати — это
  про пользовательские программы, а не про самоприменение
  (`docs/zettel/what-is-deferred.md`: «цель C нужна ради самораскрутки»).

## Чего я НЕ мерил

Полную перепечатку (`sh scripts/raskrutka.sh`, замер 20 минут и 150 ГиБ
адресного пространства) и сверку `--check` я не гонял: на машине десять
исполнителей, а дешёвая половина уже дала ответ. Значит вопрос «совпадает ли
печать байт в байт» **сегодня открыт**: известно только, что входы печати не те.
