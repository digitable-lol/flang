# Выброс реализации на JavaScript: что уже вынуто, что мешает и сколько это стоит

Замер от 16 августа 2026, ветка `work/bez-etalona`, дерево `origin/main` (`8e62870`).
Все числа получены прогоном на этом дереве, а не оценкой. Где число взято из
чужого замера, это сказано прямо.

Решение владельца: реализация компилятора на JavaScript выбрасывается, потому
что эталоном языка обязан быть компилятор на самом языке. Довод принят. Цена
названа отдельно, в конце, — и она не нулевая.

---

## 1. Что считает `flang check --proof` сегодня

Замер прямой: команда прогоняется под учётом загрузки модулей, считается, какие
файлы на JavaScript она втянула и сколько в них строк.

| | до обязательств | после обязательств | после ведомости |
|---|---:|---:|---:|
| файлов на JavaScript загружено | 35 | 37 | **36** |
| строк в них | 34 031 | 34 267 | **33 926** |
| из них — переключатель (`bridge.mjs`, `self.mjs`) | 0 | 236 | 508 |
| **обязательства считает** | JavaScript | **flang** | **flang** |
| **ведомость считает** | JavaScript | JavaScript | **flang** |

Число загруженных строк почти не изменилось, и это честный результат, а не
неудача: `obligations.mjs` продолжает загружаться, потому что ядро
доказательств берёт из него две вспомогательные функции (`какНаписан`,
`одинаковы`). Изменилось не «сколько загружено», а **кто считает**: связку
«постусловие → цель → предложенное доказательство» теперь строит
`flang/self/obligations.flang`.

Что подтверждает переключение:

- ведомость до и после совпала **побайтово**: 230 программ дерева, 65 из них с
  утверждениями, 176 строк утверждений, расхождений 0 — и машинный вид, и текст
  человеку;
- испорченный слой на flang меняет ответ рабочего пути (иначе считал бы кто-то
  другой);
- в `flang/bin/flang.mjs` нет ни одного ввоза `obligations.mjs`;
- запасного пути к реализации на JavaScript нет: сорвётся слой — сорвётся команда.

Всё четыре — исполняемые проверки в `flang/test/self-cli.test.mjs`.

**Ведомость переключена там же и теми же тремя утверждениями.** `--proof` зовёт
`ведомость` из `flang/src/self.mjs`, та — слой `flang/self/proof.flang`, и
`flang/src/proof.mjs` из списка загруженных файлов ушёл: 37 файлов и 34 954
строки стали 36 и 33 926, минус 1 028 строк. Подтверждают это:

- ведомость до и после совпала **побайтово**: 66 программ дерева с
  утверждениями, 180 строк утверждений, расхождений 0 — и машинный вид, и текст
  человеку. Отдельным прогоном сверены **все 226 программ дерева**: расхождений
  0 (`flang/test/self-proof.test.mjs`);
- испорченный слой (у ведомости отняли ключ `claims`) меняет ответ рабочего пути;
- в `flang/bin/flang.mjs` нет ни одного ввоза `src/proof.mjs`;
- запасного пути к реализации на JavaScript нет.

### Цена по часам

Замерена медианой пяти запусков команды целиком, до и после переключения.

| программа | до | после | разница |
|---|---:|---:|---:|
| `flang/stdlib/lists.flang` | 141 мс | 472 мс | +331 мс (×3,3) |
| `flang/self/proof.flang` | 497 мс | 815 мс | +318 мс (×1,6) |

Переключение ведомости добавило столько же плоской прибавки и сверх того свой
счёт (медиана пяти прогонов команды целиком, на дереве после обязательств):

| программа | до | после | разница |
|---|---:|---:|---:|
| `flang/stdlib/lists.flang` | 523 мс | 825 мс | +302 мс (×1,6) |
| `flang/self/proof.flang` | 923 мс | 1 727 мс | +804 мс (×1,9) |

Сам счёт ведомости стоит 41 мс на `core/json.flang` против 0,2 мс у свидетеля и
632 мс на `self/proof.flang` против 1,9 мс — в 150–430 раз дороже, и это
единственное место, где прибавка не плоская: она растёт с числом объявлений.

Прибавка **плоская, около 320 мс**, и почти вся она — разовая сборка слоя на
flang (разбор и связывание 536 строк эталона плюс всё, что он ввозит): 286 мс,
один раз на процесс. Сам счёт обязательств стоит 4–83 мс против 0–0,4 мс у
реализации на JavaScript, то есть в 18–209 раз дороже, но в абсолютных числах
это десятки миллисекунд.

Отсюда следует, куда смотреть, если прибавка окажется дорогой: **не в слой, а в
его сборку**. Связанная программа эталона детерминирована — её можно собрать
один раз и сохранить, и тогда останутся те самые десятки миллисекунд. Работа не
сделана: сначала нужен спрос, а не догадка о нём.

### Дыра, которую видно только отсюда

Поиск нарушений на сетке примеров (`grid.mjs`) сличал постусловие функции с
целью обязательства **сравнением по ссылке**. У реализации на JavaScript это
точно: она кладёт в обязательство тот же самый объект выражения. Любой слой,
отдающий обязательства **данными** — слой на flang и слой на любом другом
языке, — несёт выражение, равное узлу знак в знак и не равное ему по ссылке.
Поиск отвечал «не искали» на **каждом** постусловии, ведомость печатала это
вместо «нарушений не найдено», и **ни одна проверка не краснела**: побайтовая
сверка обязательств совпадала, а тождества объектов в ней нет.

Сведение переведено на счёт по месту в списке. Это не обход, а снятие
зависимости, которую не может воспроизвести ни одна вторая реализация.

---

## 2. Раскладка по слоям

Реализация на JavaScript целиком — **57 773 строки в 57 файлах** (замер 18 августа
на стволе `063d2f4d`; прежние 54 800 в 53 — замер 17 августа, разложен ниже). Измеритель
здесь не пересказан словами, а назван КОМАНДОЙ, потому что словами его уже
пересказывали и разошлись (см. следующий абзац):

```
LC_ALL=C.UTF-8 cat $( { find flang/src -name '*.mjs'
                        ls flang/bin/*.mjs
                        echo flang/proof/initial.mjs
                        echo flang/proof/reduce.mjs
                        echo flang/proof/search.mjs
                      } | grep -Ev 'flang/src/(bridge|self)\.mjs' ) | wc -l
```

То есть все `.mjs` каталога `flang/src` вместе со всеми подкаталогами (`emit/` с
восемью целями внутри и `host/`), оба файла `flang/bin` и три файла ядра рядом с
ним. Переключатель на слой flang сюда НЕ входит, и это не округление, а решение:
он и есть то, чем реализацию выбрасывают. Переключатель — это два файла: 131
строка в `flang/src/bridge.mjs` и 368 в `flang/src/self.mjs`. Их вычет и делает
из 55 файлов 53 — то самое число файлов, которое стояло здесь и раньше.

Число ПЕРЕСЧИТАНО прогоном на собранном дереве, а не перенесено. В этом месте
успели постоять четыре разных значения ОДНОЙ величины: «53 030 строк в 49
файлах», «53 680 в 49», «54 467 в 53» — и четвёртое, 53 680, стояло знаменателем
в абзаце под таблицей, споря с первой же строкой раздела. Спорить было чем:
измеритель был описан, но не был исполним, и каждая ветка описывала его чуть
иначе. Теперь он исполним, и обе половины спора считаются одним прогоном.

Файлов по-прежнему 53, а строк стало 54 800. Прибавка разложена по слоям и
сходится: типы 6 675 → 6 761, разбор 5 466 → 5 499, завершаемость 1 788 → 1 803,
понижение 831 → 888, вычислитель 1 923 → 1 970, печать в цели 15 532 → 15 660.
Столбец «строк» таблицы ниже суммируется ровно в 54 800, и это
проверяемое свойство, а не обещание: раньше он не суммировался ни во что —
четыре файла, переехавшие из вынесенного проекта FTS (`naming.mjs`, `bidi.mjs`,
`toolchain.mjs`, `targets.mjs`), не попали ни в один слой, и 427 их строк не
считались нигде. Им заведена своя строка.

**ТАБЛИЦА ОТСТАЛА ОТ СТВОЛА, И ОТСТАВАНИЕ НАЗВАНО ЧИСЛОМ, А НЕ ОГОВОРКОЙ.** Тот
же измеритель на стволе `063d2f4d` даёт **57 773 строки в 57 файлах**, то есть
столбец таблицы сходится в свой замер (54 800), но не в сегодняшний. Разница
разложена поимённо и сходится без остатка:

* **пришли пять файлов, 1 564 строки** — `src/digest.mjs` (511),
  `src/target-occupied.mjs` (301), `src/proofcache.mjs` (253),
  `src/host/browser.mjs` (250), `src/lockfile.mjs` (249);
* **ушёл один, 1 300 строк** — `src/proof.mjs`, стёртый вместе с переносом
  ведомости на flang;
* **выросли те, что уже были, — 2 709 строк.**

54 800 + 1 564 − 1 300 + 2 709 = **57 773**, файлов 53 + 5 − 1 = **57**.
Пересчитывать по слоям здесь нечего до следующего замера всей таблицы: слой
каждому из пяти новых файлов не назначен, и назначить его на глаз значило бы
вернуть ровно ту болезнь, от которой измеритель и заведён.

| слой | строк | эталон | чем сверен | зазор | можно заменить сегодня |
|---|---:|---|---|---|---|
| разбор (лексер + парсер) | 5 499 | есть, 9 802 строки | побайтово `JSON.stringify` разбора на всех `.flang` дерева | **1 слово из 148** (`в монаде`) + 4 поверхности диагностик | **нет** |
| связывание | 595 | **есть, 1 308 строк** (`self/link.flang`) | побайтово: связанная программа, диагностики с кодом, текстом И МЕСТОМ, отбрасывание недостижимого — 232 программы дерева + 28 дурных наборов | **0** (было: 23 программы + 2 названных расхождения) | не проверялось |
| типы | 6 761 | есть, 5 418 | код, текст и место каждой диагностики, в том же порядке, на 4 каталогах + порченые файлы | 0 на входах дерева | не проверялось |
| завершаемость | 1 803 | есть, 3 566 + 176 | множество доказанных + диагностики + отметка меры — побайтово, весь корпус дважды; **все пять носителей обещания — побайтово, 234 программы** | 1 известное (`span` не пара чисел) | **да**: носители сверены на всём корпусе, расхождений 0 |
| **обязательства** | **373** | **есть, 536** | **побайтово, 246 программ и 262 обязательства, 11 диагностик** | **0** | **ЗАМЕНЕНО** |
| ядро доказательств | 5 296 | есть, 6 624 | побайтово, 257 программ (233 дерева + 22 нарочно дурных + 2 собранных руками) | **9 программ поимённо** (было 14) | **нет** |
| **ведомость** | **1 300** | **есть, 2 771** | **побайтово в двух видах (машинный и текст), 226 программ дерева из 226 + 66 через рабочий путь** | **0** | **ЗАМЕНЕНО** |
| поиск нарушений на сетке | 201 | нет | — | — | нет |
| поиск доказательств | 578 | нет | — | — | нет |
| законы и свойства | 5 024 | у 5 из 14 (2 033 строки) | побайтово, весь корпус | у 9 эталона нет вовсе (2 991 строка) | нет |
| вычислитель | 1 970 | есть, 2 931 | значение И число витков, 2 318 примеров из 2 318 | 0 форм вне среза | не проверялось |
| понижение | 888 | есть, 1 727 | побайтово, весь корпус | 0 | не проверялось |
| печать в цели | 15 660 | у 7 из 8 (21 966 строк) | побайтово, 101–102 программы на цель | цель JavaScript эталона не имеет (2 934) | не проверялось |
| процессы | 1 666 | нет | — | — | нет |
| ввод-вывод | 832 | нет | — | — | нет |
| оболочка и языковой сервер | 2 023 | у оболочки есть (1 926) | побайтово, 91 ввод | языкового сервера нет (900) | нет |
| командная строка | 1 501 | нет | — | — | нет |
| прочее (отказы, совместимость с FTS, достижимость, метки, горячая замена) | 2 403 | нет ни у одного | — | 2 403 без эталона | нет |
| перенос из FTS (`naming.mjs`, `bidi.mjs`, `toolchain.mjs`, `targets.mjs`) | 427 | нет ни у одного | — | 427 без эталона | нет |

**Эталона нет вовсе у 15 305 строк из 54 800** (28 файлов, 27,9 %). Знаменатель
здесь тот же, что и в таблице выше — то есть замер 17 августа, а не сегодняшний
ствол; расходятся они ровно на разложенные выше 2 973 строки. Совпадение
знаменателя с таблицей не случайность, а требование:
раньше тут стояло «14 711 из 53 680» при 54 467 сверху — одна величина двумя
числами, и спор был виден невооружённым глазом.

Измеритель числителя тоже назван, а не описан: у файла нет эталона, если в
`flang/self/` нет одноимённого `.flang` — при трёх известных переименованиях,
`src/emit/X.mjs` → `self/emit-X.flang`, `proof/initial.mjs` →
`self/proof-initial.flang`, `proof/reduce.mjs` → `self/proof-kernel.flang`, — а
`flang/bin` эталона не имеет по определению. Правило проверено на трёх местах
таблицы, где разложение написано отдельно и вручную: печать («цель JavaScript
эталона не имеет», 2 934) и языковой сервер (900) оно воспроизводит знак в
знак, а по законам и свойствам даёт ровно те же 9 файлов и 2 991 строку.

**Прежние 14 711 не воспроизвелись, и это сказано, а не подогнано.** По
собственным строкам таблицы старое число не собиралось ни при каком чтении: сумма
её беззеркальных клеток давала 13 839. Разница нашлась и оказалась не арифметикой,
а пропуском — вне слоёв висели `builtins.mjs` (872), `host/node.mjs` (162) и те
самые четыре файла из FTS (427), а три слоя («ввод-вывод», «командная строка»,
«прочее») с того замера успели сдвинуться. Сегодня столбец сходится: 15 305
беззеркальных строк входят в 54 800 по слоям без остатка.

Прежний замер в `docs/HANDOFF.md` называл 29 646 строк из 50 926. Число
устарело вдвое: с тех пор в дерево приехали семь целей печати, вычислитель,
оболочка и обе половины ядра.

Так же устарел и пункт 4.5 передачи: разборщик на flang **знает** и `требует`,
и `обеспечивает`, и все прочие слова слоя доказательства. Здесь стояло «не
разбирается ровно одно слово из 147 — `в монаде`»; сегодня разбираются **все 148
слов таблицы**, потому что за разбором написан отдельный слой разворачивания
(§3.5).

---

## 3. Что мешает выбросить оставшееся — поимённо

### 3.1. Ядро доказательств: ЗАЗОР ЗАКРЫТ — 291 программа из 291 побайтово

**Замер от 18 августа на сегодняшнем стволе.** Сверка эталона
`flang/self/proofterm.flang` со свидетелем `flang/src/proofterm.mjs` побайтовая и
идёт по всему корпусу: **291 программа, совпало 291, расхождений 0, названный
зазор 0**. Список `ЗАЗОР` в `flang/test/self-proofterm.test.mjs` стал ПУСТЫМ
СПИСКОМ, а не исчез: сличается он `deepEqual`-ом, значит краснеет теперь на
любом расхождении вообще — в том числе на том, которое кто-нибудь захочет «пока»
назвать.

| кусок | программ было | стало | чем закрыт |
|---|---:|---:|---|
| начальная алгебра встроенного списка | 3 | **0** | «Объявление списка» в `self/proof-initial.flang`, чтение конструкторов — в `self/proof-kernel.flang` |
| точные десятичные с правилом остатка | 1 | **0** | третий вопрос к подписи — ЦЕЛОСТЬ — в `self/proof-kernel.flang` |
| принцип по отрезку `нат` | 2 + 1 дурная | **0** | «Принцип по числу» в `self/proof-initial.flang`, второй носитель принципа |
| три формы тела (свёртка, условие, вызов) | 1 + 1 дурная | **0** | пятый ход — в `self/proof-kernel.flang`, шестой — в `self/proof-initial.flang`, седьмой — в `self/proofterm.flang` |
| четвёртый ход, ОТКАЗНАЯ половина | 1 дурная | **0** | «Дописать о вычислении» в `self/proofterm.flang» |
| четвёртый ход, ПРИНИМАЮЩАЯ половина | 4 | **0** | «Свести счётом» в `self/proof-kernel.flang» |

**Долг про четвёртый ход переписан числом ТРИЖДЫ, и каждый раз прежняя запись
оказывалась ложной.** Это стоит записать целиком, потому что беда тут не в
числах, а в том, как долг живёт: запись остаётся, когда причина уже ушла.

1. Записан он был как **«у эталона вычислителя нет»**. Вычислитель есть —
   `self/interpret.flang`, сверенный на всех примерах корпуса.
2. Тогда долг переписали **ЦЕНОЙ**: подготовка стоила 5 679 мс при 552 функциях,
   а при 1074 ответа не было вовсе. Цена была снята 17 августа измеренно —
   список тегов диспетчера перестал собираться заранее (77 % подготовки), а
   проверка «функция объявлена дважды» перестала быть квадратичной (16 %).
3. А 18 августа выяснилось, что **цена вообще не при чём**. Довод про неё был
   правдой, пока в зазоре стояли программы на тысячу-две функций; их вынес донос
   теорем по ребру импорта (`flang/src/link.mjs`), и вместе с ними ушёл довод —
   а ЗАПИСЬ ОСТАЛАСЬ. В зазоре сидели программы в **27, 1 и 12 функций**;
   подготовка на них стоит **40, 2 и 10 мс**, а на замкнутой цели
   эталон-вычислитель тратит **10–17 витков**.

**Мешала ФОРМА ПЕРЕДАЧИ ХОДА, и снята она тремя вещами.**

**Переезд слоя.** «Свободные имена» с помощниками — 15 функций — стояли в
`self/proof-initial.flang`, а тот ИМПОРТИРУЕТ `self/proof-kernel.flang`, где
живёт `Свести`. То есть слой, обязанный решать «свободно ли здесь хоть одно
имя», лежал НИЖЕ слоя, умеющего ответить, и ход был заперт направлением ребра, а
не ценой счёта. Функции переехали вниз, текст перенесён знак в знак, места
вызова не тронуты.

**Явный довод вместо замыкания.** Свидетель отдаёт ход замыканием:
`вычислениемЗамкнутой` захватывает программу и уезжает пятым доводом в `свести`.
В языке замыкания нет и не будет — на нём ломается доказательство завершаемости,
ради которого язык существует. Поэтому программа проведена ЯВНО через **13
функций** цепочки сведения, седьмым доводом нового входа `Свести счётом`. Пять
прежних входов отдают `Нет программы` — это `вычислитель = null` свидетеля: хода
нет, ответ знак в знак прежний.

**Два места вызова, а не восемь.** Сверено со свидетелем, а не выбрано: вычислитель
у него уезжает ровно в двух местах — сведение цели с телом функции
(`flang/src/proofterm.mjs:672`) и посылка, закрытая самим ядром (`:966`).

**Что это стоило.** Эталон считает три бывшие красными программы за 561, 359 и
180 мс против 82, 62 и 116. Доказанных ядром в ведомости **132 из 153** — ход не
отнял ни одного. `АКСИОМЫ` остались пусты, и это проверяется отдельным тестом.
Точка раскрутки перепечатана: 7 файлов, 12 851 808 байт, связано функций 3511.

**Ловушка, которую поймал сторож чисел, а не человек.** Вычислитель, въехавший в
`proof-kernel.flang`, потянул за собой имена, которых не было в списках `только`
у `self/grid.flang` и `self/obligations.flang`. Оба файла ушли из ведомости
МОЛЧА, целиком — доказанных стало 130 при 132. Списки расширены. Это ровно тот
класс, ради которого числа прозы сличаются прогоном.

**Переключить ядро теперь можно по этому пункту**: на всех 291 программе корпуса
эталон отвечает то же, что реализация на JavaScript, знак в знак. Что осталось
держать реализацию на JavaScript — разделы ниже и §5.

### 3.2. Ведомость: раздел утверждений перенесён, зазор пуст

**Было.** `flang/self/proof.flang` не печатал ни строки утверждений и не считал
их итогов. Разошлось это на **каждой** программе дерева — 226 из 226, — и в
одном и том же месте: после `"laws"` реализация на JavaScript писала `"claims"`,
эталон не писал ничего. Семь проверок `flang/test/self-proof.test.mjs` были
красные.

Отдельно молчала сверка про категорию, функтор и пять объявленных свойств: слов
этих в эталоне было **ноль вхождений**, а в корпусе сверки — ни одной
программы, где они есть. Корпус был 9 рукописных программ плюс 3 файла дерева, и
ни в одной из двенадцати нет ни категории, ни свойства. Это ровно тот случай,
ради которого в проекте заведено правило про проверку, переставшую сравнивать:
зелёная сверка здесь означала «не на чем разойтись», а не «сошлось». Шесть
программ `examples/cat` втащены в сверку **до всякой починки**, и зазор
назван числом: расходятся **все шесть**.

**Стало.** Перенесено 1 387 строк на flang (было 1 431, стало 2 771):

- раздел утверждений целиком — восемь вердиктов и их тексты, включая индукцию по
  трём носителям (начальность объявленной суммы, конечность отрезка `нат`, виток
  свёртки), три ответа поиска нарушений и «НАРУШЕНО на примере автора»;
- итоги утверждений — десять чисел, ключ есть всегда;
- категория, естественное преобразование, связь модулей и пять объявленных
  свойств со своими словами точек и своими законами;
- раздел разрешений — то, что из посчитанного следует;
- исход счёта на сетке у всех восьми видов законов: нарушения, пропущенные точки
  и оборванный обход. До этого эталон печатал «нарушений не найдено» безусловно,
  и монада с восемью несравнимыми точками расходилась со свидетелем молча;
- печатный словарь доведён до свидетельского: было 4 строки, стало 10.

Ведомости-эталону довезены на входе обязательства и ответы поиска нарушений:
раньше ей передавали только итоги завершаемости и законов. Перекладывает поля
проверка (`flang/test/self-proof.test.mjs`), решает — эталон: порядок разделов,
слова точек и вся арифметика итогов написаны на flang.

**Числом:** 226 программ дерева из 226 сходятся побайтово в обоих видах, 22
проверки из 22 зелёные, зазор 0 из 6. Изъятие показано десятью правками: снять
итог утверждений — краснеет 14 проверок из 29, снять раздел утверждений — 8,
снять шесть строк печатного словаря — 13, вернуть слово «точек» вместо «троек» —
3, а снять законы категорий, пять свойств, пропущенные точки сетки, свойства
операции в строке моноида, раздел разрешений или нарушения закона — по одной.

Корпус сверки вырос с 11 программ до 25, и шесть программ бывшего зазора
переехали в него, а не в пустой список: список, сравнивающий ничего с ничем,
зеленел бы вечно. Восемь вердиктов раздела утверждений в дереве встречаются
пятью — «НАРУШЕНО», «отвергнуто», «сетка» ядра, три из четырёх ответов поиска и
теорема наследия не написаны никем, — поэтому на них стоят отдельные сверки с
подложенными итогами: **оба** исполнителя получают один и тот же подложенный
ответ и обязаны совпасть знак в знак.

**И ведомость вышла из рабочего пути.** `flang check --proof` зовёт
`ведомость` из `flang/src/self.mjs`, а та — слой `flang/self/proof.flang`;
`flang/src/proof.mjs` из списка загруженных файлов ушёл (37 файлов и 34 954
строки стали 36 и 33 926). Три исполняемых утверждения об этом стоят в
`flang/test/self-cli.test.mjs` — те же три, что про обязательства, — и подробнее
они разобраны в разделе 1.

Цена уплачена и названа. Только счёт ведомости, медиана пяти прогонов:

| программа | функций | свидетель | эталон |
|---|---:|---:|---:|
| `flang/core/json.flang` | 31 | 0,2 мс | 41 мс |
| `flang/stdlib/lists.flang` | 28 | 0,4 мс | 63 мс |
| `flang/self/proof.flang` | 438 | 1,9 мс | 632 мс |
| `flang/self/parser.flang` | 678 | 2,2 мс | 940 мс |

Это в 150–430 раз дороже, и в абсолютных числах — до секунды на файл, плюс
разовая сборка слоя около 300 мс. Команда целиком на `flang/stdlib/lists.flang`
стоит 825 мс вместо 523, на `flang/self/proof.flang` — 1 727 вместо 923.
Свод по корпусу (`flang/scripts/proof-ledger.mjs`, 225 файлов) на эталон НЕ
переведён и продолжает считать свидетелем: он и есть то самое второе мнение, и
переворачивать в нём отношение — отдельная работа. Куда смотреть, если прибавка
окажется дорогой, известно из переключения обязательств: не в слой, а в его
сборку — связанная программа эталона детерминирована и собирается один раз.

Остался и один долг поимённо: слово «вычисление замкнутой цели» эталон
**списал**, а не прочитал. Свидетель берёт его из того файла, где ход написан
(`ВЫЧИСЛЕНИЕМ` в `flang/proof/reduce.mjs`); эталону взять его неоткуда — слоя
сведения на flang нет. Две копии одного слова расходятся молча, и на это
поставлена отдельная сверка в `flang/test/self-proof.test.mjs`.

### 3.3. Завершаемость: носители сверены на корпусе, расхождений ноль

`self/totality.flang` возвращает вердикт, диагностики и множество доказанных
функций. Ведомости нужны сверх того носители обещания — циклы, структурные
убывания, сторожа, точные шаги, спуски. Их даёт `self/carriers.flang`, и он был
сверен с реализацией на JavaScript на **5 рукописных и 3 настоящих**
программах, а не на корпусе.

**Улика, числом.** На тех трёх настоящих (`flang/core/json.flang`,
`flang/stdlib/lists.flang`, `flang/stdlib/tree.flang`) сверялись 20 циклов, 26
структур, 2 точных шага, 1 сторож и **ноль спусков**: объявленной меры в них
нет ни одной, и единственным входом объявленной меры была рукописная программа
про «НОД». В дереве спусков 98 — в 44 программах, и ни одна из сорока четырёх в
сверку не входила.

**Сделано этой работой.** Сверка расширена до корпуса и приёмом соседей —
`flang/test/self-carriers.test.mjs` идёт по всем программам `flang/**` и
`examples/**` и сравнивает **побайтово** пять списков носителей целиком: имена
компонент цикла в их порядке; ребро, позицию и имя убывающего аргумента у
структуры; шаг словами у точного; стережённый аргумент у сторожа; обе меры,
имена параметров вызываемого и три текста отказа у спуска.

Охват вырос с 8 программ до **234** (231 дерева + 3 нарочно дурных), и вместе с
ним — число сверяемых носителей: 1 085 циклов, 1 908 структур, 25 точных шагов,
6 сторожей, 98 спусков. **Расхождений 0.**

Три дурные программы заведены не для полноты. Дерево зелёное, и в нём нет ни
одной программы, где функция объявлена тотальной, а завершение не доказано, —
значит две ветви эталона, отделяющие доказанное от объявленного («Цикл
доказан» и отсев структур в «Структуры компонент»), на одном дереве не
сверялись бы вовсе: у них не было бы ни одного входа. Это измерено изъятием, а
не предположено: снятие обеих ветвей проходит по восьми настоящим программам
незамеченным и краснеет только на дурных.

Что зелёный цвет здесь значит, показано **одиннадцатью изъятиями**: шесть кусков
`self/carriers.flang` и пять кусков `self/totality.flang` ломаются по одному в
памяти, и каждое изъятие сверка называет — на программах, которых в прежних
восьми не было.

Цена названа: полный проход стоит **496 секунд**, из них 240 — две самые
большие связанные программы дерева (`self/repl/repl.flang` и
`self/bootstrap/compiler.flang`). Самый дорогой вызов корпуса — «Носители
программы» на первой из них, 968 734 115 витков; предел взят вдвое больше
измеренного и не больше.

**Право переключить завершаемость получено.** Остаётся одно известное
расхождение — `span`, не являющийся парой чисел, — и оно названо и стережётся
в `flang/test/self-totality.test.mjs`.

**Это уже сделано, но не в этом дереве.** Ветка `work/nositeli-na-korpuse`
(`cc6f70c`, `69fb19c`) расширила сверку до **234 программ** (231 дерева и 3
нарочно дурных): 1 085 циклов, 1 908 структур, 25 точных шагов, 6 сторожей,
98 спусков, расхождений 0, цена прохода 496 секунд. Прежние восемь программ не
несли ни одного спуска — объявленной меры в них нет, — а в дереве спусков 98 в
44 программах, и ни одна из сорока четырёх в сверку не входила. Числа взяты из
сообщений тех коммитов, а не из прогона на этом дереве: здесь этой правки ещё
нет, и строка таблицы выше описывает то, что лежит здесь. Сольётся ветка —
строку и этот абзац снимать вместе.

### 3.4. Законы: девяти эталонов нет

Моноид, монада, изоморфизм, множества и факт-чекинг переписаны и сверены
побайтово на всём корпусе. Не переписаны девять: сетоид (785), функтор (362),
частичный порядок (241), монотонность (231), идемпотентность (196),
ограниченность внутри `bounded.mjs`, свойства (172), дистрибутивность (157),
коммутативность (131) — **2 991 строка**.

### 3.5. Разбор: зазор ЗАКРЫТ — 148 слов из 148 и четыре поверхности

Здесь стояло: «эталон разбирает 146 слов из 147, не разбирает `в монаде`, плюс
четыре поверхности диагностик». Оба пункта закрыты, и оба — числом.

**Слово.** `в монаде` — не пробел грамматики: узел `inMonad` живёт от конца
своей функции до конца разбора, где его заменяет разворачивание. Эталону нужен
был не разбор, а **разворачивание** — отдельный слой. Он написан:
`flang/self/monad-expand.flang`, 1006 строк против 635 у свидетеля (×1,59 при
оценке ×1,6), и это ПЕРЕЕЗД, а не вторая копия — `flang/self/monad.flang` теперь
ввозит его и оставляет себе законы. Продукция сверх слоя стоила двенадцати
функций, слой поверхностей — ещё четырёх.

Улика — настоящий файл дерева: `examples/monad/order-total.flang` встал в
корпус побайтовой сверки, и AST на нём сходится знак в знак (19 331 байт). До
слоя он туда не вставал. Изъятие сделано на нём же: снимешь вызов
разворачивания — сверка краснеет (16 406 байт против 19 331, расхождение на
байте 10 312), а список неразобранных слов остаётся пустым. Значит держит
сверка, а не список.

`ПРОБЕЛЫ_РАЗБОРА` в `flang/test/self-parser.test.mjs` теперь ПУСТ, и тест
по-прежнему сравнивает равенство множеств: приедет в лексер слово без продукции
— покраснеет так же, как краснел с непустым списком.

**Четыре поверхности.** Слой написан тремя частями: `«Поверхность потока»` и
`«Слово на поверхности»` в `self/lexer.flang`, `«На поверхности»` и
`«Слово понятия»` в `self/parser.flang`. Обе таблицы (625 записей «фраза →
поверхность» и 563 «понятие + поверхность → первое слово») ПОРОЖДЕНЫ из
`SURFACE_TABLE` свидетеля скриптом и сверены с ним поштучно: 625 фраз, 592 пары,
27 файлов — расхождений 0. Диагностика сверена на 18 отказах руками (четыре
поверхности, включая русскую, которая обязана остаться нетронутой) и 168
обрезках настоящих нерусских файлов, из них 110 отказов, — расхождений 0.

Изъятие сделано и здесь: снять вызов переписывания цитат, оставив обе таблицы, —
и сверка краснеет с первого случая (`у пересечения ожидалось 'из'` против
`'from'`). Держит слой, а не таблицы сами по себе.

Цена в точке раскрутки названа: связано 2150 функций против 2028, печать выросла
с 7 858 802 байт до 8 287 851 (+5,5 %).

**Разбор (5 466 строк) из рабочего пути этим не выходит:** здесь закрыт ЗАЗОР, а
переключение — отдельная работа.

### 3.6. Связывание: сделано, и вот чем это стоило подождать

**Закрыто.** Связывание вынуто в `flang/self/link.flang` (1 382 строк, ×2.2 к
свидетелю) и сверено на корпусе тем же приёмом, что обязательства и ядро: 232
программы дерева плюс 28 дурных наборов, побайтово по связанной программе, по коду,
тексту И МЕСТУ каждой диагностики и по результату отбрасывания недостижимого
(`flang/test/self-link.test.mjs`). Прогоном: 260 программ, 21 129 объявлений,
10 диагностик, расхождений 0; у отбрасывания — 62 программы с импортами,
выброшено 4 709 функций, расхождений 0.

**Цена ожидания названа числом.** Пока сверка шла по тринадцати синтетическим
наборам, она молчала о **23 разошедшихся программах дерева** — и молчала не
потому, что всё сходилось, а потому, что в тринадцати наборах не на чем было
разойтись. Эталон не сливал и не выкладывал восемь родов объявлений теорката,
функтор, процессы, надзор, прогоны и планы; чтением нашлось ещё четыре
расхождения, которых не достаёт и корпус (имя модуля у уже привезённого файла,
повтор имени внутри файла, встроенный тип двух файлов с процессами, место у бед
импорта). Оба названных прежде расхождения тоже закрыты: хвост `ENOENT` и
отбрасывание недостижимого — `examples/import-check.flang` печатает 2 функции
вместо 29.

Мораль та же, что записана в конце этого документа про отставшего свидетеля:
сверка, которой не на чем разойтись, зеленеет ровно так же, как сверка, которая
сошлась, — и отличить их можно только числом охвата.

### 3.6. Языковой сервер: эталон написан, расхождений ноль, граница названа

**Улика ДО, числом.** `flang/src/lsp.mjs` — 900 строк, одиннадцать методов
протокола, два вида исходящих сообщений; `flang/bin/flang-lsp.mjs` — ещё 185
строк транспорта. Эталона не было **ни у одного** из 26 файлов самоприменения:
слова `lsp` и «Языковой сервер» не встречались в `flang/self/**` ни разу. На все
восемнадцать обещаний `flang/test/lsp.test.mjs` отвечала только реализация на
JavaScript.

**Стало.** `flang/self/lsp.flang` — 2 893 строки, 270 функций. Сверка
`flang/test/self-lsp.test.mjs` подаёт одну и ту же последовательность сообщений
JSON-RPC в свидетеля и в слой на flang и сравнивает **весь поток исходящего**
побайтово: `JSON.stringify` свидетеля против «Печати значения» эталона, сообщение
за сообщением, включая их число, порядок и код выхода.

**Числом:** 102 сообщения корпуса обещаний (103 исходящих, 30 поручений
проверки), 3 376 запросов наведения и перехода сеткой — в каждой позиции каждой
строки шести документов, — 181 дополнение сеткой. Всего **3 659 запросов,
расхождений 0**, 12 проверок из 12 зелёные. Изъятие показано **девятью**
порчами: каждая ловится на настоящем сообщении корпуса, а не на выдуманном, и
число пойманных сообщений названо у каждой — от 1 до 23 из 102.

В корпус втащена НАСТОЯЩАЯ программа дерева — `examples/cat/order-shipment.flang`:
без неё три ветки подсказки (морфизм, его композиция, единица категории) не
имели бы ни одного входа, потому что морфизма нет ни в одном из выдуманных
документов. Тем же доводом заведена нарочно дурная программа с объявленной и
недоказанной тотальностью: в правильной программе такой ветки не встретить, а
сверка ветки без входа зеленеет вечно.

**Что найдено по дороге.** Таблица ключевых слов эталона лексера
(`flang/self/lexer.flang`) содержала те же 625 фраз, что и `SURFACE_TABLE`
свидетеля, но в другом порядке групп: **138 фраз из 625 стояли не на своём месте**.
Токенизации это не меняло — фразы уникальны, поиск идёт по ключу `|фраза:`, — и
дифференциальная сверка потоков токенов молчала. Поймал расхождение первый, кто
таблицу ПЕРЕЧИСЛЯЕТ: дополнение языкового сервера. Порядок сведён, сторож на него
стоит в `flang/test/self-lexer.test.mjs`.

**Что отдано хозяину, и почему.** Три вещи, и все три свидетель сам ввозит из чужих
модулей:

- `checkSource` (`repl.mjs`) — разбор со **связыванием**, типы, завершаемость.
  Связывания на flang нет вовсе (`src/link.mjs` в самоприменение не переписан), а
  чтение импортируемых модулей — эффект. Эталон говорит «проверь вот этот путь с
  вот этим текстом», хозяин зовёт **ту же самую** `checkSource` и приносит ответ.
  Оба исполнителя получают ответ одной функции, разойтись на этом нельзя;
- `typeName` (`types.mjs`) — имя ПОСЧИТАННОГО типа, приезжает строкой. Эталон
  `typeName` в `self/types.flang` говорит о своём «Типе» — с отрезками, масштабом
  и бесконечностью, — и перевод одного в другой был бы третьим словарём имён
  типов;
- перевод адреса в путь и обратно (`кПути`, `кАдресу`, 14 строк) — `path.resolve`
  и процентное кодирование UTF-8, то есть знание об ОС и о кодировке.

Отсюда следует и предел: **переключить рабочий путь на эталон сегодня нельзя**,
и упирается это не в сервер, а в связывание. Пока `link.mjs` не переписан,
диагностику по-настоящему считает свидетель.

**Цена названа.** Один ответ дополнения — 625 ключевых слов плюс имена модуля;
слой на flang строит и печатает его за 0,9 с против миллисекунд у свидетеля.
Поэтому сетка дополнения идёт по одному документу (181 запрос, 171 с), а не по
трём: три документа — это 700 запросов и 613 секунд, и в общий прогон такая цена
не влезает.

---

## 4. Порядок работ и цена

Порядок вынужденный: каждый следующий шаг нужен предыдущему.

| # | шаг | размер | что закрывает |
|---|---|---|---|
| 0 | ~~втащить шесть программ `examples/cat` в корпус сверки ведомости~~ | 20 строк проверки | **СДЕЛАНО**: зазор измерен прогоном — расходятся 6 из 6 |
| 1 | ~~раздел утверждений + категория, функтор, пять свойств в ведомости-эталоне~~ | 1 387 строк на flang | **СДЕЛАНО**: 7 красных проверок закрыты, зазор пуст — 226 программ дерева из 226 сходятся побайтово |
| 1а | ~~переключить `flang check --proof` на ведомость-эталона~~ | 272 строки переключателя | **СДЕЛАНО**: ведомость (1 300 строк) вышла из рабочего пути, загружаемых строк JavaScript стало на 1 028 меньше |
| 2 | ~~сверка носителей обещания на всём корпусе~~ | проверка на 234 программах, расхождений 0 | **СДЕЛАНО**: право переключить завершаемость (1 788) получено |
| 3 | ~~начальная алгебра встроенного списка~~ | СДЕЛАНО, 200 строк flang | 3 из 14 тогдашних |
| 4 | ~~точные десятичные с правилом остатка~~ | СДЕЛАНО, 190 строк flang | 1 из 14 тогдашних |
| 5 | ~~принцип по отрезку `нат`~~ | СДЕЛАНО, 560 строк flang | 3 из 14 тогдашних |
| 6 | три формы тела: свёртка, разбор цели по условию, сличение по вызову | 950–1200 строк | 2 из 6 нынешнего зазора: обе `examples/library-api` (пятый ход) |
| 6а | ~~удешевить подготовку в `self/interpret.flang`~~, потом связать четвёртый ход | СДЕЛАНО в части цены (15,5 раза); проводка не написана | 4 из 6 (четвёртый ход) |
| 7 | ядро (5 296 строк) выходит из рабочего пути | — | зазор пуст |
| 8 | ~~разворачивание `в монаде` + поверхности диагностик~~ **СДЕЛАНО** | 1 006 строк слоя + 12 функций продукции + 4 функции и 2 таблицы поверхностей | зазор разбора пуст (§3.5) |
| 9 | девять эталонов законов | ~3 000 строк | законы (2 991) |
| 10 | цель печати в JavaScript, процессы, ввод-вывод, языковой сервер, командная строка | 15 305 строк без эталона | остальное |

Шаги 1–7 закрывают цепочку доказательства целиком — **8 384 строки** самой
цепочки уходят из рабочего пути (ведомость 1 300, завершаемость 1 788, ядро
5 296; было 7 734, пока ядро было 4 659, а ведомость 1 287). Это и есть та
часть, ради которой затевалось: «доказано» перестаёт зависеть от чужого языка.

Шаги 0, 1, 1а и 2 сделаны, и вместе с ними — три куска ядра (3, 4, 5) и цена
подготовки из 6а. Разница между «зазор пуст» и «строки ушли из рабочего
пути» вынесена в отдельный шаг нарочно и остаётся записанной: первое — свойство
эталона и проверяется побайтовой сверкой, второе — свойство рабочего пути и
проверяется тем, что команда не ввозит ни одного модуля свидетеля. Считать первое
за второе значило бы отчитаться о непроделанной работе, и порядок здесь
вынужденный: переключать до закрытия зазора было нельзя.

Шаги 8–10 — про то, чтобы **удалить** файлы, а не про доказуемость.

---

## 4а. ПОРЯДОК УДАЛЕНИЯ: весь свидетель на JavaScript, поимённо и числом

Владелец потребовал не «вывести из пути», а **убрать реализацию на JavaScript из
продукта совсем**, и потребовал видеть это конечным списком задач, а не «когда-
нибудь». Список ниже — весь рабочий путь, каждый файл, снятый ПЕРЕПИСЬЮ прогона
(`flang/test/rabochiy-put.test.mjs`), с ответом на два вопроса: есть ли эталон на
flang и **что именно держит**.

Считано на `work/zakony-na-flang` поверх ствола `0c2880d5`: **29 файлов, 36 419 строк**
(`wc -l`; перепись считает на единицу больше на файл и даёт 36 448 — это одна и
та же величина в двух соглашениях). Было 36 файлов и 37 335 на стволе `1d02a4cb` (перепись 37 371): семь
модулей ушли двумя переносами оракула (ярус III ниже). Первый перенос уже влит
в ствол, второй — на ветке; числа сняты переписью одного дерева в двух
состояниях.

Разложение по ярусам ниже снято на `cc269ffa` и с тех пор правлен ОДИН ярус —
третий. Сумма ярусов после правки даёт 35 214 против сегодняшних 36 312, и
разница в 1 098 строк ветке не принадлежит: это рост уже впущенных файлов за
влитые ветки ствола, из которых 380 — плата за само переключение (`self.mjs`
+332, `bin/flang.mjs` +48). Разложить его пофайлово может только новая перепись,
и до неё числа ярусов честнее оставить как есть, чем подогнать под итог.

Кто кого держит — тоже прогоном: разбор ввозов всех 29 файлов, имена и потребители.

### Ярус I — ОСНОВАНИЕ: машина, на которой едет эталон (9 файлов, 19 533)

**Замер пересняли 19 августа 2026: 18 710 → 19 533.** Ярус не вырос новыми
файлами — их по-прежнему девять; выросли уже впущенные, и весь прирост в трёх
(`types.mjs` 7 618 → 7 884, `interpret.mjs` 1 200 → 1 366, `parser.mjs` без
изменений). Число 18 710 снято 16 августа и с тех пор кочевало по заданиям как
постоянная; постоянной оно не было.

**УСЛОВИЕ ВЫХОДА ВЫПОЛНЕНО, и это состояние яруса, а не обещание.** «Сменить
хозяина на двоичный» значило, что собранный из `bootstrap/` компилятор умеет
всё, зачем человек зовёт `flang`. С 19 августа 2026 он исполняет **все десять
команд** полного инструментария: последними закрыты `lock` и `package`
(`flang/test/self-bootstrap.test.mjs`, побайтовая сверка со свидетелем). Список
«есть в полном инструментарии, а в этом двоичном нет» пуст впервые.

**Чего это НЕ значит.** Удалить девять файлов сейчас — значит удалить вместе с
ними три вещи, которые на двоичный не переехали и в ярус I не входят: семь целей
печати из восьми, законы на сетке и **свидетеля как оракула**. Побайтовых сверок
в дереве десятки, и предметом у них служит именно вторая реализация; стереть её —
значит стереть их (порядок этого назван в §5). То есть теперь это решение
владельца о цене, а не техническое препятствие: препятствия больше нет.

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/src/types.mjs` | 7962 | `self/types.flang` | 10 имён у 5 потребителей пути |
| `flang/src/parser.mjs` | 4738 | `self/parser.flang` | разбор нужен всем |
| `flang/src/totality.mjs` | 1803 | `self/totality.flang` | 3 имени у 3 потребителей |
| `flang/src/interpret.mjs` | 1375 | `self/interpret.flang` | 6 имён у **13** потребителей — на нём считают все |
| `flang/src/lexer.mjs` | 1094 | `self/lexer.flang` | 6 имён у 2 |
| `flang/src/builtins.mjs` | 956 | нет | 25 имён у **20** потребителей: встроенные формы самого языка |
| `flang/src/defunc.mjs` | 888 | `self/defunc.flang` | 1 имя у 1 |
| `flang/src/link.mjs` | 661 | `self/link.flang` | 2 имени у 2 |
| `flang/src/tags.mjs` | 186 | `self/tags.flang` | 4 имени у 5 |

Выкинуть их значит выкинуть ИСПОЛНЕНИЕ: эталон на flang не может ехать сам на
себе, пока его гоняет `src/interpret.mjs`. Уходят они **последними и все разом**,
и не переключением, а сменой хозяина — собранным двоичным компилятором из
`bootstrap/`. `builtins.mjs` эталона не имеет и иметь не будет: это встроенные
формы языка, они есть в рантайме каждой из восьми целей печати.

### Ярус II — ПЕРЕКЛЮЧАТЕЛЬ (2 файла, 522)

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/src/self.mjs` | 1366 | нет и не нужен | это ОН зовёт эталонов |
| `flang/src/bridge.mjs` | 131 | нет и не нужен | перевод значений в обе стороны |

Уходят последними по построению: пока хоть один эталон зовётся из JavaScript,
звать его нечем другим.

### Ярус III — ОРАКУЛ: держит не подпись, а недостающий довод (6 файлов, 2 471)

Было тринадцать файлов и 3 874 строки. Семь ушли — двумя переносами оракула, — и
то, чем ярус держался, названо теперь измеренным, а не предположенным.

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/src/setoid.mjs` | 785 | `self/setoid.flang` | таблица ответов вычислителя |
| `flang/src/sets.mjs` | 291 | `self/sets.flang` | то же + `checkSetShapes` у типов |
| `flang/src/factcheck.mjs` | 448 | `self/factcheck.flang` | то же |
| `flang/src/functor.mjs` | 362 | `self/functor.flang` | то же |
| `flang/src/grid.mjs` | 201 | `self/grid.flang` | то же |
| `flang/src/svoystva.mjs` | 162 | `self/svoystva.flang` | **НЕ оракул**: словарь пяти имён `СВОЙСТВА` у типизатора |

**ПОПРАВКА ВТОРАЯ, того же 19 августа (заход «дропни javascript прямо сейчас»):
из шести файлов таблицы В ДЕРЕВЕ ОСТАЛСЯ ОДИН.** Первая поправка ниже вывела их
из ПУТИ; здесь они СТЁРТЫ с диска, по одному, коммит на файл. Числа сняты
`git diff --stat`, а не пересчитаны: **1 801 строка удалена пятью файлами**.

| Файл | Строк | Стёрт | Чем замещено покрытие | Проверок снято |
|---|---:|---|---|---:|
| `flang/src/setoid.mjs` | 785 | да | запись ответов яруса (`fixtures/zapis-oracula.json`), считает слой на flang, отпечаток заморожен свидетелем и совпал; два предела (12, 64) заморожены числами в проверке | **11** |
| `flang/src/functor.mjs` | 362 | да | та же запись яруса, та же незыблемость отпечатка | **10** |
| `flang/src/sets.mjs` | 291 | да | та же запись яруса; ИЗЪЯТИЯ оракула — снимком ответа на ЦЕЛОМ оракуле, снятым до порчи | **1** |
| `flang/src/grid.mjs` | 201 | да | НОВАЯ запись (`scripts/snyat-zapis-setki.mjs` → `fixtures/zapis-setki.json`): отпечаток ответа `checkGrid` на 296 программах, снят ДО удаления | **0** |
| `flang/src/svoystva.mjs` | 162 | да | копия предела в `src/self.mjs` + запись семи ответов `ключТипа`, снятая прогоном в день удаления | **0** |
| `flang/src/factcheck.mjs` | 448 | **НЕТ** | — | — |

`factcheck.mjs` стереть НЕ ВЫШЛО, и причина названа числом: его зовёт живая
команда `flang facts` (`bin/flang.mjs:461`, отложенный ввоз), эталон
`self/factcheck.flang` в `src/self.mjs` не выведен ни одним именем, и пять
проверок командной строки в `factcheck.test.mjs` без него отвечать перестанут.

Двадцать две снятые проверки — это ВСЕ, что потеряло предмет, и каждая названа
однострочной записью на своём месте в своём файле. Ни одна из них не заменена
сравнением слоя с самим собой: подставить эталон на место стёртого свидетеля
значило бы зеленеть вечно, и там, где точки отсчёта не нашлось, проверка снята,
а не подделана.

**ПОПРАВКА 19 августа (заход «дропай javascript»): из шести строк таблицы
осталась одна.** Числа сняты переписью, а не пересчитаны на бумаге —
`node --test flang/test/rabochiy-put.test.mjs` до и после: **27 файлов и 37 081
строка → 23 файла и 35 991 строка**.

| Файл | Строк | Как снят | Чем это подтверждено |
|---|---:|---|---|
| `flang/src/grid.mjs` | 201 | **переключением решения**: сетку нарушений считает `self/grid.flang` через `src/self.mjs` | `self-grid.test.mjs` — 15 проверок, побайтово на всём корпусе; цена ×1,4 на худшей программе |
| `flang/src/factcheck.mjs` | 448 | **изъятием из загрузки**: зовёт его одна команда `facts`, ввоз отложен внутрь неё | `self-factcheck.test.mjs` — 32 зелёных; `flang facts` отвечает тем же вердиктом |
| `flang/src/svoystva.mjs` | 162 | **разделением**: словарь пяти имён уехал в `types.mjs`, число 12 — копией в `src/self.mjs` под сторожем | `self-svoystva.test.mjs` — 10 зелёных, обе копии сверены с объявлением слоя |
| `flang/src/sets.mjs` | 291 | **разделением**: `checkSetShapes` уехал в `src/set-shapes.mjs` (252 строки), сам модуль из пути ушёл | `self-setsmonad.test.mjs` + `cat-sets.test.mjs` — 47 зелёных |
| `flang/src/setoid.mjs` | 785 | вычеркнут раньше, 19 августа (`work/orakuly`) | — |
| `flang/src/functor.mjs` | 362 | вычеркнут раньше, 19 августа (`work/udalenie-js`) | — |

Осталась одна строка яруса, и это `flang/src/monad.mjs` (635): `checkMonadLaws`
до сих пор стоит **строкой таблицы** в `bin/flang.mjs`, то есть решение о законах
монады принимает свидетель. Приём для него — тот же оракул, что закрыл `sets` и
`grid`. Двух других его потребителей приём не снимет: `expandMonads` сидит в
разборщике, `устройствоМонады` — в типах, и оба синхронны.

**Это по-прежнему ОДНА задача, а не восемь**, и цена её теперь названа числом.

Законам и свойствам мало разобрать программу — надо её ВЫПОЛНИТЬ. Здесь стояло
объяснение, чем это мешает: «передать вычислитель функцией в flang нечем —
функций первого класса у языка нет». **Объяснение было неверным**, и выяснено
это пробой в два прогона:

- тотальная функция, зовущая «Вызвать» из `self/interpret.flang`, отвергается с
  кодом `FLANG_NOT_TOTAL` и текстом «обычная функция может не завершиться»;
- та же функция **без слова «тотальная»** проходит проверку с нулём диагностик.

Вычислитель зовётся **по имени** (`использует «Вычислитель flang» из
"interpret.flang"`), и так его уже звал `self/proof-kernel.flang`. Мешала
**тотальность**, а не первый класс.

Цена от этого другая и платится иначе. Оракул выполняет ЧУЖУЮ программу;
обещать её завершение значило бы обещать разрешимость остановки, поэтому 23 его
функции из 42 обещания тотальности не несут. У самих законов их по-прежнему
ноль: граница проходит ровно по вызову вычислителя, а не размазана по слою.

**Что сделано — ДВА переноса, а не один.** `flang/self/oracle.flang`, 441 строка,
— оракул пяти объявленных свойств в дереве; `flang/self/law-oracle.flang`,
370 строк, — оракул моноида и изоморфизма. Второй берёт у первого общее (как
позвать вычислитель, как построить ключ, как не посчитать вызов дважды) ВВОЗОМ,
а не копией: две копии разошлись бы молча и разошлись бы там, где обе зелены.
Тем самым доказано, что приём общий, а не подогнан под первый случай.

Оракул моноида сверен побайтово на 276 программах корпуса (4 непустые таблицы,
713 строк) и 16 нарочных (1078 строк), вместе с единицами моноидов; оракул
изоморфизма — там же, 1 непустая таблица корпуса. Вердикт на таблицах из дерева
совпал со свидетелем. Две порчи в одно слово краснеют
(`flang/test/self-laws.test.mjs`, 33 из 33).

Про первый оракул подробно: `flang/self/oracle.flang`, 441 строка, — оракул пяти
объявленных свойств в дереве. Сверен побайтово с прежним оракулом из проверки,
вместе с порядком строк таблицы: 274 программы корпуса (4 непустые таблицы,
706 строк) и 48 нарочных (39 непустых, 1169 строк) — расхождений 0. Вердикт,
посчитанный на таблице из дерева, совпал со свидетелем на 48 программах × 5
законов. Три порчи в одно слово краснеют на настоящих программах корпуса
(`flang/test/self-svoystva.test.mjs`).

**Семь законов переключены в рабочем пути** (`flang/src/self.mjs`, разделы «пять
объявленных свойств» и «законы моноида и изоморфизма»): `commutative.mjs` 131,
`distributive.mjs` 157, `idempotent.mjs` 196, `iso.mjs` 202, `monoid.mjs` 245,
`monotone.mjs` 231, `partialorder.mjs` 241 — **1 403 строки ушли из пути
целиком**, и это первый раз, когда падает потолок ФАЙЛОВ, а не только строк:
36 файлов и 37 371 строка стали 29 и 36 448 (ствол за это время вырос
на 107 строк уже впущенных файлов). Заплачено 380 строками JavaScript
(`self.mjs` +332, `bin/flang.mjs` +48) и 811 строками flang; чистая убыль пути —
1 030 строк. `flang check --proof` до и после совпал побайтово на 64 программах
корпуса и отдельно на всех, где объявлены моноид, изоморфизм или свойство.

**Последнее, что держало моноид и изоморфизм, — не решение, а ДВА ЧИСЛА.**
Решение было отдано слою и сверено побайтово, а `self.mjs` продолжал брать из
каждого модуля `ПРЕДЕЛ_СЕТКИ` (по 12) для ведомости: два файла грузились ради
двух чисел. Читать число у слоя лениво пробовалось и
отвергнуто прогоном: ведомость печатается синхронно и не только из
`externalChecks`, и отказ «предел не прочитан» покраснел тремя проверками в
`zakon-commutative.test.mjs`. Копия числа стоит теперь в `src/self.mjs`, а
хозяином остаётся слой — `self-laws.test.mjs` сверяет копию с его объявлением,
и разъехаться молча они не могут. Это ровно то различие, из-за которого
`obligations.mjs` год стоял в долге: «вынут из решения» и «вынут из загрузки» —
два разных события.

**Цена по часам** (медиана трёх прогонов `flang check --proof`): программа без
объявленных свойств не платит ничего — 749 → 758 мс на `euclid.flang`, в шуме:
слой не связывается, пока свойство не объявлено, и связывается только тот закон,
чьё свойство объявлено. С объявленными: `retry-safe` 757 → 1 168 мс (×1,5),
`fold-by-cores` 755 → 1 417 (×1,9), `crdt-merge` 795 → 1 928 (×2,4),
`split-lines` 740 → 4 321 (×5,8). Худший случай — дистрибутивность: она кубична
по сетке, и таблица там 588 строк против 34 у соседей. Таких программ в корпусе
**4 из 274**.

**Чего перенос НЕ снял, и это надо знать.** `svoystva.mjs` остался, и держит его
не оракул: `types.mjs` берёт из него словарь пяти имён `СВОЙСТВА`, которым
проверяет написанное автором в ёлочках. Уйдёт он вместе с типизатором либо
разделением — тем же приёмом, каким выехал `obligations.mjs`.

**ЗАКРЫТО 19 августа, и именно предсказанным приёмом.** Словарь уехал в
`types.mjs` (хозяином остался слой — `self/svoystva.flang`, «Имена свойств»;
копию сверяет `self-svoystva.test.mjs`), число 12 — копией в `src/self.mjs`, и
`svoystva.mjs` из рабочего пути ушёл целиком. Из ДЕРЕВА он не ушёл: на нём стоит
сверка `ключТипа` с эталоном.

**Что осталось яруса.** Пять файлов, 2 299 строк: их оракулы (категория, функтор,
множества, сетка, факт-чекинг) живут в `self-setoid.test.mjs`,
`self-functor.test.mjs`, `self-grid.test.mjs` и `self-factcheck.test.mjs`.

**ПОПРАВКА 19 августа: «переносятся они тем же приёмом» — неверно, и это
разобрано чтением кода.** Здесь стояло «переносятся тем же приёмом и той же
ценой». Образец (`oracle.flang` + `law-oracle.flang`) закрывает ТРИ файла из
пяти — `functor` 362, `setoid` 785 и половину `sets` (`checkSetLaws`), около
1 400 строк из 2 299. Два оставшихся — задачи другого рода:

**ОТДЕЛЬНАЯ ЗАДАЧА №1 — отказ прогона значением (нужна для `grid`). СДЕЛАНА
19 августа.** `interpret.flang` обязан был начать отдавать отказ прогона
ЗНАЧЕНИЕМ, а не исключением хозяина. Сделано, и сделано с обеих сторон границы.

*Замер, с которого начали.* **Мерка названа:** блок `try`, внутри которого стоит
запуск программы flang (`evaluate(`, `createRuntime(`, `.call(`, `.доиграть(`,
`.начать(`), по всем `.mjs` дерева. Ветка `work/otkaz-znacheniem`, 19 августа
2026, ствол `725c7831`.

Мест — **52**: **19 в рабочем коде** (13 файлов) и **33 в проверках** (26
файлов). В рабочем коде: `proofterm.mjs` 4, `compat.mjs` 2, `conc.mjs` 2,
`grid.mjs` 2, по одному — `factcheck.mjs`, `functor.mjs`, `hotswap.mjs`,
`monad.mjs`, `repl.mjs`, `setoid.mjs`, `sets.mjs`, `svoystva.mjs`,
`conc/bench.mjs`. То есть правка шире `grid` в полсотни раз по числу мест — и
это ответ на вопрос «а не частный ли это случай».

*Сколько переведено на значение.* Той же меркой после правки — **50**: в
проверках 33 → 30 (три места в `self-interpret.test.mjs` спрашивают исход
прямо), в рабочем коде 19 → 20, и прибавка — сам `interpret.mjs`: ЕДИНСТВЕННОЕ
теперь место, где бросок превращается в значение. Остальные 30 мест ловят
бросок по-прежнему, но бросок этот стал ПРОИЗВОДНЫМ от значения, а не наоборот.

*Что стало границей.* Замкнутая сумма из ЧЕТЫРЁХ видов, одинаковая по обе
стороны: у свидетеля — `прогнать` / `runtime.исход` (`ВИДЫ_ИСХОДА`), у эталона —
`Вызвать исходом` / `Вызвать готовое исходом` (тип «Исход прогона»).

| вид | что значит | кто уже различал его руками |
|---|---|---|
| значение | прогон дошёл | все |
| отказ | программа отказала: код и текст | все |
| **предел** | витки или глубина кончились | `factcheck.mjs`, `compat.mjs`, `grid.mjs` |
| **не готова** | программу не удалось подготовить | `grid.mjs` (второй `try`) |

Двух последних у пары «значение либо исключение» нет вовсе, и именно их не
хватало `grid`: предел — не отказ (ответ мог бы быть, кончился бюджет), а
неготовая программа — не отказавший вызов (не проверен НИ ОДИН пример, а не
«первый отказал»). Вида под «вычислитель сломался» здесь нет нарочно: исключение,
которое не диагностика flang, — дефект реализации, а не исход программы, и оно
летит наружу как летело. Тем же доводом `failures.mjs` не пускает
`FLANG_INTERNAL` в свой список видов отказа.

*Путь один, а не два.* У свидетеля `call` — тонкая обёртка над `исход`, бросающая
ТОТ ЖЕ объект ошибки; у эталона плоская запись «Итог прогона» осталась ПРОЕКЦИЕЙ
суммы (одна тотальная функция), чтобы шесть прежних потребителей — `conc`,
`oracle`, `law-oracle`, `repl`, `proof-kernel`, `compiler` — не переписывались
разом. Разъехаться проекции с суммой нечем: она из неё получается. Непокрытый
вид в `поИсходу` — ошибка `FLANG_UNCOVERED_OUTCOME`, ровно как непокрытый отказ
в `failures.mjs`.

*Чем это подтверждено.* Побайтовая сверка (`self-interpret.test.mjs`) переведена
на сумму и получила ТРЕТИЙ измеритель — вид исхода — сверх значения и витков.
Зелёная: 156 программ, 4 407 примеров, 68 сломанных программ; виды исхода среди
сломанных все три (отказ, предел, не готова), четвёртый даёт корпус. Отдельное
утверждение требует, чтобы сверенных видов было ровно столько, сколько их у
суммы: половина суммы покраснеет. Две новые порчи ловятся ТОЛЬКО видом — у
«предел перестал отличаться от отказа» и «неготовая программа стала отказом
вызова» совпадают и код, и текст, и витки; всего порч 11, краснеют все.

*Цена по часам — ноль.* Медиана пяти прогонов `flang check --proof` на
`stdlib/lists.flang`: 0,99 с до, 0,99 с после.

*Цена в строках названа и она НЕ нулевая.* `src/interpret.mjs` вырос на 167
строк и −1, и рабочий путь `flang check` вместе с ним: 36 915 → 37 081
(`rabochiy-put.test.mjs`, потолок перемерен). Это рост JavaScript в дереве,
которое от JavaScript уходит, и оправдан он ровно одним: без этих строк слой на
flang не может ответить на вопрос «чем кончился прогон», а значит не может
забрать себе ни `grid`, ни факт-чекинг, ни песочницу. Эталон вырос на 243
строки — больше, и это правильное соотношение.

**ОТДЕЛЬНАЯ ЗАДАЧА №2 — разделение `sets.mjs`.** `checkSetShapes` уедет вместе с
`types.mjs` тем же разделением, каким выехал `obligations.mjs`: не переключение,
а вынос. `checkSetLaws` к ней отношения не имеет и переносится образцом.

* **`grid` (201). ПЕРЕНОСИМ с 19 августа.** Его оракул не звал функцию, а
  строил тень программы, поднимал на ней среду и ЛОВИЛ ОТКАЗ прогона примера
  (`self-grid.test.mjs` 263-296: `createRuntime` в `try`, затем `среда.call` в
  `try`). Отказ был исключением хозяина, а не значением, а исключений у языка
  нет by design. Границу починили (задача №1 выше), и три ответа оракула теперь
  получаются из суммы ОДНИМ разбором: «Прогон дал значение» → «дошёл», «Прогон
  не удался» и «Прогон исчерпал предел» → «отказал» с кодом, «Программа не
  готова» → тень не собралась. Проверено не рассуждением: модуль на flang,
  делающий ровно это поверх `Вызвать исходом`, проходит `flang check` без
  диагностик.

  **Во что обойдётся перенос.** Оракул из `self-grid.test.mjs` (`таблицаПрогонов`,
  34 строки) уходит целиком. Из `grid.flang` уходит вся проводка таблицы: три
  функции ключей и поиска (`Ключ прогона`, `Ключ подготовки`, `Найти прогон` —
  28 строк с примерами), объект «Прогон», вариант «Прогона нет в таблице» и
  параметр «таблица», протянутый через 8 сигнатур. Взамен появляется
  `использует «Вычислитель flang» из "interpret.flang"` и один `разбор` на
  четыре ветви. Цена та же, что заплатил `oracle.flang`: функции, зовущие
  «Вызвать исходом», становятся ОБЫЧНЫМИ, а не тотальными, — и это честно,
  потому что считается чужая программа.
* **`sets` (503).** Вторая половина, `checkSetShapes`, берёт контекст у
  типизатора, а не программу целиком, и отдельно от него не снимается. Уедет
  вместе с `types.mjs` — тем же разделением, каким выехал `obligations.mjs`.

Запись ответов свидетеля яруса снята ДО удаления
(`flang/test/fixtures/zapis-oracula.json`, 284 программы, 4 свидетеля,
отпечаток заморожен) — чтобы не повторить потерю 36 порч, случившуюся на ярусе
законов. Образец теперь есть, он рабочий и применён дважды. Шестой, `svoystva.mjs`, к оракулу отношения не имеет вовсе. Сторож счёта стоит:
`rabochiy-put.test.mjs`, «ДОЛГ ЗАКОНОВ ДЕРЖИТ ОРАКУЛ» — пропади у эталона второй
довод, и утверждение покраснеет.

### Ярус IV — ЦЕПОЧКА ДОКАЗАТЕЛЬСТВА (3 файла, 5 384)

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/proof/reduce.mjs` | 2858 | `self/proof-kernel.flang` | 7 имён у 2; горячий цикл переписывания |
| `flang/src/proofterm.mjs` | 2380 | `self/proofterm.flang` | проводка головы конвейера |
| `flang/proof/initial.mjs` | 1411 | `self/proof-initial.flang` | 9 имён у 1; тот же горячий цикл |

**Зазор закрыт: 301 программа из 301 побайтово, названный зазор 0.** Держит
теперь не он, а две вещи, и обе названы: ядру подаются СНАРУЖИ обязательства,
спуски и исходы прогона примеров — переключается оно вместе с проводкой, а не
отдельно; и цена — один виток машины на flang стоит полторы-три тысячи витков
свидетеля, а ядро зовут в горячем цикле.

### Ярус V — БЛИЖАЙШАЯ РАБОТА: подпись совпадает, оракул не нужен (7 файлов, 6 355)

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/src/conc.mjs` | 2140 | `self/conc.flang` | 13 имён у 5 потребителей — самая широкая подпись пути |
| `flang/src/failures.mjs` | 1075 | `self/failures.flang` | 5 имён у ОДНОГО (`types.mjs`) |
| `flang/src/compat.mjs` | 560 | нет | 5 имён у 2: `errorCode`, `evaluateFlang`, `flangError`, `runExamples`, `checkFunctorDictionary` |
| `flang/src/io.mjs` | 1010 | `self/io.flang` | 10 имён у 3 |
| `flang/src/bounded.mjs` | 721 | `self/bounded.flang` | 4 имени у ОДНОГО (`failures.mjs`) |
| `flang/src/monad.mjs` | 635 | `self/monad.flang` | 4 имени у 3 |
| `flang/src/reachable.mjs` | 248 | `self/link.flang`, «Отбросить недостижимое» | **ОДНО имя у ОДНОГО** |

**`conc.mjs` из этой очереди уже вышел, и это сказано числами.** Эталон
`self/conc.flang` вырос с 260 строк до 2135 и покрывает планировщик целиком:
словарь действий, генератор чередований, дерево надзора, надзор в действии,
доставку, таймеры, порождение, разбор отклика, седьмое действие и прогон.
Сверок две, обе побайтовые: планировщик — на сетке семян (11 программ,
28 прогонов по 12 семян = 336 исполнений, 1889 доставок, 84 поручения),
словарь — по корпусу (174 программы, 4 129 556 байт). Расхождений 0 у обеих,
изъятий 19, все краснеют.

Охват назван числом, и остаток разложен весь, до строки. Было покрыто 115 строк
из 2140 (один словарь действий), стало 1296. Оставшиеся 844:

| Что | Строк | Выразимо на flang |
|---|---:|---|
| граница печати: `ВОЗМОЖНОСТИ_ЦЕЛЕЙ`, `требуетПланировщика`, `требуетХозяина`, `гдеПоручение`, коды `FLANG_CONC_UNSUPPORTED` и `FLANG_IO_UNSUPPORTED` | 315 | да — но это не планировщик, а таблица восьми бэкендов |
| шапка файла (проза, кода нет) | 117 | — |
| проверялка примеров: `runConcurrentExamples`, `расхожденияСемени` | 163 | да |
| долги: потолок задержки (`доигрывать`), вытеснение по кванту, горячая замена | 109 | да |
| **граница хозяина: `границаХозяина` и вызов `options.исполнить`** | **97** | **НЕТ** |
| `LINK_DOWN_CODE` — связь узлов | 43 | да, и уже покрыто соседом `self/distributed.flang` |

Невыразимых строк, таким образом, 97 из 2140, и причина у них одна: хозяин
приезжает ФУНКЦИЕЙ (`options.исполнить`), а передать функцию туда, где она
приезжает данными, язык не умеет. Прогон без хозяина при этом сделан целиком:
поручение отвечает названным сбоем `FLANG_IO_NO_HOST`, и 84 поручения на сетке
семян сверены побайтово.

Это и есть очередь. Порядок в ней по цене подписи, а не по размеру файла, и
самый дешёвый — последний: `reachable.mjs` держится единственным `dropUnreachable`
у единственного потребителя `flang/bin/flang.mjs`, а его эталон уже сверен
ПОБАЙТОВО и уже краснеет на порче — пять изъятий достижимости в
`self-link.test.mjs`, все зелёные. Цена переключения у него не в подписи, а в
том, что эталон живёт внутри `self/link.flang`: связать его придётся целиком.

`compat.mjs` УЖЕ РАЗДЕЛЁН, и это сделано тем же приёмом, что у
`obligations.mjs`: не переключение, а РАЗДЕЛЕНИЕ. Перевод модели FTS
(`flangType`, `fromFtsDocument` и пять его помощников, `INPUT_PARAM`,
`RESULT_BINDING`) выехал в `src/fts-legacy.mjs` — из пути его не зовёт никто, он
нужен одной проверке. Из рабочего пути ушло 230 строк, эталона не написано ни
одной: переводить нечего, языка, из которого переводили, в дереве больше нет.
Осталось в мосте 560 строк, и держат их пять живых имён.

### Ярус VI — ОБОЛОЧКА (1 файл, 1 651)

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/bin/flang.mjs` | 2139 | `self/cli.flang` (чистая половина) | эффекты: файлы, каталоги, поток, код возврата |

Эталон покрывает то, что РЕШАЕТ; всё, что ТРОГАЕТ МИР, на flang невыразимо —
эффектов в языке нет by design. Оболочка уходит не переписыванием, а тем же
переездом на двоичный компилятор, что и ярус I.

### Ярус VII — ЧИТАЕТ ПОВЕРХНОСТЬ, эталон не нужен (1 файл, 121)

| Файл | Строк | Эталон на flang | Что держит |
|---|---:|---|---|
| `flang/src/as-written.mjs` | 121 | не нужен | `какНаписан` и `одинаковы` не решают ничего |

### Итог порядка

| Ярус | Файлов | Строк | Одна задача или много |
|---|---:|---:|---|
| V — ближайшая работа | 7 | 6 355 | шесть отдельных, самая дешёвая `reachable.mjs`; седьмой (`compat.mjs`) разделён |
| III — оракул | 6 | 2 471 | **ОДНА**: перенести оракула в дерево — образец есть и применён дважды, семь модулей уже сняты |
| IV — цепочка доказательства | 3 | 5 384 | одна: проводка головы конвейера |
| VI — оболочка | 1 | 1 651 | вместе с ярусом I |
| I — основание | 9 | 19 533 | одна: сменить хозяина на двоичный — **условие выполнено 19 августа 2026** |
| II — переключатель | 2 | 522 | сам собой, последним |
| VII — поверхность | 1 | 121 | не уходит и не должен |

Задач **шесть**, а не тридцать шесть, и это главное, что список говорит.
Строк 35 093 в ярусах I–VI по разложению `cc269ffa` с правленым третьим ярусом;
121 остаётся навсегда и назван.

**ЧТО ЭТОТ ПОРЯДОК ТЕПЕРЬ ЗНАЧИТ.** Абзац ниже описывал ОТМЕНЁННОЕ решение и
оставлен как история: устаревшая запись про долг опаснее отсутствующей, поэтому
она перечёркнута, а не стёрта. Действующее решение — в §5: свидетель уходит и из
продукта, и из проверок. ~~§5 ниже — записанное решение владельца, и оно
про другое: реализация на JavaScript уходит **из продукта**, но остаётся **в
проверках** как второе мнение. Вывести из пути и из пакета — это и есть «убрать».
Стереть из репозитория побайтовые сверки нельзя ничем, кроме потери свидетеля, а
заменить свидетеля нечем: 37 файлов `self-*.test.mjs` сравнивают ДВЕ порознь
написанные реализации, и подставь на место стёртой тот же эталон — они сравнивали
бы слой сам с собой и зеленели бы вечно. Там, где свидетеля всё же стирают
(`src/proof.mjs`), его место занимает ЗАПИСЬ ответов, снятая до удаления, и она
слабее: её можно переснять. Порядок непреложен и остаётся:
**эталон умеет → свидетель заморожен → проверено порчей, что сверка краснеет →
JavaScript уходит из пути.**~~ (конец отменённого абзаца)

Действующий порядок короче: **эталон умеет → сверено побайтово → свидетель
СТЁРТ вместе с проверками, чьим предметом он был, и число стёртых проверок
названо.**

---

## 5. Цена: что теряется вместе с второй реализацией

Возражение поднято до работы и владельцем услышано; здесь оно записано числом,
чтобы не потерялось.

**Неподвижная точка доказывает согласованность, а не правильность.** Компилятор,
собравший сам себя байт в байт, доказал ровно одно: он согласован с собой.
Компилятор с устойчивой ошибкой воспроизведёт её идеально и не заметит — это
ловушка Кена Томпсона («Reflections on Trusting Trust», 1984). Ловит такое
единственная вещь: **вторая реализация, написанная порознь по той же
спецификации**.

Сегодня она есть, и она работает: 15 828 сверок ядра, 4 480 ответов
вычислителя, 246 программ обязательств, 2 318 примеров — всё это сравнения
двух порознь написанных реализаций, и каждое расхождение ловилось именно так.

**РЕШЕНИЕ ОТМЕНЕНО 19 августа 2026.** Владелец распорядился дословно:
«javascript не должен быть свидетелем, вторым мнением и прочим, удаляй к ебеням.
Всё, что должно остаться от JS — это возможность скомпилить flang в него, чтобы
люди в свой язык встраивали.»

**Новое решение: вторая реализация компилятора на JavaScript удаляется целиком —
и из продукта, и из проверок. От JavaScript остаётся РОВНО ОДНО: цель печати
`--target js`.**

### Что остаётся, поимённо и числом

<!-- сторож чисел: не размеры файлов — столбец «Строк» считает остающуюся ЧАСТЬ файла, а не файл целиком -->
| Что | Файлов | Строк | Почему остаётся |
|---|---:|---:|---|
| `flang/src/emit/js.mjs` + каталог `flang/src/emit/js/` | 1 + рантайм | 3 198 | генератор кода: печатает программу на flang в JavaScript. Это НЕ вторая реализация, а восьмая цель печати, ровно как `emit/c.mjs` или `emit/go.mjs` |
| проверки печати в js (`emit-js.test.mjs`, `emit-js-cli.test.mjs`, `emit-js-conc.test.mjs`, `self-emit-js.test.mjs`) | 4 | ~2 200 | предмет у них — цель печати, а не свидетель |
| `flang/src/builtins.mjs`, 295 строк представления значения | часть файла | 295 | **невыразимо языком**: у каждой из восьми целей своё представление значения внутри вычислителя. Это РАНТАЙМ, а не вторая реализация |
| `flang/src/reachable.mjs` | 1 | 248 | **невыразимо по цене**: сторона на flang даёт тот же ответ, но в 181 раз медленнее на горячем пути (143 мс → 25 905 мс), а зовётся при каждой печати во все восемь целей. Остаётся как часть машины печати, не как мнение о языке. **С 19 августа — вне рабочего пути `flang check`**, см. врезку ниже |
| `flang/src/as-written.mjs` | 1 | 121 | читает поверхность, ничего не решает — назван «остаётся навсегда» ещё в §4а |

### `reachable.mjs`: два документа спорили, и оба были правы о разном

Здесь стояло «остаётся НАВСЕГДА», а сторож рабочего пути
(`flang/test/rabochiy-put.test.mjs`) держал тот же файл в списке ДОЛГА.
Противоречие разобрано прогоном 19 августа, и разрешилось оно не в пользу одного
из двух: **они мерили разные вещи**.

**Цена подтвердилась.** Прогон свидетеля против «Отбросить недостижимое» из
`self/link.flang`, ответы совпали ПОБАЙТОВО на всех трёх программах:

| программа | функций | свидетель | сторона на flang | во сколько раз |
|---|---:|---:|---:|---:|
| `examples/import-check.flang` | 29 | 1 мс | 160 мс | ×203 |
| `flang/self/emit-c.flang` | 547 | 19 мс | 2 262 мс | ×116 |
| `flang/self/bootstrap/compiler.flang` | 3 880 | 164 мс | 31 494 мс | ×192 |

плюс 981 мс на сборку слоя. Записанные здесь «×181» и «25 905 мс» — та же
величина на дереве месячной давности. **Переключать решение по-прежнему нельзя,
и строка таблицы остаётся в силе.**

**А сторож был прав о другом.** Модуль ехал в перепись `flang check` не потому,
что проверка его зовёт, — она его не зовёт ни разу, — а потому, что ввоз стоял
статической строкой наверху `flang/bin/flang.mjs` и грузился в КАЖДУЮ команду.
Отбрасывание нужно одной команде `emit`. Ввоз отложен внутрь `commandEmit`, и
248 строк ушли из переписи `check`, не тронув ни цены, ни ответа.

Урок здесь тот же, что записан у `obligations.mjs`: **«вынут из решения» и
«вынут из загрузки» — два разных события**, и документ, который меряет первое,
не отвечает на вопрос про второе. Пока оба числа не названы порознь, спор двух
записей нерешаем, потому что спора нет.

Оговорка про `reachable.mjs` и `builtins.mjs` важна и должна читаться буквально:
они остаются **не как второе мнение**, а как части рантайма, у которых мнения
нет вовсе. Ни один тест не спрашивает у них «а что скажешь ты» — они отвечают на
вопрос «как это исполнить», и другого ответчика нет.

### Что уходит

Всё остальное: 29 файлов рабочего пути минус названные выше остатки, плюс всё,
что уже вышло из пути и держится только ролью второго мнения. Порядок ярусов
из §4а остаётся в силе как порядок работ, но конец у него теперь другой: не
«вывести из продукта, оставить в проверках», а **стереть**.

Первым (19 августа, коммит `3a6c59f2`) стёрт ярус законов: семь модулей,
1 403 строки — `commutative.mjs`, `distributive.mjs`, `idempotent.mjs`,
`iso.mjs`, `monoid.mjs`, `monotone.mjs`, `partialorder.mjs`. Вместе с предметом
ушли 207 проверок, чьим предметом был свидетель; 20 проверок стороны на flang
сохранены и перенацелены.

### Чем заменяется ловля односторонних правок — сказать честно

Заменять нечем, и это надо признать вслух, а не спрятать. Свидетель ловил
односторонние правки, и ловил их не гипотетически: за 18 августа так нашлись
отставание печати у 7 целей из 8, командная строка без `lock`, завершаемость
без постусловий. С удалением свидетеля остаётся три вещи, и ни одна не равна ему:

1. **корпус порч** — испорченный слой на flang обязан краснеть. Ловит порчу,
   НЕ ловит одностороннюю правку: правка не порча, она зелёная с обеих сторон;
2. **встречная сверка двоичного из `bootstrap/`** — ловит расхождение
   «исходник против собранного», не ловит устойчивую ошибку в самом исходнике;
3. **запись ответов, снятая ДО удаления** — слабее свидетеля тем, что её можно
   переснять, и тем, что она не растёт вместе с языком.

Ловушка Кена Томпсона после удаления НЕ закрыта ничем. Это не довод против
решения владельца — решение принято с названной ценой, — но записать цену
обязательно, иначе через месяц её сочтут закрытой.

Почему не годятся остальные ходы (проверено рассуждением, не прогоном):

- **печатать компилятор в две цели и сверять их между собой** — ловит ошибки
  печати, не ловит ошибку в исходнике на flang: он один и тот же для обеих целей.
  Устойчивая ошибка воспроизведётся в обеих;
- **сверять текущий компилятор с предыдущим выпуском** — ловит откаты, не ловит
  устойчивую ошибку: она была и в предыдущем;
- **точка раскрутки в C** — порождена из тех же исходников на flang.
  Независим только сборщик (`cc`), не источник. Ловит зависимость от хозяина,
  не ошибку в языке.

Ни один из трёх не заменяет вторую реализацию. Заменить её нечем — можно только
сохранить.

**Условие, при котором цена всё-таки будет уплачена:** второе мнение перестаёт
работать не тогда, когда его удалят, а тогда, когда его перестанут догонять.
Отставший свидетель молчит так же, как отсутствующий. За сборку `work/svodka3`
это случилось **пять раз подряд** с одним и тем же эталоном ядра. Значит
отставание надо мерить и называть числом, а не оговаривать словами.

Здесь стояло «14 программ зазора ядра», и число это выросло когда-то с 12 до 14
не от порчи эталона, а от того, что свидетель уехал дальше. **Сегодня зазор ядра
равен нулю:** `self-proofterm.test.mjs` даёт 301 программу из 301 побайтово,
названный зазор 0, и список зазора в самом тесте — ПУСТОЙ СПИСОК, то есть
краснеет теперь на любом расхождении вообще, а не на «сверх названного».

И это ровно тот случай, о котором раздел говорит: правку сделали, а число
осталось. Абзац переписан прогоном, а не по памяти. Число здесь не приводится
больше как постоянная — его держит guard, и место ему там.

---

## ЗАКРЫТЫЙ КРАСНЫЙ: теоремы на импорте — оба возят по ребру импорта

Заведён 18 августа, **закрыт в тот же день** коммитом `1c9a1f83`, а запись здесь
осталась стоять «не закрыто» ещё сутки. Она переписана прогоном, и сама эта
история — предмет раздела: **устаревшая запись про долг опаснее отсутствующей**,
потому что читающий видит причину и не имеет способа узнать, что причины больше
нет. За сутки в этом дереве такое нашлось пять раз.

`src/link.mjs` научился возить теорему импортированного модуля по ребру импорта
(ею закрыты четыре расхождения зазора ядра). Здесь стояло, что `self/link.flang`
этого **не** умеет; он умеет — `«Теоремы программы»`, `«Донести из разбора»`,
`«Донести теорему»`, `«Постусловия из файла»` (строки 1055–1113).

**Улика, числом, снята прогоном.** Программа: `главный.flang` импортирует
`опора.flang`, у которого функция «Опора» с постусловием «опора неотрицательна»
и теорема при нём.

| | теорем в связанной программе | ключ `theorems` |
|---|---:|---|
| `src/link.mjs` | **1** («опора неотрицательна») | есть |
| `self/link.flang` | **1** («опора неотрицательна») | есть |

JSON у обоих 2572 знака и **совпадает побайтово**. `self-link.test.mjs` на всём
корпусе: **283 программы, 30 506 объявлений, расхождений 0**.

Устройство у эталона то же, что у свидетеля, и это главное в закрытии: донесение
идёт по **ребру импорта**, а не по имени. Сличение по имени привезло бы
доказательство про чужое тело — имена постусловий совпадают у несвязанных файлов
(`удвоенное неотрицательно` есть и в `proof/examples/precondition.flang`, и — про
другую функцию, в файле без единого импорта — в `four-words.flang`), — и
побайтовая сверка эталона этого **не поймала бы**: оба ядра прочли бы
донесённое одинаково и одинаково согласились.

Аксиом не прибавилось: донесённая теорема не объявляется доказанной, её проверяет
на месте прибытия то же ядро и по тем же правилам, что и свою.

**И ВТОРОЕ, СТОЯВШЕЕ РЯДОМ, — тоже закрыто.** `src/link.mjs` берёт у входного
файла ещё и ключ `requirements`; в `«Ключи входного файла»` эталона его не было.
Теперь список у обоих один и тот же знак в знак:
`["processes", "supervisors", "runs", "plans", "requirements"]`.

Названо и то, что при этом **осталось незакрытым, и оно слоем выше**: программу
с `требует` У ЛЕВОГО КРАЯ эталон разбора (`self/parser.flang`) не строит вовсе —
отдаёт пустую программу там, где свидетель отдаёт полную. В связывании этот зазор не
виден и виден быть не может; он назван исполняемым тестом при самом связывании.
Спит он ровно потому, что **ни одна программа дерева не пишет `требует` у левого
края** — ноль файлов `.flang`.
