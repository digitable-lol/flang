# Журнал, вынесенный из исходников

Комментарий в `.flang` объясняет ПОВЕДЕНИЕ функции, которое из кода не
считывается. Всё прочее — замер с датой, история решения, разбор чужой ошибки,
ссылка на ячейку или коммит — это ЖУРНАЛ: знание настоящее, но лежащее не там.
Стереть его нельзя, читать его в исходнике незачем. Он лежит здесь.

Порядок записи: файл, откуда взято, строки на момент выноса, текст КАК БЫЛ.
Ничего не переписано и не сокращено — иначе документ перестал бы быть заменой
исходнику.

## Почему выноса стало много именно сейчас

Свидетель на JavaScript удалён из дерева целиком (`fe8e8a37` — 48 файлов,
56 072 строки; `5a4ff767` — последний остаток). Комментарии вида «как у
свидетеля», «`Object.keys(части ?? {})` свидетеля», «Копия `flang/src/*.mjs`»
ссылаются на файлы, которых в дереве больше НЕТ. Читателю кода они не говорят
ничего, проверить их он не может, а знание о том, почему перенос сделан так, —
настоящее. Оно здесь.

---

## `flang/self/bounded.flang`

Оценка сверху на витки от размера входа. Перенесено из `flang/src/bounded.mjs`
(файла в дереве больше нет).

### Что покрыто оценкой, и почему список не повторён в исходнике

> Оценка — многочлен от размера входа с ЧИСЛОВЫМИ коэффициентами, а не «O(n)»:
> `витков ≤ 12·н + 7` можно сверить с квантом, а «линейна» — нельзя. Что
> покрыто и что нет — сказано в шапке свидетеля списком из четырёх пунктов и
> семи, и повторять его здесь второй раз незачем: этот файл переносит счёт, а
> не переписывает границу.

### Чем перенос оплачен: три изменяемых вещи, которых в языке нет

> У свидетеля обход держит ТРИ изменяемых вещи: память уже посчитанных оценок
> (`итог`), множество функций «в работе» (`вРаботе`) и состояние разбираемой
> функции (`состояние.беда`, `состояние.спуски`). Изменяемого состояния в языке
> нет, поэтому:
>
> * ПАМЯТЬ едет насквозь и возвращается наружу. Её порядок — порядок, в
>   котором свидетель клал записи в `Map`, и он сверяется побайтово: имя
>   неизвестной функции попадает в память В ТОТ МОМЕНТ, когда его встретили в
>   теле, а имя посчитанной — когда обход её тела закончился;
> * «В РАБОТЕ» едет только ВНИЗ. Наверх её возвращать не надо: у свидетеля
>   `вРаботе.delete` в `finally` возвращает множество ровно к тому, каким оно
>   было у вызывающего, а это и есть переданное вниз значение;
> * СОСТОЯНИЕ едет вместе с итогом обхода, потому что «первая беда побеждает»
>   (`состояние.беда ?? …`) и «спуски сужаются каждым вызовом» — оба правила
>   о порядке, а не о значении.

### Почему обход выражения объявлен обычным

> Обход выражения объявлен ОБЫЧНЫМ — как объявлен он и у соседа по анализу,
> `self/totality.flang` (72 обычные функции из 329). Довод один и тот же: цикл
> «оценить → считать → оценить» идёт по графу вызовов ЧУЖОЙ программы, а он
> ничем не ограничен; у свидетеля его держит только стек JavaScript. Обещать
> завершение здесь значило бы обещать то, чего никто не проверял.

Довод остался в исходнике без ссылки на свидетеля: он объясняет поведение.

### ОШИБКА СВИДЕТЕЛЯ, ПЕРЕНЕСЁННАЯ НАРОЧНО И НЕ ИСПРАВЛЕННАЯ

Разобрано задачей `tasks/1734-element-n-counts-the-number-not-the-list.md`.

> `элемент N в СПИСОК` разбирает НЕ ТОТ аргумент. `происхождение` в
> `bounded.mjs` берёт часть от аргумента 0 — а это САМ НОМЕР, а не список
> (`{ "хвост": 0, "голова": 0, "элемент": 0 }`). Анализ завершаемости на том же
> месте берёт аргумент 1 и объясняет почему: «часть считается от аргумента 1, а
> аргумент 0 (сам номер) в порядке не участвует» (`totality.mjs`,
> `DESTRUCTORS`). Два прохода читают одну форму по-разному, и оценка витков
> теряет спуск там, где завершаемость его видит. Перенесено КАК ЕСТЬ: правка
> свидетеля без правки эталона увеличивает долг проекта.

### Сверка с свидетелем построчно — что именно повторено и зачем

Эти пометки стояли рядом с кодом и объясняли не поведение, а происхождение
записи. Поведение осталось в исходнике, происхождение — здесь.

| Место (строка до выноса) | Что было сказано про свидетеля |
|---|---|
| «Ноль многочлен», шапка арифметики (77) | «У свидетеля циклы по индексу — у него нет причины поступать иначе» |
| «Умножить многочлены» (204) | длина итога — `new Array(первый.length + второй.length - 1)` |
| «Степень многочлена» (258) | «у свидетеля цикл идёт снизу вверх и переписывает» |
| печать многочлена (324) | «Обход идёт от СТАРШЕГО коэффициента к младшему — так печатает свидетель» |
| размер выражения (395) | «у свидетеля её тоже держит только стек» |
| размер варианта и записи (418) | «Вариант свидетеля — это объект с полем `fields`; запись — просто объект. Ключ `fields` и есть та единственная примета, по которой свидетель их различает» |
| размер частей (443) | `Object.keys(части ?? {})` свидетеля: у записи это её ключи, у списка — номера, у строки — номера её знаков. Три из четырёх случаев в дереве не встречаются ни разу |
| размер строки (455) | `Object.keys` свидетеля даёт у строки номера её знаков |
| вид узла в тексте (558, 568) | «у свидетеля это `undefined`, и в строку он попадает словом. В дереве такого узла нет ни одного, но текст обязан совпадать и там» |
| связывание имени (615, 626) | «свидетель делает это тем же `Map.set` — с сохранением места первого вхождения» |
| образец `любое` (729) | «свидетель кладёт туда `откуда`, а не `часть`» |
| разбор `bind` (781) | `Array.isArray(связи) ? связи.map(поле => [поле, поле]) : Object.entries(связи)` |
| «беда» (1033) | «Первая беда побеждает, как у свидетеля (`состояние.беда = состояние.беда ?? …`)» |
| повторное имя (1144) | `if (!поИмени.has(фн.name))` свидетеля |
| память о неизвестном имени (1224) | «Порядок памяти сверяется побайтово, поэтому „когда“ здесь важнее „что“» |
| взаимная рекурсия (1236) | «у свидетеля это единственный возврат мимо `итог.set`» |
| нумерация позиций (1279, 1291) | «Позиции нумеруются с нуля, как у свидетеля» |
| наименьшая позиция (1342) | `[...спуски].sort()[0]` свидетеля, у которого пустое множество даёт `undefined` |
| скаляр (1372, 1387) | `выражение === null \|\| typeof выражение !== "object"` свидетеля; список объектом у свидетеля является |
| остаток блока (1477) | `выражение.in ?? выражение.body` свидетеля |
| сумма размеров (1681) | «свидетель складывает С НУЛЯ (`размерАргументов = константа(0)`), а „Сумма размеров“ начинает с единицы» |
| порядок памяти (1766) | «память уезжает наружу списком, а не отображением, потому что отображения в языке нет, а порядок — есть, и у свидетеля он тоже есть» |

---

## `flang/self/conc.flang`

Планировщик процессов. Переносился со свидетеля `flang/src/conc.mjs`
(2140 строк; файла в дереве больше нет).

### Почему слой процессов был узлом плана выброса свидетеля

> `flang/src/types.mjs` ввозит `conc.mjs` безусловно. Значит JavaScript про
> процессы лежит в рабочем пути КАЖДОГО `flang check`, а не только конкурентной
> программы: прогон на программе с нулём процессов грузит 39 модулей, из них 33
> из `flang/src`, и `conc.mjs`, `failures.mjs`, `bounded.mjs` среди них все три.
> Пока эталона нет, типизатор без JavaScript не соберётся — и слой процессов
> оказывается не хвостом плана выброса свидетеля, а узлом.

Свидетель с тех пор выброшен целиком (`fe8e8a37`), и довод исполнен.

### Замер доказанных обещаний — числа с датой

> ЧИСЛО ЗДЕСЬ ОТСТАЁТ ОТ ФАЙЛА, ЕСЛИ ЕГО НЕ ПЕРЕМЕРИТЬ. Прежняя редакция этой
> строки говорила «утверждений в файле 15, доказаны все 15» — к 25 августа 2026
> своих обещаний тут было 186, доказанных из них 101. Мерить надо прогоном
> `bootstrap/flang check --proof flang/self/conc.flang`, и считать ТОЛЬКО
> первое число вердикта. На 25 августа 2026: своих обещаний 200, доказано 123,
> «на сетке» 10, объявлено 67; вместе с ввезёнными модулями — 796 и 463.

| снято | своих обещаний | доказано | на сетке | объявлено |
|---|---:|---:|---:|---:|
| до 25 августа 2026 | 186 | 101 | — | — |
| 25 августа 2026 | 200 | 123 | 10 | 67 |
| 25 августа 2026, с ввезёнными | 796 | 463 | — | — |

Мерить: `bootstrap/flang check --proof flang/self/conc.flang`, считать ТОЛЬКО
первое число вердикта.

### Почему ключа `--предел-шагов` в прогоне больше нет

Прогон **29 августа 2026** двоичным со счётом заряда (`FLANG_TICKS_OUT`):

| мера | число |
|---|---:|
| шагов | 12 621 954 611 |
| витков | 982 231 323 |
| заряда | 11 639 723 288 |
| время | 540 секунд |
| пик памяти | 4,1 ГиБ |

> Стояло `--предел-шагов 4000000000`. Ключ убран по замеру, а не по вкусу.
> Четыре миллиарда меньше замеренного в 3,2 раза. То есть ключ предел не
> поднимал, а ОПУСКАЛ — ниже того, что этому файлу нужно.
>
> Пока семя считало одни витки и не знало ключа «--предел-шагов», это не било
> по одной причине: витков здесь 982 231 323 при зашитом в семя потолке в
> миллиард — 98,2 % потолка, запаса 1,8 %. Семя перепечатано 30 августа 2026
> (`52996005`): ключ оно ЗНАЕТ, считает виток и заряд вместе и несёт потолок
> из `scripts/raskrutka.sh` — 1 400 000 000 000. Те же 12,6 млрд шагов при
> нём — 0,9 % потолка, запас 110,9 раза. Без ключа прогон проходит с этим
> запасом; с ключом на четыре миллиарда — упирается: четыре миллиарда ниже
> потолка в 350 раз, то есть ключ его ОПУСКАЕТ. Предел здесь выведен из
> замера, поэтому его и не назначают.
>
> Здесь стояло «потолок из `scripts/raskrutka.sh` — триста миллиардов», «4,2 %
> потолка» и «запас 23,8 раза», а рядом — что нынешнее семя ключа не знает
> вовсе. Всё это перестало быть правдой после перепечатки.

### Цена генератора чередований, названная числом

> Каждая битовая операция выражена арифметикой по 32 весам разрядов, то есть
> один вызов генератора стоит порядка тысячи действий вместо шести. Свойство
> при этом сохранено ТОЧНО: сверка со свидетелем на 11 семенах по 200 вызовов
> даёт 2200 совпадений и 0 расхождений.

### Чем сверялся перенос

> ПЛАНИРОВЩИК сверяется НА СЕТКЕ СЕМЯН: одно семя — один журнал доставок,
> побайтово; значит сверять надо журнал, а не итог, и не на одном семени, а на
> сетке — 336 исполнений. СЛОВАРЬ действий сверяется НЕ на сетке: он
> СТАТИЧЕСКИЙ, семени в нём нет ни одного разряда. Корпус там вместо сетки —
> 174 программы дерева, до которых дотягивается связывание. Обе сверки —
> `flang/test/self-conc.test.mjs`.

Файла `flang/test/self-conc.test.mjs` в дереве больше нет.

### Обработчик зовётся эталоном вычислителя — чем это было обосновано

> `self/interpret.flang` уже сверен с `src/interpret.mjs` на 2012 примерах
> корпуса — значения и витки совпали все, — и его же коды и тексты отказов
> сверены на сломанных программах. Второй вычислитель здесь означал бы второй
> ответ на тот же вопрос.

### Чего в переносе нет — с именами полей свидетеля

| чего нет | как это звалось у свидетеля |
|---|---|
| хозяин поручения | `options.исполнить` |
| горячая замена | `options.замены`, подавалась из `hotswap.mjs` |
| вытеснение по кванту | `options.квант` |
| потолок задержки | `options.доигрывать` |

Само поведение (отказ `FLANG_IO_NO_HOST`, пустое поле `замены`, список нулей в
`кадров`) осталось объяснённым в исходнике: оно читается программой, а имена
полей свидетеля — нет.

---

## `flang/self/failures.flang`

Множество отказов процесса как вычислимое множество. Близнец —
`flang/src/failures.mjs` (1069 строк), файла в дереве больше нет.

### Откуда файл в нынешнем виде — разбор двух редакций

> Он существовал в ДВУХ редакциях сразу: 98 функций у одной, 150 у другой, 25
> имён общих. Разбор общих имён поимённо показал не «половины одного файла», а
> два разных РАЗЛОЖЕНИЯ одного и того же: 12 имён совпали побайтово, 13
> расходятся только тем, как разрезаны, а покрытие свидетеля шире у этой —
> у неё есть все три входа, которые ввозит `types.mjs` («Достижимые отказы»,
> «Отказы начального», «Отказы связи»), и оценка витков многочленом. Поэтому
> взята эта редакция целиком, а из второй перенесено единственное, чего здесь
> не было: таблица «вид → код» («Код вида» и «Коды отказа»).
>
> Мирить их СКЛЕЙКОЙ было нельзя, и это не вкус: типы у двух редакций разные
> («Отказ» и «Отказы функции» против «Запись отказа» и «Свои отказы»), и файл
> из кусков обеих перестал бы разбираться — а тогда проверки просто перестали
> бы загружаться, молча.

### Дифференциального критерия у слоя больше нет

> ⚠ ПРОВЕРКИ, СВЕРЯВШЕЙ ЭТО, БОЛЬШЕ НЕТ. Здесь стояло «Проверка —
> `flang/test/self-failures.test.mjs`»; тот файл снят **20 августа 2026**
> коммитом `105943cd` вместе с эталоном `flang/src/failures.mjs`, с которым
> сверялся. Дифференциального критерия у этого слоя больше нет и быть не может:
> второй стороны не существует.

Прежний критерий, пока вторая сторона была:

> Дифференциальный и побайтовый: на каждой программе дерева таблица отказов
> близнеца обязана совпасть с таблицей эталона знак в знак после
> `JSON.stringify`, вместе с порядком ключей и порядком записей.

### Что судит слой сегодня — замер с датой

Заведено **30 августа 2026**:

* `flang/проверки/process-failures.flang` — 18 утверждений над тотальными
  функциями слоя; ввозит этот файл ПУТЁМ и потому ходит на каждый пуш
  обходчиком `flang/проверки/обход.sh` (работа `sborka`). Прогон:
  «обходчик: чисто — проверок 180, сошлись все 180»;
* свои 112 примеров и 138 обязательств — `flang check` этого файла.

**Первый в жизни файла прогон, 30 августа 2026:** «проверено — разбор, типы,
завершаемость, ядро и примеры; замечаний нет», **137,45 с, пик 10,22 ГиБ**.

### Почему оценки едут доводом, а пределы объявлены здесь

> У `оценок` есть и вторая причина ехать доводом, и она про разделение труда:
> близнец ограниченности пишется отдельно (`flang/src/bounded.mjs`), и ввозить
> его сюда `использует`ом значило бы связать два переноса в один коммит.
>
> Два предела пробега объявлены ЗДЕСЬ, а у эталона — в `conc.mjs`, откуда
> `failures.mjs` их ввозит. Расхождение названо, а не спрятано: близнец
> планировщика пишется следом, и когда он появится, пределы переедут туда.

Близнец планировщика с тех пор написан (`flang/self/conc.flang`), а свидетели
удалены. Переезд пределов не сделан — довод, по которому их держали в двух
местах, истёк.

---

## `flang/self/lexer.flang`

Лексер языка, написанный на самом языке. Образец — `flang/src/lexer.mjs`,
сверка — `flang/test/self-lexer.test.mjs`; обоих файлов в дереве больше нет.

> Воспроизводится он дословно: те же виды токенов, те же значения, те же коды и
> тексты диагностик, те же места (строка и столбец). Критерий верности —
> дифференциальная сверка: `flang/test/self-lexer.test.mjs` прогоняет каждый
> `.flang` репозитория через оба лексера и требует совпадения поток в поток.

### Три долга, уплаченные при переносе

**Первый — проход по позиции вместо посимвольного.**

> Раньше он шёл ПО ПОЗИЦИИ: `подстрока линия с позиция по позиция`, а рекурсия
> убывала по разности «размер минус позиция». Это число, а не значение, и
> анализ завершаемости (`src/totality.mjs`) числа частью значения не считает —
> весь проход был нетотален, и «Долги» `flang/self/SPEC.md` перечисляли его
> функцию за функцией.

**Второй — столбец в кодовых точках вместо единиц UTF-16.**

> Столбец считался в кодовых точках, а у образца — в единицах UTF-16
> (`text[offset]`), и расходились они за первым же символом вне BMP в той же
> строке. Долг уплачен: прирост даёт «Ширина точки», мера у обеих сторон одна,
> и вход с эмодзи стоит в корпусе — в примерах этого самого файла.

**Третий — то же самое в месте токена.**

> `разложить … на символы` режет строку по кодовым точкам, и столбец, считанный
> НОМЕРОМ элемента списка, за первым же знаком вне BMP отставал от образца на
> единицу: `text[offset]` образца идёт по единицам UTF-16, а суррогатная пара —
> это две единицы на одну точку.

Почему мера именно UTF-16:

> Мера выбрана не вкусом, а протоколом: языковой сервер объявляет редактору
> `positionEncoding: "utf-16"` — и свидетельский (`src/lsp.mjs`, `приветствие`),
> и эталон (`self/lsp.flang`), — а место токена уезжает в редактор именно через
> него.

### Порядок таблицы ключевых слов — расхождение, пойманное числом

> ПОРЯДОК СВЕРЕН ЧИСЛОМ, а не обещан. Здесь он однажды уже разъехался с
> образцом молча: те же **625 фраз** стояли в другом порядке групп, и **138 из
> 625** оказались не на своём месте. Токенизации это не меняло — фразы
> уникальны, и поиск по `|фраза:` от порядка не зависит, — поэтому
> дифференциальная сверка потоков токенов молчала. Поймал расхождение первый,
> кто таблицу ПЕРЕЧИСЛЯЕТ: дополнение языкового сервера
> (`flang/self/lsp.flang`), у которого порядок элементов — часть побайтового
> ответа редактору. Сторож на порядок стоит в
> `flang/test/self-lexer.test.mjs`.

Сторожа этого в дереве больше нет.

### Как ловилось расхождение в таблице поверхностей

> Иероглиф, попавший в таблицу поверхности мимо этой строки, немедленно ломает
> дифференциальную сверку: образец (`\p{ID_Start}`) прочтёт его именем, а лексер
> на flang — «недопустимым символом», и `flang/test/self-lexer.test.mjs` покажет
> расхождение поимённо. Сверка идёт по каждой поверхности таблицы, а не по паре
> примеров.

### Две переделки ради доказуемости

> **Склейка ключевых фраз.** Раньше здесь убывала длина фразы (4 → 1), то есть
> число, и доказательства не получалось.
>
> **Продолжение после склейки.** Раньше склейка продолжалась с суффикса,
> снятого «Отбросить токены», а суффикс — результат чужого вызова, и частью
> значения он не считается.

### Цена заглядывания вперёд

> «Второй впереди» стоит копии хвоста в интерпретаторе на JavaScript
> (`list.slice(1)`, builtins.mjs) — столько же, сколько сам шаг.

---

## `flang/self/link.flang`

Связывание — седьмой слой самоприменения. Свидетель `flang/src/link.mjs`, файла
в дереве больше нет.

### Цена того, что связывание жило внутри точки входа

> Седьмой слой самоприменения и последний слой контура компилятора, у которого
> не было своего файла. Лексика, разбор, типы, завершаемость, понижение и печать
> жили каждый своим `self/*.flang`, а связывание — внутри
> `self/bootstrap/compiler.flang`, то есть внутри точки входа, а не слоем. У
> такого размещения была цена, и она измерена: сверялось связывание на
> **тринадцати синтетических наборах файлов**, а не на корпусе, и **три рода
> объявлений — теоркат, процессы и планы — не переезжали в связанную программу
> ВОВСЕ**. Расхождение было недостижимо: в тринадцати наборах ни морфизма, ни
> процесса, ни плана нет.

### Почему разбор приезжает готовым — замер

> Сверять связывание на корпусе значит прогнать его **232 раза**, и разбор
> корпуса вычислителем стоит при этом **26 минут против 2** у самого
> связывания.

### Улика, ради которой повтор внутри файла не роняется молча

> `types.mjs` ловит «объявлена дважды» на СЛИТОЙ программе, и в слитой
> оставалось бы одно объявление из двух. Улика измерена в
> `flang/test/link.test.mjs`: файл с двумя `функция «Ответ»` и одной строкой
> `использует` проходил `check` с нулём диагностик.

### Ввоз по имени модуля без пути — замер, по которому ключ не заводился

> `использует «Списки»` без пути. Файл ищется не по имени файла и не по
> каталогу, а по СТРОКЕ `модуль «Списки»` в самом файле. Ключ этот не заводится
> сегодня: связывание УЖЕ требует, чтобы имя во ввозе совпадало с именем модуля
> («Сверить имя модуля», FLANG_IMPORT_NAME), и **на дереве 20 августа 2026 это
> выполнено у 206 ввозов из 206 разрешимых**. Здесь по нему просто НАЧИНАЮТ
> искать.

### Имена полей свидетеля, стоявшие рядом с кодом

| место | что было сказано |
|---|---|
| путь ссылки | `resolve(dirname(файл), ссылка)` свидетеля |
| «всё» вместо ограничения | «`Всё` играет роль `null` свидетеля» |
| сужение | `narrow` свидетеля |
| экспорты | `exportsOf`: пустой список — это «ограничения нет» |
| теоркат | `КАТЕГОРНЫЕ` свидетеля, порядок ключей знак в знак из `src/link.mjs` |
| состояние обхода | «всё, что свидетель держит в девяти `Map` и `Set`» |
| имя модуля файла | `moduleOf` свидетеля |
| слияние | `merge` свидетеля |
| встроенный тип | `if (type.builtin === true) continue` свидетеля |
| видимость | `видно` свидетеля |
| накопление `только` | `wanted` свидетеля |

---

## `flang/self/builtins.flang`

Словарь встроенных форм. Свидетель `flang/src/builtins.mjs` (956 строк), сверка
`flang/test/self-builtins.test.mjs`; обоих файлов в дереве больше нет.

### Копии словаря УЖЕ разошлись — измерено

> У свидетеля **десять** синонимов формы, а трое из эталонов печати — C, C# и
> Python — несут **семь**, без `код_символа`, `charCode` и `к_числу_или_беда`.
> Сегодня от этого не краснеет ничего, потому что подчёркнутым синонимом форму
> не пишет ни одна программа корпуса. Долг записан долгом.

Словарь стоит в четырёх местах: `self/interpret.flang` (имена, синонимы,
арности, склонение), `self/types.flang` (формы, снимаемые непустотой),
семь эталонов печати (формы, снимаемые непустотой, и правило суффикса),
`self/parser.flang` (тип «Исход числа»).

### Квадратичность накопления и приём, которым она закрыта — замер

Списка с запасом (`Список`, `продлить`, `приписать`, `материализовать`) в
словаре нет, и это не недоделка: это представление списка ВНУТРИ вычислителя,
приём «занять ячейку за концом общего массива».

| «добавить» вызовов | без приёма |
|---:|---:|
| 50 000 | 14 375 мс |
| 100 000 | 48 668 мс |
| 200 000 | 229 634 мс |

> У каждой из восьми целей печати приём свой: `fl_grow` в C, `Vec` в Rust,
> срез в Go. Наружу этот вид не выходит вовсе, поэтому и эталона на языке у
> него быть не может: язык говорит о значениях, а не о том, в каких ячейках они
> лежат.

### Чего в словаре нет и почему

> Нет здесь и `FlangError`: отказ, прекращающий вычисление, — это исключение
> JavaScript, а исключений в языке нет и не будет.
>
> Нет `reifyValue`, `isScalar`, `isRecord`, `isVariant` и `isList`: все пять —
> вопросы к ПРЕДСТАВЛЕНИЮ значения в JavaScript, а не правила языка. У
> свидетеля `isRecord` ложен на варианте, потому что вариант у него отдельный
> класс; у документа `«Значение»` вариант — это запись.

---

## `flang/self/zapis.flang` — запись доказательства

### Столкновение к многочленным отпечаткам решается за секунду — замер

**Замер 31 августа 2026 (ячейка Ч40).** Многочленные отпечатки шапки линейны
по кодам знаков, а оба модуля меньше 2³⁰: подобрать к ним столкновение — не
перебор, а решение двух сравнений приведением решётки.

> Честная программа переделана в ложную, пять знаков примечания подобраны, и
> ВСЕ ЧЕТЫРЕ числа шапки — строк, знаков и оба отпечатка — сошлись; подбор
> занял **0,976 с на одном ядре**. После этого честная запись, выданная ядром и
> не тронутая ни на байт, прошла сверку против другой программы.

Отсюда: привязку к исходнику держит **SHA-256**, а не отпечатки. Отпечатки
оставлены как есть — расхождение с ними тоже отказ.

### Правка ячейки Ч56, 31 августа 2026 — терм и номер строки вместе

> Здесь стояло: «Здесь нет самих термов целей и допущений — ни в каком виде»,
> и довод был такой: пересказ цели стал бы ВТОРЫМ ИСТОЧНИКОМ одного и того же и
> разошёлся бы молча. Довод верен ровно наполовину. Второй источник молчит
> только там, где его не с чем сличить.

### Зачем запись вообще заведена

> До этой работы ядро говорило «доказано» и на этом кончалось: доказательство
> жило внутри прогона и умирало вместе с ним. Проверить чужими руками было
> нечего — приходилось верить ядру целиком.

### Замер 29 августа 2026 — почему цель записана именно так

Ядро складывает длины у ПАРНОЙ склейки («соединить А с Б») и не умеет этого у
списка со связкой («соединить [А, Б, В] по ""»), а «плюс 1 плюс 1» не
сворачивает в «плюс 2».

| как записано | вердикт ядра |
|---|---|
| «плюс 2» и список | объявлено, не доказано |
| «плюс 1 плюс 1» и парная склейка | доказано сведением цели с телом функции |

Тот же замер стоял и в `flang/self/builtins.flang`.

---

## `flang/self/monoid.flang` и `flang/self/setoid.flang` — законы

### Чем передавался вычислитель у свидетеля

> Законы моноида — единственная проверка языка, которой мало РАЗОБРАТЬ
> программу: ей надо программу ВЫПОЛНИТЬ. Свидетель делает это `createRuntime`
> из `src/interpret.mjs`, и вычислитель у него — ПАРАМЕТР, а не спрятанный
> вызов.
>
> Вычислитель flang на flang в дереве ЕСТЬ — `self/interpret.flang`, — и ответы
> вычислений считает теперь он. Но приезжают они сюда по-прежнему ДАННЫМИ,
> списком «Ответ», и причина этому не лень, а сам язык: ПЕРЕДАТЬ ВЫЧИСЛИТЕЛЬ
> ФУНКЦИЕЙ В FLANG НЕЧЕМ — функций первого класса у языка нет. Таблица здесь и
> есть тот параметр, которым свидетель передаёт `options.evaluate`.
>
> На свидетеля уходят только формы за границей его среза (`к числу`, функции
> первого класса, постусловия), и каждая такая уступка названа числом в
> `flang/test/self-laws.test.mjs`.

Файла `flang/test/self-laws.test.mjs` в дереве больше нет.

### Изъян свидетеля, названный и не исправленный (`setoid`)

> У свидетеля первая из трёх рекурсий предела не имеет ВОВСЕ — и это его изъян,
> названный здесь, а не исправленный: `применить` в `src/setoid.mjs` на
> композиции, собранной сама из себя, уходит в бесконечную рекурсию и валит
> стек, а не отвечает. Достижимой эта дорога у свидетеля не оказалась
> (замыкание концов такой стрелке концов не даёт, а `реализована` отвечает про
> неё «нет» по своему пределу глубины), поэтому расхождения на корпусе нет — но
> обещание «отказ, а не переполнение» у свидетеля держится случайностью, а
> здесь типом.

> Запас глубины для «Реализована»: у свидетеля предел записан подъёмом
> (`глубина > 32` от нуля), здесь — спуском, и 33 = 32 + 1.

---

## `flang/self/defunc.flang` — дефункционализация

### Порядок тегов сведён к порядку объявления — и почему свидетель исправлен тоже

> Свидетель сортировал теги `Array::sort`, но сравнения строк по порядку в
> языке нет вовсе («сравнения порядка допустимы только для чисел»), а кода
> символа — тоже нет, значит повторить лексикографию UTF-16 здесь нечем.
> Поэтому порядок сведён к тому, который обе стороны считают одинаково и без
> единой новой формы, — к порядку объявления. **Свидетель исправлен так же:
> чинить одну сторону было нельзя, побайтовое совпадение обязано сохраняться.**
> Тот же приём и по той же причине уже применён в `self/totality.flang`, где
> Тарьян приводится к порядку объявления.

### Почему у этого файла свой набор навигации по AST

> Навигация по AST («Взять поле», «Элементы узла») уже написана в
> `self/emit-c.flang`, и остальные слои берут её оттуда импортом. Здесь так
> нельзя: проход обязан звать `self/emit-c.flang` (его точка входа «Печать
> программы» понижает программу первым делом, ровно как `emitC` свидетеля), а
> взаимный импорт двух файлов связывание не разрешает — обход в глубину поймает
> цикл.

Довод остался в исходнике: он объясняет, почему семь функций с суффиксом «при
понижении» стоят здесь, а не берутся импортом.

---

## Приписки ячеек, снятые с `flang/self/zapis.flang`

Кто что дописал — знает `git log`; в исходнике эти строки говорили читателю
кода ровно ничего. Сняты такие:

| было в исходнике | где стояло |
|---|---|
| «ПРОЧИЕ ДОВОДЫ, ТЕРМ И ТРЕБОВАНИЕ — ДОБАВЛЕНО ЯЧЕЙКОЙ Ч56» | заголовок раздела |
| «ПРИВЯЗКА ШАГА АВТОРА К ИСТОЧНИКУ — ДОБАВЛЕНО ЯЧЕЙКОЙ Ч71» | заголовок раздела |
| «Всё от этой черты и до слов „строки посылок с ходами“ добавлено ячейкой Ч24» | врезка |
| «Всё от этой черты и до слов „строки посылок с ходами“ добавлено ячейкой Ч60» | врезка |
| «САМОПРОВЕРКА (узор Ч24)» | заголовок |
| «ОГЛАВЛЕНИЕ СПИСОЧНЫХ ЛИТЕРАЛОВ — ЯЧЕЙКА Ч87» | заголовок |
| «правка здесь границу доверия не двигает (замер Ч84)» | довод |
| «тем же приёмом, что терм при номере строки (Ч56)» | довод |
| «чекер Ч29 читает „ядро“ и „утверждений“ ПО НОМЕРУ строки» | довод |
| «…сломала бы это сличение. Ч71.» | довод |

Замер, который стоял при одной из них и который стоит сохранить:

> Замер Ч56 по **86 записям корпуса**: мест, стоящих словами «принято на слово
> ядра», — **97, из них 93 — `по примеру`** (Ч71, счёт по записям,
> 31 августа 2026).

И два счёта по `flang/proof/examples` (74 посылки всего):

| что | посылок | доля |
|---|---:|---:|
| цели-равенства, не получавшие ни хода | 20 | 27 % |
| посылки, закрытые сведением ядра, без ходов | 13 | 18 % |

---

# Вторая волна выноса: остаток корпуса

Первая волна (десять файлов) лежит выше. Здесь — остаток: все прочие
файлы `flang/self` и `flang/core`, разобранные тем же приёмом.
Порядок разделов — по имени файла.

## Дополнения к разделам выше


### `flang/self/lsp.flang` — `slice` свидетеля у начала и хвоста строки

> // `текст.slice(0, к)` и `текст.slice(к)` — на встроенной «подстрока», у которой
> // индексация с единицы и обе границы включительно.

### `flang/self/emit-csharp.flang` — `Object.is(-0)` как имя проверки на минус ноль

> // проверка «текст равен 0, а значение не равно 0» и есть Object.is(-0).

## `flang/core/evaluate.flang`


Вычислитель утилит ядра FTS.

### Сверка с настоящим ядром на всех `.fts` репозитория

Было в шапке файла:

> Сверка с настоящим ядром —
> `flang/test/core-evaluate.test.mjs`: все `.fts` репозитория, сетка входов,
> совпадение значений по `Object.is` и совпадение кодов диагностик.

Ссылки на `src/utility.ts` и на «образец поведения» ядра FTS оставлены: ядро на
TypeScript — не удалённый свидетель на JavaScript, а внешний контракт, на
который ссылаются и `flang/core/SPEC.md`, и соседний `flang/core/json.flang`.

## `flang/core/json.flang`


### Замер 29 августа 2026: вложенная склейка против списка со связкой

Было у раздела «ПОЧЕМУ ЗДЕСЬ ВЛОЖЕННАЯ СКЛЕЙКА, А НЕ СПИСОК ЧЕРЕЗ «по ""»»:

> // и не умеет у списка со связкой («соединить [А, Б, В] по ""»). Замер 29 августа
> // 2026 на этом файле, двоичный из семени ствола с рантаймом дерева:
> //
> //     до  (список через «по ""»): доказано  3, сетка 6, объявлено, не доказано 16
> //     после (вложенная склейка):  доказано 18, сетка 3, объявлено, не доказано  4
> //
> // Переписаны четыре тела, ни одно обещание не переписано и не снято, примеры те
> // же — «flang test» даёт 18 из 18. Напечатанный C потерял три сторожа при работе
> // («проверок при работе снято 1 → 4», сторожей в напечатанном 25 → 22), то есть
> // три функции вернули себе плоский стек.

Само правило («вложенность обязана быть именно такой, не сворачивайте её обратно
в список») в исходнике осталось.

### Ссылка на `builtins.mjs` в контракте печати чисел

Было в шапке файла, пункт 3:

> //    `builtins.mjs` определяет «к строке» от числа как `String(значение)` —

---

## `flang/core/lexer.flang`


Лексер ядра FTS.

### Анализ завершаемости назывался файлом свидетеля

Было в шапке файла, врезка «почему лексер целиком тотален»:

> «строка стала короче на символ» анализ
> завершаемости (`flang/src/totality.mjs`) частью значения не считает […]

### Кодовые точки объяснялись через `builtins.mjs`

Было у счёта отступа:

> Отступ считается по кодовым точкам, потому что «разделить» режет строку
> именно по ним (builtins.mjs: Array.from, а не единицы UTF-16).

---

## `flang/core/parser.flang`


Парсер ядра FTS на flang. Ссылки на `flang/src/totality.mjs`,
`flang/test/core-parser.test.mjs`, `src/parser.mjs` и
`flang/test/self-parser.test.mjs` — на файлы, которых в дереве больше нет.
Ссылки на ядро на TypeScript (`src/parser.ts`, `src/natural-parser.ts`) в
исходнике ОСТАВЛЕНЫ: это контракт слоя, он назван так же в `flang/core/SPEC.md`.

### Чем сверялась побайтовая верность парсера

Было в шапке файла, концом второго абзаца:

> … печать JSON на flang обязано совпасть с `JSON.stringify(compile(текст))`
> настоящего ядра на TypeScript. Проверка — `flang/test/core-parser.test.mjs`.

Осталось всё, кроме последней фразы.

### Где живёт анализ завершаемости

Было в шапке файла, врезка «почему парсер целиком тотален»:

> Рекурсивный спуск тотальным не доказывается: анализ завершаемости
> (`flang/src/totality.mjs`) видит убывание только по прямому аргументу, а
> «остаток токенов стал короче» частью значения не считает. …

Ссылка на модуль свидетеля убрана, само объяснение осталось.

### «Слово»: история перевода со свёртки на встроенную форму и замер витками

Было у функции «Слово», двумя абзацами и таблицей (убраны целиком):

> БРАЛОСЬ ОБХОДОМ, БЕРЁТСЯ ФОРМОЙ. Свёртка шла по всему списку при ЛЮБОМ
> номере — даже при первом. А первым его и зовут чаще всего: «Начинается
> словами» ниже рекурсивно спрашивает «Слово … и 1», отбрасывая по слову, и
> платила за K слов образца по длине W всей строки — то есть K×W там, где
> достаточно K.
>
> Замер витками интерпретатора (наименьший бюджет шагов, при котором
> вычисление доходит; секунды на одной точке про класс не говорят ничего):
>
>                                     | 250 слов | 1000 слов | показатель
>       «Слово» и 1           обходом |     6024 |    24 024 |      0,998
>                              формой |       39 |        39 |      0,000
>       «Слово с конца» и 1   обходом |     6039 |    24 039 |      0,996
>                              формой |       54 |        54 |      0,000
>       «Имя по номеру» и 1   обходом |     6014 |    24 014 |      0,999
>                              формой |       33 |        33 |      0,000
>       «Начинается словами»  обходом |   47 957 |   191 957 |      1,000
>        (образец из восьми)   формой |      749 |       749 |      0,000
>
> Обход стоил длины строки за первое же слово; форма не стоит ничего. На
> тысяче слов «Начинается словами» дешевле в 256 раз.

Врезка «СТОРОЖ ПЕРЕД ФОРМОЙ НЕ ПЕРЕСТРАХОВКА» осталась в исходнике: она
объясняет, зачем перед встроенной формой стоит проверка. Из неё убраны две
отсылки к прежней редакции:

> … Поэтому прежняя свёртка оставлена ровно для тех номеров, на которых ответы
> расходятся, и достаётся им буква в букву прежняя.

> Сюда же попадает −0: … номер уходит в свёртку и получает "" — как получал
> раньше. …

### «Или» и «Оба»: почему их не заменили инфиксной записью

Было над функцией «Или» (убрано целиком, оставлено только высказывание про
голое `и`):

> Здесь стояло «логических «и»/«или» в языке нет — есть вложенное «если»». Это
> перестало быть правдой 9 августа 2026: инфиксными стали `или`, `не` и
> составное `и притом`, и разбираются они все три. Голое `и` инфиксной
> конъюнкцией не стало и не станет — оно разделяет аргументы вызова, и
> `«Ф» от а и б` обязано и дальше значить два аргумента.
>
> Две функции ниже оставлены, и языковой причины у этого больше нет: инфиксную
> запись разбирает и `src/parser.mjs`, и `self/parser.flang` (проверено
> прогоном обоих — корпус в `flang/test/self-parser.test.mjs`). Оставлены они
> потому, что уже написаны, вызваны из многих мест этого файла и покрыты
> примерами; замена вызова на инфикс дала бы то же самое дерево и здесь не
> делалась.

Осталось: «Инфиксные в языке — `или`, `не` и составное `и притом`, и разбираются
они все три. Голое `и` инфиксной конъюнкцией не стало и не станет — оно
разделяет аргументы вызова, и `«Ф» от а и б` обязано и дальше значить два
аргумента.»

### «Псевдоним типа»: долг «нельзя» оказался долгом «не написано»

Было у функции «Псевдоним типа», вторым абзацем (убран; первый абзац и запись
про «Долги» остались):

> Здесь стояло, что причина — «формы «код символа» в языке нет». Это перестало
> быть правдой 9 августа 2026, и вместе с формой ушла сама причина: перевод
> кода в шестнадцатеричную запись на этом языке ПИШЕТСЯ и доказывается
> тотальным — `код символа`, деление с остатком и `символ N в
> "0123456789abcdef"`, проверено прогоном на «A», «a», «я» и «0» против
> свидетеля. Значит долг здесь не «нельзя», а «не написано», и это разные вещи:
> первое оправдание, второе задача. Ни одна модель репозитория в эту ветку не
> заходит; расхождение записано в «Долги».

### «Имя по номеру»: ссылка на тот же перевод, что у «Слова»

Было у функции «Имя по номеру», началом абзаца:

> Имя по номеру, считая с единицы. Тот же перевод и по той же причине, что у
> «Слова» выше: свёртка шла по всему списку при любом номере, а зовут её
> номерами 1 и 2 — … Договор сохранён сторожем: вне списка и на дробном номере
> встроенная форма ОТКАЗЫВАЕТ, а свёртка отдавала пустую строку, и на этих
> номерах работает по-прежнему она. …

Объяснение сторожа осталось; убрана отсылка к прежней редакции «Слова».

## `flang/self/builtins.flang`


Словарь встроенных форм. Файл уже правила ячейка Ч334; это её остаток.

### Замер разницы `Object.is` и `===` прогоном

Было в шапке раздела «равенство значений»:

> Разница ровно в
> двух местах, и оба измерены прогоном на этом самом языке:

### Минус ноль кусал трижды за сутки: «Чётное», «Делится на», «Уникальные»

Было там же, отдельным предложением:

> Второе кусало трижды за сутки: так сломались «Чётное», «Делится на» и
> «Уникальные».

### Сравнение записей объяснялось через свидетеля

Было там же, последним предложением абзаца о порядке ключей:

> Свидетель спрашивает счёт
> ключей и присутствие каждого левого ключа справа — то же и здесь.

## `flang/self/carriers.flang`


Чем доказано завершение — наружу, из самоприменённого анализа.

### Зачем отдельный файл: свидетель отдавал циклы и структуры сам

Было в шапке файла:

> Свидетель отдаёт это сам: `checkTotality` (`flang/src/totality.mjs`) возвращает
> `cycles` и `structures` вместе с диагностиками, а `externalChecks`
> (`flang/bin/flang.mjs`) отдаёт наружу весь `results`, а не один
> `diagnostics`.

и оттуда же — довод про JavaScript:

> В самоприменённом слое такого не было: `«Итог тотальности»`
> (`self/totality.flang`) отдаёт `«доказано»`, `«диагностики»`, `«тотальные»` и
> `«меры»`, а способ доказательства считает и выбрасывает. Значит ведомость на
> flang кормить было нечем, и всякий её носитель приходил бы из JavaScript —
> то есть «доказуемость» так и осталась бы «доказуемостью при наличии Node».

Само объяснение («без циклов снаружи нельзя отличить доказано композицией от
доказано структурой», «отчёт живёт здесь, а не в самом анализе») в файле
осталось.

### Цитата свидетеля о том, почему признак цикла считается заново

Было в шапке файла («Почему здесь не появляется второго ответа»):

> Ровно так
> же поступает свидетель с признаком цикла: «Считается он здесь заново, а не
> возвращается из checkComponent, чтобы порядок проверок в ней не зависел от
> учёта» (`src/totality.mjs`).

### Мелкие ссылки на свидетеля

Было у формы отчёта о структурном убывании:

> Свидетель держит то же самое в `structures` и ровно
> затем же — снаружи назвать этот аргумент нечем.

Было у отбора доказанных циклов:

> называть её рекурсию доказанной было
> бы неправдой — та же причина, по которой свидетель фильтрует и сторожей.

## `flang/self/cli.flang`


Чистая половина командной строки flang.

### Улика: разметка свидетеля по областям — где прошла граница переносимого

Было в шапке файла — раздел целиком:

> ── Улика: где именно прошла граница ───────────────────────────────────────
>
> Свидетель `flang/bin/flang.mjs` — 1316 строк, из них 537 комментарий и 79
> пустых; кода 700 строк. Разметка по областям (все 35 областей файла):
>
>   чистое      258 строк  разбор доводов (101), справка (53), отказы (23),
>                          строение программы (26), отказ .fts (16), связывание
>                          доводов (8), ключи печати (8), пределы (6), файлы
>                          печати (7), список целей (3), отсев предупреждений (7)
>   эффект      238 строк  externalChecks (52), оболочка (54), io (28), разбор
>                          файла (28), отметки (18), запуск (12), ввоз (9),
>                          readInput/writeJson (9), emitTargets (9), загрузка
>                          программы (14), ast (4), версия (1)
>   смешанное   203 строки main (43), commandEmit (46), commandCheck (28),
>                          commandTest (21), loadEmitter (18), commandRun (12),
>                          loadPlacement (12), readProgram (10), facts (9),
>                          checkProgram (4)
>
> То есть переносимо примерно 258 строк наверняка и та доля из 203 смешанных,
> где решение отделимо от вызова: выбор команды, форма ответа, выбор кода
> возврата. Эффектных 238 строк не переносимо НИ ОДНОЙ — и эталон их не
> изображает.

### Критерий верности и корпус вызовов: 177 вызовов, расхождений 0

Было в шапке файла — раздел целиком:

> ── Критерий верности — побайтовый ─────────────────────────────────────────
>
> Тот же, каким проверены слои самоприменения до этого: один и тот же вызов,
> поданный в `flang/bin/flang.mjs` и сюда, обязан дать те же байты и тот же код
> возврата. Сверка — `flang/test/self-cli-args.test.mjs`, корпус — настоящие
> вызовы, а не три рукописных: 177 вызовов, расхождений 0, из них 160 эталон
> решает сам, а 17 честно отдаёт хозяину — все семнадцать требуют прочесть файл.

### История справки: что было до двух этажей

Было у справки:

> СПРАВКА В ДВА ЭТАЖА — та же, что у свидетеля, знак в знак.
> … Прежняя справка вываливала восемь команд с двумя
> десятками ключей сразу, и первым, что видел человек, была простыня.

### Расхождение с `Number("1e3")`, найденное сверкой на корпусе

Было у разбора числа одним проходом:

> Проход завёлся не для красоты: разбор по точке не знал показателя, и
> `--max-steps 1e3` эталон отвергал, а `Number("1e3")` свидетеля давал 1000 и
> вызов проходил дальше. Расхождение нашла сверка на корпусе — ровно то, ради
> чего корпус и строится перечислением, а не пишется руками.

Само решение (проход с состоянием вместо разбора по точке) и его довод в файле
остались.

### История голой команды: раньше печаталась справка

Было у выбора команды по умолчанию:

> ГОЛАЯ КОМАНДА ОТКРЫВАЕТ ОБОЛОЧКУ — как `iex` у Elixir, как `python`. Раньше
> здесь стояло "help", и `flang` печатал простыню использования: инструмент, у
> которого главный вход — справка о других входах.

### Мелкие ссылки на свидетеля, снятые по одной

Было у разных объявлений — журнальная половина вычеркнута, поведенческая
осталась:

> Всё, что ТРОГАЕТ мир, остаётся хозяину: `flang/bin/flang.mjs` читает файлы,
> пишет каталоги, ввозит модули на ходу, гоняет терминал оболочки.

> «вид» — что вышло: … Различать их
> приходится, потому что свидетель различает: `--args` требует объект…

> Ключи вызова — то же, что `options` свидетеля, но записью с явными признаками
> «задан ли ключ» вместо `undefined`. … у свидетеля `--no-check` без значения и отсутствие ключа ведут к
> разным отказам.

> Разбор числа тем же срезом, каким его читает `Number()` свидетеля…
> Дальше свидетель спрашивает `Number.isFinite` или `Number.isInteger`…

> Значение берётся встроенной `к числу` — тем же преобразованием, каким его
> берёт `Number()` свидетеля.

> Знаки вне ASCII свидетель НЕ экранирует, и здесь они тоже проходят
> насквозь…

> Отказ вызова целиком: то, что печатает `errorResult` свидетеля, пропущенное
> через `JSON.stringify(…, null, красиво ? 2 : 0)` … а
> сверка идёт по байтам.

> У свидетеля они и есть семейства: «требует значение
> строкой», «требует значение числом», «требует JSON», «переключатель»,
> «сужение полномочий».

> Текст требования — знак в знак то, что свидетель отдаёт `require_`.

Последняя строка была единственным комментарием у функции «Требование ключа» и
ушла целиком: без свидетеля она ничего не объясняла.

> Требование и негодность —
> два разных отказа у свидетеля, и здесь они тоже врозь.

> Отказ по JSON-ключу: сперва сам разбор, потом форма. Порядок тот же, что у
> свидетеля, и он виден…

> Строковые ключи
> негодными не бывают — свидетель берёт любой довод… это не оплошность разбора, а
> его поведение, которое сверка обязана повторить.

> Обход доводов слева направо, ровно как `for` свидетеля.

> Свидетель отказывает на таком, и порядок проверок у него ровно этот:
> сперва `--proof`, потом `--json`…

> Всё, что дальше, начинается с `loadProgram`, то есть с чтения файла, и
> потому остаётся хозяину.

> Порядок тот же, что у свидетеля.

> Отказ РАЗБОРА свидетель печатает сжато всегда — `writeJson(…, false, stderr)`, —
> а отказ команды уважает `--pretty`.

## `flang/self/commutative.flang`


Закон коммутативности. Проверка объявленного свойства `а∘б = б∘а`.

### Шапка называла файл копией свидетеля на JavaScript

Было в шапке файла:

> // Коммутативность: `а∘б = б∘а`. Копия `flang/src/commutative.mjs`.

Файла `flang/src/commutative.mjs` в дереве больше нет.

### Ссылка на `types.mjs` в разделе «Что здесь ПРОВЕРЯЕТСЯ, а не доказывается»

Было в шапке файла:

> // ── Что здесь ПРОВЕРЯЕТСЯ, а не доказывается ───────────────────────────────
> // Закон. Устройство доказано раньше и в другом месте (`types.mjs`). Здесь —
> // равенство вычислений на всех парах значений носителя, а оно неразрешимо:
> // значит конечная сетка, квадратичная по ней, и ведомость обязана называть её
> // размер строкой, оканчивающейся словами «Это не доказательство».

Файла `flang/src/types.mjs` в дереве нет; объяснение оставлено без ссылки.

### Приём свидетеля, которым выбирались уезжающие наружу поля

Было у объекта «Проверенная коммутативность»:

> // «нарушений» и «оборвано» уезжают наружу ТОЛЬКО когда им есть что сказать —
> // решает это перевод в форму свидетеля (`flang/test/self-svoystva.test.mjs`), тем
> // же приёмом, каким свидетель пишет `...(нарушений === 0 ? {} : { violations })`.
> // Ноль и пустая строка здесь означают «не уезжает»: необязательных полей записи
> // у языка нет, и заводить их ради сериализации значило бы менять язык под отчёт.

Ни `flang/test/self-svoystva.test.mjs`, ни свидетеля в дереве нет.

### Обрыв назван через `break` свидетеля

Было у объекта «Ход пар»:

> // Обрыв — это `break пары` свидетеля: дальше по сетке не идём. Своего слова для
> // него в языке нет и не нужно — признак в накопителе свёртки говорит ровно то
> // же и виден в сигнатуре.

### Ранний выход сверялся с ранним выходом свидетеля

Было у функции «Проверить коммутативность»:

> // Программа без единого объявления отдаёт четыре пустых списка — тот же ранний
> // выход, что у свидетеля, и он не украшение: почти весь корпус языка именно
> // таков, и молчание на нём обязано быть настоящим.

---

## `flang/self/conc.flang`


Планировщик конкурентности. Ч334 файл уже правила; здесь остаток.

### Веха «до В2» как мера неизменности поведения

Было в шапке файла, в списке «Чего здесь НЕТ»:

> • ВЫТЕСНЕНИЕ ПО КВАНТУ и ПОТОЛОК ЗАДЕРЖКИ. Без кванта планировщик обязан
>   вести себя ровно так, как вёл до В2, иначе побайтовая сверка
>   напечатанного C перестала бы что-либо значить.

### Тотальность обработчиков по корпусу процессов

Было в шапке файла, в разделе «Что про планировщик ДОКАЗАНО, а что не выразимо»:

> тотальность обработчика доказывает сам flang, по программе, и по корпусу
> процессов это 22 из 23 (единственный недоказанный — «разобрать пакет» в
> `conc/examples/budget.flang`, у него объявлен запас 2000 витков, и это
> нарочно).

### Разобранный тупик: попытка научить ядро читать условие как факт в ветви

Было там же, у «НАДЗОР ПЕРЕЗАПУСКАЕТ РОВНО УПАВШЕГО»:

> а ядро не читает условие как факт в
> ветви (мерено: попытка научить дала ноль закрытых целей).

### Разрыв на стволе, найденный замкнутой целью

Было в шапке файла и, вторым разом, у функции «Адресат поручения»:

> тело «Адресата поручения» спрашивает словарь, а цель называет ОТВЕТ, — и
> разрыв ровно этого вида на стволе и был.

> упадёт НАРУШЕНИЕМ, не дожидаясь сверки. Разрыв ровно этого вида на стволе и
> был: «Поле процесса у действия» отвечало на «поручить» пустой строкой.

### Цена переноса генератора чередований

Было у функции «Исключающее или»:

> и это единственная известная цена переноса: тысяча действий на
> вызов генератора вместо шести.

(Тот же счёт остаётся в шапке файла, решение 3, — там он не о переносе.)

---

## `flang/self/defunc.flang`


Файл уже правила ячейка Ч334; остаток — один замер.

### Цена лишнего полного обхода — сто миллионов шагов на 3300 строках

Было при списке текстов отказов (довод остался, ушли числа):

> …за него платила бы каждая программа, включая те, где числовой меры нет
> вовсе, — печать `self/emit-c.flang` (3300 строк) на лишнем полном обходе
> вышла за сто миллионов шагов.

## `flang/self/distributed.flang`


### Что и почему вынуто из `flang/conc/distributed.mjs` — 996 строк

Было в шапке файла:

> // `flang/conc/distributed.mjs` — 996 строк, эталона у них не было. Из этих 996
> // строк МИР занимает большинство: `node:net`, `setInterval`, `setTimeout`,
> // `Date.now`. Мир в язык не втаскивают — у программы на flang есть аргументы и
> // результат, больше ничего, — поэтому эталон берёт ровно ту часть свидетеля, у
> // которой нет ни сокета, ни часов, ни изменяемого состояния:

Там же, в перечне вынутого, стояли имена функций свидетеля:

> //   • ЗНАЧЕНИЯ НА ПРОВОДЕ — `закодировать` целиком, вместе с `NaN`, обеими
> //   • РАЗМЕЩЕНИЕ — `размещениеУзла`: обращение «узел → процессы» в
> //     «процесс → узел» и поимённая ловля процесса, размещённого дважды;
> //   • АДРЕС — `разделитьАдрес`;

И там же — побайтовая сверка на корпусе:

> // «Значение» из `core/json.flang`, та же побайтовая сверка на корпусе
> // (`flang/test/self-distributed.test.mjs`), тот же отказ выдумывать своё.

### Замер подъёма соседа: 1233–1432 мс, медиана 1338

Было у «Срок первого знакомства»:

> // Тридцать сроков, а не полтора, и число это измерено, а не выбрано на вкус:
> // подъём соседа — это ПЕЧАТЬ его программы. На этой машине узел примера
> // поднимается за 1233–1432 мс (12 замеров, медиана 1338), а печать самой
> // крупной программы дерева стоит 3,1–3,5 с. 30 000 мс — это 21 худший подъём
> // примера и восемь печатей самой крупной программы.

Первая половина довода («подъём соседа — это ПЕЧАТЬ его программы») осталась.

### Дата появления состояния «не состоялась»

Было у списка состояний связи:

> // было ни разу. Оно появилось 20 августа, и до него это событие было НЕ
> // НАЗВАНО, то есть невидимо: узел, чей сосед не поднимется никогда, ждал его
> // вечно и молча.

### Сверки со свидетелем, снятые построчно

Было у «Сроков», «Периода сторожа», «Видов кадров», «Сбора потока», «Пуста по
сути», «Числа на проводе», «Это вариант», «Собрать вариант», «Кадры отправки»,
«Пары размещения», «Имя, встреченное дважды» и «Замкнуть моноиды»:

> // Три числа, и все три у свидетеля — доводы узла, а не постоянные кода:

> // Как часто сторож просыпается: `Math.max(20, Math.floor(срок / 5))` свидетеля.

> // Четыре вида, список закрытый. Порядок — тот же, в каком они объявлены у
> // свидетеля: `привет`, `письмо`, `пульс`, `отбой`.

> // поток на входе, кадры и остаток на выходе. Свидетель держит остаток в
> // замыкании (`собиратель` в `distributed.mjs`), эталон возвращает его значением,
> // и это то же самое, сказанное чисто.

> // `строка.trim() !== ""` свидетеля, сказанное без `trim`: строка пуста по сути,
> // если в ней не осталось ни одного непробельного знака.

> // Число на проводе: четыре значения, которых JSON не умеет, уезжают строками с
> // теми же именами, что у свидетеля, остальные — самими собой.

> // `variant` СТРОКА и есть поле `fields`. Ровно этот вопрос задаёт свидетель, и
> // задаёт его в этом порядке.

> // узнаёт «Это вариант»: сначала имя, потом поля. Свидетель на этом месте зовёт
> // конструктор напечатанного модуля — эталону конструктор взять негде, и он
> // возвращает ровно ту запись, которую «Закодировать» принимает обратно.

> // его негде взять: подтверждений свидетель не ждёт, номеров не ведёт, таблицы
> // виденного не держит.

> // Пары «процесс → узел» в том же порядке, в каком их кладёт свидетель: по узлам
> // файла, внутри узла — по списку процессов.

> // адресом: два узла разошлись бы в том, кому идёт письмо. Свидетель здесь
> // отказывает и НАЗЫВАЕТ процесс; эталон возвращает его имя, а «никто не
> // размещён дважды» — пустая строка.

> // Единицы и узлы идут парами по порядку объявления: свидетель берёт их
> // `единицы[номер]`,

### Раскодирование: чего у эталона не было и два расхождения со свидетелем

Было в шапке «раскодирование»:

> // Обратная сторона провода. У свидетеля она есть (`раскодировать`, 31 строка
> // кода), у эталона её не было вовсе — и это не мелочь: без неё цель печати
> // умеет ПОСЛАТЬ письмо и не умеет его ПРИНЯТЬ, то есть узлом быть не может.
> //
> // Расхождение со свидетелем названо сразу, потому что оно принципиальное:
> // свидетель на неизвестной метке БРОСАЕТ, а тотальная функция бросать не умеет
> // и не должна — отказ здесь значение, а не исключение.
> //
> // Второе расхождение — на ПОРЧЕНОМ проводе, и оно тоже названо: кадр вида
> // `["с"]` (метка есть, груза нет) свидетель превратит в строку "undefined", а
> // эталон вернёт «ничто». Сойтись здесь нельзя, не заведя в языке `undefined`,
> // и сходиться незачем: такой кадр «Закодировать» не порождает ни при каком
> // входе. Сверка идёт на том, что порождено кодированием.

### Первая редакция утверждения о размещении

Было у «Размещение однозначно»:

> // Утверждение здесь НЕ тавтология, и это стоит сказать вслух, потому что
> // тавтология была первой редакцией и принималась даром:

### Надзоры из плана: последние двенадцать строк свидетеля и чем это сверялось

Было во врезке «надзоры из плана»:

> // Последние двенадцать строк свидетеля, у которых нет ни сокета, ни часов.

> // парами: `процессы: [[имя, стратегия]]`. Дерево надзоров строит общий со
> // свидетелем `построитьНадзор` (`flang/src/conc.mjs`) — тот же самый код, что
> // у прогона внутри одного узла, — а он читает записи: `watch: [{process,
> // strategy}]`. Весь перевод в этом, и он ЗДЕСЬ, а не вторым экземпляром дерева:
> // дерево, порог и «выше» считает свидетельский код.
> //
> // ── Чем это сверяется, раз свидетель наружу не отдан ────────────────────────
> // `надзорыИзПлана` из `distributed.mjs` не экспортирована, и дописать ей
> // `export` значило бы править чужой файл ради собственной проверки. Сверять
> // есть с чем ЛУЧШИМ: с выходом РАЗБОРА той же программы. Печать теряет ровно
> // одно — положения в исходнике; всё остальное обязано вернуться байт в байт.
> // Перепутанная стратегия, потерянный вложенный надзор, порог, съехавший на
> // соседнее поле, — всё это выйдет наружу расхождением.

---

## `flang/self/distributive.flang`


Закон дистрибутивности. Свидетеля `flang/src/distributive.mjs` в дереве больше
нет.

### Файл был копией модуля-свидетеля

Было в шапке файла, первой строкой:

> Дистрибутивность: `а×(б+в) = а×б + а×в`. Копия `flang/src/distributive.mjs`.

Осталось: «Дистрибутивность: `а×(б+в) = а×б + а×в`.»

### Где доказано устройство

Было в шапке файла, врезка «Что здесь ПРОВЕРЯЕТСЯ, а не доказывается»:

> Закон. Устройство доказано раньше и в другом месте (`types.mjs`). Здесь — …

Осталось: «Устройство доказано раньше и в другом месте.»

### Сборка первого отказа у свидетеля

Было у функции «Первый отказ из трёх», единственной строкой комментария
(убрана целиком):

> `целиком.ошибка ?? первая.ошибка ?? вторая.ошибка` свидетеля.

### Почему сумма считается один раз

Было у функции «Сверить тройку»:

> Сумма считается ОДИН раз на тройку и идёт в обе стороны: у свидетеля она тоже
> вычисляется до перебора сторон, и посчитать её дважды значило бы спросить
> таблицу дважды об одном.

Осталось: «Сумма считается ОДИН раз на тройку и идёт в обе стороны: посчитать её
дважды значило бы спросить таблицу дважды об одном.»

### Откуда взялся порядок «сначала левая, потом правая»

Было у функции «Обе стороны»:

> Сначала левая, потом правая — тот же порядок, в каком свидетель перебирает пары
> «сторона, как собрать». Он виден в тексте диагностики, поэтому переставить его
> нельзя, не переставив отчёт.

Осталось: «Сначала левая, потом правая. Этот порядок виден в тексте диагностики,
поэтому переставить его нельзя, не переставив отчёт.»

## `flang/self/emit-c.flang`


### Критерий верности — побайтовое совпадение со свидетелем

Было в шапке файла целой врезкой; убрано целиком вместе с заголовком:

> // ── Критерий верности ──────────────────────────────────────────────────────
> // Один и он побайтовый: для каждой программы репозитория текст, который даёт
> // «Печать программы», обязан совпасть с тем, что печатает `flang/src/emit/
> // c.mjs`, байт в байт — включая комментарии, пробелы и порядок временных имён.
> // Не «компилируется и работает»: этот критерий не отличает правильную печать от
> // случайно похожей. Проверка — `flang/test/self-emit-c.test.mjs`.

### Почему AST не типизирован: свидетель и тест, который проверял бы себя

Было в шапке файла, решение 1:

> // 1. AST приходит как обычное значение JSON — сумма типов «Значение» из
> //    `flang/core/json.flang`, уже доказанная побайтово на 56 моделях. Своих
> //    типов под AST здесь нет намеренно: свидетель (`c.mjs`) тоже работает с
> //    голым объектом, различает «ключа нет» и «ключ со значением ничто», и
> //    принимает `bind` образца то записью, то списком. Типизированный AST
> //    пришлось бы собирать в тесте — то есть тест проверял бы себя.

Осталось в исходнике без ссылок на свидетеля и без счёта моделей.

### Что у свидетеля лежало в замыкании

Было в шапке файла (решение 2) и у объекта «Состояние»:

> // 2. Состояния в языке нет, поэтому всё, что в `c.mjs` лежит в замыкании
> //    (счётчик временных имён, таблица констант файла, множество занятых
> //    идентификаторов, флаг «нужен math.h»), едет записью «Состояние» через
> //    каждую функцию печати и возвращается изменённым.

> // Всё, что в свидетеле лежит в замыкании: счётчик временных имён, константы
> // уровня файла, признак «нужен math.h», занятые идентификаторы тела и первая
> // ошибка печати. Едет через каждую функцию и возвращается изменённым.

### Замер круга взаимной рекурсии: 51 функция, 92 ребра

Было в шапке файла, раздел «Тотальность»:

> // Обход AST вглубь — это один круг взаимной рекурсии; замером на дереве в нём
> // 51 функция. Правило анализа: вдоль круга ОДНА И ТА ЖЕ позиция довода обязана
> // убывать на КАЖДОМ ребре, а «убывать» значит «быть строгой частью параметра».
> // Из 92 рёбер круга 33 уже спуск (он спрятан за «Взять поле», и его увидит
> // типизация узла), 44 везут узел неизменным, 4 везут первым доводом накопитель
> // свёртки.

Правило анализа осталось в исходнике; счёт функций и рёбер убран.

Тот же счёт стоял ещё раз, в шапке «литеральное значение»:

> // Раньше литерал и выражение ходили по одним и тем же «Печать списка» и
> // «Печать полей» с признаком «литерал», и из-за этого литеральный обход попадал
> // в общий круг взаимной рекурсии на 51 функцию — и не доказывался вместе с ним.

и там же, в конце той же шапки:

> // Печать этим не изменилась ни на байт: порядок временных имён, порядок строк
> // и текст те же, что были у свёртки.

### Сверки со свидетелем и с удалёнными `.mjs`, снятые построчно

Было у «Взять поле», «Это истина», «Элементы узла», «Замены кириллицы»,
«Слова», «Змейка», «Положить в таблицу», «Символы слова», «Замены C»,
«Число C», «Уникальные идентификаторы», «Подготовка программы»,
«Собрать предусловие», «Слить запись типа», «Тело пусть», «Компоненты
связности», «Отсутствующее поле варианта», «Вызов по имени» и «Объявленные
типы параметров»:

> // Значение ключа; отсутствующий ключ даёт «ничто» — так же, как `node.поле`
> // в JavaScript даёт undefined, а `undefined` свидетель везде сводит к «ничто».

> // `fn.total === true` свидетеля: истинно только для настоящего признака «да».

> // Элементы списка; всё, что списком не является, даёт пустой список — свидетель
> // в таких местах пишет `?? []`.

> // Транслитерация из `flang/src/naming.mjs`. Обход по символам не нужен:

> // Разбиение на слова: `words` из naming.mjs — всё, что не буква и не цифра,
> // служит разделителем.

> // `snake` из naming.mjs: транслитерация, слова, нижний регистр, подчёркивания.

> // Как `Map.set`: существующий ключ обновляется на месте, новый дописывается в
> // конец — порядок пар обязан совпадать с порядком вставки в свидетеле.

> // Свидетель гасит неиспользованные переменные `(void)имя;`, а «использована ли»
> // проверяет регулярным выражением `\bимя\b`. Подстроки мало: `fl_t1` входит в
> // `fl_t12`. Поэтому здесь честная граница слова — символ слева и справа не
> // должен быть из [A-Za-z0-9_], ровно как `\w` в JavaScript.

> // тот же результат, что посимвольный обход свидетеля, и остаётся тотальной.

> // Число как литерал C: `String(значение)` — кратчайшая запись, читающаяся
> // обратно тем же double, и strtod компилятора обязан её так и прочитать.

> // Минус ноль отличается по напечатанному виду, а не сравнением: `String(-0)`
> // это «0», и единственное значение с такой записью — сам ноль, поэтому
> // проверка «текст равен 0, а значение не равно 0» и есть Object.is(-0).

> // `uniqueIdents` свидетеля: транслитерация плюс развод столкновений.

> // Повторяет `prepare` свидетеля, а тот — `prepareProgram` интерпретатора: те же
> // проверки, те же коды и тексты. Разница только во времени срабатывания — при
> // печати, а не при вычислении.

> // Тем же разбором и с теми же умолчаниями, что у интерпретатора
> // («normalizePreconditions» в src/interpret.mjs): код FLANG_PRECONDITION, текст
> // «не выполнено требование …».

> // `Map.set`: повтор имени обновляет значение, но не переставляет ключ.

> // `expr.in ?? expr.body`: у `пусть` тело лежит то под одним ключом, то под
> // другим — свидетель принимает оба, и печать обязана принимать оба.

> // Компоненты сильной связности. Свидетель разворачивает рекурсию в явный стек,
> // чтобы не упасть на глубине графа вызовов; здесь рекурсия честная, а глубина
> // ограничена числом функций программы — то есть тем же, чем ограничен свидетель.

> // не подошёл»: так же ведёт себя matchPattern интерпретатора.

> // FFI. Ровно так же устроен интерпретатор: `callFunction` (src/interpret.mjs) —
> // дверь и проверяет, `applyFunction` — нет.

> // параметра объявленный тип, — работа слоя типов (`src/types.mjs`,
> // `таблицаВхода`; на flang — `self/types.flang`), а не печати в C. Печать здесь
> // отвечает за то, что эти данные лягут в C правильными байтами, и вот это
> // сверяется со свидетелем побайтово.

> // условие отказа ОДНО И ТО ЖЕ — «длина ноль». Свидетель держит тот же список
> // одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в `src/builtins.mjs`), и разойтись им
> // нельзя: печать сверяется со свидетелем побайтово.

> // анализ (`src/types.mjs`), и здесь она только читается.

> // Доказывает не печать — отметку кладёт анализ (`src/types.mjs`, `markProven`),
> // и здесь она только читается.

> // Программа без единого `требует` не получает отсюда ни строки и ни одного
> // временного имени — печать обязана остаться побайтово прежней, иначе рухнула
> // бы сверка со свидетелем на всём корпусе.

> // вовсе. Ровно там же зовёт проход и свидетель (`emitC`), и это не совпадение, а
> // условие побайтовой сверки: понизь программу в другом месте — и печать
> // разошлась бы на первой же программе с функцией-значением.

> // объявленных типов строит слой типов свидетеля, и взять её больше неоткуда.

> // выдаёт (ни одна встроенная форма не даёт код символа — счёт формам стоит в
> // `flang/test/builtins.test.mjs`, а не в этом комментарии),

### Свидетель этих же наборов имён, занятых целью

Было над «Занято именем модуля C»; абзац убран целиком:

> // Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> // стороны без другой молчит до первой подходящей формы в корпусе.

### История обхода по индексу — дважды

Было у «Слова»:

> // Здесь стоял обход по индексу — двумя взаимно рекурсивными функциями, — и он
> // не доказывался: убывала разность «длина минус индекс», то есть число, а
> // анализ (`src/totality.mjs`) числа частью значения не считает. Теперь строка
> // раскладывается в список символов встроенной формой, и обход стал свёрткой по
> // готовому списку: свёртка конечна по построению, доказывать нечего.
> // Заодно исчез повод для приёма «итог через пусть»: компоненты взаимной
> // хвостовой рекурсии, на которой бэкенд C печатал шаг батута с
> // неиспользованным `result`, больше нет.

Было у «Байтов в строке»:

> // Раньше здесь шёл обход по индексу («Байтов в строке с»), и он не
> // доказывался: убывала разность «длина минус индекс». Разложение строки в
> // список символов сводит его к свёртке, а свёртка конечна по построению.

Было у «Гашение неиспользованных»:

> // Проход по индексу заменён свёрткой по списку символов:

### Дата, до которой у `обеспечивает` не было продукции в разборщике

Было у «Байтов у символа»:

> // Утверждение о поведении, и оно первое в самоприменённом компиляторе: до
> // 15 августа 2026 у слова `обеспечивает` не было продукции в
> // `flang/self/parser.flang`, а этот каталог входит в корпус побайтовой
> // неподвижной точки целиком — строка ниже развалила бы сверку разборщика.

### Длинная постоянная: прогон 29 августа 2026 и замеры компиляторов

Было во врезке «длинная постоянная едет октетным массивом»:

> // Ровно это и случилось 29 августа 2026. Перепечатка дошла до конца впервые за
> // заход — 6 ч 23 мин, код 0, семя 40,7 МБ, — и `make -C bootstrap` упал за
> // 37,30 с на `compiler_flang.c:5944`: постоянная в 4614 байт, пришедшая из
> // `flang/self/proof-kernel.flang`. Семя при этом годное: без `-pedantic` оно
> // собирается за 101,59 с и двоичный работает. Сломана была ПАРА — печать и её
> // собственное обещание.
> //
> // РАЗРЕЗАТЬ НА СОСЕДНИЕ ПОСТОЯННЫЕ НЕ ПОМОГАЕТ, и это замер, а не мнение.
> // В C соседние строковые постоянные склеиваются сами, и первым делом хочется
> // написать `"а" "б"`. Предел назван «после конкатенации», и оба компилятора
> // считают именно так — проверено на той самой постоянной в 4614 байт:
> //
> //   цельная                      gcc 15.2.0: ошибка, 4614 против 4095
> //   разрезанная на три соседних  gcc 15.2.0: ТА ЖЕ ошибка, те же 4614
> //   цельная                      clang: ошибка та же, слово в слово

> // Массив БЕЗЗНАКОВЫЙ, а указатель приводится, и это тоже замер: октет доходит

> // Цена платится только теми постоянными, что не влезли: 4614 байт текста дают
> // 18 052 байта исходника вместо 4669. На семени 29 августа такая постоянная
> // одна из 6391, и вторая по длине — 3501 байт, под пределом.

Было у «Объявление текста» (тот же счёт постоянных в семени):

> // КАЖДОЙ постоянной значило бы заплатить эту цену 6391 раз (столько текстовых
> // постоянных в семени 29 августа 2026) ради одной, которая не влезла. Здесь
> // они считаются только в той ветке, куда дошло.

### Переезд списка частичных форм из `self/failures.flang`, 20 августа

Было во врезке «Восемь частичных форм и МЕСТА, которые они занимают в теле»:

> // До 20 августа список и предикат жили в `self/failures.flang`, и оттуда их
> // не видел никто: тот модуль не подключён ни к чему, а подключить его к
> // ведомости нельзя — шесть его имён сталкиваются с уже связанными модулями.
> // Переезд сюда оставляет ОДНО определение на всех и делает счёт мест доступным
> // ведомости доказательства.

### Замер квадратичности «Элемента по номеру»

Было в шапке «Элемент по номеру с НУЛЯ»:

> // Замер витками интерпретатора (наименьший бюджет шагов, при котором печать
> // доходит; секунды на одной точке про класс не говорят ничего). Печать
> // программы с записью из N полей и функцией из N параметров:
> //
> //     N |        ДО |     ПОСЛЕ
> //    25 |   506 398 |   412 023
> //    50 | 1 189 598 |   807 098
> //   100 | 3 182 377 | 1 642 377
> //   200 | 9 685 577 | 3 505 577
> //
> // Показатель роста на 25→200: был 1,419, стал 1,030. Линейная доля осталась —
> // печать N полей и не может стоить меньше N, — а квадратичная ушла: на двухстах
> // полях дешевле в 2,76 раза, и чем шире запись, тем больше разрыв.
> //
> // Само взятие мерилось отдельно, на худшем для обхода входе — последнем
> // элементе: обходом 7767 витков на 250 элементах и 31 017 на 1000, показатель
> // 0,999 — линия; формой 37 и 37, показатель 0,000 — постоянная.

Там же, в объяснении сторожа перед формой:

> // −0: `равен` — это `Object.is`, `(−0 остаток от 1) равен 0` ложно, номер
> // уходит в обход и получает "" — как получал раньше. Прямая подмена отдала бы
> // на −0 ПЕРВЫЙ элемент, потому что `Number.isInteger(−0)` истинно, а

### Дефект печати, найденный сверкой, и его исправление в свидетеле

Было у «Обход хвоста ветвей»:

> // Итог возвращается через «пусть», а не прямо из ветки, и это не украшение.
> // Приём пришёл из дефекта печати в C, который нашла эта сверка: компонента
> // взаимной хвостовой рекурсии, у которой ВСЕ хвостовые позиции — отскоки,
> // давала шаг батута с неиспользованным `result`, то есть -Werror. Дефект
> // исправлен в обеих реализациях и назван в «Долгах» self/SPEC.md, а `пусть`
> // здесь оставлен намеренно: форма напечатанного C сверяется побайтово, и
> // менять её ради красоты исходника — плохой размен.

Было у печати батута:

> // -Werror делает такой C несобираемым. Дефект найден этой печатью на её
> // собственном исходнике и исправлен в свидетеле тем же способом.

### Замер счёта витков у бэкенда C

Было у «Печать тела функции»:

> // считал бы его дважды. Измерено: на «Правильные скобки» от 9 бэкенд C
> // насчитывал 30 631 шаг там, где остальные семь целей насчитывают 23 713.

### Улика про сырую «Змейку» в подписи

Было у «Печать со счётчиком»:

> // на вызове строкой ниже. Здесь стояла сырая «Змейка» от имени модели, и
> // параметр «до» приезжал в подпись словом `do`: проверки зелёные, а
> // напечатанный C не собирается. Разъехаться подпись с вызовом больше не
> // может — оба берут одно и то же значение.

### Прогон пересборки семени 31 августа

Было у «Проверить столкновения по программе»:

> // столкновение имён вылезало ПЕРЕД судом ядра, а не на последней стадии:
> // 31 августа пересборка семени шла девять часов пятьдесят шесть минут и упала
> // на печати от пары «Занято именем модуля C++» и «Занято именем модуля C».

---

## `flang/self/emit-cpp.flang`


Печать программы flang в C++.

### Довод задачи 3380 и прогон, который его не подтвердил

Было в шапке файла, врезка «Чем эта цель отличается от «c», и почему замерено,
а не объявлено» — ушли ссылка на задачу, рецепт прогона и его итог:

> ── Чем эта цель отличается от «c», и почему замерено, а не объявлено ───────
>
> Довод задачи 3380 звучал так: «у C++ своя объектная модель и свой ABI, и ни в
> одну из восьми целей он не стирается». Довод проверен прогоном, и прогон его
> НЕ ПОДТВЕРДИЛ на той половине, где он важнее всего, — на самом коде:
>
>   печать корпуса в цель «c», затем сборка того же вывода компилятором C++
>   под `-Wall -Wextra -Werror -pedantic`, затем одна и та же сетка запросов
>   через оба собранных прогонщика с побайтовой сверкой ответов.
>
> Ответы совпали до байта.

Названный поимённо назначенный инициализатор и вывод «C++20, а не C++17»
остались на месте.

### Побайтовая сверка, которая «сегодня говорит, что расхождений ноль»

Было в шапке файла, пункт 1 устройства цели:

> и ловить
> его пришлось бы той же побайтовой сверкой, которая сегодня говорит, что
> расхождений ноль.

### Долг: пространство имён модуля не напечатано

Было в шапке файла, в пункте «НЕ печатает модуль в `namespace`»:

> Цена названа; работа не сделана.

### Ссылка на замер из шапки в объяснении Makefile

Было у Makefile цели:

> ПОЧЕМУ C++20, А НЕ C++17. Замер назван в шапке файла: под `-std=c++17` […]

---

## `flang/self/emit-csharp.flang`


Печать программы flang в C# — на самом flang.

### Файл назван эталоном свидетеля, а критерий верности был побайтовой сверкой с ним

Было в шапке файла:

> // Эталон свидетеля `flang/src/emit/csharp.mjs`. Слой самоприменения
> // (`flang/self/SPEC.md`): после него программа flang ложится классом рядом с
> // предметной логикой .NET, и для этого не нужен Node.
> //
> // ── Критерий верности ──────────────────────────────────────────────────────
> // Один и он побайтовый: для каждой программы репозитория текст, который даёт
> // «Печать программы CSharp», обязан совпасть с тем, что печатает свидетель, байт в байт
> // — включая комментарии, пробелы, порядок временных имён и `///`-документацию.
> // Не «компилируется и работает»: этот критерий не отличает правильную печать
> // от случайно похожей. Проверка — `flang/test/self-emit-csharp.test.mjs`.

Ни `flang/src/emit/csharp.mjs`, ни `flang/test/self-emit-csharp.test.mjs` в
дереве нет. **Критерия верности у этой печати теперь нет вовсе** — прежний был
дифференциальным, и с уходом свидетеля исчез вместе с ним. Это не оформленная
задача, но и не пустяк: «побайтово» стояло доводом ещё в пяти местах файла
(см. ниже), и каждое из них теперь висит без опоры.

### Свидетель в «Трёх решениях, без которых остальной файл не читается»

Было в шапке файла:

> // 1. AST приходит как обычное значение JSON — сумма типов «Значение» из
> //    `flang/core/json.flang`. Своих типов под AST здесь нет намеренно: свидетель
> //    тоже работает с голым объектом, различает «ключа нет» и «ключ со
> //    значением ничто», и принимает `bind` образца то записью, то списком.
> //
> // 2. Состояния в языке нет, поэтому всё, что в свидетеле лежит в замыкании
> //    (счётчик временных имён, множество занятых имён тела, первая ошибка),
> //    едет записью «Состояние CSharp» через каждую функцию печати и возвращается
> //    изменённым.
> //
> // 3. … Свидетель правит уже напечатанные
> //    строки в двух местах — переносит вычисленное левее (`emitOrdered`) и
> //    гасит неиспользованную привязку (`hideUnused`), — и обе правки здесь
> //    сделаны иначе: текст, который правился бы, просто печатается позже, когда
> //    ответ уже известен. Порядок выделения временных имён при этом сохранён —
> //    он и есть то, что сверяется побайтово.

### Замер 29 августа 2026: буквы хань и эсперанто терялись в идентификаторах

Было у раздела «БУКВЫ ЗАЯВЛЕННЫХ ПОВЕРХНОСТЕЙ, КОТОРЫЕ CSharp ПРИНИМАЕТ В ИМЕНИ»:

> // Словарные символы — латиница и цифры; всё прочее разделитель. Для знаков
> // препинания это верно, и менять там нечего: одного длинного тире в именах
> // дерева 5196 попаданий, и ни одно из них в идентификатор попасть не должно.
> //
> // А БУКВЫ ДВУХ ЗАЯВЛЕННЫХ ПОВЕРХНОСТЕЙ ТЕРЯЛИСЬ ВМЕСТЕ С ПРЕПИНАНИЕМ.
> // Замер 29 августа 2026, двоичный собран из семени этого дерева:
> //
> //   flang emit examples/surfaces/factorial.zh.flang --target python
> //   → печать ОТКАЗАЛА: имя функции «乘积» даёт идентификатор «fn_value»,
> //     уже занятый (функции «前置»)
> //
> //   «Antaŭmeti» → anta_meti;  «De unu ĝis kvin» → de_unu_is_kvin

и там же — проверка прогоном:

> // CSharp ЭТИ БУКВЫ ПРИНИМАЕТ — проверено прогоном, а не вычитано из стандарта:
> // `fn_乘积` собрался и ответил 24 на всех четырёх целях со своей
> // транслитерацией.

и два счётных замера по дереву:

> // Заводить шесть пар регистра ради этого сегодня не за что: заглавных
> // букв эсперанто в именах дерева НОЛЬ (замер 29 августа 2026 по всем 849
> // файлам вне точки раскрутки).

> // Значит правило меняет
> // вывод ровно у тех имён, где стоят хань или эсперанто, — их в дереве 22 в
> // четырёх файлах.

Само правило (какие буквы считаются символами слова и почему, и что регистр оно
не чинит) в файле оставлено целиком.

### Мелкие сверки со свидетелем по всему файлу

Было у отдельных функций — по одному-два предложения на место:

> // Значение ключа; отсутствующий ключ даёт «ничто» — так же, как `node.поле`
> // в JavaScript даёт undefined, а `undefined` свидетель везде сводит к «ничто».

> // `fn.total === true` свидетеля: истинно только для настоящего признака «да».

> // Элементы списка; всё, что списком не является, даёт пустой список — свидетель
> // в таких местах пишет `?? []`.

> // Транслитерация из `flang/src/naming.mjs`.

> // Разбиение на слова: `words` из naming.mjs — всё, что не буква и не цифра,
> // служит разделителем.

> // Первая буква вверх, остальные как есть — ровно `charAt(0).toUpperCase() +
> // slice(1)` свидетеля.

> // `pascal` из naming.mjs: слова с заглавных, слитно; пустое имя даёт «Value».

> // `camel` из naming.mjs: паскаль с первой строчной.

> // Как `Map.set`: существующий ключ обновляется на месте, новый дописывается в
> // конец — порядок пар обязан совпадать с порядком вставки в свидетеле.

> // Свидетель проверяет «использовано ли имя» регулярным выражением `\bимя\b`.
> // … ровно как `\w`.

> // Форма временного имени свидетеля — `t` и цифры.

> // … поэтому свёртка по
> // таблице даёт тот же результат, что посимвольный обход свидетеля.

> // Последний шаг печати: снять сырые двунаправленные управляющие со ВСЕГО
> // вывода (`flang/src/bidi.mjs`).

> /// `uniqueIdents` свидетеля: транслитерация в camelCase плюс развод столкновений.

> // Повторяет `prepare` свидетеля, а тот — `prepareProgram` интерпретатора: те же
> // проверки, те же коды и тексты.

> // `expr.in ?? expr.body`: у `пусть` тело лежит то под одним ключом, то под
> // другим — свидетель принимает оба, и печать обязана принимать оба.

> // Компоненты сильной связности. Свидетель разворачивает рекурсию в явный стек,
> // чтобы не упасть на глубине графа вызовов; здесь рекурсия честная, а глубина
> // ограничена числом функций программы — то есть тем же, чем ограничен свидетель.

> // `${["ctx", ...args].join(", ")}` свидетеля.

> // Всё, что в свидетеле лежит в замыкании: счётчик временных имён, занятые
> // идентификаторы тела и первая ошибка печати.

> // Свидетель делает это правкой уже напечатанного (`out.splice`); здесь текст
> // соседей копится отдельно, и подъём просто вклинивается между ним и текстом
> // текущего узла.

> // Проверка дословно повторяет
> // encodedVariant свидетеля: расхождение здесь означало бы, что литерал, который
> // интерпретатор считает вариантом, напечатанный код считает записью.

> // Свидетель держит тот же список
> // одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в `src/builtins.mjs`), и разойтись им
> // нельзя: печать сверяется со свидетелем побайтово.

> // Доказывает не печать — отметку `доказана` кладёт
> // анализ (`src/types.mjs`), и здесь она только читается.

> // Остальные случаи после него не печатаются
> // вовсе — их проверки, впрочем, уже подготовлены выше, как и у свидетеля.

> // Печать отвечает за то, что эти данные лягут в C# правильными байтами,
> // и вот это сверяется со свидетелем побайтово.

> // таблицу собирает одна и та же сторона (слой типов у свидетеля, оболочка на C у
> // двоичного), и разойдись имена — ей пришлось бы собирать её дважды.

> // … и сверять их нечем — ровно как молчит
> // о них проверка значений свидетеля.

> // Ровно там же зовёт проход и свидетель, и это не совпадение, а условие
> // побайтовой сверки: понизь программу в другом месте — и печать разошлась бы
> // на первой же программе с функцией-значением.

> // Последний шаг — снять сырые двунаправленные управляющие со всего вывода
> // (`flang/src/bidi.mjs`).

### Свидетель второго набора занятых целью имён

Было у раздела «имена, занятые самой целью C#»:

> // Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> // стороны без другой молчит до первой подходящей формы в корпусе.

### Замер о скобках в мере «Печати тел»

Было у функции, считающей меру:

> // Прежняя запись
> // «… плюс ((длина «функции») плюс (длина «шаги»))» — то же число, но другой
> // терм, и обещание из-за одних скобок стояло недоказанным. Замер: проба на
> // двух одинаковых по значению целях даёт «доказано» левой и сетку парной.

Правило («ядро сличает терм цели с тем, что даёт вложение вызовов, и само сумму
не переставляет») в файле оставлено.

## `flang/self/emit-elixir.flang`


Печать программы flang в Elixir. Восьмая цель, единственная с настоящими процессами.

### Эталон печати в Elixir был свидетелем на JavaScript

Было в шапке файла, первой строкой абзаца про нишу Elixir:

> Эталон свидетеля `flang/src/emit/elixir.mjs`.

Файла `flang/src/emit/elixir.mjs` в дереве больше нет.

### Критерий верности был побайтовым — против свидетеля

Было в шапке файла, отдельной врезкой:

> ── Критерий верности ──────────────────────────────────────────────────────
> Один и он побайтовый: для каждой программы репозитория текст, который даёт
> «Печать программы Elixir», обязан совпасть с тем, что печатает свидетель, байт в байт —
> включая комментарии, пробелы, порядок временных имён и приставки «_» у
> погашенных связываний. Не «компилируется и работает»: этот критерий не
> отличает правильную печать от случайно похожей. Проверка —
> `flang/test/self-emit-elixir.test.mjs`.

Ни свидетеля, ни `flang/test/self-emit-elixir.test.mjs` в дереве больше нет.

### Почему AST — голое значение JSON: довод шёл от свидетеля

Было в шапке файла, решение 1:

> Своих типов под AST здесь нет намеренно: свидетель
> тоже работает с голым объектом, различает «ключа нет» и «ключ со значением
> ничто», и принимает `bind` образца то записью, то списком.

### Почему печатаемый текст — список строк, а не строка

Было в шапке файла, решение 3, последним предложением:

> Свидетель правит элемент массива по индексу;
> список строк — то же самое, только без изменяемости.

### Отсутствующий ключ — сверка с `undefined` свидетеля

Было у функции «Взять поле Elixir»:

> Значение ключа; отсутствующий ключ даёт «ничто» — так же, как `node.поле`
> в JavaScript даёт undefined, а `undefined` свидетель везде сводит к «ничто».

### Замер 29 августа 2026: буквы хань и эсперанто терялись вместе с препинанием

Было в шапке раздела «БУКВЫ ЗАЯВЛЕННЫХ ПОВЕРХНОСТЕЙ, КОТОРЫЕ Elixir ПРИНИМАЕТ В
ИМЕНИ». Замер, прогон и три счёта по дереву:

> Для знаков
> препинания это верно, и менять там нечего: одного длинного тире в именах
> дерева 5196 попаданий, и ни одно из них в идентификатор попасть не должно.

> А БУКВЫ ДВУХ ЗАЯВЛЕННЫХ ПОВЕРХНОСТЕЙ ТЕРЯЛИСЬ ВМЕСТЕ С ПРЕПИНАНИЕМ.
> Замер 29 августа 2026, двоичный собран из семени этого дерева:
>
>   flang emit examples/surfaces/factorial.zh.flang --target python
>   → печать ОТКАЗАЛА: имя функции «乘积» даёт идентификатор «fn_value»,
>     уже занятый (функции «前置»)
>
>   «Antaŭmeti» → anta_meti;  «De unu ĝis kvin» → de_unu_is_kvin

> Elixir ЭТИ БУКВЫ ПРИНИМАЕТ — проверено прогоном, а не вычитано из стандарта:
> `fn_乘积` собрался и ответил 24 на всех четырёх целях со своей
> транслитерацией.

> ЧЕГО ЭТО НЕ ЧИНИТ, И ЭТО НАЗВАНО, А НЕ СПРЯТАНО: регистр. […]
> Заводить шесть пар регистра ради этого сегодня не за что: заглавных
> букв эсперанто в именах дерева НОЛЬ (замер 29 августа 2026 по всем 849
> файлам вне точки раскрутки).

> Значит правило меняет
> вывод ровно у тех имён, где стоят хань или эсперанто, — их в дереве 22 в
> четырёх файлах.

### История «Слов Elixir»: обход по индексу двумя функциями не доказывался

Было у функции «Слова Elixir»:

> Здесь стоял обход по индексу — двумя взаимно рекурсивными функциями, — и он
> не доказывался: убывала разность «длина минус индекс», то есть число, а
> анализ (`src/totality.mjs`) числа частью значения не считает. Теперь строка
> раскладывается в список символов встроенной формой, и обход стал свёрткой по
> готовому списку: свёртка конечна по построению, доказывать нечего.
> Заодно исчез повод для приёма «итог через пусть»: компоненты взаимной
> хвостовой рекурсии, на которой бэкенд C печатал шаг батута с
> неиспользованным `result`, больше нет.

### Долг: приём «итог через пусть» и дефект печати в C, найденный сверкой

Было в разделе «хвостовые позиции тела», отдельным абзацем — ушло целиком:

> Итог возвращается через «пусть», а не прямо из ветки, и это не украшение.
> Приём пришёл из дефекта печати в C, который нашла эта сверка: компонента
> взаимной хвостовой рекурсии, у которой ВСЕ хвостовые позиции — отскоки,
> давала шаг батута с неиспользованным `result`, то есть -Werror. Дефект
> исправлен в обеих реализациях и назван в «Долгах» self/SPEC.md, а `пусть`
> здесь оставлен намеренно: форма напечатанного C сверяется побайтово, и
> менять её ради красоты исходника — плохой размен.

### Почему рекурсия честная: глубина сравнивалась со свидетелем

Было у функции «Компоненты связности Elixir»:

> Свидетель разворачивает рекурсию в явный стек,
> чтобы не упасть на глубине графа вызовов; здесь рекурсия честная, а глубина
> ограничена числом функций программы — то есть тем же, чем ограничен свидетель.

### Однострочные ссылки на свидетеля и `naming.mjs`

Было у функций (по одной строке у каждой; вычеркнута только ссылка, поведение
осталось, кроме двух отмеченных — они ушли целиком):

> `fn.total === true` свидетеля: истинно только для настоящего признака «да».

> Элементы списка; всё, что списком не является, даёт пустой список — свидетель
> в таких местах пишет `?? []`.

> Транслитерация из `flang/src/naming.mjs`.

> Разбиение на слова: `words` из naming.mjs — всё, что не буква и не цифра,
> служит разделителем.

> `snake` из naming.mjs: транслитерация, слова, нижний регистр, подчёркивания.

> Как `Map.set`: существующий ключ обновляется на месте, новый дописывается в
> конец — порядок пар обязан совпадать с порядком вставки в свидетеле.

> Свидетель гасит неиспользованные переменные `(void)имя;`, а «использована ли»
> проверяет регулярным выражением `\bимя\b`.

> Повторяет `prepare` свидетеля, а тот — `prepareProgram` интерпретатора […]
> Первая найденная ошибка побеждает, и порядок обхода здесь значим — свидетель
> бросает на первой же.

> `Map.set`: повтор имени обновляет значение, но не переставляет ключ.

> `expr.in ?? expr.body`: у `пусть` тело лежит то под одним ключом, то под
> другим — свидетель принимает оба, и печать обязана принимать оба.

> `out[i] = …` свидетеля: изменяемости нет, поэтому список пересобирается.

> `out.slice(from)` свидетеля.  ← ушло целиком (у функции «Срез с Elixir»)

> `String.replace(что, чем)` свидетеля — ПЕРВОЕ вхождение, а не все.

> поэтому свёртка по таблице даёт тот же
> результат, что посимвольный обход свидетеля, и остаётся тотальной.

> `safeIdent` свидетеля: идентификатор Elixir начинается со строчной буквы или
> подчёркивания.

> `pascal` из naming.mjs.  ← ушло целиком (у функции «Паскаль Elixir»)

> `/^t[0-9]+$/` свидетеля: имя, похожее на временное, занятым считается всегда […]

> `uniqueIdents` свидетеля: транслитерация плюс развод столкновений.

> Всё, что в свидетеле лежит в замыкании: счётчик временных имён, занятые
> идентификаторы тела и первая ошибка печати.

> `item.expr === undefined` свидетеля: ключ со значением `null` проверку проходит,
> отсутствующий — нет.

> `Array.isArray(node.args)` свидетеля: `args`/`items` разрешено не быть вовсе […]

> Проверка дословно повторяет encodedVariant
> из builtins.mjs: расхождение здесь означало бы […]

> `bind` образца приходит то записью, то списком имён — свидетель принимает оба.

> Порядок
> замен здесь значим и повторяет свидетель: сперва тройная кавычка, потом слеш […]

> `Number.isFinite` свидетеля: и не число, и NaN, и бесконечность — не в счёт.

> `Math.trunc` свидетеля — сразу в записи, потому что печатается именно она.

> Запас витков ставится ТОЛЬКО нетотальному обработчику […] — ровно так же решает
> планировщик свидетеля, и разойтись здесь было бы нельзя.

### Список «снимаются непустотой» держал свидетель, отметку клал `src/types.mjs`

Было у функции «Формы, снимаемые непустотой Elixir»:

> Свидетель держит тот же список
> одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в `src/builtins.mjs`), и разойтись им
> нельзя: печать сверяется со свидетелем побайтово.

> Доказывает не печать — отметку `доказана` кладёт
> анализ (`src/types.mjs`), и здесь она только читается.

### Таблица входа приезжает снаружи — `таблицаВхода` у свидетеля

Было у раздела «граница входа: объявленные типы параметров данными»:

> решить,
> какой у параметра объявленный тип, — работа слоя типов (`таблицаВхода` у
> свидетеля), а не печати. Печать отвечает за то, что эти данные лягут в
> Elixir правильными байтами, и вот это сверяется побайтово.

### Понижение зовётся там же, где у свидетеля

Было у прохода понижения:

> Ровно там же зовёт проход и свидетель, и это не совпадение, а условие
> побайтовой сверки.

### Свидетель наборов занятых имён — `target-occupied.mjs`

Было в разделе «имена, занятые самой целью Elixir» — ушло целиком:

> Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> стороны без другой молчит до первой подходящей формы в корпусе.

---

## `flang/self/emit-go.flang`


Печать программы flang в Go. Второй печатник самоприменения после C.

### Ниша Go была названа в шапке свидетеля `go.mjs`

Было в шапке файла:

> Ниша Go
> названа в шапке свидетеля `flang/src/emit/go.mjs`: один статически слинкованный
> файл без рантайма снаружи […]

### Критерий верности был побайтовым — против `go.mjs`

Было в шапке файла, отдельной врезкой — ушло целиком:

> ── Критерий верности ──────────────────────────────────────────────────────
> Один и он побайтовый: для каждой программы репозитория текст, который даёт
> «Печать программы в Go», обязан совпасть с тем, что печатает
> `flang/src/emit/go.mjs`, байт в байт — включая комментарии, пробелы, порядок
> временных имён и имена переменных ошибки. Проверка —
> `flang/test/self-emit-go.test.mjs`.

Ни `flang/src/emit/go.mjs`, ни `flang/test/self-emit-go.test.mjs` в дереве нет.

### Замер ставки перевода на flang: ×1.8 у печати в C и ×1.13 здесь

Было в шапке файла, врезка «Почему этот файл почти вровень со свидетелем, а не
в полтора раза длиннее» — ушла целиком вместе с заголовком:

> Перевод на flang стоит примерно ×1.8: у печати в C 3386 строк против 1885 у
> `c.mjs`. Здесь 1873 против 1643, то есть ×1.13; по ставке печати в C вышло бы
> около 2950, и разница в тысячу с лишним строк — это то, что НЕ написано
> заново.

### Побайтовая сверка как довод переиспользования печати в C

Было в шапке файла, двумя местами:

> Всё это у восьми целей устроено
> ОДИНАКОВО и уже сверено побайтово печатью в C.

> Переименовать их значит переписать `emit-c.flang`, который
> сверен побайтово, — плохой размен.

### Своё у Go перечислялось по пунктам шапки свидетеля

Было в шапке файла, врезка «Что тут своё»:

> Своим осталось ровно то, чем Go отличается от C (пункты 1–7 шапки свидетеля):

### Измеренная граница на порченом AST: 14 программ, 1 совпадение, 11 отказов

Было в шапке файла, врезка «Чего здесь нет». Ушли имена функций свидетеля и
замер границы:

> Проверок ПОРЧЕНОГО AST здесь тоже нет — тех, что у свидетеля живут в `prepare`
> и `requireExpr` […]
> Граница измерена, а не оценена: из четырнадцати порченых программ эталон
> повторяет отказ свидетеля в одном случае. Число держит тест «граница на
> порченом AST» […] Все ОДИННАДЦАТЬ
> отказов, достижимых с настоящего AST, повторяются слово в слово.

### Набор двунаправленных управляющих жил в `flang/src/bidi.mjs`

Было у экранирования строкового литерала:

> Двунаправленные (набор — `flang/src/bidi.mjs`, общий на все бэкенды
> обоих компиляторов) уезжают в `\uXXXX` […]

### Однострочные ссылки на свидетеля и `naming.mjs`

Было у функций:

> поэтому свёртка по таблице даёт тот же
> результат, что посимвольный обход свидетеля, и остаётся тотальной.

> `pascal` из naming.mjs: транслитерация, слова, заглавная у каждого слова.

> Суффикс приписывается БЕЗ подчёркивания и начинается с двойки — так делает
> `uniqueIdents` свидетеля, и это наблюдаемо в напечатанном Go.

> `uniqueIdents` свидетеля: транслитерация плюс развод столкновений.

> Порядок тот же, что у свидетеля: сперва фабрики записей, потом конструкторы
> вариантов, потом функции.

> Свидетель держит тот же список
> одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в `src/builtins.mjs`), и разойтись им
> нельзя: печать сверяется со свидетелем побайтово.

> Доказывает не печать — отметку `доказана` кладёт
> анализ (`src/types.mjs`), и здесь она только читается.

> значение-функция, параметр
> полиморфизма и применение типа с аргументами не сверяются, ровно как молчит о
> них проверка значений свидетеля.

> Ровно там же зовёт проход и свидетель (`emitGo`), и это не совпадение, а
> условие побайтовой сверки.

### Свидетель наборов занятых имён — `target-occupied.mjs`

Было в разделе «имена, занятые самой целью Go» — ушло целиком:

> Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> стороны без другой молчит до первой подходящей формы в корпусе.

### Замер: шесть целей из восьми теряли процессы

Было у отказа по конкурентности. Довод остался в условном наклонении, ушёл счёт:

> шесть целей из восьми ТЕРЯЛИ процессы, печатали обработчики обычными
> функциями и кончались кодом 0.

---

## `flang/self/emit-java.flang`


Печать программы в Java.

### Критерий верности был побайтовой сверкой с печатью на JavaScript

> Один и он побайтовый: для каждой программы репозитория набор файлов, который
> даёт «Печать программы в Java», обязан совпасть с тем, что печатает
> `flang/src/emit/java.mjs`, байт в байт — включая комментарии, пробелы,
> порядок временных имён и раскладку по файлам. Не «компилируется и
> работает»: этот критерий не отличает правильную печать от случайно похожей.
> Проверка — `flang/test/self-emit-java.test.mjs`.

Ни того, ни другого файла в дереве нет.

### Замер 29 августа 2026: буквы хань и эсперанто терялись вместе с препинанием

> Замер 29 августа 2026, двоичный собран из семени этого дерева:
>
>     flang emit examples/surfaces/factorial.zh.flang --target python
>     → печать ОТКАЗАЛА: имя функции «乘积» даёт идентификатор «fn_value»,
>       уже занятый (функции «前置»)
>
>     «Antaŭmeti» → anta_meti;  «De unu ĝis kvin» → de_unu_is_kvin

Проба, которой решение подтверждено:

> Java ЭТИ БУКВЫ ПРИНИМАЕТ — проверено прогоном, а не вычитано из стандарта:
> `fn_乘积` собрался и ответил 24 на всех четырёх целях со своей
> транслитерацией.

Довод, по которому регистр не чинили, — тоже замер:

> Заводить шесть пар регистра ради этого сегодня не за что: заглавных букв
> эсперанто в именах дерева НОЛЬ (замер 29 августа 2026 по всем 849 файлам вне
> точки раскрутки).

### Улика, из-за которой заведён набор «имена, занятые целью»

> Улика: `модуль «JSON»` печатался в `json.py` рядом с прогонщиком, тот делал
> `import json` и получал НАШ модуль вместо библиотеки — ломалось не при
> печати, а при запуске.

И там же — второй свидетель того же набора:

> Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> стороны без другой молчит до первой подходящей формы в корпусе.

### Полтора десятка построчных сверок со свидетелем

Сверки вида «`fn.total === true` свидетеля», «`expr.in ?? expr.body`»,
«`uniqueIdents` свидетеля», «`renderFields` свидетеля», «`escapeBidiInFiles`
свидетеля», «`СНИМАЮТСЯ_НЕПУСТОТОЙ` в `src/builtins.mjs`», «`snake`/`pascal`/
`words` из `naming.mjs`» переписаны на описание поведения. Ни одного из
названных файлов в дереве нет.

## `flang/self/emit-js.flang`


Печать программы flang в JavaScript. Самый большой печатник самоприменения.

### Свидетель как мера: размер файла и довод «зачем эта цель»

Было в шапке файла:

> Свидетель — `flang/src/emit/js.mjs`, 2332 строки, самый большой из семи
> оставшихся печатников. Он же самый нагруженный смыслом: именно на JavaScript
> написан сегодняшний свидетельский компилятор, и уход от Node — заявленная причина
> всей работы по самоприменению. Печатник в JS, написанный на flang, — шаг,
> после которого язык умеет напечатать сам себя в ту среду, из которой уходит.

Последняя фраза (про уход от Node и шаг самоприменения) оставлена в файле;
снята сверка со свидетелем и его размер.

### Критерий верности был дифференциальным и побайтовым

Было в шапке файла:

> ── Критерий верности ──────────────────────────────────────────────────────
> Один и он побайтовый: для каждой программы репозитория текст, который даёт
> «Печать программы JS», обязан совпасть с тем, что печатает свидетель, байт в байт —
> включая комментарии, пробелы, порядок временных имён и нумерацию.
> Не «компилируется и работает»: этот критерий не отличает правильную печать от
> случайно похожей. Проверка — `flang/test/self-emit-js.test.mjs`.

Ни `flang/src/emit/js.mjs`, ни `flang/test/self-emit-js.test.mjs` в дереве нет.

### Почему текст рантайма лежит строками: у свидетеля он был функциями

Было в шапке файла («Чем эта цель отличается от C»):

> Сам текст рантайма лежит в этом файле строками, а не приезжает параметром, и
> это не лень. У свидетеля он лежит настоящими функциями и печатается через
> `Function.prototype.toString()` — то есть у свидетеля он ЕСТЬ, а у нас его
> взять неоткуда: читать файлы язык не умеет и не должен. Положить его сюда —
> единственный способ сделать печатник самодостаточным: собранный без Node
> компилятор обязан уметь напечатать JavaScript, не имея рядом `js.mjs`.
> Расхождение со свидетелем ловит сверка — она побайтовая.

### Долги, названные числами: граница входа и повтор имени функции

Было в шапке файла — раздел целиком:

> ── Долги, названные числами ───────────────────────────────────────────────
>
> 1. ГРАНИЦЫ ВХОДА здесь нет, и это единственный долг, из-за которого сверка
>    вычитает у свидетеля блок, а не сравнивает всё подряд.
>
>    Свидетель печатает её в таблицу `$PROGRAM` полем `entry` (`renderEntry`), а
>    СТРОИТ её `таблицаВхода` из `src/types.mjs` — то есть типизатор, а не
>    печать. Эталона типизатора она не имеет: слово «Граница входа» в
>    `self/types.flang` не встречается вовсе, а построить её здесь заново
>    значило бы завести ВТОРОЙ ответ на вопрос «какой у параметра тип» — тот
>    самый случай, ради запрета которого сюда импортируется «Заменить всё» из
>    `core/json.flang`, а не пишется вторая замена подстроки.
>
>    Долг общий с четырьмя соседями (elixir, java, python, csharp), назван и
>    вычтен один раз на всех — `flang/test/entry-debt.mjs`, — и у вычитания
>    зубы с двух сторон: блок ищется по ДВУМ меткам, обе обязаны найтись, а его
>    пропажа требует снять вычитание, а не промолчать. Проверено изъятием:
>    строка `stackMb: 79,`, стоящая ВПЛОТНУЮ к вычитаемому блоку, при подмене
>    на `80` красит сверку на всех 101 программе.
>
>    Снимается долг не здесь, а в `self/types.flang`: там появляется «Таблица
>    входа», сюда приезжает импортом, и вычитание уходит из `entry-debt.mjs`
>    сразу у пяти целей.
>
> 2. Повтор имени функции свидетель ловит при подготовке программы
>    (`функция «X» объявлена дважды`), здесь — нет: собираются все узлы
>    подряд. То же упрощение, что в `emit-c.flang`; ни одна программа корпуса
>    его не задевает.

Первый долг с тех пор оплачен: раздел «граница входа» в файле есть и печатает
поле `entry` таблицы `$PROGRAM`. Второй (повтор имени функции не ловится)
остаётся в силе — знание вынесено сюда, а не потеряно.

### Тот же долг, названный второй раз — у таблицы `$PROGRAM`

Было у функции «Строка гейта JS» (в хвосте комментария о таблице программы):

> ДОЛГ, НАЗВАННЫЙ ЧИСЛОМ: границы входа (`renderEntry` свидетеля) здесь нет.
> Её строит `таблицаВхода` из `src/types.mjs` — то есть ТИПИЗАТОР, а не
> печать, — и эталона типизатора (`self/types.flang`) она не имеет вовсе.
> Тот же долг и у четырёх соседей (elixir, java, python, csharp); он назван и
> ВЫЧТЕН в одном месте на всех — `flang/test/entry-debt.mjs`, — и вычитание
> красит сверку с двух сторон: блок ищется по двум меткам, а его пропажа
> требует снять вычитание, а не промолчать.

### Замер вычитания: доля вывода, которую сверка не сравнивала

Было в разделе «граница входа»:

> Раньше блока здесь не было вовсе, и побайтовая сверка ВЫЧИТАЛА его у
> свидетеля перед сравнением (`flang/test/entry-debt.mjs`). Вычитание — это
> проверка, которая зеленеет, ничего не сравнив на 7,66 % вывода: замер по
> корпусу дал блок у 102 программ из 102, 218 158 байт из 2 848 619.

### История ложного обещания «Пробельные JS» и снятых из-за него примеров

Было у функции «Пробельные JS»:

> Тело возвращает ШЕСТЬ знаков: к четырём названным добавлены вертикальная
> табуляция и подача страницы — они стоят в литерале сырыми байтами
> (0x0B, 0x0C). Обещание называло четыре, и потому было ЛОЖНЫМ: сторож
> при работе падал на каждом вызове с FLANG_PROPERTY, а ядро отказывалось
> его доказывать и было право. Теперь обещание называет тот же литерал,
> что и тело, знак в знак.

Было у функции «Без хвостовых пробелов JS», над двумя примерами:

> Эти два примера были сняты соседом, пока «Пробельные JS» обещало неправду:
> сторож того обещания падал на каждом вызове отсюда. Обещание починено, и
> примеры вернулись — теперь видно, что хвост вправду срезается.

### Коммит и дата, которыми объяснено число имён уточнённого числа

Было у функции, дающей имена уточнённого числа («типы в документации»):

> ПЕРЕСЧИТАНО, А НЕ ПОДОГНАНО. Имён здесь десять, и десятым «неотрицательное»
> стало коммитом 12123389 («язык принимает имя «неотрицательное» рядом с
> «нат»», 24 августа 2026 16:22 UTC): список пополнили, а число в обещании не
> тронули. Цель ЗАМКНУТА — у функции нет входов, — значит ядро её вычисляет, и
> вердикт «объявлено, не доказано» означал не нехватку вывода, а ЛОЖЬ.

### Два замера TypeScript на `examples/leetcode`

Было у печати конструктора варианта:

> отвечает TS2739 на КАЖДЫЙ такой конструктор (замер на 36 программах
> `examples/leetcode` — 8 замечаний из 87, все здесь).

Было у печати таблицы `$PROGRAM`:

> берёт тип пары из ПЕРВОЙ строки таблицы, и каждая следующая функция с
> другой подписью становится замечанием TS2769. Замер на 36 программах
> `examples/leetcode`: 79 замечаний из 87, все этого вида.

Сами утверждения о TS2739 и TS2769 (почему приведение стоит на литерале массива
и почему у варианта с полями нет значения по умолчанию) в файле остались.

### Мелкие ссылки на свидетеля, снятые по одной

Было у разных объявлений — журнальная половина вычеркнута, поведенческая
осталась:

> 1. AST приходит обычным значением JSON … Тот же довод, что у печати в C: свидетель работает с
>    голым объектом, различает «ключа нет» и «ключ со значением ничто», и
>    принимает `bind` образца то записью, то списком.

> 2. Состояния в языке нет, поэтому всё, что в свидетеле лежит в замыкании …

> `pascal` из naming.mjs: слова с заглавной буквы, склеенные без разделителя.

> `/^[A-Za-z_$][\w$]*$/` свидетеля: аргумент, который и так уже имя, во временное
> перекладывать незачем.

> Набор — общий на все шестнадцать бэкендов (`tools/ftsc/src/bidi.mjs`), форма
> записи — `\uXXXX`, та же, что в литерале JavaScript. Свидетель снимает их
> последним шагом со ВСЕГО вывода, и здесь так же…

> `Map.set`: повтор имени обновляет значение, но не переставляет ключ.

> `createNamer` свидетеля: именователь помнит выданное и отказывает, если два
> разных имени модели дали один идентификатор.

> Всё, что в свидетеле лежит в замыкании: счётчик временных имён, занятые внутри
> тела идентификаторы, множество затребованных помощников рантайма и первая
> ошибка печати.

> Значение выражения: напечатанные ради него операторы, само выражение JS и
> признак «чистое» — свидетельское `pure`.

> `ctx.fresh` свидетеля: желаемое имя, а при столкновении — «имя$2», «имя$3».

> Хвостовые пробелы у планировщика снимаются, как это делает `trimEnd()`
> свидетеля…

> Свидетель держит тот же список одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в
> `src/builtins.mjs`), и разойтись им нельзя: печать сверяется со свидетелем
> побайтово. … Доказывает не печать — отметку `доказана` кладёт
> анализ (`src/types.mjs`), и здесь она только читается.

> Полноценная проверка типов — дело types.mjs; здесь только защита от опечатки
> в имени, ровно как в интерпретаторе.

> `bind` образца варианта — то запись «поле → имя», то список имён: свидетель
> принимает оба вида, и печать обязана принимать оба.

> Пустой список печатается ОДНОЙ строкой `имя: [],`, а не тремя: у свидетеля
> рядов просто нет, а не есть пустой ряд.

> Программа без единой функции даёт `new Map(… ([` и `])),` подряд, БЕЗ пустой
> строки между ними: у свидетеля рядов просто нет в списке, а не пустой ряд.

> Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> стороны без другой молчит до первой подходящей формы в корпусе.

> Ровно там же зовёт проход и свидетель, и это не совпадение, а условие
> побайтовой сверки.

> Считает её тот, кто уже прошёл по объявлениям программы: у
> свидетеля это «таблицаВхода» слоя типов, у двоичного — впечатанная таблица
> собственной программы.

## `flang/self/emit-python.flang`


Печать программы flang в Python. Второй печатник самоприменения после C.

### Критерий верности был дифференциальным и побайтовым

Было в шапке файла:

> ── Критерий верности ──────────────────────────────────────────────────────
> Один и он побайтовый: для каждой программы репозитория текст, который даёт
> «Печать программы», обязан совпасть с тем, что печатает `flang/src/emit/
> python.mjs`, байт в байт — включая комментарии, отступы, порядок временных
> имён и строки документации. Не «импортируется и работает»: этот критерий не
> отличает правильную печать от случайно похожей. Проверка —
> `flang/test/self-emit-python.test.mjs`.

Ни `flang/src/emit/python.mjs`, ни `flang/test/self-emit-python.test.mjs` в
дереве нет.

### Замер 29 августа 2026: буквы хань и эсперанто терялись вместе с препинанием

Было в шапке раздела «БУКВЫ ЗАЯВЛЕННЫХ ПОВЕРХНОСТЕЙ, КОТОРЫЕ Python ПРИНИМАЕТ
В ИМЕНИ». Само правило (почему буквы хань и эсперанто считаются символами
слова) в файле осталось; вынесены замеры и прогоны:

> Словарные символы — латиница и цифры; всё прочее разделитель. Для знаков
> препинания это верно, и менять там нечего: одного длинного тире в именах
> дерева 5196 попаданий, и ни одно из них в идентификатор попасть не должно.
>
> А БУКВЫ ДВУХ ЗАЯВЛЕННЫХ ПОВЕРХНОСТЕЙ ТЕРЯЛИСЬ ВМЕСТЕ С ПРЕПИНАНИЕМ.
> Замер 29 августа 2026, двоичный собран из семени этого дерева:
>
>   flang emit examples/surfaces/factorial.zh.flang --target python
>   → печать ОТКАЗАЛА: имя функции «乘积» даёт идентификатор «fn_value»,
>     уже занятый (функции «前置»)
>
>   «Antaŭmeti» → anta_meti;  «De unu ĝis kvin» → de_unu_is_kvin

> Python ЭТИ БУКВЫ ПРИНИМАЕТ — проверено прогоном, а не вычитано из стандарта:
> `fn_乘积` собрался и ответил 24 на всех четырёх целях со своей
> транслитерацией.

> Заводить шесть пар регистра ради этого сегодня не за что: заглавных
> букв эсперанто в именах дерева НОЛЬ (замер 29 августа 2026 по всем 849
> файлам вне точки раскрутки).

> Значит правило меняет
> вывод ровно у тех имён, где стоят хань или эсперанто, — их в дереве 22 в
> четырёх файлах.

### Долг границы входа: сверка вычитала блок из вывода свидетеля

Было в разделе «граница входа»:

> ЭТОТ БЛОК ЗДЕСЬ ПОЯВИЛСЯ ПОЗЖЕ ОСТАЛЬНОГО ФАЙЛА, И ВОТ ЧЕМ ЭТО ОБОШЛОСЬ.
> Пока его не было, сверка со свидетелем ВЫЧИТАЛА блок из вывода свидетеля
> перед сравнением (`flang/test/entry-debt.mjs`) — и зеленела, не сверяя ровно
> того, без чего напечатанный модуль не работает: `flang_cli.py` зовёт
> `program.entry()`, и на модуле без него прогонщик падает на первом же вызове.
> Долг был назван честно, но названный долг всё равно остаётся долгом.

### Раздел «долги» целиком

Было в подвале файла:

> ═══════════════════════════ долги ═══════════════════════════
>
> Названо здесь, а не умолчано: побайтовая сверка идёт по корпусу, и то, чего в
> корпусе нет, ею не поймано.
>
>   • `требуетПланировщика` — отказ печатать программу с процессами. Ни одна
>     программа корпуса конкурентности не объявляет, поэтому проверки здесь
>     нет; у `emit-c.flang` тот же долг.
>   • Проверки `prepare` свидетеля, которые ловят битый AST (функция без имени,
>     объявленная дважды, тело-ничто, `params` не списком): их даёт разборщик
>     раньше печати, и на корпусе они молчат.
>   • Общий проход `escapeBidiInFiles` по ВСЕМ файлам: двунаправленные
>     управляющие снимаются только в строковых литералах (таблица «Замены
>     Python»), а не в комментариях и строках документации.

Три ограничения печати в Python, названные комментарием, а не проверкой. Знание
живое: ни одно из них с тех пор не закрыто.

### Мелкие ссылки на свидетеля, снятые по одной

Было у разных объявлений — журнальная половина вычеркнута, поведенческая
осталась:

> 1. AST приходит как обычное значение JSON … Своих типов под AST здесь нет намеренно: свидетель
>    тоже работает с голым объектом, различает «ключа нет» и «ключ со
>    значением ничто», и принимает `bind` образца то записью, то списком.

> 2. Состояния в языке нет, поэтому всё, что в `python.mjs` лежит в замыкании …

> здесь отступ — часть синтаксиса, и он же — часть
> побайтового критерия. Отступ едет параметром «отступ» через каждую печать
> оператора, ровно как в свидетеле, и нигде не вычисляется заново.

> Второе отличие — неиспользованное имя. … в Python гасить нечем, и свидетель вместо
> этого ПЕРЕПИСЫВАЕТ саму строку связывания, меняя `имя = …` на `_ = …`.

> Значение ключа; отсутствующий ключ даёт «ничто» — так же, как `node.поле`
> в JavaScript даёт undefined, а `undefined` свидетель везде сводит к «ничто».

> `fn.total === true` свидетеля: истинно только для настоящего признака «да».

> Элементы списка; всё, что списком не является, даёт пустой список — свидетель
> в таких местах пишет `?? []`.

> Первое вхождение, а не все: `String.prototype.replace` со строковым образцом
> меняет ровно одно место…

> Транслитерация из `flang/src/naming.mjs`.

> Разбиение на слова: `words` из naming.mjs — всё, что не буква и не цифра,
> служит разделителем.

> `snake` из naming.mjs: транслитерация, слова, нижний регистр, подчёркивания.

> Как `Map.set`: существующий ключ обновляется на месте, новый дописывается в
> конец — порядок пар обязан совпадать с порядком вставки в свидетеле.

> Свидетель проверяет «использовано ли имя» регулярным выражением `\bимя\b`.
> … ровно как `\w` в JavaScript.

> `isAtom` свидетеля: выражение, которое можно передвинуть по тексту, ничего не
> изменив, — имя либо временное.

> поэтому свёртка по таблице даёт
> тот же результат, что посимвольный обход свидетеля, и остаётся тотальной.
>
> Двунаправленные управляющие (набор — `flang/src/bidi.mjs`, общий на все
> бэкенды обоих компиляторов) уезжают в `\uXXXX`…

> Суффикс приписывается без подчёркивания и начинается с двойки — ровно как в
> свидетеле: `${wanted}${suffix}`.

> `uniqueIdents` свидетеля: транслитерация плюс развод столкновений.

> Повторяет `prepare` свидетеля, а тот — `prepareProgram` интерпретатора: те же
> проверки, те же коды и тексты.

> `Map.set`: повтор имени обновляет значение, но не переставляет ключ.

> `expr.in ?? expr.body`: у `пусть` тело лежит то под одним ключом, то под
> другим — свидетель принимает оба, и печать обязана принимать оба.

> Компоненты сильной связности. Свидетель разворачивает рекурсию в явный стек,
> чтобы не упасть на глубине графа вызовов; здесь рекурсия честная, а глубина
> ограничена числом функций программы — то есть тем же, чем ограничен свидетель.

> Всё, что в свидетеле лежит в замыкании: счётчик временных имён, занятые
> идентификаторы тела и первая ошибка печати.

> ровно так его читает reifyValue интерпретатора.

> Свидетель держит тот же список одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в
> `src/builtins.mjs`), и разойтись им нельзя: печать сверяется со свидетелем
> побайтово. … отметку `доказана` кладёт анализ (`src/types.mjs`).

> так же ведёт себя matchPattern интерпретатора.

> Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> стороны без другой молчит до первой подходящей формы в корпусе.

> работа слоя типов
> (`таблицаВхода` в `flang/src/types.mjs`), а не печати.

> Ровно там же зовёт проход и свидетель (`emitPython`), и это не
> совпадение, а условие побайтовой сверки.

## `flang/self/emit-rust.flang`


Печать программы flang в Rust. Свидетеля `flang/src/emit/rust.mjs` и проверки
`flang/test/self-emit-rust.test.mjs` в дереве больше нет.

### Эталон и критерий побайтовой верности

Было в шапке файла, вторым абзацем:

> Эталон свидетеля `flang/src/emit/rust.mjs`. Критерий верности один и он
> побайтовый: для каждой программы репозитория файлы, которые даёт «Печать
> программы в Rust», обязаны совпасть с печатью свидетеля байт в байт — включая
> комментарии, пробелы, порядок временных имён, расстановку `mut` и приставку
> `_` у мёртвого имени. Проверка — `flang/test/self-emit-rust.test.mjs`.

Осталось: «Критерий верности один и он побайтовый: значимо всё, что попадает в
файлы, — комментарии, пробелы, порядок временных имён, расстановка `mut` и
приставка `_` у мёртвого имени.»

### Чем свидетель снимал не-ASCII из идентификатора

Было в шапке файла, пункт 2:

> 2. ИДЕНТИФИКАТОР ОБЯЗАН БЫТЬ ASCII. `snake` (naming.mjs) оставляет буквы
>    любого письма, а Rust принимает не всякую; свидетель снимает лишнее функцией
>    `ascii`, и здесь она называется «ASCII идентификатор».

### Счёт тотальных функций файла

Было в шапке файла, врезка «Тотальность»:

> … компилятор вправе упереться в лимит шагов и честно сказать об этом. Из 203
> функций 128 тотальны; остальные 75 — обход AST вглубь и всё, что его зовёт: …

Осталось: «Нетотальны здесь обход AST вглубь и всё, что его зовёт: …»

### Свидетель наборов «занято целью»

Было во врезке «имена, занятые самой целью Rust», последним абзацем:

> Свидетель этих же наборов — `flang/src/target-occupied.mjs`. Правка одной
> стороны без другой молчит до первой подходящей формы в корпусе.

### Построчные пометки о свидетеле

| Место | Что было сказано про свидетеля |
|---|---|
| «Змейка Rust» | «`rustSnake` свидетеля: змейка naming.mjs плюс отсев не-ASCII» |
| «Свободное имя Rust» | «Суффикс приписывается БЕЗ подчёркивания — «а», «а2», «а3»: так у свидетеля» |
| «Уникальные имена Rust» | «`uniqueIdents` свидетеля: транслитерация плюс развод столкновений» |
| «Замены Rust» | «свёртка по таблице даёт тот же результат, что посимвольный обход свидетеля» |
| «Замены Rust» | «Двунаправленные управляющие (набор — flang/src/bidi.mjs) уезжают в `\u{X…}`» |
| шум: комментарии и литералы | «Два прохода в том же порядке, что у свидетеля: сперва литералы (`"…"` → `""`), потом комментарии» |
| «Снять литералы» | «Незакрытая кавычка не совпадает с образцом свидетеля вовсе» |
| «Снять шум в строке» | «Разница со свидетелем ровно одна и она не наблюдаема: строковый литерал, начатый на одной строке и закрытый на другой» |
| замена слова целиком | «`text.replace(/\bимя\b/gu, "_имя")` свидетеля» |
| «Переприсваивание точно» | «Свидетель убирает такие строки перед поиском чтений» |
| «Синонимы форм Rust» | «Полный набор синонимов имён форм из `flang/src/builtins.mjs` … `hasBuiltin` свидетеля смотрит именно через них» |
| «Состояние Rust» | «Всё, что в свидетеле лежит в замыкании … Счётчик временных — ОДИН на всю программу: у свидетеля он живёт в `shared`, а не в контексте функции» |
| «Печать списка Rust» | «Временное под массив берётся ДО печати элементов: у свидетеля счётчик временных общий на программу» |
| «Печать значений полей Rust» | «Временное под массив полей берётся ПОСЛЕ печати значений … Так у свидетеля, и порядок временных имён сверяется побайтово» |
| формы, снимаемые непустотой | «Свидетель держит тот же список одним именем (`СНИМАЮТСЯ_НЕПУСТОТОЙ` в `src/builtins.mjs`), и разойтись им нельзя: печать сверяется со свидетелем побайтово» |
| там же | «отметку `доказана` кладёт анализ (`src/types.mjs`)» |
| «Это закодированный вариант Rust» | «Значение варианта в JSON — объект ровно с двумя полями (builtins.mjs, reifyValue)» |
| «Просьбы имён Rust» | «Порядок просьб тот же, что у свидетеля: записи, варианты, функции, шаги батута» |
| «Печать программы в Rust» | «Ровно там же зовёт проход и свидетель (`emitRust`), и это не совпадение, а условие побайтовой сверки» |

## `flang/self/factcheck.flang`


Факт-чекинг — исходное назначение системы.

### Почему файл вообще появился

> Копия `flang/src/factcheck.mjs`. Перенесена потому, что факт-чекинг —
> ИСХОДНОЕ назначение всей системы […]. Побайтовую сверку со свидетелем имели
> семь слоёв компилятора — лексер, разборщик, типы, тотальность, понижение,
> печать в C, ведомость, — а сам факт-чекинг на flang написан не был вовсе. То
> есть самоприменённый компилятор не умел делать то, ради чего язык создан.

### Что считал JavaScript до переноса

> Прежде её считал свидетель на JavaScript, и факт-чекинг, ради которого язык и
> создан, отвечал «подтверждено или нет» чужим языком. […] число таких вызовов
> печатается прогоном `flang/test/self-factcheck.test.mjs`.

### Три места, где перенос был точен не полностью

> 1. `\s` свидетеля — 25 кодовых точек JS. […] А вот `.` регулярного выражения
>    переводом строки НЕ является, и утверждение с переводом строки внутри
>    свидетель разбирал бы иначе. Условие названо и проверяется корпусом в
>    `flang/test/self-factcheck.test.mjs`.
> 2. `Object.is` на ЗАПИСЯХ и СПИСКАХ — тождество ссылок. […] свидетель
>    КОПИРУЕТ факты (`freeze(clone(...))`) […]
> 3. `поле N факта «список»` у свидетеля могло бы найти элемент массива по
>    номеру (`in` работает и на массивах).

Первый пункт — НЕ просто ссылка: он называет условие, при котором перенос мог
расходиться с оригиналом (утверждение с переводом строки внутри). Оригинала
больше нет, сверять не с чем, и в исходнике оставлено только само правило.

### Сверки со свидетелем, переписанные на правило

«`fn.total !== true` свидетеля», «`callees` свидетеля», «`^-?\d+(?:\.\d+)?$`»,
«`(?:«([^»]+)»|"([^"]+)"|([^\s,«»"]+))`», «`JSON.stringify` печатает их так, как
они положены» — всюду оставлено правило, снята ссылка.

## `flang/self/failures.flang`


### Хвост от двух редакций файла

Было в шапке файла, сразу под первой строкой; абзац остался без своего
подлежащего («их» ничего уже не называло) и убран целиком:

> // Мирить их СКЛЕЙКОЙ было нельзя, и это не вкус: типы у двух редакций разные
> // («Отказ» и «Отказы функции» против «Запись отказа» и «Свои отказы»), и файл
> // из кусков обеих перестал бы разбираться — а тогда проверки просто перестали
> // бы загружаться, молча.

### Дыра, предъявленная прогоном: 260 036 витков против глубины 10 001

Было во врезке «пределы пробега»:

> // МЕНЬШЕЕ из них. Отсюда дыра, которую однажды предъявил прогон: обработчик с
> // постоянной оценкой в 260 036 витков анализ объявлял безотказным, а прогон
> // ронял процесс `FLANG_RECURSION_LIMIT` на глубине 10 001.

Правило «сводить надо МЕНЬШЕЕ из двух пределов» осталось, и следом за ним —
довод, почему меньшего достаточно.

### Замер 29 августа 2026 у «В ёлочках при отказах»

Было у «В ёлочках при отказах»:

> // Замер 29 августа 2026: с «плюс 2» и списком — «объявлено, не доказано», с
> // этой записью — «доказано сведением цели с телом функции». Свернёте обратно —
> // обещание снова поедет проверкой в напечатанный код и купит кадр стека.

Последнее предложение осталось в исходнике.

### Запрет «адресат обязан быть литералом», которого больше нет

Было у «Посетить вычисленного адресата»:

> // планировщик откажет отправителю при доставке. Это ровно то место, где раньше
> // стоял запрет «адресат обязан быть литералом»: запрета нет, а цена названа —
> // один вид отказа, который надзору придётся покрыть.

### Врезка «места частичных форм», из-под которой уехал весь код

Было отдельным разделом; врезка и абзац убраны целиком, кода под ними не было:

> // ═══════════════════════════ места частичных форм ═══════════════════════════
> //
> // Счёт мест уехал в `emit-c.flang` («Мест частичных форм») и там же стал
> // ТОТАЛЬНЫМ: обход написан разбором объявленной суммы «Значение», а не
> // проверками вида, и потому доказан структурой. Здесь его не было кому звать —
> // множеству отказов процесса нужны не числа, а записи, — а ведомости
> // доказательства он нужен, и второй копии заводить нельзя.

### `Set` эталона у имён вызовов

Было у «Имена вызовов»:

> // Кого функция зовёт: имена вызовов и функций, отданных значением. Эталон
> // собирает `Set`, то есть повторы схлопываются, а порядок остаётся порядком
> // первого появления.

---

## `flang/self/functor-oracle.flang`


### Ошибка, найденная переносом, и прогон 19 августа

Было у функции «Это единица»:

> // `узел.identity === true` свидетеля: не «истинно похоже», а РОВНО признак `да`.
> //
> // ЗДЕСЬ БЫЛО ПЕЧАТЬЮ, И ЭТО БЫЛО ЛОЖЬЮ НА КАЖДОМ ВХОДЕ. Стояло
> // `(«Строка поля» от «узел» и "identity") равен "да"`, а «Строка узла» отдаёт
> // признаку ПУСТУЮ строку — значит на настоящей единице объекта
> // (`identity: true`) условие давало `нет`. Прогон 19 августа: функция отвечала
> // `false` на узле `{kind:"morphism", identity:true}`. Побайтовая сверка этого не
> // поймала: в корпусе единица объекта до применения стрелки не доходит ни разу, и
> // расхождение с оракулом в проверке оставалось недостижимым.

В исходнике осталось само правило: поле `identity` — не «истинно похоже», а
РОВНО признак `да`.

### Прочие ссылки на свидетеля

> // разрешимость остановки. Предел витков и предел глубины стоят на ней снаружи,
> // как у свидетеля.

> // `value`. Это форма свидетеля (`программа.legacy.filter(...)`), а не наша: её
> // выбирал разборщик, и спорить с ним здесь нечем.

> // Исход применения. «Ничего» — это `{нет: true}` свидетеля: значения не вышло, и

> // Запас глубины тот же, что у свидетеля (32): развернуть композицию глубже
> // нельзя, и оба обрываются на одном месте.

> // Композиция считается справа налево, как у свидетеля: правая на входе, левая

> // Чем переводится объект. Первая пара выигрывает — как `if (образОбъекта.has)
> // continue` у свидетеля; пустая строка значит «объекта в связи нет» и «у него
> // нет «даёт»» разом, и свидетель отвергает оба одним условием.

---

## `flang/self/functor.flang`


Законы функтора — квадрат связи модулей на самом flang.

### Шапка называла файл копией свидетеля

Было в шапке файла:

> // Квадрат связи модулей — на самом flang. Копия `flang/src/functor.mjs`.

### Ссылка на `src/types.mjs`, `checkFunctors`

Было в шапке файла:

> // Устройство связи доказано раньше и в другом месте
> // (`src/types.mjs`, `checkFunctors`): концы сходятся, композиция и единицы
> // сохраняются, полнота на категории домена считается.

### `builtins.mjs` свидетеля в разделе «Почему сосед, а не третий модуль»

Было в шапке файла:

> // Равенство значений, ключ вызова, поиск ответа в таблице и печать значения в
> // текст диагностики у свидетеля общие: все три проверки берут их из
> // `builtins.mjs`. В самоприменённом слое такого общего места нет, и заводить
> // третий модуль ради восьми функций значило бы плодить слой без своего
> // вопроса. Поэтому общее приезжает от соседа, а СВОЁ остаётся своим: предел
> // сетки объявлен здесь, как объявлен он и у свидетеля отдельной константой в
> // каждом из файлов, — предел объявляет тот, кто сетку строит.

Довод (общего места нет, поэтому общее приезжает от соседа) в файле оставлен.

### `reifyValue` свидетеля в разделе «Об овеществлении»

Было в шапке файла:

> // Свидетель прогоняет каждое значение сетки через `reifyValue`, и здесь этого шага
> // нет — не по недосмотру.

### Предел глубины у свидетеля записан подъёмом `глубина > 32`

Было в шапке файла, в разделе «Тотальность»:

> // У свидетеля
> // тот же предел записан подъёмом (`глубина > 32`), здесь — спуском, потому что
> // доказывается в языке спуск, а не подъём.

и у функции «Запас глубины функтора»:

> // У свидетеля предел записан числом 32 и сравнивается подъёмом: `глубина > 32`
> // при начальной глубине 0, то есть законны уровни 0…32, а 33-й отказывает.
> // Здесь тот же предел записан ЗАПАСОМ — сколько уровней ещё можно потратить, —
> // и запас этот `неотрицательное`, убывающий ровно на 1.

Довод «спуск доказывает завершаемость сам, а подъём доказать нечем» в файле
оставлен.

### Приём свидетеля, которым выбирались уезжающие наружу поля

Было у объекта «Проверенный функтор»:

> // Поля «нарушений», «пропущено» и «оборвано» уезжают наружу только при
> // ненулевом значении — так же, как у свидетеля (`...(нарушений === 0 ? {} :
> // { violations })`). Решает это перевод в форму свидетеля
> // (`flang/test/self-functor.test.mjs`): необязательных полей записи у языка
> // нет, и заводить их ради сериализации значило бы менять язык под отчёт.

Файла `flang/test/self-functor.test.mjs` в дереве нет.

### `Map.set` свидетеля у отображения стрелок

Было у функции, строящей отображение «имя стрелки → узел»:

> // Повторное имя ВЫТЕСНЯЕТ прежнее значение,
> // но не меняет своего места в обходе — так ведёт себя `Map.set` свидетеля, а
> // порядок обхода виден в сетке объекта, значит воспроизвести его обязательно.

### `undefined`-ключ свидетеля у безымянной стрелки

> // Стрелка без имени в отображение не попадает вовсе: у свидетеля ключом стал бы
> // `undefined`, и найти её потом было бы нечем.

### Условие `годная` свидетеля у проверки `даёт`

> // Свидетель спрашивает это одним условием (`годная`), и отсутствие
> // ключа от пустой строки не отличает.

### `узел.identity === true` свидетеля

Было у функции «Это истина в узле»:

> // `узел.identity === true` свидетеля: не «истинно похоже», а РОВНО признак `да`.

### Выражения свидетеля у типа «Ход стрелки»

Было у типа «Ход стрелки»:

> // «стрелки нет» и «отказала» приезжают в отчёт по разным правилам —
> // `сделал.нет ?? сделал.ошибка` у первого пути и
> // `первый.ошибка ?? второй.ошибка ?? второй.нет` у второго.

### Ответ свидетеля на чужую арность

Было у функции «Позвать одного»:

> // Другой арности ни у перевода, ни у стрелки
> // быть не может, и свидетель отвечает на неё тем же текстом, что и на отсутствие
> // функции, — звать всё равно нечего.

### Карта образов объекта у свидетеля

Было у перевода объекта:

> // Образ объекта и перевод к нему у свидетеля лежат одной картой: у квадрата нужны
> // обе стороны сразу. Здесь от карты осталась одна половина — ЧЕМ переводится, —
> // потому что вторая (`to`, имя объекта-образа) не читается ни одной строкой
> // закона.

### `if (образОбъекта.has(пара.from)) continue` свидетеля

> // Первый по объявлению, а не последний: свидетель пропускает пару, чей объект уже
> // в карте (`if (образОбъекта.has(пара.from)) continue`).

### `годная` и `!годная(слева?.gives)` свидетеля у пустого «даёт»

> // Здесь
> // это пустая строка, и слияние двух случаев в один законно: `годная` свидетеля
> // отвергает и отсутствие ключа, и пустую строку одним условием.

> // Пусто здесь означает и «объекта в
> // связи нет», и «у объекта нет «даёт»» — свидетель отвергает оба одним условием
> // (`!годная(слева?.gives)`), и разделять их было бы различием без разницы.

### «Ровно как у свидетеля» у предела на объект

> // Предел стоит НА ОБЪЕКТ и проверяется до сравнения на повтор — ровно как у
> // свидетеля: набрав двенадцать, `добавить` не смотрит уже ни на что.

### Поле `aborted` свидетеля у «оборвано»

Было у хода обхода:

> // «квадрат сошёлся на всей сетке» и «сошёлся там, докуда
> // дошли» — разные утверждения, и вторым свидетель отчитывается полем `aborted`.

### `continue квадраты` и `break квадраты` свидетеля у трёх исходов

Было у хода по значениям одного квадрата:

> //   сорвано  — свидетель делает `continue квадраты`, откатывая и число значений, и
> //              число квадратов, и прибавляя пропуск;
> //   нарушено — `break квадраты`: обход стрелок кончается, а счётчики остаются.
> // «различные» переживают все три: свидетель не откатывает их ни при обрыве, ни при
> // нарушении, и это видно в отчёте числом.

### `continue` свидетеля у несошедшихся концов

> // пропуском такой квадрат
> // тоже не считается: свидетель уходит `continue` до всякого счёта.

### Выражение свидетеля у второго пути

> // Путь второй: перевести, потом сделать в соседнем. Отказ перевода дальше не
> // идёт — у свидетеля это `перевёл.ошибка !== undefined ? перевёл : применить(…)`.

### Выражение свидетеля у выбора первого непустого текста

> // Первый непустой из двух текстов — `первый.ошибка ?? второй.ошибка ?? второй.нет`
> // свидетеля.

### Ранний выход сверялся с ранним выходом свидетеля

Было у функции «Проверить функторы»:

> // Программа без единой объявленной связи отдаёт три пустых списка — тот же
> // ранний выход, что у свидетеля, и он не украшение: почти весь корпус языка
> // именно таков, и молчание на нём обязано быть настоящим, а не следствием того,
> // что считать было нечего.

## `flang/self/grid.flang`


Поиск нарушений на сетке примеров.

### Разобранная ошибка предшественника: сравнение по ссылке

> ── Известные грабли этого слоя: СРАВНЕНИЕ ПО ССЫЛКЕ ──
> В `grid.mjs` постусловие функции сличалось с целью обязательства сравнением
> по ссылке (`узел?.expr === обязательство.goal`). У реализации на JavaScript
> это работало: она кладёт в обязательство ТОТ ЖЕ объект. Любой слой, отдающий
> обязательства данными […] несёт выражение, равное узлу знак в знак и НЕ
> равное по ссылке. Поиск отвечал «не искали» на каждом постусловии, ведомость
> печатала это вместо «нарушений не найдено», и ни одна проверка не краснела.

Само ПРАВИЛО («счёт идёт по месту в списке, и вот почему он точен») в исходнике
оставлено — оно объясняет поведение. Вынесена только история чужой ошибки.

> Копия `flang/src/grid.mjs` (201 строка).

## `flang/self/hotswap.flang`


Горячая замена обработчика, посчитанная самим flang.

### Замер удалённого `flang/src/hotswap.mjs`: 530 строк, разрезанные ножом

Было в шапке файла, врезка «Что здесь вынуто и почему именно это». Ушли счёты
строк и имена внутренностей свидетеля; разрез на «за границей хозяина» и
«чистое решение» остался:

> `flang/src/hotswap.mjs` — 530 строк, стороны на flang у них не было. Из этих
> 530 строк 221 — проза (шапка, комментарии, пустые), 309 — код, и код делится
> надвое ножом, который проходит по одному месту:
>
>   • 45 строк ЗА ГРАНИЦЕЙ ХОЗЯИНА — это пять чужих проходов и ничего больше:
>     `parse` (текст → дерево), `checkTypes`, `checkTotality`, `оценитьВитки`
>     и `createRuntime().call` (ВЫПОЛНЕНИЕ переноса на живом значении). Сюда же
>     ввоз этих проходов и обёртка `замена({…})`, которая отдаёт планировщику
>     замыкание;
>   • 264 строки ЧИСТОГО РЕШЕНИЯ — слияние типов, функций и процессов […]

### Пять полей ответа сверялись побайтово

Было в шапке файла:

> Остальные пять полей
> сверяются побайтово (`flang/test/self-hotswap.test.mjs`).

Файла `flang/test/self-hotswap.test.mjs` в дереве больше нет.

### Ранний возврат свидетеля становился списком приговоров

Было в шапке файла, врезка «Состояния и словарей в языке нет», и у «Первого
приговора»:

> Ранний возврат свидетеля становится списком
> приговоров, из которого берётся первый непустой […]

> Ранний возврат свидетеля здесь становится значением: пустое сообщение — «этот
> повод отказа не сработал».

### Расхождение двух анализов тотальности, найденное на «Полях с ключом»

Было у функции «Поля с ключом» — ушла история и красневший сторож дерева:

> Разбор вместо «Взять поле», и это не вкусовщина, а измеренное условие.
> `checkTotality` свидетеля обходит ПОСТУСЛОВИЯ и считает нетотальной функцию,
> чьё постусловие зовёт неизвестное имя, а `flang/self/totality.flang`
> постусловия не обходит вовсе. Пока эта функция звала ввезённое «Взять поле»,
> «Ответ отказа» — единственная функция файла, у которой ТЕЛО обходится без
> ввозов, — выходила тотальной у одного анализа и нетотальной у другого, и
> сторож всего дерева краснел на этом файле (`self-totality.test.mjs`,
> «каждая программа репозитория без связывания получает тот же вердикт»).
> Дыра в анализе на flang записана заметкой; здесь она обойдена тем, что
> постусловие зовёт только МЕСТНЫЕ имена, а не тем, что постусловие снято.

### Постусловие меры: починенный когда-то баг свидетеля и сверка на 4918 случаях

Было у функции о постоянной границе витков:

> (свидетель `src/totality.mjs` обходит — и это его же починенный когда-то
> баг). […] Утверждение при этом не брошено: побайтовая сверка
> проверяет ровно его на 4918 случаях, а не на тех входах, которые придут.

### Вторая половина атомарности жила в `conc.mjs`

Было в двух местах — у ответа отказа и у списка применяемого:

> Вторая половина — «планировщик правда ничего не взял» — живёт в `conc.mjs`
> и отсюда недоказуема.

> Вторая половина — «планировщик
> правда ничего не взял» — свойство `conc.mjs`, и отсюда она недоказуема […]

Файла `conc.mjs` в дереве больше нет; планировщик на flang — `self/conc.flang`.

### Порядок отказов был порядком ранних возвратов свидетеля

Было у «Первого приговора», у «Слияния процессов» и у полного приговора:

> Порядок отказов у свидетеля задан порядком ранних
> возвратов, и он часть ответа: тест сверяет ТЕКСТ отказа, а не факт отказа.

> Три повода отказа на одном объявлении процесса, и порядок между ними тот же,
> что у свидетеля […]

> Приговор целиком: двадцать пять поводов отказа в том же порядке, в каком
> свидетель расставил ранние возвраты.

### Однострочные ссылки на свидетеля

Было у функций (вычеркнута ссылка, поведение осталось):

> Многочлен обратно в узел: оценка уезжает в ответ списком чисел, ровно как у
> свидетеля, а не строкой.

> `undefined` свидетеля отличается от `null` свидетеля ровно в одном месте […]
> А `budget` процесса
> свидетель сравнивает и с `null`, и с `undefined` сразу […]

> `budget === null || undefined` свидетеля.

> `тип?.kind === "named" ? тип.name : (тип?.name ?? null)`. Отсутствующее имя
> свидетель подставляет в текст отказа как «null» […]

> вариант свидетеля — это объект с полем `fields` […]

> при её
> отсутствии — «функция не найдена», дословно как `?? ` свидетеля.

> Счёт идёт по НАКОПЛЕННОМУ списку, как у свидетеля […]

> Одноимённых в кандидате несколько — берётся
> ПОСЛЕДНЯЯ, потому что у свидетеля это словарь, а поздний ключ затирает ранний.

> Их у кандидата
> может быть несколько — свидетель присваивает `новыйУзел` в каждом витке цикла,
> значит побеждает последнее […]

> в том порядке, в каком он назван в записи свидетеля. Порядок ключей — часть
> ответа, потому что сверка побайтовая.

> предупреждения свидетель
> отбрасывает, и отбрасывает их здесь же, а не у хозяина.

> `условия.перенос !== undefined && условия.перенос !== null` свидетеля […]

> Свои диагностики стоят ПЕРВЫМИ, чужие за ними: `[своя, ...чужие]` свидетеля.

> (за порядком диагностик следит побайтовая сверка, а не постусловие […])

---

## `flang/self/idempotent.flang`


Идемпотентность: `х∘х = х` и `ф(ф(х)) = ф(х)`.

### Мёртвые ссылки на свидетеля

Было первой строкой файла:

> Идемпотентность: `х∘х = х` и `ф(ф(х)) = ф(х)`. Копия `flang/src/idempotent.mjs`.

Было в разделе «Что здесь ПРОВЕРЯЕТСЯ, а не доказывается»:

> Закон. Устройство доказано раньше и в другом месте (`types.mjs`, сличением
> объявлений).

Было у хода закона:

> У двухвходовой формы второй вызов — ТОТ ЖЕ: свидетель пишет `второе = первое`, и
> повторять вычисление здесь значило бы спрашивать таблицу дважды об одном.

### Чего здесь нет ПРОТИВ СВИДЕТЕЛЯ: перехват отказа

Было в разделе «вопрос одной строкой»:

> ЧЕГО ЗДЕСЬ НЕТ ПРОТИВ СВИДЕТЕЛЯ: перехвата отказа. У свидетеля проверка обёрнута
> `try`, и сорвавшееся вычисление отвечает `false`; здесь срываться нечему —
> функция тотальна, и отказ вычисления приезжает ответом таблицы, а не
> исключением. Разница в форме, не в вердикте.

Поведенческая половина (перехвата отказа нет, функция тотальна, отказ приезжает
ответом таблицы) в файле осталась. Там же «И ЧЕГО НЕТ У ОБОИХ» стало «И ЧЕГО НЕТ
ВОВСЕ»: второго — свидетеля — больше нет.

## `flang/self/interpret.flang`


Вычисление AST flang на самом flang. Свидетеля `flang/src/interpret.mjs` и
проверки `flang/test/self-interpret.test.mjs` в дереве больше нет.

### «Зачем»: счёт слоёв и строк, и чем круг самоприменения был замкнут

Было в шапке файла, врезка «Зачем» (заголовок оставлен, текст заменён на
поведенческий остаток):

> У самоприменения было восемь слоёв и 19 308 строк, и все восемь — про то, как
> программу ПРОЧИТАТЬ: лексер, разборщик, типы, тотальность, доказательство,
> печать в C, ведомость, понижение. Ни одного — про то, как её ВЫПОЛНИТЬ.
> Из-за этого законам и факт-чекингу на flang приходилось получать ответы
> вычислений ДАННЫМИ снаружи (`flang/self/SPEC.md`), и круг «язык доказывает
> сам себя» не замыкался: доказательство про поведение программы опиралось на
> то, что посчитал JavaScript.
>
> КРУГ ЗАМКНУТ. Законы моноида и изоморфизма, факт-чекинг, отношения множеств и
> законы монады считают свои ответы этим файлом: 4014 вызовов из 4480; на свидетеля
> уходят 466, и каждый из них упирается в НАЗВАННУЮ границу среза — `к числу`,
> `к числу или беда`, постусловия. Число это стережёт тест: свидетелю нельзя
> посчитать ничего сверх того, что здесь названо «FLANG_SELF_EVAL_UNSUPPORTED».
> Отсюда же свойство, которого у сверок не было: сломай этот файл в одном
> месте — сверка законов, фактов и монады краснеет (`flang/self/SPEC.md`,
> «Круг самоприменения замкнут»).

Осталось: «Законы моноида и изоморфизма, факт-чекинг, отношения множеств и
законы монады считают свои ответы этим файлом. Отсюда свойство: сломай этот файл
в одном месте — сверка законов, фактов и монады краснеет.»

### «Критерий верности»: два дифференциальных измерителя против свидетеля

Было в шапке файла целой врезкой (убрана вместе с заголовком):

> ── Критерий верности ──
> Он дифференциальный и у него ДВА измерителя, а не один.
>
>   1. ЗНАЧЕНИЕ. Для каждого примера корпуса значение, посчитанное здесь,
>      обязано совпасть со значением свидетельского `flang/src/interpret.mjs` —
>      вместе с минус нулём (`равен` работает через `Object.is`, и −0 не равен
>      0), с порядком ключей записи и с формой варианта.
>
>   2. ВИТКИ. Число витков машины обязано совпасть со свидетелем ЧИСЛО В ЧИСЛО.
>      Это главный измеритель: он ловит расхождение семантики раньше, чем
>      значения. Программа, у которой ветвление посчитано не в том порядке или
>      хвостовой вызов не переиспользовал кадр, даст ТО ЖЕ значение и ДРУГОЕ
>      число витков.
>
> Проверка — `flang/test/self-interpret.test.mjs`.

### «Почему явный стек»: ссылка на шапку свидетеля

Было в шапке файла, началом врезки «Почему явный стек, а не рекурсия»:

> По той же причине, что у свидетеля (шапка `flang/src/interpret.mjs`), и ещё по
> одной. У свидетеля: обычные функции flang могут не завершаться, и единственная
> защита — предел витков и глубины; …

Осталось: «Обычные функции flang могут не завершаться, и единственная защита —
предел витков и глубины; …», дальше без изменений.

### «Тотальность»: код отказа как у свидетеля и счёт тотальных функций

Было в шапке файла, врезка «Тотальность»:

> … а честно упереться в предел витков и сказать
> FLANG_RECURSION_LIMIT — тем же кодом и тем же текстом, что свидетель.

> Всё остальное — 203 функции значений, образцов, окружения, встроенных форм и
> диагностики — тотальны и доказаны; обычных 46 из 249.

### «Что вошло в срез»: проверка тестом и долг про `--index-base 0`

Было в шапке файла, врезка «Что вошло в срез и что нет»:

> … Код «FLANG_SELF_EVAL_UNSUPPORTED» остался заведённым и
> проверяемым (`flang/test/self-interpret.test.mjs` требует, чтобы он не
> появился НИ РАЗУ): молчаливый пропуск был бы худшим из исходов — …
>
> Остаётся названным долгом ровно одно, и оно не про выражения: ключ запуска
> `--index-base 0` (`interpret.mjs`, `builtinOptions`), которым «символ»,
> «элемент» и «подстрока» переводятся на счёт с нуля. Сюда его передать нечем —
> у входа этого параметра нет вовсе, — поэтому долг записан здесь словами, а не
> изображён поддержкой.

### «Программа»: во что обходится обход тегов — замер на `self/carriers.flang`

Было у поля «теги» объекта «Программа» (абзац убран целиком):

> Прежняя запись говорила, что обход платят все программы, но платят один
> раз. Прогон показал, во что этот «один раз» обходится: на
> `self/carriers.flang` (552 функции) обход посещает 66 829 узлов и съедает
> 12 320 793 витка из 15 926 783 — 77 % всей подготовки, 184 витка на узел.
> Спрашивает же о тегах РОВНО ОДНО место — исполнение узла «применить»; во
> всём корпусе сверки функцию берут значением 1 файл из 125, а применений
> исполняется 90 на все 2318 примеров. Под доказательством при этом стоят
> программы в 1000–2000 функций, и там тот же обход не укладывался в предел
> сверки вовсе.

Там же убраны ссылки на свидетеля — «конечный список случаев диспетчера
«применить» (`flang/src/tags.mjs`)», «Считается он ЛЕНИВО — ровно как у
свидетеля (`interpret.mjs`, `knownTags`)» — и отсылка к фазе разработки: «с
фазы 4 тег — это имя функции вместе с набором захваченного».

### «Исход прогона»: чем прогон кончался у свидетеля и кто различал предел руками

Было у типа «Исход прогона»:

> ЗАЧЕМ. У свидетеля прогон кончался «значением ЛИБО исключением хозяина»: одна
> половина исхода жила в значении, вторая — в механизме JavaScript. Повторить
> такую границу на flang нельзя ничем — исключений у языка нет by design, — и
> всякий, кто хочет ПРОВЕРЯТЬ чужую программу изнутри flang, упирался не в свою
> задачу, а в границу. Оракул сетки (`self/grid.flang`) из-за неё до сих пор
> получает ответы прогонов таблицей снаружи.

> «Прогон не удался»        — программа отказала: код и текст те же, какими
>                             свидетель бросает;
> «Прогон исчерпал предел»  — витки или глубина кончились. ОТДЕЛЬНЫЙ вид,
>                             потому что три потребителя свидетеля различают
>                             его руками: `factcheck.mjs` («ответ не может
>                             быть дан за отведённый бюджет»), `compat.mjs`
>                             (у тотальной функции предел значит не то же,
>                             что у обычной) и `grid.mjs` (пример, упёршийся
>                             в предел, — «не искали», а не «нарушение»).
> «Программа не готова»     — … Ровно это
>                             различие стоит у `grid.mjs` двумя разными
>                             `try`, и ровно оно здесь выражено полем «беда»
>                             подготовленной программы.

Объяснение четырёх видов осталось; убраны свидетель и имена его модулей —
`factcheck.mjs`, `compat.mjs`, `grid.mjs` названы факт-чекингом, совместимостью
и оракулом сетки.

### «В ёлочках при вычислении»: замер записи цели

Было внутри функции «В ёлочках при вычислении»:

> Замер 29 августа 2026: с «плюс 2» и списком — «объявлено, не доказано», с
> этой записью — «доказано сведением цели с телом функции».

### «Найти описание»: линейный перебор против словаря — таблица замеров

Было над функцией «Найти описание» (убрано целиком, кроме двух поведенческих
фраз):

> ЗДЕСЬ БЫЛ ЛИНЕЙНЫЙ ПЕРЕБОР, И ОН СТОИЛ 87 % ВСЕХ ВИТКОВ ПРОВЕРКИ. Теперь
> ищет словарь имя → описание (`stdlib/hashmap.flang`, ввезён выше), уложенный
> ОДИН раз в «Собрать программу вычислителя». Замер, а не прикидка: два
> двоичных из ОДНОГО семени со счётчиками, «после» — сплайсом напечатанных
> самим flang тел; проверяемые файлы у обоих одни и те же, ключ `--proof`.
>
>   файл                функций   витков всего   в поиске    на зов   код
>   перебор:
>   self/lexer            104         8 092 291     817 611     41,5    0
>   self/interpret        504       324 123 410  38 108 036     19,0    0
>   self/proof-kernel    1474*  1 000 220 767 ⟵ 873 999 841    576,7    1
>   словарь:
>   self/lexer            104         7 454 338     104 165      5,3    0
>   self/interpret        504       300 118 660  12 009 566      6,0    0
>   self/proof-kernel    1474*     213 225 702 ⟵  14 698 772      7,4    1
>
> Со словарём третью строку снимает уже не предел витков, а предел ПАМЯТИ:
> 87 % работы ушло, но проверке proof-kernel.flang не хватает 60 ГиБ. Стену
> эту словарь не создал, а открыл — перебор к ней не успевал дойти: на своём
> последнем витке он уже занимает 33,8 ГиБ. Сам словарь стоит +7,4 % памяти.
>
> Столбец «функций» назвал сам двоичный; со звёздочкой — счёт по файлам, потому
> что у proof-kernel.flang проверка обрывается раньше вывода. Стрелка ⟵ — упор
> в предел витков: с перебором третья строка проверку НЕ ПРОХОДИЛА, и ловил её
> предел именно на «Найти описание».
>
> ПЕРВАЯ ПОПЫТКА ЭТОГО ЖЕ (август, ветка u/slovar-podklyuchenie) ПРОВАЛИЛАСЬ, и
> не на алгоритме: обещания hashmap.flang ядро тогда не доказало, а
> недоказанное обещание едет в напечатанный код сторожем. Сторож вставки звал
> «Звенья словаря» — обход ВСЕГО словаря, и построение на N ключах стоило N²/2:
> 387 610 512 витков на self/interpret.flang. Задача 0035 закрыла обещания
> (`утверждений 41: доказано 26, сетка 15`, `сторожей в рантайме: 0 мест`), и
> то же построение стало стоить 2 093 720 витков — в 185 раз дешевле.
>
> Разбор целиком: docs/zettel/a-dictionary-beats-the-scan-only-after-its-promises-are-proved.md
> Прежние замеры: docs/zettel/linear-lookup-of-a-declaration-eats-88-percent-of-check-loops.md
>                 docs/zettel/a-hash-dictionary-cheapens-the-lookup-and-the-building-costs.md

Осталось: «Ищет словарь имя → описание (`stdlib/hashmap.flang`, ввезён выше),
уложенный ОДИН раз в «Собрать программу вычислителя».» и абзац «Зовётся отсюда:
…».

### «Словарь описаний»: замер обёрток, вторая стена перепечатки, порядок укладки

Было над функцией «Словарь описаний»:

> … Обёртки делают то же самое, но лишним слоем зова; замерено, что
> без них построение на self/lexer.flang стоит 917 754 витка вместо
> 3 359 945 — в 3,7 раза дешевле при том же ответе байт в байт.

> … и даёт FLANG_TYPE_PARAM — ровно поэтому здесь прежде стоял `разбор`,
> у ветви которого ожидание берётся из строки `возвращает`.

> КАДР ЗДЕСЬ БРАЛА ПОЗИЦИЯ, А НЕ ОБЕЩАНИЕ. … Это была ВТОРАЯ стена перепечатки,
> ровно там же, в «Подготовить», сразу за «Описаниями программы». Замер на
> пробе, где «Хеш ключа», «Путь хеша», «Гнездо», «Вписать», «Развести»,
> «Вложить» и «Искать» взяты отсюда же знак в знак: спуску на тысяче описаний
> нужно 1006 кадров, свёртке — 11, и одиннадцать эти не растут с числом
> описаний.

> Порядок укладки повторяет прежний ответ перебора. Перебор отдавал ПЕРВОЕ
> описание с таким именем; спуск укладывал хвост раньше головы, и голова
> затирала уложенное хвостом, — значит побеждало опять первое. Свёртка идёт ОТ
> ГОЛОВЫ, то есть в обратном порядке, и «затирать» ей нельзя: победило бы
> ПОСЛЕДНЕЕ. Отсюда укладка «только если ключа ещё нет»: ответ тот же самый, и
> проверено это не рассуждением, а сличением словарей ЦЕЛИКОМ — на различных
> именах, на повторах и на пустом списке.

Осталось: «Побеждать обязано ПЕРВОЕ описание с таким именем. Свёртка идёт ОТ
ГОЛОВЫ, и «затирать» ей нельзя: победило бы ПОСЛЕДНЕЕ. Отсюда укладка «только
если ключа ещё нет».»

### «Применить функцию»: чем поймали расхождение глубины на хвостовом вызове

Было у функции «Применить функцию»:

> … именно не меняется: у свидетеля в этой ветви `machine.depth` не трогают,
> снятое значение уходит в новый кадр возврата, а не в машину. Разница видна в
> одном месте — в тексте «исчерпала лимит шагов … на глубине вызовов N», — и
> ровно там её и поймала сверка на зациклившейся хвостовой рекурсии: 0 против 1.

### «Уложить тело»: ошибка глубины, которую корпус не поймал

Было у функции «Уложить тело» (абзац убран целиком):

> Ошибка эта тут и стояла, и корпус её не поймал: глубина видна только в
> сообщении об исчерпании предела, а до предела в 10 000 корпус не доходит.
> Поймала проба на СТА ВЫЗОВАХ ПОДРЯД при пределе глубины 5: у свидетеля глубина
> возвращается на место после каждого, поэтому предел не задет, а с прежним
> кодом она росла с каждым вызовом и упиралась в предел на пятом.

Там же убрана ссылка на свидетеля в первом абзаце: «у свидетеля `restoreDepth`
снимается ДО увеличения глубины».

### «Прогон» и «Дальше после шага»: ссылки на наряд 1477 и замер пробой

Было у функции «Прогон»:

> Наряд 1477 говорит правило прямо: у функции с постусловием хвостовых вызовов
> НЕТ — …

> Доказать обещания круга нельзя: ядро не берёт постусловие вызванного, если
> вызванный стоит с зовущим в одном круге взаимной рекурсии — замерено пробой
> на паре функций с ОДНИМ И ТЕМ ЖЕ равенством, которое вне круга доказывается
> правилом «цель есть допущение», а в круге остаётся «объявлено, не доказано».

Было у функции «Дальше после шага»:

> … И имеет пример (наряд 1477, «Постусловие снимается» в
> flang/self/bootstrap/compiler.flang).

### «Списки равны знач»: первая редакция на девяти функциях

Было у функции «Списки равны знач», последней фразой абзаца:

> Первая редакция раскладывала сравнение на девять функций разной арности —
> убывание было, но на разных позициях, и «тотальная» не доказывалась ни одной
> из девяти.

### «Сложить поля»: перевёрнутая запись и чем её поймали

Было у функции «Сложить поля»:

> Порядок ключей записи НАБЛЮДАЕМ: свидетель кладёт их в объект по очереди, и
> `JSON.stringify` печатает их в том же порядке. Первая редакция собирала
> список рекурсией «добавить голову к обработанному хвосту» — и переворачивала
> запись, потому что «добавить» дописывает в КОНЕЦ. Поймала это сверка с
> свидетелем на первой же записи из двух полей.

Осталось: «Порядок ключей записи НАБЛЮДАЕМ: печать выдаёт их в том порядке, в
каком они уложены. Поэтому поля складываются В КОНЕЦ, а не приписыванием головы
к обработанному хвосту, — иначе запись перевернулась бы.»

### «Применить по имени тега»: порядок отказов «разошёлся однажды»

Было у функции «Применить по имени тега»:

> Порядок двух отказов ЗНАЧИМ, и разошёлся он здесь однажды.

### «Теги программы»: счёт файлов корпуса и отсылка к фазе 4

Было у функции «Теги программы»:

> `knownTags` свидетеля: список тегов считается ЗДЕСЬ, при применении, а не при
> подготовке. Программа, ни в одном месте не берущая функцию значением, не
> платит за него ничего — а таких в корпусе сверки 124 файла из 125.
>
> Считается он ЦЕЛИКОМ: с фазы 4 «Теги узла» отдают «Тег» — …

### «Описания программы»: перепечатка, умершая на 845-й минуте

Было над функцией «Описания программы»:

> Спуск ставил на каждую функцию программы кадр глубины и упирался в потолок:
> перепечатка 25–26 августа 2026 умерла на 845-й минуте с
> «FLANG_RECURSION_LIMIT: функция «Описания программы» превысила предел
> глубины вызовов (20000)». Не память (пик 10,3 ГБ) и не витки — глубина.

> … Замер на пробе — та же цепь, тот же
> вычислитель, «flang run … --max-depth»: спуск просит глубины, растущей с
> числом функций, свёртка — постоянной.

> ПОРЯДОК НЕ МЕНЯЕТСЯ НИ НА ОДИН ЭЛЕМЕНТ: … как их дописывал спуск. …
> и наружу выходит беда ПЕРВОЙ сломанной функции — та же, что выходила у
> спуска.

Рядом, у «Шаг описаний», убрана фраза «Спуск на первой беде останавливался».

### «Сбор описаний с именами»: цена проверки повторов, N·(N+1)/2

Было у типа «Сбор описаний с именами» (абзац убран целиком):

> Повтор имени свидетель ловит вопросом к словарю (`functions.has`). Словаря в
> языке нет, и эталон искал имя СВОЕЙ рекурсией по уже собранным описаниям —
> то есть один зов «Найти описание» на каждую пару функций. Число это точное:
> N·(N+1)/2, и прогон его подтвердил знак в знак — 152 628 зовов при 552
> функциях (`self/carriers.flang`), 2 588 052 витка, 16 % всей подготовки. При
> 1074 функциях это 577 275 зовов, при 2028 — 2 057 406, то есть 35 млн витков
> на одну проверку повторов при пределе сверки в 40 млн.

### «Готовая программа»: сверка корпуса девять минут против минуты

Было у типа «Готовая программа»:

> Подготовленная программа — то же разделение, что у свидетеля между
> `createRuntime` и `call`: … Сверка корпуса без этого разделения шла
> девять минут, с ним — минуту.
>
> Обёрткой-вариантом, а не самой записью «Программа», нарочно: значение,
> пришедшее снаружи, свидетель приводит `reifyValue`, и запись он обходит вглубь,
> а готовый вариант возвращает как есть. …

### Построчные пометки о свидетеле

Эти пометки объясняли не поведение, а происхождение записи. Поведение осталось
в исходнике, происхождение — здесь.

| Место | Что было сказано про свидетеля |
|---|---|
| тип «Знач» | «Ровно те же семь видов, что различает `typeName` свидетеля; вариант — отдельный вид … потому что свидетель различает их классом (`FlangVariant`)» |
| тип «Обстановка» | «у свидетеля оно строится `Object.create(родитель)`, то есть новое связывание ЗАТЕНЯЕТ прежнее» |
| «Постусловие при вычислении» | «(`normalizePostconditions` свидетеля)»; «`compat.mjs` обязан отобразить «свойства» утилиты FTS в постусловия функции»; «У свидетеля стоит `property.message ?? умолчание`, поэтому сообщение «» (пустая строка) СВОЁ» |
| «Постусловие при вычислении», долг | «Долг, названный вслух: «имя» свидетель берёт как `item.name ?? ""` и вставляет в текст шаблонной строкой, то есть нестроковое имя (число, признак) он молча приводит правилами JavaScript. Здесь нестроковое имя даёт «», и это расхождение на AST, которого разборщик не порождает» |
| «Описание вычислителя» | «то же, что кладёт в свою таблицу `prepareProgram` свидетеля» |
| «Это запись варианта» | «`encodedVariant` свидетеля»; «цена одного представления вместо двух, названная у свидетеля» |
| «Овеществить» | «(`reifyValue` свидетеля)» |
| «Овеществить может» | «ровно так же, как `reifyValue(undefined)` у свидетеля» |
| «Имя вида знач» | весь комментарий: «`typeName` свидетеля — дословно» |
| «Описать знач» | «`describeValue` свидетеля — дословно, включая кавычки у строки (там стоит `JSON.stringify`, и «Печать строки» ядра даёт тот же байт)» |
| «Поля равны знач» | «`recordsEqual` свидетеля» |
| «Толкнуть» | «`pushEval` свидетеля»; «Отсутствующий ключ свидетель печатает словом «undefined» (там `JSON.stringify(undefined)` даёт не строку, и стоит `?? "undefined"`)»; «означает `undefined` свидетеля» |
| «Первое значение» | «свидетель кладёт параметры в объект по очереди, и при повторе имени побеждает ПОСЛЕДНИЙ» |
| «Шаг возврата» | «`stepReturn` свидетеля» |
| «Постусловие дальше» | «у свидетеля стоит `Object.create(frame.env)` и присваивание по имени связки» |
| «Шаг поля при вычислении» | весь комментарий: «`stepField` свидетеля» |
| «Шаг если» | «`stepIf` свидетеля» |
| «Шаг ряда» | «`stepSeq` свидетеля» |
| «Начать ряд» | «`startSeq` свидетеля зовёт `stepSeq` НАПРЯМУЮ, а не через укладку кадра» |
| «Применить свой тег» | «обоснование в tags.mjs» |
| «Применить известный тег» | «обоснование в `flang/src/tags.mjs`» |
| «Шаг разбора» | «`stepMatch` свидетеля» |
| «Сверить образец» | «`matchPattern` свидетеля» |
| «Связки головы» | «у свидетеля стоит `pattern.head !== undefined && pattern.head !== null`» |
| «Пары связывания» | «`bind` свидетеля» |
| «Пара узлов» | «у свидетеля `[expr.left, expr.right]` может содержать `undefined`» |
| «Поле списком при вычислении» | «`Array.isArray` свидетеля»; «(`expr.args ?? []`)»; «правило у свидетеля одно» |
| «Вычислить вызов» | «стоит у свидетеля в `startSeq`, то есть до вычисления первого аргумента» |
| «Запись объявлена» | «ровно с тем же условием, что у свидетеля» |
| «Имя формы» | «`canonicalBuiltinName` свидетеля» |
| «Слово аргумента» | «то же, что у `plural` свидетеля» |
| «Описание с телом» | «Порядок проверок при подготовке повторяет свидетель дословно»; «обе стороны обязаны пожаловаться на одно и то же» |
| «Все параметры именованы» | «Третьего свидетель не принимает» |
| «Постусловия функции» | «`normalizePostconditions` свидетеля»; «(`typeof item.bind === "string"`)» |
| «Параметры описаний при вычислении» | «(`tags.mjs`, `declared`)»; «у свидетеля там же стоит `functions.has`» |
| «Вызвать исходом» | «(та же причина, по которой у свидетеля отчёт пишется в `finally`)» |
| «Вызвать в программе исходом» | «Свидетель различает то же самое тем, что `createRuntime` бросает ДО вызова, а не из него» |
| «Вызвать готовую исходом» | «у свидетеля они летят из `callFunction`» |
| «Это целое» | «`Number.isInteger` свидетеля» |
| «Форма символ по коду при вычислении» | «порядок их тот же, что у свидетеля» |
| «Знач не разобрано» | «а «код» стоит у свидетеля перед «сообщением»» |
| «Форма приписать при вычислении» | «а тот у свидетеля идёт запасом СПЕРЕДИ (`builtins.mjs`, «Список с запасом»)» |

## `flang/self/io.flang`


Проверка объявления `план` — на самом flang.

Три приметы «снято» в шапке НЕ ТРОНУТЫ: их читает
`scripts/prose-numbers-guard.sh`, и числа 22, 20 и 4 стоят от них в тех же трёх
строках выше, что и прежде.

### Эталон, семь его спутников и дифференциальный критерий

Было в шапке файла:

> Эталон `checkPlans` и семи его спутников из `flang/src/io.mjs`
> (`состояниеПлана`, `проверитьНачало`, `проверитьШаг`, `сверитьИмяТипа`,
> `типОдним`, `planStateType`, `planResultIsShared`). Критерий верности тот же,
> что у остальных слоёв самоприменения, и он дифференциальный: для каждой
> программы корпуса множество диагностик `FLANG_PLAN`, выданное этим файлом,
> обязано совпасть с тем, что даёт свидетель, — код, текст сообщения и место, в
> том же порядке. Проверка — `flang/test/self-io.test.mjs`.

### Обещания этого файла: замер 29 августа 2026 и цена недоказанного

Было в шапке файла — раздел целиком:

> ── ОБЕЩАНИЯ ЭТОГО ФАЙЛА: ДОКАЗАННЫЕ ОСТАЛИСЬ, ОСТАЛЬНЫЕ СНЯТЫ ─────────────
>
> Было 60 объявленных `обеспечивает`. Отчёт о доказательствах 29 августа 2026
> (`flang check flang/self/io.flang --proof`) разложил их так: доказано 19,
> «сетка N значений» 5, «объявлено, не доказано» 36. Осталось 19 — те самые
> доказанные, и у каждого из них есть `пример`.
>
> ЦЕНА НЕДОКАЗАННОГО ИЗМЕРЕНА, А НЕ ПРЕДПОЛОЖЕНА. `flang emit
> flang/self/io.flang --target c` до этой чистки печатал 128 проверок
> `fl_post`, и 41 из них — обещания этого файла: ровно те, у которых нет пары
> «ядро сказало доказано» плюс «есть хотя бы один пример». Оба условия и есть
> правило снятия («Постусловие снимается» в `flang/self/bootstrap/compiler.flang`);
> все 19 оставшихся ему удовлетворяют и в напечатанный код не уезжают вовсе.
>
> Проверка при работе — не вся цена. Функция с постусловием теряет хвостовые
> вызовы и не свёртывается в цикл («Хвостовые вызовы» и «Самовызов в хвосте» в
> `flang/self/emit-c.flang`): кадру есть что посчитать после возврата. Ввези
> этот файл в замыкание перепечатки как он был — и 53 функции прибавились бы к
> тем, что растят стопку вызовов вместо цикла.
>
> Снято, а не переписано в доказуемый вид: снятое обещание честнее ложного и
> дешевле недоказанного, а переписывание 41 утверждения — отдельная работа с
> отдельной ценой. Примеры при этом не тронуты ни один: они и есть корпус,
> которым этот файл сверялся с удалённой реализацией на JavaScript.

### Три имени переименованы под ввоз и контрольный опыт сторожем столкновений

Было в шапке файла:

> ── ТРИ ИМЕНИ ПЕРЕИМЕНОВАНЫ ПОД ВВОЗ ───────────────────────────────────────
>
> … и три его
> имени, занятые в замыкании компилятора, пришлось развести:
>
>   «Имена через запятую» → «Имена планов через запятую»   (занято types.flang)
>   «Скаляр по имени»     → «Скаляр плана по имени»        (занято types.flang)
>   «Проверить шаг»       → «Проверить шаг плана»          (занято proofterm.flang)
>
> Контрольный опыт: со старыми именами
> `node flang/scripts/link-collision-guard.mjs --дерево` называет три настоящих
> столкновения, с новыми — ни одного.

Довод, по которому имена длиннее обычного (плоское пространство имён связывания,
`только` роняет объявление), в файле остался; список имён переписан как факт, а
не как событие переименования.

### Мелкие ссылки на свидетеля, снятые по одной

Было у разных объявлений — журнальная половина вычеркнута, поведенческая
осталась:

> ИСПОЛНИТЕЛЯ. `runPlan` зовёт вычислитель значением, а не разбирает AST: его
> эталон стоит на `self/interpret.flang` целиком…

> `checkPlans` спрашивает у `ctx` ровно четыре вещи…

> `сверитьИмяТипа` сравнивает типы ПО ИМЕНИ, а сигнатура приходит от
> `normalizeType` УЖЕ РАЗВЁРНУТОЙ… Значит развернуть псевдоним обязан и эталон —
> иначе программа, где состояние названо псевдонимом, разошлась бы со свидетелем
> молча. Отсюда «Сверяемый тип» ниже: она повторяет `normalizeType`
> ровно в той части, которая видна `сверитьИмяТипа`…

> Тип, приведённый к тому, что от него нужно сверке имён. Три случая, и все три
> взяты у свидетеля, а не придуманы:
>
>   «Джокер» — `получено?.kind === "unknown"`: сверка молчит. Сюда же уходит
>      всё, о чём `normalizeType` говорит `UNKNOWN`…
>   «Тип с именем» — `получено?.name`: запись, сумма, параметр типа.
>   «Тип без имени» — `типОдним(получено)`…

> Четыре вопроса `ctx`, и ничего сверх них.

> Псевдоним здесь
> сверяется по имени намеренно (`состояниеПлана` свидетеля)…

> Ровно три
> таблицы `ctx`, к которым по очереди обращается `состояниеПлана`…

> Повторяет `normalizeType` ровно в видимой ей части. «Глубина» — сторож цикла
> псевдонимов: `«А» это «Б»`, `«Б» это «А»` в свидетеле ловится множеством
> `aliasOpen`, здесь — счётчиком, и оба дают один ответ…

> `состояние` у узла
> типа — маркер доказательства из наследия FTS, и `normalizeType` уходит на
> джокер РАНЬШЕ всех прочих разборов.

> Порядок — свидетельский (`namedOrScalar`): встроенное имя важнее одноимённого
> объявления.

> Вид здесь и есть то,
> что уезжает в сообщение вместо имени, — `типОдним` отдаёт `тип.kind`…

> ПЕРВОЕ объявление имени
> побеждает: `collectSignatures` свидетеля о повторе жалуется и второе
> пропускает.

> Наружу торчит одна функция от программы, ровно как у свидетеля наружу торчит
> `checkPlans(program, ctx)`.

> у свидетеля `состояниеПлана` и возвращает тип, и пишет в
> `ctx`, а здесь один и тот же разбор спрашивается дважды…

> Жалоба на арность НЕ прерывает проверку: свидетель после неё сверяет ещё и тип
> возврата…

> Джокер снимает сверку целиком, и это ровно `получено?.kind === "unknown"`
> свидетеля…

> у свидетеля сравнивается `получено?.name`, а у списка и
> у скаляра его нет вовсе. Совпади слово «список» с именем объявления —
> сравнение сказало бы «сошлось» там, где свидетель говорит «нет».

## `flang/self/iso.flang`


Обратимость изоморфизма на самом flang.

### Откуда файл списан и где доказано устройство изоморфизма

Было в шапке файла:

> Обратимость изоморфизма — на самом flang. Копия `flang/src/iso.mjs`.

> Устройство изоморфизма доказано раньше и в другом месте (`src/types.mjs`): …

### Почему сосед, а не третий модуль: общее место у свидетеля

Было в шапке файла:

> Равенство значений, ключ вызова, поиск ответа в таблице и печать значения в
> текст диагностики у свидетеля общие: обе проверки берут их из `builtins.mjs`.
> В самоприменённом слое такого общего места нет …
> … предел сетки
> объявлен здесь, как объявлен он и у свидетеля отдельной константой в каждом из
> двух файлов, — предел объявляет тот, кто сетку строит.

### Об овеществлении: шага `reifyValue` здесь нет

Было в шапке файла целым разделом:

> ── Об овеществлении ────────────────────────────────────────────────────────
> Свидетель прогоняет каждое значение сетки через `reifyValue`, и здесь этого шага
> нет — не по недосмотру. `reifyValue` переводит ЗАПИСЬ, закодировавшую вариант
> (ровно два ключа, `variant` и `fields`), в настоящий вариант времени
> выполнения; на всём остальном она возвращает значение как есть. В «Значении»
> (`core/json.flang`) варианта нет вовсе — это JSON, и закодированный вариант
> остаётся записью с двумя полями. Равенство от этого не меняется: две такие
> записи свидетель сравнил бы как варианты (имя плюс поля), а здесь они
> сравниваются как записи (те же два ключа плюс те же значения), и ответ у обеих
> дорог один. Шаг, который на этом представлении тождественен, не пишется:
> написанное тождество читалось бы как перевод, которого не происходит.

(Поведенческая половина — про представление варианта записью — оставлена в файле
под врезкой «Овеществления нет».)

### Откуда взялось поле «нарушений» и как оно уезжает наружу

Было у объекта «Проверенный изоморфизм»:

> Поле «нарушений» появилось вместе с той же правкой у свидетеля
> (`flang/src/iso.mjs`, `work/ledger-honest`): без него нарушенный изоморфизм
> попадал в «проверено» ровно так же, как честный …
>
> Наружу поле уезжает только при ненулевом значении — решает это перевод в
> форму свидетеля (`flang/test/self-laws.test.mjs`), тем же приёмом, каким свидетель
> пишет `...(нарушений === 0 ? {} : { violations })`.

### Мелкие сноски на свидетеля

Было у функции «Морфизм по имени» (дважды — снаружи и внутри), у зова стрелки и
у пустой программы:

> Последний по объявлению, а не первый: свидетель складывает стрелки в отображение
> по имени, и повторное имя вытесняет прежнее.

> Свидетель складывает стрелки в отображение по имени: о самом повторе
> говорит проверка имён, а не эта.

> и свидетель отвечает на неё тем же текстом, что и на
> отсутствие функции, — звать всё равно нечего.

> Программа без единого объявленного изоморфизма отдаёт три пустых списка — тот
> же ранний выход, что у свидетеля …

---

## `flang/self/law-oracle.flang`


### Сверки со свидетелем, снятые построчно

Было у «Значения примера стрелки» (абзац убран целиком — без свидетеля он
ничего не объяснял), «Шаг круга», «Имя обёртки единицы», «Поле если есть»,
«Единица моноида» и «Замкнуть моноиды»:

> // Аргументы примера и ожидаемое — в том же порядке, в каком их берёт свидетель.

> // посчитался, идёт дальше ПУСТЫМ МЕСТОМ, а не «ничем», — как `аргументом(undefined)`
> // у свидетеля.

> // без аргументов и зовётся по её имени — ровно то же делает свидетель
> // (`вычислитьЕдиницу` в `flang/src/monoid.mjs`). Второго вычислителя ради одного
> // литерала здесь нет, как нет его и у свидетеля.

> // Поле кладётся, ТОЛЬКО если оно есть: у свидетеля перевод узла выбрасывает
> // ключи со значением `undefined`, и ключ, положенный здесь пустым, дал бы
> // вычислителю другое объявление.

> // «единицы нет»: законы проверять не на чем, и свидетель говорит ровно это.

> // Единицы и узлы идут парами по порядку объявления: свидетель берёт их
> // `единицы[номер]`, а номера у языка нет — есть два списка, которые убывают
> // вместе.

---

## `flang/self/lexer.flang`


Лексер языка на самом flang. Ч334 файл уже правила; здесь остаток.

### Что про лексер было доказано до обещаний и чем проверена их непустота

Было в шапке файла, в разделе «обещания: что здесь сказано о поведении, а не
только о завершении»:

> Долгое время про лексер было доказано ровно одно: что он ЗАВЕРШАЕТСЯ (все 99
> функций объявлены тотальными и признаны такими). О том, ЧТО он считает, не
> было сказано ни слова: обещаний в файле стояло ноль.
>
> Теперь обещания стоят там, где ядро сводит их к телу функции, а не к её
> подписи. Проверка на пустоту делалась прогоном, а не рассуждением: тело
> каждой функции с обещанием заменялось заглушкой той же подписи (число → 0,
> строка → "", признак → нет, список → пустой список, запись → запись с
> пустыми полями), и обещание обязано было при этом покраснеть. Обещание,
> пережившее заглушку, говорит о подписи, а не о функции, и в дереве не
> осталось.

---

## `flang/self/link.flang`


Связывание исходников.

### Замер 20 августа 2026: имя во ввозе совпадает с именем модуля

> и на дереве 20 августа 2026 это выполнено у 206 ввозов из 206 разрешимых.

### Замер цены второй двери связывания

> Ею пользуется сверка на корпусе — разбирать корпус вычислителем стоит
> 26 минут против 40 секунд у самого связывания, а разбор сверен побайтово
> отдельно и на всём дереве.

### Улика, ради которой заведена отсечка мёртвого после связывания

> УЛИКА. `использует «Списки» из "…/lists.flang"` тянет ВЕСЬ модуль — связывание
> так и задумано, — но печать принимала слияние за программу. Мерено на
> `flang/examples/import-check.flang` (один вызов «Сумма» из модуля в 698 строк):
> после связывания 29 функций, из них вызывается 2, а 27 не вызываются никогда.
> […] напечатанный прогонщик отвечал на «Двоичный поиск» у программы, автор
> которой двоичного поиска не писал.

Файла `flang/examples/import-check.flang` в дереве нет.

## `flang/self/lsp.flang`


Языковой сервер flang, написанный на самом flang.

### Размер снятой реализации на JavaScript

Было в шапке файла, в разделе «Зачем»:

> // Реализация компилятора на JavaScript выбрасывается, и языковой сервер — один
> // из кусков, у которых эталона не было вовсе: `flang/src/lsp.mjs` — 900 строк,
> // `flang/bin/flang-lsp.mjs` — ещё 185. Сервер отвечает на четыре вопроса
> // редактора — … — и до этой работы на них отвечал только
> // JavaScript.

### Критерий верности был побайтовым и дифференциальным — против `flang/src/lsp.mjs`

Было в шапке файла — целый раздел:

> // ── Критерий верности — побайтовый ─────────────────────────────────────────
> // Тот же, каким проверены все эталоны до этого: одна и та же
> // последовательность сообщений JSON-RPC, поданная в `flang/src/lsp.mjs` и сюда,
> // обязана дать один и тот же поток исходящих сообщений — знак в знак. Сверять
> // «эквивалентный JSON» здесь нельзя: порядок ключей у ответа LSP — часть
> // договора с редактором, и `JSON.stringify` свидетеля его сохраняет. Поэтому
> // эталон отдаёт исходящее НАПЕЧАТАННЫМ: «Значение» из `core/json.flang` и
> // «Печать значения», уже доказанная побайтово. Проверка —
> // `flang/test/self-lsp.test.mjs`.

Ни `flang/src/lsp.mjs`, ни `flang/bin/flang-lsp.mjs`, ни
`flang/test/self-lsp.test.mjs` в дереве нет. **Критерия верности у этого файла
теперь нет никакого**: он весь состоял в сверке потока JSON-RPC со свидетелем.
Поведенческая половина («порядок ключей — часть договора с редактором, поэтому
исходящее отдаётся напечатанным») в файле оставлена под заголовком «Порядок
ключей у исходящего».

### Пять изменяемых таблиц свидетеля и порядок обхода `Map`

Было в шапке файла, в разделе «Состояния в языке нет»:

> // Свидетель держит сервер ящиком с пятью изменяемыми таблицами (`документы`,
> // `поПути`, `опубликовано`, `поколения`, `готов`). Здесь то же самое — ЗНАЧЕНИЕ
> // «Сервер», которое приходит в «Принять» и возвращается из него новым.

> // Словаря в языке тоже нет, поэтому каждая таблица свидетеля — СПИСОК пар, и
> // порядок в нём хранится руками. Это не мелочь оформления: `Map` в JavaScript
> // обходится в порядке вставки, а `разнести` публикует диагностику именно в
> // порядке обхода. Список, потерявший порядок, разошёлся бы со свидетелем молча.

> // Одной таблицы свидетеля здесь нет вовсе — «поколения».

### Четыре ввоза свидетеля, названные его именами файлов и функций

Было в шапке файла, в разделе «Где проходит граница»:

> // Свидетель ввозит три вещи из чужих модулей и одну из хозяина:
> //
> //   • `checkSource` (`repl.mjs`) — разбор, СВЯЗЫВАНИЕ, типы, завершаемость.
> //     Связывания на flang нет (`src/link.mjs` в самоприменение не переписан), а
> //     чтение импортируемых модулей — эффект, которого в чистом языке быть не
> //     может. … хозяин зовёт ту самую
> //     `checkSource` и приносит ответ в «Принять проверенное».
> //   • `typeName` (`types.mjs`) — имя ПОСЧИТАННОГО типа. … перевод одного в другой был бы третьим словарём имён типов —
> //     ровно то, чего шапка свидетеля запрещает.
> //   • `KEYWORDS` и `keywordId` (`lexer.mjs`) — берутся у эталона лексера, а не
> //     переписываются сюда списком. … и так и задумано свидетелем: «список-копия разошёлся
> //     бы с языком на первой же новой форме, причём молча».
> //   • перевод адреса в путь и обратно (`кПути`, `кАдресу` — 14 строк свидетеля).
> //     Это `path.resolve` и процентное кодирование UTF-8 …

Здесь же — отдельный факт, который нигде больше не записан: **связывания
(`src/link.mjs`) в самоприменение не переписано**, и потому вся проверка
исходника отдана хозяину поручением. Сам довод в файле оставлен, без имени
файла свидетеля.

Оттуда же, из «Чего этот файл не знает»:

> // Транспорта — как и свидетель: рамок `Content-Length`, потоков и процесса здесь
> // нет ни строки, они живут в `flang/bin/flang-lsp.mjs`.

### Набор знаков имени назван регулярным выражением свидетеля

Было в шапке файла и у «Части имени»:

> // 2. `\p{L}\p{N}_-` свидетеля сужено до алфавита, которым язык вообще пишется, —
> //    того же, что у лексера («Части имени»), минус `$`, которого в наборе
> //    свидетеля нет.

> // `\p{L}\p{N}_-` свидетеля, суженное до алфавита, которым язык вообще пишется.

> // `\s` свидетеля, суженное до тех пробельных знаков, которыми пишется исходник.

### Размер печатника в C, из-за которого доступ к полям выписан заново

Было у раздела «значения AST»:

> // ввоз потянул бы за
> // собой 377 КБ печатника в цель C, которая языковому серверу не нужна ни
> // строкой.

### Сторож `proof-soundness.test.mjs`, удалённый коммитом `d5361d2c`

Было у функции «Вид объявления» — весь блок:

> // Утверждение здесь появилось 23 августа 2026, и довод против него протух:
> // сторож класса `flang/test/proof-soundness.test.mjs`, требовавший враждебной
> // сетки на КАЖДОЕ утверждение корпуса, удалён (коммит d5361d2c, «не
> // загружается»). Сетку по «Объявлению» по-прежнему построить нечем — его поле
> // «узел» это «Значение», сумма, рекурсивная ЧЕРЕЗ СПИСОК, — но с тех пор ни
> // одно утверждение дерева на сетке не гоняется, и запрет разбирал бы только
> // эту функцию.

Дата, коммит и «довод протух» — журнал; но здесь же записан и живой факт:
**сетку по «Объявлению» построить нечем**, потому что его поле «узел» —
«Значение», сумма, рекурсивная через список.

### Требования `corpus-claims.test.mjs` как довод за и против утверждений

Было у видов элемента дополнения:

> // Утверждение при каждом не украшение: ядро доказательства дотягивается до
> // литерала, и `flang/test/corpus-claims.test.mjs` требует, чтобы у всякой
> // функции корпуса, до которой оно дотягивается, утверждение было НАПИСАНО.

и у уровня диагностики:

> // Утверждения в этом файле
> // стоят только там, где они стоят O(1) и где их берёт ядро, — у семи чисел вида
> // элемента и у «Вида объявления». Тот же довод выписан в
> // `flang/test/corpus-claims.test.mjs`.

Файла `flang/test/corpus-claims.test.mjs` в дереве нет; сами правила («ядро
дотягивается до литерала — утверждение обязано быть написано» и «утверждения
стоят там, где они O(1)») в файле оставлены.

### Счёт обычных функций у соседей как довод за нетотальность

Было у типа «Нормальный»:

> // Тот же
> // довод стоит у 110 обычных функций печатника в C и у 305 обычных функций
> // разборщика: обход AST, приехавшего обобщённым значением, структурной
> // рекурсией не выражается.

### Прежняя запись «Искать пару», рекурсивная по числу

Было у функции «Искать пару»:

> // Убывающего аргумента у прежней записи не было — она рекурсивна по числу.

### Побайтовая сверка как сторож двух копий имён типов

Было у названий нормальных типов:

> // Слова здесь принадлежат `typeName` (`types.mjs`), а не серверу …
> // Расхождение этих двух копий сторожится побайтовой сверкой на корпусе: имя
> // типа выходит наружу в подписи поля, в подписи варианта и в наведении.

Сторожа больше нет — сверка была со свидетелем.

### Тридцать мест, где поведение было названо кодом свидетеля

Было по одной-две строки у отдельных функций:

> // Значение ключа; отсутствующий ключ даёт «ничто» — так же, как `node.поле`
> // в JavaScript даёт undefined, а `undefined` свидетель везде сводит к «ничто».

> // `узел.total === true` свидетеля: истинно только для настоящего признака «да».

> // Ключ, которого нет, и ключ со значением «ничто» свидетель различает: `span.file`
> // undefined уводит диагностику в журнал, а `span.file` со значением — в буфер.

> // `текст.split(/\r\n|\r|\n/u)` свидетеля.

> // `/^[ \t]*/u.exec(строка)[0].length` свидетеля.

> // `строка.trim() === ""` свидетеля.

> // `строка.trim()` свидетеля, суженный до тех же пробельных знаков.

> // `trim()` свидетеля берёт всякий пробельный знак, а `/^[ \t]*/u` отступа — только
> // пробел и табуляцию. Это два разных счёта, и путать их нельзя.

> // `строка.indexOf(знак, от)` свидетеля: место первого вхождения ОДНОГО знака,
> // считая от заданного места; −1, если вхождения нет.

> // `имяВМесте` свидетеля.

> // Пары «открыть … закрыть» просматриваются слева направо, как это делает цикл
> // свидетеля …

> // `началоСлова` свидетеля: что именно заменяет дополнение …

> // `/[\p{L}\p{N}_-]*$/u` свидетеля: сколько знаков имени стоит в самом конце.

> // `вНормальный` свидетеля: узел типа из разбора приводится к тому виду, которым
> // `types.mjs` называет типы в диагностике.

> // … и обе сводятся к одной так же, как в `types.mjs`.

> // `путь.split("/").pop() ?? путь` свидетеля; у отсутствующего пути своего имени
> // нет, и свидетель называет его словами.

> // … в том же
> // порядке, в каком их обходит `Object.entries(KEYWORDS)` свидетеля …
> // … — ровно порядок вложенного цикла.

> // ── дополнение: три ветки свидетеля ──────────────────────────────────────────

> // `/(?:^|\s)(?:разбор|match)\s+(.+)$/u` свидетеля: слово `разбор` в начале строки
> // или после пробельного знака, за ним пробел и хоть один знак.

> // `/^«([^»]*)»/u ?? /^"([^"]*)"/u ?? /^([\p{L}\p{N}_-]+)/u` свидетеля.

> // `оформитьИмя` свидетеля: имя берётся в ёлочки тогда, когда одним словом не
> // записывается.

> // Имена типов приезжают СТРОКАМИ:
> // их печатает `typeName` свидетеля, и почему он не переписан сюда — сказано в
> // шапке.

> // `с.поПути.get(файл) ?? кАдресу(файл)` свидетеля: сперва открытый буфер, потом
> // то, что о путях знает хозяин.

> // `${значение}` свидетеля: подстановка в шаблон строки. Отсутствующий ключ даёт
> // «undefined» — свидетель печатает именно это слово …

> // `узел.поле === null` свидетеля — не «ключа нет», а именно «ключ со значением
> // ничто».

> // `узел.поле === null || узел.поле === undefined` свидетеля.

> // `/^\s*(?:случай|case)\s/u` свидетеля.

> // `узел.domain ?? узел.value?.domain` свидетеля.

> // Вставляется ровно метка: свидетель пишет `элемент.insert ?? элемент.label`, и ни
> // один из четырёх сборщиков элементов `insert` не задаёт.

> // Транспорт и разбор JSON остаются у хозяина —
> // как и у свидетеля, который принимает уже разобранный объект. Поля здесь ровно
> // те, что свидетель читает у `params`, и каждое сопровождено признаком «есть»:
> // `undefined` и значение свидетель различает в шести местах.

> // Что приносит хозяин, сходив в `checkSource`. Поля — те же четыре, что свидетель
> // получает от неё, плюс тексты прочитанных модулей (свидетель копит их сам, по
> // дороге) и пары «путь ↔ адрес».

> // У свидетеля существующий документ сохраняет СВОЙ путь: `кПути` считается заново,
> // но кладётся только в таблицу путей, а в документ — нет. Повторено дословно.

> // Очередь
> // нужна `didSave`: сохранение соседнего файла меняет то, что видит импорт, и
> // свидетель перепроверяет ВСЁ открытое, по одному документу за раз.

> // Сбой `checkSource` свидетель превращает в диагностику без места — и она уходит в
> // журнал, а не подчёркивает первую попавшуюся строку.

Во всех этих местах поведение оставлено сказанным своими словами; убраны только
имена и выражения снятой реализации на JavaScript.

## `flang/self/mcp.flang`


Ничего не вынесено: единственный кусок, набранный ситом («Где проходит граница,
и почему именно там»), ссылается на живые файлы дерева —
`flang/self/lsp.flang` и `flang/src/emit/c/flang_repl.c`, — и объясняет
поведение, а не сверку со свидетелем.

## `flang/self/monad-expand.flang`


Разворачивание формы `в монаде`. Свидетель `flang/src/monad.mjs` (635 строк) и
сверки `flang/test/self-setsmonad.test.mjs`, `flang/test/self-parser.test.mjs`,
`self-totality.test.mjs` — этих файлов в дереве больше нет.

### Чем был этот файл: копия `expandMonads` на 635 строк

Было первой фразой шапки:

> Копия `expandMonads` из `flang/src/monad.mjs` (635 строк, из них
> разворачивание — строки 82—349).

### Замер, которым названа цена ввоза `monad.flang` в парсер

Было врезкой «ПОЧЕМУ ОТДЕЛЬНЫМ ФАЙЛОМ» (довод остался, ушли числа):

> Ввези парсер весь `monad.flang` — и связанный `self/parser.flang`
> вырос бы с 678 функций до 1832, то есть в 2,7 раза. Цена этого не выдумана:
> анализ завершаемости растёт по числу функций круче линейного — 30,6 млн
> витков на 678 функциях против 126,5 млн на 1201 (`self-totality.test.mjs`,
> замеры в шапке), то есть примерно как n^2,5, — и 1832 функции стоили бы около
> 350 млн при пределе 280.

> Здесь же ввозится только печать JSON, и связанный парсер вырастает на
> девяносто с небольшим функций.

### Чем проверялась дословность копий

Было в шапке, отдельным абзацем (ушёл целиком — сверять не с чем и нечем):

> Копии дословны, и это проверяется не глазом: `«Развернуть монады»` сверяется
> с `expandMonads` побайтово на всём корпусе и на четырнадцати программах с
> блоками (`flang/test/self-setsmonad.test.mjs`), а вся форма целиком — на
> `flang/examples/monad/order-total.flang` в `flang/test/self-parser.test.mjs`.

### Чем порядок работ отличался от свидетеля

Было врезкой шапки (довод про исключения и завершение остался, ушла сверка):

> ── ЧЕМ ПОРЯДОК РАБОТ ОТЛИЧАЕТСЯ ОТ СВИДЕТЕЛЯ ─────────────────────────────────
>
> Свидетель бросает исключение из середины обхода: встретил блок с незнакомой
> монадой — и всё разворачивание кончилось. … «Первый отказ блоков» проходит
> блоки В ТОМ ЖЕ ПОРЯДКЕ, в каком их встретил бы свидетель … Ответ от этого тот
> же самый.

### Замер 29 августа 2026 — почему цель записана парной склейкой

Было у обещания длины (сам довод про то, что ядро умеет, остался):

> Замер 29 августа 2026: с «плюс 2» и списком — «объявлено, не доказано», с
> этой записью — «доказано сведением цели с телом функции».

### Захват имени — дефект, найденный запуском

Было врезкой «свободное имя» и повтором у функции (переписано в сослагательном
наклонении, как свойство нынешнего кода):

> ЗАХВАТ ИМЕНИ — ДЕФЕКТ, НАЙДЕННЫЙ ЗАПУСКОМ. Соседние поля несущего варианта
> копировались под своими же именами, и это молча крало имена у автора.

> Захват имени — дефект, найденный запуском: соседние поля несущего варианта
> копировались под своими же именами и молча крали имена у автора.

### Мелкие сверки со свидетелем, снятые с абзацев-объяснений

| было в исходнике | где стояло |
|---|---|
| «Два места переписаны не дословно» | врезка «мелочи, переписанные ради независимости» |
| «Это `String(значение)` свидетеля, а не JSON: у него запись печатается именно так, и сообщение об отказе обязано совпасть знак в знак» | «Как значение в монаде» |
| «так, как его напечатала бы вставка в строку у свидетеля» | «Имя поля в монаде» |
| «`{ …узел, ключ: значение }` свидетеля» | «Положить поле» |
| «`JSON.stringify(значение)` свидетеля» | «Показать в монаде» |
| «это и есть `encodedVariant` свидетеля» | «Это вариант» |
| «`устройствоМонады` свидетеля» | врезка «устройство монады из объявлений» |
| «Ключи детей те же, что у свидетеля» | обход типа |
| «`параметры.includes(узел.param)` свидетеля» | сличение имени параметра |
| «ровно как у свидетеля, где `занято.add` живёт внутри поиска свободного имени» | ветвь без параметра |
| «даёт ровно то, что свидетель делает руками (`{ …узел, binds: …, result: … }`)» | обход блока `в монаде` |
| «в каком их встретил бы обход свидетеля… свидетель бросает исключение на ПЕРВОМ негодном блоке» | «Первый отказ блоков» |

---

## `flang/self/monad.flang`


### Копия свидетеля и соответствие имён двух работ

Было в шапке файла:

> // Копия `flang/src/monad.mjs`. Здесь ДВЕ разные работы, и граница между ними —
> // главное, что надо прочитать раньше кода:
> //
> //   «Развернуть монады»      ← expandMonads   — РАЗВОРАЧИВАНИЕ. Работает внутри
> //     разбора и обязано оставить программу, в которой никакой монады уже нет:
> //     обычные вызовы, обычный `разбор`, обычные конструкторы вариантов;
> //   «Проверить законы монад» ← checkMonadLaws — ПРОВЕРКА. Она вычисляет и
> //     предъявляет контрпример, потому что законы монады — это равенства
> //     ВЫЧИСЛЕНИЙ, а их на бесконечном множестве не решить.

### Замер: во сколько вырос бы связанный парсер

Было в шапке файла:

> // вырос бы с 678 функций до 1832. Замер и предел, в который это упирается,
> // названы в шапке соседа.

### Свидетель: откуда берутся вычисленные значения

Было в шапке файла, раздел «ОТКУДА БЕРУТСЯ ВЫЧИСЛЕННЫЕ ЗНАЧЕНИЯ»:

> // намеренно: свидетель берёт `valuesEqual` и вызов вычислителя из одного места на
> // оба слоя (`builtins.mjs`, `interpret.mjs`), и две копии одного ответа
> // разъехались бы молча.

### Чем порядок работ отличался от свидетеля

Было в шапке файла:

> // ── ЧЕМ ПОРЯДОК РАБОТ ОТЛИЧАЕТСЯ ОТ СВИДЕТЕЛЯ ─────────────────────────────────
> //
> // Свидетель бросает исключение из середины обхода: встретил блок с незнакомой
> // монадой — и всё разворачивание кончилось. Исключений в языке нет, а
> // протаскивать отказ через каждый узел обхода значило бы удвоить обход и
> // потерять его доказанное завершение. Поэтому здесь отказы ищутся ДО обхода —
> // «Первый отказ блоков» проходит блоки В ТОМ ЖЕ ПОРЯДКЕ, в каком их встретил бы
> // свидетель (внутренние раньше внешних, связывания раньше `возврат`), и обход
> // зовётся только тогда, когда отказов нет. Ответ от этого тот же самый, а
> // доказательство завершения остаётся при обходе.

Заголовок в исходнике переписан на «ПОЧЕМУ ОТКАЗЫ ИЩУТСЯ ДО ОБХОДА», само
объяснение порядка осталось.

### Прочие ссылки на свидетеля и на удалённые файлы

> // значило бы проверять закон не на том типе. Отсутствующего поля нет и в
> // свидетеле: `undefined` в сетку не кладётся.

> // которого лечился весь отчёт (`flang/src/monad.mjs`, `work/ledger-honest`).

> // АССОЦИАТИВНОСТЬ. Тройка считается ДО проверки — так же, как у свидетеля:

> // той же причине: у честной монады запись обязана остаться прежней байт в байт —
> // на её форму сослались тесты законов дословно, и «skipped: 0» в каждой записи

> // Порядок ключей — порядок свидетеля: `diagnostics`, `checked`.

---

## `flang/self/monoid.flang`


Файл уже правила ячейка Ч334; остаток — один кусок.

### Нижняя граница «Единицы по номеру» — цена, ИЗМЕРЕННАЯ прогоном

Было у функции «Единица по номеру» (довод остался, переписан в настоящем
времени):

> НИЖНЯЯ ГРАНИЦА СТОИТ ЗДЕСЬ НЕ ДЛЯ КРАСОТЫ, и цена её отсутствия измерена
> прогоном: `flang check` на этом файле давал `valid: true`, а «Единица по
> номеру» с номером 0 падала `«элемент»: индекс 0 вне списка длиной 0` —
> тотальная функция, отказывающая на законном входе. Верх сторожил, низа не
> было вовсе.

---

## `flang/self/monotone.flang`


### Копия `flang/src/monotone.mjs` и ссылка на `types.mjs`

Было в шапке файла:

> // Монотонность: `а ≤ б ⟹ ф(а) ≤ ф(б)`. Копия `flang/src/monotone.mjs`.

> // Закон. Устройство доказано раньше и в другом месте (`types.mjs`: функция
> // объявлена и берёт ровно один вход).

### Почему текст ответа отношения не сводят к одному со свидетелем

Было у «Меньше»:

> // значения. Свести их к одному тексту значило бы поправить свидетель молча.

## `flang/self/obligations.flang`


Обязательства: что именно надо доказать, в виде данных.

### Эталон обязательств — `flang/src/obligations.mjs` (343 строки)

Было в шапке файла, второй строкой:

> Эталон `flang/src/obligations.mjs` (343 строки).

### Зачем этот файл: связка на JavaScript и «доказуемость при наличии Node»

Было в шапке файла, врезка «Зачем этот файл»:

> Между поверхностью языка и двумя перенесёнными половинами ядра
> (`self/proof-initial.flang` — принцип, `self/proof-kernel.flang` — сведение)
> стояла СВЯЗКА на JavaScript, и пока она там стоит, «доказуемость» языка
> означает «доказуемость при наличии Node». Этот файл переносит первую её
> половину […]

### Три места, где дословный перенос дал бы другой ответ — доводы от свидетеля

Было в шапке файла. Сами три различия остались (они объясняют поведение), ушли
ссылки на свидетеля и на побайтовую сверку:

> Названы поимённо, потому что молчаливое отступление и есть тот дефект, ради
> которого сверка побайтовая.

> У свидетеля `одинаковы`
> различает их: `одинаковы(null, null)` истинно, а `одинаковы(undefined,
> null)` ложно (первое сравнение `===` их разводит).

> У свидетеля `span: теорема?.span` пропадает из вывода
> целиком, когда места нет; здесь запись строится списком полей […] Это и есть
> дословный перенос: сверяется JSON, а не объект в памяти.

> `равен` языка — это `Object.is`, у свидетеля `===`:
> на `-0` и `0` они отвечают противоположное.

### Долг восьми обычных функций: почему не закрыт пределом глубины

Было в шапке файла, врезка «Почему восемь функций ОБЫЧНЫЕ». Объяснение
осталось, ушли сверка со свидетелем и оговорка о долге:

> — то есть развёл бы сообщение со свидетельским на первом же вложенном списке.
> Долг записан как долг, а не спрятан за оговоркой.

### Правило корпуса про утверждение при версии формата держал тест

Было у «Версии обязательств»:

> в каталогах побайтовой
> неподвижной точки полагается сопровождать утверждением там же, где она стоит
> (`flang/test/corpus-claims.test.mjs`).

Файла `flang/test/corpus-claims.test.mjs` в дереве больше нет.

### Замер квадратичности: 23 517 918 витков `добавить` вместо 6140

Было у сбора обязательств программы, под врезкой «СВЁРТКОЙ, А НЕ СПУСКОМ ПО
ХВОСТУ». Довод остался, ушли счёт по дереву и улика из напечатанного семени:

> Счёт по дереву:
> 7798 функций, 6140 обязательств — 23 517 918 витков `добавить` вместо 6140,
> в 3830 раз больше. В напечатанном семени это видно прямо
> (`bootstrap/compiler_flang.c`): тело зовёт себя на хвост, а `slit_uzly`
> прогоняет циклом весь возвращённый хвост, и так на каждом уровне спуска.

### Порядок обязательств сверяла побайтовая сверка перепечатки

Было там же:

> ПОРЯДОК НЕ МЕНЯЕТСЯ НИ НА ОДИН ЭЛЕМЕНТ, и это здесь главное: побайтовая
> сверка перепечатки читает обязательства по порядку. […] Ровно то же давал спуск.

### У свидетеля обязательства правились на месте

Было у состояния свёртки по теоремам:

> У свидетеля обязательства ПРАВЯТСЯ НА МЕСТЕ
> (`цель.proof = теорема`), и порядок правок значим: вторая теорема того же
> имени видит первую уже закрытой и отвергается. Здесь состояние несётся явно,
> и это единственная разница — ответ тот же.

### История правила о границе над «числом»: что было до него

Было в разделе «ГРАНИЦА НАД «ЧИСЛОМ» ЛОЖНА НА «НЕ ЧИСЛЕ»». Переписано в
условное наклонение — «без этого правила»; исходная форма была историей:

> До этого правила ядро на такое утверждение отвечало «объявлено, не доказано»,
> а `flang check` — «проверено, замечаний нет» и кодом 0. То есть о ЛОЖНОМ
> утверждении говорилось ровно то же, что о недоказанном истинном, и разницу
> между ними видел только тот, кто догадался позвать функцию с `0 делить на 0`.

### Однострочные ссылки на свидетеля

Было у функций и врезок:

> `а ?? б` свидетеля: `null` и `undefined` в «Значении» неотличимы […]

> Порядок здесь значим: обязательства и диагностики
> читаются по порядку появления, и побайтовая сверка их порядок сличает.

> Имена в кавычках-ёлочках — так их пишет свидетель в каждом сообщении.

> Параметр обязательства — имя с типом, и порядок ключей тот же, что у свидетеля.

> Место в диагностике ставится ТОЛЬКО когда оно есть: у свидетеля `span:
> теорема?.span` при отсутствующем месте пропадает из JSON целиком, а не
> становится `null`. Сверка побайтовая, значит и здесь ключа быть не должно.

> Порядок свидетеля сохранён — `continue` после первой.

> Спуски приезжают сюда ДАННЫМИ (`итоги.totality.descents` свидетеля) […]
> Не передали ничего — у мер ноль стережённых мест, ровно как у
> свидетеля при `итоги === undefined`.

---

## `flang/self/obyazatelstva-i-yadro.flang`


Слой обязательств и ядро доказательств под своими примерами.

### Замер «своих примеров ноль»: греп по 77 файлам и таблица четырёх файлов

Было в шапке файла, в разделе «Улика, ради которой файл заведён»:

> // Своих примеров в слое доказательств не было ни одного. Считано грепом
> // `пример «` по 77 файлам `.flang` четырёх каталогов:
> //
> //     строк   своих примеров   что печатает `flang test`
> //      3970          0         flang/self/proof-kernel.flang     5 5 0
> //      2702          0         flang/self/proofterm.flang       23 23 0
> //      1948          0         flang/self/proof-initial.flang    5 5 0
> //       536          0         flang/self/obligations.flang      5 5 0
> //
> // Числа справа — примеры ВВЕЗЁННЫХ модулей: `flang test` считает их вместе со
> // своими, поэтому слой без единого своего примера показывает зелёный ответ с
> // ненулевым счётом и выглядит проверенным.

Поведенческая половина (что `flang test` считает примеры ввезённых модулей
вместе со своими) в файле оставлена; таблица и счёт файлов вынесены.

### Опыт «снятием правки»: испорченный `obligations.flang` даёт зелёные 40 из 40

Было там же:

> // Замерено снятием правки: в `obligations.flang` заменён разделитель в функции
> // «Через запятую», то есть испорчен текст всякого перечисления в диагностиках
> // обязательств. `flang test` двоичным на испорченном файле — 40 из 40, зелено.
> // `self-obligations.test.mjs` — красно. Держала слой только проверка на
> // JavaScript, а её выбрасывают вместе со второй реализацией.

Файла `flang/test/self-obligations.test.mjs` в дереве нет.

### Мост на JavaScript, через который шли «Итоги проверок»

Было в шапке файла, в разделе «Чего здесь НЕТ»:

> // Нет полной ведомости: она собирается из «Итогов проверок», а их сегодня
> // перекладывает мост на JavaScript (`flang/src/self.mjs`), и на стороне flang
> // точки входа «какие итоги» пока нет.

Файла `flang/src/self.mjs` в дереве нет.

### Номер задачи у цели `Е не меньше Г`

Было в разделе «Зачем эти шесть»:

> // Цель `Е не меньше Г` не брала НИ ОДНОГО правила ядра и уходила в «Отказ без
> // правила» (задача 1547).

Убран только номер задачи; само объяснение, зачем взяты шесть целей, оставлено
на месте.

### Замер 30 августа 2026 о глубине рекурсии трамплина вычислителя

Было в разделе «Почему цель собирается узлами, а не разбирается из исходника»:

> // Замер 30 августа 2026, тот же двоичный:
> // один разобранный исходник — `FLANG_RECURSION_LIMIT` на глубине 20001 при
> // умолчании, шесть — на 200001 при `--предел-глубины 200000`, а стек хозяина
> // кончается около 2 074 974.

### Цена связывания исходников компилятора в витках

Было там же:

> // Собрано в одну функцию потому, что связывание исходников компилятора стоит
> // минут (14,2 млрд витков), а шесть вопросов к ядру — секунд.

Оставлено «стоит минут … — секунд» как объяснение, почему шесть вопросов собраны
в одну функцию; убран точный счёт витков.

## `flang/self/oracle.flang`


Оракул пяти объявленных свойств — таблица ответов вычислений на самом flang.

### Чем строилась таблица ответов до этого файла и сколько файлов свидетеля она держала

Было в шапке файла, в разделе «Зачем этот файл»:

> // Законам и свойствам мало РАЗОБРАТЬ программу — надо её ВЫПОЛНИТЬ. До этого
> // файла таблицу ответов строил JavaScript в файлах проверок
> // (`flang/test/self-svoystva.test.mjs` и четыре его соседа), и рабочий путь
> // позвать её не мог: проверка — не дерево. Отсюда держались тринадцать файлов
> // свидетеля, которые иначе снялись бы разом.

Ни `flang/test/self-svoystva.test.mjs`, ни четырёх его соседей в дереве нет.

### Разобранный тупик: «функций первого класса нет» оказалось не тем, что мешало

Было в шапке файла — целый раздел:

> // ── Что мешало, и чем это оказалось на самом деле ───────────────────────────
> // Записано было так: «передать вычислитель функцией нечем — функций первого
> // класса у языка нет». Мера показала другое. Вычислитель зовётся ПО ИМЕНИ, и это
> // язык умеет: `использует «Вычислитель flang» из "interpret.flang"` уже стоит в
> // `self/proof-kernel.flang`. Упирается перенос в ТОТАЛЬНОСТЬ: «Вызвать»
> // объявлена обычной функцией (в `interpret.flang` таких 49 из 280), а тотальной
> // функции звать обычную нельзя — компилятор отвечает `FLANG_NOT_TOTAL` и
> // называет обе. Снимаешь обещание тотальности — модуль проходит проверку без
> // единой диагностики.

### «Цена переноса» и предел витков «там же, где у свидетеля»

Было там же, следующим абзацем:

> // Значит цена переноса названа точно: не «нужны функции первого класса», а
> // «оракул — обычная функция, а не тотальная». И это честно: оракул выполняет
> // ЧУЖУЮ программу, а чужая программа может не завершиться. Обещать её
> // завершение значило бы обещать разрешимость остановки. Предел витков и предел
> // глубины приезжают сюда аргументами и стоят там же, где у свидетеля.

Поведенческая половина («оракул — обычная функция, а не тотальная», и почему)
оставлена в файле; убраны отсылка к разобранному тупику и сверка со свидетелем.

### `reifyValue` свидетеля у перевода варианта

Было у функции «Знач значением»:

> // Вариант переводится записью с полями `variant` и `fields` — ровно так, как это
> // делает `reifyValue` свидетеля, когда значение времени выполнения попадает в
> // печать: своего вида под вариант у «Значения» нет.

### «Оба случая у свидетеля значат одно» при сборе аргументов

Было у объекта «Сбор аргументов»:

> // Сбор записи аргументов: имена кончаются раньше или позже значений, и оба
> // случая у свидетеля значат одно — недостающий аргумент приезжает как `ничто`,
> // лишний отбрасывается.

### `Object.hasOwn` свидетеля у пустого места аргумента

Было у функции «Значение аргумента»:

> // Пустое место аргумента — это `ничто` В ЗАПИСИ, но «нет аргумента» В КЛЮЧЕ, и
> // различие здесь не мелочь: свидетель спрашивает `Object.hasOwn`, поэтому ключ,
> // выставленный руками, для него ЕСТЬ и функция получает `ничто`, а не отказ
> // «не задан аргумент».

### `createRuntime` свидетеля у хода построения

Было у объекта «Ход оракула»:

> // Ход построения: готовая программа строится ОДИН раз, как `createRuntime` у
> // свидетеля, и обход её больше не пересобирает.

### «Как у свидетеля» у пределов витков и глубины

Было у функции «Спросить вычислитель»:

> // Предел витков и предел глубины стоят на ней
> // снаружи, как у свидетеля, но завершение чужой программы не доказуемо, и
> // обещать его значило бы обещать разрешимость остановки.

### `аргументом(undefined)` свидетеля у пар ответов

Было у функции «Пары ответов»:

> // Пары ОТВЕТОВ: образ, которого не удалось посчитать, приезжает пустым местом, а
> // не `ничто`, — как `аргументом(undefined)` у свидетеля.

## `flang/self/otkazy-totalnosti.flang`


Отказы компилятора, выраженные примерами внутри языка.

### Файл заявлял себя как проверку без JavaScript

Было в шапке файла, первой строкой:

> ОТКАЗЫ КОМПИЛЯТОРА, ПРОВЕРЕННЫЕ БЕЗ JAVASCRIPT.

Осталось: «ОТКАЗЫ КОМПИЛЯТОРА.»

### Что этот файл замещал: счёт утверждений в тестах на Node

Было в шапке файла, врезка «Что здесь замещается» (убрана вместе с заголовком):

> Утверждения вида «такую программу компилятор отвергает, и вот кодом каким»
> живут сегодня только в проверках на Node: `flang/test/totality.test.mjs`
> (71 утверждение) и `flang/test/types.test.mjs` (57). Их предмет — модули
> `flang/src/totality.mjs` и `flang/src/types.mjs`, то есть вторая реализация,
> которую выбрасывают. Уйдёт она — уйдут и утверждения, а свойства, которые они
> держат, останутся необеспеченными: язык обещает, что помеченная «тотальная»
> функция завершается, и что негодная программа не соберётся.

### Сравнение с тем, как отказ устроен на стороне JavaScript

Было в шапке файла, первой фразой врезки «Почему это выражается ВНУТРИ языка»
(остаток врезки оставлен на месте):

> Разбор говорит «отвергнуто» исключением только на стороне JavaScript. Слои на
> flang устроены иначе: …

### Чего в файле нет: долг первого захода по месту (`span`)

Было в шапке файла, врезка «Чего здесь НЕТ и почему» (убрана вместе с
заголовком):

> Здесь нет утверждений про ФОРМУ УЗЛА разобранного дерева как объекта
> JavaScript и нет утверждений про место (`span`) в диагностике: место едет в
> той же записи и проверяется так же, но проверок на него в первом заходе не
> ставилось — они переносятся тем же приёмом и стоят по строке на штуку.

## `flang/self/parser.flang`


Разбор языка flang на самом flang. Образцом ему служил `flang/src/parser.mjs`
(файла в дереве больше нет), и почти весь вынос отсюда — сверка с ним.

### Чем поверялась верность разбора

Было в шапке файла:

> Образец — `flang/src/parser.mjs`, и воспроизводится он дословно: те же узлы,
> тот же порядок ключей, те же коды и тексты диагностик, те же места. Критерий
> верности — побайтовая сверка: `«Печать значения»` от построенного здесь AST
> обязана совпасть с `JSON.stringify(parse(исходник))` образца, байт в байт.
> Проверка — `flang/test/self-parser.test.mjs`.

### Почему AST — обобщённое значение, а не свои типы: два довода из трёх ссылались на образец

Было в шапке файла:

>   • образец различает «ключа нет» и «ключ со значением ничто»
>     (`node.optional`, `program.legacy`, `proposition.arg`), а запись flang
>     этого различить не может: поле записи есть всегда;
>   • порядок ключей в JSON задаётся порядком вставки, и образец им
>     пользуется — `bindNullaryCalls` переписывает `var` в `call`, снимая и
>     заново кладя `name` и `span`, чтобы получить порядок обычного вызова.
>     Список пар ключ-значение это воспроизводит, запись — нет.

### Почему рекурсивный спуск, а не свёртка потока в дерево

Было в шапке файла:

> Свести это к дереву
> отступов можно, но тогда сверять пришлось бы не с образцом, а с собственным
> представлением о том, как образец группирует токены, — и каждое расхождение
> становилось бы отладкой двух разных разборов сразу. Здесь же порядок вызовов
> повторяет `parser.mjs` функция в функцию, поэтому расхождение всегда
> локально: оно в одной функции, и видно, в какой.

Там же, о цене решения:

> Цена решения — нетотальность: анализ (`flang/src/totality.mjs`) признаёт
> убыванием хвост списка, поле записи и поле варианта, но не «остаток потока
> стал короче».

### Поток токенов пачками: чем мерялась цена плоского списка

Было в шапке файла:

> Список flang — массив, и `хвост` копирует его целиком (`builtins.mjs`).
> Съедать токены по одному из плоского списка значило бы копировать хвост на
> каждом шаге: на самом большом исходнике репозитория (30 000 токенов) это
> 4,5·10⁸ копий указателей. Поэтому поток разложен пачками по 32 токена, а
> «Поток» держит три токена заглядывания (`peek(0..2)` образца) отдельно.

(Замер оставлен в файле, вынесены только `builtins.mjs` и `peek(0..2)` образца.)

### Отказ значением, а не исключением

Было в шапке файла:

> Образец бросает `flangError` из середины спуска, и на этом разбор кончается.
> В flang исключений нет, поэтому «Разборщик» несёт поле «беда» …
> … наружу едет первая беда —
> ровно та, на которой упал бы образец.

### Заглядывание вперёд на четыре токена

Было у объекта «Поток»:

> Образец глядит вперёд без ограничений
> (`peek(ahead)` по массиву), здесь окно фиксировано, и его ширина обязана быть
> не меньше самого длинного взгляда в разборе.

### Исчерпанный поток стоит на месте

Было у функции «Сдвинуть поток»:

> `next()` образца стоит на месте, дойдя до последнего токена: `if (this.index
> < this.tokens.length - 1)`.

### Как образец держал объявления со своим ключом верхнего уровня

Было у «Ключей до наследия»:

> Образец держит под каждое свой список поля
> разборщика (`this.morphisms`, `this.embeddings`, …) и выкладывает их в
> программу по одному, в жёстком порядке и только непустыми.

### Четыре признака разборщика — имена полей образца

Было у объекта «Разборщик»:

> Поле «исход» — тот самый `usesOutcome` образца …
> … Признак считается РАЗБОРОМ, а не обходом готового
> дерева, ровно по той же причине, по какой его так считает образец.
> Поле «ввод-вывод» — `usesIo` образца … Причина названа образцом: функция, строящая
> поручение, обязана проверяться примерами и БЕЗ всякого плана …
> Поле «монада» — `blocksInMonad > 0` образца …
> Поле «поверхность» — `this.surface` образца …

Там же, у трёх отметок признака:

> Признак ставится один раз и не снимается: `usesOutcome = true` образца.

> Признак ставится один раз и не снимается: `usesIo = true` образца.

> Признак ставится один раз и не снимается: `blocksInMonad += 1` образца.
> Считать блоки, а не помнить «был хоть один», незачем: число нужно образцу
> ровно для одного сравнения с нулём.

Там же, у «Имён типов ввода-вывода»:

> и есть признак того, что словарь программе нужен: у образца это `noteIoType`
> и `noteIoVariant`, и зовутся они ровно в тех же пяти местах разбора.

### Пустое имя у `даёт` внутри функтора

Было у объекта «Концы объекта функтора»:

> Пустая строка признаком быть не
> может — `даёт «»` разбирается в пустое имя, и тогда ключ `gives` у свидетеля
> появился бы, а здесь нет; сверка деревьев разошлась бы на этой единственной
> программе, и нашли бы её не скоро.

### Мелкие сноски на образец у отдельных функций

Было у объекта «Шаг типа»:

> Тип поля записи и его строка в терминах ядра FTS — образец возвращает их
> парой (`parseFieldType`).

Было у функции «С параметрами типа»:

> иначе AST
> непараметрического объявления изменился бы, а он сверяется со свидетелем
> побайтово.

Было у функции «Как строку»:

> `String(значение)` образца над скаляром: …

Было у функции «С бедой»:

> Первая беда побеждает: образец бросает исключение и дальше не идёт, здесь
> разбор идёт вхолостую, но записанного места уже не меняет.

Было у функции «Отказ у токена»:

> `FLANG_AMBIGUOUS_NAME` идут «Отказом в месте» и остаются как были — у
> образца они брошены `flangError` напрямую, мимо `fail`, и цитаты в них
> («принимает», «пусть», «случай») он не переписывает тоже.

Было у функции «Слово понятия»:

> `kw(id)`
> образца, вместе с его двумя запасными ходами …

Было у функций «Добавить прочее» и «Добавить прочие»:

> Порядок внутри одного ключа —
> порядок объявлений в файле, ровно как у образца: он кладёт `push`.

> … и кладёт их в том же порядке, в каком их
> кладёт `push` образца.

Было у функции «Связать имя при разборе»:

> `bind` образца: имя дописывается в самую внутреннюю область, если её ещё нет.

Было у функции «Окончания»:

> Список закрытый и упорядочен от длинных окончаний к коротким — ровно как в
> образце: иначе разбор перестал бы быть детерминированным.

Было у функции «С прописной»:

> `/^\p{Lu}/u` образца, суженный до латиницы и кириллицы: …

Было у функций «Текст значения» и «Текст токена»:

> `String(token.value)` образца: у числового токена `value` — число, и шаблон
> печатает его как `Number::toString`, то есть как исходная запись.

> `tokenText` образца: ключевое слово отдаёт исходные слова, число — запись
> как в исходнике, остальное — значение.

Было у функции «Ждать число»:

> Число как значение узла (`expectNumber` образца).

Было у функции «Слово кодом символа»:

> но особого отказа здесь нет и у свидетеля он
> один на всю грамматику: «Разобрать постфикс» откажет сам.

Было у функции «Слово приписыванием»:

> Различие ровно в глаголе — довод у свидетеля, в таблице
> ключевых слов над `prepend`.

Было у функции «Слово функцией»:

> и побайтовая сверка разбора со свидетелем разошлась
> бы на каждой такой программе …

Было у сборки функции (четыре сноски подряд):

> … который уезжает в AST и потому обязан
> совпасть со свидетелем байт в байт.

> Место идёт ПОСЛЕ параметров типа И ПОСЛЕ меры: свидетель кладёт `span`
> последним, уже после `node.typeParams` и `node.decreases`, а порядок
> ключей — часть побайтового контракта.

> Ключ `postconditions` стоит ПОСЛЕ `decreases` и ДО `span` — ровно там, где
> его кладёт свидетель, …

> Ключ `preconditions` стоит ПОСЛЕ `postconditions` и ДО `span` — тем же
> правилом и в том же месте, что у свидетеля, …

Было у разбора индукции теоремы:

> Ключ `measure` дописывается ПОСЛЕ `span` — у образца это присваивание
> готовому объекту, — и только когда мера написана.

Было у функции «Строка меры» и у двух строк теоремы:

> Слово съедается ДО проверки на повтор — как у свидетеля: место отказа тогда
> приходится на само `убывает`, а не на выражение за ним.

> Слово съедается ДО проверки
> на повтор — как у свидетеля: место отказа тогда приходится на само `утверждаем`.

> Проверка стоит
> ДО того, как слово съедено, — как у свидетеля: место отказа приходится на само
> `индукция по`.

Было у функций «Строка изоморфизма» и «Строка моноида» (дважды одним текстом):

> Место отказа при повторе — сам токен строки, а не начало объявления: у
> образца это `const at = this.next()`, снятый ДО проверки.

Было у функции «Части моноида»:

> Место отказа — начало
> объявления, а не конец блока: у образца это `start`.

Было у функции «Разобрать пересечение»:

> Каждое из четырёх
> ожиданий несёт своё сообщение образца дословно: …

Было у функции «Разобрать вложение»:

> ПОСЛЕ `span`, а не перед: у образца это присваивание уже собранному объекту
> (`node.gives = …`), …

Было у «Сборки стрелки»:

> и после `span`: у образца это присваивание готовому объекту, а
> порядок ключей — байты канонического JSON.

Было у функции «Разобрать процесс» и у частей процесса и прогона:

> Строки НЕ проверяются на повтор, и это не упущение эталона, а поведение
> образца: он берёт `eatKw`, значит последняя строка побеждает. Повтор ловят
> моноид, монада, изоморфизм и план — там у образца `atKw` со сверкой на `null`.

> Четыре строки обязательны, и порядок жалоб — тот же, что у образца: первая
> недостающая побеждает.

> Порядок жалоб — тот же, что у образца.

Было у функции «Разобрать план»:

> В отличие от процесса, строки плана СВЕРЯЮТСЯ на повтор: у образца здесь
> `atKw` со сверкой на `null`, а не `eatKw`.

Было у «Ключей верхнего уровня» и у «Поля если непусто»:

> … и повторяет `parseProgram` образца слово в слово:

> Ключ появляется в программе ТОЛЬКО непустым: у образца `if (list.length > 0)`,
> и программа без стрелок обязана дать побайтово прежний AST.

Было у встроенных словарей действий, ввода-вывода и исхода:

> Ключи и порядок — те же, что у `ACTION_TYPE` образца
> (`src/conc.mjs`).

> Ключи и порядок — те же, что у `IO_TYPES` образца
> (`src/io.mjs`).

> узел здесь выложен ровно теми же ключами и в том же порядке, что `OUTCOME_TYPE`
> образца (`src/builtins.mjs`): вид, имя, признак встроенности, варианты.

> Имя типа и имена его вариантов — то же множество `занято`, что собирает
> `withOutcomeType` образца.

> Сверяется ТОЛЬКО имя суммы — так у образца.

Было у «Разбора токенов» и у итога разбора:

> Порядок приписывания встроенных словарей — тот же, что у образца, и он
> виден в байтах: сначала «Действие» (там же, где `parseProgram` кладёт его в
> `this.types`), потом «Исход числа» (`withOutcomeType`).

> Отказ теряет программу целиком: образец бросает исключение и не отдаёт
> ничего … Наружу едет одна беда — та, на которой упал бы образец.

> Отказ разбора сюда не
> доходит вовсе — образец бросил бы исключение раньше, чем добрался до
> разворачивания …

> Отказ разворачивания — такая же беда разбора, как всякая другая: образец
> бросает `flangError` из `expandMonads`, а `parse` его не ловит.

> Места у беды может не быть вовсе: `flangError` зовут и без узла. Образец
> кладёт тогда `span: undefined`, а сверка читает его как строку 0 и столбец 0
> — здесь то же самое и теми же числами.

> Печать AST в JSON — тот же байт, что даёт `JSON.stringify(parse(исходник))`.

Было у «Значения токена», у наследия ядра, у естественного преобразования, у
обоснования предположением, у печати типа:

> `value` числового токена — ЧИСЛО, а не его запись: так его отдавал образец,
> и проверки снаружи читают его так же.

> `value` повторяет форму канонической
> модели ядра, чтобы `compat.mjs` собрал из неё эквивалент без второго разбора.

> Проверяется он на сетке (`flang/src/setoid.mjs`), а здесь только разбор.

> Ключ `name`
> при этом стоит — со значением «ничто»: у образца это литерал `null`, и
> отсутствие ключа было бы другим байтом.

> Ровно так же это устроено у
> образца — `kw(id)` внутри сообщения.

> `formatType` образца — четыре регулярных выражения.

### Как записаны цитаты диагностики: сравнение с образцом

Было в разделе «цитаты диагностики на поверхности файла»:

> ЧЕМ ЗАПИСЬ ОТЛИЧАЕТСЯ ОТ ОБРАЗЦА. Там это `String.replace` с образцом
> `/'([^']{1,40})'/gu`; регулярных выражений в языке нет, поэтому здесь
> сообщение разрезается по апострофу, и переписываются НЕЧЁТНЫЕ куски — те,
> что стоят между открывающим и закрывающим. Оба условия образца сохранены:
> длина от 1 до 40 знаков и наличие закрывающего апострофа (последний кусок,
> если апострофов нечётное число, не цитата и не трогается).

### Таблица «не начинает выражение»: счёт слов у образца и здесь

Было у функции «Не начинает выражение» (счёт устарел — в списке уже 19 слов):

> после — обязано сказать то же, что говорит образец: «<слово> не начинает
> выражение». У образца в `NOT_EXPRESSION` 19 слов; здесь 9, и недостающие
> десять (`процесс`, `надзор`, `прогон`, `план`, `обрабатывает`, `стратегия`,
> `порог отказов`, `сид`, `бюджет`, `ящик`) — ровно те, чьих продукций у
> эталона ещё нет.

### Таблица «начало выражения»: расхождение, снятое 15 августа 2026

Было у функции «Начало выражения»:

> Список СПИСАН с `EXPRESSION_START` образца слово в слово и в том же порядке —
> он не «примерно тот же», а тот же самый, и это проверяется здесь глазами один
> раз, а сверкой байтов — на каждом краевом случае.
>
> `разложить` и `код символа` в нём НЕТ, и до 15 августа 2026 у эталона они
> стояли. Разошлись на этом два места: `длина разложить "а" на символы` свидетель
> читает как имя `длина` (и отвечает «имя «длина» не связано»), а эталон читал
> как одноместную форму с аргументом; `голова код символа "а"` свидетель
> отвергал, эталон разбирал. Пробел у ЭТОГО списка настоящий и назван тут же:
> два слова, которые выражение начинают, в него не входят, — но это пробел
> ОБРАЗЦА, и эталон обязан повторять его до буквы, пока он не закрыт там. Иначе
> «побайтово» кончается на первой же такой строке.

### Счёт вхождений слова «функция» в самом файле

Было у функции «Тип функцией»:

> потому 173 вхождения «функция» именем в
> этом самом файле и уцелели.

### Блок `в монаде`: чего нет у образца

Было у «Сборки блока монады»:

> в «Слово формой» (первичная позиция) `inMonad` не назван, потому что его нет
> и у образца в `parsePrimary` …
>
> Области видимости закрываются РАЗОМ: образец зовёт `popScope` столько раз,
> сколько связываний открыл, а здесь список областей просто возвращается к
> тому, каким он был до блока.

### Уровни логики: разворот обязан совпасть с разворотом образца

Было в разделе «уровни логики: или, и притом, не»:

> Разворот здесь обязан совпасть с разворотом
> образца поле в поле и в том же порядке ключей — сверка `self-parser`
> сравнивает готовый JSON, а не форму узла.

### `это вариант`: что было ошибкой до записи

Было в разделе про `<выражение> это вариант «Имя»`:

> ЗАЧЕМ. Вариант различал только `разбор`, а `разбор` требует блока с отступом и
> потому в постусловие не помещается — оно однострочное. Из-за этого правду о
> функции вида «Успешно» (`stdlib/result.flang`) сказать было НЕЧЕМ: попытка
> написать `результат равен (итог это вариант «Успех»)` упиралась в
> `FLANG_PARSE: ожидался знак ')'`.
>
> … а сочетание `это вариант` в позиции выражения разбор ПРЕЖДЕ
> отвергал. Запись занимает то, что было ошибкой.

### `не убывает`: чем проверена занятость слова и во что обошлась правка

Было в разделе про `<список числа> не убывает`:

> Сочетание `не убывает` ПОСЛЕ выражения
> разбор прежде отвергал: `FLANG_PARSE: неожиданное 'убывает'`. Запись занимает
> ровно то, что вчера было ошибкой, поэтому ни одна написанная программа не
> меняет смысла. Занятость проверена прогоном, а не глазами:
> `node flang/scripts/word-occupancy.mjs «не убывает»` — цепочек ноль.

Там же:

> Замерено: правка трогает два
> файла (разбор и ядро) вместо двадцати девяти, которых стоила бы встроенная
> форма со своим узлом.

### Запятая у полей конструктора

Было у разбора списка:

> Запятая
> доходит сюда ВСЕГДА: полям конструктора она больше не принадлежит
> («Ещё присвоение»).

### Постусловие обычной функции: откуда поле бралось до этой поверхности

Было в разделе «постусловие обычной функции»:

> НОВОГО В РАНТАЙМЕ НЕТ НИ СТРОКИ. Поле `postconditions` в AST было и работало:
> его проверяет интерпретатор и печатают восемь целей, а клал его туда только
> `compat.mjs` из `свойство` утилиты наследия FTS. Не было ПОВЕРХНОСТИ —
> обычная функция flang сказать о себе не могла ничего; здесь заводится ровно
> она, и потому `код` и `сообщение` берутся те же, что у свойства утилиты:
> отказ обязан быть неотличим, читателю всё равно, каким словом обещание
> записано.

### Дата у списка стрелок категории

Было у разбора категории:

> Категория: имя и — с 14 августа 2026 — список её стрелок.

### Отказ у левого края: чем поймано расхождение столбца

Было внутри функции «Разобрать базу номера»:

> Третий стоит ПОСЛЕ проверки числа нарочно — у образца тот же
> порядок, и разойдись он, вторая строка с двойкой отвечала бы разными бедами.

> Отказ помечает токен `элемент`, а НЕ тот, что стоит после него: у образца
> здесь `this.fail(…, start)`. `«Ждать слово»` пометило бы текущий — и
> столбец разошёлся бы со свидетелем на восемь знаков. Разницу поймал краевой
> случай `модуль «М»\n\nэлемент 0`, дописанный в сверку эталона вместе с
> продукцией; без него она молчала бы.

### Порядок ключей объявленного свойства

Было у функции «Разобрать объявленное свойство»:

> Порядок ключей узла повторён за свидетелем (`flang/src/parser.mjs`,
> `parseProperty`) дословно: `kind`, `name`, `carrier`, `operation`, `over`,
> `function`, `span`. Ключи, которых автор не назвал, стоят «ничем», а не
> пропущены: у свидетеля они инициализированы `null`, и пропуск дал бы другой
> JSON при том же исходнике.

### Словарь ввода-вывода: даты заведения поручений и номера задач

Было в разделе «встроенный словарь ввода-вывода», о файловой октетной паре:

> Октетная
> заведена 22 августа 2026 по тому же доводу, что и у соединения, и заведена
> потому, что без неё двоичный файл проходил сквозь текстовую МОЛЧА: 4096
> октетов на входе, 7 байт на выходе. Теперь текстовая пара на нетексте
> ОТКАЗЫВАЕТ (`FLANG_IO_NOT_TEXT`), а октетная возит байт в байт.

Там же, о двусторонности правки и о снятом образце:

> Правка эта ОБЯЗАНА быть двусторонней: разойдись эталон
> с образцом хоть одним ключом — и побайтовая сверка AST любой программы со
> словарём покажет расхождение, а не промолчит. Образца на JavaScript больше
> нет (снят коммитом fe8e8a37), и сверяться теперь не с чем — тем важнее, что
> хозяева называются поимённо: файловую октетную пару исполняют оба хозяина на
> C (`flang io` и планировщик конкурентности), а хозяин вкладки на JavaScript
> от словаря ОТСТАЁТ, и говорит об этом сторож `scripts/storozh-hozyaina-vkladki.flang`.

(`scripts/storozh-hozyaina-vkladki.flang` в дереве нет.)

Там же, о девятнадцатом и двадцатом поручениях:

> ДЕВЯТНАДЦАТОЕ И ДВАДЦАТОЕ поручения — «Удалить файл» и «Завести временный
> каталог» — заведены 23 августа 2026 по одному доводу: без них план, которому
> нужен свой каталог под работу, уезжал из языка в оболочку (`sh -c 'rm -rf …'`
> и `mktemp -d` через «Запустить процесс»), и убрать за собой не мог вовсе —
> об этом прямо сказано в `flang/examples/io/dvoichnyy-fayl-tuda-i-obratno.flang`.

(`flang/examples/io/dvoichnyy-fayl-tuda-i-obratno.flang` в дереве нет.)

Там же, о двадцать первом и двадцать втором поручениях:

> ДВАДЦАТЬ ПЕРВОЕ поручение — «Прочитать переменную среды» — заведено
> 30 августа 2026 по доводу задачи 8291: чтения среды в словаре не было ВОВСЕ,
> и обойти это можно было только уходом из языка …

> ВВЕДЕНО, И НИГДЕ НЕ ПРИМЕНЕНО. Ни один хозяин этого поручения не исполняет:
> исполнение — шаг третий, и до перепечатки семени слово живёт только здесь
> (AGENTS.md, правило «ввести слово и применить его — РАЗНЫЕ изменения»).
> ДВАДЦАТЬ ВТОРОЕ поручение — «Прочитать доводы» — заведено 30 августа 2026 по
> доводу задачи 4143: план не мог узнать, С ЧЕМ его позвали, ВОВСЕ.

### Замер занятости слова: размер корпуса

Было у отдачи `flang tokens`:

> Грепом такой вопрос не решается, и это замерено: …

> замер
> занятости слова проходит корпусом в четыре сотни файлов, и второй проход по
> каждому стоил бы ровно столько же, сколько первый.

### Обрезка пробелов: почему по списку символов

Было у функции «Без ведущих пробелов»:

> Обрезка пробелов идёт по списку символов, а не по позиции: раньше здесь
> убывала длина строки, то есть число, и анализ этого не принимал.

---

## `flang/self/partialorder.flang`


Частичный порядок. Свидетель `flang/src/partialorder.mjs` — файла в дереве
больше нет.

### Чем был этот файл: копия `flang/src/partialorder.mjs`

Было второй строкой шапки:

> Копия `flang/src/partialorder.mjs`.

### Мелкие сверки со свидетелем, снятые с абзацев-объяснений

| было в исходнике | где стояло |
|---|---|
| «(`types.mjs`, сличением объявлений: …)» | врезка «Что здесь ПРОВЕРЯЕТСЯ, а не доказывается» |
| «У свидетеля это помеченный `break законы` — выход сразу из всех трёх обходов. Здесь тот же признак «стоп»…» | врезка «Порядок проверок содержательный» |
| «`туда.нет ?? назад.нет` свидетеля» | «Первая беда отношения» (строка ушла целиком; постусловие говорит то же) |
| «Точка считается ровно там, где её считает свидетель, — во внутреннем обходе» | 3. транзитивность |
| «признак «стоп» проносит через все три то, что у свидетеля делает помеченный `break`» | три закона подряд |
| «карта свидетеля — СПИСОК, и правило у него то же» | здоровые порядки |
| «Пустой ключ — это `null` свидетеля» | ключ карты здоровых |
| «тем же правилом, по какому карта свидетеля пропускает повторный ключ молча» | первый ключ выигрывает |
| «`посчитано?.values ?? 0` свидетеля… но запасное значение перенесено» | «Значений у порядка» |

---

## `flang/self/processes.flang`


### Замер: сколько строк в `types.flang` и сколько там слова «процесс»

Было в шапке файла, раздел «Зачем этот файл заведён»:

> // (`flang/self/types.flang`) слова «процесс» не знает вовсе — ноль упоминаний на
> // 8036 строк.

На день выноса в `flang/self/types.flang` 8499 строк — число в комментарии было
уже неверным.

### Замер слоя `failures.flang` с датой пересчёта

Было в шапке файла, раздел «ЧТО ЗДЕСЬ НЕ СУДИТСЯ»:

> //     `flang/self/failures.flang` — отдельный слой на 1936 строк (здесь стояло
> //     1677; пересчитано 30 августа 2026), в замыкание компилятора не ввезённый, — и

> //     программы разом. Довод этот в силе, и ввоза сюда по-прежнему нет.

Было у правила второго:

> // `flang/self/failures.flang`, слоя на 1677 строк, в замыкание не ввезённого;

### Дата 30 августа 2026: ввоз проверок и обходчик на пуш

Было в шапке файла, тот же раздел:

> //     ⚠ Но «не ввезённый» больше не значит «не запускаемый». С 30 августа 2026
> //     слой ввозит `flang/проверки/process-failures.flang` — 18 утверждений над
> //     его тотальными функциями, — и обходчик `flang/проверки/обход.sh` гоняет
> //     их на каждый пуш работой `sborka`.

Само разграничение («не ввезённый» не значит «не запускаемый», и несудимой
осталась ровно половина — достижимый отказ на живой программе) в исходнике
осталось.

### Замер ведомостью: четырнадцать написанных и снятых обещаний

Было в шапке файла, раздел «ПОЧЕМУ ОБЕЩАНИЙ ЗДЕСЬ МЕНЬШЕ, ЧЕМ ХОЧЕТСЯ НАПИСАТЬ»:

> // Замер ведомостью на этом файле: обещания вида «прежние беды не теряются» и
> // «без X список бед не меняется» ядро не доказывает НИ ОДНО — все они стоят над
> // `свёртка` по списку, а вывод про свёртку ядру недоступен. Четырнадцать таких
> // обещаний написаны и сняты обратно: правды они не прибавляли, а цену
> // прибавляли.

### Замер: у скольких файлов дерева есть процессы

Было у ворот «Суд над объявлениями»:

> // Второй: цена. `flang check` зовут на КАЖДОЙ программе дерева, а процессы есть
> // у двадцати трёх файлов дерева. Обход всех тел ради них платился бы на всех
> // остальных ни за что.

### История: ссылка на вычисленное имя роняли молча

Было у сборщика ссылок правила пятого:

> // Прежде такое действие роняло ссылку молча, и разница между «имя не то» и
> // «имени нет» терялась ЗДЕСЬ, до всякого правила.

---

## `flang/self/proof-initial.flang`


Начальная алгебра: принцип индукции читается с объявления суммы. Свидетель
`flang/proof/initial.mjs` (1034 строки) — файла в дереве больше нет.

### Чем был этот файл: эталон свидетеля на 1034 строки

Было второй фразой шапки:

> Эталон `flang/proof/initial.mjs` (1034 строки).

### Зачем эталон — довод про «доказуемость при наличии Node»

Было врезкой шапки (вторая её половина, про роль слоя, осталась):

> ── Зачем эталон ───────────────────────────────────────────────────────────
> Довод тот же, что у слоя сведения (`self/proof-kernel.flang`) и у ведомости
> (`self/proof.flang`): пока машинерия доказательств живёт только на
> JavaScript, «доказуемость» языка означает «доказуемость при наличии Node».

### Захват имени ЗНАЧЕНИЯ — дыра, закрытая 16 августа 2026

Было врезкой «захват имени» (переписано в настоящем времени, как свойство
нынешнего кода):

> Вычёркивание связанного имени стережёт КЛЮЧ подстановки. Имена ЗНАЧЕНИЯ до
> 16 августа 2026 не стерёг никто, и свидетель утверждал в докстринге, что захвата
> здесь быть не может. Цель со `свёртка … как акк и х → результат` получала на
> место `результат` тело, свободное в `х`, — и `х` уезжал под чужой связыватель:
> снаружи это параметр функции, внутри элемент свёртки. После такого захвата
> обе стороны равенства становились одним термом, правило тождества сходилось
> знак в знак, и ведомость печатала «доказано» — в том числе «доказано
> ИНДУКЦИЕЙ по «Дерево» … обо ВСЕХ входах» — о том, что рантайм отвергал на
> первом же значении.

### Переезд сбора свободных имён в ядро и убранная вторая копия у свидетеля

Было перед разделом «свободные имена» (осталось переписанным как свойство
нынешнего устройства):

> СБОР СВОБОДНЫХ ИМЁН ПЕРЕЕХАЛ В ЯДРО (`proof-kernel.flang`), И ПЕРЕЕХАЛ, А НЕ
> РАЗДВОИЛСЯ. … и переселение было единственным ходом.
>
> ВТОРУЮ КОПИЮ ЗАВОДИТЬ БЫЛО НЕЛЬЗЯ, и причина названа у свидетеля прямо
> (`flang/src/proofterm.mjs`): вторая копия там БЫЛА, её убрали, и убрали
> потому, что два ответа на вопрос «какие имена здесь свободны» расходятся
> МОЛЧА, а список связывателей у языка один.

### Неверный отказ, стоявший при строке до её прочтения

Было в двух местах — во врезке при «Объявление списка» и в шапке «Объявление
строки»:

> Здесь прежде стоял отказ: «у строки голова — тоже строка, местом `X` оказались
> бы ОБА поля». Отказ был написан по памяти, а не по типизатору, и типизатор
> говорит другое — см. довод при «Объявление строки».

> Здесь прежде стоял отказ, и отказ был НЕВЕРЕН. Он говорил: «у строки голова —
> тоже строка, значит местом `X` оказались бы оба поля». Типизатор говорит не
> это.

### Обещание «тотальная, структурой» — счёт функций корпуса

Было в третьем чтении объявления списка (осталось без числа):

> …и на этом факте стоит обещание «тотальная, структурой» у 212 функций корпуса.

### Откуда взялось поле объявлений: третий источник фактов `work/proof-types`

Было при «Объявление имени и типа»:

> Появилось это поле в свидетеле вместе с третьим источником фактов
> (`work/proof-types`); эталон писался, когда свидетелей было три, и поле нёс
> только один из них. В собранном дереве свидетель один, и поле обязано быть.

### Ветки сборки, при которых разведено имя «Найти замену терма»

Было при «Найти замену терма» (довод про два модуля остался):

> ИМЯ РАЗВЕДЕНО ПРИ СБОРКЕ `work/svodka2`, а не выбрано так автором. Функция с
> именем «Найти замену» уже есть в `self/defunc.flang` (её принёс сторож
> объявленной меры, `work/self-measure-check`), а связывание отвергает одно имя
> в двух модулях.

### Замер цены доказательства — 58 тел из 208 разбором, 45 свёрткой

Было врезкой шапки «ПРИНЦИПА ПО СВЁРТКЕ»:

> ── Зачем это понадобилось, числом ──────────────────────────────────────────
> Замер цены доказательства (`docs/benchmark-proof-cost-2.md`) назвал главную помеху со
> знаменателем: ядро строило заключение посылки ТОЛЬКО с ветвей `разбор` по
> переменной индукции, а таких тел в `flang/stdlib` 58 из 208. Свёрткой
> написано 45, и ни к одной из них принцип не цеплялся ничем.

### Граница принципа по свёртке — замер шести функций

Было там же (сама граница осталась, ушёл счёт):

> …и она измерена: … Из шести функций замера, написанных свёрткой, такую форму
> имеют пять. Форма ТЕЛА перестала быть помехой; форма УТВЕРЖДЕНИЯ ею осталась.

### Что чинилось допущением по месту вызова

Было врезкой «ДОПУЩЕНИЕ ИНДУКЦИИ ПО МЕСТУ ВЫЗОВА» (переписано в настоящем
времени):

> ── Что чинится, и почему это не поблажка ───────────────────────────────────
> … Утверждение при этом верно, доказательство есть, а ядро отвечало «не
> свелось»: допущение говорило о другом значении второго параметра.

### Дробное «К» в условии дна: ядро ПЕЧАТАЛО «доказано» о неправде

Было во врезке «ФОРМЫ УСЛОВИЯ ДНА» (довод остался, ушли «измерено» и «починка»):

> ── ПОЧЕМУ ДВЕ ФОРМЫ ТРЕБУЮТ ЦЕЛОГО «К», И ЭТО ИЗМЕРЕНО, А НЕ ПРЕДПОЛОЖЕНО ──
> … Ядро на такой записи ПЕЧАТАЛО «доказано» об утверждении, которое рантайм тут
> же отвергал. Починка — отказ, а не подгонка.

### Мелкие сверки со свидетелем, снятые с абзацев-объяснений

| было в исходнике | где стояло |
|---|---|
| «сверяется со свидетелем ПОБАЙТОВО… порядок, в каком их кладёт свидетель» | врезка «Порядок ключей значим» |
| «Один тип на два действия свидетеля… `подставить`/`подставитьИмена`… Свидетель делит их обратным вызовом» | объект «Замена терма» |
| «`имяИменованного` свидетеля» | «Имя именованного» |
| «свидетель возвращает `null`, и `null === сумма.name` ложно даже при пустом имени суммы» | «Есть имя именованного» |
| «`сам !== null && одинаковы(тип, сам)` свидетеля» | «Тип совпал с самим» |
| «`связи[имя] ?? имя` свидетеля» | «Имя связи или само» |
| «`typeof узел.name === "string"` свидетеля» | «Убрать замену по узлу» |
| «Порядок ветвей повторён дословно за свидетелем» | «Пройти» |
| «`заменить(узел, текущие)` свидетеля» | «Прямая замена» |
| «Порядок ветвей повторён за свидетелем дословно» | «Захват в записи» |
| «это не оптимизация, а свидетель: `подставитьИмена` при пустой карте…» | «Подставить имена» |
| «`{ name, type: поле.type ?? null }` свидетеля — порядок ключей тот же» | «Несомое поле» |
| «Это `Map` свидетеля, собранная из объектного литерала» | «Положить замену» |
| «так же, как `Map.set` свидетеля» | «Последний из» |
| «`обязательство.bind ?? "результат"` свидетеля» | «Связка результата» (строка ушла целиком) |
| «строит не он, а «Отрезок ната» в свидетеле» | «Принцип индукции», поле `carrier` |
| «(`interpret.mjs`, `stepFold`: …)» | врезка «Что читается… НЕ индукция по списку» |
| «У свидетеля это отдельная постоянная (`СВЁРТКА`)» | «Свёртка как алгебра» |
| «Порядок ключей повторяет свидетель» | «Посылка свёртки» (фраза ушла целиком) |
| «— как и у свидетеля, где место занимает первый» | объект «Случаи свёртки» |
| «теми же словами и в том же порядке, в каком они стоят таблицей у свидетеля» | «Формы дна словами» |
| «печатается тем же видом, каким его печатает свидетель» | «Литерал спуска словами» |
| «Порядок ключей повторяет свидетель и отличается…» | «Посылка отрезка» |
| «Ключ «того же вызова» у свидетеля — `JSON.stringify(вызов.args)`» | «Есть дословно» |

---

## `flang/self/proof-kernel.flang`


### Эталон в шапке файла

Было в шапке файла, второй фразой:

> // Ядро доказательства: сведение заключения посылки к допущениям — на самом
> // flang. Эталон `flang/proof/reduce.mjs` (286 строк).

### Зачем этот файл — довод переноса с JavaScript

Было в шапке файла, врезка целиком:

> // ── Зачем этот файл ─────────────────────────────────────────────────────────
> // Довод тот же, что записан в шапке ведомости (`flang/self/proof.flang`):
> //
> //   пока машинерия доказательств живёт только на JavaScript, «доказуемость»
> //   языка означает «доказуемость при наличии Node», а это не то же самое
> //
> // По этому доводу перенесли ВЕДОМОСТЬ — отчёт о том, чем несётся обещание
> // каждой функции. Ведомость ничего не решает, она пересказывает. Решает — ядро,
> // и у ядра эталона не было: `flang/proof/kernel.flang` сворачивает уже
> // расставленные ПОМЕТКИ шагов в вердикт, а КТО расставил пометки — то есть кто
> // решил, выведен шаг или нет, — оставалось на JavaScript целиком.
> //
> // Этот файл переносит то самое место, где решение принимается. Дальше по
> // цепочке доверия идти уже некуда: выше него только порождение принципа
> // (`flang/proof/initial.mjs`), ниже — свёртка пометок, которая уже перенесена.

### Заголовок врезки «Что здесь есть»

Было в шапке файла:

> // ── Что здесь есть, дословно то же, что у свидетеля ───────────────────────────

Стало: `// ── Что здесь есть ──────────────────────────────────────────────────────────`

### Свидетель раздвоился на четыре вида: замер зазора по веткам

Было в шапке файла, врезка целиком:

> // ── Свидетель раздвоился, и это названо числом, а не обойдено молчанием ────────
> // На день переноса `flang/proof/reduce.mjs` существует в четырёх видах: 286
> // строк на `main` и три несведённые ветки, каждая из которых правит тот же
> // файл по-своему:
> //
> //   work/proof-types     391 строка. ТРЕТИЙ источник фактов: объявленный тип
> //                        аргумента (дно `неотрицательное`). Текст отказа стал длиннее на
> //                        строку перечня.
> //   work/proof-ceiling   591 строка. То же плюс ТРЕТЬЕ ПРАВИЛО —
> //                        «ограниченность точным потолком по построению»
> //                        (2⁵³−1). Ветка `kernel-unfold` его НЕ содержит.
> //   work/kernel-unfold   707 строк. То же, что `proof-types`, плюс ЧЕТВЁРТАЯ
> //                        переписка нормализации — развёртка определений
> //                        функций с пределом 32 и отменой при захвате имени.
> //                        Ветка `proof-ceiling` её НЕ содержит.
> //
> // Отсюда решение этого файла, и оно единственное честное: эталон не
> // притворяется, будто свидетель один. Перенесено то, что есть во ВСЕХ трёх ветках
> // (третий источник фактов, «Свести с объявлениями»), и то, что есть на `main`
> // («Свести»). Третье правило и четвёртая переписка НЕ перенесены, и об этом
> // сказано здесь, а не умолчано: эталон обязан догонять свидетеля, а догонять два
> // расходящихся свидетеля разом — значит не догнать ни одного.
> //
> // ЗАЗОР ИЗМЕРЕН, А НЕ ОЦЕНЁН НА ГЛАЗ. На одном и том же корпусе из 5520
> // утверждений вердикты эталона сличены с вердиктами каждой ветки:
> //
> //   work/proof-types    расхождений 0 — эталон догнал ветку целиком;
> //   work/kernel-unfold  расхождений 0, пока ядру НЕ ДАЮТ определений; дай их —
> //                       и на 216 утверждениях, нарочно написанных под развёртку,
> //                       решение расходится на 10, текст ещё на 15;
> //   work/proof-ceiling  решение расходится на 158 из 5520, текст ещё на 322 —
> //                       это и есть цена ненесённого третьего правила.
> //
> // Мерить это в дереве нечем: свидетелей с третьим правилом и с четвёртой
> // перепиской здесь НЕТ, они лежат на ветках. Поэтому числа выше получены
> // отдельным прогоном по копиям веток, а сверка в дереве
> // (`flang/test/self-proof-kernel.test.mjs`) сличает эталон с тем свидетелем,
> // который в дереве есть. Как только ветка приедет в `main`, сверка накроет её
> // сама и без правок.

### Два места, где flang и JavaScript расходятся — журнальная половина

Было в шапке файла, во врезке «Два места, где flang и JavaScript расходятся»:

> // Свидетель сличает узлы через `===` и `Object.keys`. Ни то, ни другое не
> // переносится дословно, и молчаливый перенос дал бы эталон, который в двух
> // точках отвечает ПРОТИВОПОЛОЖНОЕ:

и хвосты двух фраз:

> //               двойное «не меньше» даёт «нет», то есть ответ свидетеля.

> // именно этим сравнением, и эталон, считающий `-0` не нулём, отказывал бы там,
> // где свидетель выводит.

### Перекос обещаний, замер 22 августа

Было в шапке файла, во врезке «ЧТО ЭТОТ СЛОЙ ОБЕЩАЕТ О САМОМ СЕБЕ»:

> // ПЕРЕКОС, РАДИ КОТОРОГО ЭТО НАПИСАНО, ЗАМЕРЕН, А НЕ ПРОЦИТИРОВАН.
> // `flang check flang/self/bootstrap/compiler.flang` 22 августа: функций 7124,
> // с доказанным завершением 5247, типов 729, файлов вместе с импортами 36.
> // Обещаний о ПОВЕДЕНИИ в тех 34 файлах замыкания, что написаны здесь, было
> // ТРИДЦАТЬ ЧЕТЫРЕ — против 696 в библиотеке (`flang/stdlib`). В самом ядре
> // доказательства их было ДВА, и проверка заглушкой говорит про оба «про
> // подпись»: «предел развёртки неотрицателен» и «точный потолок неотрицателен»
> // переживают тело `0`, то есть верны про любую функцию этой подписи. Про место,
> // где принимается решение «доказано», содержательно не было сказано НИЧЕГО.

### Замер на лесенке из пяти ступеней: зеркало и дно произведения порознь

Было в шапке файла, во врезке «ЧТО ЭТОТ СЛОЙ ОБЕЩАЕТ О САМОМ СЕБЕ»:

> // ТО ЖЕ САМОЕ КАСАЕТСЯ ДОПУЩЕНИЙ, И ЭТО ЗАМЕРЕНО ТОЙ ЖЕ ПРОБОЙ. Дно терма ядро
> // читает у допущения `Л не больше Т`, и `Т не меньше Л` — та же истина знак в
> // знак — вида ему НЕ ИМЕЛО, пока рядом не встало «Привести допущения к одному
> // виду» (зеркало порядка, ниже). Замер на лесенке из пяти ступеней, три
> // двоичных из одного дерева:
> //
> //   зеркало без дна произведения   доказано 0 из 3 ступеней с умножением
> //   дно произведения без зеркала   доказано 0 при записи «не меньше»,
> //                                  2 при записи «не больше»
> //   оба правила вместе             см. отчёт работы

### Чем несётся обещание: проверка на пустоту, 49 сведённых, 13 переживших подмену

Было в шапке файла, во врезке «ЧТО ЭТОТ СЛОЙ ОБЕЩАЕТ О САМОМ СЕБЕ»:

> // ЧЕМ ЭТО НЕСЁТСЯ. Каждое СВЕДЕНО ядром — сказано обо ВСЕХ входах, а не
> // посчитано на сетке примеров; ведомость называет правило поимённо. Сверх того
> // каждое проверено НА ПУСТОТУ тем же приёмом, каким проверена библиотека
> // (`flang/scripts/storozh-pustoty.flang`): тело заменялось заглушкой той же
> // подписи, примеры снимались у обоих исходников, ведомость снималась заново, по
> // ОДНОЙ функции за раз. Из 49 сведённых подмену пережили 13 — все одного вида,
> // «лишнего не добавлено к <входу>»: заглушка `пустой список` короче любого
> // входа, и утверждение о ней истинно само собой. Они здесь не написаны: слово
> // «доказано» о них законно, а знания в них нет.

### Чего ядро не берёт: 248 кандидатов, 49 сведено, 30 из 36 целей упёрлись в `разбор`

Было в шапке файла, во врезке «ЧТО ЭТОТ СЛОЙ ОБЕЩАЕТ О САМОМ СЕБЕ» (поведенческая
половина — про закрытый ход внутрь `разбор` — оставлена на месте):

> // ЧЕГО ЯДРО НЕ БЕРЁТ, И ЭТО ЗАМЕРЕНО. Кандидатов было 248, сведено 49.
> // Причина отказов названа пробой, а не на глаз: цель `(длина вход) не больше
> // (длина результат)` при теле `если … то … иначе …` сводится правилом «разбор
> // цели по условию», а при теле `разбор … случай … случай …` — НЕ сводится. Ход
> // внутрь `разбор` закрыт нарочно (см. «СВЯЗЫВАТЕЛЯ НАД ДИЗЪЮНКЦИЕЙ НЕТ ПО
> // ПОСТРОЕНИЮ» ниже): образец связывает имена, и ветвь значит не то же, что
> // целое. Из 36 целей «развёрнутое не забывается» на это упёрлись 30. То есть
> // больше всего этому файлу дало бы ОДНО правило: деление цели по `разбор` там,
> // где ни одно связанное образцом имя в цель не входит. Пока его нет, эти
> // тридцать обещаний не написаны вовсе: объявить их без доказательства значило
> // бы поставить сторожа в рантайм и назвать это знанием.

### Цена проверки: было 144,7 с и 5,84 ГиБ, стало 151,4 с и 6,21 ГиБ

Было в шапке файла, во врезке «ЧТО ЭТОТ СЛОЙ ОБЕЩАЕТ О САМОМ СЕБЕ»:

> // ЦЕНА НАЗВАНА ЧИСЛОМ. `flang check --proof` этого файла: было 144,7 с и
> // 5,84 ГиБ, стало 151,4 с и 6,21 ГиБ (замер подряд на одной машине). Печать
> // компилятора снимает проверку при работе только у постусловия, которое И
> // сведено ядром, И покрыто примерами функции; примеров здесь нет ни у одной
> // функции, значит все тридцать шесть проверок уезжают в напечатанный код.

### Окружение нормализации — `Map` свидетеля

Было у объекта «Связь»:

> // Окружение нормализации: имя → уже нормализованное поддерево. У свидетеля это
> // `Map`, и семантика взята его: «положить» затирает прежнюю связь, «убрать»
> // снимает её совсем.

### Итог сведения — возврат `свести` свидетеля

Было у объекта «Сведение»:

> // Итог сведения — ровно то, что возвращает `свести` свидетеля: принято или нет,
> // каким правилом, если не принято — почему, и ЧТО ПРИ ЭТОМ РАЗВЁРНУТО.

и там же:

> // «развёрнуто» — поле `unfolded` свидетеля, список вида «имя: правило развёртки».

### Состояние нормализации — изменяемый объект свидетеля

Было у объекта «Состояние ядра»:

> // У свидетеля это изменяемый объект, который `шаг` носит с собой и правит на
> // месте. В flang изменяемого нет, и состояние ПРОТЯГИВАЕТСЯ через обход явно:
> // каждая функция обхода принимает его и возвращает вместе с узлом.

### Состояние при отказе развёртки — расхождение со свидетелем

Было у объекта «Попытка развёртки»:

> // Итог попытки развернуть вызов. Состояние возвращается и при отказе, и это
> // НЕ мелочь: свидетель правит своё состояние на месте, поэтому попытка, отменённая
> // захватом имени, уже потратила предел и уже записала вложенные развёртки.
> // Верни здесь состояние нетронутым — и эталон разошёлся бы со свидетелем ровно на
> // тех входах, ради которых ограничитель заведён.

### Дата и цена захода перепечатки: 29 августа 2026, 6 ч 23 мин

Было у функции «Отказ порядка», во врезке «ОТКАЗ РАЗРЕЗАН НА ДВЕ ЧАСТИ»:

> // -Werror`, свою же печать отвергает: заход перепечатки 29 августа 2026 дошёл
> // до конца за 6 ч 23 мин, а `make -C bootstrap` упал на этой строке за 37,30 с.

### Приписка о пределе — как она собрана у свидетеля

Было у функции «О пределе»:

> // Приписка о достигнутом пределе. Она приписывается к отказу ТРЁХ правил и не
> // приписывается к отказу «правила нет»: у свидетеля она собрана один раз и
> // подставлена в три ветви из четырёх, и место, где её нет, значимо не меньше
> // того, где она есть.

### Откуда взято число точного потолка

Было у функции «Точный потолок»:

> // Точный потолок натурального: `неотрицательное` — это отрезок [0, 2⁵³−1], и здесь стоит
> // его верх. У свидетеля число берётся у типизатора (`ТОЧНАЯ_СЕТКА.верх`), и по
> // той же причине, по какой имена `неотрицательное` ниже собраны одной строкой: второй ответ
> // на тот же вопрос расходится с первым молча.

### Эталоны свидетеля у проверок скаляров

Было у функции «Это число ядра»:

> // `typeof значение === "number"` свидетеля.

Было у функции «Это ложь»:

> // `значение === false` свидетеля: настоящий признак и именно ложь.

Было у функции «Одинаковы»:

> // `одинаковы` из `flang/src/obligations.mjs`: структурное равенство с одной
> // оговоркой — ключ `span` не сличается вовсе.

### Ошибка порядкового сличения, сделанная трижды: Ч34, Ч92, Ч45, Ч86

Было у функции «Числа сошлись по порядку»:

> // ровно наоборот на обеих парах. Пока функция звалась «Числа сошлись по порядку»,
> // имя обещало ТОЖДЕСТВО, а давало ПОРЯДОК; всякое правило, спросившее её про
> // ТЕРМ, заключало «один и тот же терм» и доказывало ложь на минус нуле. Эту
> // ошибку сделали трижды подряд и в двух разных программах — в ядре (Ч34) и в
> // независимом чекере (Ч92), — и оба раза чинили её потом рубкой: минус ноль
> // отвергали целиком, теряя правду (застава Ч45 в ядре, широкий счётчик Ч86 в
> // чекере). Верно ни то, ни другое.

### Дата прогона, которым снята улика заставы минус нуля

Было у функции «Числа одно и то же значение»:

> // УЛИКА, РАДИ КОТОРОЙ ЗАСТАВА СТОИТ ЗДЕСЬ (прогон 31 августа 2026). «Одинаковы»

### Список отбрасываемых полей — `ОТМЕТКИ_АНАЛИЗА` свидетеля

Было у функции «Не из написанного»:

> // Поля, которых нет в написанном: место в файле и отметки анализа. Свидетель
> // отбрасывает ровно их перед сортировкой ключей (`ОТМЕТКИ_АНАЛИЗА` в
> // `src/obligations.mjs`), и список обязан быть тем же — иначе два ядра ответят
> // по-разному на вопрос «одинаково ли написано».

Было у функции «Значимые поля»:

> // Поля без места и без отметок — то, что свидетель отбирает перед сортировкой ключей.

### Сличение записей — половины свидетельского сличения

Было у функции «Записи сличимы»:

> // Записи сличимы, если значимых ключей у них поровну. Это первая половина
> // свидетельского `левые.length !== правые.length → false`; вторая половина — что
> // каждый ключ нашёлся, — считается обходом ниже.

Было у функции «Поля сходятся»:

> // Ключи свидетель СОРТИРУЕТ и сличает попарно. Здесь сортировки нет, и ответ тот
> // же: у записи ключи не повторяются, поэтому «столько же значимых ключей, и
> // каждый ключ левой записи нашёлся в правой с тем же значением» — то же самое
> // утверждение, записанное без порядка.

### Чем свидетель заполняет незаполненное поле образца

Было у функции «Узел имени»:

> // Узел `{ kind: "var", name: имя }` — то, чем свидетель заполняет поле образца,
> // которого у конструкции не оказалось.

### Эталоны свидетеля у сборки узлов и окружения

Было у функции «Заменить поле ядра»:

> // `{ ...узел, ключ: значение }` свидетеля: существующий ключ меняет значение и
> // остаётся на своём месте, нового ключа у AST не бывает — но если он всё же
> // придёт, ему место в конце, ровно как у JavaScript.

Было у функции «Убрать по узлу»:

> // Снять связь по имени, записанному узлом. `typeof узел.acc === "string"`
> // свидетеля: не строка — связи нет и снимать нечего.

Было у функции «Это ноль ядра»:

> // `узел.value === 0` свидетеля — то самое место, где минус ноль решает.
> // `-0 === 0` истинно, значит цель «Е не меньше -0» — та же самая цель, и
> // отказать ей значило бы разойтись со свидетелем на литерале, который автор
> // написать вправе.

Было у функции «Это целое число»:

> // `Number.isInteger` свидетеля: конечное число, у которого дробной части нет.

### Замер двух записей конечности: дизъюнкция доказана, охрана — нет

Было во врезке «КОНЕЧНОСТЬ, СКАЗАННАЯ ОХРАНОЙ ЦЕЛИ, А НЕ ЕЁ ОГОВОРКОЙ»:

> // ЗАМЕР, А НЕ ДОГАДКА. Двоичным из ствола, одна проба, две функции знак в знак
> // с одним телом `н умножить на н`:
> //
> //   `не ((н минус н) равен 0) или (результат не меньше 0)`   ДОКАЗАНО
> //                                       правилом «неотрицательность по построению»
> //   `если ((н минус н) равен 0) то результат не меньше 0 иначе да`
> //                                       ОБЪЯВЛЕНО, НЕ ДОКАЗАНО
> //
> // Одна теорема, две записи, один вердикт из двух. Здесь читается вторая.

### `Number.isFinite` свидетеля

Было у функции «Конечное число»:

> // `Number.isFinite` свидетеля отвергает три значения: `NaN`, `+∞` и `−∞`.

### Ссылки на свидетеля в нормализации и разборе

Было у функции «Нормальная форма»:

> // Состояние здесь заводится ЗАНОВО на каждый вызов, ровно как у свидетеля: предел
> // у каждой нормализации свой.

Было у функции «Шаг элемента»:

> // Обход списка СЛЕВА НАПРАВО с протяжкой состояния — `узел.map(шаг)` свидетеля.

Было у функции «Шаг записи»:

> // Порядок ветвей повторён дословно за свидетелем: `var`, `let`, `match`, `if`,
> // `fold`, и только потом общий обход полей.

Было у функции «Условие как есть»:

> // Порядок обхода — `cond`, `then`, `else`, как в объектном литерале свидетеля.

Было у функции «Список пусто» (имена конструкторов встроенного списка):

> // Стоят они ЗДЕСЬ, а не в «Начальной алгебре», по той же причине, по какой у
> // свидетеля они лежат в `reduce.mjs`, а не в `initial.mjs`: их читают оба слоя, и
> // у вопроса «как называются конструкторы списка» обязан быть ОДИН ответ.

Было у функции «Поля звена»:

> // Имя, которого образец не написал, не связывается вовсе — как и у свидетеля,
> // где ключ кладётся только при `typeof === "string"`.

Было во врезке «ПОЧЕМУ ЭТО ТЕОРЕМА, А НЕ АКСИОМА» (разбор выписанного списка):

> // нет: выбор ветви ПРОЧИТАН У ВЫЧИСЛИТЕЛЯ. `разбор` по списку в
> // `flang/src/interpret.mjs` смотрит на длину уже посчитанного списка

Было во врезке «ШЕСТАЯ ПЕРЕПИСКА: ЗАМКНУТАЯ АРИФМЕТИКА»:

> // операция, какой считает вычислитель (`src/interpret.mjs`, `applyBinary`).

### Что было до правила разбора выписанного списка: прогон 24 августа 2026

Было во врезке «РАЗБОР ВЫПИСАННОГО СПИСКА», врезка целиком:

> // ── ЧТО БЫЛО ДО ЭТОГО, И ЭТО ЗАМЕРЕНО, А НЕ ВСПОМНЕНО ──────────────────────
> // Ход внутрь `разбор` у ядра БЫЛ — «Разбор по конструкции» выше выбирает ветвь
> // у объявленной суммы, — но смотрел он только на вид `construct`, а у
> // выписанного списка вид `list`. Отсюда измеренный ноль: охрана
> // `если Л равен пустой список то Ц иначе да` кладёт равенство допущением,
> // переписка («Переписать до неподвижной») ставит на место `Л` узел пустого
> // списка — и разбор ОСТАВАЛСЯ неразобранным, потому что ветвь по нему не
> // выбирал никто. Пара проб, снятая одним прогоном на стволе 24 августа 2026
> // (`flang check --proof`, двоичный из того же дерева):
> //
> //     свёртка пустой список начиная с основа как акк и эл → акк плюс эл
> //         обещано `результат равен основа`                     — доказано
> //     разбор пустой список случай пусто то основа случай голова «г» и хвост «х» то «г»
> //         обещано `результат равен основа`                     — сетка
> //
> // Знание об обоих термах ОДНО И ТО ЖЕ и написано одним словом; разнились только
> // правила, и разнились ровно этим местом.

### Заход 4 перепечатки: 252 818 958 531 шаг, 47,2 % всех шагов

Было у функции «Первое определение по имени»:

> // Это снятая впустую работа, а НЕ ослабление проверки: ни одного нового
> // допущения здесь не заводится, список принятого на веру не пополнился ни на
> // строку, и вердикт ядра остался прежним — сверено прогоном двух двоичных,
> // различающихся только этим местом, на 286 файлах дерева: вывод совпал слово
> // в слово, коды возврата тоже.
> //
> // ЧТО ЭТО СТОИЛО, названо числом. Заход 4 перепечатки
> // (`/srv/tmp/reprint-4/tiki-20260830-073351-78650b.txt`): «Развернуть» сняла
> // 252 818 958 531 шаг — 47,2 % ВСЕХ шагов захода, — и снимала их длиной
> // пройденного списка: 2 754 320 зовов по 91 790 элементов в каждом. Совпадение
> // при этом находится в 96,8 % зовов (2 665 506 из 2 754 320), то есть обрыв
> // работает почти всегда.

### Ссылки на свидетеля в развёртке и сборе имён

Было в теле функции «Развернуть телом»:

> // вложенные развёртки уже записаны. Так у свидетеля, и так же обязано быть здесь.

Было у функции «Связи параметров»:

> // продолжение внешнего: у свидетеля здесь `new Map()`. Обход идёт слева направо,
> // поэтому при двух одноимённых параметрах побеждает последний — как у `set`.

Было у функции «Конструктор на месте»:

> // в языке индексирует с единицы. Ноль означает «такого параметра нет» — то же,
> // что `−1` у свидетеля.

Было у функции «Имя переменной по полям»:

> // Имя узла `var` собирается И ПОСЛЕ ЭТОГО обход продолжается по полям — так у
> // свидетеля, и это не лишняя работа

Было у функции «Связыватель ловит»:

> // Три связывателя одним списком случаев. Проверка вида идёт ДО общего обхода и
> // не отменяет его: у свидетеля за `if` без `return false` идёт тот же перебор
> // полей, и узел, связавший чужое имя глубже, обязан находиться так же.

### Сверка со свидетелем у правил ограниченности и порядка

Было у функции «Форма от нуля»:

> // Имена берутся канонические и с псевдонимами — теми же, какими их сводит
> // таблица встроенных форм у свидетеля.

Было во врезке о свёртке (список целых внутрь свёртки):

> // ЦЕЛОСТЬ ВНУТРЬ СВЁРТКИ НЕ ЕДЕТ ВОВСЕ, и это не пропуск: у свидетеля свёртка
> // зовёт `неотрицательно` ЧЕТЫРЬМЯ доводами, то есть список целых там пуст. Отдай
> // его сюда — и правило остатка внутри тела свёртки заработало бы там, где у
> // свидетеля оно молчит; сверка на этом и разошлась бы.

Было у функции «Не число ли»:

> // `Number.isNaN` свидетеля. Не-число не меньше и не больше ничего, поэтому одного
> // сравнения `не больше` для него хватило бы и без этой проверки; она стоит
> // здесь затем, чтобы список случаев читался тем же порядком, что у свидетеля.

Было во врезке «ПРАВИЛО 4: порядок по построению (граница — ТЕРМ)»:

> // Эталон `поПорядку` свидетеля (`flang/proof/reduce.mjs`), и довод у него тот же,
> // слово в слово: в IEEE-754 `Е не больше Е` НЕ теорема — на не-числе это ложь.

### История правила 3: довод против потолка

Было во врезке «правило 3: ограниченность точным потолком»:

> // Третье по времени и ВТОРОЕ ПО РОДСТВУ: оно читает те же объявления, что и
> // первое, и стоит здесь потому, что `неотрицательное` — это отрезок, а у отрезка две
> // границы. Дно ядро получило раньше, потолок — нет, и довод против был назван
> // прямо: «факт, которым никто не пользуется, — доверие, за которое некому
> // отвечать». Отвечать за него стало кому: граница входа сверяет аргументы с
> // объявленным типом ДО вычисления, и значение вне `неотрицательное` до тела не доходит.

### Почему при «Есть порядок» стояло «этого не будет»

Было во врезке о ходе через середину:

> // ── ПОЧЕМУ ЗДЕСЬ СТОЯЛО «ЭТОГО НЕ БУДЕТ» ───────────────────────────────────
> // При «Есть порядок» стоял довод: «искать середину значило бы завести перебор,
> // а поиска у ядра нет ни одного». Довод был про ЦЕНУ, и цена эта не была
> // мерена ни разу. Замер назван в
> // `docs/zettel/the-price-of-the-midpoint-search-is-linear-in-the-order-facts.md`;
> // коротко: перебор здесь НЕ ветвится и НЕ откатывается — …

### Прогон 24 августа 2026: четыре записи одной мысли с телом `а`

Было во врезке «ЗАЧЕМ ВТОРОЙ ИСТОЧНИК, ЕСЛИ ХВАТАЛО ПЕРВОГО»:

> // Затем, что первого НЕ ХВАТАЛО, и это замер, а не рассуждение. … Прогон 24 августа 2026,
> // двоичным из ствола, четыре записи одной мысли с телом `а`:
> //
> //   `а: нат`,          `а не больше результат`                     ДОКАЗАНО
> //   `а: число` + `требует «вход неотрицателен» а не меньше 0`      ДОКАЗАНО
> //   `а: число`, `не ((а минус а) равен 0) или (а не больше …)`     ОБЪЯВЛЕНО
> //   `а: число`, `если ((а минус а) равен 0) то (а не больше …)`    ОБЪЯВЛЕНО
> //
> // Две нижние записи ИСТИННЫ (конечное число не больше себя) и не выводились ни
> // одним правилом. Здесь читается вторая посылка, и обе нижние закрываются.

### Мёртвые ссылки на свидетеля в сборе имён и мере списка

Было у функции «Имена аргументов»:

> // Свидетель собирает их в множество; здесь список, и повторы в нём безвредны:

Было во врезке «МЕРА СПИСКА: пять новых случаев правила 4»:

> // Близнец `мераПостроения`, `поРазбору`, `вВетви`, `свёрткаНеБыстрееСписка`
> // эталона (`flang/proof/reduce.mjs`). Довод у всех один и тот же, и он не
> // «принято считать», а ПРОЧИТАНО У ВЫЧИСЛИТЕЛЯ: каждое равенство ниже
> // вычисляют `flang/src/interpret.mjs` и `flang/src/builtins.mjs` буквально.

Было во врезке «РАЗБОР ПОД ГРАНИЦЕЙ»:

> // Значение `разбор`а есть значение ТЕЛА ровно одной ветви — так его считает
> // `flang/src/interpret.mjs`, третьего значения не бывает.

Было во врезке «ПОЧЕМУ ЭТО ТЕОРЕМА, А НЕ АКСИОМА» (свёртка по пустому списку):

> // нет: равенство ПРОЧИТАНО У ВЫЧИСЛИТЕЛЯ. `свёртка` в `flang/src/interpret.mjs`
> // заводит накопитель значением начала и идёт по элементам списка

### Цена отсутствия хода замерена: распределитель памяти, прогон 24 августа 2026

Было у функции «Слагаемое границы»:

> // ЦЕНА ОТСУТСТВИЯ ЭТОГО ХОДА ЗАМЕРЕНА. Распределитель памяти
> // (`examples/allocator/allocator.flang`, 89 утверждений) назвал его первым в
> // списке недостающего: «границы отрезка приходится писать равенствами, а не
> // неравенствами — одна и та же мысль равенством доказывается, неравенством
> // остаётся сеткой». Прогон 24 августа 2026 на стволе, `а: нат`, `б: нат`,
> // тело `а плюс б`: `а не больше результат` — ОБЪЯВЛЕНО, НЕ ДОКАЗАНО.

### Правило 11: чего не было до него, прогон 24 августа 2026

Было во врезке «ПРАВИЛО 11: СТРОГИЙ ПОРЯДОК»:

> // ── ЧТО ЭТО ЗА ВИД ЦЕЛИ, И ПОЧЕМУ ЕГО НЕ БЫЛО ВОВСЕ ────────────────────────
> // Цель `Е меньше Т` и она же, записанная другой стороной, — `Т больше Е`. До
> // этой строки строгого вида цели у ядра не было НИ ОДНОГО: «Отказ без правила»
> // перечислял семь видов, и все семь нестрогие. Замкнутую строгую цель ядро
> // берёт вычислением («Вычислением», четвёртый ход) — прогон 24 августа 2026 на
> // теле-литерале `5`: `результат меньше 6` и `результат больше 4` оба ДОКАЗАНЫ
> // счётом. Значит дыра стояла ровно там, где в цели осталось СВОБОДНОЕ ИМЯ:
> // считать нечего, а вида цели нет.

Там же, у двух проб (таблицы оставлены на месте, снята только дата прогона):

> // ── РАЗЛОЖЕНИЯ «МЕНЬШЕ ЕСТЬ НЕ БОЛЬШЕ И НЕ РАВНО» ЗДЕСЬ НЕТ, И ЭТО ЗАМЕРЕНО ─
> // Разложение это напрашивается первым и НЕВЕРНО в flang. Прогон 24 августа
> // 2026, `flang run`, три вызова подряд на одном двоичном:

> // ── ПРИБАВЛЕНИЕ ПОЛОЖИТЕЛЬНОГО СТРОГОСТИ НЕ ДАЁТ, И ЭТО ТОЖЕ ЗАМЕРЕНО ───────
> // Второе, что напрашивается: `Е меньше (Е плюс Д)` при строго положительном
> // конечном `Д`. Тем же прогоном, на той же паре:

### Замер по дереву 24 августа 2026: 116 / 17 / 3 записи охраны пустоты

Было во врезке «`пусто Л` ЕСТЬ ТО ЖЕ ДОПУЩЕНИЕ, ЧТО `(длина Л) равен 0`»:

> // ЗАЧЕМ, ЧИСЛОМ. Замер по всему дереву вне `flang/test/` (24 августа 2026): у
> // функций с телом `разбор Х` и обещанием, охраняющим себя пустотой ТОГО ЖЕ `Х`,
> // охрана записана `пусто Х` в 116 обещаниях, `(длина Х) равен 0` — в 17,
> // `(длина Х) больше 0` — в 3. То есть читаемая запись стояла у каждого седьмого,
> // а у шести из семи — нечитаемая. Тот же счёт, снятый днём раньше чуть иным
> // отбором, дал 114/18/5 и записан в
> // `docs/zettel/emptiness-guards-are-written-with-pusto-but-the-kernel-reads-only-the-measure.md`;
> // расходятся числа в мелочах отбора, порядок у обоих один.
> //
> // Разница не в математике, а В ДОРОГЕ: до этой правки `пусто Х` не доезжало до
> // правил ВОВСЕ — переписки отбирали из фактов одни равенства, а встроенная форма
> // равенством не является, и до читателя пустоты, который её и так не знал, это
> // допущение просто не доходило.

### Список имён веса — `ИМЕНА_ВЕСА` свидетеля

Было у функции «Названо весом»:

> // значение, а не край. Список имён — тот же, что у свидетеля (`ИМЕНА_ВЕСА` в
> // `src/types.mjs`), и спрашивается ОТДЕЛЬНО от натурального

### Цена обхода счёта постоянных: коммиты `70143cd1` и `6d2bec70`

Было во врезке «СЧЁТ ПОСТОЯННЫХ ПРИ ОДНОЙ МЕРЕ»:

> // идёт синтаксически, и такая пара расходилась деревом, а не значением. Цена
> // обхода уплачена дважды и названа числом: коммит `70143cd1` переписал девять
> // целей в девяти файлах `flang/self/*`, коммит `6d2bec70` — четыре тела в
> // `flang/core/json.flang`; тринадцать мест в десяти файлах ради того, чего ядро
> // не умело в одном.

### Дата улики заставы минус нуля в правиле тождества: прогон 23 августа 2026

Было в теле функции «Свести равенство»:

> // ЗАСТАВА МИНУС НУЛЯ СТОИТ И ЗДЕСЬ, и поставлена она по УЛИКЕ, а не на всякий
> // случай. Прогон 23 августа 2026 на стволе:

### Разобранная ошибка при правиле вхождения: форма искомого

Было во врезке «ПРАВИЛО 6: ВХОЖДЕНИЕ ПО ПОСТРОЕНИЮ», врезка целиком:

> // ── ЗДЕСЬ Я ОДИН РАЗ УЖЕ ОШИБСЯ, И ОШИБКА ЗАПИСАНА, А НЕ СТЁРТА ─────────────
> // Сначала правило спрашивало ещё и ФОРМУ искомого: считалось, что «значение
> // равно самому себе» истинно не для всякого значения — что функция-значение не
> // попадает ни в одну ветвь сличения и потому не равна даже себе, а список
> // функций язык объявить позволяет (`содержит` типизатор берёт у элемента ЛЮБОГО
> // типа, в отличие от `равен`). Проверка была снята ИЗЪЯТИЕМ и вердикт менялся,
> // что и приняли за доказанную ложь.
> //
> // ПРОГОН СКАЗАЛ ОБРАТНОЕ, и вот он:
> //     [функция «Меньше»] содержит (функция «Меньше»)   → да
> //     [функция «Меньше»] содержит (функция «Больше»)   → нет
> // Причина в пользу языка: ДЕФУНКЦИОНАЛИЗАЦИЯ превращает значение-функцию в
> // ВАРИАНТ, помеченный именем функции, с захваченными полями
> // (`src/interpret.mjs`, случай "fnref"), а варианты сличаются по имени и полям.
> // Значит равенство на функциях есть и оно верное.

(Поведенческая половина — что форма искомого не спрашивается и почему —
оставлена на месте под новым заголовком «РАВЕНСТВО НА ФУНКЦИЯХ ЕСТЬ, И ОНО
ВЕРНОЕ».)

### Мёртвые ссылки у пятого хода и у списка связывателей

Было у функции «Утверждение допущения»:

> // голое утверждение — само себе утверждение. `д?.claim ?? д` свидетеля.

Было во врезке «ПЯТЫЙ ХОД: РАЗБОР ЦЕЛИ ПО УСЛОВИЮ»:

> // Эталон `поУсловию` свидетеля (`flang/proof/reduce.mjs`).

Было у функции «Это связыватель»:

> // больше нет одного значения. Что списки не разошлись, проверяет отдельный тест
> // («ДРЕЙФ СПИСКОВ» в `flang/test/proof-soundness.test.mjs`).

Было у функции «Литерал признака»:

> // `value`, и `span` среди них НЕТ — свидетельские `ДА` и `НЕТ` собраны без места.

### Что закрыла охрана-факт, числом

Было во врезке о ходе, кладущем условие в допущения:

> // ── Что это закрывает, числом ───────────────────────────────────────────────
> // Охрана вида `если («Это дерево поиска» от дерево) то Ц иначе да` до этой
> // правки не давала ядру НИЧЕГО: условие подставлялось в цель значением, а в
> // допущения не попадало. Замер на пробе: цель «Хорошее» от л под охраной
> // «Хорошее» от дерево — сетка до правки, доказано после.

### Сколько конъюнкций в библиотеке

Было во врезке «КОНЪЮНКЦИЯ: ПОЛОВИНА «УСЛОВИЕ ЛОЖНО» ЕСТЬ ЛОЖЬ»:

> // обе её половины по отдельности доказуемы. Замер: таких целей в библиотеке 67.

### Порядок ходов и разбор `или` — ссылки на свидетеля и на `interpret.mjs`

Было у функции «Свести ходами»:

> // ЧЕТВЁРТЫЙ ХОД СТОИТ ПЕРЕД ПЯТЫМ, и порядок этот у свидетеля закреплён … На
> // этом стоит сверка `self-proof-kernel.test.mjs`, которая
> // зовёт `свести` свидетеля без шестого довода.

Было во врезке «ШЕСТОЙ ХОД: ЦЕЛЬ-ВЫБОР С ИСТИННОЙ ВЕТВЬЮ»:

> // `если А то да иначе Б` (`flang/src/parser.mjs`). Значит всякая цель-дизъюнкция

> // `да` отдать `да`, НЕ СЧИТАЯ второй ветви (`interpret.mjs`, `stepIf`). Значит из

Было во врезке «ПОЧЕМУ ТЕОРЕМА, А НЕ АКСИОМА» (седьмой ход, разбор случаев):

> // значение Б (`interpret.mjs`, `stepIf`). У признака значений ровно два — это

### Что восьмой ход дал библиотеке, числом

Было во врезке восьмого хода:

> // ── ЧТО ХОД ДАЛ БИБЛИОТЕКЕ, ЧИСЛОМ ─────────────────────────────────────────
> // Замер парный, на одном дереве, двоичным из ствола и двоичным с этим ходом,
> // шесть файлов `flang/stdlib`: +2 утверждения — «ограничение отдаёт либо само
> // число, либо одну из границ» у «Ограничить» (`numbers.flang`) и «всё, что не
> // запасное, подходит» у «Найти первое» (`higher-order.flang`, там ход работает
> // внутри шага индукции по списку). Остальные четыре файла — ноль, и ноль этот
> // измерен, а не предположен.

### Улика, с которой начато расщепление конъюнкции в допущении

Было во врезке «ШЕСТОЕ ПРАВИЛО СВЕДЕНИЯ: КОНЪЮНКЦИЯ В ДОПУЩЕНИИ РАСЩЕПЛЯЕТСЯ»:

> // ── Улика, с которой это начато ─────────────────────────────────────────────
> // Охрана `если («Это дерево поиска» от дерево) то … иначе да` делает утверждение
> // о дереве поиска ИСТИННЫМ, но ядру не даёт ничего: разбор цели по условию
> // подставляет в цель значение условия и всё. Замер на пробе:
> //
> //   `если («Хорошее» от дерево) то («Хорошее» от дерево) иначе да`  — ДОКАЗАНО
> //   `если («Хорошее» от дерево) то («Хорошее» от л)     иначе да`  — сетка
> //
> // Второе — это ровно первая лемма всякого доказательства о дереве поиска:
> // «часть дерева поиска — тоже дерево поиска». Отказ называл причину дословно:
> // «ни одно допущение не совпало с целью знак в знак».

### Улика зеркала порядка: два вердикта и 38 мест в библиотеке

Было во врезке «ДОПУЩЕНИЯ ПРИВОДЯТСЯ К ОДНОМУ ВИДУ: ЗЕРКАЛО ПОРЯДКА»:

> // ── УЛИКА, С КОТОРОЙ ЭТО НАЧАТО ─────────────────────────────────────────────
> // Одно и то же утверждение, записанное зеркально, ядро судило по-разному:
> //
> //   если (а не меньше 1) то (1 не больше результат) иначе да   — сетка
> //   если (1 не больше а) то (1 не больше результат) иначе да   — ДОКАЗАНО
> //
> // Причина в наборе, а не в трудности вывода. … По
> // `обеспечивает` библиотеки таких мест 38 (numbers 8, datetime 7, http 5,
> // sha256 4, lists 3, дальше по одному-два).

### Прогон 22 августа 2026 по углам IEEE-754

Было там же:

> // ── УГЛЫ IEEE-754, И ОНИ ПРОСЧИТАНЫ, А НЕ ВСПОМНЕНЫ ─────────────────────────
> // Программа `u-zerkalo-uglu.flang` считает обе записи на всех парах десяти
> // углов — не-число, `+∞`, `−∞`, `−0`, `0`, `1`, `−1`, `2.5`, наименьшее
> // денормальное, наибольшее конечное. Прогон 22 августа 2026:
> //
> //   углов 10, пар 100, расхождений 0

### Улика правила 12: задача 1547, 176 из 1026 обязательств с `gte`

Было во врезке «ПРАВИЛО 12: ЗЕРКАЛО САМОЙ ЦЕЛИ»:

> // ── Улика, с которой это начато ─────────────────────────────────────────────
> // Зеркало у ядра было ОДНО и переворачивало ФАКТЫ («Зеркало факта» выше). Цель
> // не переворачивал никто, и оттого `Е не меньше Г` не брало НИ ОДНО правило:
> // «Это неотрицательность» спрашивает `gte` с НУЛЁМ справа, «Это порядок
> // термом» — только `lte`, а строгие знаки берёт правило 11 и без зеркала.
> // Замер по замыканию перепечатки (задача 1547): из 1026 обязательств, ушедших
> // в «Отказ без правила», 176 имели верхним узлом `gte`. Автор, написавший
> // границу «снизу», не получал от ядра НИЧЕГО, хотя та же самая мысль,
> // записанная «сверху», закрывалась правилом 4.

### Порядок проверок, повторённый за свидетелем

Было у функции «Свести правилами»:

> // Порядок проверок повторён за свидетелем дословно: допущение, неотрицательность,
> // ограниченность, порядок термом, тождество, и только потом отказ без правила. … К
> // последнему отказу приписки о пределе НЕ прибавляется — у
> // свидетеля её там нет, и место, где её нет, значимо не меньше того, где она есть.

### Что правило 8 дало библиотеке, числом

Было во врезке «ПРАВИЛО 8: НЕСОВМЕСТИМАЯ ПАРА ДОПУЩЕНИЙ»:

> // ── ЧТО ПРАВИЛО ДАЛО БИБЛИОТЕКЕ, ЧИСЛОМ ────────────────────────────────────
> // Тем же парным замером на тех же шести файлах: +2 утверждения — «число внутри
> // границ не меняется» у «Ограничить» (`numbers.flang`) и «на строгом
> // неравенстве отдаётся то из двух, что больше» у «Большее»
> // (`higher-order.flang`). Оба закрылись правилом «разбор цели по условию»: само
> // правило противоречия в ведомости не названо, потому что срабатывает оно
> // ВНУТРИ половины разбора, а ведомость называет верхнее правило. Что закрылось
> // именно им, проверено изъятием — ветвь каждой из двух целей, выписанная
> // отдельной функцией с теми же двумя `требует`, двоичным из ствола остаётся
> // «объявлено, не доказано», а с этим правилом доказана.

### Столкновение имени «Сравнение»: даты, коммиты `a443b7c9` и `c2735c4c`, работа CI

Было у объекта «Приведённое сравнение»:

> // «Сравнение»` (варианты «Равно», «Не равно», «Не меньше», …) с 3 августа 2026,
> // а здешнее заведено 22 августа (`a443b7c9`) — девятнадцатью днями позже.
> //
> // Пока эти два имени не сходились ни в одной программе, связывание молчало:
> // связывание сливает объявления в ОДНУ плоскую таблицу, но только по
> // замыканию входного файла. 30 августа `c2735c4c` завёл
> // `flang/proof/midpoint-price.flang`, и он ввозит оба модуля целиком — обе
> // стороны сошлись в одном замыкании впервые. Сторож
> // `flang/scripts/link-collision-guard.mjs --дерево` ответил кодом 1, и работа
> // CI «Столкновения имён и ввозы» покраснела.

### Улика правила 9 (модус поненс по охране): прогон 22 августа

Было во врезке «ПРАВИЛО 9: МОДУС ПОНЕНС ПО ОХРАНЕ»:

> // ── Улика, с которой это начато ─────────────────────────────────────────────
> // Минимальная пара, различие в одну строку — постусловие соседа с оговоркой и
> // без неё (прогон 22 августа, `flang/proof/examples/chestnaya-modus-ponens-po-ohrane.flang`
> // против той же записи без охраны):
> //
> //   постусловие соседа `если х больше 0 то результат равен х иначе да`  — ОТКАЗ
> //   постусловие соседа `результат равен (х плюс («Ноль» от х))`         — ДОКАЗАНО
> //
> // Отказ называл причину дословно: «разбор цели по условию не прошёл: половина
> // «условие истинно» не сведена: тождество после переписки допущением не
> // проходит».

Там же:

> // посчитать `A`, и при `да` отдать значение `B`, НЕ СЧИТАЯ ветви `иначе`
> // (`interpret.mjs`, `stepIf`).

### Эталоны свидетеля у входов «Свести»

Было у функции «Свести»:

> // Эталон `свести(заключение, допущения)` свидетеля: объявления там по умолчанию
> // пусты, определения — их нет вовсе.

Было у функции «Свести витками»:

> // Ядро зовёт ЭТОТ вход и зовёт его с «Пределом ветвления» — ровно так же, как
> // все семь мест свидетеля называют `ПРЕДЕЛ_ВЕТВЛЕНИЯ` при вызове.

Было у функции «Свести счётом»:

> // ДАЮТ. Пять входов выше отдают «Нет программы», и это не умолчание «ладно», а
> // `вычислитель = null` свидетеля.

Было во врезке «свободные имена»:

> // Множества в языке нет, поэтому `Set` свидетеля записан списком с проверкой на
> // повтор. Это не мелочь исполнения: сверка побайтовая, значит и ПОРЯДОК выдачи
> // (порядок первого добавления, то есть порядок обхода), и отсутствие повторов
> // входят в предмет сверки наравне с самими именами.

### Переезд свободных имён из `proof-initial.flang` 18 августа

Было во врезке «свободные имена»:

> // ПЕРЕЕХАЛИ СЮДА ИЗ `proof-initial.flang` 18 августа, и переезд этот — не
> // уборка, а следствие СЛОЯ. … Читатели прежние: `proof-initial.flang` видит эти функции через
> // `использует`, `proofterm.flang` — тоже, и ни одно место вызова не тронуто.
> //
> // Ниже — текст как он стоял, знак в знак: переезд не правит поведения.

### Остальные ссылки на свидетеля, снятые точечно

Было у функции «Связыватель этого имени»:

> // Пять связывателей — те же самые пять, что и у списка `СВЯЗЫВАЮЩИЕ` эталона

Было у функции «Переписать»:

> // Скаляр не переписывается никогда: у свидетеля примитив уходит из функции до
> // сличения, и сличать его с узлом-образцом не с чем.

Было у функции «Свести ограниченность»:

> // сторон — со СВОИМ пределом развёртки у каждой: у свидетеля это два отдельных
> // вызова `нормализовать`, и счёт у них раздельный.

Было у объекта «Ветвление»:

> // (тогда в отказе о нём НИ СЛОВА — на этом стояла побайтовая сверка, пока хода
> // у эталона не было)

Было у функции «По условию»:

> // Порядок повторён за свидетелем: первая половина считается, и НЕ СВЕДЁННАЯ

Было у функции «Свести при известном»:

> // Три решающих правила, и ПОСЛЕ них — пятый ход. Порядок этот у свидетеля
> // закреплён

Было у функции «Есть допущением»:

> // Почему это правило теорема — разобрано при «Правило допущения» выше; эталон
> // `естьДопущением` свидетеля (`flang/proof/reduce.mjs`).
> //
> // ЧТО НЕ ЗАСЧИТЫВАЕТСЯ. Цель, которой нет вовсе, не совпадает ни с чем. У
> // свидетеля это `typeof цель !== "object"`: `одинаковы(undefined, undefined)`
> // ИСТИННО, и без проверки пустая цель закрывалась бы пустым допущением.

Было у функции «Свести ветвлением»:

> // И вход с ПЯТЫМ ХОДОМ. Ноль витков у трёх входов выше — не умолчание «ладно»,
> // а `витков = 0` свидетеля … На этом стоит сверка `self-proof-kernel.test.mjs`, которая
> // зовёт `свести` свидетеля без шестого довода.

Было у функции «Добавить свободное»:

> // `имена.add(имя)` свидетеля под связанными именами: связанное имя не свободно, а
> // уже собранное второй раз не кладётся и МЕСТА В ВЫДАЧЕ НЕ МЕНЯЕТ — так же
> // ведёт себя `Set`, и от этого зависит побайтовое совпадение.

Было у функции «Связать по узлу»:

> // Связать имя, записанное узлом: `typeof узел.acc === "string"` свидетеля.

Было у функции «Свободные из записи»:

> // Порядок ветвей повторён дословно за свидетелем, и повторён он не ради красоты

Было у функции «Свободные из узла»:

> // Скаляр имени не несёт: строки, числа, признаки и «ничто» свидетель отсекает
> // строкой `typeof узел !== "object"`, и отсутствующее заключение (`null` у
> // посылки без ветви) отсекается ею же — свободных имён у него ноль.

Было во врезке «Почему довод приезжает ПРОГРАММОЙ, а не вычислителем»:

> // У свидетеля ход отдаётся ЗАМЫКАНИЕМ: `вычислениемЗамкнутой` (`src/proofterm.mjs`)
> // захватывает программу и имя обёртки и уезжает пятым доводом в `свести`.
> // … Ответ от этого тот же.

Было у функции «Предел вычисления витков»:

> // Те же числа, что у `ПРЕДЕЛ_ВЫЧИСЛЕНИЯ` свидетеля, и названы они по той же
> // причине

Было во врезке «ИСХОД СЧЁТА — ЧЕТЫРЕ ОТВЕТА, А НЕ ОДИН ПРИЗНАК»:

> // У свидетеля (`src/proofterm.mjs`, `вычислениемЗамкнутой`) счёт замкнутой цели
> // кончается четырьмя разными ответами

> // ПЯТЫЙ вариант — «Счёт не звался» — не исход, а его отсутствие: программы не
> // дали (`вычислитель = null` свидетеля) или у цели остались свободные имена. О
> // нём в отказе НИ БУКВЫ, и на этом стоит побайтовая сверка всех тех мест, куда
> // ход не передан.

Было у функции «Нет программы»:

> // «Нет программы» — это `вычислитель = null` свидетеля: хода нет вовсе, и ответ

### Замер двух источников целости

Было во врезке о целости, сказанной охраной:

> // ЗАМЕР, А НЕ ДОГАДКА. Двоичным из ствола, одна проба, два тела знак в знак
> // (`н остаток от 5`) под одной охраной `н не меньше 0`:
> //
> //   `принимает н: целое` — целость ОБЪЯВЛЕНА       доказано, правило
> //                                                  «разбор цели по условию»
> //   `принимает н: число` + охрана целости в теле   сетка
> //
> // Одна теорема, два источника целости, один вердикт из двух. Здесь читается
> // второй.

### Слово «замер» у обещания «Ограничить»

Было во врезке восьмого хода (пример оставлен на месте):

> // Замер на настоящем обещании библиотеки (`flang/stdlib/numbers.flang`,
> // «Ограничить»):

### Цена отдельного девятого хода: `datetime.flang` и FLANG_RECURSION_LIMIT

Было у функции «Тождество счётом частей»:

> // Место выбрано ЗАМЕРОМ, а не вкусом. Ход стоял отдельным девятым и заново
> // проделывал всю подготовку правила тождества: нормализовал цель, разворачивал
> // меру длины, нормализовал каждое допущение и переписывал обе стороны. Работа
> // эта уже сделана правилом, и второй её проход стоил `flang/stdlib/datetime.flang`
> // ВСЕЙ ведомости: «Развернуть поле длины» упёрлась в предел 1 000 000 000 шагов,
> // и пять доказанных обещаний файла пропали. Замер: на стволе файл сводится
> // целиком, с отдельным девятым ходом — FLANG_RECURSION_LIMIT.

### Переезд «Свободных имён» как довод условия замкнутости

Было во врезке «Три условия, и ни одно не украшение»:

> //                индукции для `по примеру`. Именно ради этого вопроса те
> //                функции и переехали в этот файл: слой, обязанный решать, лежал
> //                ниже слоя, умеющего ответить.

### Что важно при слиянии: как менялся перебор правил и ходов

Было у функции «Тождество счётом частей», врезка:

> // ── ЧТО ЗДЕСЬ ВАЖНО ПРИ СЛИЯНИИ ─────────────────────────────────────────────
> // Ход — ВТОРАЯ ПОЛОВИНА ПРАВИЛА ТОЖДЕСТВА, а не девятый ход свода: он
> // спрашивается внутри «Свести равенство» и ровно там, где сличение сторон
> // отказало. Перебор шести правил и восьми ходов от этого не изменился ни на
> // шаг, но у ПРАВИЛА ТОЖДЕСТВА прибавилась вторая проба, и цель, которую прежде
> // закрывал один из ходов 5—8, теперь бывает закрыта раньше — тем же вердиктом
> // «доказано», но под другим именем правила. Отнять он не может ничего: принятое
> // сличением принятым и осталось.

## `flang/self/proof.flang`


### Зачем ведомость перенесена: свидетель `proof.mjs`, 519 строк

Было в шапке файла:

> // обещание каждой функции. Перенесена она сюда по той же причине, по которой
> // сюда переносились лексер и анализ: пока машинерия доказательств живёт только
> // на JavaScript, «доказуемость» языка означает «доказуемость при наличии Node»,
> // а это не то же самое. Свидетель — `flang/src/proof.mjs`, 519 строк; критерий
> // верности дифференциальный и побайтовый (`flang/test/self-proof.test.mjs`).

### Каким инструмент был до ведомости

Было в шапке файла, врезка «Зачем ведомость вообще»:

> // Язык различает доказанное и посчитанное (docs/overview.ru.md), но различал он
> // это только в документации. Инструмент говорил одно слово на всё: `flang check`
> // печатал `valid: true`, и за этим `true` стояли рядом три совершенно разных
> // вещи — завершение, доказанное обходом графа вызовов; ассоциативность,
> // посчитанная на двенадцати значениях автора; и обратимость изоморфизма, не
> // посчитанная ничем. Прочитать по выводу, какая из них чем является, было
> // нельзя.

Осталось: тот же перечень трёх разных вещей — без «инструмент говорил одно
слово» и без `valid: true`.

### Запрет слова «провер…» держится тестом на JavaScript

Было в шапке файла, врезка «Слова, и путать их нельзя»:

> // «нет» — разные утверждения (flang/cat/SETS.md). Запрет держится проверкой, а
> // не обещанием: `flang/test/self-proof.test.mjs` ищет корень слова в готовом
> // тексте и краснеет на любом вхождении.

### Почему в печатном словаре три строки, а не четыре

Было в шапке файла; абзац убран целиком:

> // В ПЕЧАТНОМ СЛОВАРЕ («Словарь ведомости») строк три, а не четыре, и это не
> // недосмотр. Печать обязана совпадать со свидетелем `flang/src/proof.mjs` знак в
> // знак, а у него четвёртой строки пока нет: слово пришло из ядра
> // доказательств, которое живёт отдельной веткой. Строка появится здесь ровно
> // тогда, когда появится там, — одним изменением в двух файлах, иначе
> // побайтовая сверка перестанет быть побайтовой. Само слово при этом печатается
> // уже сейчас, у той функции, к которой относится: молчать о нём было бы хуже,
> // чем не объявить его в шапке.

### С каких пор заполняется поле «спуски» и чем оба случая сторожатся

Было в шапке файла, врезка «Откуда берутся числа»:

> // `«Итога тотальности»`. Четвёртое поле, `«спуски»`, заполняется с тех пор, как
> // самоприменённый анализ объявленную меру не только разбирает, но и ПРОВЕРЯЕТ.
> // Менять здесь для этого не пришлось ничего, и это было условием: ведомость
> // написана так, что пустой спуск при объявленной мере даёт строку «объявлено,
> // не доказано» — и ни слова «доказано» в ней нет, — а непустой даёт строку
> // доказательства с названным носителем. Оба случая живые и оба под сторожем
> // (`flang/test/self-proof.test.mjs`).

### Сверки со свидетелем, снятые построчно

Было у «Имя параметра», «Строка утверждения», «Имена цикла», «Код носителя»,
«Литерал меры», «Знаки арифметики», «Искали», «Общая часть строки»,
«Ведомость по программе», «Ширина имени», «Ключ, которого нет» и «Строка
функции машине»:

> // Имя параметра. `«есть» равно нет` — это `edge.params[position] === undefined`
> // свидетеля: у параметра нет ключа `name`.

> // Полей больше, чем печатается у любой отдельной строки, и это не расточительство:
> // у свидетеля каждый вердикт выписан своим набором ключей, а порядок ключей —
> // поверхность совместимости. Здесь набор один, а КЛЮЧИ ВЫБРАСЫВАЮТСЯ признаками
> // («есть для всех», «есть искали», пустое «по»), и порядок получается один и тот
> // же для всех вердиктов — тот самый, что у свидетеля.

> // Имена цикла упорядочены, потому что упорядочивает их свидетель (`[...].sort()`):
> // порядок членов цикла в отчёте не должен зависеть от порядка объявлений.

> // `flang/stdlib/tree.flang`), у свидетеля — по единицам UTF-16. Расхождение
> // начинается за первым символом вне BMP в имени функции; в корпусе такого имени
> // нет, и это тот же третий долг самоприменения, что у столбца в лексере
> // (`flang/self/SPEC.md`, «Долги»).

> // Код латиницей и не меняется: по нему считают этапы. Пустой код — это `null`
> // свидетеля, то есть «носителя нет».

> // Литерал печатается так, как его напечатал бы `String` свидетеля: `1`, `0.1`,
> // `true`, а отсутствующее значение — словом «ничто».

> // (это проверяет `types.mjs` до всякого отчёта).

> // это `null` свидетеля, то есть «прогона не было вовсе», и путать его с нулём
> // нельзя: ноль означает «прогон был и не дошёл ни на одном».

> // Порядок ключей от этого один и тот же у всех вердиктов — тот самый, что у
> // свидетеля.

> // собирается. Свидетель на JavaScript бросает исключение ровно за этим, потому что
> // там аргумент — объект, у которого поля может не быть.

> // нечего — этой беды у языка нет вовсе, в отличие от свидетеля на JavaScript.

> // Ключ, которого нет. `JSON.stringify` пропускает `undefined`, и отсутствие
> // ключа — это не «ключ со значением ничто»: свидетель различает их, значит и здесь
> // различаются.

> // небрежность, а воспроизведение свидетеля. Там строка тотальной функции
> // собирается из общей части (`name`, `total`, `recursive`, `cycle`) и своей
> // (`carrier`, `says`, …), а строка обычной выписана целиком, и `carrier` в ней
> // стоит третьим.

### Долг: имя хода списано у `flang/proof/reduce.mjs`

Было у «Доказано вычислением»:

> // ИМЯ ХОДА ЗДЕСЬ СПИСАНО, А НЕ ПРОЧИТАНО. Свидетель берёт его из того файла, где
> // ход написан (`ВЫЧИСЛЕНИЕМ` в `flang/proof/reduce.mjs`), — здесь такого имени
> // взять неоткуда: у слоя сведения эталона на flang нет. Две копии одного слова
> // расходятся молча, и долг этот записан здесь, а не умолчан.

Сам долг в исходнике назван по-прежнему, без ссылки на удалённый файл.

### Замер 29 августа 2026 у «В ёлочках»

Было у «В ёлочках»:

> // Замер 29 августа 2026: с «плюс 2» и списком — «объявлено, не доказано», с
> // этой записью — «доказано сведением цели с телом функции». Свернёте обратно —
> // обещание снова поедет проверкой в напечатанный код и купит кадр стека.

Последнее предложение осталось в исходнике.

### История: чему анализ научился и что было первой редакцией

Было у «Строка без носителя»:

> // Мера ОБЪЯВЛЕНА, а спуска анализ не назвал. Случай этот перестал быть
> // «сегодняшним состоянием» — анализ мере научился, — но не перестал быть
> // возможным:

Было у строки законов моноида:

> // ЧТО ИЗМЕНИЛОСЬ У МОНОИДА, и это первое место, где объявленное свойство меняет
> // вердикт о ЧУЖОЙ конструкции.
> //
> // В объявлении моноида коммутативности не было, и это стояло записанным честно:
> // `flang/cat/SPEC.md` называл множество «моноидом, коммутативным», а сказать
> // этого языком было нечем. Теперь есть — отдельным объявлением, — и моноид, чья
> // операция объявлена коммутативной и посчитана чистой, называет это в своей же
> // строке законов. То же у идемпотентности: моноид с идемпотентной операцией есть
> // полурешётка.

Было у шестого слова вердикта:

> // СЧИТАТЬ ЕГО ЗА «ДОКАЗАНО» НЕЛЬЗЯ, и в этом весь смысл слова: пока условные
> // стояли внутри числа доказанных, итог библиотеки был на них больше правды.

Было у раздела утверждений и у раздела разрешений:

> // раздел»: пустой заголовок сообщил бы, что утверждения искали и не нашли, а их
> // не было чем высказать до этой работы, и большинство файлов дерева их не
> // высказывает до сих пор.

> // нашли, а до этой работы их не было чем выдать вовсе.

Было у спусков завершаемости:

> // нет. В ведомости они до сих пор были видны только следом — строкой функции с
> // носителем `measure`; сама мера, узел выражения, оставалась внутри анализа.

---

## `flang/self/proofterm.flang`


Ядро: проверяльщик термов доказательства. Свидетель `flang/src/proofterm.mjs`
(938 строк) и сверка `flang/test/self-proofterm.test.mjs` — обоих файлов в дереве
больше нет.

### Чем был этот файл: эталон свидетеля на 938 строк

Было первой строкой шапки, сразу под «Ядро: проверяльщик термов доказательства
— на самом flang.»:

> Эталон `flang/src/proofterm.mjs` (938 строк).

### Зазор между эталоном и свидетелем — ИЗМЕРЕН, 12 расхождений из 249

Было в шапке файла, три абзаца подряд:

> ── Свидетель ушёл вперёд, пока писался эталон, и зазор ИЗМЕРЕН ───────────────
> Эталон писался под свидетеля своего основания. Пока он писался, работа шла и по
> САМОМУ ядру, и часть её сюда перенесена при сборке: снятие предусловий у мест
> вызова, посылка, закрытая самим сведением, метка и `by` у случая, носитель
> принципа в своде, предусловия функции как допущения — всё это здесь есть, и
> на 237 программах корпуса из 249 вердикт совпадает со свидетелем ПОБАЙТОВО.
>
> НЕ ПЕРЕНЕСЕНО ТРИ КУСКА, и каждый назван числом, а не оговоркой:
>   1. начальная алгебра ВСТРОЕННОГО СПИСКА — 3 программы;
>   2. принцип по ОТРЕЗКУ «неотрицательное» (индукция по числу) — 2 программы и 1 отказ;
>   3. точные десятичные (`сотых`, `тысячных`) в списках типов с дном и
>      потолком вместе с правилом остатка, стоящим на целости, — 1 программа.
>
> К этим трём прибавился ЧЕТВЁРТЫЙ ХОД `свести` — вычисление замкнутой цели
> (4 программы и 1 отказ). Он не правило вывода, и эталон ему написать значит
> дать эталону ВЫЧИСЛИТЕЛЬ языка, а не переписать правило; долг назван там же,
> где ход написан (`flang/proof/SPEC.md`). Своей сверке ядро этот ход не
> передаёт вовсе, поэтому три правила сверены с эталоном целиком.
>
> Итого зазор — ДВЕНАДЦАТЬ расхождений из 249, и все они перечислены ПОИМЁННО в
> `flang/test/self-proofterm.test.mjs`, где сверка сличает их список целиком:
> разойдётся восьмая — проверка краснеет, сойдётся одна из семи — тоже, потому
> что тогда врёт список. Полумеры здесь хуже отсутствия: наполовину
> перенесённый принцип отвечал бы «доказано» там, где свидетель отвечает
> «отвергнуто».

### Аксиом ноль — сверка с пустым списком `АКСИОМЫ` свидетеля

Было заголовком врезки шапки и её первой фразой (осталось: «Слова «аксиома» в
этом файле нет вовсе»):

> ── Аксиом ноль, и перенос не завёл ни одной ────────────────────────────────
> Список `АКСИОМЫ` у свидетеля пуст, и здесь ему нечему соответствовать: …

### Почему вычисление примера не зовётся отсюда — половина про свидетеля

Было в шапке (осталась половина про свойства языка):

> ── Одно место, где дословный перенос НЕВОЗМОЖЕН, и оно названо числом ──────
> ВЫЧИСЛЕНИЕ ПРИМЕРА ПРИЕЗЖАЕТ ДАННЫМИ. Свидетель зовёт интерпретатор прямо
> (`evaluate` из `src/interpret.mjs`) и ловит исключение. …
>
>   1. Исключений в flang нет. Вычислитель на flang (`self/interpret.flang`)
>      отдаёт исход ЗНАЧЕНИЕМ, а текст `ошибка.message` свидетеля — это текст
>      исключения JavaScript, и совпасть они не обязаны ни на одном входе.

### Почему поле ставится только при значении — половина про свидетеля

Было в шапке, в конце врезки «Второе место, и оно про JSON»:

> У свидетеля `span` отказа и `rule` случая пропадают из вывода целиком, когда
> их нет; здесь запись строится списком полей, и поле ставится ТОЛЬКО когда
> значение есть. Это и есть дословный перенос: сверяется JSON, а не объект в
> памяти.

### ДЫРА В СОСТОЯТЕЛЬНОСТИ, найденная прогоном, — «по примеру» под квантором

Было у функции «По примеру при посылке»:

> ДЫРА В СОСТОЯТЕЛЬНОСТИ, НАЙДЕННАЯ ПРОГОНОМ, а не чтением: до этой правки шаг
> `по примеру`, стоящий ПРЯМО под `утверждаем` (то есть вне всякой посылки
> индукции), уходил отсюда сразу в «Прогнать пример» и проверки замкнутости не
> проходил ВОВСЕ — она стояла только на ветви случая («По примеру при
> заключении»). Ядро печатало «доказано: утверждение обо ВСЕХ входах» о
> постусловии, которое `flang run` нарушал на первом же входе; улика —
> `flang/test/fixtures/poddelka-primer-pod-kvantorom.flang`, и сторож при ней
> `flang/scripts/poddelki-yadra.flang`.

Из двух названных путей в дереве остался только первый; `flang/scripts/poddelki-yadra.flang`
в дереве нет.

### Что стояло здесь до седьмого хода — отказ «дождитесь сличения по вызову»

Было первым абзацем врезки «СЕДЬМОЙ ХОД: СЛИЧЕНИЕ ПО ВЫЗОВУ»:

> Эталон `поСвойству` свидетеля (`flang/src/proofterm.mjs`). Раньше здесь стоял
> отказ, и отказ честный: «свести „результат“ шага с результатом другой функции
> ядру пока нечем… дождитесь сличения по вызову». Дождались.

### ИМЯ ТЕРЯЛОСЬ ЗДЕСЬ, И ЭТО ИЗМЕРЕНО — замер ячейки Ч36

Было врезкой перед «Источники решения»:

> ── ИМЯ ТЕРЯЛОСЬ ЗДЕСЬ, И ЭТО ИЗМЕРЕНО ──────────────────────────────────────
> Отбор читал ОДИН вид факта — `property`, — и оттого вердикт называл источник
> только у шага «по свойству». У «по примеру» и «по предположению» имя
> отсекалось прямо тут, до всякой печати: на 45 программах `flang/proof/examples`
> из 108 доказанных утверждений 53 печатались словами «терм принят ядром, N
> шагов» и не называли НИЧЕГО (47 «по примеру», 4 «по свойству», 2 «по
> предположению»; замер ячейки Ч36, двоичный c15f669f, дерево e4529f8e).
> Правило вывода при этом было у всех 53 — терялось только имя.

### Оправдание правки печати источников перед проверяющим

Было там же, абзацем ниже:

> ПРАВИЛО ВЫВОДА ЗДЕСЬ НЕ ТРОНУТО НИ БУКВОЙ, и это главное. Факт, который
> «Свести по свойству» уже строил (он же едет в строку «известно» отказа),
> теперь ДОЕЗЖАЕТ до вердикта. Ни одного доказательства правка не отнимает —
> она их называет точнее.

### Сколько утверждений разом стали бы условными, расширь кто поле `props`

Было там же, в предупреждении «⚠ ПОЛЕ `props` ЭТИМ НЕ РАСШИРЕНО» (осталось без
числа и без слова «правка»):

> …и попади они в `props` — 49 доказанных утверждений разом стали бы условными,
> то есть правка печати отняла бы доказательства.

### Цена промежуточного факта: ноль новых слов, ноль правок в восьми генераторах

Было врезкой «ПРОМЕЖУТОЧНЫЙ ФАКТ», первым абзацем:

> ЧТО ЗДЕСЬ ЗАНЯТО И ПОЧЕМУ ЭТО ДЁШЕВО. Запись `затем <утверждение> по <…>`
> разбирается языком с самого появления слоя доказательств: узел шага уже несёт
> поле `claim`, типы его уже проверяют, до печати оно не доезжает. Отвергало её
> ЯДРО — словами «подменить его написанным нельзя». Значит новых слов ноль,
> новых узлов ноль, правок в лексере, разборе, вычислителе, восьми генераторах
> кода и семи рантаймах — ноль строк. Меняется только то, ЧТО ядро с этой
> записью делает.

### ОШИБКА СВИДЕТЕЛЯ, ПЕРЕНЕСЁННАЯ НАРОЧНО: четыре буквы `null` в тексте отказа

Было у функции «Имя типа в отказе» целиком (в коде остались постусловие и
пример, которые то же самое и требуют):

> У СВИДЕТЕЛЯ ЗДЕСЬ ПОДСТАВЛЯЕТСЯ `null`, И ПОДСТАВЛЯЕТСЯ ОН В ТЕКСТ ОТКАЗА.
> Имя типа берётся `имяИменованного`, а она отдаёт `null` всему, что не
> названо именем, — встроенному списку в том числе. Строчная подстановка
> JavaScript печатает это четырьмя буквами, и до принципа по свёртке место было
> недостижимо: у отрезка имя есть всегда. Эталон отвечает ЗНАК В ЗНАК, поэтому
> те же четыре буквы стоят и здесь; чинить это надо в свидетеле и обоим сразу, а
> не в эталоне молча.

### Ветка `b/proigryvatel`: что нёс вердикт до проигрываемой записи

Было у «Случая вердикта» — имя ветки в заголовке врезки и абзац под ней:

> ── ПРОИГРЫВАЕМАЯ ЗАПИСЬ (ветка `b/proigryvatel`) ─────────────────────────
>
> Прежде вердикт нёс о посылке только ИМЯ правила, и сверщику оставалось
> принять его на слово. Имя правила — это «ядро решило»; ходы — это «вот как,
> проверь сам», и разница между ними и есть весь смысл записи.

### Что было, пока исход счёта ехал признаком

Было во врезке «Отказов у хода ЧЕТЫРЕ, а не один» (осталось переписанным как
свойство нынешнего кода):

> Пока сюда ехал `признак`, три последних были неразличимы: ход отвечал «не
> закрыл», и приписка выходила одна — про свободные имена. Теперь исход едет
> суммой («Исход счёта» в `proof-kernel.flang`), и каждый отказ говорит своё.

### ЗАКЛЮЧЕНИЕ КЛАДЁТСЯ И СЮДА — ячейка Ч60

Было у «Случая сведённого ядром» (осталось без ссылки на ячейку и без «прежде»):

> ── ЗАКЛЮЧЕНИЕ КЛАДЁТСЯ И СЮДА (ячейка Ч60) ─────────────────────────────────
> Поле `conclusion` — ЧТО ИМЕННО ядро свело: цель посылки, в которой `результат`
> уже заменён телом ветви. Прежде оно сюда не клалось, и разница с «Случаем
> вердикта» была не в том, что у сведённой посылки нечего показать, а в том, что
> показанное выбрасывалось: `Запись доказательства` без заключения не может
> выписать ни одного хода, и посылка уезжала одним ИМЕНЕМ правила — то есть «ядро
> решило», а не «вот как, проверь сам».
>
> ЧТО ЭТО НЕ МЕНЯЕТ. Поля `by` и `steps` остаются прежними: `reduction` и ноль.
> Ведомость по-прежнему отличает написанное автором от закрытого ядром — а
> именно это, и только это, обещал прежний запрет. …

### ЗДЕСЬ БЫЛА НАСТОЯЩАЯ ОШИБКА — ключ повтора был печатью типа

Было у функции «Дописать инстанцию вызванного»:

> ЗДЕСЬ БЫЛА НАСТОЯЩАЯ ОШИБКА, И ЭТО НЕ МЕЛОЧЬ СТИЛЯ. Ключом стояло «Как
> написан» — печать ТИПА, — а инстанция есть ВЫРАЖЕНИЕ, и на всяком выражении
> эта печать отвечает одной и той же строкой. Значит ключ у всех фактов совпадал,
> и правило оставляло РОВНО ОДИН факт на всё заключение, первый по обходу. У
> функции, которая зовёт одну и ту же вызванную в двух ветвях с разными
> аргументами, второй факт пропадал молча, и доказательство не складывалось по
> причине, о которой отказ не говорил ни слова. Свидетель правлен тем же ходом
> и в тот же день.

### Путь без теоремы не читал предусловий функции — «не граница, а пропуск»

Было у функции «Свести цель с телом»:

> Соседние пути их уже читают («По предположению», «По свойству»); путь
> без теоремы не читал, и это была не граница, а пропуск: тот же факт, тот же
> источник, а ответ разный.

### Третья формулировка вердикта стояла в комментарии, а в коде её не было

Было у «Слов по объявлению», последним абзацем:

> А ТРЕТЬЯ ФОРМУЛИРОВКА — про вычисление — стояла в комментарии выше, а в коде
> её не было: закрытая ВЫЧИСЛЕНИЕМ цель печаталась «сведением цели с телом
> функции: правило «вычисление замкнутой цели»». Ровно то, чего комментарий и
> запрещал: вычисление названо правилом вывода, а оно им не является.

### Зачем завёлся путь без теоремы

Было врезкой «Зачем он завёлся» (осталось переписанным без «было — стало»):

> Принцип индукции у ядра был и был доказан, но звали его ТОЛЬКО из написанной
> рукой теоремы. … Мест, где утверждение верно, доказательство есть, а
> вписать его некуда, оказалось измеримое число. Этот путь закрывает их — БЕЗ
> единого нового слова в языке.

### Даты, когда приехали вторая и третья формы тела: 22 и 23 августа 2026

Было в шапке пути без теоремы:

> ФОРМ ТЕЛА ТРИ: `разбор`, `свёртка` и `если`. Вторая приехала сюда 22 августа
> 2026 вместе с усилением принципа свёртки — тем, где `P` говорит и о
> пройденном куске списка; третья — 23 августа, вместе с принципом по отрезку
> «неотрицательное», который у ядра был, но звался только из написанной рукой теоремы.

### Замеры, которыми названо «зачем это здесь»: свёртка и тело-`если` в `flang/stdlib`

Было врезками «ВТОРАЯ ФОРМА ТЕЛА» и «ТРЕТЬЯ ФОРМА ТЕЛА»:

> Зачем это здесь, названо числом. Принцип по свёртке усилен (`P` говорит и о
> пройденном куске списка, довод — в шапке «ПРИНЦИПА ПО СВЁРТКЕ» в
> `proof-initial.flang`), но звали его ТОЛЬКО из написанной рукой теоремы. Тел,
> написанных свёрткой прямо по параметру, в `flang/stdlib` больше сорока, и ни у
> одного теоремы нет: усиление до них не доставало ничем.

> Зачем это здесь, названо числом. Тел, написанных `если` на верхнем уровне, в
> `flang/stdlib` сорок (`docs/body-shapes.md`), и ни одному из них принцип
> индукции не доставал ничем: путь без теоремы читал ровно две формы тела —
> `разбор` и `свёртка`, — а всё прочее уходило в «вердикта нет». … но звали его
> ТОЛЬКО из написанной рукой теоремы.

### Пропуск в диагностике снятия предусловий, увиденный только подделкой

Было у «Проверить доказательства», в конце абзаца о порядке диагностики:

> Так же и у близнеца
> (`src/proofterm.mjs`: `diagnostics.push(...предусловия.diagnostics)`).
> Пропуск этот молчал: в корпусе все места `требует` снимаются, и увидела его
> только подделка «неоплаченное требует» (`test/self-proof-po-vyzovu.test.mjs`).

### Граница слоя снятия предусловий, ИЗМЕРЕННАЯ прогоном по дереву

Было врезкой перед объектом «Требование» (осталась только половина про
одноступенчатость):

> ── Где проходит граница этого слоя и почему она названа числом ─────────────
> У свидетеля снятие двухступенчатое: сначала сведение, а что не свелось и
> оказалось ЗАМКНУТЫМ — вычисляется. …
>
> Зазор от этого ИЗМЕРЕН прогоном по дереву, а не оценён: предусловия стоят в
> ТРЁХ программах корпуса, мест вызова в них ДЕСЯТЬ, и все десять снимаются
> сведением — вычислением ноль. То есть на сегодняшнем дереве вторая ступень не
> срабатывает ни разу, и эталон совпадает со свидетелем побайтово.
>
> Появись программа, где сведение не справилось, а замкнутая инстанция
> вычисляется в «да», — эталон скажет «не снято» там, где свидетель скажет
> «снято», и побайтовая сверка станет КРАСНОЙ в тот же прогон. Это и есть
> сторож границы: она не молчит, когда её переходят.

### Мелкие сверки со свидетелем, снятые с абзацев-объяснений

Абзац в каждом случае остался и объясняет то же самое; ушла только половина,
ссылающаяся на удалённый JavaScript.

| было в исходнике | где стояло |
|---|---|
| «итоги: то, что у свидетеля объекты» | заголовок раздела |
| «У свидетеля это объект с необязательными полями; здесь тип с признаками» | объект «Итог шага» |
| «У свидетеля это замыкание `контекст`; здесь оно записано типом» | объект «Обстановка терма» |
| «свидетель складывает их в словарь, и второе объявление затирает первое» | «Функция терма по имени» |
| «у свидетеля `span` отсутствующего места пропадает из JSON целиком» | «Отказ терма» |
| «`JSON.stringify(значение) ?? "ничто"` свидетеля… первое даёт `undefined`, и оператор `??` подставляет слово» | «Значение примера словами» |
| «(`interpret.mjs`, `stepPost`)» | врезка «Откуда берётся факт», и она же у восьмого хода |
| «Эталон `постусловияВызванных` свидетеля (`flang/src/proofterm.mjs`)» | врезка «ВОСЬМОЙ ХОД» |
| «и ядро не читало постусловие вызванного ни на одном месте» | там же |
| «„Положить замену“ стоит здесь тем же, чем у свидетеля стоит объектный литерал» | «Замены аргументов вызова» |
| «Порядок повторён за свидетелем» | «Вызовы имени» |
| «Порядок обхода тот же, что у свидетеля» | «Вызовы в узле предусловий» |
| «как его печатает `JSON.stringify` у свидетеля» | «Литерал случая словами» |
| «У свидетеля (`src/proofterm.mjs`, `вычислениемЗамкнутой`) ход отказывает…» | врезка «Отказов у хода ЧЕТЫРЕ» |
| «и порядок этот у свидетеля закреплён» | «Дописать по замкнутости» |
| «слово в слово за свидетелем… соврать о том, чего свидетель не делал» | «Дописать об исходе» |
| «у свидетеля отказы ходов ДОПИСЫВАЮТСЯ… через `[итог.why, ...хвост].join(". ")`» | «Сведение с припиской» и «Причина со свободными» |
| «у свидетеля здесь стоит СЧЁТ, а не молчание» | «Дописать о свободных» |
| «ответ сличается с близнецом ПОБАЙТОВО» | «Проверить доказательства», поле `preconditions` |
| «собирает те же три поля, что и раньше» | объект «Снятие предусловий» |

---

## `flang/self/setoid-oracle.flang`


Оракул законов категории. Свидетель на JavaScript удалён из дерева; ниже —
сверки с ним, снятые с абзацев-объяснений. Абзац в каждом случае остался и
объясняет то же самое.

| было в исходнике | где стояло |
|---|---|
| «Предел витков и предел глубины стоят на ней снаружи, **как у свидетеля**» | врезка «Почему функции, зовущие вычислитель, не обещают тотальности» |
| «У свидетеля предел записан подъёмом (`глубина > 64` от нуля), здесь — спуском, и 65 = 64 + 1» | «Запас разворота» |
| «ровно то, что делает `Map.set` свидетеля в цикле по `morphisms`» | отображение «имя → узел» |
| «у свидетеля ключом стал бы `undefined`» | стрелка без имени |
| «`узел.identity === true` свидетеля» | узнавание единицы |
| «свидетель отвергает оба одним условием» | имя функции стрелки |
| «Замыкание считается ПОВТОРАМИ, **как у свидетеля**» | концы стрелок |
| «`сетки.set(объект, [])` свидетеля» | пустая сетка объекта |
| «свидетель сравнивает `valuesEqual`, у которого запись с теми же ключами в другом порядке — та же запись» | отсев повтора по значению |
| «Порядок внутри одного примера тот же, что у свидетеля» | значения примеров стрелки |
| ««Ничего» — это `{ошибка: true}` свидетеля» | исход применения |
| «Порядок ветвей тот же, что у свидетеля» | применение стрелки |
| «Композиция считается справа налево, **как у свидетеля**» | применение композиции |
| «у свидетеля `добавить` отвергает `undefined` первым же условием» | объект без имени |
| «так же, как `[...(известно.get(…))]` у свидетеля» | снимок значений домена |
| «ровно то, что делает `new Map(пары)` свидетеля» | отображение «имя → имя» |
| «свидетель зовёт `применить` до проверки на `имяРавенства === undefined`… не досчитать вызовов, которые сделал свидетель» | квадрат естественности |

---

## `flang/self/setoid.flang` и `flang/self/bounded.flang`


Тронуто НЕ БЫЛО. У `setoid.flang` весь остаток сита — слово «прогон» в
предметном смысле («прогон программы») и объяснение границы файла;
у `bounded.flang` — указание на задачу 1734, поставленное ячейкой Ч334 взамен
вынесенного долга.

## `flang/self/sets-oracle.flang`


Оракул законов множеств.

### Почему формат ответа у этого оракула не переписывали

Было в шапке файла, врезка «Чем этот оракул отличается от четырёх соседних»,
последней фразой абзаца:

> … и ждёт их обратно записями «функция, вход, вышло, значение, беда». Так он был
> написан до появления вычислителя на flang, и переписывать его формат ради
> единообразия значило бы менять сверенный слой ради удобства оракула.

### Кто зовёт слой на flang через границу

Было в шапке файла, там же, следующим абзацем:

> … Через границу едет ЗНАЧЕНИЕ, а не тип: слой на flang зовут из JavaScript, и
> запись с теми же полями закон читает как свою.

Осталось: «Через границу едет ЗНАЧЕНИЕ, а не тип: запись с теми же полями закон
читает как свою.»

### Врезка «СВОИ ПРИМЕРЫ»: замер покрытия файла собственными примерами

Было в шапке файла целой врезкой (убрана вместе с заголовком):

> ── СВОИ ПРИМЕРЫ: восемь функций из тринадцати, и пять названы поимённо ─────
>
> До 30 августа 2026 своих примеров здесь не было НИ ОДНОГО — `grep -c
> '^\s*пример '` давал ноль, — а `flang check` при этом отвечал «проверено —
> разбор, типы, завершаемость, ядро и примеры; замечаний нет». Все примеры,
> которые на этом файле гонялись, приезжали по `использует` от одиннадцати
> файлов замыкания (17 531 строка). Зелёное держалось целиком на чужом:
> подменить тело любой здешней функции заглушкой можно было, ничего не
> покрасив. Про эту болезнь есть отдельная заметка —
> `docs/zettel/flang-test-is-always-green-on-a-layer-with-no-examples-of-its-own.md`.
>
> Ведомость СЕГОДНЯ, замер 30 августа 2026 двоичным `82f9ac40`:
>
>     bootstrap/flang test flang/self/sets-oracle.flang --ledger
>     flang/self/sets-oracle.flang    410 410 0 12
>
> Четвёртое число — СВОИХ примеров, и оно здесь главное: было 0, стало 12.
> Остальные 398 по-прежнему приезжают по `использует`, и это нормально —
> ненормально было, когда своих не было вовсе.
>
> Теперь у восьми функций стоит двенадцать своих примеров. Взяты те ветки,
> которые вычислитель НЕ зовут: пустая программа, вложение без `даёт`, разбор
> трёх исходов вызова. Этого хватает, чтобы зелёное значило хоть что-то.
>
> ПЯТЬ ФУНКЦИЙ ПРИМЕРА НЕ ПОЛУЧАТ, и причина одна на всех, устройство языка:
> `«Программа хода»`, `«Спросить о значении»`, `«Записать ответ»`,
> `«Ответы вложения»`, `«Ответы всех вложений»` принимают `«Ход множеств»`, а
> он несёт `«Ход оракула»` с полем `«готовая»: «Готовая программа»`. Это
> `вариант «Готовая» содержит «п»: «Программа»` (`self/interpret.flang:3424`),
> и внутри `«Программа»` лежит `«словарь описаний»: «Узел хеша» от «Может быть
> описание»` — дерево, которое строит ВЫЗОВ `«Подготовить»`. А `дано` в
> примере принимает только литеральное значение: вызов функции там — отказ
> разбора, и это не догадка, а измеренная яма (задача 1853, «дано ждёт
> литеральное значение, а не вызов функции»). Написать им пример значило бы
> выписать хеш-дерево руками, то есть подделать вход.
>
> Отсюда правило для того, кто будет править этот файл: тело функции из этих
> пяти не покрыто ничем, кроме своего `обеспечивает`. Менять их — только
> вместе с прогоном `flang check`, и результат называть числом.

### Долг: отношение двух пределов не стережёт никто

Было у функции «Сетка входа оракула», последним абзацем врезки над ней:

> ⚠ ОТНОШЕНИЕ ДВУХ ЧИСЕЛ НЕ СТЕРЕЖЁТ НИКТО. Здесь стояло «стережёт проверка в
> `flang/test/self-setsmonad.test.mjs`» — этот файл удалён 20 августа 2026
> коммитом `105943cd` вместе с реализацией на JavaScript, которую он сверял.
> Сдвинь сегодня любое из двух чисел — и не покраснеет ничего. Держит связь
> только пример `«Шире»` у `«Оракул шире сетки на четыре»` в
> `self/oracle.flang`, и он про пределы ОРАКУЛА и ЗАМЫКАНИЯ, а не про этот.

### Чем свидетель сравнивал значения

Было у функции «Значения примера множеств»:

> Повтор отсеивается ПО ЗНАЧЕНИЮ, а не по печати: свидетель сравнивает
> `valuesEqual`, у которого запись с теми же ключами в другом порядке — та же
> запись, а печать у них разная.

Осталось: «Повтор отсеивается ПО ЗНАЧЕНИЮ, а не по печати: запись с теми же
ключами в другом порядке — та же запись, а печать у них разная.»

### Чем значение при отказе было у свидетеля

Было у функции «Ответом множеств»:

> … Значение при отказе — `ничто`, ровно как `значение(null)` у свидетеля.

Осталось: «Значение при отказе — `ничто`.»

## `flang/self/sets.flang`


Вложение и общая часть на самом flang. Слово «свидетель» здесь ПРЕДМЕТНОЕ —
значение, подтверждающее непустоту общей части; вынесены только те места, где
им назван свидетель на JavaScript.

### Откуда файл списан и как звались его две точки входа у свидетеля

Было в шапке файла:

> Копия `flang/src/sets.mjs`, контракт — `flang/cat/SETS.md`. Слой отвечает
> сразу за ОБЕ полки границы честности …
>
>   «Проверить устройство множеств» ← checkSetShapes — ДОКАЗЫВАЕТСЯ сличением
>        объявлений, говорит обо ВСЕХ входах, стоит рядом с проверкой типов;
>   «Проверить законы множеств»     ← checkSetLaws  — ПРОВЕРЯЕТСЯ на конечной
>        сетке значений автора, говорит о КОНЕЧНОМ их числе.

### Предел сетки: то же число, что у свидетеля

Было у функции «Предел сетки множеств»:

> Больше этого числа значений в сетку не берём — то же число и та же причина,
> что в свидетеле: перебор пар квадратичен …

### Равенство значений: чем оно было у свидетеля

Было в разделе «равенство значений»:

> `valuesEqual` свидетеля над представлением «Значение».
>
> Скаляры сличаются `равен` — а он в языке И ЕСТЬ `Object.is` свидетеля
> (`interpret.mjs`: случай `eq` зовёт тот же самый `valuesEqual`), поэтому
> переносить тут нечего. Списки сличаются поэлементно, ВМЕСТЕ С ПОРЯДКОМ;
> записи — как множества полей: у свидетеля это `recordsEqual`, где сравниваются
> длины и вхождение каждого ключа, а не порядок.
>
> Вариант в этом представлении — запись с полями `variant` и `fields`, поэтому
> отдельной ветви у него нет: сличение двух таких записей поле в поле даёт
> ровно `left.variant === right.variant && recordsEqual(left.fields,
> right.fields)`. Цена одного представления названа в самом свидетеле
> (`builtins.mjs`, `encodedVariant`): запись программы ровно с двумя такими
> полями от варианта неотличима, и это цена, а не оплошность.

Было у функции «Равны поля»:

> `leftKeys.every(key => key in right && valuesEqual(…))` свидетеля: порядок полей
> не важен, важно вхождение каждого ключа с равным значением.

### Хозяйство: печать значения, ключ имени, место узла, предел, ответы

Было у функций «Показать значение», «Ключ имени», «Место устройства», «Первые во
множествах», «Ответ», «Позвать во множествах», «Вложено в»:

> `JSON.stringify(значение)` свидетеля, укороченный до сорока знаков: …
> Порог и многоточие — те же, что в свидетеле.

> Ключ имени для сличения «как ===»: отсутствующий ключ и ключ со значением
> `ничто` — разные вещи, и свидетель их различает (`Map.has(undefined)` против
> `Map.has(null)`).

> `spanOf` свидетеля: место берётся у узла, если оно там есть и не пустое, иначе
> `null`. Ключ `span` при этом стоит ВСЕГДА — так его печатает `ctx.report`.

> `slice(0, N)` свидетеля. Предел режет уже собранное, а не собираемое: свидетель
> отсекает хвост после того, как повторы вычеркнуты.

> отказ
> функции на значении сетки — не нарушение закона, а другое сообщение, и свидетель
> говорит именно это.

> Зов функции по имени на одном значении — то же, что `зов` свидетеля.
>
> Незнакомое имя отвечает отказом СРАЗУ и в спрос не записывается: свидетель и не
> зовёт вычислитель, он смотрит в список функций программы.

> Часть, вложенная в объемлющее: пара ключей «как ===».

### Имена типов: как это устроено у свидетеля

Было у функций «Имя вида типа» и «Сверить конец стрелки»:

> Ровно так устроен `имяТипа` свидетеля: он смотрит `тип.name`, а не `тип.kind`.

> Безымянный тип (число,
> строка, список) не спорит ни с чем — свидетель в этом месте проверяет `!== null`.

### Порядок сторон пары, ранний выход, итог и порядок ключей

Было у функций «Та же пара», «Проверить устройство множеств», «Итог законов»,
«Свод в значение»:

> Свидетель
> добивается этого сортировкой перед склейкой ключа; здесь та же мысль сказана
> сличением пары как пары, и порядка строк для этого не нужно вовсе.

> Программа без объявленных отношений даёт ПУСТОЙ список и не смотрит на
> объявления вовсе — ровно как свидетель, который возвращается первой же строкой.

> Итог целиком: то, что сверяется со свидетелем, и то, чего не хватило для счёта.

> Порядок ключей — порядок свидетеля: `diagnostics`, `checked`, `assumed`.

---

## `flang/self/svoystva.flang`


Объявленные свойства — общая основа пяти законов.

### Шапка называла файл копией свидетеля

Было в шапке файла:

> // Объявленные свойства — общая основа пяти законов. Копия `flang/src/svoystva.mjs`.

### Ссылка на `types.mjs` в разделе «Что ПРОВЕРЯЕТСЯ, а не доказывается»

Было в шапке файла:

> // Устройство свойства (функция объявлена, входов
> // столько, сколько нужно, типы сходятся с носителем) доказано раньше и в другом
> // месте — `types.mjs`, сличением объявлений, без единого вычисления.

### `reifyValue` свидетеля в разделе «Об овеществлении»

Было в шапке файла:

> // ── Об овеществлении ───────────────────────────────────────────────────────
> // Свидетель прогоняет каждое значение сетки через `reifyValue`, и здесь этого шага
> // нет — по тому же доводу, что записан в шапке `iso.flang`: на представлении
> // «Значение» (`core/json.flang`) шаг тождественен, а написанное тождество
> // читалось бы как перевод, которого не происходит.

Довод (шаг тождественен на представлении «Значение») в файле оставлен.

### «Перенесённая ошибка свидетеля» у примера про `list:null`

Было у функции «Ключ типа»:

> // Список ЛЮБОГО элемента даёт `list:null`, и это перенесённая ошибка свидетеля,
> // а не описка: довод — ниже, у «Ключ вида».

### ДОЛГ, НАЗВАННЫЙ КОММЕНТАРИЕМ: ошибка свидетеля в ключе списка, перенесённая и не исправленная

Было у функции «Ключ вида» — весь блок:

> // ЗДЕСЬ У СВИДЕТЕЛЯ ОШИБКА, И ОНА ПЕРЕНЕСЕНА, А НЕ ИСПРАВЛЕНА.
> //
> // `svoystva.mjs` читает у списка поля `item` и `element` (`ключТипа(тип.item ??
> // тип.element)`), а разборщик пишет `of` — `{ kind: "list", of: … }`
> // (`flang/src/parser.mjs`, «разбор выражения типа»). Ни одного узла с `item`
> // или `element` в дереве нет ни одного, значит ключ списка у свидетеля ВСЕГДА
> // `list:null`: `список числа` и `список строки` для него один и тот же порядок.
> //
> // Эталон обязан отвечать то же самое. Починить это здесь значило бы разойтись
> // с свидетелем на первой же функции со списком на конце — и разойтись МОЛЧА, в
> // пользу более правильного ответа, что и есть худший вид расхождения. Ошибка
> // названа, чинит её тот, кто владеет свидетелем.
> //
> // Отсюда же и тотальность: рекурсии по типу нет, потому что спускаться некуда.

Это ЖИВОЙ ДОЛГ, а не история: `«Ключ вида»` и его `обеспечивает «список даёт
list:null»` по-прежнему отдают один ключ на `список числа` и `список строки`, и
монотонность по этому ключу сличает концы функции. Свидетеля, ради сверки с
которым это оставлено, в дереве больше нет — значит довод «разойтись молча»
отпал, и решение чинить или не чинить надо принимать заново. В файле оставлено
только само поведение (у списка ключ всегда `list:null`) и вывод о тотальности.

### Ветка `typeof тип !== "object"` свидетеля у «Ключ скаляра»

Было у функции «Ключ скаляра»:

> // Ветка `typeof тип !== "object"` свидетеля. Узел типа в разобранной программе —
> // всегда запись либо `ничто`, поэтому на программах дерева сюда не приходит ни
> // один тип; ветка написана ради полноты перевода, а не ради случая. Строка
> // переводится собой, число — печатью: ровно это сделал бы `String`.

### «Так же, как у свидетеля» у порядка предела и отсева повторов

Было у функции «Сетка значений свойства»:

> // Предел стоит на всю сетку и снимается ПОСЛЕ отсева повторов — так же, как у
> // свидетеля: там проверка на повтор стоит перед проверкой предела, и повтор,
> // пришедший после упора в потолок, всё равно ничего не добавляет.

### Свидетель у отсева пустого имени

Было у функции «Собрать по имени свойства»:

> // Пустое имя пропускается: у дистрибутивности второй операции может не быть
> // вовсе, и свидетель отсеивает такое имя до обхода примеров.

### Два разных сравнения свидетеля (`valuesEqual` и `JSON.stringify`) в сводной сетке

Было у функции «Сетка сводная»:

> // ПОВТОР ЗДЕСЬ СЧИТАЕТСЯ ПО ПЕЧАТИ, А НЕ ПО РАВЕНСТВУ ЗНАЧЕНИЙ, и это не
> // небрежность переноса. Внутри одной сетки свидетель отсеивает повторы `valuesEqual`
> // — записи с теми же ключами в другом порядке для него равны; при СЛИЯНИИ двух
> // сеток он сравнивает `JSON.stringify`, а там порядок ключей значим. Две записи,
> // равные по значению и разные по печати, у свидетеля попадут в сводную сетку обе, и
> // сводная сетка станет длиннее — то самое число, которое уезжает в ведомость.
> // Свести это к одному правилу значило бы починить свидетель молча.

Само правило (внутри сетки — по равенству значений, при слиянии — по печати) в
файле оставлено; убраны сверка со свидетелем и «починить свидетель молча».

### `узел.operation ?? null` свидетеля у поля «есть предмет»

Было у объекта «Допущенное свойство»:

> // «есть предмет» отличает названный предмет от НЕНАЗВАННОГО: свидетель пишет
> // `узел.operation ?? null`, и `null` там не то же самое, что пустая строка.

### `null` свидетеля у пропуска на негодном устройстве

Было у функции «Допустить по узлу»:

> // Пропуск на негодном устройстве: имени могло не быть вовсе, и отличать это от
> // пустой строки обязательно — свидетель пишет тогда `null`.

### `звать(имя, а)` и `звать(имя, а, б)` свидетеля

Было у функции «Позвать одного»:

> // Вызов одного аргумента и вызов двух — то же самое, что `звать(имя, а)` и
> // `звать(имя, а, б)` у свидетеля: имена параметров подставляются по порядку, и
> // недостающий аргумент приезжает пустым местом.

### Свидетель у третьего случая «Ответа отношения»

Было у типа «Ответ отношения»:

> // функция, объявленная отношением, но вернувшая число, у свидетеля
> // обрывает закон особым текстом, а не считается ложью.

## `flang/self/tags.flang`


### Копия `flang/src/tags.mjs` и «слой без эталона»

Было в шапке файла:

> // Теги программы — конечный список случаев диспетчера. Копия `flang/src/tags.mjs`.

> // Здесь тот же довод повторяется этажом выше: у свидетеля одно определение на
> // четыре слоя, и у эталона обязано быть одно на четыре. Слой без эталона —
> // это место, где вердикт языка о самом себе всё ещё держится на JavaScript.

> // Ровно то же, что у свидетеля, и ни на шаг больше: какие узлы AST порождают тег,

### Прогон, которым восемь обещаний проверены на пустоту

Было в шапке файла, врезка «Обещания»:

> // Про этот слой было доказано ровно одно: что он завершается. Что он СЧИТАЕТ,
> // не говорилось. Восемь обещаний ниже ядро сводит к телу функции, а не к её
> // подписи, и каждое проверено на пустоту прогоном: тело заменялось заглушкой
> // той же подписи (строка → "", список → пустой список, признак → нет), и
> // обещание обязано было перестать доказываться. Умерли все восемь.

### Сверки со свидетелем, снятые построчно

Было у «Может быть параметры», «Имена параметров», «Имена полей тега», «Тег по
имени», «Тег по значению», «Тег уже собран» и «Отметить узел»:

> // `paramsOf` свидетеля: имена параметров объявленной функции — или `null`.

> // приводит к одному виду `normalizeParams` свидетеля.

> // список — свидетель в этом месте пишет `fields === null || … ? [] : Object.keys`.

> // Имя не строкой — тега нет: свидетель отвечает на это `typeof name !== "string"`
> // первой же строкой,

> // Свидетель перебирает места, где в имени варианта встречается « с », и берёт то,
> // у которого сошлось КАНОНИЧЕСКОЕ имя целиком.

> // Значит множества совпадают. Порядок свидетеля — по ВОЗРАСТАНИЮ места склейки, а
> // место склейки — это длина префикса, то есть длина имени; отсюда «кратчайший».
> // Прямое совпадение проверяется первым, как и у свидетеля.

> // функциям тотален и, в отличие от свидетеля, не зависит от того, накладываются ли
> // вхождения « с » друг на друга

> // Повтор — это совпадение КЛЮЧА, а не имени: у свидетеля теги лежат в отображении
> // по ключу, и второе вхождение с тем же ключом кладётся на место первого, не
> // сдвигая его в порядке.

> // Отсутствие ключа и ключ со значением «ничто» здесь РАЗНОЕ: свидетель спрашивает
> // `node.kind === undefined`, а не «пусто ли».

---

## `flang/self/totality.flang`


Анализ завершаемости. Пятый слой самоприменения.

### Критерий верности был дифференциальным — против свидетеля на JavaScript

> ── Критерий верности ──
> Он дифференциальный и бинарный: для каждой программы вердикт обязан совпасть
> с вердиктом свидетеля `flang/src/totality.mjs` — то же множество доказанных
> функций и те же диагностики `FLANG_NOT_TOTAL`, код в код, текст в текст,
> место в место. Разойтись можно ровно в две стороны, и обе плохи: признать
> незавершающееся или отвергнуть доказуемое. Проверка —
> `flang/test/self-totality.test.mjs`.

Ни `flang/src/totality.mjs`, ни `flang/test/self-totality.test.mjs` в дереве
больше нет.

### Расхождение двух печатей одного компилятора, найденное отметкой мер

> Отметку кладёт этот слой («Отметить меры»), и кладёт её в те же места, что
> свидетель, — сверяется побайтово (`self-totality.test.mjs`). Зовёт её печать:
> `self/bootstrap/compiler.flang`, «Напечатать связанное», — то же место, где
> зовёт свидетель (`bin/flang.mjs`, `markMeasure`). Пока не звала, две печати
> одного компилятора расходились ровно здесь: у свидетеля сторож есть, у
> напечатанного самоприменением его не было, и на таких входах его код
> зацикливался там, где свидетельский отказывает.

### Почему Тарьян, а не замыкание достижимости, и откуда взялся порядок

> Компоненты связности. Свидетель берёт замыкание достижимости: для десятков
> функций это очевидно верный код. Здесь взят готовый Тарьян из соседнего слоя
> (`self/emit-c.flang`, «Компоненты связности») — не ради красоты, а потому что
> замыкание достижимости на списках стоило бы куба от числа функций, а в
> `flang/core/parser.flang` их больше двухсот. Разбиение у обоих алгоритмов
> одно и то же (компоненты сильной связности единственны), но порядок разный, а
> порядок виден в выводе: диагностики идут покомпонентно, и имена цикла
> печатаются в сообщении. Поэтому результат Тарьяна приводится к порядку
> свидетеля — компоненты по первому по объявлению члену, члены внутри компоненты
> по объявлению («Упорядоченные компоненты»).

### Замер, ради которого «Номер годится» написан встроенной формой

> ЗАЧЕМ ОН ЕСТЬ. Три отбора ниже брали элемент по номеру СВЁРТКОЙ по всему
> списку: цена — длина списка при ЛЮБОМ номере, даже при нулевом. Замер
> витками интерпретатора для «Имени по номеру» на нулевом индексе: 15 521 виток
> при 500 параметрах и 124 021 при 4000, показатель роста 0,999 — линия.
> Со встроенной формой — 43 витка на обеих длинах, показатель 0,000.

И оттуда же — история договора:

> ПОЧЕМУ НЕ ПРОСТО ФОРМА. Договор у неё другой: на дробном номере и на номере
> вне списка она ОТКАЗЫВАЕТ, а свёртка на таком номере просто не срабатывала и
> отдавала заготовку. […] заготовку получают ровно те же номера, что получали
> её раньше.

### Замер, из-за которого три прохода по описаниям сведены в один

> Раньше здесь стояли отдельные проходы по списку описаний: за арностью и за
> точными позициями, а с приходом объявленной меры понадобился третий — за
> именами параметров. Проход этот линеен по числу функций, а зовётся он на
> КАЖДОЕ ребро графа, то есть цена у него квадратичная, и утроить её значило
> заплатить втрое за квадрат. На связанном `self/types.flang` (400 функций,
> тысячи рёбер) третий проход выбил анализ за сорок миллионов шагов. Здесь
> поиск один, и всё, что нужно ребру, берётся из найденного описания.

### Замер 29 августа 2026 о записи цели у «В ёлочках при анализе»

> Замер 29 августа 2026: с «плюс 2» и списком — «объявлено, не доказано», с
> этой записью — «доказано сведением цели с телом функции».

(Тот же замер записан и в `flang/self/failures.flang` — там он вынесен ячейкой
Ч334.)

### Сверка имён, краснеющая на «мера убывает» без уточнения

> Сверка имён (`flang/test/emit-names.test.mjs`) на этом краснеет, и краснеет
> по делу: две разные функции под одним именем в C — это молчаливая подмена.

Файла `flang/test/emit-names.test.mjs` в дереве нет.

### Двадцать с лишним построчных сверок со свидетелем

Комментарии-сверки вида «`Object.values(x ?? {})` свидетеля», «`depth === 0 &&
step === 0` свидетеля», «`params.indexOf(имя)` свидетеля», «`{ ...узел, ключ:
значение }` свидетеля», «`String(undefined)` даёт „undefined“» переписаны на
описание поведения без ссылки на удалённый JavaScript. Смысл сохранён знак в
знак; сверять теперь не с чем.

### Где стояли контрпримеры

Дважды в файле стояло «контрпример — в шапке `src/totality.mjs`» — про то,
почему требование ЕДИНОЙ убывающей позиции в компоненте необходимо, а «каждое
ребро убывает хоть где-нибудь» неверно. Самого контрпримера в дереве нет.

## `flang/self/types.flang`


### Критерий верности — дифференциальная сверка с удалённым свидетелем

Было в шапке файла:

> // ── Критерий верности ──────────────────────────────────────────────────────
> // Он дифференциальный: для каждой программы репозитория множество диагностик,
> // выданное этим слоем, обязано совпасть с тем, что даёт свидетельский
> // `flang/src/types.mjs` — код, текст сообщения и место, в том же порядке. Не
> // «ловит те же ошибки»: одинаковый набор кодов при разном тексте — уже
> // расхождение, потому что текст диагностики и есть то, что читает человек.
> // Проверка — `flang/test/self-types.test.mjs`.

Осталось в исходнике без ссылки на свидетеля: диагностика сверяется вся
целиком — код, текст сообщения и место, в том же порядке.

### Три решения шапки: чем они объяснялись через устройство свидетеля

Было в шапке файла:

> //    уже написана и проверена. Своего типизированного AST здесь нет по той же
> //    причине, что и там: свидетель работает с голым объектом и различает «ключа
> //    нет» и «ключ со значением ничто».

> // 2. Состояния в языке нет, поэтому `ctx` свидетеля разложен надвое. Таблицы

> //    обход повторяет свидетель буквально: два прохода по объявлениям типов, затем
> //    сигнатуры, затем тела функций в порядке объявления, и внутри тела —
> //    порядок, в котором `inferExpr` спускается по узлам.

### Эталонные имена полей `src/types.mjs` у именных границ

Было у «Верх, выраженный ИМЕНЕМ»:

> // «подДлиной» и «неДлиннее» — имена тех списков и строк, чьей длины это число
> // заведомо меньше (строго) и не больше (нестрого). Эталон
> // `src/types.mjs`, поля с теми же именами; довод там же.

Было у сбора именного верха, у отношения двух имён, у поля «не минус ноль», у
четвёртого вида границы, у читателя границы и у читателя границ подстроки:

> // Эталон `src/types.mjs`: `сПодДлиной`, `собратьПодДлиной`. Читается ровно
> // одна форма и обе её записи — одно утверждение:

> // Эталон `src/types.mjs`: `неБольше`, `собратьПорядок`. Третье поле именной

> // Эталон `src/types.mjs`: `неМинусНоль`, `сНеМинусНолём` и `собратьНеНоль`.

> // Эталон `src/types.mjs`: `собратьНеНоль` и четвёртый проход `сузить`.

> // Эталон `src/types.mjs`, `вГраницах`. Условие ОДНО НА ОБЕ БАЗЫ там, где база

> // Эталон `src/types.mjs`, `вГраницахПодстроки`. Рантайм считает `начало − база`

> // Эталон `src/types.mjs`, `собратьЦелость` и первый проход `сузить`. Читается

> // Эталон `src/types.mjs`, `связать`. Знание «н меньше длины эл» записано

> // ДЛИНА ПОМНИТ СВОЙ КОНТЕЙНЕР — эталон `src/types.mjs`, случай «длина».

> // Место частичной формы `элемент`: доказано границами номера или нет. Эталон
> // `src/types.mjs`, случай "элемент" — там же и довод про обе базы.

> // База номера — СВОЙСТВО ПРОГРАММЫ, а не ключ запуска (эталон
> // `src/types.mjs`, `базаНомераПрограммы`).

### Граница на арифметике: «то же свойство, что у свидетеля»

Было у «Тип по концам»:

> // ГРАНИЦА ТЕРЯЕТСЯ НА АРИФМЕТИКЕ, и не правилом, которое можно забыть: строит
> // «Вид отрезка» РОВНО ОДНА функция — «Тип по концам», — и она кладёт пустые
> // списки. Всякое вычисление идёт через неё, значит `н плюс 1` не знает про
> // длину ничего. Это то же свойство, что у свидетеля, и получено оно тем же
> // способом: не запретом, а единственностью места построения.

### История правки и ловушка минус нуля у «Знак уцелел»

Было у функции «Знак уцелел»:

> // ЗДЕСЬ РАЗЛИЧАЮТСЯ ДВА РАЗНЫХ «НЕ ГОДЕН», и до этой правки они были одним.

> // Минус ноль отсеян отдельно: сравнение «не меньше нуля» его пропускает, а
> // неотрицательным он не является. Проверка идёт делением: у минус нуля обратная
> // величина отрицательна, у нуля — нет. Это тот же приём, каким отличает их
> // свидетель, и та же ловушка, на которой сегодня попались двое.

### История: как отношений совместимости стало два

Было у раздела «подтипизация: `неотрицательное` ≤ `целое` ≤ `число`»:

> // До точного натурального типа отношение совместимости в языке было ОДНО —
> // «Одинаковые типы», то есть равенство. Стало два, и второе имеет НАПРАВЛЕНИЕ:
> // «Годится» отвечает на вопрос «можно ли положить данное туда, где объявлено
> // ожидаемое». Переведены на него ровно те десять мест, где значение ВТЕКАЕТ в
> // объявленную позицию; всё остальное осталось равенством и обязано им
> // остаться — там сверяются НОСИТЕЛИ (моноид, монада, изоморфизм, состояние
> // процесса), а носитель у `неотрицательное` и `число` один и тот же double.

### История: до появления непустоты строки в объединение не доходили

Было у «Объединить типы»:

>     // Строки сводятся тем же правилом и по той же причине. До появления
>     // непустоты они сюда не доходили вовсе — объединять у них было нечего.

### Замер 29 августа 2026: запись цели у «В ёлочках»

Было у функции «В ёлочках»:

>   // Замер 29 августа 2026: с «плюс 2» и списком — «объявлено, не доказано», с
>   // этой записью — «доказано сведением цели с телом функции». Свернёте обратно —
>   // обещание снова поедет проверкой в напечатанный код и купит кадр стека.

Правило «свернёте обратно — обещание снова поедет проверкой» в исходнике
осталось; замер с датой — здесь.

### История: обрубок `нат` не склонялся

Было у «Родительный падеж»:

>     // неотрицательного» читается так же, как «список целого». Обрубок `нат`
>     // раньше не склонялся вовсе, и это было исключением на один тип.

### Разобранная ошибка: перевёрнутый порядок аргументов подстановки

Было у «Подставить в список»:

> // Здесь стояла рекурсия `добавить голову к обработанному хвосту`, а `добавить`
> // дописывает в КОНЕЦ — список выходил перевёрнутым. Не замечали по
> // единственной причине: ни одного типа с ДВУМЯ параметрами в репозитории не
> // было, а на списке из нуля и одного элемента перестановка не видна. Нашлось
> // на `тип «Результат» от «Значение» и «Беда»` (flang/stdlib/result.flang, фаза
> // 5 в flang/cat/POLY.md): свидетель принимал модуль, а эта проверка жаловалась
> // «ожидался «Результат» от строки и числа, получен «Результат» от числа и
> // строки». Свёртка кладёт в том же порядке, в каком читает.

### История: зачем заведено поле суммы с одним вариантом

Было у «Тип поля суммы»:

> // Зачем. `связь.ключ` отвечало «доступ к полю требует записи», и правду о
> // функции «Ключ связи» (`stdlib/dictionary.flang`) сказать было нечем: `разбор`
> // в постусловие не помещается — оно однострочное. Вместо правды там стояла
> // заглушка «длина ключа неотрицательна», которая верна про любую функцию той же
> // подписи.

> // Новых слов при этом ноль: запись `значение.поле` разбор берёт с первого дня,
> // отвергала её проверка типов.

### Замер: приставочный захват против именованного (фаза 4 HOF.md)

Было у «Тип тега»:

> // Отступление от HOF.md сделано замером — приставке в дереве годятся 9 случаев
> // специализации из 56, остальным 47 прижимать надо не первый аргумент. Слова
> // `с` и `равным` уже заняты присвоением полей, поэтому новых слов поверхности
> // у фазы ноль.

### Решение о равенстве непрозрачных значений: разбор довода, проба и прогон

Было у «Сказать о сличимости»:

> // РАВЕНСТВО НЕПРОЗРАЧНЫХ ЗНАЧЕНИЙ РАЗРЕШЕНО. Здесь стоял запрет «сравнивать на
> // равенство можно только скаляры»: список, запись, вариант и параметр типа
> // сравнивать в теле функции было нельзя, а в постусловии — можно. Одна и та же
> // операция над одними и теми же значениями в одном месте проходила, в другом
> // отвергалась. Запрет снят, и вот на чём.
> //
> // ── Спека языка требует ровно обратного ─────────────────────────────────────
> // `flang/SPEC.md`, «Семантика, которая обязана совпадать у всех слоёв»:
> //     равенство списков, записей, вариантов | структурное
> // То есть равенство непрозрачных значений — не дыра, которую забыли закрыть, а
> // объявленное поведение языка. Отвергал его только слой типов.
> //
> // ── Довод «запрет держит цену видимой» не выдержал замера ───────────────────
> // Здесь стояло, что `равен` в теле — это работа: сличение двух списков стоит их
> // длины, двух записей — их глубины, и запрет держит цену на виду. Замер
> // показывает, что цену он не держит:
> //
> //   1. `куча содержит что` над `список «А»` проходит проверку в ТЕЛЕ. Это тот
> //      же самый обход двух значений, только в цикле по длине списка, — то есть
> //      в N раз дороже запрещённого. Проверено: файл с тремя такими функциями
> //      (над «А», над объявленной суммой, над списком списков) даёт код 0.
> //   2. `равен` на двух строках проходит в теле: строка считается скаляром. А
> //      сличение строк — это сверка всех их байтов, цена ничем не ограничена.
> //      То есть сам знак `равен` уже несёт неограниченную цену.
> //
> // Довод, который не работает ни для соседней операции, ни для самого этого
> // знака, границы не задаёт.
> //
> // ── Представление известно, и все восемь целей его сличают ──────────────────
> // Опасение «равенство на параметре типа «А» невыразимо при работе, потому что
> // представление неизвестно» проверено запуском, а не рассуждением. Программа
> // `«Есть среди» от «А»` с телом `куча содержит что` напечатана в C, собрана и
> // запущена на вариантах объявленной суммы:
> //     вариант «Есть» 7 среди: да | вариант «Есть» 8: нет | вариант «Нет»: да
> // Внутри — `fl_equal` из рантайма, обычный обход по имени варианта и полям.
> // Представление известно всегда: значение несёт свой тег при работе. Ту же
> // сверку печатают и остальные семь целей — `Equal(` в Go и C#, `equal(` в Rust,
> // JavaScript и Elixir, `Value.equal` в Java, `rt.equal` в Python.
> //
> // ── Что снятие НЕ даёт ──────────────────────────────────────────────────────
> // Ни одного нового права на вывод. Ядро от разрешённого в теле равенства
> // доказывать больше не начинает: тело — не цель. Выигрыш здесь в том, что
> // библиотека перестаёт переписывать сличение под каждый тип.

В исходнике осталось само правило (что разрешено, что запрещено и почему) без
истории запрета, без замера и без прогона.

### История: строка в исчерпывающности разбора

Было у «Исчерпана ли цепочка»:

> // «первый символ и остаток». Раньше она попадала в последнюю ветку — «не
> // исчерпать перечислением», — и это было правдой ровно до того дня, когда
> // образцы научились разбирать строку.

### История ускорения «Аргумент по номеру»

Было у «Аргумент по номеру»:

> // Обход остался ровно для тех номеров, на которых у формы ДРУГОЙ ответ, и
> // таких видов три, все три обслуживает по-прежнему он: номер вне списка (обход
> // доходит до пустого и отдаёт «Узел ничто», форма отказала бы), номер ДРОБНЫЙ
> // (обход округляет его вверх, потому что сравнивает с нулём на каждом витке) и
> // номер отрицательный (обход отдаёт голову). Значит ответы не изменились ни на
> // одном входе, а пять мест вызова — они спрашивают аргументы встроенных форм,
> // и номер там всегда целый и в пределах, потому что приходит из AST —
> // перестали стоить длину списка.

### Место меры, постусловия и теоремы — «там же, где стоит в свидетеле»

Было у «Проверить меру»:

> // Объявленная мера `убывает …`. Стоит МЕЖДУ телом и примерами — ровно там же,
> // где стоит в свидетеле, потому что порядок диагностик сверяется побайтово.
> //
> // Мера — тоже код, и её выражение обязано пройти тот же вывод типов, что тело:
> // `убывает (длина элементы) минус номер` при `элементы: число` жалуется на
> // `длина` из самой меры, а не откуда-то ещё. У НЕТОТАЛЬНОЙ функции мера не
> // разбирается вовсе: доказывать там нечего, и свидетель обрывается на этом же
> // месте.

Было у «Среда тела под предусловиями»:

> // Среда тела, уточнённая ОБЪЯВЛЕННЫМИ ПРЕДУСЛОВИЯМИ. Эталон `подПредусловиями`
> // свидетеля (`flang/src/types.mjs`).
> //
> // ЧТО ЭТО ДАЁТ. `требует «непуст» не пусто эл` кладёт на `эл` ту же нижнюю
> // границу длины, какую кладёт `если не пусто эл то …`, — и `голова эл` внутри
> // такой функции перестаёт быть местом частичной формы. До этого предусловие
> // тело НЕ ВИДЕЛО вовсе: слово ограничивало вызывающих и не давало телу ничего.

### Улика, с которой началась проверка постусловий: порча настоящего файла

Было у «Проверить постусловия»:

> // ─────────────── постусловие обычной функции: `checkPostconditions` ─────────
> //
> // Стоит МЕЖДУ мерой и примерами — ровно там же, где стоит в свидетеле, потому что
> // порядок диагностик сверяется побайтово.
> //
> // ЗАЧЕМ ЭТО ЗДЕСЬ. Постусловие — тоже код, и его выражение обязано пройти тот же
> // вывод типов, что тело. Пока этой проверки у эталона не было, слой пропускал
> // молча программу, которую свидетель отвергал, и увидеть это на чистом корпусе
> // было нельзя: чистые файлы дают «диагностик нет» с обеих сторон. Улику дала
> // ПОРЧА настоящего файла (`self-types.test.mjs`, «порченые файлы репозитория»):
> // у `flang/examples/leetcode/013-roman-to-integer.flang` число заменяется
> // строкой, `возвращает` становится `строка`, и свидетель говорит «левый операнд
> // сравнения «gte» имеет тип строка», а эталон молчал. Дыра была всё время —
> // показалась она в тот день, когда в корпусе неподвижной точки появилось первое
> // постусловие.
> //
> // ОБЛАСТЬ ВИДИМОСТИ взята у вычислителя, а не выдумана: связка результата
> // кладётся ПОВЕРХ параметров ровно так же, как это делает `stepPost` в
> // `interpret.mjs`, а имя связки берётся из узла — своего слова у слоя нет.

### Улика, с которой началась проверка гипотезы теоремы

Было у «Проверить теоремы»:

> // ─────────────── гипотеза теоремы: `checkTheorems` ─────────────────────────
> //
> // Стоит ПОСЛЕ всех функций — ровно там же, где стоит в свидетеле, потому что
> // порядок диагностик сверяется побайтово.
> //
> // УЛИКА, С КОТОРОЙ ЭТО НАЧАЛОСЬ. Гипотеза `дано <утверждение>` не
> // типизировалась ни одной строкой: `дано свет плюс "строка"`, `дано 5`,
> // `дано "строка"` проходили проверку с ответом «годно» и нулём диагностик.
> // Ловилась у гипотезы только область видимости, и ловил её разбор языка.
> //
> // ПОЧЕМУ ПРИЗНАК. Гипотеза едет к ядру ДОПУЩЕНИЕМ (`obligations.mjs`,
> // `цель.hypotheses`), а допустить можно только то, что бывает истинным и
> // ложным. `свет плюс "строка"` не является утверждением ни о чём.

### История: пока подстановку из значений никто не выводил

Было у «Подстановка примера»:

> // Зачем отдельный источник вывода. У вызова типы аргументов известны из
> // выражений, и подстановку решает вывод по вызову. У примера выражений нет
> // вовсе: `дано ф равно функция «Удвоить»` — это ЗНАЧЕНИЕ, обычный JSON. Пока
> // подстановку из значений никто не выводил, объявленный тип уходил в
> // «Проверить значение» как написан, и выходило одно из двух, оба неверных:
> // `ф: функция из «А» в «Б»` сравнивалось с `функция из числа в число`
> // буквально и верный пример падал, либо `значение: «А»` не проверялось вовсе.

### Падение двоичного из семени 24 августа 2026

Было у «Только диагностики»:

>   // ОБЕЩАНИЯ ЗДЕСЬ НЕТ НАРОЧНО, и вот почему. Стояло:
>   //   обеспечивает «беды не убывают» (длина «беды») не больше (длина результат)
>   // Оно ЛОЖНО: функция выбрасывает отметки анализа, значит результат бывает
>   // КОРОЧЕ входа. Ядро отказалось его доказывать — и было право; недоказанное
>   // обещание уехало в напечатанный код проверкой при работе, и двоичный,
>   // собранный из семени 24 августа 2026, падал на ПЕРВОМ ЖЕ файле:
>   //   FLANG_PROPERTY: нарушено свойство «беды не убывают — Только диагностики»
>   // Верное утверждение здесь — «накопитель не убывает», но ядро не берёт и его:
>   // нужна индукция по рекурсии.

Довод «обещание было бы ложным, а платить проверкой за почти тавтологию
незачем» в исходнике остался; дата и текст падения — здесь.

### Заслон печати: эталон и место, найденное побайтовой сверкой

Было у «Снимается непустотой»:

> // ЗАСЛОН ПЕЧАТИ, И ОН УСЛОВИЕ ЧЕСТНОСТИ ЧИСЛА. Эталон `src/types.mjs`, где
> // тот же заслон стоит на выдаче `доказаны`, и тот же список
> // (`builtins.mjs`, `СНИМАЮТСЯ_НЕПУСТОТОЙ`).

> // Сегодня за заслоном стоят `элемент` и `символ`: границы номера анализ
> // доказывает («В границах»), печатать их без сторожа нечем. Найдено это НЕ
> // чтением: эталон пометил `элемент` там, где свидетель промолчал, и побайтовая
> // сверка назвала место — `stdlib/strlists.flang`, `элемент|175|11`.

### История ветвления: шов ствола и ветки границ номера

Было у «Таблицы программы»:

> // «программа» здесь ВТОРЫМ параметром, а не полем сбора, и это шов двух веток:
> // ствол вынес построение таблиц в отдельную функцию, а ветка границ номера
> // добавила в «Таблицы» поле «база номера», которое читается из ПРОГРАММЫ.
> // Сложить их иначе значило бы либо потерять поле, либо вернуть вторую копию
> // сбора — ту самую, ради устранения которой функция и выносилась.

### Таблица входа и граница входа: что делал свидетель на Node

Было у раздела «таблица входа: объявленные типы параметров данными»:

> // `таблицаВхода` свидетеля (`flang/src/types.mjs`) — здесь, на flang, и делается
> // это не ради переписывания: без неё границы входа НЕТ У ПРОГРАММ, НАПЕЧАТАННЫХ
> // ДВОИЧНЫМ. Печать в C кладёт объявленные типы параметров данными, а решает,
> // какой у параметра тип, слой типов; пока это умел только свидетель на Node,
> // двоичный печатал `{ NULL, 0, … }` и честно предупреждал об этом, а
> // напечатанная им программа отвечала на `Factorial(−3)` при `n: нат` значением
> // `1`. Отказа не было — был неверный ответ, и «доказанное» место получало не
> // то, что обещано: доказательство завершения `тотальной` стоит НА ТИПЕ.

Было у раздела «граница входа: аргументы вызова объявленным типам»:

> // Эталон `checkArguments` свидетеля (`flang/src/types.mjs`). До него бинарник
> // принимал то, что свидетель отвергает: `flang run --function «Факториал» --args
> // '{"н":-3}'` печатал `1`, хотя `н` объявлено `неотрицательное`.

### Замер цены второй сверки тегов

Было у раздела «отметка доказанного на дереве»:

> // Вывод типов ЗНАЕТ, что оба операнда — числа: он это только что доказал. До
> // сих пор он это знание выбрасывал, и напечатанный C звал `fl_add`, который
> // первым делом сверяет теги обоих значений — ту же работу второй раз, уже в
> // рантайме. Замер назвал цену второй проверки: от 1,09 до 3,15 раза
> // (`docs/zamer-skorosti.md`).

Файла `docs/zamer-skorosti.md` в дереве нет.

### Сверка со свидетелем построчно

Эти пометки стояли рядом с кодом и объясняли не поведение, а происхождение
записи: как это же место называется и устроено у удалённого свидетеля.
Поведение осталось в исходнике, происхождение — здесь.

> // Внутреннее представление типа. `optional` свидетеля — не отдельный вид, а
> // Метка вида — то, что в свидетеле зовётся `type.kind`. Нужна затем же: сравнить
> // `fnParams` свидетеля: отсутствие поля и пустой список — одно и то же,
> // `sameType` свидетеля. Джокер совместим со всем — им гасятся каскады после
> // `typeName` свидетеля: «список числа», а не «список число» — так тип пишется
> // `appliedTo` свидетеля: применение печатается тем же оборотом, каким пишется в
> // `accusative` свидетеля: винительный падеж после `в`. Среди скаляров отличается
> // `genitive` свидетеля. Пометку «может отсутствовать» родительный падеж не
> // показывает — свидетель тоже смотрит только на вид.
> // Связывание «параметр → тип». Свидетель держит его картой; здесь список, и
> // `hasParams` свидетеля. Нужна там, где тип нельзя отдавать вниз ожиданием:
> // `expected` с несвязанным параметром внутри сбил бы вывод пустого списка.
> // `substitute` свидетеля: параметры → типы.
> // `matchAgainst` свидетеля: первый порядок, одностороннее сопоставление —
> // Признак «сошлось» едет наружу не для отчёта, а ради порядка: `every`
> // свидетеля обрывается на первом несовпадении, и связки, которые легли бы
> // после него, не ложатся.
> // `spanOf` свидетеля: место берётся у узла, если оно там есть, иначе `null`.
> // Отмечается место ОПЕРАТОРА, а не выражения: `span` двуместного узла — это
> // `token.span` знака операции (`src/parser.mjs`), поэтому у вложенной
> // `String(значение)` свидетеля. Нужна там, где свидетель печатает в сообщение то,
> // чего в исходнике быть не должно: `String(expr.kind)`, `String(pattern.name)`.
> // Отсутствующий ключ — это `undefined`, а не «ничто»: свидетель их различает.
> // `isName` свидетеля: непустая строка и ничто иное.
> // Первый непустой из двух ключей: `node.of ?? node.type` свидетеля. Возвращается
> // вместе с признаком «ключ вообще был»: `undefined` и «ничто» свидетель
> // различает, первое даёт «требует типа элемента», второе — джокер.
> // `bindingsOf` свидетеля: параметры объявления против аргументов применения.
> // `countParams` и `countArgs` свидетеля: число словами, потому что диагностика
> // Среда свидетеля — `Map<имя, Тип>`, копируемая на каждом связывании. Здесь это
> // список с головы: новое связывание приписывается в начало, поиск идёт от
> // головы, поэтому позднее связывание закрывает раннее — как `Map.set`.
> // `«Связать имя заново»`. Ровно та же пара есть у свидетеля: `Map.set` внутри
> // `сузить` и `связать` на местах связывания.
> // `expected && expected.kind === "list" ? expected.of : null` свидетеля.
> // Область параметров свидетеля (`ctx.paramScope`) — состояние, которое там
> // сохраняется и восстанавливается вокруг нормализации одного объявления.
> // `typeParamsOf` свидетеля. Параметр объявляется ЯВНО, а не выводится из
> // `openParamScope` свидетеля. Прежнюю область возвращает вызывающий: состояния
> // `normalizeType` свидетеля. Принимает и голую строку («число»), и узел
> // `namedOrScalar` свидетеля: имя скаляра, объявленной записи, суммы или
> // `applied` свидетеля: арность применения. Аргументов нет — тип остаётся ровно
> // `expandAlias` свидетеля. Мемоизация нужна не ради скорости, а ради одинакового
> // `fieldMap` свидетеля: поля объявления в порядке объявления, с проверкой имени
> // по номеру объявления — так свидетель сравнивает сами узлы.
> // Двунаправленный вывод свидетеля: «ожидаемый» — тип, которого ждёт контекст.
> // `!expr || typeof expr !== "object"` свидетеля: скаляр выражением не является,
> // а не про целость, и подделка на это стоит у свидетеля.
> //         перенос стоит у свидетеля (`src/types.mjs`, `сПодДлиной` в `сузить`).
> // `expr.cond ?? expr` свидетеля: место у операнда, а если его нет — у выражения.
> // Проход «просто вывести тип и забыть»: свидетель делает так там, где сигнатуры
> // `reportUnsolved` свидетеля. Оставлять несвязанный параметр в результате
> // `solvedResult` свидетеля: что не решили поля, решает ожидание. Без этого шага
> // `JSON.stringify(pattern.value ?? null)` свидетеля: ключ поля покрытия литералов
> // `bindPattern` свидетеля: имена, которые вводит образец, и жалобы на то, что
> // `args[index] ?? expr` свидетеля: место у аргумента, а если его нет — у формы.
> // `collectionOf` свидетеля: элемент коллекции, над которой работает форма.
> // потом строка. Так же его кладёт парсер, так же читает `builtins.mjs`.
> // Две формы, как в `builtins.mjs`: «соединить список с разделителем» и
> // `namedOrScalar` свидетеля в одном случае: имени в программе нет только тогда,
> // смотрит только на список. Порядок беды тоже совпадает со свидетелем — сперва
> // арность, потом номер, потом список, — и это не мелочь: диагностики
> // искать не ту строку. Ровно то же решение у свидетеля (`types.mjs`, `canonical`).
> // Порядок свидетеля: сначала объявленные поля («не задано поле», «не тот тип»),
> // Порядок свидетеля: сначала лишние поля не проверяются — сперва объявленные,
> // `post.expr ?? fn` свидетеля: место у выражения, а если его нет — у функции.
> // `г.expr ?? теорема` свидетеля: место у выражения, а если его нет — у теоремы.
> // подставленного: она принадлежит месту применения, и подстановка её не
> // меняет. Свидетель спрашивает там же.
> // `solveExample` свидетеля: подстановка для ОДНОГО примера полиморфной функции.
> // `exampleValueType` свидетеля: тип значения примера настолько, насколько он из
> // Место частичной формы, чья непустота ДОКАЗАНА. Наружу выходит форма и место —
> // то же, чем отличает места свидетель, и единственное, что у двух реализаций
> // общее: узлов у эталона нет, у него значение JSON.
> // ЧТО В ТАБЛИЦЕ ЕСТЬ И ЧЕГО В НЕЙ НЕТ — то же, что у свидетеля.
> // Ключ памяти о типе. Различает ровно то же, что различает ключ свидетеля
> // (`kind|имя|необязательный|целое|низ|верх`)
> // дно есть, а потолка нет — и это те же три случая, которые у свидетеля дают
> // `-Infinity`, `0` и `Infinity`.
> // числа`. Глубина ограничена восемью — тем же числом, что у свидетеля.
> // перекладывается свёрткой — и это та же цена, что у свидетеля за `типы[куда] =
> // …`, только уплаченная явно.
> // Три шага, и порядок у них тот же, что у свидетеля: сначала номера типов всех
> // же порядке, в каком её делает `сверитьАргументы` свидетеля: сначала заданные
> // нечего. Свидетель отвечает здесь так же (`signature === undefined → ok`).
> // Место у обеих бед одно и то же — «вызов функции «Имя»», — и это тот же текст,
> // каким подписывает их свидетель: подпись и метка у него совпадают.
> // Узел, который свидетель записывает литералом `{ args }`: подстановка читает у
> // Поле дописывается В КОНЕЦ, а не вставляется по алфавиту: свидетель кладёт его
> // тем же `{ ...узел, числовая: true }`, и порядок ключей входит в побайтовую
> // сверку AST.

---

## `flang/self/zapis.flang`


Запись доказательства. Ч334 файл уже правила; здесь остаток.

### Прежний довод о молчаливом расхождении

Было в шапке файла, в разделе «ТЕРМ И НОМЕР СТРОКИ ВМЕСТЕ»:

> Это не второй источник, а второе чтение одного и того же места; молчаливого
> расхождения, которого боялся прежний довод, здесь больше нет.

### Первая редакция ставила заголовок случая за шагами

Было у функции «Строки случая»:

> ЗАГОЛОВОК СЛУЧАЯ СТОИТ ПЕРЕД ЕГО ШАГАМИ, а не после. `добавить … к …`
> дописывает В КОНЕЦ, и первая редакция ставила заголовок за шагами — порядок
> строк записи расходился с порядком чтения исходника, и сверщик отвергал
> честную запись. Нашёл это он сам, первым же прогоном.

### Ветка, которой добавлен раздел ходов сведения

Было в разделе «ХОДЫ СВЕДЕНИЯ: ЧТО ИМЕННО СДЕЛАНО С ЦЕЛЬЮ»:

> ВСЁ ОТ ЭТОЙ ЧЕРТЫ И ДО СЛОВ «строки посылок с ходами» — ДОБАВЛЕНО ВЕТКОЙ
> `b/proigryvatel`. Ни одна прежняя строка файла не тронута; при разборе
> слияния этот кусок узнаётся целиком по имени раздела.

Там же, о том, что запись говорила до этого:

> До этого запись говорила о посылке одно: каким ПРАВИЛОМ ядро её закрыло. Имя
> правила — это «ядро решило, и поверь»: сверщику проверять нечего, и он честно
> печатал такие посылки числом «принято на слово ядра».

### Доля целей-равенств в корпусе `flang/proof/examples`

Было в разделе «ЦЕЛЬ-РАВЕНСТВО: ХОД „ЗАКРЫТЬ ТОЖДЕСТВОМ“», врезка «ЗАЧЕМ»:

> Ходами записывался бы ОДИН вид цели — «Т не меньше 0». Целей-равенств («Ф» от
> результат равно «Ф» от вход) в корпусе `flang/proof/examples` 20 посылок из
> 74, то есть 27 %, и ни одна не получала ни хода: сверщик считал их «принято
> на слово ядра». Закрывает такую цель ровно один ход — `закрыть тождеством`,
> и он же самый частый из тех, которых писатель не писал.

### Доля посылок, закрытых сведением ядра, и довод прежнего запрета

Было в разделе «ПОСЫЛКА, ЗАКРЫТАЯ СВЕДЕНИЕМ ЯДРА», врезка «ЗАЧЕМ»:

> Таких в корпусе `flang/proof/examples`
> тринадцать из семидесяти четырёх. Ходов им не выписывалось ни одного, и
> сверщик считал каждую «принято на слово ядра».
>
> Довод прежнего запрета звучал так: ведомость обязана отличать написанное
> автором (`term`) от закрытого ядром (`reduction`), а не выдавать второе за
> первое. Довод верен — и он про поля `by` и `steps`, которые здесь не
> трогаются вовсе. … Сличением:
> `corpus-tree-height.flang` и `corpus-depth.flang` объявляют один и тот же
> тип, у обеих база — ветвь `то 0`, и разница в записи ровно одна — у первой
> автор случай написал, и посылка получила три хода; у второй не написал, и
> посылка не получила ничего.
