# Оглавление открытых работ — 30 августа 2026

Один файл, по которому работу можно раздать сорока исполнителям. Здесь **все**
открытые задачи задачника: и заведённые сегодня, и лежавшие раньше. У каждой —
число, которым она измерена, и то, что от неё зависит.

Полный текст задачи — в её файле `tasks/NNNN-*.md`. Правила задачника —
[`tasks/README.md`](../tasks/README.md).

Снято 30 августа 2026 на стволе `f9e67fa6`. Задач в каталоге 142, из них
открытых — **67**: свободных **47** (одна из них без номера вовсе —
`tasks/a-verdict-on-a-file-does-not-depend-on-who-imported-it.md`), в работе
**19**, и одна с испорченной шапкой — **6411** (шапка починена 30 августа,
задачник цел). Закрытых 73, отменённых 2.

**Сегодня заведено 22 задачи.** За сутки ствол принял 83 коммита; числа ниже
сняты после них, а не до.

---

## Прежде чем брать любую — три шага, и второй сегодня важнее первого

### 1. Свериться со стволом

`git log --oneline -30` и поиск по теме. Заведённое как открытое, но уже
сделанное — самая дорогая ошибка этого дерева. Сегодня так закрыта задача
**9586** и опровергнуты три записи (см. «Что оказалось неправдой»).

### 2. Заглянуть в ОТДАННЫЕ, НО НЕ ВЛИТЫЕ ветки

**Это главная беда сегодняшнего дня.** Две ветки лежат в приёмной, работа в них
сделана, а в стволе её нет — и человек, взявший задачу по стволу, сделает её
второй раз.

| ветка | что в ней | какие задачи закрывает |
|---|---|---|
| `origin/m/raise-proved-share` (`44988053`) | 22 файла из 25 сняты с кучи «предел шагов»; `obyazatelstva-i-yadro` починен; знаменатель доли исправлен на 13 610; `не равен` переписан в `proof-initial` (70 → 82 из 94) | **2469** почти целиком, **6329** целиком, часть **3006** и **5578** |
| `origin/m/guards-1-66-shown-red` (`a9afced6`) | разбор 66 сторожей поимённо; задача **1389** «у каждого сторожа показано, что он краснеет на нарочной поломке» — 14 показанных из 65 | источник чисел для **1941**, **1722**, **3144**, **5090**, **5204**, **2261** |

`git merge-base --is-ancestor origin/m/raise-proved-share origin/main` отвечает
«нет» — проверьте сами, а не поверьте этой строке.

**Что с этим делать.** Свести обе ветки — работа на одного и она первая по
порядку: пока их нет в стволе, шесть задач ниже измерены дважды и по-разному.
Задача **1389** придёт вместе со второй веткой; она НЕ дублирует двенадцать
сторожевых задач этого списка — она про другое (показана ли поломка), они про
конкретные поломки. Сводить, а не делать заново.

### 3. Заглянуть в соседние рабочие копии

| копия | по чему видно | задача |
|---|---|---|
| `m-reprint-4` | **идёт перепечатка, НЕ ТРОГАТЬ** | 0003 |
| `m-raise-proved-share` | ветка выше | 2469, 6329, 5578 |
| `m-guard-audit-1-66` | ветка выше | 1389 и двенадцать сторожевых |
| `m-kernel-ledger` | `build-kernel/flang` — готовый двоичный для ведомости | 6128, 7205 |
| `m-kernel-split`, `m-okrestnost` | выходы `check --proof` по файлам | 6329, 7205 |

### 4. Вписать себя в шапку отдельным коммитом, до начала работы

И объявить доли: `/srv/priyom/claim взять <ваша-ветка> <файл>…`.

---

## Где уже лежит готовый рецепт — чтобы не мерить заново

Пять мест, куда надо посмотреть **до** того, как ставить свой замер.

| нужно | где лежит | что там |
|---|---|---|
| снять ведомость с файла, который ничем не снимался | задача **6128** + `/srv/flang-rabota/m-kernel-ledger/build-kernel/` | двоичный из семени + рантайм дерева; ключ `--предел-шагов 400000000000`; блок настроек из шапки семени; ворота `PAMYAT=45G` |
| сколько идёт ярлык и во что упирается | `docs/ci-inventory.md` | время, код и причина по большинству ярлыков |
| правила очереди и памяти | `docs/gate-rules.md` (1019 строк) | пул 64 места, потолок по памяти, старшинство по памяти |
| числа перепечатки | `scripts/raskrutka.sh:229–330` | `MEASURED_COST=456857834234`, `MAX_STEPS=1400000000000`, вилка замера, три сторожа числа |
| чего не хватает доказателю | `docs/what-the-prover-lacks.md`, `docs/body-shapes.md` | семь пород нехватки с числом утверждений у каждой |

**Ловушка, на которой уже сгорели:** ключ `--предел-шагов` **задаёт** предел, а
не поднимает. Поставить «с запасом» 400000000 значит опустить его вдесятеро.

---

## Что делится на многих — берите это, если вас много

| задача | чем делится | сколько человек | измерено |
|---|---|---|---|
| **7205** файлы без ведомости | **по файлам, они независимы** | сколько угодно, упор в места у ворот | **105 файлов / 6988 обязательств** без ведомости по описи ствола; ~182 / 7257 по исправленному знаменателю |
| **3006** `не равен` в целях | **по файлам** | до 31 | **89 строк в 31 файле**; на одном файле ход дал 70 → 82 из 94 |
| **1941** шесть тяжёлых сторожей | **по ярлыкам** | до 6 | шесть ярлыков ни разу не досмотрены: очередь стояла полной |
| **2469** остаток кучи «предел шагов» | по файлам | до 3 | **3 файла / 53 обязательства** |
| двенадцать сторожевых задач ниже | по ярлыкам | до 12 | у каждой своё число |

Три первые строки — это больше половины всей делимой работы дерева.

## Что берётся одним и делить нельзя

| задача | почему одним |
|---|---|
| **9952** ядро не заходит внутрь связывателей | правило пишется в `proof-kernel.flang`; слияние двух ядер текстом уже признано написанием третьего (`docs/zettel/two-cores-do-not-merge-as-text.md`) |
| **6744** счёт постоянных и длина склейки | тот же файл `proof-kernel.flang` |
| **8466** сверщик дороже соседа в 15 раз | один прибор, один вход, одно разложение |
| **7742** строк ядра, которым верить | один разбор одного числа |
| **3380** девятая цель печати C++ | одна цель, один рантайм |
| свод двух невлитых веток | один человек, иначе сведут дважды |

---

## Что держит всё остальное: перепечатка

Связка стала короче на одну задачу: **9586 закрыта сегодня.**

```
8721  чистое дерево для захода   ─┐
                                  ├─→  0003  перепечатка доходит до конца
5563  прогоны для ядра сужены   ─┘         │   (идёт сейчас в m-reprint-4)
      (ветка влита, статус не снят)        │
                                           ├─→ 7101  правила процессов принуждаются
                                           ├─→ 8398  планы судятся
                                           ├─→ 4025  семя несёт конкурентность
                                           ├─→ 1943  «ноль аксиом» зеленеет
                                           ├─→ 1186  английские имена в цели «c»
                                           └─→ 8291  два поручения и переменная среды
```

Заход идёт **сейчас**: `m-reprint-4`, `--max-steps 1400000000000`, пущен
29 августа. Пока он идёт, `bootstrap/**` и `scripts/otpechatok-semeni` не
трогать и `sh scripts/raskrutka.sh` не звать.

| номер | статус | кто | измерено | заголовок |
|---|---|---|---|---|
| **0003** | в работе | u | шесть заходов, пять смертей; нынешний пущен с потолком 1,4 трлн | перепечатка семени доходит до конца |
| **8721** | свободна | — | мера задачи снимается **не здесь**: в этой копии `git status -- bootstrap/` пуст, а речь про `m-reprint-2` | заход пускается из чистой копии ствола |
| 5563 | в работе | m | **ветка `m/kernel-runs-narrowed` влита** (`91b7312a`, `1a5f81a5`) — статус в шапке не снят | ядро получает прогоны только там, где на них ссылаются |
| 1093 | в работе | u | обходов 334, кадр берут 199 | обходы переведены на свёртку |
| 6411 | в работе | b | шапка починена; шесть строк первого списка сняты, четыре новые не доехали — `seed-freshness.sh` код 1, семя отстало на 5 файлов | что ждёт перепечатки — один список |

---

## Доказательства: приём есть, осталось им пройти

```
6128  приём записан  ──→  7205  файлы без ведомости (105 файлов, 6988 обязательств)
                     ├──→  2469  остаток кучи «предел шагов» (3 файла, 53)
                     └──→  6329  окрестность ядра (106 обязательств без «доказано»)
                                        │
       3006 «не равен» ────────────────┤
       6744 два расширения ядра ───────┼──→ 5578  доля доказанного по дереву
       1729 97 предусловий ────────────┘          │
                                                  └──→ 1606 сверка опубликованного
```

| номер | статус | кто | измерено | заголовок |
|---|---|---|---|---|
| **7205** | свободна | — | **105 файлов / 6988 обязательств** без ведомости; заход `obhod2` прошёл 96 из 96, из них код 0 — 25, код 124 — 71 (срок 150 с мал) | файлы без ведомости берутся поштучно |
| **6128** | свободна | — | приём работает: ядро 52 мин 18 с, пик 9,65 ГиБ, **44 из 44**; в дереве приёма **нет**, `pusk-yadro.sh` и `dolya-fajla.py` живут в `/srv/tmp/` | приём снятия ведомости записан |
| **6329** | свободна | — | ведомость снята у **всех шести**; **237 из 343**; `proofterm` 46/138, `obyazatelstva-i-yadro` 0/2 | окрестность ядра судится |
| **2469** | свободна | — | **3 файла / 53 обязательства**: `repl/repl` (память, 10,2 ГиБ), `postgres-plan` (19) и `postgres-scram-plan` (33) — эти в предел прогона примеров, 40 млн, **другой ключ** | остаток кучи «предел шагов» |
| **3006** | свободна | — | **89 строк в 31 файле**; `не равен` — токен `cmpNeq`, отрицание ядро читает в одном месте (`proof-kernel.flang:1003`) | цель с «не равен» переписана |
| **6744** | свободна | — | `плюс 2` не доказывается, `плюс 1 плюс 1` доказывается при том же теле; длина склейки складывается только у парной | ядро считает постоянные и длину склейки |
| **9952** | свободна | — | разреза по первому слову тела **в дереве нет**; есть другой замер: 96 из 208 тел (46 %), 72 формы, на которых ядро встаёт | ядро не заходит внутрь связывателей |
| **5578** | **сделана** | b | **4028 из 13 489 = 29,9 %** — снято прогоном 30 августа на стволе `4233c18c`; нижняя оценка: у 185 файлов из 510 (**7751 обязательство, 57 %**) живой ведомости нет, и 1825 доказанных не повторяются двоичным дерева. Прежние 8147 из 13 341 непроверяемы | доля доказанного — одно число с оговоркой |
| **1729** | свободна | — | **97** = `требует` 88 + `закон` 9; два прибора доли обходятся с ними по-разному | 97 обязательств вне числителя |
| **7742** | свободна | — | **4546 против потолка 3372** (+1174); достижимо **7615 против 5967**; CI красный на каждый пуш | строк ядра, которым верить |
| 0002 | **сделана** | m | — | один двоичный — одна ведомость |
| 4102 | в работе | u | 285 обещаний, ноль примеров | ядро доказано о себе |
| 0006 | в работе | b | — | обещания трёх крупных файлов доказаны |
| 0008 | в работе | b | — | строгие неравенства при свободном имени |
| 1238 | свободна | — | — | ход между двумя неравенствами |
| 0051 | в работе | u | — | ядро перестало делать проход на каждый факт |

---

## Сторожа: двенадцать задач, заведённых сегодня

Все двенадцать берутся **порознь**, друг другу не мешают, и каждая мелкая. Это
лучшая работа для того, кто пришёл на смену и хочет закрыть что-то за день.

**Осторожно с номерами ярлыков.** Ярлыков сегодня 99 (`ярлыки.flang:79–177`,
номер = позиция в списке). Разбор 1389 писан, когда их было 95, и номера там
другие: ≤14 — те же, 15–25 → +1, 26–56 → +2, 57–82 → +3, 83–95 → +4. **Во всех
задачах ниже ярлык назван ИМЕНЕМ**, и брать надо по имени.

| номер | ярлык | измерено | беда |
|---|---|---|---|
| **1941** | шесть тяжёлых: `спеки:подлог`, `доказательства:ведомость`, `доказательства:свод`, `доказательства:пустота`, `пустота:порча`, `доказанное:порча` | ни разу не досмотрены: «ярлыки 20—66 прогоном не померены» | зелень без замера |
| **2261** | `столкновения:пересчёт` | по своей шапке **30 мин 01 с, 37,8 ГБ** при пределе ворот 40 ГБ; пул вырос до 64 мест — можно пускать | не доходил |
| **1722** | `поиск:различающий` | каталог `.flang-razlichitelnyy-*` в **корне дерева**, уборка вне `try/finally`, в `.gitignore` только `.flang-storozh-*`; сторож времянок корень не смотрит | грязь в дереве, невидимая |
| **6445** | `указатель:печать` | **девять** ветвей отказа, **пять** кодов, ни одной с возвратом | полуписаный указатель уезжает в свод |
| **5963** | `двоичный:приёмка` | шесть примет, **ни одна не снимается**: `/srv/flang-rabota/u-semya5/bootstrap/flang` нет; код 1 одинаков на «нечем проверять» и «забракован» | красный цвет ничего не значит |
| **6526** | `выпуски:страница:проверка` | не влезает в умолчание 10 млн (`FLANG_RECURSION_LIMIT`), ключа в ярлыке нет; ветвь «клон без тегов» — код 0 | не бежит, а добежав — не проверяет |
| **6267** | `точка:проверка` | «НЕ ПРОВЕРЕНО» отдаёт **код 0**: три ветви — нет `MemTotal`, память < 32 ГиБ (порог зашит, строки 133–140), нет двоичного | круг раскрутки разомкнётся молча |
| **5204** | `сторожа:проверка` | обещает «каждый файл проверок дерева», смотрит `*.test.mjs` — сегодня их **4**; за 99 ярлыками стоит **76** файлов команд, сторожей ≈69 | обещание вчетверо шире дела |
| **8028** | `сведение:проверка` | «упёрся в бюджет» и «поймал ложь» в одном счётчике `BAD`; проза обещает 2 честных и 14 подделок, в файле **3 и 18** | провал значит две разные вещи |
| **3144** | `времянки:проверка`, `октет:проверка`, `октет:порча`, `память:проверка` | первый — код 3 `FLANG_UNKNOWN_PLAN` (нет `--plan` при двух планах); два `октет` зовутся `sh`, а скрипт — bash (`${BASH_SOURCE[0]}`); у памяти ключ шагов добавлен (`de1f500a`), но «сколько нужно» не снято | три не бегут вовсе |
| **5090** | шесть самопроверок | **ни одной** из шести нет в дереве; ссылаются пятеро, а `tempdir-guard.flang:367` `uzel-celi.test.mjs` **запускает** | ссылка на пустоту |
| **4107** | все 99 | у записи «Ярлык» три поля, срока нет; разброс от 1,5 с (`октет:*`) до 30 мин (`столкновения:пересчёт`) и 40 мин срока-ключа | планировать смену нечем |
| 6805 | `коды:подлог` | смотрит в свою память, а не в дерево | заведено раньше |
| 7192 | `столкновения:пересчёт` против `столкновения:проверка` | близнец нашёл 0 из трёх настоящих при 7873 видимых объявлениях против 57 516 | заведено раньше |

---

## Язык: написано, но не принуждается

| номер | статус | кто | измерено | заголовок |
|---|---|---|---|---|
| **8398** | в работе | m | судья `flang/self/io.flang` — **785 строк**, ввозов **ноль**; 12 подделок из 13 приняты; `«Ключи судимых»` съехал на `compiler.flang:1846` | планы судятся |
| **7101** | свободна | — | `processes.flang` **463 строки**; правил **12** (по фикстурам), кодов 7, разделов 5; «правило четвёртое» стоит **дважды** (`:272` и `:392`) | правила процессов принуждаются |
| **4025** | свободна | — | `fl_conc_plan`/`fl_conc_process`/`fl_conc_supervisor` — **по нулю**; семя не двигалось с `58b037c2`, 23 августа | семя несёт конкурентность |
| **8291** | свободна | — | поручений ровно **20**; чтения переменной среды нет вовсе; «с вводом» — названный отказ `FLANG_IO_NO_SPAWN` только у браузера | два поручения и переменная среды |
| **3380** | свободна | — | целей печати **восемь**, C++ нет; задач про C++ в задачнике ноль; уроборос держит C++ сквозняком и проверяет `g++` | девятая цель печати — C++ |
| 1853 | в работе | m | 17 программ из 25 не судит никто | процессы, надзор и прогоны судятся |
| 0041 | в работе | u | — | объявил процесс — получил многоядерную программу |

---

## Правда о себе

| номер | статус | кто | измерено | заголовок |
|---|---|---|---|---|
| **4838** | свободна | — | **66 файлов при потолке 63** (`опись:языки`); CI красный | долг вне JavaScript вернулся под потолок |
| **1606** | свободна | — | **наполовину закрыта**: ветка влита `07030e90`, подвал починен `12901378`; осталось **5 расхождений** из 24 | сверка опубликованного с деревом |
| **7790** | свободна | — | перепись сегодня: не на flang **209 / 600 034**, долг **107 / 25 980** | долг назван по языкам |
| **5519** | свободна | — | десять команд против тринадцати; `npm test` — 10 провалов; лицензии — 4 нарушения на 75 файлах | четыре обещания README снова верны |
| **1943** | свободна | — | сторож подделок: нарушений 40, код 1; утверждение стоит в семи местах | «ноль аксиом» подтверждается прогоном |
| **2335** | свободна | — | свод печатает «0.6.2 → 0.7.0», в дереве и в тегах везде `0.6.2` | рекомендация о версии исполняется или видна |
| **3258** | свободна | — | карта: 54 файла / 25 487 строк / долг 42; опись даёт другое | карта называет числа описи |
| 0044 | в работе | u | — | заявленные числа пересчитаны честно |
| 8235 | свободна | — | — | устройство хранилища видно по корню |
| 6094 | свободна | — | **карта «Документация» — раздела нет в ROADMAP** | курс собирается сайтом |

---

## Оснастка и цена

| номер | статус | кто | измерено | заголовок |
|---|---|---|---|---|
| **8466** | свободна | — | `сличить.sh` — **338 302 508** шагов и доходит; `сверить.sh` — больше **5 млрд** и не доходит. Отношение **14,8** | сверщик дороже соседа в пятнадцать раз |
| **5123** | свободна | — | семь мест называют потолок **300 млрд** или **4 млрд** при нынешних **1,4 трлн** | шесть мест отстали от нового потолка |
| **8026** | свободна | — | **половина введена**: удержания мест нет, старшинство по памяти есть, **по числу мест нет**; пул вырос с 8 до 64 | веер не голодает за узкими |
| **6614** | свободна | — | 87,7 % витков в перебор | почему цена поиска растёт по ходу |
| **6715** | свободна | — | сборка из `58596087` даёт файл на 80 байт меньше | общий двоичный воспроизводится |
| **1186** | свободна | — | охват вырос 237 → 311 (`73e144b5`); **612 модулей** вне охвата | правило имён доходит до 612 модулей |
| 0016, 0049 | в работе | u | — | проверки на JavaScript переписаны; последний JavaScript уходит |
| 0026, 0062 | в работе | b, u | — | имена заметок — английские слова; расширения — один список |

## Прочее в работе

| номер | кто | заголовок |
|---|---|---|
| 0028 | u | узел разобранной программы — своя сумма типов |
| 0045 | u | старый сторож жаргона снесён |
| 0048 | u | TLS собран целиком |
| 0060 | u | функция обещает свою цену в витках |

---

## Что оказалось уже сделанным — и потому НЕ заведено

Это половина ценности списка.

1. **9586 «потолок нового семени меньше цены связывания» — закрыта сегодня.**
   Числа, на котором она стояла (`MAX_STEPS=4000000000`), в файле больше нет:
   `scripts/raskrutka.sh:322` держит **1 400 000 000 000**, цена связывания
   переснята и досчитана (**14 228 379 129** витков, `:280`), довод записан
   рядом, и число сторожится тремя проверками. Идущий сейчас заход пущен именно
   им.

2. **`--bystro` сверяет со своим отпечатком, а не с семенем — неправда.**
   Ключа `--bystro` в `scripts/binary-origin.sh` нет вовсе: быстрая сверка —
   умолчание. Она вынимает `readelf -sW` определённые имена из `.symtab` и
   сличает их с именами из **шести файлов семени** (`:474–512`). Отпечаток
   скрипта участвует только в ключе памяти ответа (`remember_key()`, `:322–329`)
   — и участвует нарочно: без него починенный сторож принял бы запись,
   сделанную сломанной версией. Задачу заводить не за что.

3. **«Вхождение и приставка в строке не доказываются ничем» — неправда.** Оба
   правила в ядре есть: `proof-kernel.flang:364–370` («вхождение по построению»,
   функция `«Это вхождение»` на `:1127`) и `:380–406` («начало по построению»,
   `:1137–1143`). Отказ на `:495` перечисляет девять видов цели, и оба там
   стоят. Не хватает более узкого, и оно записано:
   `docs/what-the-prover-lacks.md:53–77`.

4. **Хеш в подвале сайта — починено в стволе** коммитом `12901378`.
   `docs/site/build.mjs:114–150` сам спрашивает `merge-base --is-ancestor` и
   печатает `(не на стволе)`. Опубликованный подвал держит старый `478d23c8`
   (объекта такого в дереве нет вовсе) до ближайшей пересборки.

5. **Регистр в напечатанных именах эсперанто — названо и закреплено, а не
   забыто.** Комментарий стоит в четырёх целях печати
   (`emit-csharp.flang:544`, `emit-java.flang:505`, `emit-python.flang:542`,
   `emit-elixir.flang:420`), и в двух из них поведение закреплено пробой
   `ожидается "DeUnuĝisKvin"`. Замер рядом: **заглавных букв эсперанто в именах
   дерева ноль** по всем 849 файлам вне точки раскрутки. Число ноль — значит
   чинить сегодня нечего.

6. **`память:проверка` «падает вместо замера» — половина починена** коммитом
   `de1f500a`: ключ шагов добавлен. Осталась вторая половина, и она в **3144**.

## Мои числа против названных

| о чём | названо | снято мной 30 августа |
|---|---|---|
| файлов без ведомости | 188 файлов / 8195 обязательств | **105 / 6988** по описи ствола (~182 / 7257 по исправленному знаменателю); 188/8195 — снимок 22:40 29 августа **на невлитой ветке** |
| второй заход по файлам | дошёл до 36 из 96 | прошёл **96 из 96**: код 0 — **25**, код 124 — **71** (срок 150 с) |
| куча «предел шагов» | 3 файла / 53 обязательства | **сходится знак в знак** |
| `не равен` в целях | 89 строк в 31 файле | **сходится** (90 строк в 32 файлах с кванторной формой) |
| `proofterm`, `obyazatelstva-i-yadro` | 46 из 138; 0 из 2 | **оба сходятся** |
| третье расширение ядра | «вхождение и приставка не доказываются ничем» | **опровергнуто**: оба правила в ядре есть |
| доли по первому слову тела | 91/90/81/62/46/20 %, 70 % недоказанного | **в дереве нет ни одного из этих чисел**; ближайший замер меряет другое (96 из 208 тел) |
| ярлык 38 нужно памяти | 46,5 ГиБ | **в дереве нет**; сторож называет свои **37,8 ГБ** при пределе ворот 40 |
| ярлык 63, ветвей отказа | пять | **девять** ветвей; **пять** — это число различных кодов |
| ярлыки 88, 91, 93 | по нынешнему списку — `журнал:проверка`, `выпуски:страница`, `раскрутка` | описания подходят к **`выпуски:страница:проверка` (92)**, **`точка:проверка` (95)**, **`сторожа:проверка` (97)** — номера из списка на 95 ярлыков |
| ярлык 29, провалов | 7 из 9 — исчерпание бюджета | **не снято**: прогон не укладывается в 600 с. Зато проза ярлыка врёт: обещает 2 честных и 14 подделок при **3 и 18** |
| разброс времени ярлыков | от 0,29 с до 38 минут | **в дереве нет**; снятые крайности — 1,5 с и 30 мин 01 с |
| строк ядра, которым верить | 4546 против 3372, 5967 → 7615 | **сходится знак в знак** |
| долг не-flang | 65 файлов при потолке 63 | **66 при 63**; числа 65 в дереве нет нигде |
| знаменатель доли | 13 610 | **13 617** (мерено на день позже) |
| `git ls-files` без `-z` | теряет 451 файл из 1154 | **опровергнуто**: в дереве везде `core.quotePath=false`; арифметика верна только если его снять |
| 97 обязательств вне числителя | `требует` 88, `закон` 9 | **сходится**; у `закон` причина жёстче — `check` отвергает такой файл кодом 2 целиком |
| проверок снято перепечаткой | 1250 против 181 | **1250 в дереве нет нигде**; 181 есть (`AGENTS.md:134`, помечено «повторно не мерено») |
| суд ядра | 456 857 834 234 шага | **сходится**, но это **верхний конец вилки**: последний отсчёт 455 989 437 934, шаг выборки 868 396 300 |
| потолок нового семени | 1,4 трлн | **сходится** (`scripts/raskrutka.sh:322`) |
| пределов шагов | три | **больше**: `io` 10 млн, `run` 40 млн, `test` 40 млн, `emit` (клеймо в вывод), `repl`, `check` (`--предел-шагов`), `FL_MAX_STEPS` в семени |

## Задачник ЦЕЛ — все семь замечаний починены

Было семь: два у задачи без номера, два про раздел «Документация» в дорожной
карте и три у **6411** (статус «открыта» не из четырёх, исполнитель и ветка не
названы). Все семь сняты; шапку 6411 починили 30 августа.

```sh
/srv/flang-rabota/b-staroe-semya-6411/bootstrap/flang \
  io flang/scripts/tasks.flang --plan 'Задачник цел'
```
`задачник цел: всего задач 150: свободно 37, в работе 34, сделано 77,
отменено 2`, поручений 152, **код 0**.

⚠ **Снято двоичным из семени ДО перепечатки** (`52996005^`, md5 `82f9ac40`), и
иначе сегодня нельзя: двоичный из нынешнего семени на этом плане падает
`FLANG_RECURSION_LIMIT` у `«Дальше после шага»` при любом `--предел-глубины`
(20 000, 200 000, 2 000 000) — код 3. Причина — распавшийся батут главного цикла
вычислителя, разбор в задаче **6411**.
