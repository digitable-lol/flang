# Что дальше

Страница о том, чего в языке ещё нет: что делается сейчас, что стоит в очереди
и что решено не делать. Дат здесь нет — ни кварталов, ни месяцев.

Что язык уже умеет, читают не здесь: [Справочник конструкций](language.html),
[Операции языка](operations.html), [Выпуски](releases.html).

## Где язык сейчас

| | |
|---|---:|
| Функций написано на flang | {{корпус.функций}} |
| Из них с доказанным завершением | {{корпус.тотальных}} |
| Утверждений о поведении высказано | {{утверждения.высказано}} |
| Из них доказано ядром — обо всех входах | {{утверждения.доказано}} |

Главный предел языка виден прямо здесь, и он же — главный пункт плана:
**завершение доказывается массово, поведение — редко**.

## Сейчас в работе

**Ядро доказывает мало обычных функций.** Мешает не скорость поиска, а сила
самих правил: решающих правил три, и на теле обычной функции они кончаются.
Как устроено ядро — [Зачем и как](proofs.html).

**У напечатанной программы нет границы входа.** Установленный `flang` доводы по
объявленным типам проверяет: `Факториал` объявлен над `нат`, и на −3 отвечает
`FLANG_TYPE: аргумент «н»: -3 вне нат` с кодом возврата 1. Программа, вышедшая
из `flang emit`, — нет: за её вход отвечает вызывающая сторона, и `flang emit`
говорит об этом при печати.

**Установленный язык печатает только в C.** Остальные цели печати приезжают с
компилятором, поставленным через npm, — [как встроить flang в чужую
программу](embedding.html).

**Английская половина сайта неполна.** Страницы сайта переведены; руководство и
спецификации — пока только по-русски.

## Дальше, и каждое держит следующее

**1. Нет менеджера пакетов.** Сами пакеты есть: `flang package` кладёт
библиотеку со всем, что она тянет, в один файл — [как это
делается](packages.html). Нет всего, что стоит над пакетом: реестра, поиска по
имени, диапазонов версий, разрешения зависимостей. Обновление сегодня — взять
новый файл и положить на место старого.

**2. Стандартная библиотека мала.** Ни базы данных, ни полноценной сети. Это
идёт после менеджера пакетов, а не до: пока библиотеку нельзя раздать по имени
и версии, растить её незачем.

**3. Прикладного кода почти нет.** Бэкенд — один пример из семи файлов
(`flang/examples/library-api`), фронтенд — демонстрация в браузере, а не
приложение. Прикладной код ждёт библиотеку, библиотека ждёт пакеты.

## Решено не делать

**Замыканий не будет.** Захват окружения ломает доказательство завершения и
прямую печать в C, Go и Rust. Функции первого класса при этом **есть**:
компилятор заменяет функцию-значение меткой и разбирает по меткам. Замыкание и
функция первого класса — разные вещи, отказано только первому.

**Двух версий одной библиотеки в одной программе не будет.** Когда две
зависимости тянут одну библиотеку разных версий, это решается подъёмом версии, а
не соседством обеих в программе. Разбор — в [Модульности и
пакетах](modules.html).

**Модель Unison целиком не берём.** Хранить код в базе вместо файлов — значит
владеть редактором, владеть хостом и потерять git. Половину — адресацию по
содержимому — берём, вторую нет.
