---
номер: 1183
заголовок: Вычислитель половин «Выч» сверщика смоделирован в Lean — либо письменно доказано, почему модель его взять не может
статус: в работе
исполнитель: a
ветка: a/1183-lean-vych
команда: первая
карта: Сколько доказано на самом деле
рядом: 1130, 3449, 3448
нужность: 2 — «Выч» вне приёмки Lean: Р2 и Р3 берут посылками только его шаги, и без него не судятся (Приёмка.lean, строки 33–38)
---

# 1183. «Выч» в Lean или довод о невозможности

## Чем измерено

`flang/proof/lean/Приёмка.lean:33–38` (15 сентября 2026, main `8c1fee225`): от
`половина_закрыта` в `сверщик.c` достижимы 78 функций и 1228 строк кода из 7401
(`оценить_терм` — 261). Сегодня (17 сентября 2026, main `cbfaf3899`): `прогон.sh` — строк
ведомости 109, с леммой 107, лемм 163, ловушек 47 — отвергнуты 47.

## Что сделать

Сначала замер: сколько правил и мест корпуса опираются на Выч (число), какова цена модели
(строки Lean по образцу «Выч-звено»/«Выч-вариант» в `Правила.lean`). Затем либо модель
вычислителя в `Модель.lean`/`Правила.lean` с приёмом в `Приёмка.lean` и леммой
состоятельности, либо ADR о невозможности с числом.

## Как поймём, что сделано

Строка «Выч» ведомости — с леммой и в приёмке (`прогон.sh` печатает её среди принятых),
ловушки на Выч отвергнуты; либо ADR-0045 принят с числом «сколько строк надо и почему нельзя».
