---
номер: 1219
заголовок: Комментарии в scripts/**/*.sh — меньше 20 % строк: шапка отвечает «что, как звать, коды возврата», история уходит в docs/zettel
статус: в работе
исполнитель: a
ветка: a/1219-comments-in-shell-scripts-fall-below-a-fifth
команда: вторая
карта: Что мешает больше всего
рядом: 1428, 8650
нужность: 2 — 5430 из 14 062 строк (38 %) — комментарии; шапки пересказывают историю правок, которую хранит git log и docs/zettel
---

# 1219. Комментарии в оболочке — меньше пятой части

## Чем измерено

Дерево `main` `cbfaf3899`, 17 сентября 2026:

```
git ls-files -z 'scripts/*.sh' 'scripts/**/*.sh' | sort -zu | xargs -0 awk '/^[[:space:]]*#/{c++} {t++} END{print c, t}'
# → 5430 из 14062 = 38 %
```

45 файлов. Приметы `# СНЯТО …` (prose-numbers-guard) — тоже комментарии, их снимать нельзя.
Сторожей в `scripts/guards/*.sh` — по числу файлов; набор ярлыков — 145 (`ярлыки.flang`).

## Как поймём, что сделано

- та же команда даёт < 20 % при том же наборе файлов (или наборе, изменённом другими
  задачами — назвать);
- у каждого `.sh` шапка не длиннее нужного и отвечает на три вопроса: что делает,
  как звать (ключи), коды возврата;
- вынесенная история — в `docs/zettel/<имя>.md` с ссылкой из шапки; ничего не потеряно
  (число вынесенных заметок);
- число ярлыков в `ярлыки.flang` и число сторожей до/после равны; `sh .githooks/pre-push` зелен;
- `sh scripts/guards/prose-numbers-guard.sh` сошёлся (приметы не тронуты).

## Чего задача НЕ делает

Не трогает `scripts/raskrutka.sh`, `scripts/otpechatok-semeni`. Не меняет поведение
скриптов: только комментарии.
