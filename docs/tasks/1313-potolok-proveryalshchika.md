---
номер: 1313
заголовок: У проверяльщика нет потолка размера, и он вырос на 846 строк за день
статус: свободна
исполнитель: —
ветка: —
команда: первая
карта: Сколько доказано на самом деле
рядом: 1301, 1311
нужность: 1 — §1 сделан (b8e12a3dd: потолок ХРАПОВИК.txt 7138, сторож С-6); §2 второй независимый проверяльщик и §3 спецификация формата — нет; доверие чекеру на пути доказуемости. §1 сделан: чекер/ХРАПОВИК.txt потолок 7138 строк кода (сейчас 6585), сторож С-6 в flang/proof/сверка-ведомости.sh; §2 (второй независимый проверяльщик) не сделан, §3 — ПРАВИЛА-ВЫВОДА.tsv+Lean
---

# 1313. Довод «прочитаешь за вечер» держится на размере, а размер никто не мерит

`flang/proof/чекер/сверщик.c` — это то, ради чего вся затея: программа, которую
посторонний соберёт одним `cc` и проверит наши доказательства, не запуская наш
компилятор вообще. Её ценность целиком в том, что её **можно прочесть**.

За 1 сентября она выросла **с 1566 до 2412 строк — на 846 за один день**
(коммит `5c7a9196`). Рост был нужный: вернули половину, писавшуюся отдельно с
22 августа, и без неё три подделки проходили кодом 0. Но:

    LC_ALL=C.UTF-8 /usr/bin/grep -rl 'сверщик\.c' scripts/ flang/proof/
    → tree-inventory.fscript, чекер/Makefile, чекер/ЧИТАТЬ.md, чекер/пробы/прогон.sh

**Ни одного места, где назван потолок.** Мерка Ч4 меряет ядро компилятора
(4534 строки по трём файлам на `884e2e42`), а сам проверяльщик не меряет никто.

Если он растёт по 846 строк в день, через неделю его не прочтёт никто, и мы
получим второй компилятор, которому опять надо верить на слово — только теперь
их два.

## Что сделать

### 1. Потолок и мерка (дёшево, делать первым)

Проверка, считающая строки кода `сверщик.c` мерой Ч4 (`--narechie c`) и
краснеющая при превышении потолка. Привязать к ярлыку.

**Потолок назначает владелец.** Сегодня 2412. Разумное — 2500 с правилом
«дальше только сокращать»; но это решение, а не замер, и выдумывать его самому
нельзя.

**Проверка, без которой работа не сдана:** дописать в `сверщик.c` строк сверх
потолка — код 1 с числом; убрать — код 0. Обе стороны прогоном.

### 2. Второй проверяльщик, написанный независимо (дорого, но без него всё шатко)

Настоящая проверка проверяльщика — когда **две реализации, написанные врозь по
спецификации, согласны на всём наборе примеров**. У Lean их две именно поэтому.

У нас одна, и она же автор своих проб. Это ровно тот изъян, который мы весь
день ловим в других местах: предмет, заверяющий сам себя.

Заметим отдельно: `flang/proof/сверщик.flang` (2165 строк) вторым **не
считается** — он написан на flang и собирается нашим же компилятором. Для
независимости нужна вторая реализация, не разделяющая с первой ни языка, ни
инструмента сборки.

### 3. Спецификация формата записи отдельно от обеих реализаций

Симптом уже случился. Формат записи вырос, а его номер версии остался **1** —
вопреки обещанию ADR-0010. При этом:

* проверяльщик на C отвалился **названно** (закрытый список видов строк);
* `сверщик.flang` новые строки шапки **молча пропустил**.

Второе и есть «читается вкривь», которого обещание не допускает. Пока формат
живёт внутри реализаций, а не отдельным описанием, такое будет повторяться.

**Сделано задачей 2907 (14 сентября 2026).** Спецификация — типы Lean
`flang/proof/lean/Запись.lean`, `ЧИТАТЬ.md` на неё ссылается. Что сверщик на C
читает запись так же, проверяет сверка вердиктов в `flang/proof/lean/прогон.sh`:
1291 блок «вывод» набора проб, расхождений 0. Второго проверяльщика (§2) это не
заменяет: чтение текста (`RecordReader.lean`) стоит вне теоремы и доверено.

Расхождения прозы и кода, найденные при моделировании:

1. `ЧИТАТЬ.md` о блоке `вывод` молчал: форма описана только в
   `docs/design/proof-object-blok-vyvoda.md` §2. Теперь есть ссылка на `Запись.lean`.
2. Основания шага. Проза (`proof-object-blok-vyvoda.md` §2): «одно из четырёх и
   только их», в таблице четыре строки, а форм пять (`сам`, `строка N`, `из N`,
   `из N M`, `из N строка M`). Сверщик в отказе печатает «их три и только три:
   «сам», «строка N», «из N [M]»» (`разобрать_шаг_вывода`), а принимает все
   пять. В `Запись.lean` у `«Основание»` пять конструкторов.
3. Строй блока. Проза: «устроен ровно как блок ходов», потолка шагов не
   называет. Сверщик берёт не больше 64 шагов, `вывод цель` — первой и одной,
   `вывод конец` — последней и одной (`проиграть_вывод`).
4. Цель блока. Проза называет одну цель — постусловие исходника. Сверщик берёт
   её из двух мест: у утверждения без теоремы — хвост строки
   `обеспечивает «имя» …` (`без_теоремы`), у блока под теоремой — хвост
   `утверждаем …` строки `цель строка N` (`сверить_теорему`).
5. Номер строки в заголовке утверждения. Имя не экранируется: в
   `утверждение «всякая строка начинается собой» функции «…» строка 139` слово
   «строка» стоит и в имени, номер читается после последнего «строка ». Проза
   об этом молчит; первый читатель Lean ошибся здесь на трёх утверждениях.
6. Выписанный список печатается `[а, б]`: перед запятой пробела нет, а скобки и
   двоеточие `терм` окружает пробелами. Проза о пробелах в формуле молчит;
   первый читатель Lean ошибся здесь на 55 блоках набора.
7. Свёртка. Хвост `свёртка Л начиная с Н как А и Э → Ш` тянется до конца терма,
   знаки внутри Ш (`плюс`) свёртку надвое не делят, скобок вокруг Ш печать не
   ставит. Порядок разбора проза не называет; читатель ошибся на 7 блоках.

Где модель и сверщик расходятся намеренно — названо в шапке `Приёмка.lean`:
у Разв2 и Э2 модель спрашивает захват имён, сверщик — нет; у П5 модель берёт
потолок не выше 2⁵³, сверщик — любой целый литерал; формулы сверщик сличает
текстом, модель — деревьями (лишние скобки модели не мешают). На наборе проб ни
одно из этих мест вердикта не развело.

## Почему это не отложить

Проверяльщик — единственное место в дереве, где размер сам по себе является
свойством, а не следствием. Везде ещё «больше строк» значит «больше работы»;
здесь «больше строк» значит **«меньше доверия»**. Мерка нужна не для порядка, а
потому что без неё нельзя заметить, что цель уходит.
