---
номер: 1404
заголовок: О последовательности шагов сказать нечем — утверждение привязано к одной функции
статус: в работе (замер, Ш0 — сверщик, Ш1 — код ядра и печати написаны; Ш1 доедет перепечаткой семени; взята Lead-K 17 сентября 2026: замер 3560 → ADR-0044 → Ш2)
исполнитель: a
ветка: a/1404-progon-sh2 (прежде r/logika, r/1404-progon)
команда: первая
карта: Чего в языке нет вовсе
рядом: 1403, 1405, 1406, 6203
---

# 1404. Один шаг доказывается, последовательность — нет

Решение: [ADR-0032](../adr/0032-one-missing-rule-and-three-other-logics.md) §3.1.

**Порог: [ADR-0026](../adr/0026-quantifiers-over-any-type-are-a-kernel-change.md) §11.3
(свободное утверждение). До него задача не начинается.**

## Чем измерено

Прогон 11 сентября 2026, двоичный `bootstrap/flang` 0.7.16, ветка
`r/logika-i-primenenie` от `gh/dev` `11afc59fb`.

**Один шаг берётся.** Функция `«Шаг счёта»`, тело `счёт плюс прибавка`,
постусловие «шаг не уменьшает счёт» (`результат не меньше счёт`) — **доказано**,
правило «порядок по построению», код 0.

**Последовательность не берётся, и написать её негде.** Приписка
`обеспечивает` к объявлению `процесс` — отказ разбора:

```
FLANG_PARSE: не разобрана конструкция: в процессе ожидаются
'состояние', 'начинает с', 'принимает' или 'обрабатывает'
```

Перечисление по дереву: объявлений `процесс` — **101**, объявлений `план` —
**222**, постусловий у них — **0**, и ни одного быть не может.

## Чего нет поимённо

`flang/self/parser.flang:4204–4215`, «Квантор постусловия»: читает **ровно одно
имя** и требует, чтобы оно было параметром функции. Значит квантор в языке один
и всегда один и тот же — «для всех входов ЭТОЙ функции». Утверждения, не
привязанного к функции, нет; значит нет и места, где говорится «при всяком шаге
прогона».

## Чего НЕ делать — это уже решено

**Временную логику не заводить.** `всегда`, `когда-нибудь`, `пока не` отвергнуты
[ADR-0017](../adr/0017-liveness-without-temporal-logic.md) после разбора трёх
настоящих утверждений дерева. Живучесть там пишется мерой, инвариантом одного
шага и названным предусловием, и это остаётся в силе.

Нужное меньше и конкретнее.

## Что сделать

1. **Сперва замер, не код.** Возьми три настоящих утверждения из `flang/concurrency/`,
   которые сегодня написать нельзя, и разбери каждое до конца — как это сделано в
   ADR-0017. Может выйти, что два из трёх складываются из имеющегося, как вышло
   тогда. Отчёт — числом и прогоном.
2. Только если после замера остаётся то, что не выражается: показать, ложится ли
   это на свободное утверждение ADR-0026 §11.3 плюс индукцию по прогону.
3. **Отдельно померить носитель индукции.** Сегодня он выбирается закрытым
   перебором из трёх (задача 6203, в работе), и прогона среди них нет. Пока 6203
   не сделана, шаг 2 оценивать нечем.
4. Назвать цену числом строк — после шагов 1–3, не раньше.

## Как поймём, что сделано

Замер шага 1 напечатан: сколько утверждений из взятых складывается имеющимся,
сколько нет, и что именно у последних не выражается. **Задача считается
выполненной и тогда, когда ответ «всё складывается имеющимся»** — это законный
исход, ровно как в ADR-0017.

## Спецификация (по замеру 11 сентября 2026, ветка `r/logika`)

### Замер шага 1 — числом и прогоном

Двоичный `bootstrap/flang` 0.7.17 (дерево `r/logika` от `gh/dev` 2c40752d0).
Три настоящих утверждения о последовательности шагов, разобранные до конца:

| утверждение | из чего складывается | исход |
|---|---|---|
| «за прогон ящик растёт ровно на число писем» (шаг — «В ящик», ADR-0017 §2; прогон — свёртка «Все в ящик» по списку писем; проба `flang/proof/чекер/пробы/семьи/прогон/pisma-po-ocheredi.flang`) | постусловие СВЁРТКИ `(длина результат) равен ((длина ящик) плюс (длина письма))` | **доказано**, «тождество после переписки допущением» — имеющимся, без новых слов |
| то же свободным утверждением с двумя связываниями (`для всех письма`, `для всех ящик`) | ADR-0026 §11 п. 3 | **доказано** — сведено к постусловию свёртки («цель есть допущение»); теорема к нему индукцией по `письма` — **отвергнута**: база — «по примеру» у утверждения без функции брать не у кого; шаг — стороны равенства после переписки остались разными термами |
| «пробег не меняет имени узла» — инвариант ОДНОГО шага планировщика, постусловие «Пробежать» в `flang/concurrency/scheduler.flang` (`(результат.«узел».«имя») равен («узел».«имя»)`) | постусловие функции-шага | **сетка**, не доказано: тело идёт через «Пробежать выбранного» → «Узел с ходом» → «С процессом», и ядро не собирает постусловия вызванных в цепочку; постусловия помощникам («имени не меняет») дописывались и доказывались, «Пробежать» это не подняло — сняты |

Итог замера: **два из трёх складываются имеющимся** (последовательность
шагов = свёртка, инвариант = постусловие свёртки), как в ADR-0017. Третье —
не новая логика, а слабость имеющейся: инвариант шага не проходит сквозь цепочку
вызовов. Прогона как отдельного носителя индукции (предположение 1 ADR-0032 §6)
НЕ нужно: список шагов — это список, и его носитель уже есть (6203 Ш0).
Чего нет: ядро не печатает блок ходов у постусловия свёртки (сверщик: «место на
слове ядра», код 3), и свободное утверждение о свёртке оно закрывает «цель есть
допущение», а не индукцией — переиграть это сверщику нечем.

### Слово и синтаксис

Нового слова НЕТ. Утверждение о последовательности пишется девятью словами
доказательств (`flang/self/lexer.flang:1220`): `утверждение «И»`, `для всех
… : список Т`, `утверждаем Ц`, где Ц зовёт функцию-прогон — свёртку по списку
шагов; теорема к нему — `индукция по` списку, `случай пусто` / `случай голова и
хвост`, обоснования `по свойству «постусловие шага»` и `по предположению`.

### Что печатает ядро (Ш1) и что сличает сверщик (Ш0 сделан, Ш2)

Запись: блок `вид statement`, под связываниями строка
`прогон «Ф» шаг «Ш» строка N` — Ф объявлена на строке N, тело её свёртка, свёртка
зовёт Ш, цель зовёт Ф. Дальше — блок ходов индукции по списку: база
(`свёртка по пустому = начало`) и шаг (постусловие Ш `по свойству` + допущение
на хвосте), как у теоремы постусловия.

- **Ш0 — сделан 11 сентября 2026** (сверщик, +19 строк кода, 6604 → 6623, в
  счёт ADR-0032). Сверщик читает строку «прогон» с исходника и отвергает
  расхождение вслух: чужой шаг, не свёртка, не та строка, цель не зовёт, не то
  место строки — пять порч, код 1 с названной причиной; честная — код 3, ни
  одной «не узнана». Семья `flang/proof/чекер/пробы/семьи/прогон/` (README там).
- **Ш1 — ядро** (партия перепечатки): `zapis.flang` печатает строку «прогон» у
  свободного утверждения, чья цель зовёт свёртку; `proof-kernel.flang` закрывает
  такое утверждение индукцией по списку свёртки, а не «цель есть допущение», и
  печатает ходы базы и шага. Правила ведомости: `Св1` (база: свёртка по пустому
  списку есть начало) и `Св2` (шаг: постусловие шага при допущении на хвосте) —
  строки в `ПРАВИЛА-ВЫВОДА.tsv` и леммы в `Правила.lean` над свёрткой списка
  (`List.foldl`) заводятся ВМЕСТЕ с кодом, как у Н12. Оценка: ≈ 60–80 строк
  flang (печать — по образцу «Шаги разности дна вывода записи», 26 строк; ядро
  — по образцу «Разность по порядку») — оценка, не замер.
- **Ш2 — сверщик переигрывает** индукцию по прогону: ≈ 40–60 строк кода C —
  оценка; цену назвать после Ш1 по эталону записи, снятому толкованием.
- **Ш3 — применение в `flang/concurrency/`**: «Прогнать узел по жребиям» (свёртка
  «Пробежать» по списку жребиев) и инвариант прогона планировщика; порог — шаг
  «Пробежать» обязан сперва пройти сам (см. третью строку замера).

### Приёмочные пробы

Честная: `pisma-po-ocheredi.flang` — после Ш1 запись ядра несёт строку «прогон»
и ходы индукции, сверщик после Ш2 даёт код 0 (сегодня 3). Подделки: пять порч
семьи «прогон» остаются кодом 1; после Ш2 — порча шага индукции («по
предположению» без допущения на хвосте) тоже 1.

### Чего задача НЕ делает

Не заводит временную логику (ADR-0017 в силе). Не делает `прогон «…» семя …`
носителем доказательства: это прогон на сетке чередований, допущение о
справедливости жребия остаётся `требует`. Не высказывает утверждений о
`процесс` и `план` как объявлениях — это 1405 и 1406. Не чинит слабость «инвариант
шага не проходит сквозь цепочку вызовов» — это отдельная заявка на правило.

### Цена числом строк

Ш0: **19 строк C — замер.** Ш1 + Ш2: 100–140 строк — оценка по образцам, не
замер; замер — после эталона записи толкованием.

## Ход работы Ш1 (13 сентября 2026, ветка `r/1404-progon` от `gh/dev` `0530e478f`)

Двоичный — `bootstrap/flang` 0.7.18, собранный начисто в этом же дереве
(`make -C bootstrap clean && make -C bootstrap -j8`).

### Имена правил: Св1 и Св2 БЫЛИ ЗАНЯТЫ — взяты Инд2 и Инд3

`Св1` в ведомости уже стоит — это «свести свидетелем» (существование внутри
правил, ADR-0026 §11 п. 15, задача 5957), и семейство `Св` означает СВИДЕТЕЛЯ, а
не свёртку. Чужое имя не переиспользовано. Правила заведены в семействе `Инд`
(индукция), где уже стоит `Инд1` — индукция по объявленной сумме:

- **Инд2** — база: свёртка по ПУСТОМУ списку есть её начало (посылок 0);
- **Инд3** — шаг: цель при звене из допущения о хвосте ПРИ НАЧАЛЕ ПОСЛЕ ШАГА
  (посылок 2: база и шаг).

Обе строки — `«—»` в колонке `правило_ядра` (как у Разв, Тожд и Св): нового имени
в поле `rule` не заводится, закрытый список имён сверщика не растёт ни на одно
имя, и С-1 остаётся зелёным без правки сверщиков. Колонка `сверено` — `нет-в-ADR`
(в §3 ADR-0022/0032 этих правил нет), поэтому счёт «правил по ADR» в стороже С-2
остался прежним — 78, и файл `сверка-ведомости.sh` править не пришлось.

### Что написано

- `flang/self/zapis.flang` (**+117 строк, −1**): печать строки
  `прогон «Ф» шаг «Ш» строка N` у свободного утверждения, чья цель зовёт
  функцию с телом-свёрткой. Строка собирается ЧТЕНИЕМ ИСХОДНИКА — тем же
  способом, каким её сверяет сверщик (Ш0): ищется объявление функции, её
  тело-свёртка, имя шага после `→`. Восемь новых функций, у каждой примеры;
  вклинена одной строкой в «Строки кванторов записи», так что строка встаёт
  сразу под связываниями — там, где её ждёт таблица мест сверщика.
- `flang/self/proof-kernel.flang` (**+375 строк, −2**): два вычислительных
  равенства свёртки («Свёртка по пустому», «Свёртка по звену») и сведение
  «Свести индукцией по прогону»: оно строит цель базы, цель шага и допущение на
  хвосте и доказывает обе цели обычными правилами при тех же фактах. Вклинено
  двумя строками в «Свести правилами при фактах» — перед правилом «цель есть
  допущение» и перед последней ступенью цепочки. Правка МОНОТОННА: не приняв
  индукцию, ядро возвращается к прежнему ответу, и ни один сегодняшний вердикт
  не теряется.
- `flang/proof/ПРАВИЛА-ВЫВОДА.tsv` (**+2 строки**) и отпечаток рядом;
  `flang/proof/lean/Правила.lean` (**+75 строк**): леммы «Инд2», «Инд3»,
  «Инд3-шаг-свёртки» и два встречных примера — без базы и без шага.
- новая семья проб `flang/proof/чекер/пробы/семьи/прогон-индукция/`
  (`письма.фп`, `письма.запись`, `README.md`).

Итого **492 строки** правок в двух файлах ядра и печати против оценки автора
задачи в 60–80 строк. Разница названа, а не спрятана: оценка считала одну печать
и один случай ядра, а сведение потребовало ещё и подстановки доводов вызова,
замены узла и разбора вызова-прогона в цели.

### Прогоны (все — в этом дереве, двоичный 0.7.18)

| прогон | до правок | после |
|---|---|---|
| `sh flang/proof/сверка-ведомости.sh` | сошлось всё, 97 строк | **сошлось всё, 99 строк**, шесть сторожей зелены |
| `LEAN=… sh flang/proof/lean/прогон.sh` | строк 97, лемм 105 | **строк 99, лемм 110**, ловушка 14/14, сошлось всё |
| `sh flang/proof/чекер/пробы/прогон.sh` | сошлось всё | **сошлось всё** (167/135/366; подделок 453 — принято 0, честных 215 — отвергнуто 0) |
| `sh scripts/доказуемость.sh` | ДОКАЗУЕМ | **ДОКАЗУЕМ** (доля 96.62 %) |
| `flang check flang/self/zapis.flang` | замечаний нет | **замечаний нет** |
| `flang check flang/self/proof-kernel.flang` | замечаний нет | **замечаний нет** |

### Чего Ш1 НЕ сделал и почему

1. **Ходы базы и шага в записи НЕ печатаются.** Первичных ходов у сверщика ровно
   тринадцать, и список закрыт (`сверщик.c`, `ход_шага`); незнакомый ход роняет
   ЧЕСТНУЮ запись кодом 1, а не оставляет её на слове ядра. Переигрывание
   индукции по прогону — это Ш2, и печать ходов до него сделала бы красным весь
   набор проб. Порядок сохранён тот же, каким шли Ш0 и Пред1: сверщик раньше
   ядра. Печать ходов — первая работа Ш1-бис, сразу после Ш2.
2. **Ничего из написанного в `flang/self/**` не проверено В ДЕЛЕ.** Правка
   доходит до собранного компилятора только перепечаткой семени (около девяти
   часов), и до неё двоичный печатает и сводит по-старому. Проверено то, что
   проверяется без перепечатки: разбор, типы, завершаемость и ПРИМЕРЫ обоих
   файлов (`flang check`), ведомость, Lean, набор проб и гейт.
3. **Закроется ли индукцией акцептная проба — НЕ ПРОВЕРЕНО и под сомнением.**
   Шаг индукции упирается в переписку `(длина Н плюс 1) плюс длина Х` против
   `длина Н плюс (1 плюс длина Х)`, а у ядра стоит названная граница: мера в
   стороне равенства обязана быть ОДНА («Предел слагаемых», «Счёт постоянных при
   одной мере»). Не закроется — ядро вернётся к прежнему вердикту «цель есть
   допущение», и потери не будет; закрыть эту переписку — отдельная заявка на
   правило, и она названа здесь, а не выдана за сделанное.
4. **Запись новой семьи проб снята ДО перепечатки** (`--записать`, 0.7.18):
   строки «прогон» в ней нет и вердикт прежний. Она положена как «до», чтобы
   после перепечатки разницу было видно `diff`-ом.
5. **Сторож `scripts/guards/no-comments-guard.sh` красен, и красен НЕ мной.**
   Замер: в `proof-kernel.flang` строк `//` на `HEAD` 3717 и сейчас 3717 (в
   ведомости долга записано 3655), в `zapis.flang` — 650 и 650 при долге 351; то
   же расхождение у `proof.flang` и `types.flang`, которых я не касался. Мои
   правки не добавили ни одной строки `//` (пояснения написаны блочными `/* */`,
   которые сторож не считает). Ведомость долга `scripts/ledgers/` — вне моей
   зоны записи, и я её не трогал.
6. **Примета величины ведомости в `сверка-ведомости.sh` устарела**: в
   комментарии сказано «строк файла 199», стало 201. Сам сторож этого не
   проверяет (примета — комментарий), но число разошлось; файл вне моей зоны
   записи, правка — за тем, кто ведёт сверщика.
7. **Два имени пришлось сменить: они были заняты, и компилятор это назвал.**
   `«Тело прогона»` уже объявлено в `flang/self/parser.flang`, `«Отказ прогона»` —
   в `flang/self/interpret.flang` (`FLANG_DUPLICATE_NAME`, по два замечания и по
   три наведённых ошибки типов на каждое). Переименованы в `«Свёртка определения
   прогона»` и `«Отказ индукции по прогону»`; чужих объявлений не тронуто.
   Замер: без переименования `flang check` давал 13 замечаний, после — ни одного.
8. **Сторож `flang/scripts/name-guard.mjs` краснеет, и краснеет НЕ мной.**
   Прогон (`node flang/scripts/name-guard.mjs`, код 1): «31 имён снято;
   перепишите долг: --debt». Ни одного НОВОГО имени он не называет, и ни одной
   строки про мои файлы: во всех 16 строках вывода — `docs/examples/**`, которых
   я не касался. Красное здесь значит «долг УМЕНЬШИЛСЯ, перепишите число», а
   ведомость долга лежит вне моей зоны записи. Звать его без доводов и с открытым
   вводом нельзя: так он висит (замер — 21 минута при 4 секундах процессорного
   времени), нужен `</dev/null`.

## ⚑ 17 сентября 2026: замер (задача 3560)

Ветка `a/3560-logic-in-time-count` над `main` `a43ab4d25`, двоичный `bootstrap/flang` 0.7.19,
1001 файл под учётом git; числа, прибор и места поимённо — [ADR-0044](../adr/0044-time-effects-and-processes-are-counted-before-they-get-rules.md) §2–3.

**Функций-прогонов** (тело — свёртка по списку, шаг — именованная функция того же файла)
без `flang/self` — **256**; свёрток в телах функций 1623, из них с шагом-функцией 590.

**M1 = 133** — прогонов, у которых шаг несёт постусловие, то есть инвариант прогона
выводится правилами Инд2/Инд3, а сверщик этого не переигрывает: `flang/stdlib` 101,
`flang/proof` 10, `flang/concurrency` 6, `docs/examples` 6, `scripts/guards` 6,
`flang/scripts` 3, `flang/test` 1. У **28** из них у самого прогона постусловия нет вовсе.
Свободных утверждений о прогоне записано **2** — обе пробы этой задачи
(`pisma-po-ocheredi.flang:34`, `прогон-индукция/письма.фп:26`); в живом коде — 0.
Корпус: прогон — в 2 источниках из 89.

Поимённо: `flang/stdlib/automaton.flang:305` «Прогон с места» (шаг «Шаг бега» — «после
первого же шага начало пройдено»); `flang/concurrency/supervisor.flang:302` «Применить к
затронутым» (шаг «Применить к одному» — длина + 1; хочется «ровно столько, сколько
затронутых» — форма пробы «письма»); `flang/concurrency/scheduler.flang:741` «Отклик
разобран»; `docs/examples/allocator/allocator.flang:135` «Годная куча» (шаг «Шаг годности» —
«ГРАНИЦА НЕ ПЯТИТСЯ»); `docs/examples/leetcode/035-search-insert-position.flang:20` «Место
вставки». Спецификация выше называла «Прогнать узел по жребиям» — такой функции в дереве
нет; свёртка «Пробежать» по жребиям не пишется, Ш3 берёт «Применить к затронутым».

**Цена Ш2 по ADR-0044 §4.1:** сверщик **+80** строк C (аналог Инд1 — 78, замер) из строки
бюджета ADR-0044 (+115); Ш1-бис (печать ходов) ≈ 30 строк `zapis.flang` (аналог 27,
замер); ведомость и леммы — 0 новых (Инд2/Инд3 стоят), 2 случая приёмки Lean. Порядок —
**первый шаг кода**: единственный без перепечатки, и без него Ш1 ходов печатать не может.
