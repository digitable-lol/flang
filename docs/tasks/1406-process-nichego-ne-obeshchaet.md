---
номер: 1406
заголовок: Разделения владения в языке нет — про два процесса нельзя сказать, что они не трогают одно и то же
статус: в работе (Ш0 написан 13 сентября 2026, в двоичный приедет перепечаткой семени; взята Lead-K 17 сентября 2026: замер 3560 → ADR-0044 → Ш1)
исполнитель: a
ветка: a/1406-vladenie-sh1 (прежде r/1406-vladenie от gh/dev 0530e478f)
команда: первая
карта: Чего в языке нет вовсе
рядом: 1403, 1404, 1405
---

# 1406. Процессы есть, утверждений о них нет

Решение: [ADR-0032](../adr/0032-one-missing-rule-and-three-other-logics.md) §3.3.

**Порог: [ADR-0026](../adr/0026-quantifiers-over-any-type-are-a-kernel-change.md) §11.3
(свободное утверждение). До него задача не начинается. Это самая дорогая из
четырёх работ — берись последней.**

## Чем измерено

Перечисления и прогоны 11 сентября 2026, двоичный `bootstrap/flang` 0.7.16,
ветка `r/logika-i-primenenie` от `gh/dev` `11afc59fb`.

**Форма есть, и она богатая.** `процесс` с полями `состояние`, `начинает с`,
`принимает`, `обрабатывает`; надзор, стратегии, почтовые ящики — `flang/concurrency/`.
Объявлений `процесс` в дереве — **101**. Постусловий в шести файлах `flang/concurrency/`
— **216** строк `обеспечивает` (`scheduler` 59, `supervisor` 55, `wire` 40,
`link` 32, `hotswap-node` 21, `node-benchmark` 9), и они доказываются.

**Но все они — о функциях-шагах, ни одного о процессе.** Приписка
`обеспечивает` к объявлению `процесс` — отказ разбора: «в процессе ожидаются
'состояние', 'начинает с', 'принимает' или 'обрабатывает'».

**Главного понятия нет вовсе: разделения владения.** Слов «владение» в
`flang/self/` — **0**. Сказать «эти два процесса не трогают одно и то же
состояние» нечем.

## Почему это не пустяк, хотя сегодня и не вредит

Сегодняшнее устройство делает разделение истинным по построению: состояние
процесса меняет только его обработчик, общей памяти нет. Но **истинное по
построению и доказанное — разные вещи.** Ни одно утверждение дерева этого не
высказывает, и если завтра появится общее состояние, ни одна проверка не
покраснеет.

## Что сделать

1. **Сперва замер, не код.** Проверить прогоном предположение ADR-0032 §6.3:
   действительно ли сегодня ни один процесс не может тронуть чужое состояние.
   Это предположение об устройстве, и оно ничем не проверено.
2. Если да — **закрепить проверкой**, а не утверждением языка: работа дешёвая и
   даёт почти всю пользу. Проверка обязана краснеть на первом же общем
   состоянии.
3. Только после этого разбирать, нужна ли логика разделения владения как вид
   утверждения, и какие настоящие утверждения дерева её требуют.
4. Назвать цену числом строк — после шагов 1–3.

## Чего не делать

- Не начинать с логики разделения. Шаг 2 может закрыть настоящий риск за долю
  цены, и не выяснив этого, браться нельзя.
- Не путать с живучестью: «упавший процесс будет перезапущен» уже разобрано и
  закрыто [ADR-0017](../adr/0017-liveness-without-temporal-logic.md) без
  новых слов языка.

## Как поймём, что сделано

Напечатан ответ по шагу 1 прогоном; если ответ «не может» — в дереве стоит
проверка, которая краснеет на подложенном общем состоянии, и подлог этот
положен рядом с ней.

## Ход работы Ш0 — 13 сентября 2026

Ветка `r/1406-vladenie` от `gh/dev` `0530e478f`, двоичный `bootstrap/flang` **0.7.18**,
собран начисто в этом дереве (`make -C bootstrap clean && make -C bootstrap -j8`).

### Правило встало НЕ туда, где его ждала спецификация

Спецификация выше называет местом `flang/self/conc.flang`, «семейство „Беда процесса“,
рядом с „Код приёма“». Прогоном по дереву: семейства «Беда процесса» в `conc.flang`
**нет**; и «Беда процесса», и «Код приёма» живут в `flang/self/processes.flang` —
это и есть суд над объявлениями `процесс`. В `conc.flang` «Код приёма» тоже есть
(строка 184), но там это КОД ОТКАЗА ПЛАНИРОВЩИКА при доставке, а не правило проверки.
Правило написано там, где судятся объявления, — в `processes.flang`; `conc.flang` не тронут.

### Что написано

- **Правило десятое** (`flang/self/processes.flang`, +91 строка): «Обработчик процесса»,
  «Типы доводов», «Первое имя типа», «Второе имя типа», «Беда владения», «Беды владения»
  плюс одна строка в «Суд над объявлениями». Отказ идёт кодом `FLANG_PROCESS` (форма
  объявления) — нового кода не заведено, состав правил у сторожа не сдвинут.
  Ловит три расхождения: доводов не два; первый довод не тот тип, что `состояние`;
  второй довод не тот тип, что `принимает`. Обработчик, которого в программе нет,
  правило молчит — о нём жалуется правило третье.
- **Проверка на flang** (`flang/test/владение-состоянием.flang`, 113 строк) —
  шесть утверждений, гоняются обходчиком `flang/test/обход.sh` на каждый пуш.
- **Сторож по всему дереву** (`flang/test/владение-состоянием.sh`, 122 строки) —
  тем же правилом, но через `flang ast`: работает УЖЕ СЕГОДНЯ, до перепечатки семени.
  Двусторонний: подлоги названы списком, каждый обязан быть пойман.
- **Подлог-применение** (`flang/concurrency/examples/distributed-foreign-state-forged.flang`,
  67 строк) — «Учётчик» с состоянием «Записи», а его «шаг учёта» принимает «Счёт».

### Чем показано, что правило краснеет ПО ДЕЛУ

| прогон | ответ |
|---|---|
| `flang run … --function «Ведомость владения состоянием»` | 6 из 6 «да» (честный — молчит, три подлога — по одной жалобе, текст жалобы назван знак в знак) |
| то же со СНЯТЫМ правилом (все три ступени закорочены) | 4 из 6 «НЕТ» — правило держит эти четыре утверждения, а не они его |
| `sh flang/test/владение-состоянием.sh` | файлов с процессами 44, процессов 94, обработчиков 93, подлогов названо 3, поймано 3, расхождений вне подлогов 0 |
| `flang check` подлога `distributed-foreign-state-forged.flang` двоичным 0.7.18 | код 2, «проверено НЕ ДО КОНЦА» — та же дыра, что у подлогов 13 и 14 |

Замер 94/93 расходится с записанным 11 сентября 101/100: здесь считаются объявления
`процесс` в РАЗБОРЕ (`flang ast`), а не вхождения слова «процесс» в текст — строка
`процесс «X» стратегия …` внутри `надзор` объявлением не является.

### Чего Ш0 НЕ делает, и это названо

Правило **не работает в собранном двоичном** и не заработает до перепечатки семени:
`flang/self/**` доезжает до `bootstrap/flang` только ею. Поэтому подлоги
`13-*.flang`, `14-*.flang` и `distributed-foreign-state-forged.flang` сегодня по-прежнему
принимаются кодом 2 — дыра названа, а не закрыта. До перепечатки стережёт сторож на
оболочке.

Текст «сверено НЕ ДО КОНЦА» у `flang check` про новое правило не знает: строка эта
живёт вне `flang/self/processes.flang`, и её правку Ш0 не делал.

### Прогоны на сдаче

- `bootstrap/flang check flang/self/processes.flang` — «проверено … замечаний нет» (17 с);
- `bootstrap/flang check flang/test/владение-состоянием.flang` — то же (36 с);
- `bootstrap/flang check flang/self/conc.flang` — то же (116 с), файл не менялся;
- `sh scripts/доказуемость.sh` — **ДОКАЗУЕМ** (до правки и после);
- `sh flang/proof/checker/tests/run.sh` — **сошлось всё**;
- `sh flang/test/обход.sh` — чисто, проверок 211 (было 205, прибыло 6 моих).

Сторож `scripts/guards/no-comments-guard.sh` был красным ДО этой работы (67 файлов);
ни одного комментария в `.flang` эта работа не прибавила.

## Спецификация (по замеру 11 сентября 2026, ветка `r/logika`)

### Замер шага 1 — числом и прогоном

Перечисление по дереву (все `*.flang`): объявлений `процесс` — **101**;
обработчиков, найденных в том же файле, — **100** (один отсутствует нарочно —
`flang/test/fixtures/processes/02-handler-not-declared.flang`); у всех 100
ровно два довода, и первый — объявленное `состояние` процесса: **100 из 100**,
расхождений 0. Чужого состояния не принимает ни один обработчик, общего
состояния в языке нет (функции чистые). Предположение 3 ADR-0032 §6 в дереве
**верно**.

**Но проверкой это не стережётся.** Прогон двоичным `bootstrap/flang` 0.7.17
двух подлогов на `flang/test/fixtures/processes/00-whole.flang`:

| подлог | что сделано | `flang check` |
|---|---|---|
| обработчик над чужим типом | `процесс «Работник»` объявляет `состояние «С»`, а «шаг» принимает `«т»: «Д»` | **принято** — код 2 («проверено НЕ ДО КОНЦА»), тот же, что у честной `00-whole.flang` |
| третий довод — чужое состояние | «шаг» принимает `«т»: «С», «чужое»: «С», «м»: «Т»` | **принято** — код 2, тот же, что у честной |

Компилятор говорит об этом сам: «объявления эти сверены НЕ ДО КОНЦА —
processes, supervisors, runs: сверено, что имена сходятся, что у нетотального
обработчика назван запас витков…». Подпись обработчика с объявлением процесса
не сличается. Подлоги лежат в `flang/test/fixtures/processes/13-*.flang` и
`14-*.flang` с пометкой «сегодня принимается».

### Слово и синтаксис

Нового слова НЕТ: разделение владения в языке уже записано формой процесса —
состояние есть первый довод обработчика и только он. Логика разделения не
нужна ни одному утверждению дерева (216 постусловий `flang/concurrency/` — все о шагах).

### Что проверяет компилятор (Ш0), что печатает ядро и сличает сверщик (Ш1)

- **Ш0 — проверка, которая краснеет** (`flang/self/conc.flang`, семейство «Беда
  процесса», рядом с «Код приёма»): обработчик из `обрабатывает` принимает РОВНО
  два довода, первый — тип `состояние` процесса знак в знак, второй — тип
  `принимает`; иначе отказ «обработчик «h» процесса «P» принимает «Д», а
  состояние процесса — «С»» / «принимает 3 довода — положено два: состояние и
  письмо». Едет партией перепечатки; до неё — сторож на оболочке
  `scripts/process-handler-own-state.sh` тем же перечислением, что замер выше
  (101 / 100 / 0), с подлогом. Оценка ≈ 20 строк flang + 30 строк sh.
- **Ш1 — ядро печатает, сверщик сличает**: строка записи `процесс «P» состояние
  «С» обработчик «h» строка N` на каждый процесс; сверщик читает объявление и
  подпись обработчика с исходника и отвергает расхождение — те же два подлога
  кодом 1 с названной причиной. Правило `Вл1` ведомости: состояние процесса
  меняет только его обработчик, потому что никакая другая функция его не
  принимает; лемма — о форме, не о числах (принадлежность довода). Оценка ≈ 25
  строк C.
- **Ш2 — применение**: `flang/concurrency/examples/distributed.flang` (два процесса,
  «Счётчик» и «Учётчик») — честная; подлог — «шаг учёта» принимает «Счёт».

### Приёмочные пробы

Честные: `00-whole.flang`, `distributed.flang` — как сегодня (код 2, «проверено НЕ
ДО КОНЦА»). Подлоги 13 и 14 — после Ш0 отказ с названной причиной (сегодня —
тот же код 2, что у честной: известная дыра, и она названа).

### Чего задача НЕ делает

Не заводит логику разделения владения — по замеру она не нужна. Не путает с
живучестью (ADR-0017). Не утверждает ничего о порядке писем — это 1404.

### Цена числом строк

Ш0 ≈ 20 flang + 30 sh, Ш1 ≈ 25 C — оценки по образцу 6203 Ш0, не замер.

## ⚑ 17 сентября 2026: замер (задача 3560)

Ветка `a/3560-logic-in-time-count` над `main` `a43ab4d25`, двоичный `bootstrap/flang` 0.7.19,
1001 файл под учётом git; числа, прибор и места поимённо — [ADR-0044](../adr/0044-time-effects-and-processes-are-counted-before-they-get-rules.md) §2–3.

**Процессов** `процесс «` — **94** (`flang/concurrency` 45, `flang/test` 41, `docs/examples`
8); разбор `flang ast` даёт те же 94 (`sh flang/test/владение-состоянием.sh`: файлов 44,
процессов 94, обработчиков 93, подлогов названо 3 — поймано 3, расхождений вне подлогов 0).

**M3 = 93** — процессов, чей обработчик объявлен в том же файле; для каждого запись Ш1
печатает `процесс «P» состояние «С» обработчик «h» строка N`, и ни у одного этой строки
сегодня нет. Один без обработчика — нарочно (`02-handler-not-declared.flang`). Корпус:
процессов 0 из 89. Постусловий `обеспечивает` в `flang/concurrency` — 287 (было 216), все
по-прежнему о функциях-шагах.

Поимённо: `flang/concurrency/examples/distributed.flang:26` «Счётчик» → «Счёт» → «шаг
счёта» и `:64` «Учётчик» → «Записи» → «шаг учёта» (честная Ш2);
`distributed-foreign-state-forged.flang:31` — подлог, сегодня код 2;
`docs/examples/service-on-processes/service.flang:36`, `:42`, `:48` — три процесса, три
состояния; `docs/examples/web/shortener/server.flang:32`, `:38`, `:44`;
`flang/concurrency/examples/backpressure.flang:14` «Насос» и `:45` «Сток» — оба со
состоянием типа «Счёт»: правило Вл1 — о типе довода, не об экземпляре (ADR-0044 §8).

**Цена Ш1 по ADR-0044 §4.2:** `zapis.flang` ≈ 20 (аналог «Строка объявления записи» — 16,
замер); сверщик **+35** строк C (аналоги 19 замер + 14) из строки бюджета ADR-0044 (+115);
ведомость +1 (Вл1), лемма и ловушка Lean; семья проб `процесс` — 1 честная + 3 порчи.
Порядок — второй шаг кода: самый дешёвый и закрывает дыру с принятым подлогом.
