---
номер: 1412
заголовок: Правил кольца в ведомости нет — есть два тождества одного узла
статус: в работе (26 сентября 2026: три закона закрыты запретами, пять правил упираются в 1908)
исполнитель: a
ветка: a/1412-pravila-kolca от gh/dev 2363d95d3
команда: первая
карта: Где именно упирается доказатель
рядом: 1411, 1413, 6130
---

# 1412. Пять правил кольца — только для нового типа

Решение: [ADR-0036](../adr/0036-an-exact-integer-is-a-new-kind-of-value-not-a-new-name.md) §3 и §11 п. 4.

**Порог: [1411](completed/1411-nositel-tochnogo-celogo-eto-novyy-vid-znacheniya.md).
До выбранного представления задача не начинается.**

## Чем измерено

Прогон `flang check --proof --строго` 13 сентября 2026, двоичный 0.7.19. Пять
законов кольца, записанных постусловиями при параметрах `целое`:

| закон | исход |
|---|---|
| коммутативность | **доказано** («тождество после переписки допущением») |
| ассоциативность | объявлено, не доказано |
| дистрибутивность | объявлено, не доказано |
| нейтральный ноль | объявлено, не доказано |
| обратный по сложению | объявлено, не доказано |

Итог: `утверждений 5: доказано 1, объявлено, не доказано 4`. **Замена типа на
`сотых` и на `неотрицательное` даёт тот же счёт 1 из 5** — носитель у всех трёх
один и тот же double.

**Чего нет в ведомости поимённо.** `flang/proof/tables/inference-rules.tsv` (199 строк:
85 правил и 12 запретов) несёт семейство Тожд из двух строк: Тожд1 — перестановка
операндов ОДНОГО узла, Тожд2 — нейтральный ноль с ОБЯЗАТЕЛЬНОЙ посылкой `0 < Т`
(без неё правило ложно на минус нуле). Ассоциативности, дистрибутивности,
обратного по сложению и нейтральной единицы нет ни строкой, и шапка ведомости
говорит почему: «`плюс` на `число` в IEEE-754 не ассоциативен, значит моноида
нет».

## Что сделать

1. Завести пять строк ведомости и пять лемм в `flang/proof/lean/Rules.lean`
   (там сегодня 105 лемм) — **вместе с кодом**, как заведены Тожд1 и Тожд2.
2. Каждое правило ограничить новым типом. **На `число` они не распространяются**,
   и проба §15 п. 2 решения это стережёт.
3. Деление и остаток в правила НЕ вносить: кольцо целых не поле. Сказать это в
   тексте отказа, а не умолчать.
4. Попросить строку бюджета в `flang/proof/checker/ratchet.txt` (потолок 7188).
   Оценка по образцу ADR-0029 — +150…+250 строк кода сверщика; **это оценка, и
   настоящая цена называется после эталона записи.**

## Как поймём, что сделано

Проба из пяти законов на новом типе даёт `доказано 5`; та же проба на `число`
даёт `доказано 1`; программа, объявляющая ассоциативность для `число`, кода 0 не
получает. У каждой новой строки ведомости колонка «опора» называет живую строку
ядра, и сторож С-1 это подтверждает.

## Чего задача НЕ делает

Не обещает роста доли корпуса: новый тип в корпусе не используется, пока на нём
ничего не написано (ADR-0036 §11 п. 7). Не трогает правил о `число`.

## Чем кончилось

Приём задачи — «проба из пяти законов на новом типе даёт `доказано 5`; та же проба на
`число` даёт `доказано 1`». Прогон 22 сентября 2026: пробы в дереве нет
(`git ls-files | grep -i 'ring-rules|kolca|кольц'` находит только саму задачу), нового типа
тоже (см. 1908). Не сделана; ветка `a/1412-ring-rules-exact-integer` на сервере не живёт.

## Ход работы

### 26 сентября 2026: замер, три запрета, одна поправка к ADR-0036

Дерево `/srv/tmp/dokazuemyy/wKolco`, ветка `a/1412-pravila-kolca` от `gh/dev`
`2363d95d3`; двоичный `bootstrap/flang` 0.7.22 собран начисто в этом дереве
(`make -C bootstrap -j8`), sha256 начинается `c3c430f4e73c`. **`flang/self/**` не
тронут ни на байт** — печать семени не запускалась.

**Замер ДО.** Шесть законов кольца свободными утверждениями при `целое`
(`flang check --proof`, код 3):

| закон | исход |
|---|---|
| перестановка `плюс` | **доказано**, правило «тождество после переписки допущением» |
| перестановка `умножить` | **доказано**, то же правило |
| сочетательность `плюс` | объявлено, не доказано |
| распределительность | объявлено, не доказано |
| нейтральный ноль (без посылки) | объявлено, не доказано |
| нейтральная единица | объявлено, не доказано |
| обратный по сложению | объявлено, не доказано |

Итог прогона: `утверждений 7: доказано 2, объявлено, не доказано 5`. **Замена
типа на `число` и на `неотрицательное` даёт ровно тот же счёт 2 и 5** — тремя
отдельными прогонами, носитель у всех трёх один.

**Почему законы нельзя записать функциями при `целое`** — прогон:
`функция «Сумма целых» принимает «а»: целое, «б»: целое возвращает целое`, тело
`а плюс б` → `FLANG_TYPE: функция «Сумма целых» объявлена как целое, а тело даёт
число`. То же слово в слово у `неотрицательное`. Сегодняшнее `целое` не замкнуто
относительно сложения, поэтому законы пишутся утверждениями, а возврат у функций
из §3 ADR-0036 — `число`.

**Находка 1: один закон из пяти закрывается сегодня, без единой строки кода.**
Правило Тожд2 («нейтральный ноль») требует посылки `0 < Т` — без неё закон ложен
на минус нуле. Проба §3 ADR-0036 и таблица «Чем измерено» выше написали закон БЕЗ
посылки и получили «объявлено, не доказано»; это замер ЗАПИСИ закона, а не набора
правил. Те же прогоны с посылкой:

* `для всех а: целое таких что а больше 0` → **доказано**;
* то же при `неотрицательное` → **доказано**;
* то же при `число` → **доказано**;
* формой §3 ADR-0036 (постусловие функции плюс `требует «слагаемое строго
  положительно» а больше 0`) → **доказано**.

Счёт таблицы ADR-0036 §3, записанной с посылкой, — **2 из 5, а не 1 из 5**.
Поправка вписана в ADR-0036 §3 датой и прогоном.

**Находка 2: три закона из пяти на этом носителе ЛОЖНЫ, и ложность снята
прогоном, а не рассуждением** (`flang run`, двоичный 0.7.22):

| закон | встречный пример | ответ |
|---|---|---|
| сочетательность | `((0.1 плюс 0.1) плюс 0.6) равен (0.1 плюс (0.1 плюс 0.6))` | `false` |
| распределительность | `(0.1 умножить на (0.1 плюс 0.3)) равен ((0.1 умножить на 0.1) плюс (0.1 умножить на 0.3))` | `false` |
| обратный по сложению | `(бесконечность плюс (0 минус бесконечность)) равен 0`, где `бесконечность равно 1 делить на 0` | `false` |

Первые два ломает ОКРУГЛЕНИЕ, третий — БЕСКОНЕЧНОСТЬ. Поэтому правилами они быть
не могут ни при каком типе над сегодняшним носителем, и в ведомости им место —
запрет, а не молчание. Заведены **Тожд✗**, **Тожд✗✗**, **Тожд✗✗✗**
(`flang/proof/tables/inference-rules.tsv`): ведомость 233 → 257 строк файла,
правил 97 без изменений, запретов 12 → 15, отпечаток переснят руками
(`flang/proof/tables/inference-rules.digest`).

**Находка 3: шестой закон — нейтральная единица — на сегодняшнем носителе ВЕРЕН,
и правила у него нет.** Прогон `flang run` в трёх неудобных точках:
`(минус ноль умножить на 1) равен минус ноль` → `true`;
`(не число умножить на 1) равен не число` → `true` (в языке `равен` — `Object.is`);
`(бесконечность умножить на 1) равен бесконечность` → `true`. Ни правилом, ни
запретом он не записан: запрет был бы ложью, правило требует кода ядра (список
мест ниже).

**Пробы — две на каждый запрет, и обе прогоняются набором проб сверщика.**
Заведены в `flang/proof/checker/tests/families/statement/`:

* `ring-laws-provable.{flang,record}` — честная: Тожд1 дважды и Тожд2 на `целое`,
  `доказано 3 из 3`, сверщик — код 0, «сведений проиграно заново 3 (ходов
  проверено 6)»;
* `ring-laws-banned.{flang,record}` — честная и пустая по существу: три
  запрещённых закона (ядро — код 3, сверщик — код 0 «ПРОВЕРЕНО ВПУСТУЮ») плюс три
  функции со встречными примерами; **примеры считает вычислитель при каждой
  проверке** — подменил ожидание с `нет` на `да`, и файл краснеет тремя
  `FLANG_EXAMPLE` (проверено);
* три подделки `corrupt/ring-*.record` — запись объявляет закон доказанным ходом
  «соседи» либо «нейтральный ноль»; сверщик отвергает все три кодом 1 с названной
  причиной («стороны равенства разные», «ни одно слагаемое не литерал 0»).

**Lean — прогнан целиком, а не на слово.** Три встречных примера —
`«Тожд-запрет»`, `«Тожд-запрет2»`, `«Тожд-запрет3»` в
`flang/proof/lean/Rules.lean`; три искажения (запрет, переписанный правилом) —
58, 59, 60 в `flang/proof/lean/Trap.lean`. `sh flang/proof/lean/run.sh` при
`LEAN=~/.elan/bin/lean` (в PATH его нет, и `scripts/доказуемость.sh` честно
пишет «здесь не гонялся») — **код 0, «сошлось всё»**:

* сборка восьми файлов — без единого сообщения;
* аксиомы «состоятельность» — три стандартные, сверх них ни одной;
* ловушка — искажений 55 (было 52), Lean отверг 55;
* перечень — строк 112, из них с леммой **112**, без леммы **0**: три новых
  запрета лемму получили, покрытие не просело;
* сверка вердиктов C ↔ Lean — пар с блоком вывода 248, блоков 1491,
  **расхождений 0**. Сочетательность в модели
ломается тем же, чем в языке, — округлением: `«Грубо»` теряет единицу выше 2⁵³,
и встречный пример взят при `2⁵³, 1, 1`. У распределительности встречный пример
модели ДРУГОЙ, чем в языке (дробей модель не знает): бесконечность на ноль.

**Числа набора после правки** (`sh flang/proof/checker/tests/run.sh`, код 0):
подделок 534 → **537**, принято кодом 0 — 0; честных 248 → **250**, отвергнуто 0.
Храповик `flang/proof/forgeries/ratchet.txt` поднят 533 → 537 с доводом.
Вердикт `sh scripts/доказуемость.sh` — **ДОКАЗУЕМ, код 0** и до, и после; доля
проигрыванием 100,00 % (650 из 650) не сдвинулась: пробы семьи в корпус доли не
входят. `sh .githooks/pre-push` — код 0, шестнадцать сторожей зелены (393 с).
Шесть сторожей ведомости (`sh flang/proof/tables-guard.sh`) — код 0: «сошлось
всё: 112 строк ведомости».

**Оговорка о командах приёмки.** `sh flang/proof/check.sh` без доводов не
вердикт, а подсказка: этот скрипт сверяет ОДНУ пару «исходник + запись» и без
двух доводов выходит кодом 2. Набор проб целиком зовётся
`sh flang/proof/checker/tests/run.sh`; им и снят счёт выше, а `check.sh` прогнан
по каждой из пяти новых пар порознь.

### Что упирается в ядро и НЕ сделано

**1. Пять правил кольца на точном целом — порог не задача 1412, а 1908.** Нового
типа в дереве нет (см. «Чем кончилось» задачи 1908: `grep 'точное целое'` по
`lexer.flang`, `emit-c.flang`, `emit-go.flang` — ноль в каждом). Пока носителя
нет, правила писать не над чем, а над сегодняшним носителем три из пяти ЛОЖНЫ —
они и стоят запретами. Эти три запрета будущему точному целому не мешают: в
строках ведомости сказано прямо, что запрет о НОСИТЕЛЕ, а не о законе.

**2. Нейтральная единица — единственное правило, которое верно уже сегодня, и
оно упирается ровно в шесть мест.** Всё, кроме двух последних, — печатаемая часть
семени:

| файл и строка | что там делать |
|---|---|
| `flang/self/proof-kernel.flang:6126` «Свести равенство», дизъюнкция на `:6176` | шестой случай рядом с пятью существующими |
| `flang/self/proof-kernel.flang:9038` «Слагаемое без нуля», `:9049` «Нейтральный ноль сошёлся» | близнец на единицу; посылки, как у Тожд2, не нужно — закон верен на всём носителе |
| `flang/self/zapis.flang:2065` «Слагаемое без нуля записи», печать хода `:2172–2178` | новый ход в записи |
| `flang/proof/checker/checker.c:1506–1525` и `:1541` | чтение нового хода; закрытый список ходов («первичных ходов семь») |
| `flang/proof/checker.flang:1784` | тот же закрытый список второго сверщика |
| `flang/proof/tables/inference-rules.tsv` и `flang/proof/lean/Rules.lean` | строка правила и лемма — **с посылкой представимости**: в модели `кон n` допускает n сверх 2⁵³, и округление такое n двигает |

Ядро правится толкованием `flang/self` (записи семьи так и снимались до
перепечаток), но правка эта — в печатаемой части семени, и по условию задачи не
делалась.

**3. Бюджет сверщика, о котором просит пункт 4 задачи, сегодня НЕ ВЛЕЗАЕТ.** Замер
`sh flang/proof/tables-guard.sh --строки-кода`: в `checker.c` **7593 строки кода
при потолке 7603** — запас **10 строк**, а оценка задачи «+150…+250». Потолок
двигает строка в ADR, и её придётся просить ДО работы, а не после. Три запрета
этой ветки сверщика не тронули вовсе: запрет — это отсутствие случая, читать
нечего.

### Своя ошибка

Сторож чисел в прозе я сперва прогнал не в своём дереве: команда была
`cd wKolco && nohup … & sh scripts/guards/prose-numbers-guard.sh`, и `&`
отправил в фон всю цепочку вместе с `cd`, так что сторож измерил соседнее дерево
`/srv/tmp/dokazuemyy/dev` и напечатал зелёное «216 примет сошлись». В своём дереве
он тут же показал две разошедшиеся приметы (строки оболочки: правка прибавила 15
строк в `tables-guard.sh` и `run.sh`). Обе переписаны приборами, прогон повторён:
231 примета, разошлось 0.
