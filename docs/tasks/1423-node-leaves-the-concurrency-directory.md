---
номер: 1423
заголовок: Node уходит из flang/conc — четыре файла на 7102 строки, и они не единственные держат package.json
статус: в работе
исполнитель: a
ветка: a/1423-node-leaves-concurrency
команда: любая
карта: Чего в языке нет вовсе
рядом: 0049, 4412, 1745, 6858
нужность: 2 — 7102 строки JavaScript в каталоге языка; но «type»:«module» держат 13 файлов, не 4
---

# 1423. Четыре файла JavaScript в каталоге многозадачности

## Чем измерено

Замеры 13 сентября 2026, дерево `dev` `fcd4a9554`.

### Сколько там Node — и поправка к прежнему счёту

| файл | строк | строк `import`/`export` |
|---|---:|---:|
| `flang/conc/scheduler.js` | **3374** | 113 |
| `flang/conc/wire.js` | **1747** | 54 |
| `flang/conc/link.js` | **1246** | 43 |
| `flang/conc/bin/node.js` | **735** | 3 |
| **всего** | **7102** | 213 |

⚠ **Числа 113, 43 и 54 — это НЕ размеры файлов, а число строк ввоза-вывоза
(`import`/`export`) в них.** Размеры больше в тридцать раз. Замечено при
перепроверке; записано здесь, чтобы счёт не пошёл по кругу.

Двойники на языке рядом уже лежат и заметно короче:
`scheduler.flang` 1049, `wire.flang` 651, `link.flang` 464 — всего 2164 строки.
У `bin/node.js` двойника нет.

### Кто их зовёт

`flang/test/planirovshchik-celi.test.mjs`, `flang/test/svyaz-celi.test.mjs`,
`flang/scripts/conc-link-emitted.flang`, `scheduler-across-targets.flang`,
`link-across-targets.flang`, `scripts/targets/conc-link-emit.flang`,
`flang/conc/bench/hot-swap.sh`, `.github/workflows/dvoyniki.yml`,
плюс слово: `flang/conc/RESILIENCE.md`, `docs/javascript-inventory.md`,
`docs/why-javascript-remains.md`, три заметки `docs/zettel/**`.

### Про package.json — порог назван неверно, и вот правда

Корневой `package.json` держит `"type": "module"`. Но ввоз-вывоз ESM есть у
**13 файлов `.js`**, а не у четырёх:

| куча | файлов | где |
|---|---:|---|
| `flang/conc` | 4 | `scheduler.js`, `wire.js`, `link.js`, `bin/node.js` |
| воплощение печати в JS | 3 | `flang/src/emit/js/flang_host_node.js`, `flang_cli.js`, `flang_host_browser.js` |
| напечатанные примеры | 6 | `docs/examples/frameworks/{nestjs-orders,vue-roman,react-invoice}/printed/*.js` |

**Значит выброс `flang/conc` НЕ освобождает `package.json`** — останутся девять
файлов, из которых шесть НАПЕЧАТАНЫ самим языком.

Задачи `tasks/3420-*` в дереве **нет** (проверено по всем четырём каталогам
задачника). Судьбу `package.json` ведут:
**1745** (пункт 1: «ОСТАВЛЕН с доводом», коммит `56c42ee31`, полей 11 → 8) и
**8235** (§4: «печатать, а не хранить»).

## Зачем

Цель языка — «всё написано на flang» (задача 0049). Семь тысяч строк
JavaScript в каталоге, который описывает собственную многозадачность языка, —
самое заметное место, где цель не выполнена.

## Порог входа

Порога в языке нет: двойники трёх из четырёх файлов уже написаны.
Порог настоящий — **сверка**: правило задачи 0049 гласит, что снос идёт по
одному файлу и каждый раз со сверкой знак в знак на всём дереве. Частичная
сверка обманывает: у сторожа жаргона 21 поверхность из 51 давала пустую
разницу, а сторож был сломан.

## Шаги

- **Ш0 — сверка двойников знак в знак.** Для `scheduler`, `wire`, `link`
  прогнать оба воплощения на всём дереве и сличить вывод и код возврата.
  Расхождение — находка, а не повод править двойник задним числом.
- **Ш1 — переключить зовущих:** `.github/workflows/dvoyniki.yml`, два
  `.test.mjs` в `flang/test`, `flang/conc/bench/hot-swap.sh`.
- **Ш2 — `bin/node.js`** (735 строк, двойника нет): либо двойник, либо снос с
  доводом «ноль вызовов», как решено для `node-death-targets.mjs` в 4412.
- **Ш3 — снять четыре файла**, по одному на коммит; приметы в
  `docs/javascript-inventory.md` и `docs/tree-inventory.md` переснять.
- **Ш4 — записать честно, что `package.json` этим НЕ освобождён**, и назвать
  оставшиеся девять файлов — в 1745 и 8235.

## Как поймём, что сделано

`find flang/conc -name '*.js' | wc -l` — **0**; `./ярлык тесты` отвечает тем же
кодом, что до сноса; `dvoyniki.yml` зелен; в `docs/javascript-inventory.md`
число пересчитано прогоном.

## Чего задача НЕ делает

Не выбрасывает `package.json` и не решает его судьбу — это 1745 и 8235.
Не трогает `flang/src/emit/js/**`: это воплощение печати, а не оснастка.
Не трогает напечатанные примеры.

> **Перед правкой прочитать грабли** — они общие для всей этой волны и
> записаны числами в [1418](completed/1418-three-directories-get-english-names.md),
> раздел «Грабли, на которые я наступил сегодня»: ведомость долей правится
> столбцом пути, `git ls-files` не видит неотслеженное, примета и проза
> правятся порознь, числа README сверяет отдельный прибор.

## Ш0 — сверка двойников, 14 сентября 2026

Ветка над `a/1418-three-moves` (каталог уже `flang/concurrency`), двоичный
`bootstrap/flang` 0.7.19. Свежая печать — `bootstrap/flang emit
flang/concurrency/<модуль>.flang --target js` (link 2 с, scheduler 10 с, wire
5 с, код 0 у всех).

**Текстом закоммиченное со свежей печатью НЕ сходится** — и это не одна строка
шапки, как записано в `.github/workflows/dvoyniki.yml`:

| модуль | строк в дереве | строк в свежей | строк `diff` |
|---|---:|---:|---:|
| `link.js` | 1246 | 1244 | 36 |
| `scheduler.js` | 3374 | 2776 | 1276 |
| `wire.js` | 1747 | 1727 | 114 |

Расходятся шапка (две строки SPDX, «`flang/src/emit/js.mjs`» против
«`flang/self/emit-js.flang`»), значения доводов по умолчанию (`(fields = {})`
против `(fields)`) и у `scheduler` — ещё 598 строк: закоммиченное напечатано
прежним печатником на JavaScript.

**Имена сходятся:** экспортов `link` 43 = 43, `wire` 54 = 54, `scheduler` 113 в
дереве ⊂ 114 в свежей печати — ни одно имя из дерева не пропало.

**Поведение сходится знак в знак.** Свидетелю задавались вопросы самих сторожей
(функция «Текст вопросов» в `link-across-targets.flang` и
`scheduler-across-targets.flang`); «как в стороже» — закоммиченный `.js` с
дописанным блоком `$PROGRAM`, против свежей печати как есть:

| свидетель | вопросов | ответы | байт |
|---|---:|---|---:|
| `link` | 2218 | совпали знак в знак, код 0 у обоих | 769 270 |
| `scheduler` | 10 004 | совпали знак в знак, код 0 у обоих | 10 006 664 |

`wire.js` свидетелем не служит нигде: его читают только сверщики печати.

**Отрицательный контроль сличения.** В свежем свидетеле `link` одна постоянная
`"Послать` переименована — 260 строк ответов из 2218 разошлись: сличение порчу
видит. Первая попытка — одно `return true` → `return false` — дала 0: ту ветвь
вопросы сторожа не задевают. Это слабость набора вопросов, не сверки.

**Кто держит три файла** (ввозы и копии по всему дереву): свидетели в
`link-across-targets.flang` и `scheduler-across-targets.flang` (копией), два
теста `flang/test/*-celi.test.mjs` (ввозом), сверщики печати
`scripts/targets/conc-link-emit.flang` (обязательство 1 — «напечатанное в дереве
совпадает со свежей печатью») и `flang/scripts/conc-link-emitted.flang`
(перепечатывает рядом с исходником). Рантайм цели `js` и сам компилятор их не
ввозят; `bin/node.js` ввозит только `./uzel_zamera.js` — печать
`node-benchmark.flang` в рабочем каталоге.

## Ш1 — зовущие переключены на свежую печать, 14 сентября 2026

Ветка перебазирована на `main` `a2015b081` (1418 влита PR #28); двоичный
`bootstrap/flang` 0.7.19, сверщик доказательств пересобран из `сверщик.c`
ствола (ствол правил его в тот же день, старый давал ложное «НЕ ДОКАЗУЕМ»).
Два коммита:

* **`031900faf` — свидетели.** `flang/scripts/link-across-targets.flang` и
  `scheduler-across-targets.flang` берут свидетелем то, что сами только что
  напечатали (`svyaz_uzlov.js`, `planirovschik_uzla.js`), а не копию
  закоммиченного `.js`; блок `$PROGRAM` не дописывается — у свежей печати он
  есть. `flang/test/svyaz-celi.test.mjs` и `planirovshchik-celi.test.mjs`
  печатают модуль во временный каталог (рядом `package.json` с
  `"type":"module"`) и ввозят его оттуда; шапки переписаны в тех же строках —
  к номерам строк привязана ведомость рукописных перечней. Тесты выросли на 14
  строк, и это единственный сдвиг прогона после коммита: `./ярлык
  опись:сверка` покраснела на строке JavaScript. Таблица `docs/tree-inventory.md`
  и две приметы «строк-в `*.js,*.mjs`» пересняты прибором: 29 711 → 29 725.
* **`30c7ead5a` — сверщик печати и освежитель.**
  `scripts/targets/conc-link-emit.flang` держит два обязательства из трёх:
  узел зовёт у напечатанного только объявленное и исполняет каждое веление
  исходника. Первое — «напечатанное в дереве совпадает со свежей печатью» —
  снято вместе с «Шапкой», «Сверкой», «Шагом сверки», «Где разошлось» и
  «Бедой печати»; оно было красно всё время (три расхождения строки 3 шапки).
  Строк 393 → 273; приговор ядра `check --proof`: утверждений 30 → 21,
  доказано 23 → 15, сетка 7 → 6, объявлено 0 — так и вписано в опись
  доказанного (`e478585c…|21|15|6|0|0`; строка в дереве до того держала
  устаревшее `4f41091e…|30|21|8|1|0`). Долг комментариев 71 → 55 — правилом
  самого сторожа, строки с `//`. Объявление из 1416 («Объявление ярлыка
  связь:печать») и запись в `ярлыки.flang` названы одинаково — «узел зовёт у
  напечатанного только объявленное и исполняет каждое веление исходника»:
  сборщик 1416 будет их сличать. Комментарий и имя шага у задания
  `pechat-svyazi` в `dvoyniki.yml` переписаны.
  Освежитель `flang/scripts/conc-link-emitted.flang` снят: он печатал модули
  рядом с исходником и воскрешал бы снятые файлы; зовущих у него не было, его
  строки в описи доказанного и в долге комментариев убраны, а его упоминания в этой
  задаче (замер «кто зовёт» и запись Ш0 — до сноса) отведены исключением сторожа ссылок. `.flang`-файлом
  стало меньше — примета «файлов `*.flang`» и число рядом с ней пересняты в
  четырёх местах, 977 → 976 (`ci.yml`, `count-guard.mjs`, `name-guard.mjs`,
  `seed-knows-type-words-guard.sh`).
* **`flang/concurrency/bench/hot-swap.sh` переключать не пришлось:** ни один из
  трёх модулей он не зовёт, копирует только `bin/node.js` (см. Ш2).

Сверка до и после каждого коммита — одной таблицей в конце записи.

## Ш3 — три напечатанных файла сняты, по одному на коммит, 14–15 сентября 2026

| коммит | снят | строк | файлов JavaScript | строк JavaScript | файлов вне flang |
|---|---|---:|---:|---:|---:|
| `6e8ab2888` | `flang/concurrency/scheduler.js` | 3374 | 53 → 52 | 29 725 → 26 351 | 248 → 247 |
| `28d677699` | `flang/concurrency/link.js` | 1246 | 52 → 51 | 26 351 → 25 105 | 247 → 246 |
| `b6aaf3399` | `flang/concurrency/wire.js` | 1747 | 51 → 50 | 25 105 → 23 358 | 246 → 245 |

После каждого снятия — полная сверка на всём дереве (таблица в конце записи).
Приметы счёта JavaScript и общего счёта файлов вне flang пересняты прибором
(`prose-numbers-guard.sh` называет, что разошлось, и сколько снято сегодня)
вместе с числом в прозе рядом; прежние числа на свои даты не тронуты — рядом с
приметой дописано «на 14 сентября 2026, после снятия напечатанного задачей
1423». Абзац «Напечатано компилятором» в описи JavaScript переснят грепом
`Сгенерировано flang`: строку шапки несут и восемь напечатанных примеров во
`frameworks`, а опись говорила «только у трёх».

**Предсказание, которое не сбылось.** Я ждал, что упоминания снятых файлов в
двух открытых задачах (эта и 3420) и в журналах изменений станут битыми
ссылками, и завёл на `scheduler.js` шесть исключений сторожа ссылок. Прогон
назвал все шесть устаревшими: сторож судит путь в кавычках, только если его
расширение — одно из шестнадцати «Известных расширений улик»
(`scripts/guards/link-guard.flang`), и `js` среди них нет. Исключения убраны
из того же коммита; устаревших исключений в дереве снова одно, как до задачи.

## Ш2 — `bin/node.js` НЕ снят, и задача поэтому остаётся в работе

У хозяина узла на цели `js` двойника нет, а довод «ноль вызовов», который
задача 4412 (статус «свободна») предлагает для `node-death-targets.mjs` —
«либо двойник на flang рядом с `*-across-targets.flang`, либо снос», — к нему
неприменим: его берут шесть мест (поиск по дереву вне истории; путь местами
собирается по частям, поэтому искался и `flang/concurrency/bin/`):

* `flang/concurrency/bench/hot-swap.sh` — копирует его в рабочий каталог;
  ключ `--заменить-позже` есть только у него;
* `flang/scripts/node-across-targets.flang` и `supervisor-across-targets.flang` —
  цель `js` с «хозяином» `node.js`, путь `flang/concurrency/bin/` + хозяин;
* `flang/concurrency/bench/node-death.sh` — `cp …/bin/$HOZ`;
* `flang/test/uzel-osnastka.mjs` — `copyFileSync(…/bin/${цель.хозяин})`;
* `scripts/targets/conc-link-emit.flang` — оба оставшихся обязательства
  сверщика печати и есть «что узел зовёт у напечатанного».

Хозяин цели `js` по природе написан на JavaScript, как `node.py`, `node.go` и
остальные — на своих языках. Снять его можно только вместе с решением, что цель
`js` узла больше не проверяется, или напечатав хозяина самим flang; ни то ни
другое не решение этой задачи. Поэтому условие «`find flang/concurrency -name
'*.js' | wc -l` — 0» не выполнено: остаётся ровно `bin/node.js`.

## Ш4 — `package.json` этим НЕ освобождён

Ввоз-вывоз ESM после сноса есть у **12 файлов `.js`** — их и держит корневой
`"type": "module"` (файлам `.mjs` он не нужен: они ESM по расширению). Счёт —
`git ls-files '*.js' | xargs grep -l -E '^(import|export) '`, 15 сентября 2026:

| куча | файлов | где |
|---|---:|---|
| напечатанные примеры | 8 | `docs/examples/frameworks/*/printed/*.js` — `nestjs-orders`, `react-invoice`, `react-ts-pure`, `vue-roman`, по два файла |
| воплощение печати в JS | 3 | `flang/src/emit/js/flang_cli.js`, `flang_host_browser.js`, `flang_host_node.js` |
| хозяин узла цели `js` | 1 | `flang/concurrency/bin/node.js` (Ш2) |

Задача называла 13 = 4 + 3 + 6; с тех пор прибавился пример `react-ts-pure`
(два файла), а три файла сняты здесь: 13 + 2 − 3 = 12. Восемь из двенадцати
напечатаны самим языком. В `flang/concurrency` JavaScript остаётся четыре файла
на 1392 строки: `bin/node.js` (735), `bin/wire.mjs` (159), `bench/gen.mjs`
(351), `bench/node-death-targets.mjs` (147; его судьбу предлагает 4412).

**Поправка к разделу «Про package.json».** «Задачи `tasks/3420-*` в дереве
нет» было верно на дереве замера `fcd4a9554` (13 сентября 2026, 11:21 UTC):
3420 вошла слиянием `c39faa1d3` в 13:39 того же дня. Она (исполнитель —
владелец, «в работе») печатает манифест рядом с выводом цели JS и держит
корневой `package.json` тремя названными вещами; её таблица называет снятые
здесь файлы — текст её не тронут. Судьба `package.json` — задачи 3420, 1745 и
8235.

## Сверка на всём дереве после каждого коммита

Прогон — набор сторожей из «до» задачи (как в 1418) плюс сверщик печати, оба
сторожа «через цели» и оба теста-свидетеля; всё последовательно в клоне,
рабочая копия после каждого прогона чистая. Красное в «до» — долг ствола и
после каждого шага то же самое, знак в знак: пять проверок `pre-push`
(`licenzii`, `opis`, `podlog`, `proza`, `zov`), пять примет прозы от 14
сентября, 41 битая ссылка (одна из них — моя, см. «Перебазировка»), храповики
комментариев и перечней.

| прибор | до `0e4ab1da8` | свидетели `950f74319` | сверщик `78aed7a24` | −scheduler `a3c5cdc9f` | −link `28d677699` | −wire `b6aaf3399` |
|---|---|---|---|---|---|---|
| `доля-корпуса.sh --проигрыванием` | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % |
| `scripts/доказуемость.sh` | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 |
| `./ярлык расширения:проверка` | код 0 | код 0 | код 0 | код 0 | код 0 | код 0 |
| `task-numbers-guard.sh` | код 0 | код 0 | код 0 | код 0 | код 0 | код 0 |
| `./ярлык задачник:проверка` | код 0 | код 0 | код 0 | код 0 | код 0 | код 0 |
| `published-vs-tree.sh --проза` | код 0 | код 0 | код 0 | код 0 | код 0 | код 0 |
| `./ярлык опись:сверка` | код 0 | код 1 | код 0 | код 0 | код 0 | код 0 |
| `sh .githooks/pre-push` | код 1: красных 5 | код 1: красных 6 | код 1: красных 5 | код 1: красных 5 | код 1: красных 5 | код 1: красных 5 |
| `flang/test/обход.sh` | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 |
| `./ярлык ссылки:проверка` | битых 41 из 6888 | битых 41 из 6888 | битых 43 из 6888 | битых 41 из 6888 | битых 41 из 6888 | битых 41 из 6888 |
| `hand-written-lists.sh --check` | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 |
| `./ярлык длина:проверка` | 405 / 284 | 405 / 284 | 402 / 281 | 402 / 281 | 402 / 281 | 402 / 281 |
| `./ярлык связь:печать` | код 1: расхождений 3 | код 1: расхождений 3 | код 0: расхождений 0 | код 0: расхождений 0 | код 0: расхождений 0 | код 0: расхождений 0 |
| `link-across-targets.flang` | 9 из 9 целей, сверок 19962, расхождений 0 | 9 из 9 целей, сверок 19962, расхождений 0 | 9 из 9 целей, сверок 19962, расхождений 0 | 9 из 9 целей, сверок 19962, расхождений 0 | 9 из 9 целей, сверок 19962, расхождений 0 | 9 из 9 целей, сверок 19962, расхождений 0 |
| `scheduler-across-targets.flang` | 9 из 9 целей, сверок 90036, расхождений 0 | 9 из 9 целей, сверок 90036, расхождений 0 | 9 из 9 целей, сверок 90036, расхождений 0 | 9 из 9 целей, сверок 90036, расхождений 0 | 9 из 9 целей, сверок 90036, расхождений 0 | 9 из 9 целей, сверок 90036, расхождений 0 |
| `planirovshchik-celi.test.mjs` | 8 из 8 | 8 из 8 | 8 из 8 | 8 из 8 | 8 из 8 | 8 из 8 |
| `svyaz-celi.test.mjs` | 8 из 8 | 8 из 8 | 8 из 8 | 8 из 8 | 8 из 8 | 8 из 8 |

Три столбца сняты на коммите ДО того, как в него вложена правка по итогам
того же прогона, и это видно в числах:

* `950f74319` → `031900faf`: прогон нашёл строку JavaScript в таблице языков
  (`опись:сверка` код 1, `pre-push` — шестая красная); пересъёмка вложена в
  коммит, после неё `опись:сверка` — код 0, `pre-push` — те же пять красных;
* `78aed7a24` → `30c7ead5a`: 43 битые ссылки — это два упоминания снятого
  освежителя в этой задаче; исключение вложено, повторный прогон сторожа ссылок
  на `30c7ead5a` — 41, те же, что в «до»;
* `a3c5cdc9f` → `6e8ab2888`: шесть лишних исключений (см. Ш3) убраны; что
  устаревших исключений снова одно, показал прогон после `28d677699`.

Сдвиги, которые остаются: «без входов» у сторожа длины 405 → 402 и судимых
284 → 281 — вместе со снятым кодом ушли его функции; перечней, набранных
руками, 310 → 306 — ушли перечни путей из свидетелей и освежителя; сверщик
печати связи — код 1 → 0, три расхождения шапки ушли вместе с первым
обязательством.

## Перебазировка на `7a9181334`, 15 сентября 2026

За ночь в `main` вошли 57 коммитов — среди них PR #18 и починка ствола: пять
примет прозы, красных в «до» этой задачи, сошлись, `pre-push` на стволе
зелёный. Перебазировка останавливалась четыре раза; каждая остановка разрешена
прибором, а не вычитанием:

* **сверщик печати** — четыре приметы «файлов `*.flang`» (`ci.yml`,
  `count-guard.mjs`, `name-guard.mjs`, `seed-knows-type-words-guard.sh`): ствол
  переснял их на 987; взята его версия, и на дереве остановки прибор снял 986 —
  примета и число рядом переписаны;
* **три снятия** — описи JavaScript и дерева и `binary.yml` взяты из ствола,
  шаг переснят заново тем же сценарием, что и 14 сентября. Числа JavaScript те
  же (файлов 53 → 52 → 51 → 50, строк 29 725 → 26 351 → 25 105 → 23 358); файлов
  вне flang у ствола на один больше — 249 → 248 → 247 → 246. После каждой
  остановки сторож прозы — 216 примет из 216. Сообщения коммитов снятий
  переписаны числами, снятыми на остановке.

Коммиты до и после перебазировки (записи выше ссылаются на левый столбец):

| до перебазировки | после | коммит |
|---|---|---|
| `6be8cfb7b` | `6296933b5` | docs(задачник): 1423 взята в работу — ветка a/1423-node-leaves-concurren |
| `0e4ab1da8` | `9ac7ab0ae` | docs(задачник): 1423 Ш0 — напечатанные link/scheduler/wire расходятся с  |
| `031900faf` | `48cd8ce08` | test(узел): свидетели связи и планировщика — свежая печать flang/concurr |
| `30c7ead5a` | `f1663986e` | refactor(узел): сверщик печати связи не сличает закоммиченный JavaScript |
| `6e8ab2888` | `9f9d0edf8` | chore(узел): напечатанный flang/concurrency/scheduler.js снят — свидетел |
| `28d677699` | `12503e3dd` | chore(узел): напечатанный flang/concurrency/link.js снят — свидетелей и  |
| `b6aaf3399` | `6792fbd7c` | chore(узел): напечатанный flang/concurrency/wire.js снят — свидетелей и  |
| `a127d7619` | `713197386` | docs(задачник): 1423 Ш1–Ш4 — три напечатанных файла сняты, bin/node.js о |

Сверка на новом стволе — полный прогон тем же набором на `origin/main` и на
вершине ветки:

| прибор | ствол `7a9181334` | вершина `713197386` |
|---|---|---|
| `доля-корпуса.sh --проигрыванием` | 630 / 651 = 96,77 % | 630 / 651 = 96,77 % |
| `scripts/доказуемость.sh` | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 |
| `./ярлык расширения:проверка` | код 0 | код 0 |
| `task-numbers-guard.sh` | код 0 | код 0 |
| `./ярлык задачник:проверка` | код 0 | код 0 |
| `published-vs-tree.sh --проза` | код 0 | код 0 |
| `./ярлык опись:сверка` | код 0 | код 0 |
| `sh .githooks/pre-push` | код 0 | код 0 |
| `flang/test/обход.sh` | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 |
| `./ярлык ссылки:проверка` | битых 42 из 6924 | битых 43 из 6974 |
| `hand-written-lists.sh --check` | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 |
| `./ярлык длина:проверка` | 405 / 284 | 402 / 281 |
| `./ярлык связь:печать` | код 1: расхождений 3 | код 0: расхождений 0 |
| `link-across-targets.flang` | 9 из 9 целей, сверок 19962, расхождений 0 | 9 из 9 целей, сверок 19962, расхождений 0 |
| `scheduler-across-targets.flang` | 9 из 9 целей, сверок 90036, расхождений 0 | 9 из 9 целей, сверок 90036, расхождений 0 |
| `planirovshchik-celi.test.mjs` | 8 из 8 | 8 из 8 |
| `svyaz-celi.test.mjs` | 8 из 8 | 8 из 8 |

Единственное новое красное на вершине — моё и давнее: путь прежнего печатника
`flang/src/emit/js.mjs` в кавычках в записи Ш0 этой задачи (цитата строки
шапки напечатанного). На прежнем стволе он сидел среди 41 битой ссылки «до» —
то «до» снято уже с записью Ш0 — и потому не выделялся; на новом стволе «до»
снято без коммитов задачи, и он вышел. Отведён исключением сторожа ссылок в
том же коммите, что и эта запись (файла нет — цитата). Прочие сдвиги — те же,
что на прежнем стволе: длина 405/284 → 402/281 и перечни, набранные руками,
313 → 309 (ушёл код); сверщик печати — код 1 → 0; мест, судимых сторожем
ссылок, 6924 → 6974 — это пути в записях задачи.
