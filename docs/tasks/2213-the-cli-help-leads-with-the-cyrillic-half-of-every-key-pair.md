---
номер: 2213
заголовок: Справка ведёт кириллической половиной пары, а один ключ пары в справке не назван вовсе
статус: в работе
исполнитель: a
ветка: a/cli-keys-latin
команда: любая
карта: Что мешает больше всего
рядом: 5502, 8161
нужность: 2 — ключи в разборе давно парные, беда только в том, каким написанием их называет справка; не на пути доказуемости, но первое, что видит человек
---

# 2213. Ключи парные с самого начала — непарной была справка

Заведено 23 сентября 2026 по словам владельца: «меня бесит почему ключи у нас
на русском почему-то сейчас».

## Замер

Снят на стволе `38105062f`, в чистой копии.

```sh
grep -oE '"--[a-zA-Zа-яё-]+' flang/self/cli.flang | sort -u
```

Ключей 36. Кириллических 4: `--на-веру`, `--предел-шагов`, `--размещение`,
`--словарь`. Остальные 32 — латиница.

Дальше главное, и оно опровергает ту картину, с которой задача ставилась.
Проверка «есть ли у кириллического ключа латинский двойник В РАЗБОРЕ»:

```sh
grep -n -- '--на-веру\|--предел-шагов\|--размещение\|--словарь' flang/self/cli.flang
```

| ключ | латинский двойник | где принимается |
|---|---|---|
| `--на-веру` | `--trust` | `cli.flang:915`, `:1159` |
| `--предел-шагов` | `--step-limit` | `cli.flang:901`, `:1159` |
| `--размещение` | `--placement` | `cli.flang:894`, `:942`, `:1159`, `:1167` |
| `--словарь` | `--legend` | `cli.flang:915`, `:1159` |

Двойник есть у ВСЕХ ЧЕТЫРЁХ, а не у трёх. Заводить нечего.

Откуда взялась картина «у двух двойника нет»: замер делался по
`bootstrap/flang_repl.c` — по СЕМЕНИ, а не по эталону. Там
`grep -c '"--размещение"'` и `grep -c '"--словарь"'` дают 0, и `--placement`
с `--legend` тоже 0: этих ключей в семени нет вовсе, оно старше эталона.
То же самое записано и без нас — `flang/scripts/cli-keys-debt.json`, раздел
«разряд-2-только-в-эталоне», и `docs/adr/0010`, строки 421–422. Двоичный
этих двух ключей не знает — но не потому, что отстал: эталон в двоичный
вообще не печатается, см. «ОТКРЫТАЯ ПОЛОВИНА» ниже.

Беда эталона — в его справке. Вхождения в блоке справки
(`cli.flang`, строки 152–500):

| ключ | было | ключ | было |
|---|---:|---|---:|
| `--placement` | **0** | `--размещение` | 2 |
| `--legend` | 1 | `--словарь` | 2 |
| `--step-limit` | 1 | `--предел-шагов` | 2 |
| `--trust` | 2 | `--на-веру` | 8 |

`--placement` справка не называла ВООБЩЕ: ключ работал, а узнать о нём было
неоткуда. Остальные три назывались кириллицей в заголовке и латиницей —
припиской в конце абзаца.

## Решение

1. **Новых имён не заводить.** Предложение задачи — `--placement` и
   `--dictionary` — принято наполовину: `--placement` уже есть, а
   `--dictionary` был бы ТРЕТЬИМ написанием одного ключа рядом с уже
   принятым `--legend`. ADR-0010 обещает пару, а не список синонимов;
   третье написание к тому же немедленно покраснило бы сторож ключей
   (`flang/scripts/cli-keys-guard.fscript` судит в обе стороны, и записи
   в ведомости долга под него нет).
2. **Справка ведёт латиницей, кириллица — синонимом рядом.** Переписаны
   строки вызова и заголовки ключей в «Краткой справке», «Справке проверки»,
   «Справке прогона» и «Справке ввода-вывода».
3. **Диагностики — тем же написанием, что справка** (6 строк). Иначе
   набравший `--legend` читал бы отказ про `--словарь`.
4. **Страницы `docs/site/cli.md` и `docs/site/cli.ru.md`** — латинская
   половина названа первой у пяти пар, которые двоичный понимает уже
   сегодня: `--strict`, `--fast`, `--step-limit`, `--depth-limit`,
   `--record`.

### Чего решение НЕ делает и почему

Четыре `--на-веру` в справке остались, и это не недоделка. Они стоят внутри
кавычек, цитирующих строку вердикта, а печатает её
`flang/self/bootstrap/compiler.flang` (строки 1577 и 1582) — кириллицей.
Справка обязана цитировать дословно; переписать её на `--trust`, не тронув
сам вердикт, значит соврать о выводе программы. Перевод самого вердикта —
чужой файл и отдельное решение (`compiler.flang` тут не правится).

Строки вызова в ```bash на страницах команд не тронуты по той же причине:
это дословный протокол того, что печатает двоичный 0.7.17.

## Приём

```sh
# 1. Разбор ключей не изменился: ни одного нового имени, ни одного убранного
diff <(git show 38105062f:flang/self/cli.flang | grep -oE '"--[a-zA-Zа-яё-]+' | sort -u) \
     <(grep -oE '"--[a-zA-Zа-яё-]+' flang/self/cli.flang | sort -u)      # пусто

# 2. Справка ведёт латиницей
sed -n '152,500p' flang/self/cli.flang | grep -c -- '--placement'        # было 0, стало 2

# 3. Файл цел — ПОЛНОЙ проверкой, с ядром и примерами
bootstrap/flang check flang/self/cli.flang
#   код 0, 12,2 с, «проверено — разбор, типы, завершаемость, ядро и примеры;
#   замечаний нет»; функций 86, с доказанным завершением 86, примеров 78
```

Замечание о цене: ячейка предупреждала, что полная проверка этого файла «не
уложится», пока идёт перепечатка. Замерено — уложилась: 12,2 с при
среднем загрузке машины 19. Предупреждение верно для дерева целиком, не для
одного модуля.

## ОТКРЫТАЯ ПОЛОВИНА: справку, которую видит человек, эта правка не меняет

Задача ставилась со словами «`cli.flang` — печатаемая часть семени, двоичный
узнает новые ключи из нового семени». Проверено — это не так, и вот чем.

```sh
awk '/^[0-9a-f]{64}  /{print $2}' scripts/otpechatok-semeni | grep -c cli.flang
#   0    — cli.flang нет среди 55 входов отпечатка печати
grep -c 'cli_flang' bootstrap/compiler_flang.c
#   0    — ни одного имени из cli.flang в напечатанном семени (671 161 строка)
grep -c 'разбор, типы, завершаемость, доказательства' bootstrap/flang_repl.c
#   1    — строка справки лежит ЗДЕСЬ
```

`flang/self/cli.flang` — ЭТАЛОН, модель командной строки. Его читает только
`flang/scripts/cli-keys-guard.fscript`, чтобы судить двоичный. Справку,
которую печатает `flang --help`, держит рукописный C —
`flang/src/emit/c/flang_repl.c`, копируемый в `bootstrap/flang_repl.c`.

Что печатает двоичный 0.7.21 сегодня (`flang --help` плюс `check|run|io|emit|new
--help`, 384 строки, `grep -oE '\-\-[а-яё][а-яё-]*'`):

| ключ | раз | ключ | раз |
|---|---:|---|---:|
| `--предел-глубины` | 5 | `--недоказанное` | 2 |
| `--на-веру` | 3 | `--записать` | 2 |
| `--быстро` | 3 | `--силой` | 1 |
| `--строго` | 2 | `--протокол` | 1 |
| `--предел-шагов` | 2 | `--машина` | 1 |

Двадцать два упоминания, десять разных кириллических ключей. `--placement` и
`--legend` двоичный не знает вовсе (`grep -c 'placement\|legend'` по справке
= 0). Ровно это и раздражает владельца, и эта задача этого НЕ чинит.

Работы там — 76 строк с кириллическими ключами в `flang_repl.c`, из них в
самих строках справки десятки. Файл не входил в ячейку и правится отдельной
ячейкой.

### Хорошая новость про цену

Развернуть справку двоичного стоит НЕ 21 часа перепечатки.
`flang/src/emit/c/flang_repl.c` — копируемая часть семени: `seed-refresh.sh`
исключает `flang/src/emit/c/*` из сверки замыкания (`case "$PUT" in
"$RUNTIME"/*) continue`). Значит после правки годен быстрый пересев
`sh scripts/seed/seed-refresh.sh` — минуты, а не часы.

## Что с этой веткой

* `sh .githooks/pre-push` — зелен, все 10 проверок, 14 с. Предсказанного
  покраснения `semya`/`semyaosv` НЕ случилось, и по той же причине:
  `cli.flang` не входит в отпечаток печати, а `bootstrap/*.c` не тронут;
* одна проверка покраснела и починена по ходу: `proza`
  (`prose-numbers-guard.sh`) — правка добавила в `flang/self/*.flang` две
  строки, и примета `docs/flang/self/SPEC.md:17` (139 806) разошлась с
  деревом (139 808). Примета переснята; после — примет 232, сошлось 232;
* эталон и двоичный по НАБОРУ ключей не разъехались: ни одного имени не
  заведено и не убрано, `flang/scripts/cli-keys-debt.json` править не нужно.
