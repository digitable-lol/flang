---
номер: 3464
заголовок: Шаг теоремы называет правило ведомости и посылки — ядро не ищет, а проверяет; сверщик переигрывает
статус: в работе (Ш0–Ш2 сделаны и измерены; Ш3 не начат)
исполнитель: Marat
ветка: r/mat-taktiki
команда: любая
карта: Сколько доказано на самом деле
рядом: 6131, 6132, 1404, 3009, 7771
---

# Шаг теоремы называет правило ведомости и посылки

Шаг 4 плана владельца: **доказательство пишет человек, ядро только проверяет.**

## Замер «до» (11 сентября 2026, r/partiya-2 `6b7c43f86`, семя бинарного `bootstrap/flang` того же дерева)

Что человек может написать в теореме сегодня — девять слов поверхности
(`docs/flang/proof/SPEC.md` §1): цель `утверждаем`, `индукция по`, случаи, и четыре
обоснования шага — `по свойству «…»`, `по примеру «…»`, `по закону «…»`,
`по предположению`. Шаг `затем <утверждение> по …` несёт промежуточный факт (ADR-0014).

Где ядро всё равно ищет. `по предположению` и `по свойству` — это НЕ правило,
а поручение ядру: «сведи цель к допущениям сам». Ядро зовёт «Свести витками» /
«Свести префиксами допущений» (`flang/self/proofterm.flang`, «Свести к
допущениям теоремы») — перебор по правилам `proof-kernel.flang`. Правило,
которым цель закрылась, человек не называл, и в записи оно едет одним именем
(`правило «…»`) либо не едет вовсе: строка шага теоремы `шаг 1 строка 43
закрывающий по свойству «…»` — и под ней ничего. Сверщик такие шаги считает
«на слово ядра: шагов N» (`checker.c`, `сверить_шаги`: `по закону` — «закон
сверщик не переигрывает», `по предположению` вне узла — «шаг сверщик по существу
не проигрывает»).

Форма `.record` уже знает язык, в котором шаг называет правило И посылки: блок
`вывод` (ADR-0022, задача 6131): `вывод N ИМЯ ⟨формула⟩ сам | строка M | из K |
из K L | из K строка M`; имена — ведомость `flang/proof/tables/inference-rules.tsv`
(84 строки), приём `проиграть_вывод` знает 57 правил. Но печатается блок ТОЛЬКО у
утверждения без теоремы (`zapis.flang`, «Строки утверждения»: `«Это ничто» от
«теорема»`), и сверщик зовёт `проиграть_вывод` только на дороге `без_теоремы`.
Значит «человек пишет шаги» = «человек пишет то, что ядро печатает в блоке
`вывод`», а не новый язык.

Пример, где ядро само не находит (опыт `c2`): `требует «а не больше б»`,
`требует «б не больше десяти» б не больше 10`, тело `а`, цель `результат не
больше 10`. Без теоремы — «сетка 1» (с примером; код 0 без `--строго`, 4 с ним),
без примера — «объявлено, не доказано 1 — код возврата 3». С теоремой
`по предположению` — `FLANG_PROOF_STEP: цель не сведена к 2 предусловию…` (ядро
ищет по правилу ограниченности и не находит транзитивности через названную
середину). У сверщика при этом есть О10 (два звена, середина знак в знак).

Прибор до: `sh scripts/доказуемость.sh` — ДОКАЗУЕМ; 1 ДА (видов 34/34, на слове
3/3); 2 ДА 625/651 = 96,01 %; 3 ДА (подделок 34/34; проб на подлог 425, принято
0; честных 203, отвергнуто 0); 4 ДА. Lean: `lean` в PATH нет — не снято.

### Измеренное основание: ядро держит обе посылки, но не складывает их (12 сентября 2026)

Снято на двоичном `bootstrap/flang` этого дерева. Проба — функция «Через середину»:
`требует «а не больше б»`, `требует «б не больше десяти» б не больше 10`, тело `а`,
цель `результат не больше 10`.

| что написано при цели | код | что ответило ядро |
|---|---:|---|
| ни теоремы, ни примеров | 3 | «объявлено, не доказано 1» — за цель ядро не бралось вовсе |
| пример есть, `--строго` | 4 | «сетка 1» — посчитано на значениях автора; это не доказательство |
| теорема с `по предположению` | 1 | `FLANG_PROOF_STEP`: «цель не сведена к 2 предусловию функции» |

Третья строка — основание всей задачи, и она же отвечает на вопрос «а ищет ли ядро
вообще». Ищет: оно берётся за цель и отказывается по существу. При этом ОБЕ посылки
у него в руках — в отказе стоит `известно: предусловие функции «Через середину»;
предусловие функции «Через середину»`, то есть и `а не больше б`, и `б не больше 10`.
Сложить их в цепочку `а не больше б, б не больше 10, значит а не больше 10` ядро не
умеет: оно пробует ограниченность точным потолком и на ней отказывается.

Правило для этого в ведомости УЖЕ есть — О10, два звена с названной серединой
(`tables/inference-rules.tsv:123`). Ядру нечем выбрать середину: середина — это имя, которого
в цели нет, и перебирать его значило бы искать. Назвать середину может только человек,
и ровно это делает явный шаг `по закону «О10» из 1 и 2`. Отсюда же вторая половина
доказательства: тело `а` доводится до `результат` правилом Разв2 («из N и строки M»).

Вывод для формы шага: правило и посылки обязан назвать человек не потому, что ядро
слабое, а потому, что выбор середины — это выбор, а не вычисление.

## Спецификация

**Форма шага.** Внутри `теорема` (без индукции), на русской и английской поверхности:

```
затем <формула> по закону «Н3» из строки 4          next <formula> by law «Н3» from line 4
затем <формула> по закону «Н5» из 1 и 2              next <formula> by law «Н5» from 1 and 2
по закону «Разв2» из 3 и строки 13                   by law «Разв2» from 3 and line 13
по закону «Н1»                                       by law «Н1»
```

`«Имя»` — имя правила ведомости знак в знак; `из …` — посылки: номер прежнего
шага теоремы, либо `строки N` (`line N`) — строка исходника внутри функции
(объявление довода, `требует`, тело). Закрывающий шаг — без формулы (его формула —
цель). Новых слов ноль: `по закону`, `из`, `и`, `строка` уже в языке.

**Что ядро проверяет** (Ш1, `proofterm.flang`, «Проверить вывод по ведомости»):
не ищет — применяет названное правило к названным посылкам и сличает с формулой
шага («Одинаковы» после «Нормализовать»): О1, О10, Н1, Н3, Н5, Разв2. Отказ
называет шаг, правило, что нужно и что стоит: «шаг 3: О10 — середина не сходится:
шаг 1 кончается «б», шаг 2 начинается «в»». Смешение шагов ведомости с
`по свойству`/`по предположению`/`по примеру` в одной теореме — отказ.
Правило ведомости вне шести — отказ с именем («ядро это правило ещё не проверяет»).

**Что печатает** (`zapis.flang`): строки шагов как прежде, обоснование —
дословно из исходника (`по закону «Н5» из 1 и 2`), и под `следовательно доказано
да` — блок `вывод`, каким его печатает утверждение без теоремы:
`вывод цель ⟨…⟩`, `вывод N ИМЯ ⟨формула⟩ основание`, `вывод конец`.
Формула шага — терм исходника (между `затем` и `по закону`), у закрывающего — цель.

**Что переигрывает сверщик** (`checker.c`, `сверить_теорему`): при блоке `вывод`
под теоремой — сличает каждую строку блока с шагом исходника (имя правила,
посылки), зовёт `проиграть_вывод` с целью `утверждаем`, и шаги `по закону … из …`
уходят не в «на слово ядра», а в «Выводов факта о типе проиграно заново».
Новых правил ведомости — ноль; новых приёмов сверщика — ноль (тот же `шаг_вывода`).

## Ш0..Ш3

- Ш0 — разбор `по закону «Имя» из …` (`parser.flang`), поле `from` у шага.
- Ш1 — проверка применимости в ядре, печать записи, приём сверщика.
- Ш2 — приёмка: три теоремы с явными шагами (одна — код 3 без шагов, код 0 с шагами);
  честные записи — сверщик 0; подделки (чужая посылка, пропущенный шаг, правило не
  по форме) — ядро и сверщик код 1 с причиной; семья проб `families/ledger-steps`;
  `доказуемость.sh` ДОКАЗУЕМ, доля ≥ 625/651, «сошлось всё».
- Ш3 (не здесь) — шаги ведомости внутри случаев индукции; остальные правила ведомости в ядре.

## Чего не делает

- Не заводит новых слов языка и новых правил ведомости.
- Не трогает поиск ядра: `по предположению`/`по свойству` работают как прежде.
- Не даёт шагов ведомости внутри `индукция по` (блок `вывод` у посылок сверщик не
  переигрывает — 6131 знает его только у утверждения без посылок).
- Ядро проверяет шесть правил из 84; остальное — отказ с именем, не молчание.
- Семя не перепечатывает: правки `flang/self/**` проверены толкованием (зонд).

## Сделано и измерено (12 сентября 2026, ветка r/mat-taktiki)

**Ш0 — разбор.** `parser.flang` принимает `по закону «Имя» [из <посылка> [и <посылка>]]`,
где посылка — номер прежнего шага либо `строки N`. Новых слов языка ноль: `из`, `и`,
`строка` уже стоят в лексере. `flang check flang/self/parser.flang` — код 0.

**Ш1 — ядро проверяет, печать печатает, сверщик переигрывает.**
Ядро (`proofterm.flang`, правка предыдущего исполнителя) применяет НАЗВАННОЕ правило
к НАЗВАННЫМ посылкам: Н1, Н3, Н5, О1, О10, Разв2. Печать (`zapis.flang`) несёт посылки
в обосновании шага дословно и печатает под `следовательно доказано да` блок `вывод` —
тот же, каким его печатает утверждение без теоремы. Приём сверщика (`checker.c`):
`сверить_теорему` зовёт `проиграть_вывод`, места переносятся из «на слово ядра» в
«выводов проиграно заново». Новых правил ведомости ноль, новых приёмов сверщика ноль.

**Ш2 — приёмка.** Семья `tests/families/ledger-steps`: три теоремы с явными шагами —
О1→О1→О10→Разв2, Н3→Н3→Н5→Разв2, Н1→Разв2.

| мерка | до | после |
|---|---|---|
| та же цель без шагов | код 3, «объявлено, не доказано 1» | — |
| та же цель с теоремой `по предположению` | код 1, «цель не сведена к 2 предусловию» | — |
| три теоремы с явными шагами (зонд) | — | «утверждений 3: доказано 3, шагов в термах 10» |
| сверщик на честной записи | — | код 0, «шагов сверено 10», на слово ядра 0 |
| подделки записи (5) | — | код 1, у каждой названа причина |
| подделка исходника (ядро) | — | `FLANG_PROOF_STEP: О10 — середина не сходится` |
| пробы | 425 подделок / 203 честных, «сошлось всё» | 430 / 204, «сошлось всё» |
| прибор | ДОКАЗУЕМ, 625/651 = 96,01 % | ДОКАЗУЕМ, 625/651 = 96,01 % |

Правки `flang/self/**` проверены ТОЛКОВАНИЕМ (зонд `flang/self/bootstrap/zond-k7.flang`),
а не перепечаткой семени: перепечатку делает ведущий.

## Поправка к ревизии задачника, 23 сентября 2026

Приём у задачи **ЕСТЬ**, но стоит не под своим заголовком: он расписан пунктом
**Ш2** в разделе «Ш0..Ш3» и там прямо назван «приёмка». Ревизия задачника
22 сентября 2026 записала «приёма нет вовсе» — ошибка ревизии, та же, что у 2040
и 8309: она искала заголовки «Приём» и «Чем измерено» и мимо пункта плана
прошла. Нового раздела приёма здесь не пишется; пункт Ш2 переписан ниже
дословно по смыслу, чтобы его было видно, и против каждой мерки поставлено, что
она отвечает сегодня.

Приём по Ш2 — пять мерок:

1. три теоремы с явными шагами (без шагов — код 3, с шагами — код 0);
2. честные записи — сверщик код 0;
3. подделки (чужая посылка, пропущенный шаг, правило не по форме) — ядро и
   сверщик код 1 с названной причиной;
4. семья проб `flang/proof/checker/tests/families/ledger-steps` заведена;
5. `sh scripts/доказуемость.sh` — ДОКАЗУЕМ, доля ≥ 625/651, «сошлось всё».

Прогон 23 сентября 2026, рабочая копия `wPriyom` на main `38105062f`,
сверщик собран здесь же (`make -s -C flang/proof/checker`, 6,0 с):

| мерка | чем снято | ответ 23 сентября 2026 |
|---|---|---|
| 4 (семья заведена) | `ls flang/proof/checker/tests/families/ledger-steps` | `README.md`, `steps.flang`, `steps.record` — **есть** |
| 2 (честная запись) | `./flang/proof/checker/сверщик flang/proof/checker/tests/families/ledger-steps/steps.flang …/steps.record` | код **0**, «ПРОВЕРЕНО … утверждений 3, шагов сверено с исходником **10**, НА СЛОВО ЯДРА: посылок и утверждений 0, шагов 0; Выводов факта о типе проиграно заново 3 (снято со слова ядра мест 10)» — число шагов сходится со строкой «сверщик на честной записи … шагов сверено 10» из таблицы автора |
| 5 (доля) | `sh flang/proof/corpus-share.sh --проигрыванием` | доля-проигрыванием **650 / 650 = 100,00 %** — выше порога Ш2 (625/651); «на слово ядра: посылок и утверждений 0, шагов 0» |
| 5 (вердикт) и 1, 3 | `sh scripts/доказуемость.sh` и прогон подделок | **не гонялись**: машина под печатью семени, а эти две мерки идут через набор проб сверщика. Цена по шапке самого прибора — около 3 с на пробы и около 5 с на линейку доли, то есть дело не в цене, а в запрете |

Итог: из пяти мерок Ш2 три сняты сегодня и зелены, две не гонялись по запрету.
Ш3 задача с себя снимает сама («Ш3 (не здесь)»), поэтому долгом задачи он не
является. Статус не менялся: чтобы закрыть, остаётся прогнать
`sh scripts/доказуемость.sh` и подделки семьи `ledger-steps` на машине, где
набор проб сверщика гонять можно.

## Ведомость называет правило и основания У КАЖДОГО ШАГА (26 сентября 2026, ветка `a/3464-chitaemyy-sertifikat`)

Работа этого захода — не ядро и не разбор, а ЧТЕНИЕ: шаги, которые ядро уже
проверило и запись уже называет, до ведомости не доезжали ни одним словом.
Рабочая копия `wCert` на `gh/main` `0190f16f9`; двоичный собран здесь же
(`make -C bootstrap -j16`, 1 мин 59 с). Замер — прогон `flang check … --proof`
и `--proof --record` по 131 программе: `flang/proof/probes` (30 файлов) и
`flang/test/fixtures` (101 файл), шестью прогонами разом, предел 120 с на файл.

### Замер ДО правки

| мерка | чем снято | ответ |
|---|---|---|
| файлов взято | `find … -name '*.flang'` | 131 |
| не досчиталось за 120 с | тот же прогон | 1 — `flang/test/fixtures/razrez-kruga/literal-print-comparison.flang` |
| ведомость напечаталась | `check … --proof` | у 71 файла |
| ведомость не печатается | то же | у 59 — программа с замечаниями, так задумано |
| строк в разделе «что высказано и чем это несётся» | те же прогоны | 180 |
| из них называют правило (`правило «…»`) | то же | **67** |
| из них НЕ называют правило | то же | **113** |
| шагов теоремы, названных строкой в СЛОВАХ ведомости | то же | **0** |
| шагов теоремы, которые называет ЗАПИСЬ доказательства | `--record`, строки `шаг N …` | 14 |
| строк `вывод N ИМЯ …` в записи (имя правила ведомости знак в знак) | `--record` | 162 |

Из 113 строк без имени правила, по вердикту утверждения (поле `verdict`
машинного вида): `declared` 49, `grid` 44, `proved` без теоремы 8,
`proved-induction` 5 с теоремой и 4 без, `proved` с теоремой 2,
`proved-conditional` с теоремой 1.

Самое резкое место замера — семья `flang/proof/checker/tests/families/ledger-steps`,
где человек НАПИСАЛ правила сам (`по закону «О10» из 1 и 2`). Ведомость до правки
отвечала на все три теоремы одинаково: «доказано: терм принят ядром, 4 шага» —
ни одного имени правила, ни одной посылки. Запись при этом несла и то и другое:
`вывод 3 О10 ⟨а не больше 10⟩ из 1 2`.

### Что сделано

Правка одна, в копируемой части: `flang/src/emit/c/flang_repl.c`, приём
`proof_say_steps` и один вызов в `proof_file`. Ведомость получает новый
последний раздел — `чем получен каждый шаг (правило и основания — строками
записи доказательства):`. Строки в нём не сочиняются: они ОТБИРАЮТСЯ из поля
`запись`, которое тот же слой на flang отдаёт в том же ответе, что и слова
ведомости, и печатаются под шапкой своего утверждения. Берутся те, что называют
правило или основание: `правило «…»`, `шаг N …`, `вывод …` (кроме `вывод конец`),
`сведение «…»`, а под индукцией — `случай …` и `посылка …`. Рядом с `--json`
раздела нет: русские слова посреди JSON сломали бы разбор.

`случай …` берётся рядом со шагами не для полноты: внутри индукции нумерация
шагов идёт от единицы В КАЖДОМ случае, и без строки случая раздел печатал два
разных шага под одним номером (снято на
`flang/proof/probes/run/programs/induction.flang`: «шаг 1 … по примеру «пусто»»
и «шаг 1 … по предположению» стояли подряд).

### Замер ПОСЛЕ правки

Та же выборка, тот же прибор, тот же предел 120 с.

| мерка | до | после |
|---|---:|---:|
| раздел шагов печатается | у 0 файлов (раздела не было) | у 71 из 71 с ведомостью |
| из них раздел говорит «ничего» | — | 39 |
| шапок утверждений в разделе (из 180 утверждений) | 0 | **87** |
| строк `шаг N …` в СЛОВАХ ведомости | **0** | **14** |
| из них называют обоснование | 0 | **14** |
| из них называют правило ведомости (`по закону «…»`) | 0 | **0** |
| строк `вывод N ИМЯ …` в словах ведомости | 0 | **162** |
| строк `правило «…»` отдельной строкой | 0 | **75** |
| строк `случай …` | 0 | **10** |
| строк `посылка …` | 0 | **18** |

Прогон по семьям сверщика (`flang/proof/checker/tests/families`, 135 программ,
не досчитавшихся 0) — там лежит `ledger-steps`, единственное место дерева, где
правила ведомости написаны рукой. До правки раздела не существовало вовсе,
поэтому столбец «до» в этом наборе — ноль по построению, а не по замеру:

| мерка | после |
|---|---:|
| ведомость напечаталась / раздел напечатан | 97 / 97 |
| раздел говорит «ничего» | 36 |
| шапок утверждений в разделе | 112 |
| строк `шаг N …` | 30 |
| из них называют обоснование | 30 |
| из них называют правило ведомости (`по закону «…»`) | **10** |
| строк `вывод N ИМЯ …` | 106 |
| строк `правило «…»` | 81 |
| строк `случай …` | 14 |
| строк `посылка …` | 45 |

### Что этим НЕ закрыто, и почему именно так

**Имя правила ведомости ПОШАГОВО — долг за перепечаткой семени, и правка его не
берёт.** У шага, написанного словом поиска (`по свойству`, `по предположению`,
`по примеру`), в ведомость уезжает ОБОСНОВАНИЕ, которое написал человек, а не
имя правила из `flang/proof/tables/inference-rules.tsv`, которым ядро шаг
закрыло. Измерено: из 14 шагов probes+fixtures правило ведомости названо у 0 —
ровно потому, что ни одна из этих программ не пишет `по закону`. Причина не в
печати: в ядре имя правила у шага ЕСТЬ (`Итог шага`.«правило»,
`flang/self/proofterm.flang:497`), но в вердикт оно уезжает ОДНИМ полем — «последнее
сработавшее правило» (`Итог цепочки`.«правило», там же строка 1909, поле `rule`
машинного вида собирается на строке 2519 и только когда оно не пусто). Пошагово
его не видит ни `zapis.flang`, ни `proof.flang`, ни машинный вид: в JSON у
обязательства с теоремой поля `discharge.rule` НЕТ вовсе (снято на
`flang/proof/probes/syllogism/programs/chain.flang`), а `proof.steps[].by.rule`
несёт слово поверхности (`"property"`), а не имя ведомости.

Чтобы шаг называл правило ведомости ВСЕГДА, нужны три правки в печатаемой части
семени — и все три требуют перепечатки, поэтому здесь они не сделаны:

1. `flang/self/proofterm.flang` — `Ход цепочки` обязан копить СПИСОК имён правил
   по шагам, а не одно последнее; `Итог цепочки` — нести этот список.
2. `flang/self/zapis.flang` — печатать имя правила в строке `шаг N …`, а не
   только обоснование автора.
3. `flang/self/proof.flang` — «Утверждение термом» обязано называть правило так
   же, как его называет «Чем сведена цель правилом» у утверждения без теоремы.

Пока перепечатки нет, ведомость читает то же, что читает сверщик, — и это
измеримо лучше, чем «терм принят ядром, 4 шага» без единого имени.

**Строка `посылка … правило «»` печатается с ПУСТЫМ именем** (снято на
`induction.flang`, начало свёртки). Это не изъян печати: имя пусто в самой
записи. Строка оставлена как есть нарочно — пустое место видно, а умолчание
читалось бы как «правила нет вовсе».

### Сторожа, 26 сентября 2026

| сторож | ответ |
|---|---|
| `./ярлык задачник:проверка` | **код 0** — «задачник цел: всего задач 162: свободно 142, в работе 15, сделано 5, отклонено 0» |
| `sh scripts/guards/prose-numbers-guard.sh` | примет 231: сошлось 229, **разошлось 2** — и оба разошлись ОТ ЭТОЙ ПРАВКИ |

Правка добавила ровно 112 строк в `flang/src/emit/c/flang_repl.c` (`git diff
--stat`), и ровно на эти 112 разошлись два счётчика строк в
`docs/tree-inventory.md`:

* `:168` `строк-в flang/src/emit/c/*,…` — в примете 49143, снято 49255;
* `:82` `строк-в *.c,*.h` — в примете 864824, снято 864936.

До правки сторож был зелен: оба числа сходятся с приметой, если вычесть 112.
Третья примета, `:167` `строк-в bootstrap/*.c,bootstrap/*.h` (816538), краснела,
пока в рабочей копии лежало пересобранное зеркало `bootstrap/flang_repl.c`
(816650), и снова зелена, когда зеркало откачено, — зеркало в коммит не вошло.
Приметы НЕ починены здесь: `docs/tree-inventory.md` вне ячейки этого захода.
Чинить их одним движением вместе с перепечаткой семени: зеркало меняется тем же
движением, и второй раз числа разойдутся.

### Чего в этом заходе не делалось, и это названо прямо

* `bootstrap/flang_repl.c` — зеркало исходника (шапка из девяти строк плюс
  побайтовая копия; `scripts/bootstrap-c.sh` кладёт его ключом `--repl`). Для
  замеров зеркало собиралось в рабочей копии и двоичный пересобирался, но в
  коммит оно НЕ ВОШЛО: файл напечатан, а не написан, и лежит вне ячейки. Значит
  на свежем клоне `make -C bootstrap` даст ПРЕЖНЕЕ поведение, пока зеркало не
  положат (`sh scripts/bootstrap-c.sh`) — это шаг, который обязан сделать
  ведущий вместе с перепечаткой.
* `sh scripts/доказуемость.sh` и доля корпуса — НЕ ГОНЯЛИСЬ. Машина в этот заход
  несла среднюю нагрузку от 26 до 41 при 38 сессиях, и один замер по 131
  программе шёл вместо двух с половиной минут больше сорока; за такую цену число
  прибора было бы снято не про дерево, а про нагрузку. Правка ни ядра, ни разбора
  не трогает, но это довод, а не замер, и вердикт остаётся не снятым.
* Ошибка захода, названная своей: раздел сначала брал только `правило`, `шаг` и
  `вывод`, и на `induction.flang` напечатал два разных шага под одним номером
  «шаг 1». Поймано прогоном, а не рассуждением; строка `случай …` добавлена
  после этого.

### Правка чисто ДОБАВЛЯЮЩАЯ — сверено двумя двоичными на одном дереве

Из рабочей копии собраны ДВА двоичных: «до» (зеркало
`bootstrap/flang_repl.c` откачено к `gh/main`) и «после». На четырёх программах
сличены три вывода сразу:

| программа | поток вывода | поток ошибок | `--json` | запись `--record` | код |
|---|---|---|---|---|---|
| `flang/proof/probes/syllogism/programs/chain.flang` | +13 строк раздела, прежние 35 знак в знак | тот же | знак в знак | знак в знак | 0 → 0 |
| `flang/proof/checker/tests/families/ledger-steps/steps.flang` | +28 строк раздела, прежние 34 знак в знак | тот же | знак в знак | не снято | 0 → 0 |
| `flang/proof/probes/run/programs/induction.flang` | +10 строк раздела, прежние 31 знак в знак | тот же | не снято | не снято | 0 → 0 |
| `flang/proof/probes/run/programs/by-type.flang` | +9 строк раздела, прежние 31 знак в знак | тот же | не снято | не снято | 0 → 0 |

Пробы силлогизма (`sh flang/proof/probes/syllogism/run.sh`) на ОБОИХ двоичных
отвечают одинаково: **разошлось 5 из 6**. Это расхождение НЕ от этой правки —
столбец «слово двоичного» в `expected.tsv` ждёт от двоичного отказа разбора слова
`следует`, а семя этого дерева уже перепечатано и слово знает. Мерка не
шевельнулась: 5 до, 5 после.
