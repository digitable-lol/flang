---
номер: 3560
заголовок: Сколько функций корпуса и примеров владельца упираются в отсутствие правил о последовательности шагов, эффектах и процессах — число до ADR и кода
статус: в работе (замер снят и ADR-0044 написан 17 сентября 2026; принят overagent'ом тем же днём — PR #61)
исполнитель: a
ветка: a/3560-logic-in-time-count
команда: первая
карта: Чего в языке нет вовсе
рядом: 1404, 1405, 1406
нужность: 1 — владелец: сначала замер, потом ADR с ценой, потом код (1404 Ш2, 1405 Ш1, 1406 Ш1)
---

# 3560. Замер: где упираются логика во времени, эффекты и параллелизм

## Чем измерено

17 сентября 2026, main `cbfaf3899`: 1404 — Ш1 сдан (Инд2/Инд3), Ш2 ждёт; 1405 — Ш0
сделан, Ш1 ждёт; 1406 — Ш0 сделан, Ш1 ждёт. Число мест, которым эти правила нужны, не снято.

## Что сделать

1. Снять числа: сколько планов (`план`), процессов (`процесс`) и свёрток по прогону в
   `flang/**/*.flang` (без flang/self), `examples/**`, `flang/concurrency/**` и корпусе; у скольких
   есть утверждение, которое сегодня нельзя высказать (по спецификациям 1404/1405/1406).
2. ADR-0044: цена по файлам (строки C у сверщика — с бюджетом в ratchet.txt, строки
   flang/self, леммы Lean) на каждый из трёх шагов; порядок.
3. После принятия ADR overagent'ом — первая ячейка кода с пробами и Lean.

## Как поймём, что сделано

Числа стоят в этой задаче и в 1404/1405/1406; ADR-0044 принят; первая ячейка кода — PR.

## ⚑ 17 сентября 2026: замер

Ветка `a/3560-logic-in-time-count` над `main` `a43ab4d25`, двоичный `bootstrap/flang` 0.7.19,
1001 файл под учётом git. Таблицы по каталогам, команды и места поимённо —
[ADR-0044](../adr/0044-time-effects-and-processes-are-counted-before-they-get-rules.md) §2–3; прибор для M1–M3 (сценарий python, критерии в ADR дословно) и его
вывод лежат в `ПЕРЕДАЧА/отчёты/3560-zamer.py` и `3560-zamer-full.txt`.

| что | число | чем снято |
|---|---:|---|
| N1 планов `план «` (без `flang/self`) | 229 (228) | `git ls-files -z -- '*.flang' '*.fp' '*.фп' '*.фланг' \| xargs -0 grep -c -E '^план «'`, сумма |
| N2 процессов `процесс «` | 94 | то же с `'^процесс «'`; `flang ast` через `владение-состоянием.sh` — 94 |
| N3 функций-прогонов (тело — свёртка, шаг — функция) | 256 | сценарий; свёрток в телах 1623, с шагом-функцией 590 |
| корпус (89 записей): планов / процессов / прогонов | 0 / 0 / 2 | по строке `исходник` каждой записи |
| M1 — прогоны с постусловием у шага | **133** (28 без своего постусловия; записано утверждений 2, оба — пробы) | сценарий |
| M2 — планы, выдающие поручения | **188** (23 только читают) | сценарий |
| M3 — процессы с обработчиком | **93** | сценарий; `владение-состоянием.sh` — 93 |
| **M1 + M2 + M3** | **414 ≠ 0** | фальсификатор «правила никому не нужны» не сработал |

Цена: строка бюджета сверщика **ADR-0044 +115** (80 на 1404 Ш2 — аналог Инд1 78, замер;
35 на 1406 Ш1 — 19 + 14, замер; 1405 Ш1 строк C не ест), `flang/self` ≈ 200 строк по
аналогам. Порядок: **1404 Ш2 → 1406 Ш1 → 1405 Ш1**, одна партия перепечатки. Первый
шаг кода — 1404 Ш2: единственный без перепечатки, держит уже написанный Ш1, самое
большое из трёх чисел.
