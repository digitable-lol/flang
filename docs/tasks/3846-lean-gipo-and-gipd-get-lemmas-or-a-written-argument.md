---
номер: 3846
заголовок: Две строки ведомости без леммы — ГипО и ГипД — получают леммы Lean либо письменный довод, почему лемма им не положена
статус: в работе
исполнитель: a
ветка: a/3846-lean-gipo-gipd
команда: первая
карта: Сколько доказано на самом деле
рядом: 1130, 6812
нужность: 2 — `прогон.sh`: «строк 109, из них с леммой 107, без леммы 2: ГипО, ГипД» (17 сентября 2026, main `cbfaf3899`)
---

# 3846. ГипО и ГипД: леммы или довод

## Чем измерено

`PATH=$HOME/.elan/bin:$PATH sh flang/proof/lean/прогон.sh` на main `cbfaf3899`, Lean 4.34.0:
перечень — строк 109, с леммой 107, без леммы 2: ГипО, ГипД; лемм 163; ловушек 47,
отвергнуто 47; расхождений сверщик↔Lean 0. ГипО и ГипД стоят в приёмке
(`Приёмка.lean:708–712`) и в состоятельности (`Состоятельность.lean:1349–1352`), но строки
ведомости без леммы.

## Что сделать

Понять, что именно «лемма» значит для открытой гипотезы (она снимается Р1/Р4/Кон2), и либо
написать леммы в `Правила.lean` с ловушками, либо записать в ведомости/ADR довод, почему
у гипотезы леммы нет, и чем тогда покрыта её состоятельность.

## Как поймём, что сделано

`прогон.sh`: строк 109, с леммой 109 — либо ADR с доводом, и `прогон.sh` печатает
«без леммы 2: ГипО, ГипД — по доводу ADR-NNNN».

## Ход работы

**17 сентября 2026** (исполнитель a, ветка `a/3846-lean-gipo-gipd` над main `63a48f925`).
Решение: леммы, не довод. Гипотеза — механизм, не закон (ADR-0027): шаг ГипО/ГипД ничего
не выводит из окружения, а кладёт себя в открытые гипотезы; в модели это «У в голове
окружения суждения», и лемма строки — ровно проекция Γ×У → У.

1. `Правила.lean` — «ГипО» (Γ, У ⊢ У; строка ведомости над леммой дословно), «ГипО-снятие»
   (пара с «Р1»), «ГипО-без-снятия-ложно»; «ГипД» (посылки: «Л или П» из окружения,
   А — ровно Л или П), «ГипД-снятие» (пара с «Р4», сечение по «Л или П»),
   «ГипД-без-снятия-ложно».
2. `Ловушка.lean` — искажения 55, 56: гипотеза принята с пустым списком открытых (ГипО без
   себя в Γ; половина дизъюнкции без открытой гипотезы). Lean отверг обе.
3. `Состоятельность.lean` — случаи ГипО и ГипД теоремы «состоятельность» переведены с
   «Т1» + `List.mem_cons_self` на леммы строк; таблица шапки поправлена.

| число (`прогон.sh`) | до (main `cbfaf3899`) | после |
|---|---|---|
| строк ведомости / с леммой | 109 / 107 | 109 / 109 |
| без леммы | 2: ГипО, ГипД | 0 |
| лемм в `Правила.lean` | 163 | 169 |
| ловушек / отвергнуто | 47 / 47 | 49 / 49 |
| аксиом сверх трёх стандартных | 0 | 0 |
| расхождений сверщик ↔ Lean | 0 | 0 |

Ведомость `ПРАВИЛА-ВЫВОДА.tsv` и отпечаток не тронуты: пару «строка ↔ лемма» `прогон.sh`
берёт по имени `theorem` в `Правила.lean`. Храповика Lean-ловушек в дереве нет: число
«искажений N» прогон считает по маркерам «ИСКАЖЕНИЕ» в `Ловушка.lean`, а
`чекер/пробы/ловушка/ХРАПОВИК.txt` стережёт ловушку проверки 1, не Lean.
