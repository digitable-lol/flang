---
номер: 4413
заголовок: Свод ведомости доказательств — последний сводитель на Node: у двойника нет веера и сужения
статус: в работе
исполнитель: a
ветка: a/4413-svod-vedomosti-bez-node
команда: вторая
карта: Чего в языке нет вовсе
рядом: 0049, 4412, 6858
нужность: 2 — `proof-ledger.mjs` и `word-guard.mjs` сняты 26 сентября 2026; осталось расхождение `доказательства:свод` и `доказательства:ведомость`
---

# 4413. Свод ведомости: двойник есть, но он верхняя оценка, а не точная

## Откуда задача

Задача 0049 сносит оснастку на JavaScript из трёх каталогов. В её итоге волны 2
(10 сентября 2026) про `flang/scripts/proof-ledger.mjs` записано «двойника нет;
прогон — часы». **Первая половина этой записи неверна, и поправка — повод завести
задачу, а не молчать:** двойник в дереве есть — `flang/scripts/ledger-summary.fscript`,
и у него свой ярлык `доказательства:свод`. Неверно другое слово: двойник не
неполон «немножко», он даёт ВЕРХНЮЮ оценку там, где `.mjs` даёт точную, и сам
об этом говорит в шапке.

## Чем измерено

Прогоны 13 сентября 2026 в дереве ветки `r/bez-node` от `dev` `fcd4a9554`,
двоичный `bootstrap/flang` 0.7.19 (sha256 `50b33427…`).

| что | число |
|---|---|
| `flang/scripts/proof-ledger.mjs` | 786 строк, 6 вывозов |
| `flang/scripts/ledger-summary.fscript` | 345 строк, один план «Свод» |

**Вывозы `.mjs`:** `proofLedger`, `ФАЙЛЫ`, `ведомостьФайла`, `подменыСлов`,
`пустотаПоМаске`, `сводКорпуса`.

## Почему он снимается последним

`proof-ledger.mjs` — замко́вый камень кучи `.mjs`: он ВВОЗИТ четырёх соседей, и
пока он жив, ни одного из них не снести, как бы ни сошлись их собственные
двойники:

| ввозится в `proof-ledger.mjs` | что берётся |
|---|---|
| `flang/scripts/direct-run.mjs` | `запущенНапрямую` |
| `flang/scripts/binary.mjs` | `ведомость`, `ведомостьИсходника`, `дерево`, `прогреть`, `прогретьВедомости`, `своё` |
| `flang/scripts/word-guard.mjs` | `СЛОВАРЬ`, `говоритДоказано`, `пустота`, `словоСтроки` |
| `flang/test/glob.mjs` | `globSync` |

Сам он ввозится в двух местах: `docs/site/site-numbers.mjs` (`ФАЙЛЫ`,
`сводКорпуса`) и `flang/scripts/count-guard.mjs` (`сводКорпуса`, ввоз по месту
требования). Второй ввоз замыкает кольцо: `word-guard.mjs` ввозит
`count-guard.mjs`, а тот — обратно `proof-ledger.mjs`.

## Чего двойнику не хватает — его же словами

Шапка `ledger-summary.fscript` называет пропущенное сама, и переписывать это
своими словами не нужно:

1. **ВЕЕР.** Файлы спрашиваются по одному, последовательно. В `.mjs` прогрев идёт
   `xargs -0 -P N`. Отсюда «свод пойдёт часами, а не пять минут».
2. **СУЖЕНИЕ ДО СВОИХ ОБЪЯВЛЕНИЙ.** `.mjs` вычитает подключения (`своё`), иначе
   `flang/stdlib/lists.flang` считается столько раз, сколько модулей его зовёт.
   В двойнике вычитания нет, и числа свода — с повторами.
3. Обратная достижимость по вызовам (`noRuntimeCheck`, `noRuntimeCheckDeep`),
   подмены слов вердикта, разбор `--json`, ключ `--files`.

Пока эти три не дописаны, свод — верхняя оценка, и так и напечатано в выводе.
Ярлык `доказательства:ведомость` поэтому и остался на `node`.

## Что НЕ входит в задачу

Второй ярлык того же `.mjs` — `доказательства:пустота` — переносить не надо и
не следует: он дублирует готовый `flang/scripts/emptiness-guard.fscript` (ярлыки
`пустота:проверка` и `пустота:перепись`), который судит тот же корпус по тем же
четырём кучам, держит снимок в `emptiness-ledger.txt` и проверяется порчей.

## Как понять, что сделано

1. `доказательства:свод` и `доказательства:ведомость` дают на одном дереве
   **одинаковые числа**, а не близкие: сверка знак в знак, как её требует 0049.
2. Сужение `своё` перенесено — свод перестал считать библиотечную функцию
   столько раз, сколько модулей её зовёт.
3. Ярлык `доказательства:ведомость` зовёт двойник; `node` из него ушёл.
4. Только после этого `proof-ledger.mjs` снимается — и вместе с ним
   разблокируются `binary.mjs`, `direct-run.mjs`, `word-guard.mjs`, `glob.mjs`.

## Грабли, названные заранее

`flang io` не принимает доводов (ни `--args`, ни хвоста строки) — отбор только
`--plan`. Ключ `--pustota` у `.mjs` на плане не выражается, и это ещё одна
причина, по которой `доказательства:пустота` остаётся за `emptiness-guard.fscript`.

---

# Ход работы 26 сентября 2026

Ветка `a/4413-svod-vedomosti-bez-node` от `gh/dev` `2363d95d3`, двоичный собран
в ней же (`make -C bootstrap -j8`, код 0). Машина под чужой нагрузкой: средняя
очередь 42→68 при 256 ядрах, в том же дереве соседняя сессия снимала ведомости.

## Замер ДО правки: условие задачи устарело наполовину

Первое, что показал прогон, — **условие этой задачи и её поле «нужность»
неверны на сегодняшнем стволе**, и неверны они с 14 сентября, когда задача 1422
сдала шаги Ш0–Ш3.

| что сказано в задаче | что показал прогон 26 сентября |
|---|---|
| «ярлык `доказательства:ведомость` зовёт node» | НЕТ: `ярлыки.flang:341` зовёт `bootstrap/flang io flang/scripts/proof-ledger.fscript --plan 'Свод корпуса'` |
| «у двойника нет веера» | ЕСТЬ: `proof-ledger.fscript`, функция «Команда веера» — тот же `xargs -P` с `FLANG_POTOKOV` |
| «у двойника нет сужения до своих объявлений» | ЕСТЬ, и есть отказ `FLANG_SVOD_ZAKONY`, когда сужение по своим именам перестаёт годиться |
| «нет `--json`, `--files`» | ЕСТЬ: планы «Свод корпуса машине» и «Свод корпуса по файлам» |

Двойник, которого задача называет неполным, — это `ledger-summary.fscript`
(ярлык `доказательства:свод`). Он и сегодня верхняя оценка. Но ПОЛНЫЙ двойник в
дереве тоже есть, и это другой файл — `proof-ledger.fscript`, 1430 строк.
<!-- СНЯТО 2026-09-26 строк flang/scripts/proof-ledger.fscript = 1430 -->

Значит, остаток задачи — не «дописать сведение», а её пункт 4: **снять
освободившийся `.mjs`**. Он и сделан.

### Что звало node на этом пути и что держалось ввозами (прогон, `grep` по дереву)

Ярлыков, зовущих `node`, было **15** строк команды в `ярлыки.flang` (примеры
внутри функций не считаны). На пути ведомости из них стоял один —
`доказательства:пустота` → `node flang/scripts/proof-ledger.mjs --pustota`.

Ввозы `proof-ledger.mjs` (`grep` по всем `.mjs` дерева) — четверо, как задача и
говорит: `direct-run.mjs`, `binary.mjs`, `word-guard.mjs`, `flang/test/glob.mjs`.
Ввозят его самого двое: `docs/site/site-numbers.mjs:53` (`ФАЙЛЫ`, `сводКорпуса`)
и `flang/scripts/count-guard.mjs:947` (`сводКорпуса`, ввоз по месту требования).

Проверено поимённо, кого держал только он: из четверых троих держат и другие
(`direct-run` — пятеро, `binary` — семеро, `glob` — четверо), а **`word-guard.mjs`
не ввозил больше никто**: его ярлыки `слово:проверка` и `слово:подлог`, как и
работы CI `binary.yml:652` и `:1687`, зовут `word-guard.fscript`.

## Что сделано

**1. Свод корпуса для оснастки на JavaScript спрашивается у двойника на flang.**
`сводКорпуса` и список корпуса `ФАЙЛЫ` переехали из `proof-ledger.mjs` в
`flang/scripts/binary.mjs` — это мост к двоичному, и другого счёта ведомости в
JavaScript в дереве теперь нет вовсе. Новый `сводКорпуса` запускает
`bootstrap/flang io flang/scripts/proof-ledger.fscript --plan 'Свод корпуса
машине'` и разбирает ответ.

Конверт `flang io` — одна строка JSON: на успехе
`{"plan":…,"result":…,"orders":…,"log":[…]}` в поток вывода, на отказе
`{"error":…}` в поток ошибок. Журнал поручений в конверте лежит ПОСЛЕ значения и
у свода корпуса весит сотни мегабайт, поэтому значение берётся ножницами по
кавычкам, а не разбором конверта целиком.

Код 3 («ведомость не снята ни у одного файла») обёртка превращает в отказ, а не
в нули: у `.mjs` в этом случае возвращался итог из нулей, и сверщик прозы
принимал бы его за «сошлось».

**2. Сняты два файла.** `flang/scripts/proof-ledger.mjs` (787 строк) и
`flang/scripts/word-guard.mjs` (706 строк).

**3. Убран ярлык-дубль `доказательства:пустота`.** Он звал снятый `.mjs`.
Переносить его эта задача и запрещает: тот же вопрос по тем же четырём кучам
задают `пустота:проверка` и `пустота:перепись` над `emptiness-guard.fscript`.
**Это решение, а не следствие:** ярлык убран, а не переведён, и владелец может
его вернуть — тогда режим пустоты придётся писать планом в
`proof-ledger.fscript`, потому что доводов у плана нет (грабли, названные в
самой задаче).

## Было → стало, всё прогоном

| что | прибор | было | стало |
|---|---|---:|---:|
| файлов `.mjs` в дереве | `git ls-files '*.mjs' \| wc -l` | 35 | 33 |
| из них в `flang/scripts` | `git ls-files 'flang/scripts/*.mjs' \| wc -l` | 8 | 6 |
| файлов `*.js,*.mjs` | сторож прозы, прибор «файлов» | 51 | 49 |
| строк в них | сторож прозы, прибор «строк-в» | 24 386 | 23 010 |
| файлов вне flang | сторож прозы, прибор «файлов» | 251 | 249 |
| ярлыков | `./ярлык ярлыки`, план «Целость» | 147 | 146 |
| ярлыков, зовущих `node` | `grep '"node ' ярлыки.flang` без примеров | 15 | 14 |
| «Ярлыки о доказательствах» | обещание функции | 20 | 19 |
| мест отбора по расширению | `./ярлык расширения:проверка` | 45 | 44 |

Строк `binary.mjs` прибавилось 117 — переехавшие `ФАЙЛЫ`, обёртка свода и
разбор конверта. Чистый счёт по JavaScript: −1493 +117 = **−1376 строк**.

## Прогоны проверок

| проверка | код | что сказала |
|---|---:|---|
| `sh scripts/guards/prose-numbers-guard.sh` | 0 | примет 231, сошлись все |
| `./ярлык ярлыки` | 0 | «ярлыков 146; посмотрено 145 — всё нашлось в дереве»; сбор: «объявлений 143; расходится 0» |
| `./ярлык расширения:проверка` | 0 | «мест отбора 44 — все называют оба целиком» |
| `./ярлык задачник:проверка` | 0 | «всего задач 162: свободно 141, в работе 16, сделано 5» |
| `./ярлык зов:проверка` | 0 | «сторожа без зова: 28, все названы в ведомости» |
| `no-comments-guard.fscript --plan Проверка` | 0 | «комментариев не прибавилось: строк 34 652 в 458 файлах» |

Сторож комментариев говорит «убрано с прошлого снятия: 6» — это НЕ моя правка:
в `.flang` и `.fscript` я ни одной строки с `//` не тронул, долг отстал раньше.
Снимать чужой долг этой веткой не стал.

## Чего эта работа НЕ сделала

**Приёмка самой задачи 4413 не выполнена.** Её пункт 1 — «`доказательства:свод`
и `доказательства:ведомость` дают одинаковые числа» — не выполнен и этой веткой
не трогался: `ledger-summary.fscript` как был верхней оценкой, так и остался.
Чинить его сведе́нием бессмысленно — точный счёт уже написан в
`proof-ledger.fscript`; решать надо иначе: либо `доказательства:свод` убрать как
второй, более слабый ответ на тот же вопрос, либо оставить и сказать в его
выводе, что он дешёвая верхняя оценка рядом с точным. **Это решение владельца, и
я его не принимал.**

**`binary.mjs`, `direct-run.mjs`, `glob.mjs` не сняты** — их держат другие
ввозящие, перечисленные выше; порядок сноса у них свой, в задаче 4412.

**`count-guard.mjs` и `site-numbers.mjs` остались на node** — переписывает их
6858 и 4412. Эта ветка только отняла у них второй счёт ведомости.

**Правка стала дороже прогоном.** У `.mjs` счёт свода занимал около 4 минут
против 21,5 у двойника (замер задачи 1422, 14 сентября, не мой). Значит,
`числа:проверка` и `подсчёты:проверка`, которые теперь зовут двойник, стали
настолько же дольше. Своим прогоном я это не перемерил — сказано словами, число
не моё.

## Мои ошибки

1. **Разбор конверта написал неверно с первого раза.** Искал ключ `plan`, а
   значение лежит за ключом `result`; на конверте отказа — за `error`. Нашёл
   своей же пробой на двух настоящих конвертах, до прогона.
2. **Забыл два обещания о числе ярлыков** (`ярлыков сто сорок семь`,
   `сколько ярлыков сто сорок семь`). Нашёл их не чтением, а красным прогоном
   `./ярлык ярлыки` — `FLANG_PROPERTY`. Правило «снял правку и посмотри, что
   покраснеет» здесь сработало за меня, а должно было — до.
3. **Сложил свои выводы прогонов в общий временный каталог**, где в ту же минуту
   писала соседняя сессия. Столкновения не вышло, но имена были общие — перенёс
   в свой подкаталог уже после того, как увидел чужие процессы.
