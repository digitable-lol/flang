---
номер: 6432
заголовок: Пять мест на слове ядра закрываются четырьмя правилами — мера добавления и свёртки, развёртка вызванного, проекция поля и разбор суммы автора
статус: в работе
исполнитель: a
ветка: a/6432-6812-3448-last-places
команда: первая
карта: Сколько доказано на самом деле
рядом: 9616, 6812, 3448, 1130
нужность: 1 — пять из двенадцати последних мест доли (ADR-0042 §2)
---

# 6432. М1, М2, Разв3, К↑, Разв1 для суммы

Решение: [ADR-0042](../adr/0042-the-last-twelve-places-get-rules-not-exceptions.md) §2.

## Места

`abilities` У3, `poddelka-svyortka-shagom` «нулей ровно столько, сколько элементов»,
`poddelka-strict-order` «в отданном списке есть хотя бы одно число» — М1/М2;
`equality` Р1 — Разв3 и К↑; `types` Т1 — Разв1 для суммы автора. Таблица и
причины — задача 9616, раздел «Что осталось до 100 %».

## Что сделать

Для каждого правила по порядку партии: строка `flang/proof/ПРАВИЛА-ВЫВОДА.tsv` →
лемма `Правила.lean` + ловушка `Ловушка.lean` → случай в `Приёмка.lean` и
`Состоятельность.lean` (ADR-0041) → приём `сверщик.c` → печать `zapis.flang` →
запись места толкованием (зонд `zond-k7`), сверщик код 0 → порча — код 1.
Коды записей-подделок «лжёт-программа» (`НАБОР.tsv`) обязаны остаться прежними.

## Как поймём, что сделано

Пять мест переиграны на печати ядра (пробы); `прогон.sh` — «сошлось всё»; Lean —
сверка вердиктов без расхождений; бюджет ADR-0042 §3 соблюдён.

## Ход работы

14 сентября, исполнитель a, ветка `a/6432-6812-3448-last-places` (от main `d8b82eaf`).

| правило | ведомость | лемма, ловушка | приёмка Lean | сверщик | печать | место на пробе |
|---|---|---|---|---|---|---|
| М1 | строка, семейство М | «М1» (посылка длина + 1 ≤ 2⁵³), ловушка 25 | **есть**: вторая сторона — литерал меньше 2⁵³ по модулю («собрать-М1») | `сторона_переписана` | «Блок меры прибавления записи» | `poddelka-strict-order` — зонд 836 с, пик 50,6 ГиБ, код 0; Lean ПРИНЯЛ блок целиком |
| М2 | строка | «М2», «М2-пустое-начало», ловушка 26 | **есть**: шаг свёртки — «добавить\|приписать Х к а» («мера2-значение») | `мера_свёртки` (реестр доказанного) | «Блок меры свёртки записи» | `abilities` У3 — зонд 2694 с, пик 51,7 ГиБ, код 0; `poddelka-svyortka-shagom` — зонд 841 с, пик 49,7 ГиБ, код 0 |
| Разв3 | строка | «Разв3», ловушка 28 | **есть**: своё тело — факт «результат равен телу», вызов — факт «функция равна своему телу» («замФ-верно») | вызов — телом, «результат» — своим телом | «Блок круга записи» | `equality` Р1 — зонд 748 с, пик 48,7 ГиБ, код 0 |
| К↑ | строка, ADR-0022 §3.8 сужен | «К↑», ловушка 27 | **есть**: поле читается числом («полеСтороны-значение») | поле выписанного конструктора | «Блок круга записи» | `equality` Р1 |
| Разв1 (сумма) | строка расширена | «Разв1-сумма», ловушка 29 | **есть**: ветвь своего варианта, пары без захвата («подстПар-значение») | «из N», цель-разбор строками | «Блок разбора суммы записи» | `types` Т1 — зонд 693 с, 48,6 ГиБ пика, код 0, на слове ядра 0 |

**М1 в приёмке Lean — со второй стороной-литералом.** Длина списка модели не
ограничена, а «плюс» модели выше 2⁵³ округляет: равенство меры «плюс 1» держится
лишь до порога. С литералом меньше порога переписка верна и за ним — выше порога
округлённое не ниже порога («моно»), и посылка ложна либо заключение истинно
(«собрать-М1», «М1-значение»). Сверщик берёт любую вторую сторону: длина списка
языка меньше 2⁵³ — граница модели, названа в шапке `Приёмка.lean`.

**Приёмка Lean — у всех пяти правил.** Модель терма (`Терм.lean`) выросла на три
вещи: списочную свёртку (М2); суммы автора — выписанный конструктор, поле
`( С ) .«п»`, разбор суммы в формуле, тело-сумму (К↑, Разв1); довод-вызов без
сорта — довод `( «Г» от … )` несёт итог вызова целиком и, подставленный на место
параметра, читается сортом этого места. Окружение утверждения узнало функции с
телом одной строкой — факт «функция равна своему телу» (Разв3; читаются только
функции, чью строку тела называет шаг Разв3 записи). Теорема состоятельности — для
71 правила из 78 приёмов `шаг_вывода`, аксиомы три стандартные, ловушек 29 из 29,
сверка вердиктов — расхождений 0.

Блоки печати ядра у этих мест сверка по-прежнему ставит «вне приёмки»: первым
шагом у каждого стоит Выч, а его вычислитель модель не повторяет (PR #29).
Приёмку новых правил проверила отдельная проба: исходник, где база — допущение
(Т1), а не Выч, и за ней К↑, три Разв3 (два вызова и своё тело), Разв1 для суммы.
Lean — ПРИНЯЛ; порченые копии — ОТВЕРГ ровно на испорченном шаге, и сверщик
отказал на том же шаге. В дерево проба не легла: рукописный блок шапку сверщика
не пройдёт (отпечатки исходника, тотальности), в набор запись кладёт печать
зонда, а зонд базой печатает Выч.

Где модель строже сверщика — названо в шапке `Приёмка.lean` (пункты 4–8): М1 —
вторая сторона-литерал; М2 — шаг без вызова; К↑ — поле только числом; Разв1 — у
каждого имени ветви есть поле, пары чисты; Разв3 — вызов не под связывателем,
пары «параметр — довод» чисты. Мягче одно: Разв3 берёт за шаг любое число
вхождений вызова. На наборе ни одно место вердикта не развело.

Бюджет ADR-0042: +166 строк кода сверщика из +180 (вместе с Пр1 и 3448).
