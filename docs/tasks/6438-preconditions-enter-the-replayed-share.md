---
номер: 6438
заголовок: Сверщик переигрывает снятие предусловий, и они входят в долю — «никогда» задачи 1729 отменяется для обязательства
статус: в работе
исполнитель: a
ветка: a/5332-6438-precondition-discharge-in-the-record
команда: первая
карта: Сколько доказано на самом деле
рядом: 5332, 1729, 9616, 1313
нужность: 1 — пока снятие на слове ядра, слово «ПРОВЕРЕНО САМОСТОЯТЕЛЬНО» о контракте наполовину ложно
---

# 6438. Снятие переигрывается и считается

Решение: [ADR-0038](../adr/0038-a-precondition-is-discharged-by-a-printed-derivation.md) §3.2–3.3.
Ш0 — до печати корпуса задачей 5332 (условие партии ADR-0026 §11).

## Чем измерено

13 сентября 2026: сверщик `flang/proof/чекер/сверщик.c` — 6982 строки кода,
потолок 7188; читает `требует` исходника как допущение (около строки 1011) и
проверяет оплату `требует` у вызванной для восьмого хода (около 980–1015);
блока снятия в записи не существует, приёма — тоже. Линейка доли
(`flang/proof/доля-корпуса.sh`, `flang/self/proof.flang`) собирает числитель из
постусловий (1729). Сегодня 629 / 651 = 96,62 %.

## Что сделать

1. **Ш0 — приём блока снятия** в сверщике по образцу приёма утверждения: цель,
   место, имя; вывод — существующими шагами (Т1/Т2, Н, факт обещания вызванной).
   Формы, которых в словаре нет, — замерить и назвать; заводить по общему порядку
   (строка ведомости, лемма Lean, ловушка, приём). Бюджет ADR-0038 §3.5: +80 строк
   кода на обе задачи.
2. Подделки: подменённая посылка; чужое место; снятие, названное для вызванной с
   неоплаченным `требует` — все код 1 с причиной; в `прогон.sh` и `НАБОР.tsv`.
3. Доля: знаменатель получает снятия, числитель — переигранные; линейка печатает
   ДВЕ доли рядом (старую и новую) до сходимости; порог 95 % — на новую только
   после печати корпуса и приёма (ADR-0038 §3.3). Недостижимые — по ADR-0023.
4. Пересъёмка записей корпуса печатью ядра (толкованием), сверка
   `доля-корпуса.sh --вложенность`.
5. Слово: 1729 — раздел «Отменено ADR-0038 в части обязательства»; числа
   `README.md`, `docs/DESCRIPTION.md`, `docs/ROADMAP.md`,
   `docs/road-to-one-hundred-measured.md` — прибором.

## Чего не делать

- Не вычислять снятие в сверщике («калькулятор снят» — ловушка проверки 1).
- Не сливать две доли в одну раньше сходимости.
- Не трогать `bootstrap/**`.

## Как поймём, что сделано

Честная запись — код 0, подделки — код 1; `sh flang/proof/чекер/пробы/прогон.sh`
— «сошлось всё»; новая доля напечатана и не ниже 95 % после пересъёмки;
`sh scripts/доказуемость.sh` — ДОКАЗУЕМ; строк кода сверщика на обе задачи
прибавилось не больше 80; `ХРАПОВИК.txt` поднят ровно на съеденное.

## Ход работы

### 14 сентября 2026 — ветка `a/5332-6438-precondition-discharge-in-the-record`, от `main` `4a438c14`, перебазирована на `d8b82eaf9`

**Ш0 — приём блока** (`сверщик.c`, `сверить_снятие`, `связано_в_теле`,
`сверить_полноту_снятий`). Блок `снятие «И» функции «Ф» зовёт «Г» строка L
столбец C`; цель сверщик строит сам (хозяин строки L, вызов «Г» со столбца C в
знаках UTF-8, `требует «И»` у «Г», подстановка доводов со сторожем захвата).
Вывод — прежними приёмами: `проиграть_вывод` (Т1, семья Н) и `проиграть_блок`
(ход «факт по свойству» — обещание вызванной с оплатой `требует`, как у
восьмого хода). **Форм вне словаря не нашлось**: на пробе и на корпусе цели
закрываются Т1, Н1 (и Н5 у двойника подделки) и фактом по свойству; строк
ведомости, лемм Lean и ловушек не заведено.

Полнота — по тексту: вхождения `«Г» от` в строках тела, по одному на каждое
`требует` «Г» из того же исходника; дважды записанное место — отказ. Запись
прежнего вида (без единого блока, печать семени) не краснеет, её места
названы числом «без блока». Замер: 486 записей дерева — коды попарно те же, что
у сверщика `main`.

| что | было | стало | чем снято |
|---|---|---|---|
| строк кода `сверщик.c` | 6982 (на `4a438c14`), 7104 (на `d8b82eaf9`) | 7057 / 7179 | `sh flang/proof/сверка-ведомости.sh --строки-кода`; +75 из бюджета +80 |
| потолок `ХРАПОВИК.txt` | 7188 | 7263 | ровно на съеденное; примитивов 15 |
| `сверка-ведомости.sh` | красных 2 (опоры Инд2, Инд3) | те же 2 | `diff` списков красного с `main` |
| `прогон.sh` до новых проб | «сошлось всё» | «сошлось всё» | подделок 459, принято кодом 0 — 0; честных 216 (на `d8b82eaf9` честных 221) |
| Lean | ловушек 14 из 14 | то же | `sh flang/proof/lean/прогон.sh` (до перебазирования) |

**Подделки.** Подменённая посылка Т1 (строка `требует` вызванной), подменённое
заключение Н1, чужое место (строка вызова другой функции), выброшенный блок —
правкой честной записи в `прогон.sh`. «Обещание вызванной при неоплаченном
`требует`» — пара `unpaid-callee.{flang,запись}` в `НАБОР.tsv`, класс
«лжёт-запись»: код 1 одной причиной «предусловие «х не меньше 0» вызванной
«Тождество под требованием» в точке вызова не снято».

**Доля** (`доля-корпуса.sh --проигрыванием`): рядом с прежней строкой
`доля-проигрыванием = 629 / 651 = 96.62 %` печатается
`доля с предусловиями = 629 / 653 = 96.32 %` — два места вызова «Высоты от дна»
в `flang/proof/examples/precondition.flang` в знаменателе без блока. Порог 95 %
не перенесён: записи корпуса — печать семени байт в байт (сторож
`--вложенность`), в корпус печать ядра ляжет перепечаткой (задача 5190).
`--самопроверка` и `--вложенность` — код 0.
