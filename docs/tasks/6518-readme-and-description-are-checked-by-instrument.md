---
номер: 6518
заголовок: README.md и DESCRIPTION.md сверены с деревом прибором — каждое утверждение с командой, устаревшее переписано
статус: свободна (на 22 сентября 2026 таблицы «утверждение → команда прибора → сошлось/нет → правка» в задаче нет)
исполнитель: —
ветка: —
команда: вторая
карта: Что уже есть
рядом: 1404, 1405, 1406, 1424
нужность: 1 — владелец подозревает устаревание разделов о логике во времени, эффектах и параллелизме и чисел; README читают первым
---

# 6518. README.md и DESCRIPTION.md сверены прибором

Слова владельца (17 сентября 2026): README и DESCRIPTION сверить прибором и переписать
устаревшее; отдельно — разделы про логику во времени, эффекты и параллелизм.

## Чем измерено

Дерево `main` `cbfaf3899`, 17 сентября 2026.

- `README.md` — 515 строк (`wc -l`), английский; `docs/README.ru.md` — 499 строк.
- `DESCRIPTION.md` — символьная ссылка на `docs/DESCRIPTION.md`, 1144 строки, 12 разделов
  (`grep -c '^## '`).
- Числа с приметой `СНЯТО` стережёт `sh scripts/guards/prose-numbers-guard.sh` —
  216 из 216 на `main`; числа БЕЗ приметы не стережёт никто.
- Задачи 1404, 1405, 1406 (логика последовательности шагов, обещания плана, обещания
  процесса) — статус «в работе (замер, Ш0–Ш1…)», ветка `r/logika`: сделаны только первые
  шаги, в `main` ничего из них не влито (`git log main -- docs/tasks/1404*` — только заведение).
- `README.md:119`: «nothing about state over time, side effects or concurrency» — это
  утверждение сверить с 1404–1406 и написать, что именно есть сегодня.

## Как поймём, что сделано

- В этой задаче — таблица «утверждение → команда прибора → сошлось/нет → правка»
  по каждому числу и каждому проверяемому утверждению обоих файлов (число строк
  таблицы названо);
- расхождений после правки — 0; у каждого числа, которое можно снять прибором,
  стоит примета `СНЯТО 2026-09-17 …`;
- разделы про логику во времени, эффекты и параллелизм говорят «только первые шаги»
  и называют 1404–1406;
- `sh scripts/guards/prose-numbers-guard.sh` — все приметы сошлись; `./ярлык ссылки:проверка` код 0.

## Чего задача НЕ делает

Строки с порогом «95 %» правит задача о пороге 100 % — здесь их не трогать.

## Чем кончилось

Приём задачи — «в этой задаче — таблица «утверждение → команда прибора → сошлось/нет →
правка» по каждому числу и каждому проверяемому утверждению обоих файлов (число строк
таблицы названо)». Прогон 22 сентября 2026: `grep -c '^|' docs/tasks/6518-*.md` → **0**,
таблицы нет. Не сделана; ветка `a/6518-readme-and-description-are-checked-by-instrument`
на сервере не живёт.
