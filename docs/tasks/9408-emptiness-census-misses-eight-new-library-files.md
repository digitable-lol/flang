---
номер: 9408
заголовок: Перепись пустоты снова красна — восемь файлов библиотеки, заведённых 9–10 сентября, не названы ни в «Наших», ни в «Отложенных»
статус: свободна
исполнитель: —
ветка: —
команда: вторая
карта: Сколько доказано на самом деле
рядом: 9283, 6234
нужность: 2 — сторож красен и потому ничего не стережёт; на путь доказуемости не ложится, но без него перепись пустоты — слово, а не замер
---

# 9408. Перепись пустоты снова красна — восемь новых файлов библиотеки

Найдено ревизией задачника 11 сентября 2026 при закрытии задачи 9283.

## Что видно

```sh
SEMYA_OTSTALO_ZNAYU=1 ./ярлык пустота:проверка
# FLANG_PUSTOTA_KORPUS: файл библиотеки не назван ни в «Наших», ни в «Отложенных»:
#   cli.flang dns.flang number-format.flang protobuf.flang stats.flang
#   toml.flang uuid.flang websocket.flang
```

Все восемь заведены 9–10 сентября 2026 (`git log --diff-filter=A -- flang/stdlib/<имя>.flang`:
dns cc6527180, number-format d16416948, protobuf 595aea44a, stats ab05c9768,
cli eae241bd7, toml 735043361, uuid e389bc6de, websocket 760717ff0). В
`flang/stdlib/` сегодня 50 файлов; списки сторожа — `flang/scripts/emptiness-guard.flang`:
«Наши файлы» (стр. 590) и «Отложенные» (стр. 595).

Задача 9283 внесла 16 файлов (92c2d5756) и закрыта; это не её остаток, а тот же
изъян устройства: каждый новый файл библиотеки красит сторож, пока его не
впишут руками. Разобрать, почему сторож не берёт список из `flang/stdlib/` сам
(9283, раздел «Почему их не внесли»), — и либо вписать восемь с ценой в
«Отложенные», либо снять рукописный список.

## Как понять, что сделано

```sh
SEMYA_OTSTALO_ZNAYU=1 ./ярлык пустота:проверка   # код 0
sh scripts/guards/hand-written-lists.sh                 # перечень не вырос
```
