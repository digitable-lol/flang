---
номер: 1130
заголовок: Приёмка Lean берёт 29 правил из 70 приёмов сверщика — на наборе проб 944 блока «вывод» из 1291 сверены одним сверщиком
статус: сделана
исполнитель: a
ветка: a/1130-3855-lean-acceptance-grows
команда: любая
карта: Сколько доказано на самом деле
рядом: 2907, 3855, 1313
нужность: 2 — `docs/road-to-1-0.md` §3: теорема о состоятельности есть для 29 правил из 70; остальные блоки держит только сверщик на C
---

# 1130. Приёмка Lean растёт от 29 правил к 70

Продолжение задачи 2907 (ADR-0041 §2.4 п.3: «по одному»).

## Чем измерено

14 сентября 2026, `sh flang/proof/lean/прогон.sh` на ветке
`a/2907-record-and-checker-in-lean`: из 1291 блока «вывод», которые сверщик
проигрывает на своём наборе проб (216 честных записей, 459 подделок), у 944
первым непринятым шагом стоит правило, которого в `Приёмка.lean` нет; сверщик
из них принял 860, отверг 84. Сколько блоков упёрлось в правило первым:

    СС1 147, О1 144, В2 76, Ч1 74, В1 50, Д1 50, Д6 50, С1 50, Цел2 41, О2 36,
    С4 34, Цел1 32, Ч2 25, Ч6 24, Выч 21, Д5 21, Р5 15, О8 12, Н7 11, Н8 11,
    Р6 7, О4 5, Д3 1, О10 1

Ещё 6 блоков — имена, которых нет и в списке сверщика (Н✗ 3, О9 1, О✗ 1,
Ч5 1): подделки, оба их отвергают.

Это число «первым», а не число блоков на правило: блок, упёршийся в СС1, может
нести дальше и О1.

## Что сделать

По правилу за раз, от частого к редкому:

1. конструктор `«ШагПринят»` с теми же побочными условиями, что у приёма в
   `шаг_вывода` (`сверщик.c`);
2. разрешимая проверка `«…?»` и её случай в «принят?-верно»;
3. случай теоремы «состоятельность» — леммой `Правила.lean`;
4. прогон: расхождений 0, «вне приёмки Lean» падает.

Не одним приёмом: Выч, Р2, Р3 опираются на вычислитель половин, Р5, Р6, О7–О9
читают тело функции — модель их пока не воспроизводит (шапка `Приёмка.lean`).
Цел1, Цел2 и леммы Или/И — задача 3855.

## Как поймём, что сделано

Строка «вне приёмки Lean» в выводе `прогон.sh` — ноль блоков либо каждое
оставшееся правило названо с причиной; `road-to-1-0.md` §3 — новое число.

## Ход работы

**14 сентября 2026** (исполнитель a, ветка `a/1130-3855-lean-acceptance-grows`).
Правило за раз, от частого к редкому: конструктор `«ШагПринят»` с побочными
условиями ветви `шаг_вывода`, разрешимая проверка и её случай в «приём-верно»,
лемма «…-терм» в `Терм.lean`, случай теоремы «состоятельность»; после каждого
правила — сборка без сообщений, после каждой партии — полный `прогон.sh`.

По ходу влилась задача 9616: у сверщика стало 73 приёма (Кон2, Разв1, Ч5), у
О5/О6 — основание «из N M K», в наборе проб — 1362 блока вместо 1291. Ствол
после неё красный: 21 блок «сверщик принял, Lean не прочёл». Починено в
читателе (`46dd61651`): «из N M K» — `«Основание».«из3»`; шаг по правилу вне
приёмки с формулой, которой читатель не знает (Ч5, «к строке»), судится «вне
приёмки», а не «не прочёл».

Замеры `прогон.sh` на наборе 221 честная запись и 475 подделок, 1362 блока:

| где | правил в приёмке | вне приёмки: принял / отверг | расхождений |
|---|---|---|---|
| ствол `d8b82eaf` | 29 из 73 | 895 / 90 | 21 (читатель) |
| `969b53a03`, после починки читателя | 39 | 712 / 70 | 0 |
| `101c7d2f0` | 49 | 451 / 50 | 12 (Разв2) |
| `da3534f9b` | 58 | 241 / 45 | 12 (Разв2) |
| `2852d5586` | 62 | 159 / 37 | 18 (Разв2) |
| `57b7464a2` | 65 | 106 / 25 | 18 (Разв2) |
| `88bdc5ecf`, после решения по задаче 8836 | 65 | 106 / 25 | **0** |

(хеши замеров — локальные, до перебазирования ветки на ствол; после них файлы Lean менялись только в комментариях.)
На последнем: оба приняли 1067, оба отвергли 135 и ещё 11 на чтении; вне
приёмки по правилам — О6 55, Выч 33, Р5 27, Р6 7, Ч5 4, Н✗ 3, О9 1, О✗ 1. Н✗, О✗ и О9 — имена, которых нет и у сверщика:
подделки, оба их отвергают.

**Покрыто 65 правил из 73** — поимённо и с причинами для восьми непокрытых — в
шапке `Приёмка.lean`:

- Выч, Р2, Р3 — вычислитель половин, модель его не воспроизводит;
- Р5, Р6, Разв1 — читают тело из нескольких строк, читатель такое тело в терм
  не собирает;
- Ч5 — «к строке» от числа, перевода числа в текст в `Терм.lean` нет;
- О6 — лемма просит представимость левой стороны: в языке она есть всегда, в
  модели — нет.

**Находки.**

1. Разв2: ведомость и приёмка Lean запрещали вызов в теле, сверщик ловит только
   вызов с именем в ёлочках — 18 блоков «сверщик принял, Lean отверг»
   (проявилось, когда в приёмку вошли О8 и Н7). Решено задачей 8836: ведомость
   и приёмка говорят то, что делает сверщик, — вызов довода-функции разрешён;
   на `88bdc5ecf` расхождений 0, оба приняли 1085.
2. Лемма «Д3» строже правила («сумма Л плюс П — число»; сверщик не спрашивает)
   — приёмка стоит на «Д3-днами».
3. Лемма «СС4» не покрывала второй способ посылки ⟨2⟩ — «СС4-противоречием».
4. После задачи 9616 ствол красный на читателе Lean — починено здесь.

`#print axioms «состоятельность»` — `propext`, `Classical.choice`, `Quot.sound`;
`sorry` в `flang/proof/lean/` — ноль; искажений в `Ловушка.lean` 22, Lean
отверг 22.

## Чем закрыта

- `a86b4c023` — Цел1, Цел2 (вместе с задачей 3855);
- `668451b34`…`55d2e3b86` — остальные 34 правила, по коммиту на правило, и
  `b5f0d6126` — леммы «СС4-противоречием», «Д3-днами»;
- `46dd61651` — читатель записи после задачи 9616;
- `29956a51b` — шапки `Приёмка.lean` и `Состоятельность.lean`;
- `88bdc5ecf` — Разв2 по решению задачи 8836: расхождений 0;
- слово и перенос в `completed/` — коммит, который закрывает задачу в ветке.

Осталось: восемь приёмов, названных выше.
