---
номер: 1261
заголовок: Предусловие вызова не снимается ни объявленным типом довода, ни доказанным обещанием вложенного вызова — снятие читает только `требует` самой вызывающей
статус: сделана
исполнитель: a
ветка: a/1261-precondition-discharge-reads-proved-facts
команда: первая
карта: Где именно упирается доказатель
рядом: 3123, 4058, 1403, 1729
нужность: 1 — без этого ни одно обещание о списке функций (ADR-0037) не доходит до места вызова: `«Ф» от («Удвоить» от 1)` отвергается, хотя «удвоение неотрицательно» доказано
---

# 1261. Снятие предусловия не знает того, что знает доказательство постусловия

Решение: [ADR-0037](../adr/0037-second-order-is-a-finite-conjunction-over-function-tags.md) §4.1.

## Чем измерено

Прогон 13 сентября 2026, двоичный `bootstrap/flang` 0.7.19, собранный из дерева
`main` `87fbd112`, `flang check <проба> --proof`. Вызываемая одна и та же:
`«Корень из неотрицательного»` с `требует «довод неотрицателен» довод не меньше 0`.
Меняется только довод на месте вызова:

| довод на месте вызова | исход |
|---|---|
| литерал `1` | снято, код 0 |
| параметр `х` при собственном `требует «х не меньше 0»` вызывающей | снято, код 0 |
| параметр `х: неотрицательное` — объявленный тип | **FLANG_PRECONDITION_CALL** |
| `«Удвоить» от 1`, у «Удвоить» ДОКАЗАНО `результат не меньше 0` | **FLANG_PRECONDITION_CALL** |
| `пусть ув равно «Удвоить» от 1`, затем `ув` | **FLANG_PRECONDITION_CALL** |
| `«Загадка» от -1`, у «Загадки» обещание объявлено, не доказано | FLANG_PRECONDITION_CALL — и обязано остаться |

Текст отказа сам называет причину: «вызывающему здесь не известно ничего: у него
самого предусловий нет, а условие ветвления фактом ядру не является». Постусловие
той же функции при том же теле ядро доказывает: `обеспечивает результат не меньше 0`
при теле `«Удвоить» от 1` — доказано восьмым ходом (проба `tag-apply` в ADR-0037 §2).

## Где дыра

`flang/self/proofterm.flang`:

- «Места функции» (около строки 4523): «факты» — только `preconditions` самой
  вызывающей. Комментарий над функцией говорит это прямо: «Факты здесь —
  предусловия САМОЙ вызывающей … это единственное, что вызывающему известно».
- «Место снято» и «Почему не снято» (около 4553–4560) зовут «Свести витками» с
  `пустой список` на месте объявлений и определений. Объявленные типы параметров
  вызывающей туда не приезжают вовсе — отсюда третья строка таблицы.
- «Постусловия вызванных» (строка 3321) собирает доказанные обещания вызванных
  для доказательства ПОСТУСЛОВИЙ (восьмой ход, заметка
  `docs/zettel/callee-postcondition-is-a-fact-only-after-it-is-proved.md`), но
  «Места функции» её не зовут — отсюда четвёртая и пятая строки.

У ядра два набора фактов для двух вопросов об одном и том же выражении:
постусловие доказывается с типами и обещаниями, предусловие снимается без них.

## Что сделать

1. В «Места функции» подавать «Свести витками» те же факты, что подаёт
   доказательство постусловия: объявления параметров вызывающей (третья строка
   таблицы) и «Постусловия вызванных» по узлу довода (четвёртая и пятая).
2. Круг закрыть неподвижной точкой, а не графом вызовов: факт берётся у
   вызванной, чьё постусловие ДОКАЗАНО на предыдущем витке и у которой все места
   оплачены к предыдущему витку. Сегодня «Снять предусловия» идёт один раз и
   отдаёт список неоплаченных восьмому ходу; теперь снятие и доказательство
   чередуются, пока хоть одно место или одно утверждение меняет исход. Порядок
   объявлений на итог влиять не должен — проверить перестановкой функций в пробе.
3. Шестая строка таблицы — контроль: обещание, которое автор написал, а ядро не
   доказало, фактом не становится. Добавить вторую подделку: у «Удвоить»
   собственное `требует`, не снятое хоть в одном месте программы, — её обещание
   на место вызова не приезжает («Имена неоплаченных»).
4. Переписать «Названо фактами» (около 4568): «у него самого предусловий нет»
   перестанет быть всей правдой — назвать три источника и сказать, которого не
   хватило.
5. Дописать заметку `callee-postcondition-is-a-fact-only-after-it-is-proved.md`
   (раздел «Побочная находка» — здесь тот же пропуск второй раз) и пункт 3
   раздела «Что дальше» в `docs/flang/proof/SPEC.md`.
6. Проверить толкованием исходников `flang/self` (зонд
   `flang/self/bootstrap/zond-k7.flang`, около 10 минут и 12–20 ГиБ на программу):
   пять честных проб — код 0; две подделки — `FLANG_PRECONDITION_CALL`.
   `flang test flang/self/proofterm.flang` — все примеры.

## Чего не делать

- Не заводить строк ведомости и приёмов сверщика: снятие предусловий в
  числитель не идёт (задача 1729) и в запись не печатается. Меняется то, что
  ядро знает, а не то, что оно печатает.
- Не читать условие ветвления фактом — это другая работа с другим доводом.
- Не брать обещание, написанное автором, но не доказанное. Это аксиома под
  другим именем, и её не поймал бы сторож пустоты «АКСИОМЫ».
- Не трогать `bootstrap/**`: правка доедет до двоичного перепечаткой семени, до
  неё проверяется толкованием.

## Как поймём, что сделано

Строки 3–5 таблицы — код 0; строка 6 и подделка с неоплаченным `требует` —
отказ; перестановка функций в пробе вердикта не меняет;
`flang test flang/self/proofterm.flang` зелёный; `sh scripts/доказуемость.sh` —
ДОКАЗУЕМ; ведомость по `flang/stdlib` не потеряла ни одного «доказано», а цена
прохода по `flang/self` названа числом до и после.

## Ход работы

### 13–14 сентября 2026 — ветка `a/1261-precondition-discharge-reads-proved-facts`, от `main` `5837f53f`, перебазирована на `89323adc`

Замер до правки повторён двоичным `bootstrap/flang` 0.7.19, собранным из этого
дерева (`make -C bootstrap`): в области `flang/self` с `87fbd112` чужих правок
не было (`git log 87fbd112..main -- flang/self` пуст), так что семя и исходники
ядра совпадают. Шесть подач таблицы дали ровно её исходы: строки 1–2 — код 0,
строки 3–6 — `FLANG_PRECONDITION_CALL`, код 1. Толкованием (зонд `zond-k7`)
проба строки 4 до правки — тот же отказ, 10 мин 11 с, пик 12,3 ГиБ.

Что сделано, по шагам «Что сделать»:

1. **Три источника фактов** («Места функции», `flang/self/proofterm.flang`):
   к собственным `требует` вызывающей прибавлены объявления её параметров
   (третьим доводом «Свести витками», как «vars» у постусловия) и «Постусловия
   вызванных» по инстанции места — той же функцией и под теми же шестью
   условиями, что у восьмого хода («Факты места»). Довод-имя из `пусть`
   подставляется своим значением до сведения («Вызовы под пусть», «Инстанция
   места»): без этого пятая строка не закрывалась бы; заодно закрыт пропуск,
   которого не искали, — `требует` о параметре снимало место, где одноимённое
   имя было переопределено через `пусть`.
2. **Круг до неподвижной точки** («Снять предусловия», «Замкнуть круг
   снятия»): первый виток снимает места без единого обещания, затем ход без
   теорем при нынешних неоплаченных, затем снова попытка снять ОСТАТОК при
   обещаниях, закрытых к этому витку, у вызванных, все места которых оплачены к
   этому витку, — пока хоть одно место или одно утверждение меняет исход. Ход
   без теорем продолжается с закрытых («Ход кеша или без» получил шестой довод);
   снятое однажды не сводится заново, а при пустом списке закрытых второй
   проход по остатку не делается. Графа вызовов нет; перестановка функций в
   пробе вердикта не меняет (проверено толкованием).
3. **Подделки.** «Загадка» с обещанием, которое ядро не доказывает, и
   «Удвоить под требованием» с доказанным обещанием и одним неснятым местом в
   другой функции — её обещание не приезжает никуда, оба места отвергнуты.
4. **«Названо фактами»** переписана: три источника по числу, нуль называет
   недостающий, а про обещания сказано поимённо, почему их нет («ядро его не
   доказало», «у неё есть место с неснятым `требует`», «обещаний нет»).
5. Заметка `docs/zettel/callee-postcondition-is-a-fact-only-after-it-is-proved.md`
   дописана разделом о том же пропуске со стороны снятия; пункт 3 «Что дальше»
   `docs/flang/proof/SPEC.md` дописан.
6. Пробы в дереве: `flang/proof/examples/precondition-discharge-reads-proved-facts.flang`
   (пять подач, вызывающие выше вызванных, у каждой функции обещание) и
   `forgery-precondition-discharge-reads-proved-facts.flang` (две лжи).

Побочно: зонд `flang/self/bootstrap/zond-k7.flang` ронял всякую программу с
`требует`, прошедшую проверку, в `FLANG_UNKNOWN_NAME: не найдена функция
«Печать значения»` — и на нетронутом дереве тоже (проверено пробой с литералом
в чистом клоне `main`). Причина — порядок ввоза: связывание берёт объявления
модуля через первого ввозящего с его `только`, а первой у зонда стояла «Печать в
C» без «Печать значения» в списке. Порядок ввоза приведён к порядку семени
(`compiler.flang` первым); ядро это не трогает.

| фальсификатор | снято |
|---|---|
| двоичный 0.7.19 до правки, шесть подач | 1–2: код 0; 3–6: `FLANG_PRECONDITION_CALL`, код 1 — таблица подтверждена |
| толкованием после правки: литерал, `требует`, `х: неотрицательное`, `«Удвоить» от 1`, через `пусть` | все пять — ведомость напечатана, отказа нет; «удвоение неотрицательно» доказано (9 мин 53 с – 10 мин 33 с, 11,5–12 ГиБ каждая) |
| файл-проба с пятью подачами, вызывающие выше вызванных | «утверждений 7: доказано 7 (без теоремы 7, объявленным типом 2)», отказа нет; 13 мин 55 с, 47 ГиБ |
| перестановка функций (`p4-permuted`) | тот же вердикт: доказано 1, отказа нет |
| «Загадка» (обещание не доказано) | `FLANG_PRECONDITION_CALL`, «обещание «Загадка» не взято: ядро его не доказало» |
| неоплаченное `требует` у вызванной (две программы) | `FLANG_PRECONDITION_CALL` у обоих мест, «у неё есть место с неснятым «требует»»; файл-подделка дерева — три отказа, как ждали |
| цена, та же проба до/после | 10 мин 11 с, 12,3 ГиБ → 10 мин 33 с, 11,5 ГиБ (машина общая, рядом три прогона); литерал: 10 мин 02 с → 9 мин 53 с — в пределах шума, витки не ограничивались |
| `bootstrap/flang check flang/self/proofterm.flang` | код 1 и до, и после: 48 замечаний, побайтно те же, что у `main` (`totality.flang`: «Компонента», «Меньшее», «Добавить уникальное» — видимость при самостоятельном связывании), ни одного в `proofterm.flang`; примеры файла двоичный не запускает по той же причине |
| `sh scripts/доказуемость.sh` | ДОКАЗУЕМ (доля 639 из 662 = 96,53 %, набор 34, проб на подлог 459, честных 216) |
| `sh flang/proof/чекер/пробы/прогон.sh` | «сошлось всё» |
| ведомость `flang/stdlib` толкованием | НЕ СНЯТА ни до, ни после: `math-classics-lists.flang` — `FLANG_MEMORY` на 150 ГиБ через 2 ч 40 мин; `math-classics.flang` — `FLANG_PROPERTY` печати записи (как и у двоичного); исходы одинаковы в обоих деревьях. Двоичным до правки: automaton 122 доказано, math-classics-lists 19, json 102 |

**Ядро решений (сторож `kernel-lines-to-trust`, план «Потолок не пробит»).**
Первая сборка ветки: 5015 → 5259 строк (+244, функций 723 → 762), достижимо
8605 → 8711 — «Место снято» звала сбор обещаний вызванных сама, и в ядро
решений въехали восьмой ход (89 строк) и подстановка `proof-initial.flang`
(121 строка). Ужато (`5249d36e`): факты приезжают в решающую функцию данными от
«Факты места» ← «Снять места при», как «Оплаченное» в восьмой ход; два обхода
вызовов слиты в один с цепью `пусть`. Стало: **5017 строк, функций 723** —
как на `main` плюс две строки «Место снято» (ответ «нет» на несобравшуюся
инстанцию); достижимо 8696 (+91: обход с `пусть`, круг, три источника, текст
отказа). Потолок поднят шестой раз ровно на съеденное: 5015 → 5017 и
8605 → 8696; довод — в `flang/scripts/kernel-lines-to-trust-ceiling.json`,
строка бюджета — в ADR-0037 §4.5. После ужатия толкованием: p4, p5 — отказа
нет, «доказано 1»; сторож — код 0.

Чего не сделал: пять честных подач кодом 0 на ДВОИЧНОМ — после перепечатки
семени (до неё двоичный отвергает строки 3–5, как в таблице); ведомость
`stdlib` толкованием — не снята по названным причинам; `check` файла ядра
самостоятельно красен чужими замечаниями, которых эта работа не прибавила.

## Чем закрыта

- `86441c63` — proof(ядро): снятие предусловия читает объявленные типы и доказанные
  обещания вызванных, кругом до неподвижной точки; пробы в
  `flang/proof/examples/`; порядок ввоза у зонда `zond-k7`.
- `2ec6fccf` — docs(доказательства): заметка о восьмом ходе и пункт 3 «Что дальше»
  в `docs/flang/proof/SPEC.md`.
- `8a51276d` — docs(задачник): задача закрыта и перенесена в `docs/tasks/completed/`.
- `5249d36e` — perf(ядро): ужатие ядра решений 5259 → 5017 (было на `main` 5015),
  потолок сторожа поднят ровно на съеденное.
- коммит этого файла — ADR-0037 §4.5 (бюджет ядра решений) и было/стало здесь.
