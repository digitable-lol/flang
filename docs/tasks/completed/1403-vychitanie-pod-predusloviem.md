---
номер: 1403
заголовок: Разность под предусловием не доказывается неотрицательной — в ядре нет случая вычитания
статус: сделана
исполнитель: Marat
ветка: r/logika
команда: первая
карта: Где именно упирается доказатель
рядом: 1404, 1405, 1406
---

# 1403. Дну разности не хватает одного правила

Решение: [ADR-0032](../../docs/adr/0032-one-missing-rule-and-three-other-logics.md) §2.

## Чем измерено

Прогон 11 сентября 2026, двоичный `bootstrap/flang` 0.7.16, собранный в дереве
ветки `r/logika-i-primenenie` от `gh/dev` `11afc59fb`. Пять функций с одним и тем
же телом `цена минус скидка`, `flang check --proof --строго`:

| подача условия | цель | исход |
|---|---|---|
| `требует «скидка не больше цены»` | `результат не меньше 0` | объявлено, не доказано, код 3 |
| охрана в цели `если (скидка не больше цена) то … иначе да` | та же | объявлено, не доказано, код 3 |
| зеркало `требует «цена не меньше скидки»` | та же | объявлено, не доказано, код 3 |
| порядок плюс конечность обеих сторон, три строки `требует` | та же | объявлено, не доказано, код 3 |
| `требует «скидка не больше цены»` | `результат не больше 9007199254740991` | **доказано**, «ограниченность точным потолком по построению» |

Последняя строка — ключ: **под тем же самым предусловием потолок разности
доказывается, дно — нет.** Подача условия ни при чём.

## Где дыра

`flang/self/proof-kernel.flang:2664`, функция «Неотрицательно». Она разбирает
`literal`, известное из окружения, `builtin`, `add`, `mul`, `mod`, `if`, `fold`,
`match` — и отвечает «нет» на всё прочее. **Случая `"sub"` нет.** Так и записано
в `flang/proof/ПРАВИЛА-ВЫВОДА.tsv` строкой-запретом Н✗.

Соседнее правило потолка П5 («Разность ограничена», `:2781`, семь строк) уже
показывает, что разность ядру подъёмна. Опора для дна уже приезжает в ту же
функцию четырьмя списками: `известные`, `конечные`, `положительные`, `целые`.

## Сколько мест на этом стоят

`sh flang/proof/доля-корпуса.sh --проигрыванием`, 88 записей: целей «не меньше 0»
— 12, из них над разностью — **0**; строк вывода с `минус` — 4 (две по П5, две
про охрану); записей, зовущих примитив `минус`, — 7.

**Работа не поднимет долю 96,01 %, и обещать этого нельзя.** Она открывает то,
чего в дереве нет: код, где разность обязана быть неотрицательной. Ради этого её
и просили.

## Что сделать

1. **Заведи строку правила в `flang/proof/ПРАВИЛА-ВЫВОДА.tsv` ПЕРВОЙ** и лемму в
   `flang/proof/lean/` к ней. Посылки: в окружении записан порядок «П не больше
   Л» (или зеркало), и П конечно. Заключение: `Л минус П : неотр`.
   Конечность П обязательна — без неё `+∞ минус +∞` даёт не число, а посылка
   остаётся истинной. Конечность Л не нужна: при конечном П случай Л = `−∞`
   посылку не проходит. **Если Lean это отвергнет — чини посылку, а не лемму.**
2. Научи приёму проверяющую программу (`flang/proof/чекер/сверщик.c`) по образцу
   приёма П5 на строке 7666 — четырнадцать строк, две посылки, сличение текста.
   Условие партии неизменно: **проверяющая программа читает новую запись раньше,
   чем ядро начинает её печатать.**
3. Положи пробу на подлог по образцу `flang/test/fixtures/poddelka-strict-order.flang`:
   та же разность БЕЗ предусловия обязана остаться отвергнутой.
4. Допиши случай `"sub"` в «Неотрицательно» и **перепиши текст отказа**
   `proof-kernel.flang:339` — сегодня он говорит «вычитания в этом списке нет и
   не будет», и после правки это перестанет быть правдой.
5. Отправь правку ядра **партией перепечатки ADR-0026 §11**, не отдельной. Одна
   перепечатка — восемь-девять часов; ради шести строк её не заводят.

## Чего не делать

- Не трогать `flang/self/**` вне шагов 4 и 5 и не запускать перепечатку в
  одиночку — сговорись с партией.
- Не расширять правило на случаи, которых не просили (разность трёх частей,
  разность под сравнением с термом): запрет О✗ той же таблицы стоит отдельно и
  имеет свой довод.
- Не отчитываться прибавкой доли: её не будет.

## Как поймём, что сделано

Четыре пробы из таблицы выше дают код 0; пятая (потолок) как была; проба на
подлог отвергнута; `sh scripts/доказуемость.sh` печатает ДОКАЗУЕМ; строк кода в
`сверщик.c` прибавилось не больше 20.

## Ход работы

### 11 сентября 2026 — ветка `r/logika` (worktree `/srv/tmp/dokazuemyy/logika`), от `gh/dev` 2c40752d0

Работа шла в две смены: первую оборвал лимит сессии с 17 незакоммиченными
правками, вторая их доделала. Что сделано (по шагам «Что сделать» выше):

1. **Ведомость и Lean.** Строка `Н12` в `flang/proof/ПРАВИЛА-ВЫВОДА.tsv`
   (посылки: «П : неотр», «П ≤ К»; побочное условие — порядок «П не больше Л»
   или зеркало в допущениях функции); запрет `Н✗` уточнён: он о посылках «обе
   стороны неотрицательны». Lean принял лемму «Н12» и два встречных примера
   («Н12-без-потолка-ложна», «Н12-без-дна-ложна»); ловушки 9 и 10 в
   `Ловушка.lean` отвергнуты. Посылка задачи «П конечно» ОСЛАБЛЕНА ЗАМЕРОМ:
   «конечно» модели не отсекает минус бесконечность, и П = Л = −∞ даёт не-число,
   — поэтому посылки правила: дно И потолок вычитаемого. Прогон
   `flang/proof/lean/прогон.sh`: 88 лемм, 10 искажений, «сошлось всё».
2. **Сверщик** (`flang/proof/чекер/сверщик.c`, приём Н12 в `шаг_вывода`): +19
   строк кода (6585 → 6604, потолок С-6 с +20 по ADR-0032). Порядок читается со
   строки `требует` исходника, как допущение у Н2.
3. **Приёмочная пара.** `flang/proof/examples/subtraction-under-precondition.flang`
   (прямой порядок и зеркало) и `forgery-subtraction-under-precondition.flang`
   (порядок о другой паре, перевёрнутый, никакого). Семья
   `flang/proof/чекер/пробы/семьи/разность/` (README там), подделка набора
   `poddelka-raznost-bez-poryadka` (`НАБОР.tsv`, класс «лжёт-запись», храповик
   33 → 34), шесть порч строк в `прогон.sh` (проб на подлог 401 → 409).
4. **Ядро** (`flang/self/proof-kernel.flang`): случая `"sub"` в «Неотрицательно»
   НЕ появилось — замер показал, что порядок из Γ туда не приезжает (списки
   несут дно, конечность, положительность, целость, а порядок между термами —
   нет). Правило встало на сбор известного снизу: «Известное снизу» →
   «Разность по порядку» (19 строк вместо 6). Текст отказа
   (`proof-kernel.flang:339`) переписан. Печать (`flang/self/zapis.flang`):
   «Шаги дна вывода записи», «Шаги разности дна вывода записи», «Порядок
   записан у функции записи», обязательство представимости под правилом
   неотрицательности. Во второй смене найдена и починена ошибка типов
   `zapis.flang:4233` («Соединить списки записи» над списком шагов вместо
   «Склеить шаги вывода записи»): до починки `flang test flang/self/zapis.flang`
   не доходил до примеров; после — 659 из 659, `proof-kernel.flang` — 896 из 896.
5. **Перепечатки не заводили** — правки ядра ждут партии. Проверено толкованием
   (зонд по образцу `zond-k7.flang`, толкование `flang/self` двоичным 0.7.17,
   619 с, пик 34 ГиБ): ядро дерева даёт обеим функциям честной пробы «доказано»
   правилом «неотрицательность по построению», вывод Н3 → Пред1 → П3 → Н12 →
   Разв2; напечатанная запись ПОБАЙТНО равна рукотворной `chestnaya.запись`
   (три рукотворные записи выровнены по печати ядра: пустой строки после
   «тотальностей N» ядро не печатает); сверщик на записи ядра — код 0.

| фальсификатор | снято |
|---|---|
| честная пара, сверщик | код **0** (было бы 1 «правила Н12 в приёме нет» до сверщика; двоичный 0.7.17 обеим даёт «сетка», код 4 при `--строго`, до перепечатки) |
| подделка набора, сверщик | код **1**, у всех трёх функций названо «порядок не записан» |
| порча «дно у уменьшаемого» | код 1, «шаг 1 заключает «цена не меньше 0», а нужно «скидка не меньше 0»» |
| честная толкованием ядра дерева | «доказано» ×2, запись = рукотворной побайтно |
| подделка толкованием ядра дерева (595 с, пик 21 ГиБ) | «сетка» ×3, доказано 0 — ни одна из трёх лжи не стала доказанной; сверщик на записи ядра: «ПРОВЕРЕНО ВПУСТУЮ», код 0 |
| `прогон.sh` | «сошлось всё»: подделок 409, принято кодом 0 — 0; честных 198, отвергнуто 0 |
| `доля-корпуса.sh --проигрыванием` | 625 / 651 = 96,01 %, снято калькулятором 6 — как и обещано в ADR-0032 §2.3: доля не двинулась |
| `scripts/доказуемость.sh` | ДОКАЗУЕМ |

Чего не сделал: пять проб §2.1 ADR-0032 кодом 0 на ДВОИЧНОМ — это после
перепечатки партии (ядро ждёт её; толкованием — доказано).
