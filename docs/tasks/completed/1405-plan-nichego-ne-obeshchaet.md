---
номер: 1405
заголовок: План ничего не обещает — доказывающие слои о нём не знают ни одного слова
статус: сделана
исполнитель: a
ветка: a/1405-effekty-sh1 (прежде r/1405-effekty)
команда: первая
карта: Чего в языке нет вовсе
рядом: 1403, 1404, 1406
---

# 1405. Эффекты есть, утверждений о них нет

Решение: [ADR-0032](../../adr/0032-one-missing-rule-and-three-other-logics.md) §3.2.

**Порог: [ADR-0026](../../adr/0026-quantifiers-over-any-type-are-a-kernel-change.md) §11.3
(свободное утверждение). До него задача не начинается.**

## Чем измерено

Прогон и перечисления 11 сентября 2026, двоичный `bootstrap/flang` 0.7.16, ветка
`r/logika-i-primenenie` от `gh/dev` `11afc59fb`.

**Форма есть.** `план` с полями `состояние`, `начинает с`, `обрабатывает`; три
суммы ввода-вывода — «Поручение» 22 варианта, «Отклик» 20, «Продолжение» 4
(`flang/self/parser.flang:6440, 6449, 6458`). Объявлений `план` в дереве — **222**.

**Доказывающие слои о плане не знают ничего:**

| файл | вхождений слова `план` | вхождений `"plan"` |
|---|---|---|
| `flang/self/proof-kernel.flang` | 0 | 0 |
| `flang/self/obligations.flang` | 0 | 0 |
| `flang/self/types.flang` | 0 | 0 |

`flang check --proof --строго examples/io/temp-directory.flang` — «утверждений
0». Файл с планом на десять функций не высказывает ни одного утверждения о самом
плане. Приписать его нельзя: отказ разбора «в плане ожидаются 'состояние',
'начинает с' или 'обрабатывает'».

Доказывается там ровно то же, что в любом чистом файле: постусловия отдельных
функций-обработчиков. Про то, ЧТО план делает с внешним миром и в каком порядке,
не сказано ничего.

## Что сделать

1. **Сперва замер, не код.** Выпиши три настоящих утверждения о планах дерева,
   которые хотелось бы иметь. Наши кандидаты, и их надо проверить, а не принять
   на веру: «этот план не пишет в файл, которого не заводил»; «после отклика
   «Сбой» продолжения нет»; «каждый заведённый временный каталог убирается».
2. Для каждого — сказать, чего именно не хватает: слова у объявления `план`,
   правила у ядра или обоих.
3. **Проверить предположение ADR-0032 §6.2:** хватит ли объявленного у плана
   НАБОРА разрешённых поручений, проверяемого по обработчикам, — без логики
   разделения владения. Оно дешевле и, возможно, покрывает настоящий запрос.
   Предположение ничем не проверено.
4. Назвать цену числом строк — после шагов 1–3.

## Чего не делать

- Не заводить логику разделения владения, не показав прогоном, что набора
  разрешённых поручений мало. Это самая дорогая из веток, и брать её вслепую
  нельзя.
- Не трогать `flang/self/**` до замера.

## Как поймём, что сделано

Напечатан замер: три утверждения, для каждого — чего не хватает поимённо, и
ответ по шагу 3 прогоном, а не рассуждением.

## Спецификация (по замеру 11 сентября 2026, ветка `r/logika`)

### Замер шага 1 — числом и прогоном

Двоичный `bootstrap/flang` 0.7.17. Вариантов ввода-вывода в языке 39
(`flang/self/parser.flang:758`). План `examples/io/temp-directory.flang`
(«Каталог на время», состояние «Ход», обработчик «Дальше») выдаёт поручений трёх
видов: «Завести временный каталог» ×2, «Записать файл» ×2, «Удалить файл» ×4 —
считано по тексту обработчиков (`поручение равным (вариант «…»`).

Три утверждения о плане, для каждого — чего не хватает поимённо:

| утверждение | чего не хватает | замер |
|---|---|---|
| «после отклика «Сбой» продолжения нет» | ничего нового: это постусловие обработчика. Написано у «После каталога»: `разбор отклик … случай вариант «Сбой» … то результат равен (вариант «Провал» …)` | **сетка**, не доказано: цель-разбор над встроенной суммой «Отклик» ядро не сводит (пробовано и в порядке случаев тела, и без случая «Заведено»); ошибка ИМЕЮЩЕЙСЯ логики (правило разбора цели по случаям над встроенной суммой), не новая |
| «этот план не пишет в файл, которого не заводил» | порядка поручений в языке нет — это утверждение о ПОСЛЕДОВАТЕЛЬНОСТИ откликов и поручений (1404) поверх плана; дешёвая форма — набор разрешённых поручений (§6.2 ADR-0032) | набор выражается одной строкой у плана и проверяется по тексту обработчиков — см. ниже; порядок — не выражается до 1404 Ш1 |
| «каждый заведённый временный каталог убирается» | то же, что предыдущее: свёртка «Дальше» по списку откликов плюс инвариант «заведено − убрано»; порог — первая строка (постусловие о разборе) | не выражается до 1404 Ш1 и до правила разбора над встроенной суммой |

Итог замера: **предположение 2 ADR-0032 §6 подтверждено наполовину**: набор
разрешённых поручений покрывает утверждение «план не делает ничего сверх
названного» (первый настоящий запрос), но НЕ порядок и НЕ «после сбоя ничего».
Логика разделения владения не нужна ни одному из трёх.

### Слово и синтаксис

Одно слово у объявления `план` по образцу его строк (`состояние`, `начинает
с`, `обрабатывает`): `разрешает «Завести временный каталог», «Записать файл»,
«Удалить файл»`. Список закрыт, имена — варианты суммы «Поручение». Русская и
английская поверхности (`allows`), как у слов ADR-0026 §11 п. 10.

### Что печатает ядро, что сличает сверщик

Запись: секция плана — `план «П» строка N`, на каждое разрешённое
`разрешено «Поручение» строка N`, на каждое выданное обработчиками
`поручение «Поручение» функции «Ф» строка M`, `конец плана`. Правило одно —
`Пл1`: всякое поручение, выданное обработчиком плана и функциями, которые он
зовёт в том же файле, стоит в списке `разрешает`; проверка по тексту
(`вариант «X»` под `поручение равным`), над-оценка: план не исполняется. Лемма
— вхождение в закрытый список (`List.mem`), строка ведомости заводится вместе с
кодом. Сверщик читает объявление плана и обработчики с исходника и сличает: имя
вне списка — код 1, «план «П» разрешает …, а «Ф» строка M выдаёт «X»»;
разрешённое, но не выдаваемое, — принимается (над-оценка законна).

- **Ш0 — сверщик**: читает секцию плана и отвергает подлог (обработчик выдаёт
  неразрешённое; строка `поручение` о функции, которой план не зовёт; порядок
  строк). Рукотворная запись по `temp-directory.flang`. Оценка ≈ 30 строк C.
- **Ш1 — ядро** (партия): слово в лексере, продукция в парсере (по образцу
  `flang/self/parser.flang:6089`), сбор поручений по обработчикам, печать секции.
- **Ш2 — применение**: `examples/io/temp-directory.flang` и планы `flang/concurrency/`
  получают `разрешает`; подделка — обработчик с «Запустить процесс» вне списка.

### Приёмочные пробы

Честная: `temp-directory.flang` с `разрешает` трёх поручений — код 0. Подделки:
обработчик выдаёт «Запустить процесс» — код 1 с названным именем; запись с
`поручение` о чужой функции — код 1; постусловие «после сбоя продолжения нет» —
остаётся сеткой, пока не сделано правило разбора над встроенной суммой (своя
заявка, не эта задача).

### Чего задача НЕ делает

Не заводит логику разделения владения. Не доказывает порядок поручений — это
1404 поверх плана. Не исполняет план для проверки: набор проверяется по
тексту. Не чинит разбор цели над встроенной суммой.

### Цена числом строк

Ш0 ≈ 30 строк C, Ш1 ≈ 80–120 строк flang — оценки по образцам (1403, 6203 Ш0),
не замер; замер — после Ш0.

## Ход работы

### Ш0 — сделан (13 сентября 2026, ветка `r/1405-effekty` от `gh/dev` `0530e478f`)

Сверщик читает секцию плана и отвергает подлог. Ядро секции не печатает и слова
`разрешает` ещё не знает — сверщик идёт раньше ядра, как было с «прогон» (1404)
и с обязательством входа (6202 §3.0), поэтому запись РУКОТВОРНАЯ.

**Цена — замером, а не оценкой: +70 строк кода C** (6912 → 6982,
`sh flang/proof/tables-guard.sh --строки-кода`). Оценка автора была ≈30: она
считала сличение списка. Состоятельными Ш0 делают ещё три проверки, и без них
правило обходится:

| проверка | без неё |
|---|---|
| кого план достаёт (`начинает с`/`обрабатывает`, до неподвижной точки) | поручение приписывают функции, которой план не зовёт |
| хозяин строки исходника | поручение записывают за чужой функцией |
| встречная: «запись не молчит о выданном» | подлог прячется не ложной строкой, а ОТСУТСТВУЮЩЕЙ — запись просто не называет неразрешённое поручение и проходит целиком |

Последняя — главная находка шага. Над-оценка («план не исполняется, читаем
текст») законна лишь в одну сторону: разрешённое и нигде не выданное
принимается молча, но выданное и не записанное — подлог умолчанием.

**Заведена семья проб** `flang/proof/checker/tests/families/plan/`: одна честная
запись (код 0, SHA-256 сошёлся) и ШЕСТЬ подделок, каждая отвергается кодом 1 с
НАЗВАННОЙ причиной (`prichina` в наборе проб сличает причину подстрокой):

| подделка | названная причина |
|---|---|
| обработчик выдаёт «Запустить процесс» (подделка ИСХОДНИКОМ) | `разрешает …, а «После записи» строка 92 выдаёт «Запустить процесс»` |
| разрешённое снято, выдача осталась | `… а «После записи» строка 92 выдаёт «Удалить файл»` |
| поручение о функции, которой план не зовёт | `поручение записано за функцией «Мимо плана», которой план не зовёт` |
| порядок строк | `строка записи 26 … стоит не сразу под строкой «план» — сличать её не с чем` |
| запись молчит о выданном | `«После записи» строка 95 выдаёт «Удалить файл», а строки об этом в записи нет` |
| поручение приписано чужой функции | `записано за функцией «Отчёт», а строка 92 исходника стоит в функции «После записи»` |

Первые три — ровно те три подлога, что названы в Ш0; остальные три стерегут
швы, без которых первые обходятся.

**Исходник проб** — `temporary-directory.flang`: `docs/examples/io/temp-directory.flang`
знак в знак (примеры переехали из `examples/` в `docs/examples/`) плюс строка
`разрешает` и функция «Мимо плана», которую план не зовёт (нужна одной
подделке). Поручений в нём восемь — «Завести временный каталог» ×2, «Записать
файл» ×2, «Удалить файл» ×4, — и это сходится с замером 11 сентября.

**Чего Ш0 не сделал.** Не тронул `flang/self/**` (там Ш1 — партия ядра) и
ведомость правил: Пл1 — правило сверщика над текстом исходника, строки в
`flang/proof/tables/inference-rules.tsv` оно не заводит. Постусловие «после сбоя
продолжения нет» осталось СЕТКОЙ (вердикт «нет вердикта»): цель-разбор над
встроенной суммой «Отклик» ядро не сводит — своя заявка, не эта задача.
Порядок поручений не выражается до 1404 Ш1. Логика разделения владения не
заведена и не понадобилась.

**Бюджет строк.** Расход идёт в строку ADR-0032 (+50), и он её превысил:
19 + 19 + 70 = 108. Потолок сверщика 7188 НЕ тронут (до него 206). Строка
записана в `flang/proof/checker/ratchet.txt` вслух и ждёт подписи владельца:
либо ADR-0032 → +110, либо 58 строк вернуть ужатием.

## ⚑ 17 сентября 2026: замер (задача 3560)

Ветка `a/3560-logic-in-time-count` над `main` `a43ab4d25`, двоичный `bootstrap/flang` 0.7.19,
1001 файл под учётом git; числа, прибор и места поимённо — [ADR-0044](../../adr/0044-time-effects-and-processes-are-counted-before-they-get-rules.md) §2–3.

**Планов** `план «` — **229** (без `flang/self` 228): `scripts/**` 81, `flang/scripts` 64,
`docs/examples` 40, `flang/test` 22, `flang/proof` 5, `flang/concurrency` 1, прочее 15.

**M2 = 188** — планов, чьи достижимые обработчики (от `начинает с`/`обрабатывает` по
вызовам до неподвижной точки, как «кого план достаёт» в Ш0) выдают хотя бы одно
поручение: `scripts/**` 81, `flang/scripts` 46, `docs/examples` 34, `flang/test` 7,
`flang/proof` 5, `flang/concurrency` 1, прочее 14. Ни у одного нет и не может быть
`разрешает`. **23** из них выдают только «Прочитать файл». Видов поручений у плана: одно —
33, два — 43, три — 42, четыре — 7, пять — 3; чаще всего — «Прочитать файл» (91 план),
«Запустить процесс» (69), «Записать файл» (42). Корпус: планов 0 из 89.

Поимённо: `docs/examples/io/temp-directory.flang:138` «Каталог на время» — 10 функций,
три вида поручений (сходится с замером 11 сентября); `flang/proof/checker.flang:1992`
«Сверить доказательство» — 147 функций, только «Прочитать файл» («сверщик ничего не
пишет и никого не запускает»); `flang/concurrency/bench/gen.flang:451` «Генератор стендов» —
«Записать файл», «Прочитать файл»; `docs/examples/db/postgres-plan.flang:455` «Поговорить
с постгресом» — только соединение; `scripts/guards/adr-numbers-guard.fscript:335` «Номера
решений» — «Запустить процесс», «Прочитать файл».

**Цена Ш1 по ADR-0044 §4.3:** `lexer.flang` +1, `parser.flang` ≈ 20 (аналог «Часть плана»
10, замер), поле разбора ≈ 10, `zapis.flang` ≈ 120–150 (аналог строки «прогон» — 117,
замер); сверщик **0** строк C (Ш0 = 70, замер); ведомость +1 (Пл1), лемма и ловушка Lean.
Порядок — третий шаг кода: весь в `flang/self`, окупается только после перепечатки.

## Чем кончилось

Приём задачи — «напечатан замер: три утверждения, для каждого — чего не хватает
поимённо, и ответ по шагу 3 прогоном».

Замер напечатан таблицей на три утверждения («этот план не пишет в файл, которого не
заводил»; «после отклика «Сбой» продолжения нет»; «каждый заведённый временный каталог
убирается»), у каждого названо, чего не хватает. Ответ по шагу 3 (хватит ли объявленного
у плана НАБОРА разрешённых поручений) снят прогоном в разделе «Ход работы Ш0»: хватает,
цена +70 строк кода C замером, а не оценкой, и найдена встречная проверка «запись не
молчит о выданном» — подлог умолчанием.

Сверено 22 сентября 2026 на стволе `d0763e8b6`: семья проб `flang/proof/checker/tests/families/plan/`
в дереве; `sh flang/proof/checker/tests/run.sh` — «сошлось всё» (подделок 534, принято
кодом 0 — 0; честных 248, отвергнуто 0).
