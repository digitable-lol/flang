---
номер: 1418
заголовок: Три переезда: flangtutor → flang/bin, flang/conc → flang/concurrency, flang/проверки → flang/test
статус: сделана
исполнитель: a
ветка: a/1418-three-moves
команда: любая
карта: Что мешает больше всего
рядом: 8235, 1745, 1419
нужность: 2 — ссылок вне переезжающих мест: 31 файл, 231 файл и 171 строка; flangtutor оказался файлом, а не каталогом
---

# 1418. Три переезда, и объём у каждого разный

Слова владельца: `flangtutor` → `flang/bin`, `flang/conc` → `flang/concurrency`,
`flang/проверки` → `flang/test`.

## Чем измерено

Замеры 13 сентября 2026, дерево `dev` `fcd4a9554`.

### 1. `flangtutor` — это НЕ каталог, а файл

`flangtutor` — исполняемый скрипт оболочки в корне дерева: POSIX shell,
**744 строки**, 48 157 байт, права `rwxrwxr-x`.

Ссылаются на него **31 файл вне его самого**, и они разнородны — это главная
цена переезда:

| где | примеры |
|---|---|
| поставка | `packaging/flang.1`, `packaging/homebrew/flang.rb`, `packaging/homebrew-tap/Formula/flang.rb`, `packaging/asdf/bin/install`, `packaging/asdf-plugin/bin/install` |
| надзор | `.github/workflows/install-path.yml`, `.github/actions/release-archive/action.yml` |
| оснастка | `scripts/flangtutor-proba.sh`, `scripts/release/release-in-c.flang`, `scripts/guards/postavka-guard.flang`, `scripts/site/changelog.flang` |
| слово | `README.md`, `CHANGELOG.md`, `docs/README.ru.md`, `docs/overview.ru.md`, `docs/tutor/README.md`, `docs/tree-inventory.md`, заметки `docs/zettel/**` |

Каталога `flang/bin` в дереве **нет** — его заводит эта задача.

⚠ Среди заметок есть
`docs/zettel/a-lesson-directory-under-share-flang-would-break-the-target-count-in-two-packagers.md`
— прежний разбор о том, что перенос уроков ломает счёт целей у двух
пакетировщиков. Читать ДО правки `packaging/**`.

### 2. `flang/conc` → `flang/concurrency`

Файлов в каталоге — **66**. Ссылаются на путь `flang/conc` **231 файл** вне
каталога. Это самый дорогой из трёх переездов.

### 3. `flang/проверки` → `flang/test`

Файлов — **26**. Ссылок вне каталога — **171 строка**.
Каталог `flang/test` **уже существует** и держит 14 файлов, значит это не
переименование, а СЛИЯНИЕ двух каталогов. Столкновений имён по верхнему уровню
нет.

Внутри `flang/проверки` лежат и кириллические имена (`жаргон.flang`,
`ведомость.txt`, `обход.sh`, `владение-состоянием.sh`) — они попадают ещё и в
1419, и два переименования одного файла в разных задачах обязаны не
пересечься.

## Зачем

По корню дерева не видно, что здесь язык (задача 8235), а кириллица и транслит
в путях — отдельное замечание владельца (1745, пункт 4).

## Порог входа

Порога нет, но есть **очерёдность**: 8235 и 1745 уже двигают дерево, и пункт 3
задачи 1745 (`examples/`) остановлен запретом править `flang/proof/**`. Взять
1418, не сверившись с их исполнителями, — значит устроить встречную правку.

## Шаги

- **Ш0 — снять числа заново** (31 / 231 / 171) и записать «было».
- **Ш1 — `flang/проверки` → `flang/test`**, как самое дешёвое из трёх и уже
  наполовину английское. Слияние, а не переименование.
- **Ш2 — `flangtutor` → `flang/bin/flangtutor`.** Прежде правки — заметка о
  счёте целей у пакетировщиков; поставка и надзор правятся вместе с файлом.
- **Ш3 — `flang/conc` → `flang/concurrency`**, последним: 231 ссылка.
- Каждый переезд — свой коммит, после каждого `sh .githooks/pre-push` зелен и
  ссылок на старый путь ноль.

## Как поймём, что сделано

`grep -r 'flangtutor\|flang/conc\|flang/проверки'` по дереву не находит старых
путей нигде, кроме истории и нарочно названных мест; сторожа зелены; сайт
собирается без битых ссылок.

## Чего задача НЕ делает

Не переименовывает файлы ВНУТРИ каталогов — это 1419 и 1420. Не трогает
`flang/proof/**`. Не решает судьбу `examples/` (пункт 3 задачи 1745).

## ⚑ Цена переезда, снята 13 сентября 2026 на dev (прогоном, не на глаз)

| каталог | файлов | живых ссылок | хук | прогоны | приметы | ведомость долей |
|---|---:|---:|---:|---:|---:|---:|
| `flang/conc` → `flang/concurrency` | 66 | 133 | 0 | 3 | 0 | **33 строки** |
| `flangtutor` → `flang/bin/` | 617 строк | 24 | 0 | 1 | 1 | 0 |
| `flang/проверки` → `flang/test/` | 26 | 47 | 0 | **14** | 4 | 0 |

«Живые ссылки» — файлы вне `docs/tasks/completed`, `docs/zettel`, `docs/archive`:
историю переписывать НЕ надо, там имена описывают тогдашнее.

## Грабли, на которые я наступил сегодня — не повторять

1. **Ведомость долей.** У `flang/conc` 33 строки в
   `scripts/ledgers/proved-share-ledger.txt`. Путь в строке — последнее поле;
   md5 считается от СОДЕРЖИМОГО и при переезде не меняется, так что правится
   только столбец пути. Не сделать — `pol-dokazannogo-sverka.sh` скажет
   «ЛИШНЯЯ СТРОКА … есть в описи, а в дереве обязательств нет».
2. **Порядок.** Сторож описи перечисляет файлы через `git ls-files`: пока новые
   пути не под версионным контролем, он их НЕ ВИДИТ. Сперва `git add`, потом
   прогонять.
3. **Приметы и проза правятся ПОРОЗНЬ.** В комментарии `<!-- СНЯТО … = N -->`
   число слитно, в прозе рядом — с разделителем разрядов. Одной заменой по
   строке чинить нельзя: сторож ответит «примета оторвалась от прозы».
4. **Числа README сверяет другой прибор** — `published-vs-tree.sh`, а не сторож
   примет. Сторож примет может быть зелен, пока облако красно.
5. **Не запускать параллельно git-команды по одному рабочему дереву**: `git
   reset --hard` из соседнего разбора затирает несохранённые правки.

## Проверка после переезда (всё прогоном)

    sh .githooks/pre-push                     — зелен
    sh scripts/доказуемость.sh                — ДОКАЗУЕМ
    sh scripts/guards/pol-dokazannogo-sverka.sh — опись сходится
    sh scripts/guards/prose-numbers-guard.sh  — приметы сошлись
    sh scripts/guards/published-vs-tree.sh --проза — сходится
    ./ярлык тесты                             — ссылки не порваны

## Ш0 — числа пересняты 14 сентября 2026, ветка `a/1418-three-moves` над `main` `89323adc`

Считано `git grep --text -l` по отслеживаемым файлам (сам переезжающий путь
исключён); «живые» — вне `docs/tasks/completed`, `docs/tasks/rejected`,
`docs/zettel`, `docs/archive`. Прежние 31 / 231 / 171 снимались `grep -r` по
рабочей копии и без `-a`, поэтому расходятся с этими: область другая, а не
дерево.

| путь | файлов ссылается | из них живых | строк | в `flang/proof/**` |
|---|---:|---:|---:|---:|
| `flangtutor` | 32 | 27 | — | 0 |
| `flang/conc` | 202 | 115 | — | 1 (комментарий в `checker/tests/families/run/pisma-po-ocheredi.flang:4`; до 1420 не правится) |
| `flang/проверки` | 68 | 55 | 182 (+17 внутри каталога) | 0 |

Файлов в `flang/проверки` — 26 (в том числе каталог `oblast/` с пробами на C);
в `flang/test` — 181 отслеживаемый (14 записей верхнего уровня, остальное
`fixtures/`, `snimok/`, `zapis/`). Столкновений имён нет.

**Сторожа и доля ДО первого переезда** (двоичный `make -C bootstrap` на
`51c2625e`, тот же коммит семени, что у `89323adc`):

| прибор | ответ |
|---|---|
| `sh flang/proof/corpus-share.sh --проигрыванием` | **629 / 651 = 96,62 %**, исключено мест 22 (без ключа `--проигрыванием` прибор отвечает кодом 2: «назвать набор») |
| `sh scripts/доказуемость.sh` | ДОКАЗУЕМ, код 0 |
| `./ярлык расширения:проверка` | код 0, мест отбора 30 |
| `sh scripts/guards/task-numbers-guard.sh` | код 0, двойных номеров 0 |
| `./ярлык задачник:проверка` | код 0 |
| `sh .githooks/pre-push` | 12 из 12 зелены, 36 с |
| `sh flang/проверки/обход.sh` | чисто — проверок 211, сошлись все 211 |
| `sh flang/проверки/обход-примеров.sh` | **не снят**: `flang test . --ledger` шёл 7 ч 19 мин процессорного времени и 21,8 ГиБ, снят по номеру процесса без ответа; порог в `binary.yml` — «десятки минут». Отдельная беда, не этой задачи |

## Ш1 — `flang/проверки` → `flang/test`, 14 сентября 2026

Слияние: 26 файлов `git mv` (в том числе `oblast/` с пробами на C и три
ведомости), столкновений имён не было. Правлено 48 файлов со ссылками; в
истории (`docs/tasks/completed`, `docs/tasks/rejected`, `docs/zettel`,
`docs/archive`) пути не трогались.

**Что потребовало не только замены пути:**

* `flang/test/обход.sh` (бывший `flang/проверки/обход.sh`) обходил `*.flang`
  своего каталога и ронял прогон на файле без `тотальная функция «Ведомость…»`.
  В `flang/test` таких файлов шесть (`glob.flang`, `jargon-guard.test.flang`,
  `surface-pair.flang`, `tempdir.flang`, `toolchain-guard.flang`,
  `uzel-osnastka.flang`) — это двойники проб на JavaScript, а не проверки
  обходчика. Теперь они пропускаются, и пропуск называется поимённо в stderr;
  потерю проверок по-прежнему ловит diff имён с `ведомость.txt`. Прогон:
  «чисто — проверок 211, сошлись все 211», код 0.
* `scripts/guards/tree-inventory.flang:115`: правило «проба рантайма C» ищет
  `flang/test/oblast/` — иначе 13 файлов на C стали бы долгом, и
  `опись:сверка` покраснела бы.
* `flang/scripts/closed-length-guard.flang:668`: перечень каталогов сторожа
  длины назван путями от `flang/scripts`, и `"../проверки"` поиск по
  `flang/проверки` не видит — нашёлся уже после записи этого шага, при
  подготовке Ш3. Стал `"../test"`, каталогов по-прежнему 21. Обход прибавил
  шесть двойников проб на JavaScript (поручений 323 → 329), а обещаний о
  длине столько же: 405, судимо 284, все сошлись; `./ярлык длина:проверка`
  код 0 и на стволе `4a438c14`, и на ветке.
* Ведомости с путями: `scripts/ledgers/proved-share-ledger.txt` (3 строки,
  md5 не менялся), `no-comments-debt.tsv` (5 строк, файл пересортирован в
  порядке C — `join` в стороже иначе молчит), `hand-written-lists-ledger.tsv`
  (1 строка), `link-guard-known-not-a-path.tsv` (3 строки довода).
* Близнецы журнала изменений (`scripts/site/changelog.flang`,
  `build-changelog.mjs`): `flang/проверки` ОСТАВЛЕН в списке служебных корней
  рядом с `flang/test` — журнал читает старые коммиты, и путь истории обязан
  оставаться известен правилу видимости.
* Приметы чисел пересняты, потому что строк оболочки стало на 7 больше
  (`обход.sh`) и JavaScript на 4 меньше (`build-changelog.mjs`):
  `docs/tree-inventory.md:81,89`, `docs/javascript-inventory.md:74`,
  `.github/workflows/binary.yml:396-398` — числа взяты из ответа
  `prose-numbers-guard.sh` и `опись:сверка`, не из головы.
* Ведомость обходчика примеров `flang/test/ведомость-примеров.txt`: строка
  `flang/проверки/встроенные-формы.flang	2 2 0` стала
  `flang/test/встроенные-формы.flang	2 2 0` на том же месте (307). Место
  выведено из правила порядка в `flang/self/bootstrap/corpus.flang`
  («Упорядочить пути», вставка по строковому сравнению): `flang/test/fixtures/…`
  < `flang/test/в…` < `fspec/…`. **Прогоном не подтверждено**: `flang test .
  --ledger` на этой машине не кончился за 7 ч 19 мин ЦП (см. Ш0), а `flang test
  'flang/test/*.flang'` — за 15 мин; обе попытки сняты по номеру процесса.

**Нарочно оставленные старые пути** (`git grep flang/проверки` вне истории):
`bootstrap/flang_repl.c:3084` (машинный вывод, правится перепечаткой — источник
`flang/src/emit/c/flang_repl.c:3075` правлен), `flang/test/fixtures/zapis-oracula.json:288`,
`flang/test/snimok/proofterm-witness.json:282`, `flang/test/fixtures/zapis-setki.json:287,595`
(замороженные снимки ответов удалённой реализации, ключуются путём на момент
снимка; читают их только заметки), пометки «переехало из `flang/проверки`» в
двух README, `docs/tree-inventory.md` и близнецах журнала.

**Сторожа после переезда** (двоичный тот же, `make -C bootstrap` на `51c2625e`):

| прибор | ответ | на `main` `bdce1f75` (тот же двоичный, рабочая копия в стороне) |
|---|---|---|
| `sh flang/test/обход.sh` | чисто, 211 из 211, код 0 | 211 из 211 |
| `sh .githooks/pre-push` | 12 из 12 зелены (после пересъёмки примет) | 12 из 12 |
| `./ярлык расширения:проверка` | код 0, мест отбора 30 | код 0 |
| `./ярлык ссылки:проверка` | **код 1: битых 35 из 6818, устаревших исключений 1** — список побайтово тот же, что на `main` (`diff` пуст) | код 1, те же 35 |
| `sh scripts/guards/no-comments-guard.sh` | **код 1**: прибавка в 9 файлах, все девять — чужие (`flang/self/types.flang` +5, `zapis.flang` +299, …); у переехавших `встроенные-формы.flang` 76 → 77 и `владение-состоянием.flang` 0 → 2 ровно так же на `main` | код 1, то же |
| `sh scripts/guards/hand-written-lists.sh --check` | **код 1**: новых 59, мёртвых 29 — на `main` те же 59/29, включая `прогонщик-корпуса.flang:89` против записи `:97` | код 1, то же |
| `sh scripts/guards/pol-dokazannogo-sverka.sh` | опись сходится, код 0 (протухло 121 из 913 — md5 правленных файлов) | — |
| `sh scripts/guards/prose-numbers-guard.sh` | 216 из 216, код 0 | — |
| `./ярлык опись:сверка` | сходится, 17 языков, код 0 | — |
| `sh scripts/доказуемость.sh` | ДОКАЗУЕМ; **доля 629 / 651 = 96,62 %** | 629 / 651 |
| `./ярлык задачник:проверка`, `task-numbers-guard.sh` | код 0 / код 0 | — |
| `bootstrap/flang check` шести правленных `.flang` (corpus-runner, file-extensions, tree-inventory, name-splicing-guard, region-in-c-target, changelog) | у всех «замечаний нет» | — |
| `node docs/site/build.mjs --check` | страниц 778, битых ссылок 0 | — |

## Ш2 — `flangtutor` → `flang/bin/flangtutor`, 14 сентября 2026

`git mv` одного файла (сейчас 31 846 байт — после PR #9 он короче замера
13 сентября), каталог `flang/bin` заведён. Правлено 11 файлов рядом: девять — самим
переездом, два — починкой сдвига, описанной ниже.

**Что потребовало не только замены пути:**

* Сам проводник ищет уроки и двоичный относительно своего каталога. В дереве
  это теперь `$ROOT/../../docs/tutor` и `$ROOT/../../bootstrap/flang`; в
  развёрнутом архиве (`tutor/`, `flang` рядом) и после установки
  (`../share/flang-tutor`) — как было. Подсказки `./flangtutor` в справке и в
  сообщениях (17 мест) стали именем команды `flangtutor`: файла `./flangtutor`
  в корне больше нет, а поставившему язык и прежде нужно было одно слово.
* **Поставка не правлена — и это проверено, а не решено на глаз.** Формула
  Homebrew и `packaging/asdf/bin/install` ставят из архива выпуска, а
  `scripts/release/release-in-c.flang` кладёт проводник в корень архива как
  прежде (берёт его теперь из `flang/bin/flangtutor`). Каталогов под
  `share/flang` не прибавилось — ровно то, о чём предупреждает заметка
  `a-lesson-directory-under-share-flang-would-break-the-target-count-in-two-packagers.md`.
* Сторож состава поставки (`scripts/guards/postavka-guard.flang`) спрашивал у
  `find` исполняемые файлы ТОЛЬКО корня — после переезда он сказал бы
  «flangtutor пропал». Теперь `find ../.. ../../flang/bin -maxdepth 1`, обещанное
  названо путём от корня (`flang/bin/flangtutor`, `ярлык`), строка формулы
  сверяется по короткому имени. Новых `обеспечивает` нет (иначе разошёлся бы
  столбец «написано» описи доказанного), функций 32 → 34.
* Проба проводника `scripts/flangtutor-proba.sh` (7 вызовов и один на Python),
  README уроков, `packaging/flang.1`, комментарий в `install-path.yml`, перечни
  подкаталогов `flang/` в `README.md`, `docs/README.ru.md`, ADR 0020.

**Прямые проверки после переезда:**

| прибор | ответ |
|---|---|
| `bootstrap/flang check` сторожа поставки и сборщика архива | у обоих «замечаний нет» |
| `./ярлык поставка:проверка` | код 0: «исполняемых файлов в корне и в flang/bin 2, из них едет 1; уроков 12 — все едут в архиве и ставятся формулой» |
| тот же сторож, план «Подлог» | код 1: «подлог пойман … flang/bin/flangtutor: поставка его обещает, а архив не несёт» |
| `sh scripts/flangtutor-proba.sh` | зелена, код 0, 13 с |
| `flang/bin/flangtutor --список` из чужого каталога | 12 уроков, код 0 |

**Сдвиг, найденный полным прогоном, и его починка.** После переезда сторож
рукописных перечней насчитал мёртвых записей 30 против 29 на стволе:
запись `scripts/site/build-changelog.mjs:106` объясняла, почему в перечне корней
стоит несуществующий `flang/bin` («удалён 3 сентября 2026 вместе с обёртками
npm»), а теперь каталог снова есть. Запись убрана, довод соседней
(`scripts/site/changelog.flang:257`) переписан на то, что там по-прежнему
исторично (`flang/cat`, `flang/examples`), комментарий у `flang/bin` в
`build-changelog.mjs` назван проводником. После — мёртвых 29, список `diff`-ом
тот же, что на стволе.

## Ш3 — `flang/conc` → `flang/concurrency`, 14 сентября 2026

`git mv` каталога целиком: 66 файлов, содержимое то же. Ссылки правлены
правилом «`flang/conc`, если перед ним нет `docs/` и за ним нет `urrency`» —
файлов 120, вхождений 482 (заранее по HEAD ожидалось 120 файлов и 451
строка; сошлось), — и 13 точечными правками относительных путей.

**Что потребовало не только замены пути:**

* Относительные пути, которых поиск по `flang/conc` не видит: `"../conc"` в
  перечне каталогов `flang/scripts/closed-length-guard.flang` (стал
  `"../concurrency"`, каталогов 21), ввозы `"../conc/scheduler.js"` и
  `"../conc/link.js"` в `flang/test/planirovshchik-celi.test.mjs` и
  `svyaz-celi.test.mjs`, перечни подкаталогов `flang/` в `README.md`,
  `docs/README.ru.md`, ADR 0020, комментарии «`conc/examples/…`».
* Правило видимости журнала изменений (`scripts/site/changelog.flang` и
  близнец `build-changelog.mjs`) знает `flang/concurrency` и по-прежнему знает
  `flang/conc` — журнал читает старые коммиты.
* Ведомости с путями (`no-comments-debt.tsv`, `proved-share-ledger.txt`,
  `target-function-drift-known.tsv`, `ведомость-примеров.txt`) правлены
  столбцом пути; порядок C у двух упорядоченных не сдвинулся (проверено
  `sort -c`).
* `flang/self/cli.flang:196` — строка справки; до перепечатки двоичный
  печатает старый путь.
* `docs/site/numbers.json`, «планировщик.строк» 1048 → 1049 — число, измеренное
  `published-vs-tree.sh --числа` в дереве. Правило этого ключа считает строки
  `flang/concurrency/scheduler.flang`; на снимке `d6e88d50`, с которым прибор
  сличает «тогда», по новому пути файла нет, и давнее устаревание на строку из
  «УСТАРЕЛО» стало «РАЗОШЛОСЬ» — так покраснела работа `published-check` у PR #28.
  Отметка «дешёвые сняты на d6e88d50» не тронута: остальные ключи не переснимались.

**Нарочно не тронуто:** `docs/flang/conc/` (контракты слоя; указатели
`flang/concurrency/*.md` ведут туда тем же относительным путём);
`flang/proof/checker/tests/families/run/pisma-po-ocheredi.flang:4` (комментарий,
ждёт 1420); `bootstrap/flang_repl.c`, `bootstrap/flang_runtime.h` (комментарии,
до перепечатки); снимки `flang/test/fixtures/zapis-*.json`,
`flang/test/snimok/proofterm-witness.json`; история.

**Сдвиг, найденный полным прогоном, и его починка.** Первая редакция вписала
в `scripts/site/build-changelog.mjs` корень `flang/concurrency` отдельной
строкой рядом с историческим `flang/conc`, и полный прогон назвал два сдвига:
строк JavaScript стало 29 699 вместо 29 698 (приметы описи дерева и описи
JavaScript, `./ярлык опись:сверка` покраснела), а номера строк, к которым
привязана ведомость рукописных перечней, съехали на единицу (новых
расхождений 61 против 59, мёртвых записей 30 против 29). Оба корня поставлены
в одну строку — номера и счёт вернулись; перечень видимых корней теперь
помнит исторический `flang/conc`, и это записано в ведомость
(`build-changelog.mjs:106`), довод записи `changelog.flang:257` дополнен.
После — опись сходится, перечни 59 / 29, списки те же, что на стволе.

**Сторожа до и после каждого переезда** (двоичный `bootstrap/flang` 0.7.19, одна машина; «после Ш2» и «после Ш3» — итоговые коммиты, с поправками ведомости перечней и журнала):

| прибор | ствол `4a438c14` | после Ш1 | после Ш2 | после Ш3 |
|---|---|---|---|---|
| `corpus-share.sh --проигрыванием` | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % |
| `scripts/доказуемость.sh` | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 |
| `./ярлык расширения:проверка` | код 0 | код 0 | код 0 | код 0 |
| `task-numbers-guard.sh` | код 0 | код 0 | код 0 | код 0 |
| `./ярлык задачник:проверка` | код 0 | код 0 | код 0 | код 0 |
| `published-vs-tree.sh --проза` | код 0 | код 0 | код 0 | код 0 |
| `./ярлык опись:сверка` | код 0 | код 0 | код 0 | код 0 |
| `sh .githooks/pre-push` | код 1: красных 2 | код 1: красных 2 | код 1: красных 2 | код 1: красных 2 |
| `flang/test/обход.sh` | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 |
| `./ярлык ссылки:проверка` | битых 37 из 6814 | битых 37 из 6848 | битых 37 из 6848 | битых 37 из 6848 |
| `hand-written-lists.sh --check` | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 |
| `./ярлык длина:проверка` | 405 / 284 | 405 / 284 | 405 / 284 | 405 / 284 |

Красное, общее со стволом: в `pre-push` — опись доказанного (две «НЕТ СТРОКИ» у файлов PR #19) и 8 примет чисел; ссылки — те же 37 битых (знаменатель вырос за счёт новых строк задачника); комментарии и перечни — долг дерева, списки сличены `diff`-ом целиком, разница только в путях.

## Чем закрыта

- `8fb527ab` — refactor(дерево): проверки на flang живут в flang/test — flang/проверки слит туда, 26 файлов.
- `5a130eed` — docs(задачник): запись Ш1.
- `a99b10f3` — refactor(дерево): проводник по языку живёт в flang/bin/flangtutor, а не в корне дерева.
- `a4d8c88b` — refactor(дерево): модель процессов живёт в flang/concurrency — flang/conc переименован, 66 файлов.
- коммит этого файла — записи Ш2 и Ш3, статус «сделана», перенос в `docs/tasks/completed/`.

Доля доказанного на каждом шаге — 629 / 651 = 96,62 %, как на `main` `4a438c14`;
сторожа отвечают тем же кодом и тем же списком красного, что ствол.

Перед сдачей ветка перебазирована пять раз: на `main` `0d957921` (влит PR #20),
на `341ee1ec` (влиты #21, #26, #27), на `3b3c89f5` (Lean, 8836, 3855), на
`6fa73f56` и на `abbf51e1` (1416). Конфликты — только в приметах и
числах: строка оболочки описи дерева и `binary.yml` (у ствола свои числа, у
ветки +7 строк обходчика), строка рантайма цели печати рядом с путями Ш3 и
строка заметок в `docs/course/README.md` (у ствола 675, с этой заметкой 676).
Числа всякий раз пересняты прибором на совмещённом дереве, а не сложены:
оболочка — 24 413 строк и долг 15 959, в `binary.yml` — 23 774 строки,
заметок — 676 (`prose-numbers-guard.sh` — 216 из 216, `./ярлык опись:сверка`
— код 0).

**Итог на стволе `341ee1ec`** — тот же набор приборов, вершина ветки против
ствола; двоичный тот же (`bootstrap/**` с `51c2625e` не менялся), сверщик
пересобран из нынешнего `checker.c`:

| прибор | ствол `341ee1ec` | вершина ветки |
|---|---|---|
| `corpus-share.sh --проигрыванием` | 629 / 651 = 96,62 % | 629 / 651 = 96,62 % |
| `scripts/доказуемость.sh` | ДОКАЗУЕМ, код 0 | ДОКАЗУЕМ, код 0 |
| `./ярлык расширения:проверка` | код 0 | код 0 |
| `task-numbers-guard.sh` | код 0 | код 0 |
| `./ярлык задачник:проверка` | код 0 | код 0 |
| `published-vs-tree.sh --проза` | код 0 | код 0 |
| `./ярлык опись:сверка` | код 0 | код 0 |
| `sh .githooks/pre-push` | код 1: красных 4 | код 1: красных 4 |
| `flang/test/обход.sh` | проверок 211, сошлись все 211 | проверок 211, сошлись все 211 |
| `./ярлык ссылки:проверка` | битых 39 из 6857 | битых 39 из 6838 |
| `hand-written-lists.sh --check` | новых 59, мёртвых записей 29 | новых 59, мёртвых записей 29 |
| `./ярлык длина:проверка` | 405 / 284 | 405 / 284 |

Красное совпадает со стволом строка в строку. В `pre-push` у обоих четыре
красных сторожа — `licenzii`, `opis`, `podlog`, `zov`: долг ствола после
PR #26. Опись доказанного у обоих — «бед 4: нет строки 2, «написано» не
сходится 2»; протухло 122 → 137 строк из 913 — md5 правленных здесь файлов,
приговоры ядра в описи не пересчитывались. Битые ссылки: у ствола 39 из 6857,
у ветки 39 из 6838; полные списки сличены `comm` — различий нет; знаменатель
меньше, потому что файл задачи 1418 переехал в `docs/tasks/completed/`, а этот
каталог сторож ссылок не проверяет. Рукописные перечни — новых 59, мёртвых 29
у обоих, списки те же с точностью до переименованных путей; прибавка
комментариев — та же.

**Про сверщика.** На перебазированной вершине `scripts/доказуемость.sh` сначала
ответил «НЕ ДОКАЗУЕМ»: пять честных проб на правила Кон2, Ч5, Разв1, О5 и О6
отвергнуты. Причина не в переездах: собранный в клоне `flang/proof/checker/сверщик`
(не отслеживается) был собран 13 сентября, а `checker.c` ствол обновил PR #21,
и прогон проб пересобирает сверщика, только если двоичного нет. После пересборки
той же командой, что в `run.sh`, — ДОКАЗУЕМ и на стволе, и на ветке. Доля с
этим двоичным — 629 / 651 у обоих; 631, которые ствол называет после PR #21, этим
двоичным не воспроизводятся ни на стволе, ни на ветке.

**Третье перебазирование — на `3b3c89f5`.** Ствол принёс 46 коммитов (Lean,
8836, 3855); конфликт тот же — строка оболочки, числа пересняты прибором. Полный
набор не повторялся: мои правки те же, а в принесённом нет ни одной ссылки на
старые пути (поиск по новым строкам ствола — 0). Быстрый набор совпал со стволом
`3b3c89f5`: красное `pre-push` строка в строку то же (`licenzii`, `opis`, `podlog`,
`zov`), доля 629 / 651 = 96,62 % у обоих, `scripts/доказуемость.sh` — ДОКАЗУЕМ у
обоих, задачник и номера задач — код 0.

**Четвёртое перебазирование — на `6fa73f56`.** Конфликт тот же — строка оболочки,
числа пересняты прибором (`binary.yml` — 23 752, опись — 24 391 / 15 937). Быстрый
набор совпал со стволом `6fa73f56`: красное `pre-push` строка в строку то же
(`licenzii`, `opis`, `podlog`, `proza`, `zov`; проза красна девятью приметами
ствола, которых эта ветка не касается: число `*.flang` 974 → 976, примеры и строки
`flang/self`, строки `checker.c`, файлы `flang/proof`), доля 629 / 651 у обоих,
раздел чисел сайта — 0 расхождений у обоих. `scripts/доказуемость.sh` сначала
ответил «НЕ ДОКАЗУЕМ» и на стволе, и на ветке: ствол снова обновил `checker.c`
(`d9dd94b6`); после пересборки сверщика — ДОКАЗУЕМ у обоих.

**Пятое перебазирование — на `abbf51e1`.** Задача 1416 объявила цели ярлыка в
самих сценариях и правила 20 тех же файлов, что эта ветка: git слил их сам, девять
слитых `.flang` проверены двоичным — замечаний нет. Конфликты — приметы строк
оболочки и JavaScript (29 711: Ш1 сжал комментарий журнала на 4 строки) и число
заметок (676 с этой заметкой). Быстрый набор совпал со стволом `abbf51e1`:
красное `pre-push` строка в строку то же, доля 629 / 651 у обоих, ДОКАЗУЕМ у обоих
(сверщик пересобран), раздел чисел сайта — 0 расхождений у обоих.
