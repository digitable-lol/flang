---
номер: 4058
заголовок: У выписанного списка, у `отобразить` и у применённого ярлыка функции нет правила «все элементы по построению» — обещание о списке функций не доказывается и не снимается на месте вызова
статус: сделана
исполнитель: a
ветка: a/3123-4058-second-order-over-tags
команда: первая
карта: Чего в языке нет вовсе
рядом: 6206, 5957, 9526, 1261, 3123
нужность: 1 — это и есть второй порядок ADR-0037: конечная конъюнкция над ярлыками функций
---

# 4058. Три случая, которых нет у семейства Э

Решение: [ADR-0037](../../adr/0037-second-order-is-a-finite-conjunction-over-function-tags.md) §4.3.

## Чем измерено

Прогон 13 сентября 2026, двоичный `bootstrap/flang` 0.7.19 из дерева `main`
`87fbd112`, `flang check <проба> --proof`:

| проба | исход |
|---|---|
| `обеспечивает для всех р из результат: р не меньше 0` при теле `отобразить функции как ф → ф от 1` | «сетка 1 значение», доказано 0 |
| `пусть ф равно функция «Удвоить»` и тело `ф от 1`, цель `результат не меньше 0` | доказано — через `пусть` ядро ярлык видит |
| место вызова `«Применить все к единице» от [функция «Удвоить», функция «Квадрат»]` с квантором в `требует` (после задачи 3123) | снять нечем: у выписанного списка правила нет |

«Отказ всех элементов» в ядре называет пропуск дословно: «Выписанный список с
элементами, отображение и вызов чужой функции не выводятся: у них нет правила».

## Где дыра

`flang/self/proof-kernel.flang`, «Все элементы по построению» (около строк
9085–9105): случаи — пустой `list`, `filter`, `if`, встроенное прибавление.
Комментарий над правилом (около 9052–9073) объясняет, почему трёх случаев нет:
выписанный список — «у сверщика в шаге не больше двух посылок»; отображение —
«свойство образа требует ∀ над прообразом с ДРУГИМ предикатом — своё правило и
своя лемма»; вызов чужой функции — восьмой ход, не построение. Первые два довода
— про цену, не про законность. Ведомость `flang/proof/tables/inference-rules.tsv` несёт
Э1–Э4 (строки 166–169); Lean — «Э1»…«Э4» в `Rules.lean`; `Model.lean`
отображения не знает (по словам `отображ` и `map` — ноль вхождений).

Применение выписанного ярлыка: после подстановки Э3/Э5 в цели встаёт терм
`(функция «Удвоить») от 1`. Вычислитель читает его тем же диспетчером, что и
`«Удвоить» от 1` (`interpret.flang`, случай `fnref`; `docs/flang/SPEC.md`,
«Функции первого класса»), а ядро — нет: проверить толкованием первым делом, и
если восьмой ход уже читает такой узел, третий случай не заводить.

## Что сделать

Ведомость и Lean — ПЕРВЫМИ, сверщик — раньше печати ядра (условие партии
ADR-0026 §11 неизменно).

1. **Э5 — выписанный список с головой.** Посылки: 1) Γ ⊢ П[э:=Х₁]; 2) Γ ⊢
   ∀э∈[Х₂…Хн]. П. Заключение: Γ ⊢ ∀э∈[Х₁…Хн]. П. Две посылки — форма сверщика
   соблюдена; хвост-литерал снова Э5, пустой — Э1. Захват — как у Э3.
2. **Э6 — отображение.** Посылка: Γ ⊢ ∀х∈Л'. П[э:=Ф]. Заключение: Γ ⊢
   ∀э∈(отобразить Л' как х → Ф). П. Заперто, если П связывает `х` или имя,
   свободное в Ф. В `Model.lean` завести отображение и лемму
   «всеЭ p (отобр f l) = всеЭ (p ∘ f) l»; ловушка — подстановка без замены имени.
3. **Яр1 — применение выписанного ярлыка** (если нужен по замеру выше):
   `(функция «Ф») от а …` есть `«Ф» от а …` — переписка по вычислителю, строка
   ведомости с «—» в правиле ядра, как у развёртки; в сверщике — отдельный шаг с
   одной посылкой, а не молчаливая нормализация внутри `терм()`.
4. Ядро: новые случаи в «Все элементы по построению»; печать —
   «Шаги всех элементов записи» в `flang/self/zapis.flang` (около 5642) печатает
   Э5/Э6/Яр1 с основаниями; сверщик — приёмы по образцу Э3 (около строки 8506),
   не больше 45 строк кода в бюджете ADR-0037 §4.5.
5. Пробы — семья `все-элементы`. Честные: выписанный список литералов;
   `отобразить` с предикатом, совпадающим после подстановки; сквозная программа
   ADR-0037 §2 п.7 (после задач 1261 и 3123): три обещания доказаны, место
   вызова снято. Подделки: Э5 с пропущенным элементом; Э6 с предикатом не о Ф;
   Яр1 с чужим именем функции; `отобразить`, где П связывает `х`.
6. Слово: семейство Э в `docs/flang/proof/SPEC.md` (сегодня о нём там ни
   строки — по словам `Э1`, `семейство Э`, `все элементы по построению` ноль
   вхождений), README семьи, текст «Отказ всех элементов».

## Чего не делать

- Не заводить квантор над ТИПОМ функции (`для всех ф: функция из …`): ADR-0026
  §11 п.8 и ADR-0037 §5 — у него нет носителя.
- Не вести индукции по функции.
- Не расширять Э6 на предикат, зависящий от номера элемента.
- Не печатать `forallIn` в цели печати — задача 7098.
- Не трогать `bootstrap/**`.

## Как поймём, что сделано

Первая строка таблицы с `требует` из 3123 — «доказано», запись проиграна
сверщиком кодом 0; сквозная программа — «утверждений 3: доказано 3», код 0 и без
`--строго`, и с ним; каждая подделка — код 1 с названной причиной; Lean принял
Э5/Э6 (и Яр1, если заведён) и отверг ловушки; `sh flang/proof/tables-guard.sh`
не хуже ствола (сегодня на стволе 2 красных: опоры правил «Инд2» и «Инд3» разъехались со строками `proof-kernel.flang:9251` и `:9284` — чужой долг, не этой задачи); строк кода
сверщика прибавилось не больше 45; `sh scripts/доказуемость.sh` — ДОКАЗУЕМ.

## Ход работы

**Состояние 14 сентября 2026: сделана.** Ветка `a/3123-4058-second-order-over-tags`
(PR #18) над стволом `a2015b08`, где влиты 1261, 2907, 9616, 1130, 5332 и 6438. Правки `flang/self/**` проверены
толкованием — зонд `flang/self/bootstrap/zond-k7.flang`, 600–660 с и 11–15 ГиБ на
программу; до двоичного `bootstrap/flang` (0.7.19) они доедут только перепечаткой
семени.

**Порядок партии соблюдён.** Ведомость и Lean раньше ядра: Э5 и Э6 — `2e94f534`,
Яр1 — `9fa45e30`. Сверщик раньше печати: приёмы Э5/Э6 в `шаг_вывода` — +38 строк
кода (6996 → 7034), храповик 7202 → 7240; Яр1 сверщику не стоил ни строки (ниже —
почему). Ядро и печать: Э5 и Э6 — `16dfa4a8`, ужатие — `cadc9135`, Яр1 — `cd65410b`.

**Яр1: замер и решение.** До влития 1261 место «Две» упиралось в снятие обещаний
вызванных, и Яр1 не заводился. После влития (зонд, 14 сентября) — в ярлык:
`FLANG_PRECONDITION_CALL`, «доказанных обещаний вызванных в доводе — 0». Э5
раскладывает список, и член `(функция «Удвоить») от 1 не меньше 0` остаётся без
счёта: на месте вызова сведение идёт без программы. Условие, записанное здесь
раньше, выполнено — Яр1 заведён. Перепиской внутри сведения он бы не помог:
обещания вызванных собираются по инстанции, а вызовов в ней нет. Поэтому Яр1
стоит в сборе фактов места вызова (`proofterm.flang`, «Ярлыки в узле», «Факты
ярлыка», «Обещания в инстанции»): в теле верхнего квантора над выписанным
списком с членом на месте элемента ищутся применения ярлыков, и доказанное
обещание «Ф» без `требует` берётся о самом узле применения. Узнавание ярлыка —
«Ярлык применения» в `proof-kernel.flang`, опора строки ведомости Яр1 (семейство
«Разв», сверено «код-уже»). Восьмой ход тел ярлыка по-прежнему не читает; место вызова
стоит в записи блоком снятия (ADR-0038), но без вывода — сверщик держит его на
слове ядра, и переигрывать шаг Яр1 ему нечего.

**Печать тотальности.** Сверщик читает ярлык функции в теле зовомым
(`вызовы_тела` — по тексту), а печать называла только вызовы, и запись сквозной
отвергалась: «тело F зовёт «Удвоить», а запись о нём молчит». В
`obligations.flang` («Зовы тела») узел `fnref` теперь зовомый наравне с `call`.

**Потолок ядра.** Первая сборка давала 5048 (+31 к 5017): «Выписанное всё»
повторяла «Прибавленное всё» строка в строку, и Э5 теперь решает функция Э3
(−16). Остаток — седьмой подъём `flang/scripts/kernel-lines-to-trust-ceiling.json`:
ядро решений 5017 → 5032, достижимо 8696 → 8745 (Яр1 — +28 достижимых, в ядро
решений не вошёл); строка — в ADR-0037 §4.5.

**Пробы** (семья `все-элементы`; записи — печать ядра зондом):

| проба | ядро | сверщик |
|---|---|---|
| `выписанные-литералы` (`[1, 2]`) | доказано: Э1, Выч, Э5, Выч, Э5, Разв2 | 0 |
| `отображение` | доказано: Т2 о прообразе, Э6, Разв2 | 0 |
| `вызов-с-выписанным` | место снято: Э5 (Т1 о «н»), Э1; после 1261 запись та же байт в байт | 0 |
| `сквозная-вторым-порядком` (ADR-0037 §3) | «утверждений 3: доказано 3», место «Две» снято Э5 и Яр1 | 3: на слове ядра — «квадрат неотрицателен» по объявлению (остаток 9616) и блок снятия «Две» без вывода (ADR-0038) |
| `порча/shortcut-without-promise`, `порча/shortcut-promise-not-proved` | место «Две» — `FLANG_PRECONDITION_CALL`, известно одно обещание (о «Удвоить») | 1, запись чужая |
| порчи `э5-подменён-член`, `э5-не-выписанный`, `э6-чужой-предикат`, `э6-не-отображение` | — | 1, причины названы |

## Чем закрыта

Ведомость и Lean — `2e94f534`, `9fa45e30`; ядро, сбор фактов, печать, сверщик,
пробы и записи — `16dfa4a8`, `cadc9135`, `cd65410b`; слово (SPEC, README семьи,
ADR-0037 §4.3 и §4.5) и этот файл — следующий коммит ветки. SHA — после
перебазирования на `a2015b08`; после нового — искать по заголовкам.

По пунктам «Как поймём, что сделано»:

- сквозная — «утверждений 3: доказано 3», сетка 0, «объявлено, не доказано» 0
  (зонд); код `flang check --proof` без `--строго` и с ним двоичным не снят —
  0.7.19 этих правок не несёт до перепечатки семени;
- каждая подделка — код 1 с названной причиной; `run.sh` — «сошлось всё»,
  честных 227, подделок 484 (вместе с пробами 9616);
- Lean — лемм 134 приняты, искажений 26 отвергнуто 26 (Э5, Э6, Яр1 — ловушки
  24, 25, 26), аксиомы теоремы состоятельности — три стандартные; сверка
  вердиктов — «расхождений: 0»: читатель Lean читает `[]` пустым списком (проба
  `#guard`); Э5 и Э6 вне приёмки Lean — долг ADR-0041 (у Э6 нет терма-отображения);
- `sh flang/proof/tables-guard.sh` — «сошлось всё», 102 строки, шесть
  сторожей зелены (опоры Инд2 и Инд3, красные на стволе, переставлены здесь);
- строк кода сверщика: 4058 — 38 из 45, всё ADR-0037 — 52 из 60 (7231 при
  храповике 7315; на стволе `a2015b08` — 7179 при 7263, прирост ветки тот же +52);
- `sh scripts/доказуемость.sh` — ДОКАЗУЕМ (доля проигрыванием 96.62 %, 629 из 651).

Не сделано здесь, и почему. Запись сквозной переигрывается кодом 3, а не 0:
одно место «по объявлению» у «Квадрат» сверщик не повторяет и после влития PR #21
(запись, переснятая на слитой печати, та же) — это остаток задачи 9616;
`run.sh` ждёт 3 и одно место на слове ядра и скажет сам, когда место снимут. ADR-0037 §8 п.8 («то же двоичным, без зонда») — после перепечатки семени.
