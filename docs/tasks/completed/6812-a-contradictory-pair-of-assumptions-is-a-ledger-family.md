---
номер: 6812
заголовок: Противоречие пары допущений ядро берёт правилом 8, а ведомость семейства не несёт — два места доли «сняты калькулятором»
статус: сделана
исполнитель: a
ветка: a/6432-6812-3448-last-places
команда: первая
карта: Сколько доказано на самом деле
рядом: 9616, 6432
нужность: 1 — два из двенадцати последних мест (ADR-0042 §2)
---

# 6812. Семейство Пр

Решение: [ADR-0042](../adr/0042-the-last-twelve-places-get-rules-not-exceptions.md) §2.

Места: `abilities` У23, `poddelka-protivorechie` «что угодно из несовместимой пары».
Шаг записи вида «Пр1 ⟨цель⟩ строка N строка M»: из `а меньше б` и `б не больше а` в
допущениях — любая цель. Строка ведомости (семейство Пр — первое, открытое сверх
закрытого списка, основание — ADR-0042), лемма Lean (обе посылки вместе ложны
при любых значениях модели, NaN включительно), ловушка (пара без противоречия —
`а меньше б` и `а не больше б`), случай в приёмке Lean, приём сверщика, печать
ядра, пробы и порча. Сделано — оба места переиграны, «калькулятором» у них ноль.

## Ход работы

14 сентября, исполнитель a, ветка `a/6432-6812-3448-last-places` (от main `d8b82eaf`).

Шаг записи вышел не «строка N строка M», а «Пр1 ⟨Ц⟩ из N M» над двумя листами
Т1: оснований в записи и так ровно пять видов, шестой («строка N строка M»)
потребовал бы разбора в сверщике, строки в `Запись.lean` и в читателе Lean, а Т1
уже есть и в сверщике, и в приёмке Lean. Посылки из шагов блока шире «допущений
функции» и так же состоятельны: пара ложна при любых значениях, откуда бы ни пришла.

| слой | что | чем проверено |
|---|---|---|
| ведомость | строка Пр1, семейство Пр; сторож С-2 знает Пр и М | `sh flang/proof/сверка-ведомости.sh` — красных 2, обе на стволе (опоры Инд2/Инд3) |
| Lean | лемма «Пр1» (`cases` по пяти видам значения, NaN включительно), встречный пример без перестановки, ловушка 24 | `прогон.sh`: ловушек 29 из 29 |
| приёмка Lean | `«ШагПринят».«Пр1»`, «Пр1?», «Пр1?-верно», случай в «шаг-верен» (`False.elim`) | поверх PR #29 покрыто 65 → 66, аксиомы — три стандартные |
| сверщик | приём Пр1, +10 строк кода | пять порч — код 1 с причиной |
| печать | «Блок противоречия записи» в `zapis.flang` | `bootstrap/flang check flang/self/zapis.flang` — код 0 |
| место | `poddelka-protivorechie` — зонд 661 с, 13,3 ГиБ; `abilities` У23 — зонд 2694 с, пик 51,7 ГиБ | сверщик: код 0 и 0, на слове ядра 0; Lean: ПРИНЯЛ оба блока; «разбор цели по условию» (калькулятор) 1 → 0, выводов 1 |

## Чем закрыта

`05b08008b` (ведомость и Lean), `067078d69` (сверщик), `f55786ada` (печать),
`e4f7540e1` и `3db005991` (пробы), `46701e369` (читатель Lean). Оба места переиграны на печати зонда,
«калькулятором» у них ноль. В корпус записи лягут перепечаткой семени (5190).
