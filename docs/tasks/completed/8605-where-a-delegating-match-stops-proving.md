---
номер: 8605
заголовок: Граница, на которой разбор с делегированием перестаёт доказываться, названа числом — и сказано, что её двигает
статус: сделана
исполнитель: a
ветка: a/where-a-delegating-match-stops-proving
команда: любая
карта: Где именно упирается доказатель
рядом: 5353, 1853, 5732
---

# 8605. Граница, на которой разбор с делегированием перестаёт доказываться, названа числом

Задача 5353 оставила строчку незакрытой, и закрыть её надо. Обработчик, чьё
тело — `разбор` с делегированием (каждая ветвь зовёт помощника, у помощника
доказанное постусловие того же вида), доказывается **на семи ветвях и не
доказывается на пятнадцати и восемнадцати**:

| файл, функция | ветвей | вышло |
|---|---:|---|
| `flang/conc/services/file-read.flang`, `«шаг почты»` | 7 | доказано |
| `flang/conc/services/host-io-survey.flang`, `«шаг почты»` | 15 | не доказано |
| `flang/conc/services/host-io-survey.flang`, `«шаг пробы»` | 18 | не доказано |

Три точки — не кривая. Между семью и пятнадцатью не мерено ничего.

## Почему это не «померить число ветвей» и не выйдет одним прогоном

Эти три случая отличаются **не только числом ветвей**, и пока отличия не
разведены, найденное число ничего не объяснит. Известные отличия:

* у `host-io-survey` разбираемая сумма несёт варианты **без полей**
  (`вариант «проба читать файл»`), у `file-read` — **с полями**;
* помощник `«записать»` принимает две строки, помощник `«доложить»` —
  состояние процесса и сообщение;
* обещания разные по виду: `равен 0` против `равен 1` и `не больше 1`.

Значит работа состоит из двух частей, и вторая важнее первой.

## Что сделать

1. **Проба, где меняется ровно одна величина.** Порождаемый файл: сумма из N
   вариантов, помощник с доказанным постусловием, функция с телом-`разбор` на N
   ветвей, каждая ветвь — вызов помощника, у функции то же постусловие. Всё
   остальное между N одинаково байт в байт, кроме самих ветвей.
2. **Найти точку делением пополам** между 7 и 15, потом раздвинуть в обе
   стороны, чтобы убедиться, что это порог, а не яма.
3. **Проверить каждое из трёх отличий отдельно**, удерживая остальные: поля у
   вариантов, вид доводов помощника, вид обещания.
4. **Сверить с настоящими случаями.** Найденная граница обязана объяснить, что
   `«шаг почты»` в `file-read` доказывается, а в `host-io-survey` нет. Не
   объясняет — значит дело не в ветвях, и это надо написать прямо.

**Пробу заводить БЕЗ объявления процессов.** Тогда работает
`flang check --proof` и вердикт по каждому утверждению читается прямо
(`доказано` / `на сетке` / `объявлено`), а не выводится из числа напечатанных
байт: на программе с процессами отчёт не печатается вовсе (задача 1853).

## Как понять, что сделано

Названо число ветвей, на котором вердикт перестаёт быть `доказано`, и названо,
при каких условиях это число меняется. Каждая точка — прогон с командой, её
выводом и кодом возврата. Отрицательный ответ («ветви ни при чём, решает
такое-то») — полноценный итог этой задачи, а не её провал.

## Зачем это, кроме любопытства

Сейчас человек, дописывающий утверждение, узнаёт о стене, только упёршись в неё:
обещание молча уезжает проверкой в каждую напечатанную программу, и заметно это
лишь по выросшему размеру вывода. Названная граница переводит это из случайности
в правило, которое можно написать в навыке.

## Чем закрыта

Коммиты `c7379850` (починка), `a1848333` (поправки), `c7a8728e` (правило в
навыке) в ветке `a/where-a-delegating-match-stops-proving`.

**Ответ отрицательный: границы по числу ветвей НЕ СУЩЕСТВУЕТ.** Проба, где
менялось ровно одно — число ветвей, — доказывается на **1, 7, 15, 18 и 30**
ветвях; с полями у вариантов и без полей; и при четырёх таких парах
«помощник + разбор» в одном файле. Вынутый из настоящего файла `«шаг почты»` со
своими пятнадцатью ветвями доказывается тоже.

## Почему казалось, что стена есть

Две причины, обе мои.

**Первая: двух случаев из трёх не было вовсе.** У `«шаг пробы»` и `«шаг почты»`
в `host-io-survey.flang` не хватало `пример`а. Обещание снимается из
напечатанного кода при двух условиях сразу — доказано ядром И есть пример;
примеры были дописаны помощникам, а обработчикам нет. Дописан пример — оба
доказаны: **453 089 байт (как в стволе), сторожей 1, снято 3 → 6**.

**Вторая, породившая первую: признак был негодный.** «Напечатанные байты не
должны измениться» — так задача 5353 называла признак доказанности. Он им не
является: байты растут и когда обещание не доказано, и когда при нём нет
примера, и по ним эти два случая не различить. Правило дописано в навык
`flang-code`.

## Настоящая стена, названная точно

Третий случай — `node-reference-checked-on-receipt.flang`, `«шаг узла»` — стена
настоящая, но она не про ветви. Сведена к пробе в тридцать строк без процессов,
где отчёт о доказательствах печатается и вердикт читается прямо:

| помощник обещает | цель зовущего | вышло |
|---|---|---|
| `(длина результат) равен 1` | `равен ((длина соседи) плюс 1)` | **доказано** |
| `(длина результат) равен 1` | `не больше ((длина соседи) плюс 1)` | **доказано** |
| `(длина результат) не больше 1` | `не больше ((длина соседи) плюс 1)` | сетка |
| `если … то 1 иначе 0` | `не больше ((длина соседи) плюс 1)` | сетка |
| `если … то 1 иначе 0` | `не больше ((длина соседи) плюс (длина «вызов»))` | **доказано** |

Между третьей строкой и первой меняется ровно одно: **точное значение против
границы**. Условная форма постусловия ни при чём — четвёртая падает так же, как
третья, а пятая с той же условной формой проходит.

Читается так: у зовущего есть `результат = соседи + новые` (равенство от
`«Приписать ссылки»`) и `новые ≤ 1` (граница от помощника). Свести их в
`результат ≤ соседи + 1` ядро не берётся. Подставить равенство — берётся.

Обход есть, но не для этого случая: переписать границу цели через то же
выражение, которое ядро выводит точно (пятая строка). У `«шаг узла»` так
написать нечем — `имя` и `назвался` связаны в ветви `разбор`а, а постусловие
говорит только о доводах функции.

## Что стало с деревом

Замер по всем семнадцати файлам `flang/conc/**` двоичным `bootstrap/flang`
дерева, очередь на тяжёлые прогоны `PAMYAT=45G`:

| | до работ 5353 | сейчас |
|---|---:|---:|
| проверок при работе снято | 3 | **68** |
| обработчиков с обещанием (из 38) | 0 | **37** |
| файлов, где напечатанное изменилось | — | **0 из 17** |

Без обещания остался один обработчик — тот, у которого стена названа выше.

Заметка: [[a-callee-promise-reaches-the-handler-through-seven-branches-not-fifteen]]
переписана поправкой — прежний её заголовок про «семь ветвей против пятнадцати»
опровергнут, и видно, чем именно.

