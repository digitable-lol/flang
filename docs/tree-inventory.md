# Опись дерева по языкам: 266 файлов вне flang, долг вне JavaScript — 106 при потолке 63
<!-- СНЯТО 2026-09-05 файлов *.sh,*.c,*.h,*.py,*.html,*.css,*.awk,*.erl,*.js,*.mjs,*.java,*.cs,*.ex,*.exs,*.go,*.rs,*.lua,*.vim,*.rb,*.cpp,*.cc,*.hpp,*.hh,ярлык,packaging/asdf/bin/download,packaging/asdf/bin/install,packaging/asdf/bin/list-all,.githooks/pre-push = 266 -->

⚠ **ХРАПОВИК ДОЛГА КРАСЕН, и заголовок это теперь говорит.** Прогон
`./ярлык опись:языки` **5 сентября 2026** отвечает кодом 1: «ДОЛГ ВНЕ
JavaScript: файлов 106, строк 15484, потолок файлов 63» — «ДОЛГ ВЫРОС».
Разбор и перевод — [задача
4838](../tasks/4838-the-debt-outside-flang-crossed-its-ceiling.md), счёт по
родам — [задача 9688](../tasks/9688-debt-outside-flang-is-103-and-the-ceiling-cannot-be-raised-honestly.md),
починка самого правила и первые закрытия — [задача
7405](../tasks/7405-dolg-vne-flang-razobran-po-razryadam-i-pravilo-scheta-pochineno.md).

⚠ **106 БОЛЬШЕ, ЧЕМ БЫЛО 104, И ЭТО НЕ РОСТ ДОЛГА, А КОНЕЦ НЕДОСЧЁТА.**
5 сентября 2026 в правиле счёта найдены три слепых зоны, и все три впустили
файлы уже ПОСЛЕ того, как снят потолок (задача 7405):

* файл оболочки без расширения виден правилу только по имени, а имён было три
  (`ярлык` и три пути `packaging/asdf/bin/`). `.githooks/pre-push` — 122 строки
  оболочки, заведён 5 сентября — не попадал в счёт ВОВСЕ;
* довод «замеряемый материал» был записан как «`benchmarks/` и не `.sh`», то
  есть по расширению, хотя сам довод говорит «а не оснастка». Оснастка замера
  на Python уходила из долга даром — семь файлов, из них
  `кеш-доказанного/ключи-по-дереву.py` (2 сентября) и
  `кеш-приговоров/наложить.py` (3 сентября) заведены уже при снятом потолке;
* C++ не был языком совсем: `flang/src/emit/cpp/flang_cpp.hpp` (404 строки,
  30 августа) не попадал ни в одну строку таблицы. Долгом он не был бы и тогда,
  но `.cpp` в любом другом месте дерева храповик пропустил бы молча.

Недосчёт был восемь файлов. Правило починено, недосчёта нет; в тот же день
закрыто шесть файлов, и 112 стало 106.

Прежние замеры, чтобы виден был ход: 29 августа — 63 файла и 5 452 строки;
1 сентября — 74 и 8 729; 5 сентября утром — 104 и 13 875 (по правилу с
недосчётом). Ни одно из этих чисел не описывает нынешнее дерево, и они
оставлены только как след.

Снято 29 августа 2026 одним прогоном в корне дерева:

```
$ ./ярлык опись:языки
$ bootstrap/flang io scripts/tree-inventory.flang --max-steps 50000000
```

Прогон стоит 17,7 секунды и 471 МБ; двоичному нужны только `git ls-files` и
`wc -l`. Он же стоит проверкой на каждый пуш — работа «Опись дерева по языкам
(долг не вырос)» в `.github/workflows/binary.yml`.

## Зачем эта опись понадобилась

Счётчиков долга в дереве было три, и все три считали ОДИН язык:

| счётчик | что считает |
|---|---|
| `ROADMAP.md`, раздел «Вне языка» | 54 файла на JavaScript, 25 487 строк |
| [`docs/javascript-inventory.md`](javascript-inventory.md) | те же 54, разложенные по кучам |
| числа сайта (`docs/site/site-numbers.mjs`) | те же файлы на JavaScript |

Оболочки, HTML, CSS, проб на C, Python, awk и Erlang не считал никто. Это
**66 файлов оболочки на 6 305 строк** плюс мелочь — то есть примерно половина
всего, что в дереве написано не на flang, была невидима: её не было ни в
отчёте, ни в проверке, ни на странице. Долг, которого никто не считает, не
убывает — он растёт молча.

## Что нашла опись

| язык | файлов | строк | долг файлов | долг строк |
|---|---:|---:|---:|---:|
| оболочка | 100 | 17 530 | 89 | 11 562 <!-- СНЯТО 2026-09-05 файлов *.sh,ярлык,packaging/asdf/bin/download,packaging/asdf/bin/install,packaging/asdf/bin/list-all,.githooks/pre-push = 100 --><!-- СНЯТО 2026-09-05 строк-в *.sh,ярлык,packaging/asdf/bin/download,packaging/asdf/bin/install,packaging/asdf/bin/list-all,.githooks/pre-push = 17530 --> |
| C | 38 | 774 574 | 0 | 0 <!-- СНЯТО 2026-09-04 файлов *.c,*.h = 38 --><!-- СНЯТО 2026-09-05 строк-в *.c,*.h = 774574 --> |
| C++ | 1 | 404 | 0 | 0 <!-- СНЯТО 2026-09-05 файлов *.cpp,*.cc,*.hpp,*.hh = 1 --><!-- СНЯТО 2026-09-05 строк-в *.cpp,*.cc,*.hpp,*.hh = 404 --> |
| Python | 22 | 7 744 | 16 | 3 843 <!-- СНЯТО 2026-09-05 файлов *.py = 22 --><!-- СНЯТО 2026-09-05 строк-в *.py = 7744 --> |
| HTML | 4 | 1 226 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.html = 4 --><!-- СНЯТО 2026-08-31 строк-в *.html = 1226 --> |
| CSS | 1 | 559 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.css = 1 --><!-- СНЯТО 2026-08-31 строк-в *.css = 559 --> |
| awk | 1 | 79 | 1 | 79 <!-- СНЯТО 2026-08-31 файлов *.awk = 1 --><!-- СНЯТО 2026-08-31 строк-в *.awk = 79 --> |
| Erlang | 1 | 69 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.erl = 1 --><!-- СНЯТО 2026-08-31 строк-в *.erl = 69 --> |
| JavaScript | 62 | 28 327 | считает [`javascript-inventory.md`](javascript-inventory.md) <!-- СНЯТО 2026-09-04 файлов *.js,*.mjs = 62 --><!-- СНЯТО 2026-09-05 строк-в *.js,*.mjs = 28327 --> ||
| Java | 7 | 3 910 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.java = 7 --><!-- СНЯТО 2026-08-31 строк-в *.java = 3910 --> |
| C# | 7 | 4 451 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.cs = 7 --><!-- СНЯТО 2026-08-31 строк-в *.cs = 4451 --> |
| Elixir | 4 | 4 619 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.ex,*.exs = 4 --><!-- СНЯТО 2026-08-31 строк-в *.ex,*.exs = 4619 --> |
| Go | 3 | 2 862 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.go = 3 --><!-- СНЯТО 2026-08-31 строк-в *.go = 2862 --> |
| Rust | 3 | 3 855 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.rs = 3 --><!-- СНЯТО 2026-08-31 строк-в *.rs = 3855 --> |
| Lua | 3 | 224 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.lua = 3 --><!-- СНЯТО 2026-08-31 строк-в *.lua = 224 --> |
| Vimscript | 8 | 390 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.vim = 8 --><!-- СНЯТО 2026-08-31 строк-в *.vim = 390 --> |
| Ruby | 1 | 271 | 0 | 0 <!-- СНЯТО 2026-08-31 файлов *.rb = 1 --><!-- СНЯТО 2026-09-05 строк-в *.rb = 271 --> |


### Почему у столбцов долга приметы нет, и её нечем поставить

Числа «файлов» и «строк» здесь стережёт `scripts/prose-numbers-guard.sh`:
рядом с каждым стоит примета, и сторож пересчитывает её на каждом пуше.
**У столбцов «долг файлов» и «долг строк» приметы нет ни в одной строке**, и
это не забывчивость.

У сторожа пять приборов — `строк`, `файлов`, `строк-в`, `примеров-в`,
`список`, — и все пять считают по ОБРАЗЦУ ПУТИ. Долг образцом не считается:
он определяется функцией `«Довод не-долга»` (`scripts/tree-inventory.flang`),
а это правило из пятнадцати ветвей, где у каждого исключения свой довод, и
дозволенное основание ровно одно — КРУГ. Прибор, считающий долг «по
образцу», давал бы другое число, чем опись, и приметы разошлись бы с ней
молча.

**Проверять эту таблицу должна сама опись**, а не сторож чисел: она уже
считает все четыре столбца, уже печатает их этой же разметкой и уже стоит
проверкой на каждый пуш (`binary.yml`). Второй счёт того же — та самая
болезнь, а не лекарство.

### Оговорка о приметах, которые здесь всё-таки стоят

Опись считает языком не только расширение, но и **имена файлов без
расширения**. Шебанг она НЕ читает — до 5 сентября 2026 и этот раздел, и
комментарий в самой описи утверждали обратное, и это была неправда: имена
перечислены в правиле руками. Сегодня их пять: `ярлык`, три в
`packaging/asdf/bin/` и каталог `.githooks/` целиком. Поэтому у строки
«оболочка» образец примет перечисляет их поимённо — иначе примета зеленела бы
на 95 при столбце 100, то есть стерегла бы не то число, которое написано рядом.

Прочие шесть строк с непустым долгом такого расхождения не дают: у их языков
файлов без расширения в дереве нет. Совпадение это, а не правило.

**Хрупкость эта уже сработала, и никто не заметил.** Раздел предсказывал:
«появится ПЯТЫЙ файл оболочки без расширения — опись насчитает 106, столбец
останется 105, и примета останется зелёной». Пятый появился — `.githooks/pre-push`,
122 строки, 5 сентября, — и вышло ХУЖЕ предсказанного: опись насчитала не 106,
а те же 105, потому что нового файла она не увидела вовсе. Сверка таблицы с
описью тут не помогла бы: обе стороны считали по одному и тому же слепому
правилу. Помогло чтение правила глазами (задача 7405). Взят теперь каталог,
а не имя: хук git не бывает не программой, и следующий хук считаться будет сам.
Для файла оболочки без расширения ВНЕ `.githooks/` дыра остаётся открытой —
закрыть её нечем, кроме чтения первой строки, а этого опись не делает.

**Долг вне JavaScript — 106 файлов, 15 484 строки.** JavaScript опись считает, но
по кучам не раскладывает: его раскладывает
[`javascript-inventory.md`](javascript-inventory.md), и два разных разбора
одного языка разъехались бы на первой же правке. Числа этой описи и той
сходятся: 54 файла, 26 383 строки. (29 августа стояло 25 527 против 25 487
двумя днями раньше — за два дня прибавилось 856 строк, и опись этого не
заметила, потому что число было набрано рукой и сторожа над ним не было.)

Три четверти миллиона строк C — это не долг и даже не почти долг: 607 416 из
них — `bootstrap/compiler_flang.c`, то есть сам компилятор, напечатанный из
flang в C.
<!-- СНЯТО 2026-09-05 строк bootstrap/compiler_flang.c = 607416 -->
Ровно этот файл и есть доказательство того, что язык себя обслуживает.

## Не долг: двенадцать доводов, у каждого свой счёт

У каждой строки не-долга есть довод, а не мнение. Опись хранит их не в прозе, а
в самой программе — функция «Довод не-долга» в `scripts/tree-inventory.flang`
называет причину для каждого пути, и проверка падает, если в дереве появился
файл, которому довода нет.

| довод | что под ним | файлов | строк |
|---|---|---:|---:|
| напечатано самим компилятором | `bootstrap/**` — шесть файлов на C, вывод печати | 6 | 736 625 <!-- СНЯТО 2026-08-31 файлов bootstrap/*.c,bootstrap/*.h = 6 --><!-- СНЯТО 2026-09-05 строк-в bootstrap/*.c,bootstrap/*.h = 736625 --> |
| рантайм цели печати | `flang/src/emit/{c,cpp,python,java,csharp,elixir,go,rust}/**` — уезжает в напечатанную программу дословно | 28 | 45 867 <!-- СНЯТО 2026-09-05 файлов flang/src/emit/c/*,flang/src/emit/cpp/*,flang/src/emit/python/*,flang/src/emit/java/*,flang/src/emit/csharp/*,flang/src/emit/elixir/*,flang/src/emit/go/*,flang/src/emit/rust/* = 28 --><!-- СНЯТО 2026-09-05 строк-в flang/src/emit/c/*,flang/src/emit/cpp/*,flang/src/emit/python/*,flang/src/emit/java/*,flang/src/emit/csharp/*,flang/src/emit/elixir/*,flang/src/emit/go/*,flang/src/emit/rust/* = 45867 --> |
| замеряемый материал | `benchmarks/**` без оболочки и без оснастки на Python (`tasks.py` — набор задач замера, он материал) плюс `flang/conc/bench/beam.erl` — это то, с чем сравнивают | 12 | 3 217 |
| проба рантайма C | `flang/проверки/oblast/*.c` и `flang/conc/bench/sizes.c` — двенадцать проб памяти, их заголовок и размеры записей; на flang они проверяли бы не рантайм, а себя. Гоняет их `scripts/region-in-c-target.flang`, уже написанный на flang | 14 | 674 |
| чужая среда: редактор | `editors/vim/**` — vim и neovim грузят только vimscript и Lua | 11 | 614 <!-- СНЯТО 2026-08-31 файлов editors/vim/*.vim,editors/vim/*.lua = 11 --><!-- СНЯТО 2026-08-31 строк-в editors/vim/*.vim,editors/vim/*.lua = 614 --> |
| код на стороне цели | `flang/conc/bin/node.{c,cs,ex,go,java,py,rs}` — семь хозяев узлов на семи языках, и `peer.py` — конец связи на цели python | 8 | 8 227 |
| разметка и оформление | четыре `.html` и `docs/site/style.css` — цели HTML у языка нет и не заявлено | 5 | 1 785 |
| чужая среда: установщик | `packaging/homebrew/flang.rb` и три файла `packaging/asdf/bin/` — homebrew понимает Ruby, asdf зовёт свои три раньше, чем flang в системе есть | 4 | 470 |
| точка раскрутки и приёмка | `scripts/raskrutka.sh`, `scripts/bootstrap-c.sh`, `scripts/new-binary-acceptance.sh`, `scripts/build-ledger-binary.sh` — разобраны отдельно ниже | 4 | 4 080 |
| сверка двоичного и его печати | `scripts/binary-origin.sh` и `scripts/overlong-string-guard.sh` — заведены 29 августа, разобраны 30-го, см. ниже | 2 | 876 |
| чужой хозяин примера | `examples/host-boundary/host.c` — этим примером и показывают границу с чужим кодом | 1 | 227 <!-- СНЯТО 2026-08-31 строк examples/host-boundary/host.c = 227 --> |
| независимый чекер записи | `flang/proof/чекер/**` — сверщик записи доказательства на C, прогон его проб и одна проба-подделка на оболочке; заведён 31 августа, разобран ниже | 3 | 4 417 |

Восьмой хозяин узла (`node.js`) и восьмой рантайм (`flang/src/emit/js/**`)
стоят в счёте JavaScript и разобраны в его собственной описи — по тем же
доводам.

### Почему точка раскрутки и приёмка остаются на оболочке

Довод один и тот же, и он записан в шапках самих файлов: **судья не может
исполняться подсудимым.**

`scripts/raskrutka.sh` печатает компилятор заново и сверяет напечатанное с
закоммиченным байт в байт. Напиши эту сверку планом на flang — и её будет
исполнять исполнитель планов ТОГО САМОГО двоичного, который проверяют. Зелёный
ответ переставал бы что-либо значить: сломанный двоичный одинаково способен и
напечатать не то, и сказать, что напечатал то. То же у
`scripts/bootstrap-c.sh` (второй путь печати) и у
`scripts/new-binary-acceptance.sh` (приёмка нового двоичного после перепечатки).

Оболочка при этом ничего нового на путь сборки не приносит: `sh`, `make`, `cc`
и `cmp` там нужны и так, а Node не нужен ни на одном шаге.

## Долг: 106 файлов, 15 484 строки

Снято прогоном `./ярлык опись:языки` 5 сентября 2026, уже по починенному
правилу (задача 7405). По языкам: оболочка 89, Python 16, awk 1.

| место | файлов | строк | чем это держится |
|---|---:|---:|---|
| `scripts` — сборка и сторожа | 33 | 6 551 | **главная часть долга; тринадцать самых крупных разобраны ниже** |
| `benchmarks` — оснастка замеров | 27 | 2 808 | собирает сборки одной программы через `cc`, вшивает приборы в семя, гоняет чужие службы моделей; переводимо |
| `flang/conc/bench` — оснастка замера узлов | 18 | 710 | гонит настоящие процессы на восьми целях и убивает их сигналами; переводимо, но польза мала |
| `flang/proof` — прогон подделок, сверка правил и приборы привязки | 10 | 1 893 | ищет и возит свидетеля; сам приговор уже на flang |
| `flang/scripts` — счётчики на Python | 3 | 1 398 | доля доказанного по файлам, сличение двух сводителей и одна линейка на flang и C |
| `web` — печать браузерных приложений и проба в браузере | 3 | 376 | зовёт `clang`, `wasm-ld` и настоящий браузер |
| `editors/vim/checks` — прогон подсветки | 3 | 136 | поднимает настоящий vim |
| `docs/tools` — доля целей и охват правил у связывателя | 2 | 411 | переводимо |
| `flang/проверки` — обходчики | 2 | 357 | ищут файлы и возят их в двоичный; ведомость уже на flang |
| `docs/ifl` — воспроизведение чисел статьи | 2 | 262 | переводимо |
| `ярлык` — точка входа репозитория | 1 | 186 | круг: спрашивает двоичный о команде и запускает ответ. **Довода ему не выписано** — см. ниже |
| `.githooks/pre-push` — хук перед пушем | 1 | 122 | зовёт дешёвых сторожей; переводимо, но зовёт его git, а не flang |
| `examples/host-boundary/run.sh` — прогон стыка | 1 | 54 | переводимо |

### `ярлык` в долге, а рядом написано «круг» — это не описка

Столбец «чем это держится» у `ярлык` говорит «круг», и это правда: чтобы
прочитать `ярлыки.flang`, нужен двоичный, а ярлык умеет собирать его сам, когда
двоичного ещё нет. Ровно по этому основанию из долга вычеркнуты
`scripts/raskrutka.sh` и `scripts/binary-origin.sh`.

**Довода `ярлык` всё-таки не выписано, и это решение, а не забывчивость.**
Выписать его — значит опустить долг на единицу и на ту же единицу ослабить
храповик, а разбор довода дороже перевода одного файла. Решение здесь за
владельцем: либо довод пишется (тогда долг 105), либо строка остаётся в долге
как есть. Сама опись до этого решения не меняется.

### Тринадцать файлов в `scripts` — по убыванию, разбор 29 августа 2026

| файл | строк | что делает |
|---|---:|---|
| `target-collisions.sh` | 337 | столкновения имён на восьми целях печати <!-- СНЯТО 2026-08-31 строк scripts/target-collisions.sh = 337 --> |
| `bad-octet-guard.sh` | 314 | сторож негодных октетов <!-- СНЯТО 2026-08-29 строк scripts/bad-octet-guard.sh = 314 --> |
| `seed-knows-type-words-guard.sh` | 309 | слова в позициях типа известны закоммиченному семени (стоит первой работой в `ci.yml`) <!-- СНЯТО 2026-09-01 строк scripts/seed-knows-type-words-guard.sh = 309 --> |
| `prose-numbers-guard.sh` | 416 | числа, набранные в прозе рукой, сходятся с деревом сегодня <!-- СНЯТО 2026-09-05 строк scripts/prose-numbers-guard.sh = 416 --> |
| `memory-limit.sh` | 259 | предел памяти прогона <!-- СНЯТО 2026-08-29 строк scripts/memory-limit.sh = 259 --> |
| `target-census.sh` | 214 | перепись целей <!-- СНЯТО 2026-08-29 строк scripts/target-census.sh = 214 --> |
| `seed-freshness.sh` | 188 | отказ судить о доказательствах при отставшем семени <!-- СНЯТО 2026-08-31 строк scripts/seed-freshness.sh = 188 --> |
| `test-remote.sh` | 129 | прогон на чужой машине <!-- СНЯТО 2026-08-29 строк scripts/test-remote.sh = 129 --> |
| `one-string-measure-guard.sh` | 119 | подаёт сырые октеты в `string-measure.flang` <!-- СНЯТО 2026-08-29 строк scripts/one-string-measure-guard.sh = 119 --> |
| `identical-declarations.sh` | 118 | что можно ввезти вместо переименования <!-- СНЯТО 2026-08-29 строк scripts/identical-declarations.sh = 118 --> |
| `seed-parses-sources-guard.sh` | 220 | семя разбирает исходники, которые потом печатает <!-- СНЯТО 2026-08-31 строк scripts/seed-parses-sources-guard.sh = 220 --> |
| `targets-inventory.sh` | 79 | опись целей: имя, код, время, причина <!-- СНЯТО 2026-08-29 строк scripts/targets-inventory.sh = 79 --> |
| `names-in-c.awk` | 79 | «идентификатор C → русское объявление» из напечатанного заголовка <!-- СНЯТО 2026-08-29 строк scripts/names-in-c.awk = 79 --> |

Три из них — `seed-knows-type-words-guard.sh`, `seed-freshness.sh`,
`seed-parses-sources-guard.sh` — стоят рядом с точкой раскрутки, но НЕ под тем
же доводом: они не судят напечатанное, а спрашивают, читает ли семя исходники.
Ответ на такой вопрос от правил семени не зависит, и переводить их можно —
осторожно и по одному.

`names-in-c.awk` — единственный файл на awk во всём дереве. Перевести его значит
вычеркнуть из дерева целый язык; мешает то, что его зовёт `target-collisions.sh`,
и переводить их придётся вместе.

Список этот снят 29 августа и с тех пор не пересматривался: в `scripts` сегодня
33 файла долга, а не 13. Поимённый список даёт сам прогон — `./ярлык опись:языки`
печатает каждый файл долга с языком и числом строк, и другого списка заводить не
надо: второй счёт того же — та самая болезнь, а не лекарство.

## Что закрыто 5 сентября 2026 (задача 7405)

Закрыто шесть файлов, и все шесть — одного рода: **записки отдельных прогонов,
прибитые к чужим машинам.** Ни один не запускался из свежего клона, потому что
первой же строкой делал `cd` на путь, которого в дереве нет. Разбор рода —
[задача 9688](../tasks/9688-debt-outside-flang-is-103-and-the-ceiling-cannot-be-raised-honestly.md),
там же они названы поимённо.

Пути ниже в дереве уже не лежат, поэтому названы в блоке, а не ссылками:

```
убрано                                          строк  почему это сор, а не долг
benchmarks/proof-cost/all-20.sh                    24  зовёт vydelit.mjs, которого в дереве нет
                                                       (ведомость перечней звала его мёртвым с задачи 4717)
benchmarks/proof-cost/check.sh                     13  cd на рабочий каталог агента
benchmarks/proof-cost/prove.sh                      5  cd на рабочий каталог агента
benchmarks/proof-cost/test.sh                       5  cd на рабочий каталог агента
benchmarks/model-authoring/queue-extra-arms.sh      7  cd на /home/m/..., sudo -u m, pgrep -f
benchmarks/model-authoring/queue-fix-arm.sh         8  то же
```

Два файла того же рода ОСТАВЛЕНЫ, а `cd` у них переведён на путь от себя:
`benchmarks/proof-cost/all-tests.sh` (на него ссылается сторож
`scripts/file-extensions.flang`) и `benchmarks/суд-ядра-по-ядрам/доли.sh`
(часть живого замера Ч180; каталог вывода тоже отвязан от чужой машины). Долга
они не двигают: файлы остаются в дереве и в долге.

Две строки из `scripts/hand-written-lists-ledger.tsv` убраны вместе с файлами:
ведомость сверяется в обе стороны и покраснела бы на записи, переставшей быть
расхождением.

## Что переведено 29 августа 2026

Оба перевода сверены с прежними прогоном обоих на одних входах: не «похоже», а
слово в слово и код в код.

| было | стало | сверка |
|---|---|---|
| `scripts/adr-numbers-are-unique-guard.sh`, 93 строки | `scripts/adr-numbers-guard.flang` | шесть входов, вывод знак в знак, коды совпали |
| `scripts/kernel-abilities-guard.sh`, 108 строк | `scripts/kernel-abilities-guard.flang` | пять входов, вывод знак в знак, коды совпали |

**Номера решений** — шесть входов: дерево как есть (код 0, 91 байт), подлог с
двумя двойнями (код 1, 239 байт), решение без номера в заголовке (код 1,
112 байт), имя файла разошлось с шапкой (код 1, 162 байта), настоящая двойня
по существу (код 1, 178 байт), дерево, возвращённое как было (код 0, 91 байт).

**Умения ядра** — пять входов: дерево как есть (код 1, 267 байт — сторож в
дереве СЕГОДНЯ красный, см. ниже), лишняя строка списка со всеми четырьмя
бедами разом (код 1, 823 байта), неполная строка (код 1, 463 байта),
укороченный список — расхождение счёта (код 1, 130 байт), список, возвращённый
как был (код 1, 267 байт).

Одно отличие названо вслух в шапке нового файла: пропавший файл прежняя
редакция называла полным путём от корня машины, новая — путём от корня дерева,
как названы все прочие файлы в её же выводе.

### Сторож умений ядра красен в стволе, и это не мой перевод

Обе редакции — прежняя на оболочке и новая на flang — отвечают одним и тем же:

```
У24: ЯДРО отстало — якоря «прибавление к ГРАНИЦЕ неотрицательного конечного
литерала при уже известном порядке» в proof-kernel.flang нет
разошлось мест: 1
```

Поэтому в проверки на каждый пуш он НЕ поставлен: работа встала бы красной с
первой минуты, и красной не по делу перевода. Ставить его надо тем же
изменением, которым чинят У24.

## Два наблюдения о `scripts/raskrutka.sh`

Прогонять его нельзя — это полная перепечатка, — но прочесть можно, и
прочитанное стоит записать.

**Наблюдение «два замера в одном файле расходятся на порядок» снято 1 сентября
2026: оба числа заменены снятыми.** Стояло: шапка называет 19 мин 58 с и
25,1 ГиБ (22 августа), собственный отказ — 5 ч 30 мин и 259 ГБ (24 августа).
Сегодня в файле стоит одна цена — 8 ч 34 мин 02 с, последний дошедший заход
печати 30 августа (`docs/reprint-ledger.tsv`), — и рядом с ней пик по трём
заходам 31 августа: 24,3 / 35,4 / 34,7 ГиБ. Замер 24 августа снят ДО починки
арены рантайма и по памяти врёт в 7,3 раза
([задача 1310](../tasks/1310-semya-sverka-zheleza.md)).

Дешёвая половина прогоняется и стоит секунды. `sh scripts/raskrutka.sh
--bystro` на этом дереве 29 августа: **0,66 с, расхождений 45.** То есть семя
отстало от исходников на сорок пять файлов, и всё, что судит о доказательствах,
сегодня судит о дереве, которого нет.

**Первое: потолок шагов, который уедет в новое семя, был ниже цены одного шага
печати. Закрыто 29–30 августа.** В `scripts/raskrutka.sh` строка 322 —
`MAX_STEPS=1400000000000`. Это число попадает прямо в напечатанный байт
(`#define FL_MAX_STEPS` в `bootstrap/flang_runtime.h`) и становится потолком
НОВОГО семени; семя перепечатано 30 августа 2026 (`52996005`), и в дереве лежит
уже оно. Цена самого дорогого шага печати измерена — 456 857 834 234 шага, — и
потолок втрое выше неё.

На день описи здесь стояло другое, и это записано нарочно: `MAX_STEPS=4000000000`
при потолке семени `1000000000`, то есть перепечатки с поднятым числом не было
ещё ни разу, а одна «Связать исходники» стоила 14,2 млрд витков — и старое
число, и новое были ниже цены ОДНОГО шага печати.

**Второе: побайтовой сверки перепечатки нет ни в одной сборочной проверке.**
Она живёт в `scripts/raskrutka.sh`, строка 573 (`cmp`), и зовётся ключом
`--check`. В `.github/workflows/` слово `raskrutka` встречается трижды: дважды
в `install-path.yml` как имя файла в списке путей и один раз в `binary.yml` как
`--bystro`. `--bystro` сверяет ВХОДЫ печати, а не её выход, и сам об этом
говорит: «Чего он не доказывает: побайтового совпадения семени с печатью».
Причина уважительная — 19 мин 58 с и 25,1 ГиБ на прогон, — но следствие надо
называть: **сегодня ничто в проверках не сверяет закоммиченное семя с тем, что
печатают исходники.**

## Ещё 569 строк JavaScript, которых не видит ни один счётчик

Обе описи — эта и `javascript-inventory.md` — считают файлы по имени: `.mjs` и
`.js`. Внутрь HTML не смотрит ни одна, а там лежит написанный руками JavaScript:

| файл | всего строк | из них JavaScript внутри `<script>` |
|---|---:|---:|
| `docs/zettel/index.html` | 851 | 396 (строки 455–850) |
| `web/wasm/demo/index.html` | 187 | 173 (строки 14–186) |
| `web/shortener/index.html` | 100 | 0 |
| `web/app/index.html` | 88 | 0 |

Две страницы из четырёх чисты, и это не случайность, а решение: их тег `<script>`
несёт только `src` на напечатанный модуль, и страница сайта об этом прямо
говорит — «строк JavaScript, написанных руками ради этой страницы, — ноль».
Проверено грепом: в обеих один тег и ноль строк внутри.

Две другие несут **569 строк**, и на счёт JavaScript они не попадают. То есть
25 527 строк из описи JavaScript — это нижняя оценка, а не полная.

Здесь они посчитаны в HTML, а не в JavaScript: делить чужую опись пополам без
её хозяина нельзя. Но назвать число надо, и оно названо.

## Что выросло за сутки и что с этим сделано 30 августа 2026

Потолок 63 снят 29 августа. К 30-му, к 13 часам, счёт стоял **72**, и работа
«Опись дерева по языкам» в `.github/workflows/binary.yml` была красной по
существу, а не по памяти: 19 с и 0,44 ГиБ, верхний ключ ответа `{"error"`.

Девять лишних названы поимённо. Ни один из них не оказался разовой считалкой:
у каждого есть страница, ссылка из дерева или незакрытая задача. Это и есть
главный итог разбора — прибыло не мусора, а рабочих приборов, и потому дёшево
долг не убирается.

| файл | строк | заведён | решение |
|---|---:|---|---|
| `scripts/build-ledger-binary.sh` | 130 | 30 авг, `42c641be` | **не долг**: третий путь сборки компилятора |
| `scripts/binary-origin.sh` | 549 | 29 авг, `b10bff9a` | **не долг**: спрашивает у двоичного о нём самом |
| `scripts/overlong-string-guard.sh` | 327 | 29 авг, `f36239c0` | **не долг**: обязан отвечать, когда семя не собирается |
| `scripts/resource-plan.py` | 975 | 30 авг, `7544e149` | **вынесен из дерева 30 авг**: живёт в `/srv/work/resource-plan.py` — прибор машины и смены, а не языка |
| `scripts/published-vs-tree.sh` | 796 | 31 авг, `03fb4060`+Ч72 | **долг**: переводим; работа `published` в CI нарочно идёт без собранного двоичного |
| `docs/tools/binder-rule-reach.py` | 242 | 30 авг, `0e3b8918` | **долг**: прибор незакрытой задачи 9952 |
| `docs/tools/binder-goal-share.py` | 169 | 30 авг, `0e3b8918` | **долг**: прибор незакрытой задачи 9952 |
| `scripts/take-proof-ledger.sh` | 106 | 30 авг, `42c641be` | **долг**: переводим — это оснастка, а не сборка |
| `scripts/proved-share-of-a-file.py` | 88 | 29 авг, `0a9ea2d1` | **долг**: переводим; накладывается на `flang/scripts/per-file-proof-share.py` |

Три первых вычеркнуты из долга разбором, а не подъёмом потолка: **72 → 69**.
Четвёртым ушёл `resource-plan.py` — не разбором и не переводом, а ВЫНОСОМ из
дерева: **69 → 68**. Потолок остался на 63, и работа остаётся красной на пять
файлов.

### Почему эти три — не долг, а остальные шесть — долг

Основание дозволено ровно одно, и оно записано в самой описи: **круг**. Файл не
долг, если он судит или собирает то, чем его самого пришлось бы исполнять.

* `build-ledger-binary.sh` собирает компилятор (семя дословно + рантайм дерева).
  Написать сборку компилятора программой, которую исполняет компилятор, нельзя:
  на этом шаге его ещё нет.
* `binary-origin.sh` спрашивает у ДВОИЧНОГО, из этого ли он семени. Двоичный,
  несущий чужой код, одинаково способен и нести его, и сказать, что не несёт.
* `overlong-string-guard.sh` спрашивает, собирается ли напечатанное семя своим же
  напечатанным Makefile. Он обязан отвечать ИМЕННО ТОГДА, когда семя не
  собирается, — то есть когда исполнить программу на flang нечем.
* `flang/proof/чекер/**` отвечает на вопрос «сходится ли выданная ядром запись
  доказательства с программой». Напиши чекер на flang — и проверять
  доказательства станет та самая программа, о доказательствах которой
  спрашивают; собери его `bootstrap/Makefile` — и его сборкой будет распоряжаться
  напечатанный подсудимым файл, который вдобавок сверяется байт в байт. Довод
  покрывает ровно этот каталог: оснастка вокруг чекера, которой круг не мешает,
  долгом остаётся.

Шести остальным круг не мешает. `published-vs-tree.sh` дешевле и не требует
двоичного — но «дешевле» доводом не бывает: под него подпадает любой сторож в
дереве. `binder-*.py` и `proved-share-of-a-file.py` читают вывод доказателя,
а чтение вывода — это арифметика над текстом, и её исполнителем может быть кто
угодно. `take-proof-ledger.sh` гоняет чужой двоичный, но гонять процессы `flang
io` умеет. `resource-plan.py` о языке не говорит вовсе — он считает по истории
git скорость, цену и потери; долгом дерева он от этого быть не перестаёт.

### Чем эти шесть закрываются

Порядок — по цене за вычеркнутый файл, дешёвое сверху:

1. `proved-share-of-a-file.py` (88) вместе с `flang/scripts/per-file-proof-share.py`
   (138): один прибор на flang вместо двух на Python — **минус два файла разом**.
   Оба под задачами 3022 и 1729, обе в работе.
2. `take-proof-ledger.sh` (106) — оснастка снятия ведомости, переводима как есть.
3. `binder-goal-share.py` + `binder-rule-reach.py` (411) — их судьбу решает
   задача 9952: пока правило не написано, приборы нужны ей самой.
4. `published-vs-tree.sh` (796 на 31 августа 2026) — перевод потянет за собой сборку двоичного в
   работе `published`, которая сегодня укладывается в пять минут без него.
5. `resource-plan.py` (975) — **вынесен 30 августа 2026**, решением владельца,
   в `/srv/work/resource-plan.py`: туда же, где живёт сторож памяти машины
   (`/srv/work/storozh-pamyati.sh`), и по той же причине — прибор говорит о
   машине и о смене, а не о языке.

   Вынос сменил строку, которую набирает владелец. Было
   `python3 scripts/resource-plan.py`, стало — **стоя в своём дереве**:

   ```sh
   python3 /srv/work/resource-plan.py                      # как есть сейчас
   python3 /srv/work/resource-plan.py --out docs/resource-plan.md
   python3 /srv/work/resource-plan.py --repo <путь>        # стоя не в дереве
   ```

   Дерево берётся из текущего каталога: прежде программа лежала в дереве и
   знала его собой, теперь не лежит. Каталог без `tasks/` она отвергает вслух,
   а не считает по нему молча. Отчёт остался в дереве —
   [`docs/resource-plan.md`](resource-plan.md), — и в его шапке напечатано, где
   искать программу.

## Как держать опись честной

Проверка не сверяет число строк — оно меняется каждой правкой, и такая сверка
краснела бы на всём подряд, а жали бы её не глядя. Сверяется **число файлов
долга**, и только сверху: потолок стоит на 63, долгу разрешено убывать и
запрещено расти. Новый файл на оболочке или на Python остановит работу в тот
же день, а не через месяц на чьей-то описи.

Опустить потолок обязан каждый, кто перевёл файл: число живёт в функции
«Потолок долга» в `scripts/tree-inventory.flang` и там же объяснено.

**Новый довод не-долга заводится только разбором, и разбор дороже перевода.**
Дозволенное основание одно — круг: файл судит или собирает то, чем его самого
пришлось бы исполнять. «Мне так дешевле» доводом не бывает: под него подпадает
любой сторож дерева, и храповик перестаёт держать. Довод пишется в трёх местах
сразу — в шапке самого файла, в функции «Довод не-долга» и здесь, — и каждое из
трёх называет причину словами, а не ссылкой на два других.

**Сторож умеет краснеть, и это проверено, а не обещано.** Положенный в
`scripts/` пустой файл на оболочке даёт код 1 и строку «ДОЛГ ВЫРОС: файлов 64
при потолке 63. Новый файл вне flang заведён и не разобран»; после уборки файла
код снова 0. Считается при этом и НЕ ДОБАВЛЕННОЕ в git
(`ls-files --cached --others --exclude-standard`): иначе новый файл ждал бы
своего `git add`, а до него сторож молчал бы.
