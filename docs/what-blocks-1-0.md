# Что мешает 1.0

Список того, что обязано быть сделано до номера 1.0, и того, что до него делать
не обязано. Под каждым числом стоит команда, которая его печатает; ни одно число
здесь не взято из памяти.

**Снято 23 августа 2026, дополнено 24 августа. Перемерено 31 августа 2026.**
Версия — **0.6.2** (`flang --version` и `package.json` согласны; проверено на
стволе `main` `03fb4060`).

**Что сделано 31 августа.** Три числа этой редакции сняты заново и оказались
неверными (отставание семени, цена перепечатки, размер завышения счёта);
одно утверждение — «ноль аксиом» — опровергнуто; два пункта, стоявшие как
долг, закрылись сами (2.2-бис и пример с ключом `--предел-шагов`); добавлен
пункт 2.4, которого здесь не было, потому что мерить его было нечем. Правило
файла не менялось: **число без команды или без имени снявшей его ячейки сюда
не вписывается.**

**Дополнено вечером 31 августа (Ч139): восемь находок вечерних прогонов.** Три из
них — новые пункты «обязательно до 1.0»: независимый checker пропускал ложь до
`exit 0` (2.5), derivation trace не пишет никто (2.6), десять заплат ядра не
влить из-за одного занятого имени типа (2.7). **Пунктов «обязательно» стало
семь.** Остальные пять уточнили то, что уже стояло: ещё один случай гонки семени
(2.1), доказанных «ни на чём» ноль на 208 файлах (4.2), кругооборот печати
(4.4), TCB одной линейкой и число `5466`, за которым не стояло замера (раздел 6),
и проверки, зелёные по построению (раздел 6). Порядок работ в разделе 5
пересмотрен: впереди встал 2.7, потому что он держит и 2.5, и 2.6.

⚠ **Где что лежит.** Часть приборов, названных ниже, в стволе ещё НЕТ — они в
клонах ячеек и ждут слияния. Столбец «в стволе» больше НЕ ПИШЕТСЯ РУКАМИ: его
печатает и сверяет с деревом сторож, и расхождение красит.

```sh
sh scripts/what-blocks-inventory-guard.sh --print   # напечатать столбец по дереву
sh scripts/what-blocks-inventory-guard.sh           # сверить записанное с деревом
```

| прибор | в стволе | где сегодня |
|---|---|---|
| `docs/reprint-cost.md`, `docs/reprint-ledger.tsv` | есть | — |
| `docs/kernel-axioms-list.md`, `flang/scripts/kernel-axioms.flang` | нет | клон `B4-spisok/stvol` |
| `sh scripts/raskrutka.sh --imena` | есть | коммит `4d0dcb7b` |
| `flang/proof/чекер/` (чекер на C) | есть | коммит `e8380e2d` |
| починка checker'а `+48 LOC` (2.5) | не файл | заплата `Ч126-vernut-g2/vernut-g2.diff` |
| печать derivation trace `+194 LOC` (2.6) | не файл | черновики Ч70, Ч91, Ч71 |

Ряд, который не называет ни одного пути дерева, помечен словами «не файл»:
вердикт «есть/нет» о работе, а не о файле, проверить нечем, и писать его сюда
нельзя. Команда, которой сегодня негде выполниться, — всё ещё команда, но врать
про это нельзя.

**Почему столбец отняли у руки (Ч158, 1 сентября 2026).** Эта таблица лгала в
минуту собственного коммита. Ряды про `scripts/raskrutka.sh --imena` и про
`flang/proof/чекер/` стояли со словом «нет», а оба прибора легли в ствол за 7 и
за 11 минут ДО неё:

```sh
git -c core.quotepath=false log -1 --format='%ad' --date=iso e8380e2d  # 2026-08-31 20:04:02  чекер
git -c core.quotepath=false log -1 --format='%ad' --date=iso 4d0dcb7b  # 2026-08-31 20:08:21  --imena
git -c core.quotepath=false log -1 --format='%ad' --date=iso 86494646  # 2026-08-31 20:15:36  сама таблица
git -c core.quotepath=false show 86494646:docs/what-blocks-1-0.md | sed -n '27,28p'
#   → | `sh scripts/raskrutka.sh --imena` | нет | `Ч59-imena/stvol`, коммит `907c27af` |
#   → | `flang/proof/чекер/` (чекер на C) | нет | `Ч63-chekker-v-derevo/clon` |
```

Никто не ошибся в замере: замер был снят раньше, чем написан ряд, а между
замером и коммитом дерево ушло. Ровно поэтому запись, которую можно вычислить,
и не должна лежать записанной.

**Два ряда перемерены вечером 31 августа (Ч139), и оба поменялись на «есть».**
Ствол за день ушёл от `03fb4060`, по которому мерился весь остальной файл, на
17 коммитов вперёд — на `fbc56541`:

```sh
git -c core.quotepath=false log --oneline 03fb4060..main | wc -l          # → 17
git -c core.quotepath=false ls-tree -r --name-only main -- 'flang/proof/чекер' | wc -l   # → 137
git -c core.quotepath=false log --oneline --diff-filter=A -- 'flang/proof/чекер/Makefile'
#   → e8380e2d feat(чекер): независимый чекер на C, его пробы и рецепт сборки лежат в дереве
wc -l flang/proof/чекер/сверщик.c                                          # → 1566
LC_ALL=C.UTF-8 /usr/bin/grep -a -c -- '--imena' scripts/raskrutka.sh       # → 11
```

Те же 17 коммитов открыты как PR №4 (`digitable-lol/flang`, ветка
`a/svod-yacheek-31avg`). Что PR существует — видно командой; что он `MERGEABLE` —
названо владельцем, проверить это отсюда нечем, `gh` под нашим пользователем не
ходит:

```sh
git -c core.quotepath=false ls-remote origin | /usr/bin/grep -a 'pull/4'
#   → c2dbcb25...  refs/pull/4/head
```

**Все числа ниже, снятые «на стволе `03fb4060`», от этого не портятся** — файлы,
которые они мерили, в этих 17 коммитах не менялись; но два прибора, про которые
в таблице стояло «в стволе нет», теперь в стволе есть, и это меняет раздел 5.

---

## Почему этот файл переписан целиком, а не поправлен

Предыдущая редакция снята 18 августа и мерила **дерево, которого больше нет**.
Она ссылалась на `flang/src/lexer.mjs`, `flang/src/proofterm.mjs`,
`flang/src/lockfile.mjs`, `flang/bin/flang.mjs` — то есть на реализацию языка на
JavaScript.

```sh
ls flang/src/*.mjs | wc -l   # → 0
```

**Компилятор теперь один, и написан он на flang** — 103 875 строк в
`flang/self/**`, печатает сам себя, собирается одним `make -C bootstrap` без
Node. Всё, что старая редакция мерила по JS-исходникам, надо мерить заново по
другим файлам, и результаты не переносятся.

Поэтому ниже — новый замер, а не правка старого. Что я перемерил сам, названо;
что не перемерил — тоже названо, и таких пунктов немного.

---

## 0. Обещание совместимости — по-прежнему первое

Номер 1.0 — это не «стало хорошо». Это **обещание не ломать названное**. Ниже
формулировка, обновлённая под нынешнее дерево.

### 1.0 обещает не ломать в пределах 1.x

| Что | Чем измеряется сегодня | Почему это в обещании |
|---|---|---|
| **Ключевые слова и форма объявления** на русской и английской поверхностях | `flang/self/lexer.flang`; сверка поверхностей | это буквально текст программы пользователя |
| **Коды диагностик `FLANG_*`** — код не меняет смысл и не исчезает | `grep -rhoE 'FLANG_[A-Z_]+' flang/ bootstrap/*.c \| sort -u \| wc -l` → **239** | коды читают скрипты и редакторы |
| **Замкнутое множество отказов процесса** — из него не убирают вид | `flang/self/failures.flang` | на нём держится рассуждение об отказоустойчивости |
| **Замкнутый словарь поручений ввода-вывода** | `flang/self/parser.flang` | «неизвестное поручение» означает расхождение хозяина со словарём, а не нехватку языка |
| **Смысл слова `тотальная`** — «завершится или честно откажет» | `flang/self/SPEC.md` | если это тихо изменить, все доказательства пользователя сменят смысл |
| **Каждая аксиома ядра названа и обоснована, новая — не заводится молча** | `flang/scripts/kernel-axioms.flang`; список — `docs/kernel-axioms-list.md` (28 аксиом, 7 переадресовок доверия; отчёт B4, 31 авг 2026) | доказательство, опирающееся на непоимённую аксиому, стоит столько же, сколько эта аксиома |
| **Четыре слова отчёта о доказательствах** — не смешиваются и не переименовываются | `flang check <файл> --proof` | читатель обязан отличать доказанное от прогнанного |
| **JSON командной строки** — имена полей и коды возврата | `flang/self/cli.flang` | это то, во что упираются чужие скрипты |
| **Печать в C собирается системным `cc -std=c99`** без Node | `make -C bootstrap` | единственный путь установки, не требующий нас |

**Слов отчёта стало четыре, а не три, и это изменение 23 августа.** К «доказано»,
«сетка» и «объявлено, не доказано» добавилось **«доказано ПРИ УСЛОВИИ»** —
утверждение, чей вывод верен, но опёрся на посылку, которую ядро само не
доказало. Раньше такие попадали в общий счёт доказанных. Замер 23 августа по
библиотеке: таких «несколько процентов от всего доказанного», и в отдельных
файлах до девяти штук. **Перемерено 31 августа: 2,33 %** — 100 утверждений из
4300, которые двоичный со старым семенем числит доказанными (замер Ч45, счёт
Ч54; разбор — в 2.3).

Это ровно тот случай, ради которого обещание и пишется: если бы 1.0 уже был
выпущен со старым счётом, поправка читалась бы как поломка обещания. Значит
формулировку надо принимать **после** того, как отчёт устоялся, а не до.

### 1.0 НЕ обещает

- **Тексты диагностик** — только коды.
- **Точные байты напечатанного кода.** Обещание поведенческое: та же программа
  даёт те же значения и те же коды отказа.
- **Поверхности эсперанто и китайская** — они неполные по построению.
- **Что «доказано» покроет больше форм утверждений.** Какое утверждение
  доказуемо в 1.4 — не предмет обещания. Правил у ядра сегодня девять видов цели,
  и их станет больше.
- **Скорость и память.**

---

## 1. Короткий ответ

**Держат 1.0 три вещи**, и ни одной из них не было в прошлой редакции, потому
что тогда их не существовало. 31 августа каждая из трёх названа числом, и все
три числа этой редакции оказались неверными:

1. **Напечатанное семя отстаёт от исходников на 29 функций ядра решений.**
   Компилятор в дереве и его исходники — разные программы, и расхождение
   названо поимённо: `sh scripts/raskrutka.sh --imena` (ячейка Ч59, 0,35 с).
   Тот же список побайтово совпал с независимым счётом ячейки Ч54, снятым
   другим приёмом. **Причина — не оплошность, а гонка:** печать идёт 8,5 часа,
   входы печати трогают с медианой в десять минут, значит семя не МОЖЕТ быть
   свежим. Подробно — 2.1.
2. **Отчёт сменил смысл, и завышение счёта измерено: 2,33 %.** Из 4300
   утверждений, которые двоичный со старым семенем числит доказанными, 100 —
   «доказано ПРИ УСЛОВИИ» (замер Ч45 по 120 файлам, счёт Ч54). Подробно — 2.3.
3. **Перепечатка стоит 8 ч 34 мин, а не два часа; память — 25…37 ГБ, а не
   100.** И за 31 августа три захода подряд семени не дали: два убиты сигналом
   при свободной машине, причина не найдена, третий идёт. Подробно — 2.2.

**И четвёртое, чего в этом списке не было, потому что мерить было нечем:**
доказанное проверено НЕ ядром на **1,25 %** (ячейка Ч56). Пока это число такое,
всякая доля доказанного — слово того же ядра, которое доказывает. Подробно —
2.4.

**И ещё три, найденные вечером 31 августа. Первая держит две другие:**

5. **Десять заплат ядра не влить: занято одно имя типа.** Строка
   `использует «SHA-256»` втягивает `flang/stdlib/sha256.flang` в замыкание
   компилятора, где «Сбор слов» уже объявлен (`flang/self/emit-c.flang:454`) →
   `FLANG_DUPLICATE_NAME`. Отказ наблюдён самим компилятором на девятистрочной
   пробе (Ч135) и проверен на двух разных линиях ядра (Ч123). Подробно — 2.7.
6. **Независимый checker пропускал ложь до `exit 0`.** Три adversarial-кейса из
   пятнадцати приняты как «проверено самостоятельно» на файлах, которые
   `flang check` отвергает поимённым контрпримером (замер Ч119). Починка
   `+48 LOC` написана, измерена и в дерево не сдана. Подробно — 2.5.
7. **Derivation trace не пишет никто** — ноль вхождений и в исходниках, и в
   семени, и в обоих bootstrap binary; из 43 proof object'ов, которые компилятор
   печатает сегодня, ходов нет ни в одном. Подробно — 2.6.

Итого пунктов «обязательно до 1.0» — семь, и первым в работе стоит не самый
дорогой, а тот, что держит остальные: 2.7 (раздел 5).

**Не держат 1.0**: пакеты, фронтенд, регулярные выражения — последние **уже
сделаны**, см. раздел 3. И 31 августа закрылись ещё два пункта, стоявшие здесь
в «обязательно»: линейный поиск описания (2.2-бис) и пример с ключом
`--предел-шагов` (2.1). Оба помечены на месте, каждый — командой, которая это
показывает.

---

## 2. Обязательно до 1.0

### 2.1. Семя отстаёт от исходников — 23 августа проверка это ЗНАЛА, 31 августа молчит

Самый тяжёлый пункт, и он же самый простой для показа. В дереве есть сторож,
который отказывается судить:

```sh
sh scripts/seed-freshness.sh
```

```
ОТКАЗЫВАЮСЬ СУДИТЬ О ДОКАЗАТЕЛЬСТВАХ

семя отстало от исходников на 38 файлов, ведомость показывает состояние,
которого нет; перепечатайте `sh scripts/raskrutka.sh`
```

**Что это значит на деле.** Правка компилятора попадает в исходники и не
попадает в работающую программу, пока кто-то не прогонит перепечатку. За 21–22
августа так трижды за сутки лежали в стволе неработающими правило ядра,
четвёртый вердикт и два поручения ввода-вывода.

Показать это можно пальцем. Ключ `--предел-шагов` объявлен в
`flang/self/cli.flang` шесть раз:

```sh
grep -c 'предел-шагов' flang/self/cli.flang   # → 6
grep -c 'предел-шагов' bootstrap/*.c          # → 0
```

Собранный компилятор про этот ключ не знает и отвечает «непонятный ключ». Из-за
этого 23 августа два независимых агента потеряли по часу: отчёт о доказательствах
для `json.flang` и `aes.flang` не печатается вовсе, и каждому пришлось собирать
себе отдельный двоичный.

**Этот пример 31 августа закрылся, и палец теперь показывает не туда.** Те же
две команды на стволе `main` `03fb4060` (клон `Ч73-do-odin-nol/clon`):

```sh
grep -c 'предел-шагов' flang/self/cli.flang   # → 8   (было 6)
grep -c 'предел-шагов' bootstrap/*.c          # → 9   (было 0)
```

Девять — это `flang_cli.c` 1, `flang_runtime.c` 1, `flang_repl.c` 7,
`compiler_flang.c` 0. Ключ доехал до семени; отставание никуда не делось, но
показывать его надо другим числом — тем, что ниже.

#### Перемерено 31 августа: отставание — 29 функций ядра, и это гонка

Сторож `seed-freshness.sh` считает ФАЙЛЫ входов и отвечает на вопрос «те ли
входы я печатал». На вопрос «то ли ЯДРО в моём двоичном» он не отвечает вовсе.
С 31 августа есть сверка, которая на него отвечает: имена функций трёх файлов
ядра решений (`proofterm.flang`, `proof-kernel.flang`, `proof-initial.flang`)
против блок-комментариев ` * Функция flang «Имя».` в `bootstrap/compiler_flang.c`.

```sh
sh scripts/raskrutka.sh --imena        # 0,35 с, 8 МиБ
```

Три исхода, а не два: сошлось / ОТСТАЛО (в исходниках есть, в семени нет) /
ЛИШНЕЕ (в семени есть, в нынешнем ядре уже нет). На дереве `ee3465cd` она
называет **29 имён поимённо**, лишних — ноль. Замер ячейки Ч59; тот же список
побайтово совпал с независимым счётом ячейки Ч54, снятым другим приёмом
(множества имён через `awk` и `sort -u`). Первые пять из 29: «Величина полного
порядка», «Есть порядок через середину», «Замыкание середин», «Зеркало цели»,
«Мера в счёт».

Значит двоичный, которым мы судим о доказательствах, **не знает 29 правил,
которые в исходниках есть**, и судит по правилам, которых там уже нет.

**И самое неприятное: сторож при этом зелёный.** Проверено 31 августа в клоне
ствола `main` `03fb4060`:

```sh
FLANG_BINARY_UNKNOWN_OK=1 sh scripts/seed-freshness.sh   # → код 0, молча
```

Ни одного расхождения; ключ выставлен только потому, что в свежем клоне ещё нет
собранного двоичного, и он снимает ВТОРУЮ сверку (двоичный против семени), а не
ту, что про входы. Сверка входов на этом дереве честно зелена. Три файла ядра и
`bootstrap/compiler_flang.c` на `03fb4060` и на дереве замера Ч59 `ee3465cd`
**побайтово одинаковы** (`git diff --stat 03fb4060 ee3465cd -- <четыре файла>` —
пусто), так что 29 относятся и к стволу.

**Отпечаток входов и сверка тела отвечают на другой вопрос.** Первый — «те ли
входы я печатал», вторая — «то ли соберётся». Ни один не отвечает «то ли ЯДРО
в моём двоичном». Пример 23 августа с ключом `--предел-шагов` был из первого
класса и потому закрылся; отставание ядра — из третьего, и оно осталось.

**Причина — не оплошность, а гонка, и это меняет вид работы.** Печать семени
идёт 8 ч 34 мин (`docs/reprint-cost.md`), а входы печати трогают с медианой в
десять минут (там же). Пока заход считает, ствол уходит вперёд на десятки
коммитов, и семя, которое этот заход напечатает, устареет раньше, чем заход
кончится. Свежесть семени здесь **недостижима дисциплиной**: это гонка, а не
забывчивость.

**И отсюда — почему проверка «коммит печати — предок HEAD» бесполезна.** Она
зелена по построению: коммит, из которого печатали, всегда предок того, куда
ствол успел уйти. Она пропускает гонку не по недосмотру, а по устройству.
Проверять надо не родство коммитов, а имена (`--imena`) и тело (`--telo`).

**Что нужно до 1.0.** Не «перепечатать один раз», а сделать так, чтобы отставание
не копилось: перепечатка в CI на каждое слияние, трогающее `flang/self/**`, либо
принуждение сторожем на входе в ствол. **Дополнено 31 августа:** одной
перепечаткой в CI это не закрывается, пока печать длиннее промежутка между
правками. К принуждению нужно окно — запрет трогать `flang/self/**`, пока идёт
заход, — либо сверка имён на входе в ствол, которая красна ровно тогда, когда
двоичный судит не тем ядром.

#### Ещё один случай той же гонки, снятый вечером 31 августа

Отказ печатать ведомость у **35 файлов**, который держали за свойство языка,
оказался недоделкой семени. В дереве это исправлено 30 августа коммитом
`c143a3c9`; семя `c15f669f` закоммичено в тот же день **на 4 ч 46 мин позже** —
и правку не забрало. Причина видна из отпечатка: печатали не с того дерева.

```sh
git -c core.quotepath=false log -1 --format='%h %aI' c143a3c9   # → 2026-08-30T17:32:28Z
git -c core.quotepath=false log -1 --format='%h %aI' c15f669f   # → 2026-08-30T22:18:53Z
LC_ALL=C.UTF-8 /usr/bin/grep -a -n 'коммит' scripts/otpechatok-semeni | head -1
#   → коммит 1eb168da…                       (дерево 30 авг 12:30:03Z)
git -c core.quotepath=false merge-base --is-ancestor c143a3c9 1eb168da; echo $?   # → 1
```

Последняя команда и есть доказательство: правка **не предок** того дерева, с
которого печатали. Ч124 собрала двоичный с этой одной строкой — **все 35 дали
ведомость: 787 утверждений, 727 доказанных**, и стоило это 195 секунд на 34 файла
из 35 (замер Ч124, 31 августа; числа — `Ч124-slepye-69/ved-otkaz35-svod.json`).

Это тот же пункт 2.1, но с другой стороны: не «двоичный не знает 29 правил», а
«двоичный молчит там, где дерево уже отвечает», и молчание читалось как свойство
языка целых сутки.

### 2.2. Перепечатка стоит 8 ч 34 мин, а не два часа — и это ЛЕЧИТСЯ

**Заголовок исправлен 31 августа: «два часа и до 100 ГБ» — число прошлой
недели, и по обеим осям неверное.** Обе оси разошлись, и в разные стороны:
время выросло вчетверо, память упала. Ведомость всех замеров лежит в дереве —
`docs/reprint-ledger.tsv`, разбор — `docs/reprint-cost.md`.

| заход | время | пик памяти | итог |
|---|---:|---:|---|
| 29–30 авг, ствол | 7 ч 50 мин 45 с | 27,7 ГиБ | код 0 |
| 30 авг, ствол — **последний дошедший** | **8 ч 34 мин 02 с** | не замерен | код 0, семя `c15f669f` |
| 31 авг, заход 1 (наш) | 9 ч 19 мин 12 с | 24,3 ГиБ | **убит сигналом, `Exit status: 143`** |
| 31 авг, заход 2 (наш) | 3 ч 24 мин 40 с | 35,4 ГиБ | **убит сигналом, `Exit status: 143`** |
| 31 авг, заход 3 (наш) | идёт | — | — |

Строки ствола — из `docs/reprint-ledger.tsv`. Наши три захода сняты
`/usr/bin/time -v`; журналы `/srv/tmp/relizy/perepechatka-zahod1-ubita.log` и
`…zahod2-ubita.log`, числа — поля `Elapsed (wall clock)`, `Maximum resident set
size` (25 453 544 и 37 140 416 КиБ) и `Exit status`.

Три вывода, и каждый меняет планирование:

- **Память — 24…36 ГиБ (25…37 ГБ), а не 100 и не 259.** «259 ГБ» — замер
  24 августа; с тех пор пик упал вдевятеро (`docs/reprint-cost.md`), и
  планировать по нему нельзя. Мешает теперь не память, а длина.
- **Два захода подряд убиты сигналом при свободной машине.** Пределы не
  исчерпаны, журнал чист, замечаний нет. **Причина не найдена.** Пока она не
  найдена, всякое «перепечатка стоит N часов» описывает удачный заход, а не
  работу: за сутки три захода — два убитых и один незаконченный.
- **Разброс сам по себе — число:** 3 ч 25 мин и 9 ч 19 мин на одном и том же
  дереве и коммите. Одним числом эту цену называть нельзя.

Ниже — прежний замер 23 августа. Он верен на свой день и объясняет, ОТКУДА
цена берётся, но как оценка работы устарел.

Пункт 2.1 не чинится дисциплиной, пока перепечатка стоит столько. Замер
23 августа, две печати одного и того же дерева:

| что печаталось | время | пик памяти | итог |
|---|---|---|---|
| с нашими 5952 обещаниями в исходниках компилятора | 1 ч 53 мин | 85 ГБ | снята сигналом |
| те же исходники с вырезанными обещаниями | **19 мин 45 с** | **23 ГБ** | напечаталась, код 0 |

**Вшестеро по времени и вчетверо по памяти** — это цена того, что компилятор
проверяет собственные постусловия, пока печатает сам себя.

И у неё есть прямой рычаг, названный самой перепечаткой в её же журнале:

```
проверок при работе снято 3: постусловие доказано ядром обо всех входах и
проверено примерами функции — в напечатанный код оно не едет.
```

**Доказанное обещание не превращается в проверку при работе.** То есть работа по
доказательству обещаний компилятора — это не только счёт, это прямое удешевление
перепечатки. За один вечер 23 августа так снято больше пятисот проверок в
`parser`, `types`, `failures`, `lsp`, `interpret`, `emit-c`, `emit-python`,
`emit-csharp`, `emit-java`, `emit-go`.

**И доказательства бывают дешевле или дороже — это тоже замерено.** На
`aes.flang`, где двадцать четыре тела-`разбор` разглажены до чтения полей
точкой:

```
до:    доказано 140 (из них индукцией 66) (из них без теоремы  65)
после: доказано 177 (из них индукцией 32) (из них без теоремы 136)
```

Индукции вдвое меньше, «без теоремы» вдвое больше. Плоское тело ядро берёт за
один ход вместо раскрутки по типу. Дорогие доказательства и есть то, из-за чего
печать стоит часы.

### 2.2-бис. НАСТОЯЩАЯ причина, найденная 24 августа — ПОЧИНЕНА, проверено 31 августа

Три попытки перепечатки провалились. Первые две сняты руками — машина была
перегружена, и тот же файл считался 9 минут при загрузке 30 и 7 часов 23 минуты
при 110. Третья, на свободной машине и с измеренным пределом 4 млрд, упала сама
и **назвала виновника поимённо**:

```
ЗАПАС ШАГОВ НА ИСХОДЕ: «Суд ядра о программе» съел 4000000001 витков
                       из 4000000000 (100 %)
FLANG_RECURSION_LIMIT: функция «Найти описание» исчерпала лимит шагов
                       на глубине вызовов 8
flang emit: печать отменена — программа не проходит проверку, замечаний 1
```

Три часа, пик 227 ГиБ. Виновник — `flang/self/interpret.flang:810`:

```flang
тотальная функция «Найти описание»
  разбор «описания»
    случай пусто            то вариант «Нет описания»
    случай голова и хвост   то если («первое».«имя») равен «имя»
                                 то … иначе «Найти описание» от «прочие» и «имя»
```

**Вычислитель ищет функцию по имени ПЕРЕБОРОМ списка.** Зовётся из трёх горячих
мест (`interpret.flang:1257, 1348, 2619`) — на каждое обращение к имени. Функций
в компиляторе около двух тысяч, и ядро доказательств гоняет вычислитель
постоянно. **`Словарь` в `interpret.flang` не ввезён вовсе**, хотя
`flang/stdlib/hashmap.flang` есть и доказан.

Это объясняет и часы, и гигабайты лучше прежних объяснений, и объясняет ещё одно:
почему рост дерева на 464 обещания за сутки сделал перепечатку невозможной при
пределе, которого раньше хватало. Предупреждение самой печати говорит то же:
«Рост дерева в полтора раза стоит вдвое с лишним».

**До 1.0 это чинится, а не обходится подкруткой предела.**

#### ЗАКРЫТО (проверено 31 августа на стволе `03fb4060`)

Починено, и не подкруткой предела. `«Найти описание»` теперь принимает не
список, а хеш-словарь, уложенный ОДИН раз в `«Собрать программу вычислителя»`:

```sh
sed -n '945,947p' flang/self/interpret.flang
# тотальная функция «Найти описание»
#   принимает «словарь»: «Узел хеша» от «Может быть описание», «имя»: строка
grep -c 'Функция flang «Словарь описаний»' bootstrap/compiler_flang.c   # → 1
```

Вторая команда важнее первой: **словарь доехал до семени**, то есть работает в
двоичном, а не только в исходниках, — редкий случай для этого дерева.

Замер лежит в самом коде (`interpret.flang`, шапка функции), снят двумя
двоичными из ОДНОГО семени со счётчиками:

| файл | витков в поиске, перебор | витков в поиске, словарь |
|---|---:|---:|
| `self/lexer` | 817 611 | 104 165 |
| `self/interpret` | 38 108 036 | 12 009 566 |
| `self/proof-kernel` | 873 999 841 | 14 698 772 |

87 % работы проверки ушло. Стена, однако, не исчезла, а сменилась: с ключом
`--proof` на `proof-kernel.flang` теперь не хватает не витков, а 60 ГиБ памяти.
Словарь эту стену не создал, а открыл — перебор к ней не успевал дойти.

**Что отсюда следует для 1.0.** Пункт из «обязательно» вычёркивается. Но
предложение «доказать обещания компилятора» (2.2) им не отменяется, и вот
почему: 87 % — это витки ПРОВЕРКИ, а печать после починки дешевле не стала.
По ведомости `docs/reprint-ledger.tsv` дошедшие заходы идут 5 ч 30 мин
(24 авг), 7 ч 50 мин (29–30 авг), 8 ч 34 мин (30 авг) — то есть вверх. Сколько
из снятых витков приходилось на печать, а не на проверку, **не замерено**, и
приписывать словарю падение пика памяти (259 → 27,7 ГиБ) без такого замера
нельзя: между этими заходами менялось не одно.

### 2.3. Счёт доказанного надо пересчитать четырьмя словами, а не тремя

До 23 августа отчёт различал три исхода. Теперь четыре, и четвёртый —
**«доказано ПРИ УСЛОВИИ»** — вычитается из того, что раньше называлось
доказанным.

Пример из `numtree.flang`, дословно из отчёта:

```
постусловие «сортировка деревом не теряет и не добавляет чисел» — доказано ПРИ
УСЛОВИИ: шаг «по свойству» засчитал фактом 1 посылку, которых ядро не доказало
```

Старый двоичный считал это утверждение доказанным. Новый — нет.

#### Перемерено 31 августа: завышение равно 2,33 %

23 августа здесь стояло «несколько процентов». Теперь есть число. Ячейка Ч45
сняла `flang check --proof --json` по **120 файлам** (`flang/stdlib`,
`flang/proof/examples`, `flang/test/fixtures`) двоичным, собранным из семени
`c15f669f` — тем, который четвёртый вердикт знает. Сложение поля
`totals.claims` по всем 120 ведомостям:

| вердикт | утверждений |
|---|---:|
| доказано (безусловно, включая индукцию) | 4200 |
| **доказано ПРИ УСЛОВИИ** | **100** |
| сетка | 1917 |
| объявлено, не доказано | 139 |
| **всего** | **6356** |

```sh
# каталог замера Ч45: shir-posle/
grep -ho '"verdict": *"proved-conditional"' *.json | wc -l   # → 100
```

Двоичный со старым семенем этого вердикта не знает вовсе — его в семени нет ни
одного вхождения — и зовёт все 4300 просто доказанными (счёт ячейки Ч54 по тем
же данным, с встречной проверкой транслитерации имён по семени). Отсюда:

**Всякая доля доказанного, снятая ствольным двоичным, завышена на
100/4300 = 2,33 %.** На этой выборке честная доля — 4200/6356 = 66,1 %, а
ствольный двоичный напечатал бы 4300/6356 = 67,7 %.

Число невелико, и в этом всё дело: **опровергает главный довод языка не
величина ошибки, а то, что инструмент ошибался в свою пользу**. Публиковать
1.0 с долей, снятой таким инструментом, нельзя независимо от того, 2,33 % это
или 20 %.

**Что нужно до 1.0.** Перемерить всё дерево двоичным, который различает четыре
вердикта, и опубликовать числа. Две оговорки, и обе честные: замер Ч45 — это
120 файлов, а не всё дерево, на остальное числа нет; и всякий такой пересчёт
делается двоичным, собранным из СЕМЕНИ, то есть наследует отставание пункта 2.1
— каким бы оно ни было на день замера. Отдельно мерить, какие из 29 отставших
правил ядра меняют вердикт, никто не пробовал: этого числа нет.

---

### 2.4. Доказанное проверено НЕ ядром на 1,25 % — и печатью записи это не поднять

**Этого пункта 23 августа не было, потому что проверять было нечем:**
доказательство проверяло то же ядро, которое доказывало, и вопрос «а сколько
проверено кем-то ещё» не имел ни прибора, ни числа. С 31 августа прибор есть —
независимый чекер на C (раздел 6), — и число появилось.

**1,25 %.** Замер ячейки Ч56: взяты 86 записей доказательств, снятых ядром с
настоящих файлов корпуса; в них 359 утверждений, из которых числятся
доказанными **240**; чекер досмотрел до конца и принял кодом 0 — **три**.
Остальные получают код 3 «НЕ ПРОВЕРЕНО»: противоречий не найдено, но проверено
не всё, и непроверенное названо числом.

Рядом — второе число того же дня, и оно строже. Ячейка Ч55 добавила чекеру
требование криптографической привязки записи к программе (строка
`отпечаток256`); по тому же корпусу её чекер даёт **0 %**: ни одна из 86
записей такой строки не несёт. Числа не спорят: 1,25 % — «сколько проверено»,
0 % — «сколько проверено привязкой, которую нельзя подделать». Пока семя не
перепечатано, честно называть оба.

**Почему это не лечится видом записи.** Ч56 научила ядро писать в запись термы
и перемерила: одно утверждение перешло из кода 3 в код 0, и это всё. Счёт мест,
где чекер вынужден верить ядру на слово, не сдвинулся ни на одно — их
по-прежнему **308** на 86 записей:

| мест | что это | снимается ли термом в записи |
|---:|---|---|
| 159 | теоремы нет вовсе | **нет**: ядро закрыло цель правилом и не выписало ходов — проигрывать нечего |
| 97 | шаг обоснован примером или свойством | **нет**: держится ли обоснование, решало ядро (допущение чекера, названное им самим) |
| 52 | посылка | частично: у 23 из 52 заключение уже несётся термом |

Потолок стоит не на формате записи, а на том, **сколько ядро вообще
выписывает**. Ходы есть у **13 посылок из 74** — снято одной линейкой
1 сентября 2026 сегодняшним двоичным (задача 1303). Прежние «16 из 74» (Ч24 и
Ч60) — то же дерево с двумя неслитыми правками: 13 на стволе, 15 с правкой Ч24,
16 с обеими. Пока ядро закрывает 159 целей молча, чекеру их не проверить ни при
каком формате.

**И эти 1,25 % в работающий компилятор не приехали.** Правка ядра лежит в
исходниках `flang/self/zapis.flang`; в двоичный она попадает только
перепечаткой семени — так записала ячейка Ч55 про правку того же файла, и
перепечатка эта стоит 8,5 часа и 31 августа не дошла ни одним из трёх заходов. Пункт 2.4 упирается
в пункт 2.1, как и 2.3.

---

### 2.5. Independent checker пропускал ложь до `exit 0`

Пункт 2.4 меряет корпус прибором из раздела 6 — независимым checker'ом на C.
Вечером 31 августа этот прибор проверили тем, чего в его батареях не было, и он
пропустил ложь.

**Три adversarial-кейса из пятнадцати получили `exit 0` «ПРОВЕРЕНО
САМОСТОЯТЕЛЬНО»** на файлах, которые `flang check` отвергает поимённым
контрпримером; запись при этом утверждает ровно то ложное постусловие и печатает
«вердикт доказано». Замер Ч119, 31 августа; повторить — `bash
/srv/tmp/dokazuemyy/Ч119-schel-D1/rabota/povtorit-ataku.sh`, протокол попыток —
`Ч119-schel-D1/ПОПЫТКИ.tsv` (15 попыток: С1 — 6, С2 — 2, С3 — 2, С4 — 3, С5 — 2).

Счёт удачных попыток зависит от того, что считать одним кейсом, и это надо
называть честно: **сильных нулей три**, всего нулей на тех же пятнадцати
попытках — **шесть**, а Ч126, пересчитав по-своему, называет **пять**. Общее у
всех трёх счётов одно: ноль достижим. Прежние 82 кейса батареи checker
по-прежнему отвергает — он падает на новом разряде, а не разучился.

**Механизм один: checker читает исходник ИНАЧЕ, ЧЕМ ЯЗЫК.** Мест пять, и все
пять в одном файле — `Ч119-schel-D1/rabota/pribor/sverschik.c`, 2587 строк:

- `sverschik.c:2317–2319` — вхождение подстроки без границ слова: строка
  «второе: нат», спрятанная в хвостовом примечании, засчитывается за объявление
  типа, и правило «неотрицательность по построению» переигрывается на выдуманном
  типе. Пример в файле даёт 2 + (−3) = −1, `flang check` говорит «нарушено
  свойство», checker — `exit 0`;
- `sverschik.c:1114` — `blok_funkcii` ищет блок функции вхождением подстроки в
  заголовок: функция-приманка с телом в 20 знаков подменяет настоящую в 19, и шаг
  засчитывается «проверенным по существу»;
- `sverschik.c:2012` и `:2019` — счётчики полноты пропускают ложное постусловие и
  ложную теорему, набранные с ДВУМЯ пробелами: обещание «запись говорит обо всей
  программе» молча не держится;
- `sverschik.c:600` (функция `hvost_dovodov`, объявлена строкой 594) — пятое
  место того же рода; его нашла не Ч119, а Ч126, когда чинила первые четыре.

Изъятие сделано в обе стороны на двух главных кейсах: убрал протащенную
подстроку — `exit 1` с поимённой бедой; вернул — файл побайтово тот же, `exit 0`.
Хвостовое примечание при этом законный flang, проверено отдельным прогоном.

**И вторая половина беды — самозаверение, и оно не в checker'е, а в оснастке.**
Checker'а зовут 38 мест, и **23 берут путь исходника из проверяемой записи**;
отпечаток третьим доводом подаёт **одно** место, и оно снимает sha256 с того же
файла, который проверяет (замер Ч126, `Ч126-vernut-g2/rabota/ZVATELI.tsv`). Из
независимой ведомости — **ноль**: такой ведомости на корпус в стволе нет вовсе.
Тот, кто правит в записи строку «исходник», правит и строку `отпечаток256` — и
код снова 0.

**Починка написана и измерена: `+48 LOC`** (код 1789 → 1837 по мерке
`python3 /srv/tmp/dokazuemyy/Ч4-merka/merka.py --narechie c <файл>`; это 2,7 %
checker'а, TCB раздела 6 не задет). На ней: все пять кейсов Ч119, доходивших до
нуля, дают `exit 1`; 82 кейса прежней батареи — ни одного нуля и ни одной
четвёрки; шесть новых кейсов другого рода — все давали ноль до и дают `exit 1`
после; **344 честных объекта — ни одного `exit 1`** (замер Ч126, повторить —
`bash /srv/tmp/dokazuemyy/Ч126-vernut-g2/rabota/povtorit-vsyo.sh`, ведомости
`BATAREYA-114.tsv`, `DOBAVKA-114.tsv`, `KORPUS-344.tsv`).

✅ **СДАНА 1 сентября 2026.** Ствольная линия выросла с 1566 строк до **2412**
тремя коммитами:

| коммит | что принёс |
|---|---|
| `89baa02f` | половина ствольного чекера, писанная ячейками Ч56, Ч71, Ч76 и Ч87 |
| `fa017ef2` | чтение исходника так же, как его читает язык — **павший гейт Г2, починка Ч126** |
| `5c7a9196` | пробы на эту половину: без них её не сторожит ничто |

То есть вторая из двух половин «что нужно до 1.0» (см. ниже) сделана: места,
читавшие исходник иначе, чем язык, починены в самой ствольной линии, а не в
чужой заплате на 2587 строк. Первая половина — звать checker с отпечатком из
своей ведомости — не сделана.

**Что осталось красным, проверено старшим 1 сентября:** семь подделок доходят до
кода 0 на ветке Ч201; **в стволе они не проходят**, но порода настоящая
(задача 1314). И у проверяльщика нет потолка размера, а он вырос на 846 строк за
один день (задача 1313).

Ниже — разбор, как беда стояла до починки, и он оставлен нарочно: заплата
`Ч126-vernut-g2/vernut-g2.diff` писалась для линии в 2587 строк, ствольная линия
другая, и что из пяти изъянов в ней было — перемерено отдельно (Ч139,
31 августа, на линии в 1566 строк):

```sh
sed -n '1351p;1358p' flang/proof/чекер/сверщик.c   # оба счётчика полноты — те же
LC_ALL=C.UTF-8 /usr/bin/grep -a -c 'blok_funkcii' flang/proof/чекер/сверщик.c   # → 0, код 1
```

То есть два изъяна из пяти в стволе были, а поиска блока функции ствольная
линия не умела вовсе. Свести две линии и перенести починку — та самая отдельная
работа, и она сделана коммитом `fa017ef2`; числа выше относятся к линии
в 1566 строк, которой в дереве больше нет.

**Оговорка Ч130, вечер того же дня, и она важнее самой починки.** Все числа выше
сняты на записях, СОБРАННЫХ СКРИПТАМИ, а не напечатанных ядром (2.6). На тех, что
ядро печатает сегодня, батарея сдувается: настоящих кейсов **44, а не 82** (33
портят строки derivation trace, каких в записи нет; у пяти нет файла записи), а
из одиннадцати кейсов, на которых Ч126 возвращала критерий, **семь перестают
вскрываться — `exit 1` превращается в `exit 3`**. Причина названа прямо: на
записях ядра непустой ноль не получает никто — ни честный, ни лживый, — и ворота,
в которые не проходит никто, отвергают ложь даром.

**Что нужно до 1.0** — две половины, обе обязательны:

1. звать checker с ожидаемым sha256 третьим доводом, беря отпечаток из СВОЕЙ
   ведомости, а не из проверяемого объекта. Новой работы ноль: довод уже есть
   (`sverschik.c:2557, 2563–2564`);
2. починить места, читающие исходник иначе, чем язык. Приём написан рядом:
   `est_term`, строка 433, сличает по пробельным краям.

И только после 2.6 эти числа перестанут быть числами на записях, которых ядро не
печатает.

---

### 2.6. Ходы вывода ядро пишет — но у 8 записей из 43, и это меряли не тем прибором

Ходы вывода (derivation trace) — то, чем независимая проверка отличается от
доверия: без них checker может сверить запись с исходником, но не может
проиграть доказательство.

⚠ **ЗДЕСЬ СТОЯЛО «В дереве их нет нигде». Это неверно, и разобрано 1 сентября
2026 (задача 1303).** Мерили строку `след ядра`, а строка хода зовётся `ход` —
две разные строки записи. Следа в дереве и правда нет ни одной; ХОДЫ ЕСТЬ.
Числа ствола, снятые одной линейкой `flang/proof/линейка-ходов.sh` (двоичный
`05b2e008…b93ee95c`, чекер `39be36a5…`, 45 примеров → 43 записи):

| | |
|---|---:|
| записей с ходами | **8 из 43** |
| утверждений с ходами | **8 из 129** |
| посылок с ходами | **13 из 74** |
| строк хода | 58 |
| строк `след ядра` | **0** |

Чекер переигрывает **13 сведений** и оставляет на слово ядра 61 посылку и
93 шага. Кодом 0 не проходит ни одна запись, и **не из-за ходов**: всем 43 не
хватает строки `отпечаток256` (заплата Ч48 приложена коммитом `38f6819e`, до
двоичного доедет перепечаткой).

Линейка написана ячейкой Ч154 (ветка `a/линейка-ходов`, коммит `96a69668`) и в
ствол не влита — числа выше берутся из задачи 1303, а не прогоном этого дерева.

Ниже — прежний замер Ч139 от 31 августа на стволе `fbc56541`, оставленный как
запись о том, ЧТО именно мерили (строку `след ядра`, а не `ход`):

```sh
LC_ALL=C.UTF-8 git -c core.quotepath=false grep -n 'след ядра' main -- '*.flang'   # → пусто, код 1
LC_ALL=C.UTF-8 /usr/bin/grep -a -c 'след ядра' bootstrap/compiler_flang.c          # → 0, код 1
LC_ALL=C.UTF-8 git -c core.quotepath=false show main:flang/self/proofterm.flang |   LC_ALL=C.UTF-8 /usr/bin/grep -a -c 'trace'                                       # → 0, код 1
ls -l bootstrap/compiler_flang.c                                                   # → 32 383 400 байт
```

Ни в одном из двух bootstrap binary СЛЕДА тоже нет (замер Ч130: `05b2e008…` —
семя нынешнего ствола, `addfc85a…` — прежнее). Вывод «печатать нечего» из этого
НЕ следует: имена в семени транслитерируются («Мера склейки» →
`compiler_flang_mera_skleyki`), и поиск русской строки в
`bootstrap/compiler_flang.c` слеп по построению. При поиске обеих форм `ход
конец` в семени **находится**.

**Ствол это про себя знает.** `flang/proof/SPEC.md:4637–4643`, «Долги, названные
прямо», п. 1:

> **Ходы никто не пишет.** Проигрыватель есть и проверен на записях, набранных
> руками; ядро таких записей не выдаёт. Писать их может только тот, кто их
> находит, — `proof-kernel.flang`, — и это работа над ядром, а не над сверщиком.

**Что из этого следует для счёта.** Из **43** proof object'ов, которые компилятор
печатает по корпусу сегодня, ходы несут восемь; checker даёт **40 × `exit 3`**
и **3 × `exit 0`**, причём все три нуля — «ПРОВЕРЕНО ВПУСТУЮ», доказанным не
числится ничего (замер Ч130, ведомость
`/srv/tmp/dokazuemyy/Ч130-sled-yadra-v-zapisi/rabota/SUD-YADRO1.tsv`, 44 строки с
шапкой; повторить — `bash …/rabota/повторить.sh`). Разрыв измерен построчно:
записи ядра — 1728 строк, собранные скриптами — 1840; разница 112 строк, все
одного из двух родов. **Запись ядра — это скриптовая запись без строк trace.**

**Починка показана в клоне, оба конца.** С наложенным черновиком ядро из дерева
выдаёт ходы, printer печатает запись, побайтово равную скриптовой записи Ч111
(значит скрипты ничего не сочиняли), а checker на ней даёт `exit 0`
«ПРОВЕРЕНО», «на слово ядра 0»; на записи сегодняшнего двоичного — `exit 3`.
Цена, по той же мерке Ч4: **`+194 LOC` и `+26` функций** (черновик Ч70 с Ч91,
`zapis.flang` код 341 → 500, `proofterm.flang` 1825 → 1860) и **`+94 LOC` и
`+14`** (черновик Ч71, вторая печать — `пример строка N`). Черновиков, значит,
два и они разного рода. **В дереве нет ни одного**, и порознь оба ложатся чисто,
а друг на друга — нет (конфликт в `flang/self/zapis.flang`), так что 288 LOC —
оценка сверху, а не замер.

**Что нужно до 1.0.** Печать ходов в ядре решений — не «завести с нуля», а
довести с восьми записей из сорока трёх до всех. Пока ядро закрывает цели молча,
и независимая проверка (2.5), и доля корпуса (2.4) упираются в то, сколько ядро
вообще выписывает, а не в вид записи.

Часть этого уже в стволе: коммит `841c3f38` (`flang/self/zapis.flang`, +481
строка) научил печать нести sha256 исходника, терм при номере строки и
оглавление списочных литералов. До собранного двоичного правка доедет
перепечаткой.

---

### 2.7. Столкновение имён, державшее десять заплат ядра — СНЯТО 1 сентября 2026

✅ **Пункт закрыт, и закрыт тремя коммитами, а не одним.** Столкновений того же
рода нашлось три, и все три в стволе:

| коммит | что переименовано |
|---|---|
| `de3fef84` | «Занято именем модуля C++» — плюсы не выживают в идентификаторе |
| `ef718648` | тип «Сбор слов» → «Сбор слов C» в `flang/self/emit-c.flang` |
| `cd5a9f26` | «Обойти занятое целью C++» — третье того же рода |

Третье нашла сама переставленная проверка (`27789197`: столкновение имён ловится
ПЕРЕД судом ядра и только на печати в C), а не человек: отказ приходит за 580 с
вместо 35 760, в 61,6 раза раньше. Проверка на стволе:

```sh
LC_ALL=C.UTF-8 /usr/bin/grep -a -n '^объект «Сбор слов»\|^тип «Сбор слов»' \
  flang/self/*.flang flang/stdlib/*.flang
# flang/self/bounded.flang:318:объект «Сбор слов»
# flang/stdlib/sha256.flang:70:тип «Сбор слов»       ← emit-c.flang ушёл
```

Этим разблокированы десять заплат ядра — Ч48, Ч55, Ч56, Ч70, Ч71, Ч75, Ч76,
Ч86, Ч87, Ч91, — среди которых линия печати ходов вывода Ч56 → Ч71 → Ч87. Часть
из них уже в стволе: `841c3f38` (печать ходов, `zapis.flang` +481 строка),
`89baa02f` + `fa017ef2` + `5c7a9196` (половина независимого проверяльщика,
`flang/proof/чекер/сверщик.c` 1566 → 2412 строк).

Названное здесь же следом столкновение «Связка постусловия» в стволе тоже
снято: имя объявлено только в `flang/self/proofterm.flang:1409`, в
`flang/self/totality.flang` его нет (`grep -c` → 0).

Ниже — разбор беды, как она стояла до починки. Оставлен потому, что мина не одна
(двенадцать общих имён) и следующая сядет так же.

---

Все **десять** несданных заплат ядра — Ч48, Ч55, Ч56, Ч70, Ч71, Ч75, Ч76, Ч86,
Ч87, Ч91 — несли третьей строкой `flang/self/zapis.flang` одно и то же:
`использует «SHA-256»`. Она втягивает `flang/stdlib/sha256.flang` в замыкание
компилятора, а там то же имя было занято:

```sh
LC_ALL=C.UTF-8 /usr/bin/grep -a -n '^объект «Сбор слов»\|^тип «Сбор слов»' \
  flang/self/*.flang flang/stdlib/*.flang
# flang/self/bounded.flang:318:объект «Сбор слов»
# flang/self/emit-c.flang:454:объект «Сбор слов»
# flang/stdlib/sha256.flang:70:тип «Сбор слов»
```

Понятия при этом разные — запись из двух полей против варианта из трёх, — то есть
чинится это переименованием, а не слиянием.

**Отказ наблюдён не предсказанием, а самим компилятором.** Ч135 свела беду к
девятистрочной пробе, которая ввозит оба модуля:

```sh
cd /srv/tmp/dokazuemyy/Ч135-sbor-slov/clon && ./bootstrap/flang check \
  'flang/self/проба-Ч135-столкновение.flang'
# FLANG_DUPLICATE_NAME, строка 70, столбец 1: тип «Сбор слов» объявлен в двух
# модулях: …/flang/self/emit-c.flang и …/flang/stdlib/sha256.flang
```

Быстрая проверка того же — `node flang/scripts/link-collision-guard.mjs --дерево`:
на любом из десяти клонов код 1 и «столкновений: 1», на стволе код 0
(`Ч135-sbor-slov/progony/10-guard-do-vsyo.txt` — десять строк подряд). Ч123
проверила это на двух разных линиях ядра — на Ч56 без Ч71 и на Ч91; цепочку
Ч56 → Ч71 она влила ровно как предписано, ядро вышло побайтово (`cmp` код 0) — и
тут же откатила.

**Чего это НЕ ломает, и это важно не преувеличить.** Проверку отдельного файла
корпуса столкновение не рубит: `check flang/self/zapis.flang` с заплатой Ч56
проходит чисто, замечаний нет (`Ч135-sbor-slov/progony/01-Ч56-do-pravki.out`) —
заплата ввозит «Печать в C» с ограничителем, и «Сбор слов» в него не входит.
Красным становится замыкание `flang/self/bootstrap/compiler.flang`, который
ввозит «Печать в C» без ограничителя: **43 файла замыкания на стволе → 45 с
заплатой**, и связывание отвечает `FLANG_DUPLICATE_NAME` (протоколы
`Ч135-sbor-slov/progony/05-guard-stvol.out` и `10-guard-do-vsyo.txt`). **То есть
это блокер перепечатки и сдачи в ствол** — а через них 2.5 и 2.6, потому что
печать ходов лежит среди этих десяти.

**Починено 1 сентября, в дереве лежит** (`ef718648`, см. врезку выше).
Переименование было показано в клоне Ч135: после него все десять заплат дают
код 0 —
`замыкание: 45 файлов … столкновений: 0` на каждой строке
(`Ч135-sbor-slov/progony/40-guard-posle-vsyo.txt`, снято 31 августа в 22:14;
до починки тот же список давал десять строк `rc=1`).

**Мина не одна.** Имён, объявленных и в `flang/self/**`, и в `flang/stdlib/**`,
сегодня двенадцать (замер Ч139, ствол `fbc56541`):

```sh
LC_ALL=C.UTF-8 /usr/bin/grep -a -h -oE '^(тип|объект) «[^»]+»' flang/self/*.flang \
  | sed 's/^[^ ]* //' | LC_ALL=C sort -u > /tmp/self.txt          # → 866 имён
LC_ALL=C.UTF-8 /usr/bin/grep -a -h -oE '^(тип|объект) «[^»]+»' flang/stdlib/*.flang \
  | sed 's/^[^ ]* //' | LC_ALL=C sort -u > /tmp/stdlib.txt        # → 131 имя
LC_ALL=C comm -12 /tmp/self.txt /tmp/stdlib.txt | wc -l           # → 12
```

Двенадцать — это не двенадцать поломок: столкнутся только те, что попадут в одно
замыкание. Сколько именно — **не мерено**, и померить это стоит раньше, чем
чинить поодиночке: чинить одно имя, когда рядом лежат ещё одиннадцать, значит
завтра снова упереться.

**Что нужно до 1.0.** Переименовать «Сбор слов» и влить десять заплат — работа на
часы. Пока она не сделана, ни 2.5, ни 2.6 сдать нельзя, поэтому в разделе 5 она
стоит первой.

---

## 3. Что закрылось с прошлой редакции

### 3.1. Регулярные выражения — СДЕЛАНЫ

Их не было вовсе, и они стояли долгом. Теперь `flang/stdlib/automaton.flang`,
1253 строки.

**Устройство честное, а не «как у всех».** Обычный движок регулярных выражений —
цикл с возвратом, у которого число шагов ничем не ограничено: `(a+)+b` на строке
из двадцати «a» перебирает 2²⁰ разложений. Язык, где каждая функция обязана
доказано завершаться, такой движок не примет — и это не придирка проверки, а
правда о движке.

Вместо него — **производные Бжозовского**: состояние автомата это сам образец,
укороченный на прочитанный знак. Прогон — свёртка по знакам строки, ровно один
шаг на знак, ни одного возврата. Число шагов равно длине входа **по построению**.

Прогон:

```sh
flang test flang/stdlib/automaton.flang
```

```
flang/stdlib/automaton.flang: примеров 104, прошло 104, не прошло 0
```

Названо и то, чего не будет: обратных ссылок (`\1`) и просмотров вперёд и назад.
Оба требуют перечитать прочитанное, то есть ровно того возврата, ради отказа от
которого всё написано. Образец с ними не молчит, а **отказывает названно**.

**И он же — лучший пример того, как быстро это чинится.** Модуль приехал с
пятью обещаниями на 1253 строки, ни одно из которых не было доказано:
`утверждений 5: доказано 0, сетка 5`. За один заход, без единой теоремы:

```
утверждений 102: доказано 98 (из них без теоремы 98), сетка 4
```

Дописано 97 обещаний, доказаны все 97, плюс одно чужое перешло из сетки. Все 98
ядро взяло само разбором цели по условию. Примеры при этом не тронуты — 104 из
104 до и после.

Оставшиеся четыре сетки — те самые, что стояли в файле изначально: про свёртку и
про границу-неравенство, то есть в стены, названные разделом 4.3.

### 3.2. Служба поднимает сама себя — СДЕЛАНО

Хозяин ввода-вывода на C отказывал всем шести поручениям соединения:
`FLANG_IO_NET: у хозяина на C нет способа ждать сеть: планировщик процессов не
ждёт`.

Довод был верен наполовину — пробег атомарен, и останавливать его на `accept`
нельзя. Неверен был вывод: ждать должен не пробег, а планировщик, и «ждать» в
этой модели уже определено — процесс, которому не пришло письмо, ждёт.

Сокет неблокирующий; поручение, на которое ответить нечем, ложится в список
ожиданий, а `poll` в цикле планировщика приносит отклик тем же путём, каким
приходит всё остальное. **Словарь поручений и словарь откликов не тронуты ни на
слово.**

Прогон: сокращатель ссылок (`examples/web/shortener/`, 1150 строк на
flang) поднят и отвечает `curl` — 200 на `/здоровье`, 201 на `POST /ссылки`, 200
со списком, 301 с `Location` и сосчитанным переходом, 204 на `DELETE`; код
возврата `curl` — 0 на всех семи обращениях. Тот же сценарий против хозяина
планов даёт побайтово тот же вывод. Под valgrind — 0 байт в использовании, 0
ошибок.

Цена названа замером: `poll` стоит 402 нс на одном сокете, на ходу идёт раз в
1024 пробега — **0,4 %**. Программа без сети не платит ничего.

### 3.3. Менеджер пакетов — ЕСТЬ РЕЕСТР, чего раньше не было

Прошлая редакция писала: «есть замок, нет реестра, версий, разрешения
конфликтов». Замок остался (`flang lock`, `flang package`), а реестр появился —
`flang/stdlib/registry.flang` (670 строк) и `scripts/registry-tool.flang` (441).

**И он единственный модуль библиотеки, доказанный полностью:** отчёт
`утверждений 20: доказано 20, сетка 0`. Двадцать из двадцати обо всех входах.

Это, между прочим, самый полезный факт дня для планирования: реестр писали уже
зная, какие формы записи ядро берёт. Модули, написанные вслепую, стоят на
30–40 %. **Низкие числа по библиотеке — след того, КАК писали обещания, а не
предел ядра.**

---

## 4. Это 1.1, а не 1.0

### 4.1. Фронтенда нет

`web/` на месте: `app/` — работающее приложение в браузере без сборки и
бандлера; `wasm/` — разведка с выводом «не делать» (в 12,5 раза тяжелее обычной
печати в JS). Ставка языка — доказуемость, а не браузер.

### 4.2. Корпус доказанного мал

По библиотеке доказано обо всех входах меньше половины обещаний (замер
23 августа). Это не мешает обещать стабильность названного в разделе 0 — но
мешает называть язык формально-доказуемым без оговорки. Пока правильная
формулировка: **«язык, в котором доказанное отделено от прогнанного, и это
отделение видно числом»**.

**Дополнено 31 августа, и две выборки сводить нельзя.** По 120 файлам
(`flang/stdlib` + `flang/proof/examples` + `flang/test/fixtures`) доля
доказанного — 4200/6356 = 66,1 % (замер Ч45), то есть заметно больше половины;
но это ДРУГАЯ выборка, шире библиотеки, и подставлять её вместо «меньше
половины» — значит менять две переменные разом. Что верно про обе: **сама доля
завышена на 2,33 %, если снята ствольным двоичным** (2.3), а проверена не ядром
из неё 1,25 % (2.4).

**И третье, снятое вечером 31 августа: доказанных «ни на чём» — ноль.** Два
замера вместе покрывают **208 файлов** (151 у Ч103 и 57 у Ч124 — те, что вообще
дали ведомость): **42 045 утверждений, 29 220 доказанных, «ни на чём» — 0, «на
веру» — 0**. Правило счёта названо в коде замера:
`verdict==="proved" && by!=="declaration" && steps===0`
(`/srv/tmp/dokazuemyy/Ч124-slepye-69/chisla.mjs:11`); сами числа —
`Ч103-bez-proverki-pereschet/rez-svod.json` (18 918 и 13 347) и
`Ч124-slepye-69/chisla.итог` (23 127 и 15 873).

Ядро решений досмотрено тем же двоичным без единой правки — ему просто дали срок
вместо 300 секунд (`/usr/bin/time` рядом с каждым прогоном,
`Ч124-slepye-69/ved-yadro/*.time`):

| файл | секунд |
|---|---:|
| `proofterm.flang` | 3549 |
| `proof-initial.flang` | 2405 |
| `proof-kernel.flang` | 2147 |

Предел шагов при этом не тронут: самый долгий съел 6 % вшитого.

**Две оговорки, обе честные.** Первая: складывать эти два замера можно только
зная, что они сняты РАЗНЫМИ приборами — Ч124 гоняла двоичный
(`./bootstrap/flang check <файл> --proof --json`), а Ч103 — реализацию на JS
(`node` над `flang/scripts/proof-ledger.mjs`). Вторая: ноль здесь не про
независимую проверку, а про то, что ядро нигде не числит доказанным утверждение,
под которым у него самого ничего нет. Кто проверяет само ядро — это 2.4 и 2.5, и
там числа другие.

### 4.3. Пять пределов ядра, названных поимённо

Замерены 23 августа, ни один не снимается записью обещания:

1. **Строгих неравенств у ядра нет вовсе.** Семь видов цели, `меньше` и `больше`
   среди них не значится.
2. **Целость терма берётся только с объявленного типа, никогда с факта** — из-за
   чего стоит весь столбик хеш-таблицы, где параметр объявлен `число` нарочно.
3. **`содержит` и `начинается с` на строке, собранной склейкой** — оба правила
   выводятся только по построению **списка**. Это большой класс в компиляторе:
   всё, что печатает код.
4. **Граница свёртки** — уровень шага открыт полностью, подъём постусловия наверх
   закрыт.
5. **`разбор` по вариантам** — навигация по дереву разбора, самая большая часть
   компилятора.

Три другие дыры закрыты 23 августа: разбор цели по пустоте списка, свёртка над
пустым списком, переписка «элемент N в списке». Все три написаны с
**проверяемыми списками предсказаний** — каждое правило называет, какие именно
обещания обязаны стать доказанными и какие обязаны остаться недоказанными.
Списки проверяются перепечаткой.

### 4.4. Печать программы обратно в текст почти сходится, и «почти» — это 1 файл из 296

Измерено впервые 31 августа (Ч133 и Ч136), перемерено 5 сентября прибором,
который теперь лежит в дереве (задача 9751 — до неё он лежал во временном
каталоге, и этот раздел отсылал читателя туда абсолютным путём). Собиратель
гоняет `текст → flang tokens → текст` по 296 файлам корпуса (45
`flang/proof/examples`, 58 `flang/self` верхнего уровня, 193 `examples`).

```sh
LC_ALL=C.UTF-8 python3 flang/proof/привязка/кругооборот.py
```

| | сколько | собрано байт в байт | было 31 августа |
|---|---:|---:|---:|
| строк кода | 113 347 | **112 629 (99,37 %)** | 111 643 (99,36 %) |
| строк пустых | 13 086 | 13 086 (100 %) | 12 986 (100 %) |
| строк комментария | 28 878 | **223 (0,77 %)** | 221 (0,7 %) |
| **файлов ЦЕЛИКОМ байт в байт** | 296 | **1** | 1 |

**Корпус тот же самый** — 296 файлов, состав совпадает пофайлово. Сдвинулись
не файлы, а их содержимое: за пять дней их правили, оттого строк кода стало
на 986 больше, а комментария на 1653 меньше. Доля не сдвинулась ни на сотую
там, где важно: 99,36 → 99,37 %.

Единственный сошедшийся целиком файл — `import-check.flang`, 10 строк.

**Оговорка, которая важнее числа.** Это НЕ ответ на гейт Г5. Г5 спрашивает,
падает ли доверие к разбору с 5287 строк; снимает разбор с границы только путь
«печать из дерева разбора обратно в текст», а печатника в flang нет вовсе.
Кругооборот меряет готовность ингредиента — довезли ли токены то, из чего
текст можно собрать. Записать это число ответом Г5 значило бы выдать
готовность за результат (задачи 7855 и 9751). Прибор печатает эту оговорку
каждым прогоном.

**Мешают ровно три вещи, и все три показаны порчей, а не рассуждением** (разбор —
`python3 flang/proof/привязка/кругооборот.py --причины`): строчный
комментарий не выдаётся вовсе — 30 310 строк; блочный — 213; форма экранирования
забывается (`"\u0400"` и сам знак неразличимы) — 492; широкая пунктуация теряет
поверхностный вид — 10; ещё 3 строки — вина самого собирателя (столбцы в UTF-16
против кодовых точек). Сумма 31 028 сходится с ведомостью знак в знак. Оговорка
про комментарии: 221 строка всё же собралась — это те, где `//` стоит внутри
строкового литерала.

**Сколько строк лексера это стоит починить — числа НЕТ.** Функций верхнего уровня
в `flang/self/lexer.flang` девяносто девять — `LC_ALL=C.UTF-8 /usr/bin/grep -a -c
'^тотальная функция' flang/self/lexer.flang`, — а кода в нём 1018 строк (мерка
Ч4, замер Ч136). Сколько из них придётся тронуть, померено не было — оценка в черновиках Ч136 стоит как предсказание, а не как замер. Снимается
это так же, как всё остальное здесь: правкой и повторным прогоном собирателя.

**Почему это 1.1, а не 1.0.** Обещание раздела 0 на этом не стоит — точные байты
печати там прямо НЕ обещаны. Но на этом стоит удешевление доверия: вынести разбор
за границу доверия (5287 строк, 20,2 % TCB — см. раздел 6) можно только тем, что
печать сходится и это видно числом. И оговорка Ч133, без которой число обманет:
собиратель идёт через `flang tokens`, то есть снимает **лексер** (1018 строк), а
не parser; чтобы он снял parser, печатать надо из AST, а позиции у AST стоят на
69,9 % строк корпуса.

---

## 5. Порядок, если делать

| # | работа | пункт | оценка |
|---|---|---|---|
| 1 | перепечатать семя и закрепить это в CI | 2.1 | ~~два часа прогона~~ **8 ч 34 мин прогона** плюс день на принуждение; за 31 авг два захода убиты сигналом, третий идёт |
| 2 | перемерить дерево четырьмя вердиктами и опубликовать числа | 2.3 | день ПОСЛЕ пункта 1 (нужен двоичный, знающий четвёртый вердикт) |
| 3 | доказать обещания компилятора — каждое снимает проверку из рантайма | 2.2 | недели, но окупается сразу |
| 4 | принять формулировку раздела 0 и записать её отдельным файлом | 0 | день |
| 5 | доказать `automaton.flang` — 5 обещаний на 1253 строки | 3.1 | дни |

Пункты 1–2 — это и есть 1.0 по существу: после них числа, которые мы называем,
перестают быть числами старого инструмента.

### Проверено 31 августа: оценка пункта 1 неверна вчетверо. Порядок она не меняет

«Два часа прогона» — число 23 августа. Сегодня перепечатка стоит 8 ч 34 мин
(`docs/reprint-ledger.tsv`), а за сутки 31 августа семени не дал ни один из
трёх заходов: 9 ч 19 мин — убит сигналом, 3 ч 25 мин — убит сигналом, третий
идёт (2.2).

**Порядок остаётся тем же.** Причина не в том, что пункт 1 дёшев — он вчетверо
дороже, чем думали, — а в том, что **всё остальное ждёт ровно его**. Правка ядра
про минус ноль (Ч45), криптографическая привязка записи (Ч48/Ч55), термы в
записи (Ч56), сверка имён (Ч59) — все лежат в исходниках и ни одна не работает
в двоичном, пока семя не напечатано. Пункты 2, 2.4 и Г4 без пункта 1 не мерятся
вовсе, а не мерятся хуже. Переставить их вперёд нечем.

**Меняется не порядок, а содержание пункта 1.** «Перепечатать семя» — это не
работа целиком, а её обречённая половина: печать идёт 8,5 часа, входы печати
трогают раз в десять минут (замер Ч59), значит напечатанное устареет до конца
захода. Работа пункта 1 — три вещи, и порядок между ними тоже перемерен:

| # | что | состояние, числом |
|---|---|---|
| **1б** | **найти, кто убивает заход сигналом** | два журнала с `Exit status: 143` при свободной машине, пределы не исчерпаны; причины нет |
| 1а | сверять ИМЕНА ядра, а не родство коммитов | `sh scripts/raskrutka.sh --imena` — 0,35 с, называет 29 отставших имён поимённо; **с вечера 31 авг в стволе**, коммит `4d0dcb7b` |
| 1в | окно печати: не трогать `flang/self/**`, пока идёт заход | не сделано, числа нет |

**1б идёт впереди 1а и 1в, и это единственная перестановка, которую наши
замеры требуют.** Планировать работу, два захода которой за сутки убиты
неизвестно чем (третий ещё идёт), нельзя: без 1б у пункта 1 нет оценки — есть
только удачные заходы прошлой недели.

Из этого же следует, что **«перепечатка в CI на каждое слияние» (2.1) в
нынешнем виде невыполнима**: слияния идут чаще, чем печать успевает кончиться.
До 1.0 нужно либо окно (1в), либо сторож, который краснеет не на возраст
коммита, а на расхождение имён (1а).

### Пересмотрено вечером 31 августа: впереди встало то, чего утром не было

Три вечерние находки меняют порядок, и меняют в одну сторону. Раньше первым стоял
пункт 1 (семя, 8 ч 34 мин); теперь перед ним встаёт работа на часы, без которой
не идёт ничего из написанного за день.

| # | работа | пункт | почему здесь | состояние, числом |
|---|---|---|---|---|
| ~~**0**~~ | ~~переименовать «Сбор слов» и влить десять заплат~~ | 2.7 | **СДЕЛАНО 1 сентября 2026** | три столкновения сняты (`de3fef84`, `ef718648`, `cd5a9f26`); из десяти заплат в стволе печать ходов (`841c3f38`) и половина проверяльщика (`89baa02f`, `fa017ef2`, `5c7a9196`). Имён, объявленных и в `self/`, и в `stdlib/`, по-прежнему 12 — следующая мина сядет так же |
| 1 | печать ходов вывода в ядре | 2.6 | без неё 2.4 и 2.5 меряют записи, которых ядро не печатает | черновики `+194 LOC` и `+94 LOC`, в дереве нет ни одного |
| 2 | отпечаток снаружи и чтение исходника по правилам языка | 2.5 | ложь доходит до `exit 0` | `+48 LOC` написано, к ствольной линии checker'а не прикладывается |
| 3 | найти, кто убивает заход печати сигналом | 2.2 | без этого у пункта 4 нет оценки | два журнала с `Exit status: 143`, причины нет |
| 4 | перепечатать семя и закрепить это в CI | 2.1 | всё, сделанное в 0–2, живёт в исходниках, а судит двоичный | 8 ч 34 мин на удачном заходе |
| 5 | перемерить дерево четырьмя вердиктами | 2.3 | нужен двоичный, знающий четвёртый вердикт | день после пункта 4 |

**Почему ноль стоял впереди четвёрки, хотя семя дороже.** Пункт 4 остаётся самым
дорогим и самым важным, но он ничего не переносит в двоичный, пока переносить
нечего: десять заплат лежали вне дерева, и печать ходов — среди них. С
1 сентября ноль снят, и первым снова встаёт пункт 1. Перепечатать
семя раньше, чем влиты заплаты, значит потратить восемь с половиной часов на то,
чтобы напечатать сегодняшнее дерево ещё раз.

**И половина работы пункта 1 сделана без нас.** Сверка имён (1а) с вечера
31 августа в стволе — коммит `4d0dcb7b`, `sh scripts/raskrutka.sh --imena`;
остаются 1б (кто убивает заход) и 1в (окно печати).

---

## 6. Что говорить, пока 1.0 нет

0.6 сейчас честнее, чем была бы 1.0, и это не отговорка. У языка есть редкая
вещь — **измеренный список собственных провалов**, и он ценнее бодрого списка
успехов.

23 августа этот список пополнился находкой, которую стоит рассказывать первой:
**инструмент проверки завышал собственный счёт**, засчитывая доказанными
утверждения, опёртые на недоказанную посылку. Это поймано не рассуждением, а
прогоном, и поймано нами самими. **31 августа завышение названо числом:
2,33 %** — 100 утверждений из 4300 (2.3). Число небольшое, и рассказывать надо
именно так: важна не величина, а направление — инструмент ошибался в свою
пользу.

Показывать надо то, что предъявляется машиной. На 31 августа это четыре вещи.

**1. Аксиомы — их 28, и список машиночитаемый.** Здесь и в строке таблицы
раздела 0 стояло «ноль аксиом». Это неправда, и смягчать её нельзя: надо
предъявлять список. Ячейка B4 перебрала **154 места** трёх файлов ядра, где
решение даёт положительный исход, и разнесла их по разрядам: 60 следствий
(в условии стоит вызов другого правила — выведено, а не принято), 40 узнаваний
формы (о значениях не утверждают ничего), **36 мест-аксиом**, **7
переадресовок доверия** чужой программе (вычислителю, типизатору, оракулу
законов), 6 конструкторов, 5 отборов. Аксиомы и переадресовки складываются в
**35 различных утверждений: 28 аксиом и 7 переадресовок**, и каждое выписано
с местом в коде — `docs/kernel-axioms-list.md`, сторож
`flang/scripts/kernel-axioms.flang`.

Сторож проверен изъятием, а не на слово: в ядро вписали новое место принятия
без довода, без слова «аксиома» в коде и в комментарии — сторож его назвал
(`B4/PROBA-IZYATIEM.md`). Это и есть мерка для всякого списка: **что он делает
с неизвестным именем.** Пропускает — он хуже отсутствующего, потому что создаёт
уверенность.

Четыре аксиомы из 28 — про плавающую точку, ровно там, где учебный текст
обещал «ноль аксиом о плавающей точке» (находка ячейки Ч52).

**2. Независимый чекер — 1566 строк C, и он отвергает 61 подделку на настоящих
объектах.** Отдельная программа: пять заголовков libc, ни строки от flang, и
она **не собирается вместе с компилятором и не зовётся из
`bootstrap/Makefile`** — судья, которого собирает подсудимый, судьёй быть
перестаёт.

```sh
make -C flang/proof/чекер && sh flang/proof/чекер/пробы/прогон.sh
```

Итог прогона — два числа, и одно без другого не значит ничего, потому что
«отвергнуто 61» описывает и сторожа, который отвергает всё подряд:

- **подделок 61 — принято кодом 0 ноль** (59 отвергнуто кодом 1 «НЕ СОШЛОСЬ»,
  2 ушли в третий исход, и там причина названа вслух);
- **честных записей 95 — отвергнуто ноль.**

Подделки не выдуманы: это правки одной строки в записях, снятых ядром с
настоящих файлов, — 30 по полям записи, 4 «доказательство лжи», 14 по сведению,
5 по привязке к программе, 8 порчей свежих записей. Каждая правка проверяется
на то, что она вообще применилась: правка, не нашедшая своей строки, сравнивала
бы запись саму с собой и зеленела бы всегда. Полторы секунды, компилятор flang
не нужен. Написан Ч29, правлен Ч55, перенесён в дерево Ч63 — и **31 августа
сдан в ствол**: коммит `e8380e2d`, `flang/proof/чекер/сверщик.c`, 1566 строк.

```sh
git -c core.quotepath=false ls-tree -r --name-only main -- 'flang/proof/чекер' | wc -l   # → 137
```

**Но два эти числа — 61 и 95 — вечером 31 августа получили две оговорки, и без
них они читаются неверно.** Первая: тем же вечером checker пропустил ложь до
`exit 0` на трёх adversarial-кейсах из пятнадцати (2.5) — то есть «отвергает
всё, что подложили» верно ровно про то, что подкладывали до сих пор. Вторая: и
61, и 95 сняты на записях, СОБРАННЫХ СКРИПТАМИ; на записях, которые ядро печатает
сегодня, счёт другой — 44 против 82 и 86 против 344 (2.6). Говорить «независимый
checker отвергает ложь» можно, только называя обе.

У чекера три исхода, а не два: 0 «ПРОВЕРЕНО», 3 «НЕ ПРОВЕРЕНО — и непроверенное
названо числом», 1 «НЕ СОШЛОСЬ». Третий исход не украшение: слить его с нулём
значит соврать про запись, слить с единицей — оболгать честную. Свои девять
допущений чекер печатает сам.

**3. Отчёт, который отличает доказанное от прогнанного четырьмя словами**, и
42 спеки на настоящих предметных областях (скидки, налоги, возвраты,
лояльность, бронирование) с 295 утверждениями, доказанными из этих же
названных аксиом.

**4. Подлог из 14 нарочно испорченных случаев**, на каждом из которых проверка
обязана покраснеть — **а на честном изменении промолчать**.

**Чего говорить НЕЛЬЗЯ, даже имея всё это.** «Корпус проверен» — неправда:
проверено 1,25 % (2.4). «Мы проверили доказательства» — тоже: чекер отвечает на
вопрос, ЧЕМУ верить, а не «всё ли проверено». И «доверие сведено к 4548
строкам» — неправда прямая: 4548 считаны по трём файлам ядра, а вычислитель,
парсер и лексер, на которые ядро опирается, в это число не входят; честная
полоса — 4548…6710.

**И вот чем эта полоса меряется с 31 августа.** TCB измерен одной линейкой по
союзу десяти файлов (`parser`, `lexer`, `types`, `totality`, `link`, `interpret`,
`proofterm`, `proof-kernel`, `proof-initial`, `proof`) — замер Ч133 на стволе
`03fb4060`, повторён Ч139 знак в знак:

| слой | всего | код | достижимо | ядро решений |
|---|---:|---:|---:|---:|
| **союз десяти файлов** | 49 334 | **26 234** | 24 669 | 9 138 |
| из них `parser.flang` | 8709 | **5287** | 5038 | 465 |
| рантайм на C, 4 файла `bootstrap/*.c` | 622 561 | **517 099** | — | — |

```sh
python3 /srv/tmp/dokazuemyy/Ч4-merka/merka.py --narechie flang --json \
  --zovushchiy flang/self/bootstrap/compiler.flang \
  flang/self/parser.flang flang/self/lexer.flang flang/self/types.flang \
  flang/self/totality.flang flang/self/link.flang flang/self/interpret.flang \
  flang/self/proofterm.flang flang/self/proof-kernel.flang \
  flang/self/proof-initial.flang flang/self/proof.flang
python3 /srv/tmp/dokazuemyy/Ч4-merka/merka.py --narechie c --json \
  bootstrap/compiler_flang.c bootstrap/flang_cli.c bootstrap/flang_repl.c \
  bootstrap/flang_runtime.c
```

Ключ `--zovushchiy` обязателен: без него «достижимо» выходит нулём, и число
24 669 по такой команде не получить. **Доля parser — 20,2 %** (5287/26234): он
больше type checker и второй после ядра доказательств. На нынешнем стволе
`fbc56541` те же десять файлов дают 26 258 и 24 692, доля 20,1 % — дерево
движется, и число надо называть с коммитом.

**И одно число, которое надо назвать отдельно, потому что замера за ним не
стояло.** В критерии, по которому мы себя проверяем, было записано «доверие к
разбору падает с **5466** строк». Такого замера нет: 5466 не даёт ни одно из
шестнадцати чисел мерки, как число в дереве оно не встречается, файла на 5466
строк нет, а **76 коммитов `parser.flang` за всю историю файла дают полосу
3180…5287** — 5466 не достигалось никогда (25 последних коммитов проверила Ч133,
все 76 — Ч139). В критерии число заменено на измеренное 5287 31 августа. Это тот
же разряд беды, что и «два часа перепечатки» в разделе 5: число, взятое из
памяти, живёт в документе как факт, пока его не прогонят.

### Проверено 31 августа: часть наших собственных проверок зелена по построению

Находка пункта 2.1 — «проверка „коммит печати есть предок HEAD“ зелена по
построению» — оказалась не единичным случаем, а породой. Три замера того же дня,
каждый со своим адресом:

**1. Списки, набранные руками, расходятся с деревом чаще, чем сходятся.** Таких
списков в дереве **105** (63 нашла Ч121 адресно, 50 — сплошной проход Ч127,
пересечение 8), и **57 из них сегодня расходятся** с тем, что рядом лежит
(`Ч127-spiski-vo-flang/ИТОГ.md`, раздел 7; переписи —
`Ч121-spiski-rukami/СПИСКИ.tsv` и `Ч127-spiski-vo-flang/СПИСКИ.tsv`). Сама Ч127
называет 105 нижней границей: `.md`, `.c` и `.h` в перепись не вошли.

**2. Пять проверок под путём с кириллицей выходили `exit 0` с нулём байт
вывода** — при том что на латинском пути то же дерево было красным на трёх из
них: 2670, 2555 и 8479 байт замечаний (замер Ч104; прогон — `bash
/srv/tmp/dokazuemyy/Ч82-kirillica/run5.sh`, пять проверок в том же порядке).
Причина названа поимённо — вопрос «меня запустили напрямую?» задавался так:

```js
if (import.meta.url === `file://${process.argv[1]}`)
```

Вне-ASCII в пути приезжает percent-кодированным, сравнение ложно, и тело проверки
не зовётся вовсе. **Это уже починено в стволе** (`ab1150b8`, поверх — `fbc56541`),
и замер надо датировать `03fb4060`, на котором он снят.

**3. Проверка, отвечающая на одно и на два незнакомых имени буква в букву
одинаково.** `flang/scripts/kernel-forgeries.flang`: и на одном незнакомом
заголовке в ядре, и на двух — ответ «нарушений: 1 • в ядре завелась аксиома вне
названного довода», код 1 (замер Ч117). Второй случай хуже:
`scripts/file-extensions.flang` на двух незнакомцах, на одном и на «ничего не
подложено» даёт три побайтово одинаковых ответа, и все три — код 0 (замер Ч127).

Мерка для всякой такой проверки — та же, что названа выше для списка аксиом:
**что она делает с неизвестным именем.** Ответ, не зависящий от числа находок, —
признак того, что проверка не считает, а рапортует.

Связанное: `docs/site/kak-dokazat.ru.md` (какие формы записи ядро берёт — семь
форм, каждая с замером), `docs/zettel/proven-is-not-correct.md` (чего
доказательство не говорит никогда), `docs/zettel/proof-cost-0-of-20.md` (сколько
стоит доказать обычную функцию).
