модуль «Замер2 06: Приписать в начало»

// Функция и её зависимости перенесены из flang/stdlib/higher-order.flang ДОСЛОВНО;
// примеры автора убраны. Копирование в измеренные строки не входит.

тотальная функция «Приписать в начало»
  принимает первый: число, элементы: список числа
  возвращает список числа
  // ── ДОКАЗАТЕЛЬСТВО ──
  для всех элементы обеспечивает «приписывание удлиняет ровно на один» (длина результат) равен ((длина элементы) плюс 1)
  // ── ТЕСТЫ ──
  пример «В пустой список»
    дано первый равно 1
    дано элементы равно []
    ожидается [1]
  пример «В непустой список»
    дано первый равно 1
    дано элементы равно [2, 3]
    ожидается [1, 2, 3]
  пример «Повтор не мешает»
    дано первый равно 2
    дано элементы равно [2, 2]
    ожидается [2, 2, 2]
  пример «Отрицательное и ноль»
    дано первый равно -1
    дано элементы равно [0]
    ожидается [-1, 0]
  // ── КОНЕЦ ТЕСТОВ ──
  свёртка элементы начиная с [первый] как акк и эл → добавить эл к акк

теорема «приписывание удлиняет ровно на один»
  дано первый: число
  дано элементы: список числа
  утверждаем (длина результат) равен ((длина элементы) плюс 1)
  индукция по элементы
    случай пусто
      то по примеру «В пустой список»
    случай голова и хвост
      то по предположению
  следовательно доказано
