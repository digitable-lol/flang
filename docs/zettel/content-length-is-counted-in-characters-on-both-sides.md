# `http.flang` считает Content-Length ЗНАКАМИ в обе стороны, и потому чинить одну сторону нельзя

Заголовок `Content-Length` по спецификации HTTP считает ОКТЕТЫ. Стандартная
библиотека считает знаки: `«Напечатать ответ»` и `«Напечатать запрос»` пишут
`(к строке (длина тл))`, а `«Свести тело»` по той же мере решает, дочитано ли
тело. На кириллице это расходится ровно вдвое.

**Чем подтверждено.** `examples/web/shortener/service.flang`: на теле
«живой» служба печатает `Content-Length: 5` при десяти октетах тела. Число снято
примером в самом файле, а не оценкой. То же значение стоит ещё в пяти местах
пяти файлов сокращателя.

**Почему это не чинится одной строкой.** Обе стороны согласованы МЕЖДУ СОБОЙ:
что напечатали знаками, то знаками и прочли. Поправить только печать значит
рассогласовать её с разбором, и служба начнёт ждать октетов, которых по её же
мере уже хватило. Поправить обе стороны значит перемерить корпус HTTP
(`flang/test/http-corpus.mjs`): его ожидания сняты с нынешней меры, и часть
входов, сегодня «неполных», станет целыми, а часть целых — неполными.

**Чего НЕ хватало раньше и чего хватает теперь.** В `service.flang` стоял
комментарий: зазор «стоит одной функции, которой в языке сегодня нет: сколько
байтов в строке UTF-8». Функция есть — `flang/stdlib/utf8.flang`,
`«Длина в байтах»`, и её примеры считают "Мир" за шесть. Комментарий поправлен
на месте, а не переписан: он показывал, как рассуждали, и рассуждение было верно
ровно до дня, когда функцию написали.

**Чем ограничено.** На теле из одних ASCII-знаков обе меры совпадают, и служба
честна. Ошибка видна только там, где в теле есть знак вне ASCII.

## Дополнение 20 августа 2026: разбор врёт хуже печати, и второй половины починки нет до сих пор

Прежде улика была снята с ПЕЧАТИ. Обратная сторона опаснее, и её проще не
заметить: правильный по протоколу запрос с `Content-Length: 10` и телом «живой»
(5 кодовых точек, 10 октетов) разбор отвечает **«Запрос неполон»**.

```
«Собрать запрос» от "POST / HTTP/1.1\r\nContent-Length: 10" и "живой"
  → {"вариант":"Запрос неполон"}
```

То есть служба ждёт ещё пять знаков, которых не придёт никогда, и держит
соединение до срока хозяина. Печать всего лишь называет неверное число; разбор
ВЕШАЕТ живого клиента, и ловится это только настоящим клиентом, потому что оба
конца библиотеки ошибаются одинаково.

**Чем подтверждено.** Ветка `vypusk/utv-http` на основании `github/main`
`df055a6b`, `flang run` на пробнике из двух функций, двоичный из `bootstrap/`.

**Чего не хватает для починки, названо точнее.** `«Длина в байтах»` из
`flang/stdlib/utf8.flang` закрывает только ПЕЧАТЬ. Разбору нужна вторая функция,
которой нет нигде: **«взять первые N ОКТЕТОВ»**. `подстрока` режет по кодовым
точкам, и граница в N октетов может прийтись на середину знака — то есть надо
ещё и решить, что делать с обрезанным хвостом, а это решение, а не арифметика.
Плюс `использует` в `http.flang` нет нарочно (`flang/test/stdlib.test.mjs`
проверяет каждый файл поодиночке), поэтому обе функции придётся писать в самом
модуле заново.


## Дополнение 26 августа 2026: «чинить одну сторону нельзя» — неверно, и печать починена

Заголовок этой заметки утверждал, что поправить печать значит рассогласовать её
с разбором. **Это неверно, и опровергается прогоном.** Печать и разбор смотрят в
РАЗНЫЕ стороны провода: печать уходит к клиенту, который считает октеты, разбор
принимает от клиента, который считает октеты. Согласованы они только в одном
месте — там, где программа читает СВОЙ ЖЕ напечатанный вывод, то есть в круговых
проверках самой библиотеки. Служба круговой проверкой не является.

`examples/web/shortener/service.flang` печатает ответ сам — `«Напечатать ответ
службы»`, — а длину берёт у своей `«Длина тела в октетах»`. После этого все 313
примеров файла зелены, включая все примеры РАЗБОРА запроса: рассогласования не
наступило ни в одном.

Мера считается **сложением по знакам**, а не через `«Длина в байтах»` из
`utf8.flang`, и это замер, а не вкус: библиотечная идёт через `«Байты текста»`,
которая строит список всех байтов ради одного числа. Тот же довод и тот же счёт
уже стоят в `web/stand.flang` — два места пришли к одному решению порознь.

Взять `utf8.flang` сюда вводом модуля НЕЛЬЗЯ, и это отдельная находка:
`http.flang` вводит «UTF-8» выборочно (`только «Байты кодовой точки»,
«Одинокий суррогат»`), и запрос `«Длина в байтах»` из соседнего файла отвечает
`FLANG_UNKNOWN_NAME`, а ввод по пути — `FLANG_IMPORT_AMBIGUOUS` (в дереве два
файла с именем модуля «UTF-8»: свой и тот, что рядом с двоичным).

Дословно, на запросе `GET /чего-то-нет`:

| | до | после |
|---|---|---|
| `Content-Length` | 15 | 28 |
| тело | 28 октетов, 15 знаков | 28 октетов, 15 знаков |
| что читает клиент | `нет тако` | `нет такого пути` |

**Вторая половина как была, так и есть.** `«Свести тело»` сравнивает объявленное
число с `длина сырое`. Нужной ей функции «взять первые N ОКТЕТОВ» по-прежнему
нет нигде, и решение про обрезанный на середине знака хвост по-прежнему не
принято. Из браузера у службы теперь работает `GET`; `POST` с кириллицей в теле
— нет.

**Чем подтверждено.** Ветка `u/examples-services-proved`, коммит `7646750b`;
`flang io examples/web/shortener/plan.flang --in-dir`, код возврата 0 до и после;
`flang test examples/web/shortener` — файлов 8, взято 8, примеров 2054, не
прошло 0.


Связано: [[a-comparison-cannot-see-a-distinction-the-representation-cannot-express]],
[[a-connection-pipe-carries-text-not-octets]],
[[proved-claims-in-http-and-json-were-all-gratis]]
