модуль «Subsets»

// LeetCode 78. Subsets.
// Дан список различных чисел. Вернуть все его подмножества (степенное множество).
//
// Приём: перебор без рекурсии. Свёртка по элементам, состояние — уже готовый
// список подмножеств: каждый новый элемент удваивает набор, приписывая себя
// к каждому из готовых. Так «перебор с возвратом» превращается в один проход,
// и обещание тотальности несёт носитель composition — рекурсии в файле нет
// вовсе, поэтому ни меры, ни убывания придумывать не пришлось.
//
// Порядок ответа: подмножества идут в порядке двоичного счёта по элементам —
// сначала все без последнего элемента, потом те же с ним:
// [], [1], [2], [1,2], [3], [1,3], [2,3], [1,2,3]. Это ровно порядок примера
// из условия задачи.
//
// Цена: приписывание в конец («добавить … к …») копирует список, поэтому
// склейка двух половин стоит O(2^n) копирований на каждом шаге — итог O(n·2^n)
// работы вместо теоретического O(2^n). Приписывания в начало язык не даёт,
// изменяемого буфера тоже нет.

тотальная функция «Соединить наборы»
  принимает первый: список (список числа), второй: список (список числа)
  возвращает список (список числа)
  пример «Две половины склеиваются в порядке первый, потом второй»
    дано первый равно [[], [1]]
    дано второй равно [[2], [1, 2]]
    ожидается [[], [1], [2], [1, 2]]
  пример «Склейка с пустым набором»
    дано первый равно []
    дано второй равно [[7]]
    ожидается [[7]]
  свёртка второй начиная с первый как акк и эл → добавить эл к акк

тотальная функция «Дописать элемент во все»
  принимает наборы: список (список числа), значение: число
  возвращает список (список числа)
  пример «Тройка приписывается к каждому набору»
    дано наборы равно [[], [1], [2], [1, 2]]
    дано значение равно 3
    ожидается [[3], [1, 3], [2, 3], [1, 2, 3]]
  отобразить наборы как набор → добавить значение к набор

тотальная функция «Все подмножества»
  принимает элементы: список числа
  возвращает список (список числа)
  пример «Пример 1 из условия»
    дано элементы равно [1, 2, 3]
    ожидается [[], [1], [2], [1, 2], [3], [1, 3], [2, 3], [1, 2, 3]]
  пример «Пример 2 из условия»
    дано элементы равно [0]
    ожидается [[], [0]]
  пример «Пустой вход — одно пустое подмножество»
    дано элементы равно пустой список
    ожидается [[]]
  пример «Два элемента, в том числе отрицательный»
    дано элементы равно [-1, 2]
    ожидается [[], [-1], [2], [-1, 2]]
  свёртка элементы начиная с [пустой список] как акк и эл
    «Соединить наборы» от акк и («Дописать элемент во все» от акк и эл)
