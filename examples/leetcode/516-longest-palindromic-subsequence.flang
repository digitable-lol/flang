модуль «Longest palindromic subsequence»

// LeetCode 516. Longest Palindromic Subsequence.
// Найти длину самой длинной подпоследовательности строки, читающейся одинаково
// в обе стороны (буквы берутся по порядку, но не обязательно подряд).
//
// Приём: ДВУМЕРНАЯ таблица динамики, разложенная ПО ДЛИНАМ ОКОН, а не по границам.
// Ряд «окно» — ответы для всех подстрок этой длины: одна буква даёт единицу,
// совпавшие крайние — клетку на два ряда назад плюс два, иначе большую из двух
// клеток предыдущего ряда. Порядок заполнения выходит СТРОГО ВПЕРЁД: число в конец
// ряда, готовый ряд в конец таблицы, назад только чтение по двум номерам.
// Классическое заполнение по границам i и j писало бы в СЕРЕДИНУ готового ряда, то
// есть требовало замены элемента по номеру, которой в языке нет вовсе, — разложение
// по окнам обходит именно эту недостачу. Тотальность: меры «длина буквы минус окно
// минус длина ряд плюс 2» (ряды с ростом окна короче, потому в мере и «окно») и
// «длина буквы минус длина таблица плюс 2»; клетка — композиция.
// ЦЕНА (вторая недостача): приписывания в конец за постоянное время нет, «добавить»
// копирует список — O(n) в вычислителе и в семи целях из восьми, амортизированно
// O(1) только в C; ряд из k клеток стоит O(k²), треугольная таблица из n рядов —
// O(n³) копирования вместо честных O(n²).

тотальная функция «Клетка палиндрома»
  принимает буквы: список строки, окно: число, место: число, таблица: список (список числа)
  возвращает число
  обеспечивает «окно в одну букву — палиндром длины один» если окно равен 1 то (результат равен 1) иначе да
  пример «Окно в одну букву»
    дано буквы равно ["а", "б"]
    дано окно равно 1
    дано место равно 1
    дано таблица равно пустой список
    ожидается 1
  пример «Две одинаковые буквы»
    дано буквы равно ["б", "б"]
    дано окно равно 2
    дано место равно 1
    дано таблица равно [[1, 1]]
    ожидается 2
  пример «Две разные буквы»
    дано буквы равно ["а", "б"]
    дано окно равно 2
    дано место равно 1
    дано таблица равно [[1, 1]]
    ожидается 1
  пример «Крайние совпали через ряд назад»
    дано буквы равно ["б", "а", "б"]
    дано окно равно 3
    дано место равно 1
    дано таблица равно [[1, 1, 1], [1, 1]]
    ожидается 3
  если окно равен 1
    то 1
    иначе
      если (элемент место в буквы) равен (элемент (место плюс окно минус 1) в буквы)
        то если окно равен 2 то 2 иначе 2 плюс (элемент (место плюс 1) в (элемент (окно минус 2) в таблица))
        иначе
          пусть первый равно элемент место в (элемент (окно минус 1) в таблица)
          пусть второй равно элемент (место плюс 1) в (элемент (окно минус 1) в таблица)
          если первый больше второй то первый иначе второй

тотальная функция «Ряд палиндрома»
  принимает буквы: список строки, окно: число, таблица: список (список числа), ряд: список числа
  возвращает список числа
  убывает (длина буквы) минус окно минус (длина ряд) плюс 2
  пример «Первый ряд — одни единицы»
    дано буквы равно ["б", "б", "б", "а", "б"]
    дано окно равно 1
    дано таблица равно пустой список
    дано ряд равно пустой список
    ожидается [1, 1, 1, 1, 1]
  пример «Второй ряд для bbbab»
    дано буквы равно ["б", "б", "б", "а", "б"]
    дано окно равно 2
    дано таблица равно [[1, 1, 1, 1, 1]]
    дано ряд равно пустой список
    ожидается [2, 2, 1, 1]
  если (длина ряд) не меньше ((длина буквы) минус окно плюс 1)
    то ряд
    иначе
      пусть место равно (длина ряд) плюс 1
      пусть новое равно «Клетка палиндрома» от буквы и окно и место и таблица
      «Ряд палиндрома» от буквы и окно и таблица и (добавить новое к ряд)

тотальная функция «Таблица палиндрома»
  принимает буквы: список строки, таблица: список (список числа)
  возвращает список (список числа)
  убывает (длина буквы) минус (длина таблица) плюс 2
  пример «Треугольная таблица для cbbd»
    дано буквы равно ["ц", "б", "б", "д"]
    дано таблица равно пустой список
    ожидается [[1, 1, 1, 1], [1, 2, 1], [2, 2], [2]]
  если (длина таблица) не меньше (длина буквы)
    то таблица
    иначе
      пусть окно равно (длина таблица) плюс 1
      пусть новый равно «Ряд палиндрома» от буквы и окно и таблица и пустой список
      «Таблица палиндрома» от буквы и (добавить новый к таблица)

тотальная функция «Длина палиндромной подпоследовательности»
  принимает текст: строка
  возвращает число
  пример «Пример 1 из условия»
    дано текст равно "bbbab"
    ожидается 4
  пример «Пример 2 из условия»
    дано текст равно "cbbd"
    ожидается 2
  пример «Одна буква»
    дано текст равно "a"
    ожидается 1
  пример «Все буквы разные»
    дано текст равно "abcde"
    ожидается 1
  пример «Целиком палиндром»
    дано текст равно "aabaa"
    ожидается 5
  пример «Пустая строка»
    дано текст равно ""
    ожидается 0
  пусть буквы равно разложить текст на символы
  если пусто буквы
    то 0
    иначе
      пусть таблица равно «Таблица палиндрома» от буквы и пустой список
      пусть конец равно длина буквы
      если конец больше 0 и притом конец не больше (длина таблица)
        то
          пусть последний равно элемент конец в таблица
          если (длина последний) не меньше 1
            то элемент 1 в последний
            иначе 0
        иначе 0
