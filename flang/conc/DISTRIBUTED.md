# Распределённость — контракт

Второй половины OTP у нас не было вовсе: процессы, надзор, пороги и планировщики
жили внутри одного процесса операционной системы, и «узел» был словом из чужой
документации. Здесь эта половина начата — не целиком, а ровно настолько, чтобы
каждое слово было предъявлено прогоном: два узла, письмо через границу, надзор
через границу и **разрыв связи, который рвётся по-настоящему**.

Контракт модели конкурентности — `flang/conc/SPEC.md`; путь к
отказоустойчивости — `flang/conc/RESILIENCE.md`. Оба уже приняли решения, и
здесь они не переизобретаются, а продолжаются. В границе честности `SPEC.md`
стояло:

> *Распределённости.* Узлы, `:net_kernel`, прозрачная отправка на другую
> машину — вне модели.

Эта строка перестала быть правдой ровно наполовину, и половину надо назвать.

## Как читать этот документ

У каждого утверждения ровно одна пометка из четырёх, и они не взаимозаменяемы:

* **сделано и испытано** — работает в `flang/conc/distributed.mjs` и предъявлено
  прогоном в `flang/conc/distributed.test.mjs`. Не «написано и выглядит
  правильно», а «есть проверка, которая покраснеет, если сломать»;
* **объявлено, не сделано** — решение принято и записано, кода под ним нет либо
  есть, но без прогона. Такое утверждение ничего не обещает;
* **отвергнуто** — не будет никогда, и рядом стоит довод. Это НЕ то же, что
  «объявлено, не сделано»: помечать отвергнутое вторым способом значило бы
  соврать, только вежливо;
* **названо, не устранено** — расхождение известно, измерено и оставлено
  нарочно, потому что устранять его дороже, чем знать о нём. Такая пометка
  обязана нести, на каких входах расхождение случается.

Это не украшение, а правило языка: у flang ведомость доказательства печатается
четырьмя словами (`flang/src/proof.mjs`), и «объявлено, не доказано» там значит
ровно то же, что здесь «объявлено, не сделано». Сводная ведомость — в конце.

Испытание запускается одной командой:

    node --test flang/conc/distributed.test.mjs

Тринадцать прогонов, около сорока секунд, вся сеть внутри `127.0.0.1`.

Одиннадцатое испытание лежит не здесь: анализ отказов сокетов не требует, значит
и держать его среди прогонов с узлами незачем. Восемь прогонов на седьмой вид
отказа стоят в `flang/test/failures.test.mjs` — то есть в шаблоне
`npm test`, а не сбоку от него.

## Что сделано, в трёх строках

Узел — процесс операционной системы, несущий **подмножество** объявленных
процессов программы. Программа при этом **одна и та же на всех узлах, до
байта**: распределённость не добавила языку ни одного слова. Кто где живёт,
сказано размещением (`flang/conc/examples/distributed.json`) — данными узла, а
не текстом программы.

    node flang/conc/bin/node.mjs --размещение flang/conc/examples/distributed.json \
      --я «учёт» --слушать 127.0.0.1:9002
    node flang/conc/bin/node.mjs --размещение flang/conc/examples/distributed.json \
      --я «счёт» --слушать 127.0.0.1:9001 --сосед «учёт»=127.0.0.1:9002

Дальше — три вопроса, на которых ломаются все такие системы, и наши ответы на
них. Ответы названы прямо, вместе с тем, чего они стоят.

---

# Вопрос первый: ИМЯ

## Решение: имя не меняется вовсе, потому что менять его нечему

**Процесс на узле А называет процесс на узле Б ровно так же, как назвал бы
соседа по своему узлу: строкой из объявления.**

```flang
пусть доклад равно (вариант «отправить» с «кому» равным "Учётчик" и «что» равным …)
```

Эта строка в `flang/conc/examples/distributed.flang` не знает и не может знать,
на каком узле живёт «Учётчик». Составного адреса вида `{имя, узел}`, каким
адресуют в BEAM, здесь нет — и не потому, что мы поленились, а потому, что
составлять нечего:

* **множество процессов закрыто объявлением.** `породить` в модели нет
  (`SPEC.md`, «Граница честности»), набор процессов задан текстом программы, и
  два процесса с одним именем — ошибка проверки, а не гонка в рантайме;
* значит **имя уже уникально во всей программе**, а не в пределах узла. Ему
  нечего добавлять, чтобы стать адресом: оно уже адрес;
* значит **регистрация имени не нужна**. В OTP `{имя, узел}` существует потому,
  что pid динамичен, а имя регистрируется во время работы и может быть занято,
  освобождено, перерегистрировано на другой процесс. Здесь имя — константа
  времени компиляции.

Прозрачность размещения досталась не механизмом, а тем, что её **нечем
нарушить**: тот же текст программы, тот же напечатанный модуль, тот же хэш.
*Сделано и испытано:* в испытании 1 оба узла печатают один и тот же хэш плана, и
это же испытание проверяет, что письмо, посланное строкой `"Учётчик"`, меняет
состояние процесса на другом узле.

## Размещение — данные узла, а не текст программы

```json
{
  "программа": "flang/conc/examples/distributed.flang",
  "узлы": {
    "счёт": { "слушать": "127.0.0.1:0", "процессы": ["Счётчик"],
              "звонить": { "учёт": "127.0.0.1:0" } },
    "учёт": { "слушать": "127.0.0.1:0", "процессы": ["Учётчик"] }
  }
}
```

Почему не в программе. Потому что размещение — это решение эксплуатации, а не
свойство задачи: тот же «Счётчик» на испытательном стенде живёт вместе с
«Учётчиком», а в работе — врозь, и переписывать ради этого исходник значило бы
пересобирать программу ради смены хоста. И потому, что новое слово в языке
пришлось бы вводить в `parser.mjs` и `types.mjs` — файлы, которые в этой работе
не наши; ограничение оказалось не помехой, а редкой удачей: оно заставило искать
решение, при котором язык не меняется вовсе, и решение нашлось.

*Сделано и испытано:* размещение обязано назвать место **каждому** объявленному
процессу — узел с процессом без места не поднимается (испытание 9), а процесс,
названный дважды, отвергается сборкой размещения (испытание 8): имя, значащее
два места, перестало быть адресом. Узел без единого своего процесса тоже не
поднимается и говорит об этом в журнал (испытание 7).

## Что происходит с именем, когда узел исчез

**С именем — ничего.** Оно не ссылка, не pid и не описатель: оно константа,
и мёртвый узел не делает её недействительной. Меняется не имя, а **достижимость**,
и достижимость — отдельное, наблюдаемое, надзираемое понятие:

> У каждого процесса, размещённого на другом узле, на этом узле есть
> **представитель** — запись в той же таблице процессов, у которой нет ни
> состояния, ни ящика, ни обработчика. У него есть только связь, и он умеет
> ровно одно: **отказать**, когда связь потеряна.

Отсюда всё остальное встаёт на место само:

| | процесс | представитель |
|---|---|---|
| состояние | значение, перезапуск возвращает начальное | нет |
| ящик | есть | нет |
| обработчик | есть | нет |
| отказы | шесть видов из `SPEC.md` | один, седьмой: `FLANG_LINK_DOWN` |
| надзор | обычный | **тот же самый обычный** |
| надзор ЗАБЫТ | ошибка сборки | **тоже ошибка сборки** (с этой правки) |

Последняя строка — весь смысл приёма. Надзор через границу узла не потребовал ни
нового понятия, ни новой стратегии, ни новой строки в языке: представитель стоит
в таблице процессов, значит его накрывает обычный `надзор`, объявленный обычными
тремя стратегиями с обычным порогом.

*Сделано и испытано:* испытание 2 — разрыв роняет представителя кодом
`FLANG_LINK_DOWN`, и объявленный в программе надзор принимает по нему решения.

## Чего имя не умеет, и это не «пока»

* **Переселения нет.** Процесс живёт на том узле, который назначен размещением;
  перенести его на другой узел, не остановив систему, нечем. *Объявлено, не
  сделано.*
* **Маршрутизации нет.** Связь только прямая: полный граф или ничего. Письмо,
  пришедшее узлу, на котором адресат не живёт, пропадает и записывается в журнал
  как `потеря` — молча выбросить его значило бы спрятать ошибку размещения.
  *Объявлено, не сделано:* и маршрутизация, и прогон на эту запись (размещение
  проверяется при подъёме, поэтому подстроить такое письмо, не ломая узел
  нарочно, испытанию сегодня нечем).
* **Динамического состава узлов нет.** Кто с кем говорит, известно из
  размещения при подъёме. Ни обнаружения соседей, ни присоединения на ходу.
  *Объявлено, не сделано.*
* **Имя значимо только внутри одной программы**, и это не оговорка, а угроза:
  два узла с разными программами, договорившись по именам, доставляли бы письма
  не тем процессам. Поэтому в рукопожатии едет **sha256 напечатанного модуля**,
  и несовпадение — отказ рукопожатия. *Сделано и испытано* (испытание 5).

---

# Вопрос второй: ОТКАЗ

## Сначала — то, что не наша беда, а доказанный факт

Разрыв связи неотличим от медленного узла. Это не недостаток нашей реализации и
не то, что чинится лучшим кодом: узел, который молчит, и узел, до которого нет
пути, посылают одно и то же — **ничего**. Отличить «ничего, потому что умер» от
«ничего, потому что занят» нельзя ничем, кроме как подождав, а «подождав» — это
уже выбор срока.

Значит выбор придётся сделать, и прятать его нельзя. Вот он.

## Решение: мы объявляем отказ СВЯЗИ, а не смерть узла

**Мы никогда не утверждаем, что узел мёртв.** Утверждается ровно одно, и оно
записано прямо в тексте отказа:

    связь с узлом «учёт» потеряна: молчание 1013 мс при сроке 1000 мс

Это утверждение о ЗДЕШНЕМ узле («отсюда туда пути нет»), а не о том. Код отказа
поэтому `FLANG_LINK_DOWN`, а не `FLANG_NODE_DEAD`: второе было бы неправдой,
которую нечем проверить.

## Коды, названные здесь и не существующие

Имя `FLANG_NODE_DEAD` только что названо, а кода такого нет и не будет. Пока это
видно лишь из абзаца, читателю оно ничем не отличается от кода, который просто
ещё не написали, — а разница огромная. Поэтому имя стоит в таблице той же формы,
что ведомость в конце документа, и с тем же смыслом: обещание читателю обязано
быть ПОМЕЧЕНО, а не подразумеваться.

| код | вердикт | почему |
|---|---|---|
| `FLANG_NODE_DEAD` | **отвергнуто** | смерть узла утверждать нечем: молчание умершего и молчание занятого неотличимы, и отличить их нельзя ничем, кроме как подождав. Отказ объявляется о СВЯЗИ, и это утверждение о здешнем узле — проверяемое |

Вердиктов ровно два, и «отвергнуто» — не то же, что «объявлено, не сделано»:
первое говорит «не будет никогда», второе — «будет, но пока нет». Помечать
отвергнутое вторым способом значило бы соврать, только вежливо.

Стережёт это `flang/test/codes.test.mjs`: каждое имя `FLANG_*`, названное прозой
дерева, обязано либо существовать в исходниках, либо стоять в таком списке — с
вердиктом и с доводом. Стереть упоминание и промолчать третьим путём не
считается: обещание, тихо убранное из документа, остаётся ложью, но теперь
незаметной.

## Через сколько: числа, и они настраиваются

| | по умолчанию | что значит |
|---|---|---|
| `пульс` | 200 мс | как часто узел шлёт признак жизни, когда сказать нечего |
| `срок` | 1000 мс | сколько молчания считается потерей связи |
| `пауза` | 250 мс | сколько ждать перед попыткой дозвониться после решения надзора «перезапустить» |

Все три — доводы узла (`--пульс`, `--срок`, `--пауза`), а не константы кода, и
это часть решения: срок обнаружения отказа обязан зависеть от сети, в которой
узел стоит, а не от мнения автора рантайма. Константы в
`flang/conc/distributed.mjs` (`СРОКИ`) — умолчание, а не закон.

**Пять пульсов на срок.** Одна потерянная посылка не должна ронять связь; пять
подряд — уже событие. Устройство то же, что у BEAM: там тик уходит заметно чаще
срока, и срок этот по умолчанию — 60 секунд. Наш в шестьдесят раз короче, и это
не потому, что мы смелее: у нас петля, а у BEAM в уме сеть между машинами.
Число обязано пересчитываться под сеть, и потому оно довод, а не константа.

**Почему тысяча, а не сто и не десять тысяч.** Оборот «туда-обратно» по петле на
этой машине измерен, а не оценён на глаз:

| | 2000 оборотов, прогон 1 | прогон 2 |
|---|---|---|
| медиана | 38,4 мкс | 40,5 мкс |
| 99-й процентиль | 250,1 мкс | 195,1 мкс |
| худший | **5065,6 мкс** | **4829,4 мкс** |
| лучший | 30,8 мкс | 31,3 мкс |

Худший случай — вот ради чего замер и делался. На **петле**, без сети, без
коммутаторов, без чужого трафика один оборот из двух тысяч занял пять
миллисекунд: в 130 раз больше медианы. Срок, выбранный по медиане, объявлял бы
отказ примерно раз в две тысячи посылок на совершенно здоровой связи. Срок в
1000 мс — это 26 000 медиан и 200 худших случаев, и на локальной петле он
избыточен ровно настолько, чтобы ложные отказы были не «редки», а «не
наблюдались ни разу за всё время работы».

*Сделано и испытано:* испытание 3 замораживает провод — сокеты целы, узлы живы,
байты копятся — и требует, чтобы узел объявил связь потерянной **не раньше**
объявленного срока, и чтобы причиной было названо молчание, а не событие сети.
Измерено в прогонах: 983–1013 мс от заморозки.

Условия замера названы, потому что без них числа не числа: Linux 7.0.0-29,
256 ядер, 512 ГиБ, Node v26.7.0, всё внутри `127.0.0.1`, машина не свободна.

## Немедленный отказ: закрытый сокет

Кроме молчания есть второй повод, и он мгновенный: сокет закрылся или отказал.
Это законно **при нашем определении отказа**: мы объявляем потерю СВЯЗИ, а
закрытый сокет — это и есть потеря связи, без всякой неопределённости. Если бы
мы объявляли смерть узла, такой вывод был бы неправдой: сокет закрывается и у
живого соседа.

*Сделано и испытано:* испытание 2 — перекушенный провод роняет связь за
миллисекунды, без ожидания срока.

## Что из этого следует, и это главная цена

**Разделение мозга у нас ВОЗМОЖНО, и мы выбрали его сознательно.** Ни кворума,
ни членства, ни ограждения (fencing), ни арбитра: узел, объявивший связь
потерянной, действует по своему надзору, а узел на том конце — по своему, и оба
правы. Мы выбрали **доступность вместо исключительности**.

Что это значит для автора программы, прямым текстом:

* **программа, которой нужна исключительность, на этой распределённости не
  пишется.** Один хозяин ресурса, единственный держатель блокировки,
  «печатаем чек ровно один раз» — всё это здесь сделать нельзя, и притворяться,
  что можно, мы не будем. Нужно — понадобится кворум, а его нет;
* **ложный отказ не отменяется.** Если связь вернулась, а надзор уже принял
  решение, решение остаётся принятым: перезапуск не «отменяет» отказ, а заводит
  связь заново. Отменяемых решений в надзоре нет нигде в модели, и заводить их
  ради связи было бы отдельным механизмом с отдельной ценой.

*Сделано и испытано:* испытание 2 предъявляет разделение мозга целиком. Провод
перекушен; узел «счёт» объявил отказ, отработал по объявленной стратегии и
остановился весь; узел «учёт» **всё это время был жив, здоров и сохранил
состояние** — прогон спрашивает его состояние после смерти соседа и сверяет с
тем, что было до разрыва. Ни один из двух не ошибся: оба выполнили объявленное.

*Сделано и испытано:* испытание 3 идёт дальше и предъявляет то, что обычно
только описывают словами: провод в момент объявления отказа был **цел** и
закрылся **после** — потому что трубку положил объявивший. То есть разрыв не
случился с нами, мы его **объявили**.

## Как отказ доходит до надзора

Тем же путём, что любой другой отказ процесса, и это не экономия строк.

```
связь потеряна
  → представители процессов того узла отказывают кодом FLANG_LINK_DOWN
    → надзорВДействии() из flang/src/conc.mjs — ТОТ ЖЕ КОД, что у прогона свидетеля
      → стратегия / порог / «передать выше» / «отказ дошёл доверху»
```

`надзорВДействии` вынесен из `runConcurrent` отдельной функцией именно ради
этого: **надзор обязан остаться одним**. Второй экземпляр тех же двадцати строк
разошёлся бы с первым на первой же правке порога, и «надзор через границу узла»
означал бы не то же самое, что надзор внутри узла. Подставляются ровно два
действия: «перезапустить» для местного процесса — вернуть начальное состояние,
для представителя — **завести связь заново**.

| стратегия | над процессом | над представителем |
|---|---|---|
| `перезапустить` | вернуть начальное состояние | дозвониться заново (через паузу) |
| `остановить` | процесс мёртв | больше не звонить; письма туда пропадают молча |
| `передать выше` | надзору над надзором | **то же самое** |

Из трёх строк этой таблицы испытаны две. Стратегия `остановить` над
представителем в прогонах не случается: программа примера объявляет за связью
`перезапустить`, а порог уводит отказ выше. Ветка написана и коротка — не
звонить, письма отвечают «некому», — но прогона на неё нет. *Объявлено, не
сделано.*

*Сделано и испытано:* испытание 2 требует ТОЧНОЙ последовательности решений,
объявленной в программе, — `Связь/перезапустить`, `Связь/перезапустить`,
`Связь/передать выше`, `Цех/передать выше` — и исхода «отказ дошёл доверху».
Каждый перезапуск при этом — **настоящая попытка дозвониться**, а не отметка в
журнале: её неудача и есть следующий отказ в окне порога, и прогон проверяет,
что три отказа не уложились в паузу.

## Чего у обнаружения нет

* **Потолка задержки нет.** Отказ объявляется не позже, чем через `срок +
  пульс` после последнего принятого байта, но письмо, ушедшее в порванную связь,
  не отказывает никогда — оно просто пропадает (см. вопрос третий).
* **Отказ при заводе связи отказом не считается.** Потерять можно только то, что
  было: узел, чей сосед ещё не поднялся, ждёт и звонит снова, не будя надзор. Без
  этого правила порог отказов сгорал бы на старте, когда узлы поднимаются не
  одновременно. Цена названа: сосед, который не поднимется никогда, будет
  ожидаться вечно и молча. *Сделано и испытано* (испытание 4: полторы секунды
  звонков в пустоту при паузе 100 мс — больше десятка неудачных попыток — и ноль
  записей об отказе; а когда сосед появился, связь завелась сама), *срока на
  первое знакомство — объявлено, не сделано.*
* **Здоровье узла не проверяется** — только достижимость. Узел, у которого все
  процессы мертвы, а сокет цел, считается достижимым. *Объявлено, не сделано.*

---

# Вопрос третий: ДОСТАВКА

## Решение: НЕ БОЛЬШЕ ОДНОГО

Ровно один раз недостижимо, и выбирать приходится из двух. Мы выбрали
**«не больше одного»** (at-most-once): сообщение через границу узла либо
доставляется ровно один раз, либо не доставляется вовсе, и отправитель об этом
не узнаёт.

Три довода, и все три — про устройство языка, а не про удобство реализации.

**Первый: «не меньше одного» ломает обработчик.** Обработчик flang чист и
возвращает новое состояние значением; повторная доставка того же письма
складывается в состояние дважды. Чтобы это было безопасно, каждый обработчик
обязан был бы стать идемпотентным — а идемпотентность в языке **нечем выразить и
нечем проверить**. Модель везде устроена наоборот: `тотальная` проверяется,
запас витков проверяется, полнота разбора проверяется, покрытие отказов
проверяется. Переложить на автора обязанность, которую язык не умеет ни описать,
ни проверить, — это ровно то, чего модель избегает во всех остальных местах.

**Второй: местная отправка УЖЕ «не больше одного».** `положить` возвращает
`некому`, когда адресат мёртв, и сообщение пропадает молча — так с первого шага,
и так же в BEAM. Другая гарантия через границу означала бы, что **смысл
`отправить` зависит от размещения**, то есть прозрачность имени, ради которой всё
и затевалось, сломана в первом же протоколе.

**Третий: за «не меньше одного» пришлось бы платить тем, чего у нас нет.**
Номера сообщений, таблица виденного у получателя, подтверждения, повторы,
срок хранения неподтверждённого — это состояние, которое живёт **не в процессе**
и потому не принадлежит никому: ни обработчику (он чист), ни ящику (он очередь
значений). Заводить его пришлось бы отдельным механизмом с отдельным
жизненным циклом, и это была бы вторая модель рядом с первой.

## Что из этого следует автору — по пунктам

1. **Письмо через границу может пропасть, и `отправить` об этом не отказывает.**
   Единственный след — запись `потеря` в журнале узла, то есть сведения для
   эксплуатации, а не для программы. *Сделано и испытано:* испытание 3 —
   «замерить 40» уходит в замороженный провод без ошибки, не доходит никогда, и
   потеря видна в состоянии обоих процессов: у «Счётчика» `всего` 142, а
   `подтверждено` 2.
2. **Значит любой протокол «спросил — жду ответа» обязан иметь свой срок.** И он
   в языке есть: `через N отправить` плюс состояние «чего я жду». Модель уже
   вынудила автора держать это состояние — выборочного приёма нет, `отложить`
   стоит видимых денег (`SPEC.md`, «Почтовый ящик») — и здесь это окупается:
   протокол, написанный правильно для одного узла, готов к двум. *Объявлено, не
   сделано:* `через` на узле заведено настоящим таймером хозяина, но ни один
   прогон таймера не заводит, и примера такого протокола в каталоге нет —
   значит обещать тут нечего.
3. **Повторов не бывает.** Обработчик НЕ обязан быть идемпотентным, и это
   положительная половина выбора, которую тоже надо назвать. Счётчик, который
   прибавляет к состоянию, — законная программа.
4. **Порядок сохраняется между парой, пока связь цела** (его даёт TCP), и **не
   восстанавливается после разрыва**: часть писем потеряна навсегда, новая связь
   начинает с чистого листа. Дыру в потоке мы не нумеруем и не обнаруживаем —
   значит **автор не может отличить «пришло не всё» от «пришло всё»** иначе, как
   своим протоколом. *Сделано и испытано* (порядок до разрыва — испытание 1;
   потеря при разрыве — испытания 2 и 3), *обнаружение дыры — объявлено, не сделано.*
5. **Ящик за границей узла потолка не соблюдает.** Отказ `FLANG_MAILBOX_FULL`
   (шаг А3 карты) достаётся ОТПРАВИТЕЛЮ — а отправитель на другом узле, и
   отказать ему в тот момент, когда ящик полон, нечем. Поэтому письмо, пришедшее
   в полный ящик по проводу, **пропадает** и записывается в журнал как потеря.
   Обратное давление (backpressure) через границу узла не работает. *Объявлено,
   не сделано:* ветка написана, но прогона на переполненный через границу ящик
   нет, и потому обещать её поведение нельзя.

## Значения на проводе

Кадр — строка JSON и перевод строки; видов ровно четыре: `привет`, `письмо`,
`пульс`, `отбой`. У каждого ЗНАЧЕНИЯ внутри письма метка одной буквой: `ч`
число, `с` строка, `п` признак, `н` ничто, `л` список, `з` запись, `в` вариант.

Голый JSON здесь не годится по двум причинам, и обе настоящие:

* **запись и вариант в JSON неразличимы.** Запись flang — обычный объект,
  вариант — тоже; служебное поле-метка могло бы столкнуться с полем автора, потому
  что имена полей в flang произвольны;
* **числа языка — IEEE-754**, а JSON не умеет ни `NaN`, ни бесконечностей, ни
  минус нуля и превратил бы их в `null` молча.

Цена посчитана, а не оценена: кадр с вариантом «записать» и строкой «142» —
**76 байт** с метками против **85** без них (метки дешевле: слова `variant` и
`fields` длиннее любой из них); запись `{всего, подтверждено}` — **48 байт**
против **30**, то есть в 1,6 раза дороже. Общего множителя нет, и называть его
было бы выдумкой.

*Сделано и испытано:* испытание 6 гоняет значения туда-обратно, включая `NaN`,
обе бесконечности и минус ноль, и требует, чтобы вариант вернулся **классом
напечатанного модуля**, а не похожим объектом: обработчик отвергает похожий
объект отказом `FLANG_MATCH_NOT_EXHAUSTIVE`, и это предъявлено, а не
предположено.

## Сколько стоит письмо через границу

| | на оборот |
|---|---|
| голый TCP по петле (медиана) | 38,4 мкс |
| оборот через границу узла flang | **85–99 мкс** |

Измерено на 2000 и 5000 оборотов (98,5 / 90,4 / 87,3 мкс на 2000 и 85,1 мкс на
5000); оборот здесь — это письмо туда и ответ обратно, то есть два пробега
обработчика, два кодирования, два раскодирования и два решения планировщика
сверх сети. Накладные расходы модели — примерно 50–60 мкс на оборот, и это
число, а не впечатление.

---

# Устройство: что где лежит

| файл | что |
|---|---|
| `flang/conc/DISTRIBUTED.md` | этот контракт |
| `flang/conc/distributed.mjs` | узел: таблица процессов, представители, связь, сторож, планировщик |
| `flang/conc/bin/node.mjs` | передний край: доводы, печать программы, журнал наружу, команды снаружи |
| `flang/conc/bin/wire.mjs` | **провод** — связь, которую можно порвать, заморозить и починить снаружи |
| `flang/conc/examples/distributed.flang` | программа: «Счётчик» и «Учётчик» |
| `flang/conc/examples/distributed.json` | размещение |
| `flang/conc/distributed.test.mjs` | испытание: десять прогонов |
| `flang/src/failures.mjs` | седьмой вид отказа и `отказыСвязи`: анализ, который читает размещение |
| `flang/src/conc.mjs` | `надзорВДействии`, `построитьНадзор`, `LINK_DOWN_CODE` — общие со свидетелем |

Что тронуто ЗА пределами этого каталога, и почему ровно это:

* `flang/src/conc.mjs` — `надзорВДействии` вынесен из `runConcurrent` наружу
  (чистый вынос: журналы прогонов не изменились ни на байт, 79 проверок
  `conc.test.mjs` и `failures.test.mjs` зелены), плюс экспортированы помощники и
  добавлен `LINK_DOWN_CODE`;
* `flang/src/failures.mjs`, `flang/src/types.mjs`, `flang/bin/flang.mjs` — седьмой
  вид отказа и второй вход у анализа. Тронуто ровно столько: вид с кодом,
  `отказыСвязи`, необязательный второй довод у `checkTypes` и ключ
  `--размещение` у `check`. Печать, планировщик и прогон свидетеля не изменились ни
  на байт — программа без размещения проверяется тем же кодом и с тем же
  результатом, что вчера;
* `flang/test/proof.test.mjs` и `docs/overview.ru.md` — три числа свода корпуса:
  новая программа в `conc/examples/` добавила корпусу четыре тотальные функции
  без рекурсии (2677 → 2681), и свод обязан это назвать. Это ритуал дерева, а не
  правка чужой работы: тот же прогон и говорит, какое число вписать.

**Печать не тронута.** `flang/src/emit/js.mjs` не изменился ни на байт — и это
не аккуратность, а условие: узел обязан работать с тем, что цель печатает
СЕГОДНЯ, иначе «распределённость у цели JavaScript» значило бы «у нашей ветки
цели JavaScript».

## Чем узлом управляют и чем за ним смотрят

Журнал уходит в `stdout` по строке JSON на событие — и это не отладочная печать,
а единственное окно наружу: испытание смотрит именно им, увидел ли надзор отказ.
Виды записей: `поднят`, `связь`, `пробег`, `отказ`, `решение`, `потеря`,
`вброшено`, `состояния`, `связи`, `беда`, `конец`.

Команды приходят в `stdin`, тем же видом:

    {"в":"вбросить","кому":"Счётчик","что":["в","замерить",{"сколько":["ч",2]}]}
    {"в":"состояния","метка":"до обрыва"}
    {"в":"связи"}
    {"в":"стоп"}

Значение во `вбросить` подаётся ЗАКОДИРОВАННЫМ — тем же кодом, что ходит по
проводу. Второй способ построить значение означал бы второй способ ошибиться, а
так внешний мир говорит с узлом ровно тем же языком, что и соседний узел.

*Сделано и испытано:* всеми прогонами со стендом — иначе им нечем было бы ни
подать сообщение, ни увидеть, что вышло.

## Почему провод — отдельная программа

Затем, что «узел умер» и «связь порвалась» — **разные события**, и вся честность
этого документа держится на том, что различить их нечем. Испытание, которое
рвёт связь убийством узла, проверяет только первое и молча притворяется, что
проверило второе. Провод стоит между узлами, и рвётся ровно он: оба узла при
этом живы, и видно, что один объявил отказ, а другой в тот же момент работал.

У провода четыре команды порчи и починки, и третья — самая важная:

* `резать` — порвать всё и не принимать новых: перекушен;
* `чинить` — принимать снова;
* `морозить` — **не передавать байты, но держать сокеты открытыми и всё
  копить**. Ни одного признака отказа, кроме тишины, — а тишина неотличима от
  медленного узла. Только этим и можно испытать выбранный СРОК, а не умение
  заметить закрытый сокет;
* `оттаять` — отдать накопленное.

---

# Расхождения с контрактом конкурентности

Узел — не прогон, и в пяти местах он ведёт себя иначе. Все пять названы здесь, а
не обнаружены потом.

**1. Виртуального времени нет.** Прогон свидетеля живёт по виртуальному времени и
скачет к ближайшему сроку таймера, когда делать нечего (`SPEC.md`,
«Воспроизводимость»). Двум узлам разделить одно виртуальное время нечем, поэтому
узел живёт по настоящим часам. *Сделано и испытано* — в той части, что видна:
часы узла настоящие, и на них меряется срок связи (испытание 3). `через N` как
настоящая задержка в N миллисекунд — *объявлено, не сделано:* написано, не
испытано.

**2. Порог отказов меряется настоящими миллисекундами.** В прогоне свидетеля окно
порога считается ПРОБЕГАМИ (`SPEC.md`, «Что сделано в шаге 3»: «порог считается
пробегами, а не миллисекундами»), потому что виртуальное время идёт по пробегу
на пробег. На узле `порог отказов 2 за 5000 миллисекунд` наконец значит ровно
то, что написано. Одно объявление, два прибора — и об этом надо знать, потому
что программа, отлаженная на прогоне, на узле получит другое окно. *Сделано и
испытано* (испытание 2: три отказа разнесены на ~800 мс НАСТОЯЩЕГО времени, и
порог отработал ровно на третьем, как объявлено).

**3. Воспроизводимость по семени есть ВНУТРИ узла и её нет за границей.** Семя
упорядочивает готовые процессы внутри узла и делает это ровно так же, как
свидетель. Но порядок прихода писем выбирает сеть, а не семя, поэтому побайтовая
сверка журналов (`SPEC.md`, «Что проверяется и чем», пункт 2) через границу узла
**неприменима** — ровно по той же причине, по которой она неприменима к BEAM.
Испытание проверяет ИСХОДЫ и решения надзора, а не журнал доставок.

*Сделано и испытано* (испытание 11): размещение ставит оба процесса примера на
ОДИН узел — программа при этом не меняется ни на байт, — сокетов, пульса и
таймеров нет вовсе, значит источник чередования ровно один. Шесть семян, по два
прогона на семя, 12 замеров и 36 доставок в журнале: внутри семени журналы
совпадают побайтово, между семенами вышло **6 разных журналов из 6**. Вторая
половина обязательна: без неё первая зелена и у генератора, который всегда
возвращает ноль.

*Измерено, а не предположено* (испытание 12): «по-видимому, невозможно» про
сверку ЧЕРЕЗ границу заменено числом. Четыре прогона стенда с одним семенем дали
**1 исход и 4 разных сшитых журнала доставок**. Порядок, в котором записи двух
узлов легли рядом, выбрал наблюдатель, а не система: общих часов у двух узлов
нет, и сшивать их нечем. Сверять побайтово нечего — не «трудно», а нечего.

**4. Витков на пробег в журнале узла нет.** Предел витков ставится тем же
прибором, что у обычной напечатанной программы — счётчиком модуля через
`$newContext`, — но показание счётчика напечатанный модуль наружу не отдаёт.
Значит запас витков на узле ставится, а измерить задержку пробега нечем.
*Объявлено, не сделано:* ни предела, ни его отсутствия прогон не проверяет —
обработчики примера тотальны и коротки.

**5. Седьмой вид отказа ВХОДИТ в замкнутое множество. Был самый крупный долг
здешних — закрыт.** `SPEC.md` перечисляет виды отказа процесса и доказывает, что
множество замкнуто и вычислимо, а непокрытый достижимый отказ — ошибка сборки
(`FLANG_UNCOVERED_FAILURE`).

Здесь стояло: «`FLANG_LINK_DOWN` этого не ломает — им отказывает не процесс, а
представитель, — но и не входит в анализ; значит автор, забывший накрыть надзором
представителя чужого процесса, не получит ошибки сборки, хотя за таким же местным
процессом она бы пришла». Это было правдой, и правдой дорогой: обещание языка
кончалось ровно на той границе, за которой оно нужнее всего.

*Улика, снятая до правки, а не описанная.* Два процесса, безотказных ПО ТЕКСТУ
(оба тотальны, оценка витков постоянная, частичных форм нет, `остановить` нет,
ящик не объявлен). `flang check` — `"valid": true`, ноль диагностик, код 0. Узлы
поднялись, соседа убили, и журнал сказал
`{"в":"отказ","процесс":"Слушатель","код":"FLANG_LINK_DOWN"}` при **ноле решений
надзора**: процесс остался мёртвым до конца прогона, и решать об этом было
некому.

*Чем закрыто.* Вид седьмой в `src/failures.mjs` (`ВИДЫ_ОТКАЗА.СВЯЗЬ`, код берётся
из `conc.mjs`, а не переписывается) и `отказыСвязи(программа, размещение)`. Ответ
на вопрос «откуда анализ узнаёт размещение» такой: **доводом, а не из текста**.
Размещение — данные узла (см. выше), и класть его в исходник значило бы отменить
главное решение этого контракта. Поэтому входа два, и обойти их нечем:

* `flang check <файл> --размещение узлы.json` — сборка;
* подъём узла: `conc/bin/node.mjs` отдаёт проверке СВОЙ файл размещения, и на
  непокрытой программе узел не поднимается (код выхода 1, запись `проверка` с
  `FLANG_UNCOVERED_FAILURE`). Мимо этого пути распределённой программы не бывает
  вовсе, значит забыть ключ можно, а обойти проверку — нет.

Правило: процесс отказывает связью тогда и только тогда, когда в размещении есть
ещё хотя бы один НЕСУЩИЙ узел, кроме его собственного. Узел без единого своего
процесса в счёт не идёт — он не поднимается вовсе (испытание 7), значит и
представителей не держит. Программе без размещения седьмой вид не приписывается:
на одном узле представителей не бывает, и требовать за них надзора значило бы
требовать обряда.

*И проверка не обрядовая — она требует того, что работает.* Та же улика с
дописанным надзором, те же два узла, тот же разрыв: **3 отказа и 4 решения
надзора**, кончая исходом «отказ дошёл доверху», против **1 отказа и 0 решений**
до правки. Ошибка сборки требует не строки в файле, а разбора отказа, которого
раньше не было ни у кого.

*Сделано и испытано:* испытание 10 здесь (узел не поднимается; с дописанным
надзором — поднимается) и восемь прогонов в `flang/test/failures.test.mjs`,
раздел «седьмой вид», — тот файл лежит в шаблоне `npm test`, поэтому
статическая половина испытана обычным прогоном дерева, а не только этим файлом.

---

# Что испытано, и чем

`node --test flang/conc/distributed.test.mjs` — тринадцать прогонов, ~40 секунд.

| # | прогон | что доказывает |
|---|---|---|
| 1 | письмо переходит границу и меняет состояние на той стороне | обмен в обе стороны; один хэш программы на двух узлах; у представителя нет состояния |
| 2 | провод перекушен: надзор увидел отказ и отработал | `FLANG_LINK_DOWN`; ТОЧНАЯ последовательность четырёх решений; три настоящих попытки дозвониться; «отказ дошёл доверху»; разделение мозга |
| 3 | провод заморожен: отказ объявлен по сроку | обнаружение по молчанию, а не по событию сети; срок соблюдён; провод был цел и закрылся ПОСЛЕ объявления; потеря письма видна в состоянии |
| 4 | узел ждёт соседа, которого ещё нет | несостоявшееся первое знакомство надзор не будит; связь заводится сама, когда сосед появился |
| 5 | узлы с разными программами связи не заводят | рукопожатие по хэшу напечатанного плана |
| 6 | значение переживает провод туда-обратно | метки значений; `NaN`, ±∞, −0; вариант классом модуля |
| 7 | узел не поднимается на программе без процессов | названный отказ в журнал и код выхода 1 |
| 8 | размещение отвергает процесс, названный дважды, и незнакомый узел | одно имя — одно место; довод перекрывает файл |
| 9 | узел не поднимается, если размещение молчит про процесс | имя обязано быть адресом |
| 10 | узел не поднимается, если над представителем нет надзора | `FLANG_LINK_DOWN` в анализе непокрытых отказов: забытый надзор через границу — ошибка сборки; с дописанным надзором тот же узел поднимается |
| 11 | чередование внутри узла воспроизводимо по семени | 6 семян × 2 прогона: внутри семени журнал доставок побайтово один, между семенами 6 разных из 6 |
| 12 | через границу повторяются исходы, а не порядок | 4 прогона одним семенем: 1 исход, 4 разных сшитых журнала — сверять побайтово нечего |
| 13 | поток, порезанный не по кадрам, доходит целыми письмами | границы кадров: 797 знаков резаны по 7, 10 кадров по счёту эталона, недописанный кадр дождан, а не выброшен |

Прогон, который стоит прочитать глазами, — второй. Вот его журнал, снятый с
живых узлов (время и адреса — настоящие, лишнее убрано):

```
[учёт] {"в":"поднят","адрес":"127.0.0.1:45053","хэш":"a28c29d85819","свои":["Учётчик"],"чужие":["Счётчик"]}
[счёт] {"в":"поднят","адрес":"127.0.0.1:41855","хэш":"a28c29d85819","свои":["Счётчик"],"чужие":["Учётчик"]}
[счёт] {"в":"связь","сосед":"учёт","что":"заведена"}
[счёт] {"в":"состояния","это":{"Счётчик":{"состояние":["з",{"всего":["ч",5],"подтверждено":["ч",2]}]}}}
[учёт] {"в":"состояния","это":{"Учётчик":{"состояние":["з",{"строки":["л",[["с","2"],["с","5"]]]}]}}}
                                  ── рвём провод ──
[провод] {"в":"провод","что":"перекушен","пар":1}
[счёт] {"в":"связь","часы":…000036,"сосед":"учёт","что":"потеряна","почему":"сокет закрыт"}
[счёт] {"в":"отказ","процесс":"Учётчик","код":"FLANG_LINK_DOWN"}
[счёт] {"в":"решение","процесс":"Учётчик","надзор":"Связь","стратегия":"перезапустить"}
[учёт] {"в":"отказ","процесс":"Счётчик","код":"FLANG_LINK_DOWN"}
[учёт] {"в":"решение","процесс":"Счётчик","надзор":"Цех","стратегия":"перезапустить"}
[счёт] {"в":"связь","часы":…000441,"сосед":"учёт","что":"потеряна","почему":"сокет отказал"}
[счёт] {"в":"решение","процесс":"Учётчик","надзор":"Связь","стратегия":"перезапустить"}
[счёт] {"в":"связь","часы":…000844,"сосед":"учёт","что":"потеряна","почему":"сокет отказал"}
[счёт] {"в":"решение","процесс":"Учётчик","надзор":"Связь","стратегия":"передать выше"}
[счёт] {"в":"решение","процесс":"Учётчик","надзор":"Цех","стратегия":"передать выше"}
[счёт] {"в":"конец","исход":"отказ дошёл доверху"}
[счёт] вышел 0
[учёт] {"в":"состояния","это":{"Учётчик":{"жив":true,"состояние":["з",{"строки":["л",[["с","2"],["с","5"]]]}]}}}
```

Последняя строка — та, ради которой всё это писалось. Узел «счёт» объявил отказ,
надзор отработал по объявленной стратегии и остановил узел целиком; узел «учёт» в
этот момент был жив и держал ровно то состояние, что и до разрыва. Оба сделали
то, что было объявлено. Никто не ошибся. И именно это — цена решения «отказ
связи, а не смерть узла», предъявленная, а не описанная словами.

---

# Граница честности: что это против распределённой BEAM

**Чего у нас может быть лучше.**

* *Имя не регистрируется и не протухает.* Множество процессов закрыто
  объявлением, значит имя уникально во всей программе по построению, а не по
  соглашению. В OTP `{имя, узел}` нужен потому, что имя регистрируется на ходу и
  может уехать на другой процесс.
* *Надзор через границу — тот же самый надзор.* Ни `:global`, ни
  `:pg`, ни отдельного механизма присмотра за узлами: представитель стоит в
  таблице процессов, и его накрывают те же три стратегии и тот же порог. В OTP
  присмотр за узлом (`monitor_node`) — отдельное понятие, живущее рядом с
  супервизорами, а не внутри них.
* *Программа не знает о распределённости ни слова.* Ноль новых слов языка. Тот
  же текст идёт на один узел и на два, и это проверяется прогоном
  `на одном узле доходит всё` в самом примере.
* *Рукопожатие по хэшу программы.* У BEAM это cookie — общий секрет, который
  говорит «мы одной семьи», но ничего не говорит про то, ОДИНАКОВЫЙ ли код.
  Здесь узлы сверяют sha256 напечатанного модуля.

**Чего у нас нет, и это не «пока».**

* *Кворума, членства, ограждения.* Разделение мозга возможно; программа,
  которой нужна исключительность, не пишется.
* *Динамического состава.* Ни обнаружения соседей, ни присоединения на ходу, ни
  переселения процессов.
* *Маршрутизации.* Только прямая связь.
* *Обратного давления через границу.* Потолок ящика за границей не работает.
* *Порядка после разрыва и обнаружения дыры.*
* *Настоящей сети.* Всё испытано на `127.0.0.1`. Ни задержек, ни потерь пакетов,
  ни MTU, ни разрывов посередине кадра — то есть ни одной из тех бед, ради
  которых распределённость и трудна. Числа срока выбраны по замеру петли и на
  настоящей сети обязаны быть другими.
* *Безопасности.* Ни шифрования, ни подписи, ни проверки, кто звонит: хэш
  программы — защита от ошибки, а не от злого умысла. Кто дотянулся до порта,
  тот и узел.
* *Узла на восьми целях печати.* СВЯЗЬ есть у одной цели — JavaScript: сокеты,
  часы и таблица процессов написаны на JavaScript и в язык не втаскиваются.
  Доказуемая половина — провод, сроки, размещение, адреса — с 20 августа есть у
  всех восьми (см. «Вторая цель» ниже), но узлом это цель ещё не делает.

## Почему цель печати — JavaScript, и почему не Elixir

Довод не «нам так привычнее», а счёт.

**За JavaScript.** Напечатанный модуль **отдаёт свой план**: `concPlan()` —
процессы, надзоры и обработчики функциями-значениями (`emit/js.mjs`,
`renderConcurrency`). Значит узлу не нужно ни одной новой строки печати:
`flang/src/emit/js.mjs` не тронут вовсе, ни на байт. Сокеты — в стандартной
библиотеке хозяина, значит ни одной новой зависимости. Представление значений
то же, что у свидетеля (`emit-js-conc.test.mjs` сверяет их побайтово), значит
кодирование пишется один раз и проверяется тем же приёмом. И свидетель сам написан
на JavaScript, поэтому планировщик узла — перенос свидетеля, а не второе
проектирование: решение надзора вообще берётся из `src/conc.mjs` буквально.

**Против Elixir, хотя он и стоит на этой машине.** BEAM дала бы
распределённость даром — и вместе с ней **чужие ответы на все три вопроса**.
Имя стало бы `{имя, узел}`, отказом стал бы `net_ticktime` в 60 секунд,
доставка — at-most-once по решению Ericsson. Испытание такой распределённости
испытывало бы BEAM, а не нас, а этот документ обязан отвечать за СВОИ решения.
Взять чужие ответы можно и нужно — но потом, зная свои.

**Против C.** Планировщик в рантайме C есть (шаг 6 карты), но сокеты, разбор
кадров и таймеры пришлось бы писать руками, а печать пришлось бы трогать.
Дороже раз в десять при том же результате.

Цена выбора названа: **узел есть у одной цели из восьми**, и у той, у которой
параллелизма нет и не будет (один поток). Узел flang на JavaScript — это
конкурентность без параллелизма плюс связь. *Объявлено, не сделано:* узел в
Elixir (где он дал бы настоящие ядра) и в C.

## Вторая цель: не «напишем узел ещё раз», а «сколько узла печатает компилятор»

Вопрос «взять вторую цель» задавался как «на каком ещё языке написать узел». В
этой постановке ответ один и он плохой: **459 строк кода на каждую цель**, семь
раз, и семь копий, расходящихся на первой же правке. Числа считаны по
`distributed.mjs` (589 строк кода, без комментариев и пустых):

| часть узла | строк кода | на flang выразимо |
|---|---:|---|
| `создатьУзел` — сокеты, часы, таблица процессов, планировщик | 444 | нет: мир в язык не втаскивают |
| `конструкторыВариантов` — проба по экспортам напечатанного модуля | 15 | нет: это разговор с чужим модулем |
| **итого мир** | **459** | **нет** |
| `закодировать`, `СРОКИ`, `СВЯЗЬ`, `КАДР`, `размещениеУзла`, `разделитьАдрес` | 54 | было в эталоне |
| `раскодировать` | 31 | **добавлено 20 августа** |
| `собиратель` — границы кадров в потоке | 22 | **добавлено 20 августа** |
| `надзорыИзПлана` — план в дерево надзоров | 12 | выразимо, эталона пока нет |
| **итого без мира** | **119** | **107 из 119** |

Поэтому вопрос переставлен: не «напишем ли мы узел на седьмом языке», а
**сколько узла умеет напечатать сам компилятор**. Это и есть работа на flang: то,
что написано один раз в `flang/self/distributed.flang`, печатается во все восемь
целей тем же компилятором, что и всё остальное.

*Сделано и испытано* (`flang/test/self-distributed-celi.test.mjs`): эталон
печатается в каждую из восьми целей, собирается НАСТОЯЩИМ тулчейном этой цели,
запускается и отвечает на 443 вопроса — кодирование и раскодирование 210
значений из 22 настоящих прогонов конкурентных примеров (все семь меток
встречены), сроки, три состояния связи, четыре вида кадра, семь меток значений,
размещение, адреса, кого роняет разрыв. **3544 сверки, расхождений 0.** Изъятие
проверено: убери один из четырёх видов кадра — краснеют все восемь целей разом.

Цена цели измерена, а не оценена (эта машина, `flang/self/distributed.flang`):

| цель | печать | сборка | напечатано |
|---|---:|---:|---:|
| JavaScript | 3,4 с | — | 144 656 байт |
| Python | 3,5 с | **0,1 с** | 195 085 |
| Go | 3,3 с | 0,9 с | 209 008 |
| Rust | 3,3 с | 1,2 с | 271 171 |
| Java | 3,4 с | 1,2 с | 245 202 |
| Elixir | 3,2 с | 1,8 с | 236 675 |
| C | 3,1 с | 2,0 с | 456 832 |
| C# | 3,3 с | 5,5 с | 287 170 |

**Вторая цель по этому счёту — Python:** тулчейна для сборки нет вовсе, 0,1 с
против 5,5 с у самой дорогой. Elixir тут не выделен нарочно: его
распределённость от BEAM ничего не доказала бы про переносимость модели, а
провод, напечатанный компилятором, доказывает — тем, что байт в байт совпадает с
семью остальными.

**Чего это НЕ значит, и это надо сказать прямо.** «Распределённость у восьми
целей» здесь значит: два узла на разных целях печати поймут провод друг друга
байт в байт, размещение прочтут одинаково и срок посчитают одинаково. Это НЕ
значит «узел на Rust готов»: сокетов, часов и таблицы процессов компилятор не
печатает и печатать не может.

*Найдено попутно и названо:* NaN и обе бесконечности **не проходят входную
границу** напечатанной программы — во всех восьми целях одинаково, отказом
«аргумент не соответствует типу число». Наружу все четыре выходят исправно, и
минус ноль входит. Это про границу типов (`flang/self/types.flang`), а не про
распределённость, поэтому у трёх из четырёх спрашивается только обратная сторона
провода.

---

# Ведомость

| утверждение | пометка |
|---|---|
| Имя процесса на чужом узле — то же имя из объявления; составного адреса нет | **сделано и испытано** (1) |
| Размещение — данные узла; текст программы от него не зависит | **сделано и испытано** (1, плюс прогоны самого примера) |
| Каждый объявленный процесс обязан иметь ровно одно место | **сделано и испытано** (8, 9) |
| Узлу нечего нести → узел не поднимается и говорит об этом | **сделано и испытано** (7) |
| Имя переживает исчезновение узла; меняется достижимость, а не имя | **сделано и испытано** (2, 3) |
| Представитель чужого процесса стоит в таблице процессов и отказывает `FLANG_LINK_DOWN` | **сделано и испытано** (2, 3) |
| Надзор через границу — тот же код, тот же порог, «передать выше» и «доверху» | **сделано и испытано** (2) |
| Стратегия `остановить` над представителем | **объявлено, не сделано** — ветка написана, прогона нет |
| «Перезапустить» представителя — настоящая попытка дозвониться; её неудача — новый отказ | **сделано и испытано** (2) |
| Отказом считается потеря СВЯЗИ, а не смерть узла | **сделано и испытано** (2, 3) |
| Срок 1000 мс при пульсе 200 мс; обнаружение молчанием не раньше срока | **сделано и испытано** (3) |
| Сроки — доводы узла, а не константы кода | **сделано и испытано** (2, 3 задают свои) |
| Закрытый сокет — немедленный отказ | **сделано и испытано** (2) |
| Отказ при первом заводе связи отказом не считается | **сделано и испытано** (4: полторы секунды звонков в пустоту, ноль решений надзора) |
| Разделение мозга возможно и выбрано сознательно | **сделано и испытано** (2: один умер по объявленному, другой жив и цел) |
| Доставка «не больше одного»; потеря видна в состоянии, а не в отказе | **сделано и испытано** (3) |
| Порядок FIFO между парой, пока связь цела | **сделано и испытано** (1) |
| Отправка в порванную связь не отказывает, а пропадает; след — запись `потеря` в журнале | **сделано и испытано** (2) |
| Ящик за границей потолка не соблюдает | **объявлено, не сделано** — ветка написана, прогона на переполненный ящик через границу нет |
| Рукопожатие по sha256 напечатанного плана | **сделано и испытано** (5) |
| Значения на проводе с метками: `NaN`, ±∞, −0, вариант классом модуля | **сделано и испытано** (6) |
| Порог считается по настоящим часам узла (в прогоне свидетеля — по пробегам) | **сделано и испытано** (2) |
| `через N` на узле — настоящая задержка в N миллисекунд | **объявлено, не сделано** — написано, прогона нет |
| Программа поднимается тем же путём, что `flang check`: `loadProgram` → `externalChecks` → `emitJs` | **сделано и испытано** (положительным ходом: каждый прогон со стендом) |
| Отказ подъёма с названной диагностикой, когда проверка НЕ прошла | **объявлено, не сделано** — ветка написана (`bin/node.mjs`), прогона нет: порченый `.flang` в дереве сломал бы свод корпуса (`flang/test/proof.test.mjs` требует, чтобы сводился каждый файл), а заводить его вне дерева испытание не вправе |
| `FLANG_LINK_DOWN` входит в анализ непокрытых отказов | **сделано и испытано** (10, плюс восемь прогонов `flang/test/failures.test.mjs`) — был самый крупный долг |
| Забытый надзор над представителем — ошибка сборки, а не сюрприз в бою | **сделано и испытано** (10): узел не поднимается, код выхода 1 |
| Размещение приезжает анализу доводом (`flang check --размещение`), а не текстом программы | **сделано и испытано** (10 и прогоны анализа) |
| Отказ ЧУЖОГО процесса (не связи) доходит до надзора на этом узле | **объявлено, не сделано** |
| Переселение процесса между узлами | **объявлено, не сделано** |
| Маршрутизация писем через третий узел | **объявлено, не сделано** |
| Обнаружение соседей и присоединение на ходу | **объявлено, не сделано** |
| Кворум, членство, ограждение | **объявлено, не сделано** (и выбрано против) |
| Обратное давление через границу узла | **объявлено, не сделано** |
| Обнаружение дыры в потоке писем после разрыва | **объявлено, не сделано** |
| Срок на ПЕРВЫЙ завод связи — и на рукопожатие уже принятой | **объявлено, не сделано** — узел, чей сосед не поднимется никогда, ждёт вечно и молча; принятый сокет, который не назвался, держится открытым |
| Витки пробега в журнале узла | **объявлено, не сделано** (модуль не отдаёт показание счётчика) |
| Запас витков нетотального обработчика на узле (через `$newContext`) | **объявлено, не сделано** — написано, прогона нет |
| Семя упорядочивает готовые процессы внутри узла | **сделано и испытано** (11): 6 семян × 2 прогона, внутри семени журнал побайтово один, между семенами 6 разных из 6 |
| Побайтовая сверка журналов через границу узла | **отвергнуто** (12): сшивать два журнала нечем — общих часов у двух узлов нет. Измерено: 4 прогона одним семенем дали 1 исход и 4 разных сшитых журнала |
| Испытание в `npm test` | **объявлено, не сделано** — шаблон там `flang/test/*.test.mjs`, а этот файл лежит в `flang/conc/`; запуск руками названной командой |
| Шифрование, подпись, проверка звонящего | **объявлено, не сделано** |
| Распределённость на настоящей сети (не петле) | **объявлено, не сделано** |
| Доказуемая половина узла (провод, сроки, размещение, адреса) у всех восьми целей печати | **сделано и испытано** — 443 вопроса каждой цели, 3544 сверки, расхождений 0 |
| УЗЕЛ (сокеты, часы, таблица процессов) в Elixir и в C | **объявлено, не сделано** — 459 строк мира на цель, и на flang они не выражаются |
| Эталон границ кадра: поток → кадры и остаток | **сделано и испытано** (13) — свидетель `собиратель` наружу не отдан, поэтому эталон сверен не с формулой, а с ПОВЕДЕНИЕМ живого узла: поток режется по 7 знаков, и узел принимает ровно столько писем, сколько эталон насчитал кадров |
| Эталон `надзорыИзПлана`: план → дерево надзоров | **объявлено, не сделано** — выразимо на flang, 12 строк свидетеля; свидетель их не экспортирует, а сверять с поведением живого узла тут нечего: надзор виден только через отказ |

## Ведомость: эталон на flang и что им доказано

Эталон — `flang/self/distributed.flang`, сверка — `flang/test/self-distributed.test.mjs`.
Эталон берёт ту часть этого файла, у которой нет ни сокета, ни часов: мир в язык
не втаскивают.

| утверждение | пометка |
|---|---|
| Значения на проводе: эталон даёт байт в байт то же, что `закодировать` | **сделано и испытано** — 395 значений корпуса, 5 297 491 байт, ноль расхождений; корпус — 170 связанных программ, 25 прогонов, 70 состояний и 151 сообщение из журналов |
| `размещениеУзла` и ловля процесса, размещённого дважды | **сделано и испытано** — обе стороны настоящего файла размещения, плюс испорченный файл |
| `разделитьАдрес` | **сделано и испытано** — 5 адресов, включая IPv6 и `localhost` |
| Сроки, три состояния связи, четыре вида кадра, период сторожа | **сделано и испытано** — 6 постоянных |
| Кого роняет разрыв: представители потерянного узла и ни одного своего | **сделано и испытано** — список берётся у самого `создатьУзел` (`связи()`), а не у формулы, переписанной в проверку |
| «Одна отправка даёт через границу **не более одного** кадра» | **сделано и испытано** — `обеспечивает` в эталоне, доказано **индукцией по «Исход отправки»**, обо ВСЕХ входах, а не о четырёх написанных |
| «Если сообщение доставлено, обработчик завершится» | **сделано и испытано** — следует из тотальности: в корпусе 25 обработчиков, у 22 завершение **доказано компилятором**; у оставшихся 3 (`budget.flang`, `handler-without-budget.flang`, `server.flang`) тотальности нет, и завершение несёт сторож запаса витков — то есть обработчик кончится, но отказом `FLANG_BUDGET_EXHAUSTED`, а не результатом |
| «Обнаружение молчания не раньше объявленного срока» | **объявлено, не сделано** — `обеспечивает` в эталоне есть, ядро его не закрыло: сетка 2 значения (примеры функции). Теоремы при утверждении нет |
| «Однозначность размещения — это в точности отсутствие повтора» | **объявлено, не сделано** — `обеспечивает` есть, ядро не закрыло: ни теоремы, ни примеров |
| «Разрыв не роняет ни одного своего процесса» | **объявлено, не сделано** — `обеспечивает` есть, ядро не закрыло; побайтово сверено на настоящем размещении, но это корпус, а не доказательство |
| «Письмо, ушедшее через границу, приходит **не менее одного** раза» | **не выразимо** — и не будет: узел молчит, и отличить «умер» от «сеть моргнула» невозможно (Fischer, Lynch, Paterson, 1985). Ни повторов, ни подтверждений, ни номеров сообщений в свидетеле нет; «не менее одного» пришлось бы обещать про сеть, а про сеть язык не говорит ничего |
| «Разрыв связи виден обеим сторонам за конечное время» | **не выразимо** — у языка нет ни времени, ни второй стороны. Выразима и посчитана только АРИФМЕТИКА срока на ЭТОЙ стороне: `«Срок обнаружения молчания»` = срок плюс период сторожа. Что при этом видит сосед, зависит от его часов и его сети, а их в программе нет |
| «Надзор через границу перезапускает ровно того, кто упал» | **выразимо наполовину** — «кого роняет разрыв» посчитано и сверено (строка выше); «перезапускает» — это действие над живым узлом, у которого есть сокет и таймер, и в языке его нет |
| Раскодирование: эталон даёт байт в байт то же, что `раскодировать` | **сделано и испытано** — 402 значения корпуса ездят туда И обратно; сравнение идёт по КОДИРОВАНИЮ раскодированного, потому что в JSON `NaN`, ±∞ и −0 сравнились бы как равные |
| «Оборот через провод не меняет род значения» | **объявлено, не доказано** — `обеспечивает` есть, ядро не закрыло; считается рантаймом на 402 значениях корпуса и на восьми целях. Утверждение не тавтология: сотри метку варианта, и оборот вернёт запись |
| «Меток значений ровно семь» | **сделано и испытано** — доказано ядром сведением замкнутой цели, обо ВСЕХ входах |
| Эталон планировщика узла (очередь, ящики, пробеги) | **объявлено, не сделано** — у планировщика прогона эталона тоже нет (`flang/self/conc.flang` — только словарь действий), и заводить его здесь значило бы забежать вперёд соседа |
| Два расхождения эталона со свидетелем на ПОРЧЕНОМ проводе | **названо, не устранено** — на неизвестной метке свидетель бросает, эталон возвращает «ничто»; на кадре без груза свидетель даёт строку "undefined", эталон — «ничто». Ни того ни другого `Закодировать` не порождает ни при каком входе |

## Порядок дальнейших работ

1. ~~**`FLANG_LINK_DOWN` в анализ отказов.**~~ **СДЕЛАНО** — см. «Расхождения с
   контрактом», пункт 5. Решение вопроса «откуда анализ узнаёт размещение»
   записано там же: доводом (`flang check --размещение`), и тем же файлом, каким
   размещение получает узел; подъём узла эту проверку делает сам, поэтому мимо
   неё распределённой программы не бывает.
2. **Отказ чужого процесса через границу.** Сегодня представитель отказывает
   только на потере связи; отказ самого процесса на том конце сюда не доходит.
   Здесь нужен ответ на вопрос «два надзора над одним процессом», и ответ этот
   не очевиден — потому шаг и не сделан наспех.
3. **Реестр вариантов от печати.** Словарь «имя варианта → конструктор» узел
   строит ПРОБОЙ — зовёт каждую экспортированную функцию нулевой арности и
   смотрит, не вернулся ли вариант. Приём безобидный (конструкторы чисты) и
   испытанный, но правильное решение — одна строка в `emit/js.mjs`, отдающая
   реестр. Не сделано, потому что печать в этой работе не наш файл.
4. **Настоящая сеть и настоящие сроки.** Всё, что здесь измерено, измерено на
   петле. Первый же прогон между двумя машинами обязан пересчитать срок.
5. **Второй узел на настоящих ядрах.** Узел на JavaScript даёт связь, но не
   параллелизм. Тот же приём на цели Elixir дал бы и то, и другое, и отвечать на
   три вопроса заново не пришлось бы: ответы уже записаны здесь. Цена названа
   числом: 459 строк мира на цель, и на flang они не выражаются — ни сокет, ни
   часы, ни изменяемая таблица процессов. Доказуемая половина уже напечатана во
   все восемь целей и сверена побайтово, так что переписывать её не придётся.
6. **Эталон перевода плана в дерево надзоров.** Последние 12 строк свидетеля,
   у которых нет ни сокета, ни часов. Мешает одно: `надзорыИзПлана` в
   `distributed.mjs` не экспортирована, и сверить эталон не с чем, не тронув
   файл свидетеля. Правка на одно слово, но это правка реализации на
   JavaScript, и делать её молча нельзя. С границами кадра вышло иначе:
   `собиратель` тоже не экспортирован, но у него есть НАБЛЮДАЕМОЕ поведение —
   сколько писем узел принял из рваного потока, — и сверка пошла по нему.
7. **Кворум — развилка, а не работа.** См. следующий раздел.

## Кворум: развилка названа, решение прежнее

`DISTRIBUTED.md` с самого начала выбрал **доступность вместо
исключительности**: кворума, членства и ограждения нет, разделение мозга
возможно, и программа, которой нужна исключительность, здесь не пишется.
Решение осталось прежним, потому что новых доводов не появилось, — но развилку
надо назвать, а не подразумевать, потому что цена у обеих сторон настоящая.

**Что стоит завести кворум.** Три вещи, ни одна из которых сегодня не
существует: (а) СПИСОК УЧАСТНИКОВ — сегодня состав узлов известен только из
размещения при подъёме, динамического состава нет; (б) ГОЛОСОВАНИЕ по этому
списку с большинством — то есть протокол поверх нынешних четырёх видов кадра;
(в) ОГРАЖДЕНИЕ — способ заставить замолчать узел, оказавшийся в меньшинстве, а
такого способа у нас нет вовсе: узел объявляет отказ СВЯЗИ и это утверждение о
здешнем узле.

**Чем за это платится, и вот это главное.** Кворум ломает решение вопроса ИМЯ.
Сегодня имя процесса — константа времени компиляции, уникальная во всей
программе, и прозрачность размещения досталась тем, что её НЕЧЕМ нарушить.
Узел, отключённый кворумом, — это узел, на котором имя есть, а процесса за ним
нет; значит `отправить` начинает зависеть от того, в большинстве ты или нет, то
есть **от размещения и от состояния сети**. Ровно этого контракт и избегал.

**Развилка владельцу, двумя строками.** Либо остаётся «доступность»: программе
с исключительностью здесь места нет, и это записано прямо. Либо заводится
кворум — и тогда `отправить` перестаёт быть прозрачным, а вместе с ним
перестаёт быть правдой «программа не знает о распределённости ни слова». Третьего
не видно, и придумывать его молча здесь не станут.
