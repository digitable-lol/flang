# Rosetta Code на flang

Канонические задачи, решённые на языке. Rosetta Code — витрина, где язык
сравнивают не с обещаниями, а с тем же решением на сорока других языках,
поэтому здесь важнее не краткость, а то, что видно на просмотре: где язык
доказывает завершение, а где честно говорит, что не может.

Каждый файл запускается. Прогон — `flang/test/rosetta.test.mjs`; он проверяет
разбор, типы, завершаемость, все примеры и, сверх того, **число тотальных
функций**. Последнее не ради числа: набор показывает границу языка, и если она
сдвинется, объяснения в шапках файлов станут враньём — заметить это обязан
тест, а не читатель.

Сейчас в наборе **15 файлов, 97 функций (70 тотальных), 222 примера**; все
сходятся.

| Задача Rosetta Code | Файл | Тотальных | Чем интересна на flang |
|---|---|---|---|
| Reverse a string | `reverse-string.flang` | 4 из 4 | обращение строки доказуемо завершается, и по кодовым точкам — эмодзи не разваливается |
| Palindrome detection | `palindrome.flang` | 14 из 14 | нормализация и сравнение целиком доказаны; отдельно показана та же задача на списке |
| Sorting algorithms/Merge sort | `merge-sort.flang` | 8 из 8 | слияние доказывается: рекурсия идёт по хвостам обоих списков |
| Sorting algorithms/Quicksort | `quicksort.flang` | 4 из 5 | сама сортировка **не** доказывается — рекурсия по отфильтрованным подспискам, а подсписок не хвост |
| Levenshtein distance | `levenshtein-distance.flang` | 7 из 7 | динамическое программирование без таблицы: хранится одна строка, и она список — поэтому доказано целиком |
| Roman numerals/Encode, Roman numerals/Decode | `roman-numerals.flang` | 10 из 10 | обе стороны доказаны, и рядом стоит утверждение «запись и чтение обратны», тоже доказанное |
| Towers of Hanoi | `towers-of-hanoi.flang` | 3 из 5 | башня записана списком, и рекурсия идёт по хвосту — задача, которую обычно считают по числу, оказывается доказуемой |
| 100 doors | `hundred-doors.flang` | 5 из 9 | ответ «открыты полные квадраты» записан не прозой, а функцией, которая считает обе стороны и сравнивает |
| Run-length encoding | `run-length-encoding.flang` | 2 из 5 | сжатие доказано, разжатие — нет: повтор идёт по прочитанному числу, границы которому нет |
| Sequence of primes by trial division | `primes-by-trial-division.flang` | 2 из 6 | один алгоритм записан дважды — без топлива (не доказывается) и с топливом (доказывается) |
| FizzBuzz | `fizzbuzz.flang` | 3 из 5 | классика, на которой видно, что счёт до N в этом языке не бесплатен |
| FizzBuzz (английская поверхность) | `fizzbuzz-english.flang` | 3 из 5 | тот же файл английскими ключевыми словами; скелеты AST обеих поверхностей совпадают — проверено сравнением |
| Factorial | `factorial.flang` | 2 из 5 | то же: `Числа от и до` рекурсирует по числу |
| Fibonacci sequence | `fibonacci.flang` | 2 из 6 | и шаговый вариант, и ряд — оба упираются в счёт |
| Ackermann function | `ackermann-function.flang` | 1 из 3 | функция, которая ЗАВЕРШАЕТСЯ, но доказать это анализ не может — самый честный пример того, что значит слово «тотальная» |

## Почему часть решений не тотальна

Это не недоделка, а граница, которую язык проводит осознанно.

Тотальность доказывается **структурным убыванием**: каждый рекурсивный вызов
обязан получать структурно меньший аргумент — хвост списка, поле записи, поле
варианта. Убывание числа таковым не считается. `«Числа от и до» от 1 и n`
очевидно завершается человеку, но обосновать это компилятор может только
рассуждением о натуральных числах, которого он не ведёт.

Так же с быстрой сортировкой: `отфильтровать` даёт новый список, и он
**меньше** исходного — но не является его хвостом, а значит структурного
убывания нет.

Отсюда главное, что стоит унести из набора и что подробно разобрано в
`ackermann-function.flang`: **`тотальная` не значит «завершается»**. Она значит
«завершение доказано ЭТИМ анализом». Обратное неверно, и функция Аккермана —
самый известный тому пример: она завершается всегда, а доказать это структурным
убыванием нельзя. Анализа, который принимал бы ровно завершающиеся программы, не
существует вовсе; вопрос лишь в том, названа граница или нет.

Границы названы прямо в шапках соответствующих файлов и держатся тестом.

## Два приёма, которыми граница сдвигается

Оба видны в наборе, и оба стоят своей цены.

**Держать структуру, а не счётчик.** Ханойские башни обычно рекурсируют по числу
дисков и тогда не доказываются. Записать башню списком дисков — и та же рекурсия
пойдёт по хвосту. То же с расстоянием Левенштейна: строка таблицы это список, и
проход по ней доказывается. Цена нулевая; это просто другой способ записи.

**Возить топливо.** Список, у которого на каждом витке берут хвост; глубина
рекурсии заведомо не больше его длины. Так доказаны сортировка слиянием и
`«Просеять с топливом»`. Цена названа: топливо ничего не значит для задачи и всё
значит для доказательства, то есть засоряет сигнатуру. В
`primes-by-trial-division.flang` обе записи стоят рядом нарочно.

Топливо помогает не всегда. У Аккермана глубина растёт быстрее любой
примитивно-рекурсивной функции, и списка нужной длины не существует — не по
памяти, а вообще.

## Что пока не выражается вовсе

Список честный и короткий; он не про «не успели», а про устройство языка.

**Задачи, требующие словаря или множества.** `Anagrams` требует прочитать
`unixdict.txt` и сгруппировать слова по набору букв. Ни словаря, ни множества в
языке нет (`flang/examples/leetcode/index.json`, «ассоциативный массив»), поэтому
группировка выходит квадратичной. Сверх того, буквы слова не отсортировать
напрямую: `«a» меньше «б»` отвергается проверкой типов —
`FLANG_TYPE: сравнения порядка допустимы только для чисел`. Обойти можно
таблицей «буква → номер», но решение перестанет быть решением этой задачи.
То же касается `Letter frequency`.

**Задачи с вводом-выводом** выражаются с 7 августа 2026: эффект — это описание
действия, которое исполняет хозяин, и `flang/examples/io/link-report.flang`
читает файл, ходит в сеть и пишет отчёт. Здесь их по-прежнему нет, и это выбор
витрины: задачи Rosetta Code — про алгоритм, а не про хозяина. Не выражается
по-прежнему «спросить у пользователя»: поручения «прочитать со ввода» в наборе
нет, потому что набор закрыт и каждое поручение обязан уметь каждый хозяин.

**Задачи, требующие функций как значений** — «отсортировать заданным
компаратором», «свернуть переданной операцией», комбинаторы. У ЯЗЫКА функции
первого класса есть и печатаются во все восемь целей; у РЕПОЗИТОРИЯ их нет, пока
`flang/self/parser.flang` не разбирает новую форму (`flang/PLAN.md`, пункт 2).

**Задачи о бесконечных последовательностях** — «первые N простых ленивым
решетом». Ленивости нет, а конечное приближение — уже другая задача, и выдавать
одно за другое на витрине нечестно.

**Настоящее решето Эратосфена.** Оно вычёркивает кратные записью по индексу, а
массива с индексным доступом в языке нет. Списочный вариант с проверкой остатка
решетом не является — это пробное деление, и Rosetta Code помечает такие решения
шаблоном `{{incorrect}}`. Поэтому файл лежит под своей настоящей задачей,
`Sequence of primes by trial division`, а не под решетом.

## Как это выложить

Порядок действий, разметка, лицензионная оговорка и что делать с языковой
страницей — в `docs/public/rosetta-как-выложить.md`. Коротко: заголовок языка
`=={{header|flang}}==`, блок кода `<syntaxhighlight lang="text">`, аккаунт
заводит и правки делает владелец языка.
