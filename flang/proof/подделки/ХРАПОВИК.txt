# ХРАПОВИК НАБОРА ПОДДЕЛОК — два числа, ниже которых набор опускаться не вправе.
#
# Решение ADR-0023 §6, сторожа 3 и 4. Набор, вынесенный из знаменателя доли,
# перестаёт быть виден в проценте, а невидимое ветшает. Здесь стоит то, что
# делает убыль набора ГРОМКОЙ: scripts/доказуемость.sh, проверка 4, сличает эти
# два числа с сегодняшним замером и при недоборе печатает НЕ ДОКАЗУЕМ, назвав,
# на сколько просело.
#
# ХРАПОВИК КРУТИТСЯ ТОЛЬКО ВВЕРХ. Поднять число вправе только человек и только
# вместе с доводом в сообщении коммита. Опустить его нельзя ничем: снижение —
# это и есть та беда, ради которой файл заведён.
#
# ДВА ЧИСЛА, И ОНИ СТЕРЕГУТ РАЗНОЕ:
#
#   подделок   — сколько строк в flang/proof/подделки/НАБОР.tsv. Стережёт от
#                исчезновения подделки: убрал строку — вердикт покраснел.
#
#   числитель  — сколько мест честных половин подделочных файлов чекер
#                ПРОИГРАЛ заново (ходами и тотальностью + по существу + узлы),
#                сложенное по всем строкам манифеста. Стережёт от беды, которой
#                счёт строк не видит: файл остался в манифесте, а его честная
#                половина тихо перестала проигрываться.
#
# Замер 8 сентября 2026, ветка r/nabor-poddelok поверх gh/dev 9c228f30,
# прибор — flang/proof/доля-корпуса.sh --проигрыванием (строка «числитель набора»).
#
# ЧИСЛИТЕЛЬ ПОДНЯТ 143 → 155, замер 9 сентября 2026, ветка r/uzel-svyortka
# (задача 6190). Двенадцать мест честных половин подделочных файлов
# (poddelka-dlina, poddelka-svyortka-shagom) стали проигрываться заново: узел
# вердикта выучил носитель свёртки и домены цели «тождество» и «не больше».
# Число поднято затем, чтобы обратный ход стал громким.

# ПОДДЕЛОК ПОДНЯТО 31 → 33, замер 9 сентября 2026, ветка r/6202-6203-cheker
# (задача 6203, Ш0). Две подделки класса «лжёт-запись» на носитель индукции:
# «segment» над доводом типа «число» и над псевдонимом к «число». До Ш0 сверщик
# отвечал на обе кодом 3 (носитель брался словом записи); теперь объявление
# типа довода читается с исходника, и обе отвергнуты кодом 1. Числитель
# переснят прогоном `доля-корпуса.sh --проигрыванием` (строка «числитель набора»).

# ПОДДЕЛОК ПОДНЯТО 33 → 34, замер 11 сентября 2026, ветка r/logika (задача
# 1403, ADR-0032 §2). Подделка класса «лжёт-запись» на правило Н12 «разность под
# записанным порядком»: три функции без нужного допущения (порядок о другой
# паре, перевёрнут, нет вовсе) с подсунутым выводом Н12; сверщик отвергает
# кодом 1 и называет «порядок не записан». Честная половина — семья
# пробы/семьи/разность (запись рукотворная: ядро напечатает блок после
# перепечатки партии).

# ПРОБ НА ПОДЛОГ ПОДНЯТО 404 → 412, тот же замер 11 сентября 2026 (r/logika, 1403; после
# перебазирования на dev d6e88d50b — счёт от 404, а не от 401):
# семья «разность» — подделка набора, порча одной записи и шесть порч строк в
# прогоне (посылка 1, посылка 2, основание, обязательство входа, запрет вместо
# правила, вывод без развёртки).

# ПРОБ НА ПОДЛОГ ПОДНЯТО 412 → 417, замер 11 сентября 2026 (r/logika, задача 1404,
# Ш0): семья «прогон» — четыре порчи строки `прогон «Ф» шаг «Ш» строка N` (чужой
# шаг, не свёртка, не та строка, цель не зовёт) с названной причиной и одна
# порча места строки; честных прибавилось две (запись ядра и рукотворная со
# строкой «прогон»), обе на слове ядра, код 3.

# ТРЕТЬЕ ЧИСЛО «проб на подлог» ЗАВЕДЕНО 11 сентября 2026, ветка r/a-posle-partii
# (партия перепечатки кванторов). Это строка «подделок N:» прогона
# flang/proof/чекер/пробы/прогон.sh — сколько проб на подлог прогон прогнал.
# Стережёт беду, найденную партией: подделка прогона, которую строит sed по
# честной записи (podd), после перепечатки записи не собралась — sed не нашёл
# строки, — и выпала из счёта молча (401 → 391; каждая такая ещё и ПРОВАЛ
# прогона, но число подделок до сих пор никто не держал). 401 — число до партии
# (прогон на dev 2550e38c2), и оно же после починки десяти несобравшихся.
# ПРОБ НА ПОДЛОГ ПОДНЯТО 401 → 404, 11 сентября 2026, ветка r/a-k3c-primer (от dev
# 0d85887dc). Три пробы К3c на шаг «по примеру» в свободном утверждении: пример
# чужой функции и пример своей функции о другом доводе — дважды (у второй
# «ожидается» совпадает с ветвью тела, её держит только сверка довода). Замер —
# scripts/доказуемость.sh, «проб на подлог 404, принято кодом 0 — 0».
# ПРОБ НА ПОДЛОГ ПОДНЯТО 401 → 409, замер 11 сентября 2026, ветка r/ostatok-26.
# Заведена семья «остаток-26»: восемь новых проб на подлог (семь порч честной
# записи по одной на правило и одна подделка ИСХОДНИКОМ — охрана выбора, которой
# нет в допущениях) на пять правил ведомости, написанных в приёме «вывод» этой же
# веткой: Кон1, Н9, О8, второй способ Н8 и второй способ колонки 5 у СС4. Честных
# записей стало 198 (было 197). Число поднято затем, чтобы обратный ход стал громким.
подделок 33
числитель 155
проб на подлог 417
