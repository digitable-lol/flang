# ХРАПОВИК ДОВЕРЕННОЙ БАЗЫ СВЕРЩИКА — потолок строк кода и число примитивов.
#
# Смысл сверщика — «верят не ядру, а РАЗМЕРУ сверщика, и размер выбираем мы»
# (ЧИТАТЬ.md). Размер этот рос молча: каждая ячейка прибавляла приём, и никто не
# спрашивал, сколько человек ещё прочтёт за вечер. Здесь стоит потолок, и сторож
# С-6 (`flang/proof/сверка-ведомости.sh`) сличает с ним дерево на каждом прогоне.
#
# ЧТО СЧИТАЕТСЯ — СТРОКИ КОДА, а не строки файла (решение overagent 9 сентября
# 2026 по рецензии Lead-A, ПЕРЕДАЧА/stack-E/review-A.md §4, вариант (б)+(в)).
# Строка кода — строка `сверщик.c`, на которой после снятия комментариев
# (`/* … */` где угодно, в том числе через строки, и `//` до конца строки)
# остаётся хоть один непробельный знак. Комментарии — за читаемость, не против
# проверок, и с проверками за место не соперничают. Считает ОДНА функция
# сторожа, и снять число может любой той же командой:
#
#     sh flang/proof/сверка-ведомости.sh --строки-кода
#
# ДОВОД, ПОЧЕМУ ДОВЕРЕННАЯ БАЗА МЕРЯЕТСЯ ТАК. База меряется ПРАВИЛАМИ ведомости
# (flang/proof/ПРАВИЛА-ВЫВОДА.tsv): у каждого правила — строка ведомости и лемма
# Lean, и код сверщика лишь переигрывает написанное. Строки кода — мера того,
# сколько такого переигрывания человек прочтёт; комментарий, объясняющий, почему
# проверка такова, читать помогает, а не мешает.
#
# ХРАПОВИК ДЕРЖИТ СВЕРХУ, а не снизу, — в отличие от храповика набора подделок
# (flang/proof/подделки/ХРАПОВИК.txt), который не даёт числу ПАДАТЬ. Потолок =
# база + сумма бюджетов решений; каждый бюджет — СТРОКА В ADR, и двигается он
# только правкой той строки (и этой таблицы вслед за ней). Ячейка, которая в
# потолок не влезает, останавливается и докладывает — потолок не двигается
# молча под работу. Опустить потолок — кто угодно, вместе с сокращением файла.
#
# БАЗА: 5788 строк кода на gh/dev bc1ff161e (снято `--строки-кода`, ветка
# r/a-lovushka-dopolnenie, 9 сентября 2026; файл строк 9121, из них комментариев
# и пустых 3333). Прежняя мера — строки файла, потолок 9353 = 8753 (6dff8a3a9)
# + 600 — снята этим же коммитом.
# Ужатие (ветка r/a-uzhatie, 10 сентября 2026): три разрезателя доводов вызова
# и пять читателей строки «принимает» сведены к одному каждый — 5788 → 5737
# строк кода. Потолок НЕ опущен: освобождённые 51 строка остаются в остатке
# бюджетов ниже (рецензия ПЕРЕДАЧА/stack-E/review-A.md §4 (в)).
#
# БЮДЖЕТЫ ПО РЕШЕНИЯМ (строки кода сверх базы):
#   решение    | что                                        | бюджет | где записан
#   ADR-0026   | кванторы над произвольными типами (К1–К7)  |   +600 | docs/adr/0026-…md §11 п.9
#   ADR-0027   | семейство Р, ячейки Е-1…Е-4 (Гип, Р1–Р7…)  |   +400 | docs/adr/0027-…md §3.5
#   ИТОГО потолок = 5788 + 1000 = 6788
#
# РАСХОД ПО ЯЧЕЙКАМ (строки кода, снято `--строки-кода` на названном коммите):
#   6203-Ш0/Ш2 | носитель с объявления (Ш0); строка `объявление` (Ш2)
#              | Ш0 +73 (5709 → 5782, fd56f8b4f) — вошёл в базу, бюджета не ест;
#              | Ш2 +45 (5737 → 5782, ветка r/a-sh2) — в счёт ADR-0026 (+600)
#   К2/К6      | имена за «для всех» списком, строка `для всех «а» «б»`,
#              | переключатель строгого режима строк (Ш2, К2)
#              | +31 (5782 → 5813, ветка r/a-k2) — в счёт ADR-0026 (+600)
#
# ДВА ЧИСЛА, И ОНИ СТЕРЕГУТ РАЗНОЕ:
#
#   потолок     — строк кода в `сверщик.c` не больше; АБСОЛЮТНО: считаются все
#                 строки кода, чьи бы правки в файле ни лежали.
#
#   примитивов  — литералов в закрытом списке `ПРИМИТИВЫ[]`: операции языка,
#                 которые сертификат тотальности берёт на веру как аксиомы
#                 (сверщик.c, «[Ш3] ГРАНИЦА ДОВЕРИЯ»). Ровно столько, не больше и
#                 не меньше: лишний примитив — расширение доверия, пропавший —
#                 сломанный разбор списка.
потолок 6788
примитивов 15
