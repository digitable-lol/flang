# ХРАПОВИК ДОВЕРЕННОЙ БАЗЫ СВЕРЩИКА — два числа, выше которых сверщику расти нельзя.
#
# Смысл сверщика — «верят не ядру, а РАЗМЕРУ сверщика, и размер выбираем мы»
# (ЧИТАТЬ.md). Размер этот рос молча: каждая ячейка прибавляла приём, и никто не
# спрашивал, сколько строк человек ещё прочтёт за вечер. Здесь стоит потолок, и
# сторож С-6 (`flang/proof/сверка-ведомости.sh`) сличает с ним дерево на каждом
# прогоне: строк в `сверщик.c` не больше названного, примитивов языка, взятых на
# веру (`ПРИМИТИВЫ[]`), ровно столько, сколько названо.
#
# ХРАПОВИК ЭТОТ ДЕРЖИТ СВЕРХУ, а не снизу, — в отличие от храповика набора подделок
# (flang/proof/подделки/ХРАПОВИК.txt), который не даёт числу ПАДАТЬ. Поднять
# потолок вправе только владелец и только решением (ADR) с доводом, зачем
# доверенной базе расти; опустить — кто угодно, вместе с сокращением файла.
# Ячейка, которая в потолок не влезает, останавливается и докладывает — потолок
# не двигается молча под работу.
#
# ДВА ЧИСЛА, И ОНИ СТЕРЕГУТ РАЗНОЕ:
#
#   строк       — `wc -l flang/proof/чекер/сверщик.c`, АБСОЛЮТНЫЙ потолок: считаются
#                 все строки файла, чьи бы правки в нём ни лежали. Мандат кластера
#                 6202/6203 (9 сентября 2026): база 8753 строки на gh/dev 6dff8a3a9,
#                 разрешено +600.
#
#   примитивов  — литералов в закрытом списке `ПРИМИТИВЫ[]`: операции языка,
#                 которые сертификат тотальности берёт на веру как аксиомы
#                 (сверщик.c, «[Ш3] ГРАНИЦА ДОВЕРИЯ»). Ровно столько, не больше и
#                 не меньше: лишний примитив — расширение доверия, пропавший —
#                 сломанный разбор списка.
#
# Замер 9 сентября 2026, ветка r/6202-6203-cheker после слияния с gh/dev 9a9df2e3f:
# сверщик.c 9121 строка, примитивов 15.
строк 9353
примитивов 15
