# ХРАПОВИК ЛОВУШКИ ПРОВЕРКИ 1 — два числа, ниже которых ловушка опускаться не вправе.
#
# Ловушка (ВИДЫ.tsv рядом) стережёт проверку 1 `scripts/доказуемость.sh` с двух
# сторон: цели «на слове» сверщик обязан НЕ снимать (снял — в дереве калькулятор),
# цели «снята» — обязан переиграть, по одной на каждый вид обязательства, который
# он умеет (иначе проверку 1 брал бы сверщик, сломанный до «ничего не доказываю»).
# Строка таблицы, убранная молча, делала бы ловушку слепой на один вид — и никто
# бы этого не увидел. Здесь стоит то, что делает убыль ГРОМКОЙ: проверка 1 сличает
# эти числа со счётом строк таблицы и при недоборе печатает НЕТ, назвав, на сколько.
#
# ХРАПОВИК КРУТИТСЯ ТОЛЬКО ВВЕРХ. Поднять число вправе только человек и только
# вместе с новой строкой таблицы и доводом в сообщении коммита: появился новый
# вид обязательства, который сверщик переигрывает, — появилась цель на него.
#
#   видов      — строк «снята» в ВИДЫ.tsv: сколько видов обязательства стережёт
#                обратная половина ловушки.
#   на слове   — строк «на слове»: сколько ∀-целей стережёт решающая половина.
#
# Замер 9 сентября 2026, ветка r/k11-lovushka-po-vidam поверх gh/dev e12837854:
# виды сняты по коду сверщика (ветки `без_теоремы`/`сверить_теорему`, семейства
# `шаг_вывода` Н/П/Пред/Т/Разв/О/Д/С/СС/В/Ч, носители `сверить_покрытие`
# algebra/segment/fold и объявление, виды тотальности composition/structure/step).
# 31 → 33 (ячейка К11+, та же дата): узел algebra с доменами «не больше» и
# «охрана» — пробы семьи `семьи/algebra-domeny`, прежде видов вне корпуса не было.
# на слове 2 → 3 (ячейка К3a, 10 сентября 2026): вид «утверждение без функции».
# Доказательство такого утверждения сверщик пока не переигрывает (К3b), и цель
# стоит строкой «на слове»: снял бы её сверщик до К3b — в дереве калькулятор. С К3b
# строка переходит в «снята», и тогда поднимается «видов», а «на слове» — нет.
# видов 33 → 34 (ячейка К3b, 10 сентября 2026): утверждение без функции переиграно
# (сведением к постусловию вызываемой функции). «На слове» не опущено (решение
# overagent'а, вариант (а)): место ушедшей цели заняла новая «не бери» —
# утверждение с несколькими связываниями в одной строке «для всех», у которого
# формы записи в контракте нет. Когда контракт её даст, цель ЗАМЕНЯЕТСЯ другой
# «не бери», а не удаляется.
# видов 34 → 35 (задача 9526, правило Инд1, 11 сентября 2026): узел свободного
# утверждения о СВОЕЙ объявленной сумме — цель на каждом конструкторе, допущение
# на рекурсивных полях. Прежде этот вид стоял на слове ядра (сведения К3b нет:
# связанное имя под конструктором), и переиграть его было нечем.
видов 38
# видов 34 → 37, «на слове» 3 → 3 (задача 5144, 11 сентября 2026): свободное утверждение с
# чистой целью переигрывается ходами — соседи под несколькими связываниями в одной
# строке (форма записи названа: по строке «для всех» на связывание), нейтральный ноль
# при посылке «таких что» (Тожд2), вычисление замкнутой цели без связываний. Прежняя
# «не бери» о нескольких связываниях ЗАМЕНЕНА (не удалена) другой «не бери»: та же
# цель без блока ходов — снять её можно только вычислением.
видов 38
на слове 3
