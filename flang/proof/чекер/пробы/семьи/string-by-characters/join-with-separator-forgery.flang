модуль «Подделка: склейка разложения со связкой»

тотальная функция «Знаки строки»
  принимает текст: строка
  возвращает список строки
  обеспечивает «ЛОЖНОЕ: знаки, склеенные через дефис, — исходная строка» (соединить результат по "-") равен текст
  разложить текст на символы
