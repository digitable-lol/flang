# Семья «покрытие»: места, которые сверщик переигрывает уже принятыми приёмами

Ячейки замера Lead-E «путь от 596 к 619» (`ПЕРЕДАЧА/stack-E/zamer-put-619.md`,
группа Г — только сверщик, перепечатка не нужна). Бюджет — строка «покрытие без
новых правил» в `чекер/ХРАПОВИК.txt`: ячейка, которой нужно НОВОЕ правило
ведомости, сюда не входит.

## Ячейка 5: прямая теорема «по предположению» (10 сентября 2026)

Прямая теорема (без «индукция по») с целью «результат не меньше 0» и единственным
шагом «по предположению» верна ровно тогда, когда тело функции неотрицательно по
построению при её предусловиях (`требует «…» Т не меньше К`): других допущений у
прямой теоремы нет. Сверщик это проигрывает (`прямая_по_предположению` в
`сверить_шаги`) тем же `неотрицательно_algebra`, что у узла `algebra`: литерал,
«длина», допущение `требует` (правило Н2), «плюс» (Н5), обе ветви «если», один шаг
развёртки плоской функции. Не сошлось — шаг, как прежде, на слове ядра.

| что | было → стало |
|---|---|
| корпус `precondition` («удвоенное неотрицательно», тело `н плюс н`) | 3 → 0 |
| проба `Ч56/precondition-dereva`, семья `квантор/два-имени` (тот же шаг) | 3 → 0 |
| корпус `four-words` («удвоенное неотрицательно», тело `н умножить на 2`) | 3 → 3: произведения в `неотрицательно_algebra` нет — это правило Н8, его приём у Lead-E |

| файл | класс | код | что это |
|---|---|---:|---|
| `тело-не-неотрицательно.{flang,запись}` | подделка (никогда не 0) | 3 | исходник `precondition` с телом `н минус 1` (при н = 0 даёт −1); запись — запись корпуса с пересчитанной шапкой. Сверщик оставляет шаг на слове ядра; копия, у которой проверка тела всегда «да», принимает запись кодом 0 |

## Ячейка 4 — не взята (решение overagent'а)

«По примеру» с целью-свёрткой (`corpus-cforms` ×3, `corpus-bytes`) требует от
вычислителя сверщика отбора списков и трёх слоёв функций-помощников, а
`corpus-bytes` — строк с нулевым знаком. Это второй вычислитель языка внутри
сверщика; места остаются на слове ядра с названной причиной.

Причина называется в самом отказе (`ПРИЧИНА_СЧЁТА` в `сверщик.c`): вычислитель
ставит её там, где сдаётся по решению, — на `отфильтровать … где …` и на
разложении строки с нулевым знаком, — и «вид цели … сверщику незнаком»
дописывает её через тире. Где причина не поставлена, текст прежний байт в байт
(сплошная сверка: из 381 записи разошлись только эти две, коды 3/3).
Проба в `прогон.sh` («ячейка 4: причина названа») ждёт код 3 и подстроку
причины; копия сверщика, которая причину не ставит, её роняет.

## Узел binder-wall-map: охрана и выбор с тем же условием (10 сентября 2026)

Постусловие «разбор: при положительном пороге ноль или один» — цель-охрана
`если порог больше 0 то (результат равен 0) или (результат равен 1) иначе да`,
а тело каждого случая — `(если порог больше 0 то К иначе 5)`. Прежде узел охраны
(`охраной_случая`) искал обещанное только среди конъюнктов охраны и отказывал.
Теперь, если в Ц стоит выбор с ТЕМ ЖЕ условием, условие это под охраной есть
«да» (Р1 и гипотеза ГипО; Р3 — внутреннее условие совпало с внешним, хода нет),
и замкнутую Ц закрывает Выч (`половина_закрыта`). Меняется только условие выбора
знак в знак (`если У то`), а не всякое вхождение текста У; связыватель в Ц —
отказ. Правил ведомости не прибавлено: Р1, ГипО, Р3, Выч в ней уже есть.

| что | было → стало |
|---|---|
| корпус `binder-wall-map`: утверждение и две посылки | на слове ядра 3 → 0 (запись остаётся 3: «свёртка: счёт неотрицателен» — Н7, у Lead-E) |

| файл | класс | код | что это |
|---|---|---:|---|
| `охрана-то-же-условие.{flang,запись}` | честная | 0 | функция «Порог под разбором» из `binder-wall-map` байт в байт; запись — её блоки из записи корпуса, строки перенумерованы, шапка пересчитана. Прежний сверщик — 3 |
| `охрана-ложный-вывод.{flang,запись}` | подделка (никогда не 0) | 3 | ветвь «голова и хвост» даёт 2 — обещание ложно. Копия сверщика без замыкания (`половина_закрыта` → «да») принимает её кодом 0 |
| `охрана-другой-порог.{flang,запись}` | подделка (никогда не 0) | 3 | внутреннее условие `порог больше 1` — не охрана (при пороге 0,5 выходит 5). Отвергает её само сличение условия знак в знак; копии, которая сличала бы условие вольнее, простой правкой не собрать — довод здесь пара «честная 0 / эта 3», отличающаяся одним условием |

Не опровергнуто ни одной пробой: условие «связыватель в Ц — отказ» (страховка).

## Замер запаса №1: произведение на положительный литерал (10 сентября 2026)

`неотрицательно_algebra` берёт «А умножить на К» (и «К умножить на А») при
выписанном К в (0, 2⁵³−1] — правило Н8, второй способ: К не ноль и конечно,
значит «0 умножить на +∞» исключено; А — той же грамматикой. Правило в таблице
есть (и в приёме блоков вывода у Lead-E) — нового нет.

| что | было → стало |
|---|---|
| корпус `four-words`, шаг «по предположению» теоремы «удвоенное» | на слове ядра 1 → 0 (запись остаётся 3: «утроенное» без теоремы — страховка №2–5) |
| `вывод/р/four-words` (запись E с блоком Н8) | 3 → 0 |
| `Ч7104/nakrutka-po-predpolozheniyu` | 3 → 0: шаг теперь проверен по построению, утверждение истинно; дверь накрутки стережёт новая пара `nakrutka-neproveryaemaya` (тело «н умножить на н») |

| файл | класс | код | что это |
|---|---|---:|---|
| `удвоить-литерал.{flang,запись}` | честная | 0 | «Удвоить» с теоремой из `four-words` байт в байт; прежний сверщик — 3 |
| `удвоить-на-ноль.{flang,запись}` | подделка (никогда не 0) | 3 | тело «н умножить на 0»: при н = +∞ не число. Копия, берущая и ноль, — 0 |
| `удвоить-сомножитель-минус.{flang,запись}` | подделка (никогда не 0) | 3 | тело «(н минус 5) умножить на 2»: при н = 0 выходит −10. Копия без проверки другого сомножителя — 0 |

## Замер запаса №6: правило О9 по телу функции (10 сентября 2026)

Постусловие «результат не больше (длина Л)» без теоремы и без блока вывода (в записи
только имя правила) сверщик выводит сам по телу (`свёртка_не_длиннее`, строка О9
таблицы): тело «свёртка Л начиная с 0 как А и Э → Ш», Л в свёртке и под «длина» знак в
знак, А и Э — различные имена, Ш — «А», «А плюс К» при целом К не больше 1 или выбор из
двух таких (условие не читается). Это индукция по списку — база ноль, шаг прибавляет к
мере не больше единицы: структурный узел, вычислитель не зовётся. Место считается
узлом («разбором по случаям»), как у algebra. Зовётся ПОСЛЕ разбора цели — снятое им не
меняет породы.

| что | было → стало |
|---|---|
| корпус `corpus-hof`, «счёт по условию не больше длины» | на слове ядра 6 → 5 (запись 3) |

| файл | класс | код | что это |
|---|---|---:|---|
| `о9-счёт.{flang,запись}` | честная | 0 | «Считать единицы»: свёртка с шагом «если … то акк плюс 1 иначе акк»; прежний сверщик — 3 |
| `о9-шаг-два` | подделка (никогда не 0) | 3 | шаг «акк плюс 2». Копия без предела единицы — 0 |
| `о9-начало-один` | подделка (никогда не 0) | 3 | начало 1: на пустом списке 1 больше 0. Копия без проверки нуля — 0 |
| `о9-чужой-список` | подделка (никогда не 0) | 3 | свёртка по «другие», а под «длина» — «элементы». Копия без сличения списков — 0 |

## Замер запаса №7: правило Ч6 по телу функции (10 сентября 2026)

Постусловие «результат начинается с П» без теоремы и без блока вывода сверщик
проигрывает сам (`начало_от_вызванной`, строка Ч6 таблицы): тело — ровно один вызов
«F» от имён доводов, у F нет «требует», её постусловие «результат начинается с Б»
ПРОВЕРЕНО раньше (реестр `доказанные_свойства`, тот же, что у хода «факт по
свойству»), и Б начинается с П — сличение двух написанных литералов, не счёт
(прецедент 63ab3ce77). Т ▷ Б и Б ▷ П дают Т ▷ П. Место считается проигранным
сведением. Зовётся после разбора цели и после №6.

| что | было → стало |
|---|---|
| корпус `nachalo-po-postroeniyu-chestnaya`, «начинается с аб» | на слове ядра 3 → 2 (запись 3) |

| файл | класс | код | что это |
|---|---|---:|---|
| `ч6-приставка.{flang,запись}` | честная | 0 | «Обещает длинную приставку» и «Приставка приставки взята» из фикстуры nachalo байт в байт; прежний сверщик — 3 |
| `ч6-чужая-приставка` | подделка (никогда не 0) | 3 | цель «начинается с "аг"», а вызванная обещает "абв". Копия без сличения приставок — 0 |
| `ч6-факт-без-вердикта` | подделка (никогда не 0) | 3 | у вызванной «вердикт нет вердикта» — её факта в реестре нет. Копия без реестра — 0 |

Первая попытка второй подделки («факт на слове»: у вызванной стёрты ходы) была
негодной и заменена: без ходов её «начинается с "абв"» закрывает разбор цели
вычислением, и факт всё равно попадает в реестр — подделка ничего не стерегла.

## Замер запаса №8: правила Ч1, Ч3, Ч4 по телу функции (10 сентября 2026)

Постусловие «результат начинается с П» (П — строковый литерал) без теоремы и без
блока вывода сверщик проигрывает сам (`начало_по_телу`), когда тело без вызовов
(`плоское_тело`: многострочное «если» склеено) начинается с П по построению: сам П
или литерал с началом П (Ч1; сличение двух написанных литералов), «соединить Л с Р»
при Л ▷ П (Ч3; Р не читается), «если У то А иначе Б» при А ▷ П и Б ▷ П (Ч4; У не
читается). Место считается проигранным сведением. Зовётся после разбора цели, №6 и
№7; снятое №7 второй раз не снимается. «Склейка начинается со «*2»» не берётся:
«"*"» не начинается с «"*2"», а счёт длины списка — второй вычислитель (не брать, №10).

| что | было → стало |
|---|---|
| корпус `nachalo-po-postroeniyu-chestnaya`, «выбор начинается общим началом» | на слове ядра −1 (запись 3) |

| файл | класс | код | что это |
|---|---|---:|---|
| `ч4-общее-начало.{flang,запись}` | честная | 0 | «Выбор начинается общим» из фикстуры nachalo байт в байт; прежний сверщик — 3 |
| `ч4-ветвь-другого-знака` | подделка (никогда не 0) | 3 | ветвь «иначе» — «соединить "б" с хвост». Копия, смотрящая только первую ветвь, — 0 |
| `ч4-правая-склейка` | подделка (никогда не 0) | 3 | обе ветви — «соединить хвост с "а"». Копия, читающая правый кусок, — 0 |

## Страховка №2–5: «результат не меньше 0» без теоремы (10 сентября 2026)

Постусловие «результат не меньше 0» без теоремы и без блока вывода сверщик берёт сам,
если тело неотрицательно по построению той же грамматикой, что у узла algebra и у
прямой теоремы ячейки 5 (`прямая_по_предположению`): Н2 из «требует», Н4, Н5, Н8
(произведение на литерал, №1) и — новое здесь — свёртка-счёт по Н7 в
`неотрицательно_algebra`: начало неотрицательно той же грамматикой, накопитель и
элемент — различные имена, шаг — счёт по форме (`шаг_счёта`: накопитель, накопитель
плюс неотрицательный литерал или выбор из двух таких). Свёртка разбирается РАНЬШЕ
«если» и «плюс»: её хвост тянется до конца терма. Место считается узлом, как у
algebra. Эти места в прогнозе партии закрывает печать блоков вывода; крюк держит их и
без печати — а в сегодняшнем дереве (печати блоков ещё нет) снимает их уже сейчас.

| что | было → стало |
|---|---|
| корпус `binder-wall-map` («свёртка: счёт неотрицателен») | 3 → 0 (запись целиком) |
| корпус `corpus-lists` («счёт вхождений неотрицателен») | 3 → 0 (запись целиком) |
| корпус `four-words` («утроенное неотрицательно») | 3 → 0 (запись целиком) |
| корпус `corpus-hof` («счёт по условию неотрицателен») | на слове ядра 5 → 4 (запись 3) |

| файл | класс | код | что это |
|---|---|---:|---|
| `стр-счёт-свёрткой.{flang,запись}` | честная | 0 | «Порог под свёрткой» из binder-wall-map байт в байт; прежний сверщик — 3 |
| `стр-утроить.{flang,запись}` | честная | 0 | «Утроить» из four-words байт в байт; прежний сверщик — 3 |
| `стр-шаг-минус` | подделка (никогда не 0) | 3 | шаг «акк минус 1». Копия без проверки шага — 0 |
| `стр-начало-минус` | подделка (никогда не 0) | 3 | начало «0 минус 1». Копия без проверки начала — 0 |
| `стр-умножить-на-н` | «никогда не 0» | 3 | «н умножить на н» — без литерала грамматика не берёт. Копия, у которой крюк не спрашивает тела, — 0 |
