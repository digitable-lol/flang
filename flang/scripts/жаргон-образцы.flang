модуль «Образцы жаргона»

// ОБРАЗЦЫ ВНУТРЕННИХ СЛОВ — БЕЗ РЕГУЛЯРНЫХ ВЫРАЖЕНИЙ.
//
// Девятнадцать образцов сторожа жаргона (`docs/jargon.json`, раздел `слова`)
// были записаны регулярками JavaScript и жили только в `scripts/jargon-guard.mjs`.
// Здесь они записаны формами языка. Каждый пример «ловит» и «не ловит» из
// `docs/jargon.json` перенесён СЮДА и прогоняется при каждой проверке: образец,
// переставший ловить своё слово, роняет проверку, а не тихо зеленеет.
//
// ── Из чего сложены все девятнадцать ───────────────────────────────────────
// Регулярок в языке нет, и они не понадобились: все девятнадцать образцов —
// это ровно четыре вида, и каждый выражается делением строки по литералу.
//
//   1. ЛИТЕРАЛ БЕЗ ГРАНИЦ — «побайтов», «дефункционализ», «defunctionaliz».
//   2. ЛИТЕРАЛ С ГРАНИЦЕЙ СЛЕВА — `(?<![а-яё])сторож`: слово не считается,
//      если слева к нему прирос алфавит («осторожно»).
//   3. ЛИТЕРАЛ С ОБЕИМИ ГРАНИЦАМИ — `(?<![a-z])twins?(?![a-z])`, `свод(?:а|е|…)?`:
//      набор окончаний раскрыт в СПИСОК ВАРИАНТОВ, потому что окончаний конечное
//      число и списком они читаются, а `(?:…)?` — нет.
//   4. ДВА ЛИТЕРАЛА ЧЕРЕЗ ПРОМЕЖУТОК — `неподвижн[а-яё]{0,3}\s+точк`: до трёх
//      букв алфавита, потом хотя бы один пробел.
//
// Граница берётся ДАРОМ из деления: `разделить текст по слово` кладёт слева
// от вхождения кусок, и достаточно посмотреть его последний знак. То, что в
// регулярке требует ретроспективной проверки, здесь — «последний знак куска».
//
// Без учёта регистра: текст и варианты приводятся к нижнему регистру своими
// силами — встроенного понижения для кириллицы нет, а «Ведомост», «Сторож» и
// «Побайтов» сторож ловит с заглавной.

// ── Алфавиты границ ────────────────────────────────────────────────────────

тотальная функция «Кириллица»
  возвращает строка
  пример «Тридцать три буквы»
    ожидается "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
  "абвгдежзийклмнопрстуфхцчшщъыьэюяё"

тотальная функция «Латиница»
  возвращает строка
  пример «Двадцать шесть букв»
    ожидается "abcdefghijklmnopqrstuvwxyz"
  "abcdefghijklmnopqrstuvwxyz"

тотальная функция «Пробелы»
  возвращает строка
  пример «Пробел, табуляция, перевод строки»
    ожидается " \t\n"
  " \t\n"

// ── Нижний регистр: кириллица тоже ─────────────────────────────────────────

// Встроенного понижения регистра нет, а стдлибовское «В нижний регистр» знает
// только латиницу. Здесь верхний и нижний ряды стоят знак в знак друг под
// другом, и понижение — это перенос позиции из одного ряда в другой.
тотальная функция «Верхний ряд»
  возвращает строка
  пример «Кириллица и латиница подряд»
    ожидается "АБВГДЕЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯЁABCDEFGHIJKLMNOPQRSTUVWXYZ"
  "АБВГДЕЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯЁABCDEFGHIJKLMNOPQRSTUVWXYZ"

тотальная функция «Нижний ряд»
  возвращает строка
  пример «Тот же порядок знак в знак»
    ожидается "абвгдежзийклмнопрстуфхцчшщъыьэюяёabcdefghijklmnopqrstuvwxyz"
  "абвгдежзийклмнопрстуфхцчшщъыьэюяёabcdefghijklmnopqrstuvwxyz"

тотальная функция «Строчный знак»
  принимает знак: строка
  возвращает строка
  пример «Заглавная кириллица»
    дано знак равно "С"
    ожидается "с"
  пример «Ё тоже понижается»
    дано знак равно "Ё"
    ожидается "ё"
  пример «Заглавная латиница»
    дано знак равно "W"
    ожидается "w"
  пример «Строчное остаётся собой»
    дано знак равно "ж"
    ожидается "ж"
  пример «Не буква остаётся собой»
    дано знак равно "-"
    ожидается "-"
  если («Верхний ряд») содержит знак
    то подстрока («Нижний ряд») с ((длина (голова (разделить («Верхний ряд») по знак))) плюс 1) по ((длина (голова (разделить («Верхний ряд») по знак))) плюс 1)
    иначе знак

тотальная функция «В нижнее»
  принимает текст: строка
  возвращает строка
  обеспечивает «понижение не меняет длины» (длина результат) равен (длина текст)
  пример «Смешанный текст»
    дано текст равно "Сторож Witness"
    ожидается "сторож witness"
  пример «Пустая строка»
    дано текст равно ""
    ожидается ""
  соединить (отобразить (разложить текст на символы) как знак → («Строчный знак» от знак)) по ""

// ── Границы ────────────────────────────────────────────────────────────────

тотальная функция «Последний знак»
  принимает кусок: строка
  возвращает строка
  пример «Последний знак куска»
    дано кусок равно "осто"
    ожидается "о"
  пример «У пустого куска знака нет»
    дано кусок равно ""
    ожидается ""
  если (длина кусок) равен 0
    то ""
    иначе подстрока кусок с (длина кусок) по (длина кусок)

тотальная функция «Первый знак»
  принимает кусок: строка
  возвращает строка
  пример «Первый знак куска»
    дано кусок равно "ый список"
    ожидается "ы"
  пример «У пустого куска знака нет»
    дано кусок равно ""
    ожидается ""
  если (длина кусок) равен 0
    то ""
    иначе подстрока кусок с 1 по 1

// Граница соблюдена, если алфавита не задано ИЛИ соседнего знака нет ИЛИ он не
// из алфавита. Это ровно то, что в регулярке записано как `(?<![а-яё])`.
тотальная функция «Граница слева»
  принимает кусок: строка, алфавит: строка
  возвращает признак
  пример «Слева приросла буква — не считается»
    дано кусок равно "осто"
    дано алфавит равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    ожидается нет
  пример «Слева пробел — считается»
    дано кусок равно "это "
    дано алфавит равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    ожидается да
  пример «Границы не задано — считается всегда»
    дано кусок равно "осто"
    дано алфавит равно ""
    ожидается да
  если ((длина алфавит) равен 0) или ((длина («Последний знак» от кусок)) равен 0)
    то да
    иначе не (алфавит содержит («Последний знак» от кусок))

тотальная функция «Граница справа»
  принимает кусок: строка, алфавит: строка
  возвращает признак
  пример «Справа приросла буква — не считается»
    дано кусок равно "ка по дереву"
    дано алфавит равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    ожидается нет
  пример «Справа пробел — считается»
    дано кусок равно " по дереву"
    дано алфавит равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    ожидается да
  пример «Границы не задано — считается всегда»
    дано кусок равно "ка"
    дано алфавит равно ""
    ожидается да
  если ((длина алфавит) равен 0) или ((длина («Первый знак» от кусок)) равен 0)
    то да
    иначе не (алфавит содержит («Первый знак» от кусок))

// ── Промежуток: до трёх букв, потом пробел ─────────────────────────────────

// Убывание — точным шагом по нат: осталось спускается ровно на 1.
тотальная функция «Снять буквы»
  принимает кусок: строка, осталось: нат
  возвращает строка
  пример «Две буквы сняты, дальше пробел»
    дано кусок равно "ая точка"
    дано осталось равно 3
    ожидается " точка"
  пример «Букв нет — кусок как был»
    дано кусок равно " точка"
    дано осталось равно 3
    ожидается " точка"
  если осталось не больше 0
    то кусок
    иначе если («Кириллица») содержит («Первый знак» от кусок)
      то «Снять буквы» от (если (длина кусок) не больше 1 то "" иначе (подстрока кусок с 2 по (длина кусок))) и (осталось минус 1)
      иначе кусок

тотальная функция «Снять пробелы»
  принимает кусок: строка
  возвращает строка
  пример «Пробелы сняты»
    дано кусок равно "  точка"
    ожидается "точка"
  пример «Пробелов нет»
    дано кусок равно "точка"
    ожидается "точка"
  разбор кусок
    случай пусто
      то ""
    случай голова и хвост
      то если («Пробелы») содержит голова
        то «Снять пробелы» от хвост
        иначе кусок

// Кусок ПОСЛЕ первого литерала: годится, если до трёх букв алфавита, потом
// хотя бы один пробел, потом второй литерал.
тотальная функция «Промежуток годится»
  принимает кусок: строка, конец: строка, букв: нат
  возвращает признак
  пример «неподвижная точка»
    дано кусок равно "ая точка самоприменения"
    дано конец равно "точк"
    дано букв равно 3
    ожидается да
  пример «неподвижный список — не то»
    дано кусок равно "ый список"
    дано конец равно "точк"
    дано букв равно 3
    ожидается нет
  пусть безбукв равно («Снять буквы» от кусок и букв)
  пусть безпробелов равно («Снять пробелы» от безбукв)
  ((длина безпробелов) меньше (длина безбукв)) и притом (безпробелов начинается с конец)

// ── Образец ────────────────────────────────────────────────────────────────

объект «Образец»
  «имя»: строка
  «вместо»: строка
  «варианты»: список строки
  «слева»: строка
  «справа»: строка
  «начало»: строка
  «конец»: строка
  «букв»: нат

// Вхождение варианта с соблюдёнными границами: делим текст по варианту и
// смотрим пары «кусок слева — кусок справа». Куски слева — все, кроме
// последнего; куски справа — все, кроме первого.
// Пары соседних кусков: между кусок[i] и кусок[i+1] стоит одно вхождение.
// Тотальна по структуре — разбор идёт по части списка.
тотальная функция «Пары кусков ловят»
  принимает куски: список строки, слева: строка, справа: строка
  возвращает признак
  пример «Один кусок — вхождений нет»
    дано куски равно ["тон и стиль"]
    дано слева равно ""
    дано справа равно ""
    ожидается нет
  пример «Два куска — вхождение между ними»
    дано куски равно ["это держит ", ""]
    дано слева равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    дано справа равно ""
    ожидается да
  пример «Слева приросла буква — не ловится»
    дано куски равно ["о", "но"]
    дано слева равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    дано справа равно ""
    ожидается нет
  разбор куски
    случай пусто
      то нет
    случай голова и хвост
      то если (длина хвост) равен 0
        то нет
        иначе (((«Граница слева» от голова и слева) и притом («Граница справа» от (голова хвост) и справа)) или («Пары кусков ловят» от хвост и слева и справа))

тотальная функция «Вариант ловится»
  принимает текст: строка, слово: строка, слева: строка, справа: строка
  возвращает признак
  пример «Слово стоит отдельно»
    дано текст равно "это держит сторож"
    дано слово равно "сторож"
    дано слева равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    дано справа равно ""
    ожидается да
  пример «Слово приросло слева — не ловится»
    дано текст равно "осторожно"
    дано слово равно "сторож"
    дано слева равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    дано справа равно ""
    ожидается нет
  пример «Вхождения нет вовсе»
    дано текст равно "тон и стиль"
    дано слово равно "эталон"
    дано слева равно "абвгдежзийклмнопрстуфхцчшщъыьэюяё"
    дано справа равно ""
    ожидается нет
  «Пары кусков ловят» от (разделить текст по слово) и слева и справа

тотальная функция «Ловит образец»
  принимает текст: строка, образец: «Образец»
  возвращает признак
  пусть низ равно («В нижнее» от текст)
  если (длина (образец.«начало»)) больше 0
    то свёртка (хвост (разделить низ по (образец.«начало»))) начиная с нет как найдено и кусок
      найдено или («Промежуток годится» от кусок и (образец.«конец») и (образец.«букв»))
    иначе свёртка (образец.«варианты») начиная с нет как найдено и слово
      найдено или («Вариант ловится» от низ и слово и (образец.«слева») и (образец.«справа»))

// ── Таблица: девятнадцать образцов ─────────────────────────────────────────

тотальная функция «Таблица образцов»
  возвращает список «Образец»
  [
    запись «Образец» с «имя» равным "эталон" и «вместо» равным "реализация на JavaScript / вторая реализация" и «варианты» равным ["эталон"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "близнец" и «вместо» равным "компилятор, написанный на flang" и «варианты» равным ["близнец", "близнеч"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "корпус" и «вместо» равным "программы репозитория / примеры / библиотека — смотря о чём речь" и «варианты» равным ["корпус"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "сторож" и «вместо» равным "проверка" и «варианты» равным ["сторож"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "ведомость" и «вместо» равным "отчёт о доказательствах" и «варианты» равным ["ведомост"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "побайтовая сверка" и «вместо» равным "сравнение байт в байт" и «варианты» равным ["побайтов"] и «слева» равным "" и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "свидетель" и «вместо» равным "реализация на JavaScript / вторая реализация / контрольный прогон" и «варианты» равным ["свидетел"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "изъятие" и «вместо» равным "снять правку и посмотреть, что сломается" и «варианты» равным ["изъяти"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "неподвижная точка" и «вместо» равным "компилятор печатает сам себя без изменений" и «варианты» равным (пустой список) и «слева» равным "" и «справа» равным "" и «начало» равным "неподвижн" и «конец» равным "точк" и «букв» равным 3,
    запись «Образец» с «имя» равным "дефункционализация" и «вместо» равным "снятие функций-значений одним проходом перед печатью" и «варианты» равным ["дефункционализ"] и «слева» равным "" и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "печатник" и «вместо» равным "генератор кода" и «варианты» равным ["печатник"] и «слева» равным («Кириллица») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "свод" и «вместо» равным "сводка / ветка сборки — смотря о чём" и «варианты» равным ["свод", "свода", "своде", "сводом", "своды", "сводов", "своду", "сводам", "сводах", "сводами"] и «слева» равным («Кириллица») и «справа» равным («Кириллица») и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "witness" и «вместо» равным "the JavaScript implementation / the second implementation" и «варианты» равным ["witness"] и «слева» равным («Латиница») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "twin" и «вместо» равным "the compiler written in flang" и «варианты» равным ["twin", "twins"] и «слева» равным («Латиница») и «справа» равным («Латиница») и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "corpus" и «вместо» равным "the programs in the repository / the examples / the library" и «варианты» равным ["corpus", "corpora"] и «слева» равным («Латиница») и «справа» равным («Латиница») и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "ledger" и «вместо» равным "the proof report" и «варианты» равным ["ledger", "ledgers"] и «слева» равным («Латиница») и «справа» равным («Латиница») и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "byte-for-byte" и «вместо» равным "compared byte for byte — say it in words, not as a term" и «варианты» равным ["byte-for-byte", "byte for byte", "byte-by-byte", "byte by byte"] и «слева» равным "" и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "fixed point" и «вместо» равным "the compiler prints itself unchanged" и «варианты» равным ["fixed-point", "fixed point"] и «слева» равным («Латиница») и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0,
    запись «Образец» с «имя» равным "defunctionalization" и «вместо» равным "functions-as-values are removed in one pass before printing" и «варианты» равным ["defunctionaliz"] и «слева» равным "" и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0
  ]

тотальная функция «Образец по имени»
  принимает имя: строка
  возвращает «Образец»
  свёртка («Таблица образцов») начиная с (запись «Образец» с «имя» равным "" и «вместо» равным "" и «варианты» равным (пустой список) и «слева» равным "" и «справа» равным "" и «начало» равным "" и «конец» равным "" и «букв» равным 0) как найден и образец
    если (образец.«имя») равен имя
      то образец
      иначе найден

// Здесь стоят ВСЕ примеры «ловит» и «не ловит» из `docs/jargon.json`, знак в
// знак. Ослабленный образец роняет проверку на этом месте.
тотальная функция «Ловится по имени»
  принимает текст: строка, имя: строка
  возвращает признак
  пример «эталон: ловит — эталон сверен побайтово»
    дано текст равно "эталон сверен побайтово"
    дано имя равно "эталон"
    ожидается да
  пример «эталон: ловит — у эталона другой ответ»
    дано текст равно "у эталона другой ответ"
    дано имя равно "эталон"
    ожидается да
  пример «эталон: ловит — эталонное ядро»
    дано текст равно "эталонное ядро"
    дано имя равно "эталон"
    ожидается да
  пример «эталон: не ловит — тон и стиль»
    дано текст равно "тон и стиль"
    дано имя равно "эталон"
    ожидается нет
  пример «близнец: ловит — близнец отстаёт»
    дано текст равно "близнец отстаёт"
    дано имя равно "близнец"
    ожидается да
  пример «близнец: ловит — у близнеца свой разбор»
    дано текст равно "у близнеца свой разбор"
    дано имя равно "близнец"
    ожидается да
  пример «близнец: ловит — близнечная сборка»
    дано текст равно "близнечная сборка"
    дано имя равно "близнец"
    ожидается да
  пример «близнец: не ловит — близко к пределу»
    дано текст равно "близко к пределу"
    дано имя равно "близнец"
    ожидается нет
  пример «корпус: ловит — по всему корпусу»
    дано текст равно "по всему корпусу"
    дано имя равно "корпус"
    ожидается да
  пример «корпус: ловит — корпус из пятидесяти трёх моделей»
    дано текст равно "корпус из пятидесяти трёх моделей"
    дано имя равно "корпус"
    ожидается да
  пример «корпус: не ловит — внутрикорпусный»
    дано текст равно "внутрикорпусный"
    дано имя равно "корпус"
    ожидается нет
  пример «сторож: ловит — сторож убывания»
    дано текст равно "сторож убывания"
    дано имя равно "сторож"
    ожидается да
  пример «сторож: ловит — это держит сторож»
    дано текст равно "это держит сторож"
    дано имя равно "сторож"
    ожидается да
  пример «сторож: ловит — сторожевая страница»
    дано текст равно "сторожевая страница"
    дано имя равно "сторож"
    ожидается да
  пример «сторож: не ловит — осторожно»
    дано текст равно "осторожно"
    дано имя равно "сторож"
    ожидается нет
  пример «ведомость: ловит — ведомость доказательств»
    дано текст равно "ведомость доказательств"
    дано имя равно "ведомость"
    ожидается да
  пример «ведомость: ловит — печатает ведомостью»
    дано текст равно "печатает ведомостью"
    дано имя равно "ведомость"
    ожидается да
  пример «ведомость: не ловит — осведомлённость команды»
    дано текст равно "осведомлённость команды"
    дано имя равно "ведомость"
    ожидается нет
  пример «ведомость: не ловит — неведомость исхода»
    дано текст равно "неведомость исхода"
    дано имя равно "ведомость"
    ожидается нет
  пример «побайтовая сверка: ловит — сверен побайтово»
    дано текст равно "сверен побайтово"
    дано имя равно "побайтовая сверка"
    ожидается да
  пример «побайтовая сверка: ловит — побайтовая сверка»
    дано текст равно "побайтовая сверка"
    дано имя равно "побайтовая сверка"
    ожидается да
  пример «побайтовая сверка: не ловит — по байту за раз»
    дано текст равно "по байту за раз"
    дано имя равно "побайтовая сверка"
    ожидается нет
  пример «свидетель: ловит — сверяется со свидетелем»
    дано текст равно "сверяется со свидетелем"
    дано имя равно "свидетель"
    ожидается да
  пример «свидетель: ловит — свидетель языка»
    дано текст равно "свидетель языка"
    дано имя равно "свидетель"
    ожидается да
  пример «свидетель: не ловит — очевидец»
    дано текст равно "очевидец"
    дано имя равно "свидетель"
    ожидается нет
  пример «изъятие: ловит — изъятие правила»
    дано текст равно "изъятие правила"
    дано имя равно "изъятие"
    ожидается да
  пример «изъятие: ловит — проверено изъятием»
    дано текст равно "проверено изъятием"
    дано имя равно "изъятие"
    ожидается да
  пример «изъятие: не ловит — взятие границы»
    дано текст равно "взятие границы"
    дано имя равно "изъятие"
    ожидается нет
  пример «неподвижная точка: ловит — неподвижная точка самоприменения»
    дано текст равно "неподвижная точка самоприменения"
    дано имя равно "неподвижная точка"
    ожидается да
  пример «неподвижная точка: ловит — сошлось в неподвижную точку»
    дано текст равно "сошлось в неподвижную точку"
    дано имя равно "неподвижная точка"
    ожидается да
  пример «неподвижная точка: не ловит — неподвижный список»
    дано текст равно "неподвижный список"
    дано имя равно "неподвижная точка"
    ожидается нет
  пример «неподвижная точка: не ловит — точка входа»
    дано текст равно "точка входа"
    дано имя равно "неподвижная точка"
    ожидается нет
  пример «дефункционализация: ловит — дефункционализация снимает»
    дано текст равно "дефункционализация снимает"
    дано имя равно "дефункционализация"
    ожидается да
  пример «дефункционализация: ловит — после дефункционализации»
    дано текст равно "после дефункционализации"
    дано имя равно "дефункционализация"
    ожидается да
  пример «дефункционализация: не ловит — функциональный язык»
    дано текст равно "функциональный язык"
    дано имя равно "дефункционализация"
    ожидается нет
  пример «печатник: ловит — печатник цели C»
    дано текст равно "печатник цели C"
    дано имя равно "печатник"
    ожидается да
  пример «печатник: не ловит — печать в C»
    дано текст равно "печать в C"
    дано имя равно "печатник"
    ожидается нет
  пример «свод: ловит — влито в свод»
    дано текст равно "влито в свод"
    дано имя равно "свод"
    ожидается да
  пример «свод: ловит — ветка свода»
    дано текст равно "ветка свода"
    дано имя равно "свод"
    ожидается да
  пример «свод: ловит — собрано на своде»
    дано текст равно "собрано на своде"
    дано имя равно "свод"
    ожидается да
  пример «свод: не ловит — сводка по дереву»
    дано текст равно "сводка по дереву"
    дано имя равно "свод"
    ожидается нет
  пример «свод: не ловит — сводный отчёт»
    дано текст равно "сводный отчёт"
    дано имя равно "свод"
    ожидается нет
  пример «свод: не ловит — это сводится к одному»
    дано текст равно "это сводится к одному"
    дано имя равно "свод"
    ожидается нет
  пример «свод: не ловит — свободный порядок»
    дано текст равно "свободный порядок"
    дано имя равно "свод"
    ожидается нет
  пример «witness: ловит — checked against the witness»
    дано текст равно "checked against the witness"
    дано имя равно "witness"
    ожидается да
  пример «witness: ловит — witness implementation»
    дано текст равно "witness implementation"
    дано имя равно "witness"
    ожидается да
  пример «witness: не ловит — without ness»
    дано текст равно "without ness"
    дано имя равно "witness"
    ожидается нет
  пример «twin: ловит — the twin lags behind»
    дано текст равно "the twin lags behind"
    дано имя равно "twin"
    ожидается да
  пример «twin: ловит — both twins agree»
    дано текст равно "both twins agree"
    дано имя равно "twin"
    ожидается да
  пример «twin: не ловит — twinkle»
    дано текст равно "twinkle"
    дано имя равно "twin"
    ожидается нет
  пример «twin: не ловит — intertwined»
    дано текст равно "intertwined"
    дано имя равно "twin"
    ожидается нет
  пример «corpus: ловит — across the corpus»
    дано текст равно "across the corpus"
    дано имя равно "corpus"
    ожидается да
  пример «corpus: ловит — two corpora»
    дано текст равно "two corpora"
    дано имя равно "corpus"
    ожидается да
  пример «corpus: не ловит — corporate»
    дано текст равно "corporate"
    дано имя равно "corpus"
    ожидается нет
  пример «corpus: не ловит — corporation»
    дано текст равно "corporation"
    дано имя равно "corpus"
    ожидается нет
  пример «ledger: ловит — the proof ledger says»
    дано текст равно "the proof ledger says"
    дано имя равно "ledger"
    ожидается да
  пример «ledger: ловит — two ledgers»
    дано текст равно "two ledgers"
    дано имя равно "ledger"
    ожидается да
  пример «ledger: не ловит — knowledge»
    дано текст равно "knowledge"
    дано имя равно "ledger"
    ожидается нет
  пример «ledger: не ловит — acknowledged»
    дано текст равно "acknowledged"
    дано имя равно "ledger"
    ожидается нет
  пример «byte-for-byte: ловит — byte-for-byte identical»
    дано текст равно "byte-for-byte identical"
    дано имя равно "byte-for-byte"
    ожидается да
  пример «byte-for-byte: ловит — compared byte by byte»
    дано текст равно "compared byte by byte"
    дано имя равно "byte-for-byte"
    ожидается да
  пример «byte-for-byte: не ловит — a byte of output»
    дано текст равно "a byte of output"
    дано имя равно "byte-for-byte"
    ожидается нет
  пример «fixed point: ловит — reaches a fixed point»
    дано текст равно "reaches a fixed point"
    дано имя равно "fixed point"
    ожидается да
  пример «fixed point: ловит — fixed-point property»
    дано текст равно "fixed-point property"
    дано имя равно "fixed point"
    ожидается да
  пример «fixed point: не ловит — fixed the point of failure»
    дано текст равно "fixed the point of failure"
    дано имя равно "fixed point"
    ожидается нет
  пример «defunctionalization: ловит — defunctionalization pass»
    дано текст равно "defunctionalization pass"
    дано имя равно "defunctionalization"
    ожидается да
  пример «defunctionalization: ловит — after defunctionalizing»
    дано текст равно "after defunctionalizing"
    дано имя равно "defunctionalization"
    ожидается да
  пример «defunctionalization: не ловит — functional language»
    дано текст равно "functional language"
    дано имя равно "defunctionalization"
    ожидается нет
  «Ловит образец» от текст и («Образец по имени» от имя)
