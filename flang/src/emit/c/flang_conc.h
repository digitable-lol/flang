/* SPDX-FileCopyrightText: 2026 Digitable (Marat Zimnurov) */
/* SPDX-License-Identifier: BSD-2-Clause */
/*
 * Планировщик конкурентности flang для бэкенда C — заголовок.
 *
 * Контракт модели — `flang/conc/SPEC.md`, шаг 6. Этот файл печатается бэкендом
 * как есть, байт в байт, и только той программе, в которой есть хоть один
 * `процесс`: программе без процессов планировщик не нужен, и возить его ей
 * значило бы возить неисполнимый код.
 *
 * ── Два режима, и второй объявляется вслух ─────────────────────────────────
 * Контракт называет два режима: проверочный — «одним потоком, чередование по
 * семени», рабочий — «параллельно, сколько даёт среда». Здесь есть ОБА, и
 * выбирает между ними одно поле запроса — `workers`.
 *
 * `workers` в 0 или 1 — проверочный режим, и он остался ровно тем же, чем был:
 * один поток, чередование берётся из генератора, заведённого семенем. Пункт 2
 * раздела «Что проверяется и чем» требует ПОБАЙТОВО того же журнала доставок
 * при том же семени, что у эталона, и это требование держится только здесь.
 *
 * Отсюда устройство проверочного режима: единственный источник недетерминизма —
 * выбор процесса из очереди готовых, и он берётся из семени. Всё остальное
 * (порядок ящика, порядок действий отклика, порядок срабатывания таймеров при
 * равном сроке, порядок перечисления процессов) определено жёстко и повторяет
 * `flang/src/conc.mjs` строка в строку.
 *
 * `workers` больше единицы — рабочий режим: столько потоков ОС, и каждый берёт
 * ГОТОВЫЙ ПРОЦЕСС и вычерпывает его ящик пачкой. Не пробег, а процесс, и это не
 * вкус: замер шага 6 (`flang/conc/SPEC.md`, «Кооперативный планировщик на одном
 * потоке») назвал числом, что раздать другому потоку ОДИН пробег в четыре–
 * четырнадцать раз дороже, чем выполнить его на месте, а взять незанятый
 * замок стоит 6–11 наносекунд. Значит платить за передачу надо один раз на
 * пачку сообщений, а не один раз на сообщение.
 *
 * ЧТО В РАБОЧЕМ РЕЖИМЕ ПЕРЕСТАЁТ БЫТЬ ГАРАНТИЕЙ — списком, чтобы это нельзя
 * было разменять молча:
 *
 *  1. Семя больше НЕ ОПРЕДЕЛЯЕТ чередование. Два прогона с одним семенем дают
 *     разные журналы доставок. Побайтовая сверка с эталоном к этому режиму
 *     неприменима, и итог прогона говорит об этом вслух — полем `workers`.
 *  2. Порядок записей журнала — порядок ЗАВЕРШЕНИЯ пробегов, а не их начала.
 *  3. Виртуальное время пробега — номер, который пробег занял в общем счёте;
 *     между процессами оно упорядочено лишь приблизительно.
 *  4. Порядок решений надзора между РАЗНЫМИ упавшими процессами не определён.
 *  5. Порядок срабатывания таймеров с равным сроком не определён.
 *
 * ЧТО ОСТАЁТСЯ ГАРАНТИЕЙ и в рабочем режиме:
 *
 *  1. Тотальность. Предел пробегов и предел числа процессов держатся ТОЧНО, а
 *     не приблизительно: пробеги роздают ломтями из общего счётчика, невыбранное
 *     возвращается, как только потоку становится нечего делать, и «пробеги
 *     кончились» объявляется лишь тогда, когда роздано сошлось с выполненным.
 *     Поэтому выполненных пробегов ровно столько, сколько объявлено, — ни одним
 *     больше и ни одним меньше.
 *  2. Атомарность пробега. Обработчик отработает целиком; половины изменения
 *     состояния не бывает, потому что изменения не бывает вовсе до возврата.
 *  3. Порядок ящика: сообщения от ОДНОГО отправителя приходят одному адресату в
 *     том порядке, в котором отправлены.
 *  4. Множество исходов. Всякое чередование, которое выдаёт рабочий режим, —
 *     одно из чередований атомарных пробегов, то есть такое, какое может выдать
 *     и проверочный на каком-то семени. Ровно это `SPEC.md` называет
 *     «наблюдательно неотличим», и ровно это проверяется на `race.flang`:
 *     исход рабочего режима обязан лежать в множестве исходов сетки семян.
 *
 * ── Что делит потоки ──────────────────────────────────────────────────────
 * Состояние принадлежит одному процессу и никому больше, значения неизменяемы,
 * разделяемой памяти в языке нет вовсе. Поэтому делить приходится ровно три
 * вещи, и у каждой свой замок:
 *
 *  - ЯЩИК И КУЧА ПРОЦЕССА: пишет отправитель, читает владелец. Замок на
 *    процесс, и он же несёт признак «покоится / в очереди / бежит», по которому
 *    процесс не может бежать на двух потоках разом.
 *  - СКЛАД ГОТОВЫХ: свой у каждого потока, свой замок. Процесс кладётся на
 *    СВОЙ склад — на склад того потока, который его разбудил, — а пустой поток
 *    подворовывает у соседей. Раскладка по остатку от номера была бы хуже, и
 *    насколько, измерено: при ней все потоки пишут во все склады, и на стенде
 *    «пары» шестнадцать потоков давали 1,15× при пятнадцати занятых ядрах.
 *  - ОБЩЕЕ ХОЗЯЙСТВО (счёт пробегов, журнал, отказы, решения, таймеры, таблица
 *    имён, арена вызывающего): один замок на всё. В горячем пути он берётся раз
 *    на `FL_CONC_TURN_SLICE` пробегов — за ломтём пробегов, — а не на каждый, и
 *    ровно этим числом определяется потолок: замером показано, что при ломте в
 *    64 пробега потолок стоит на десяти миллионах пробегов в секунду, а при
 *    ломте в 1024 доходит до тридцати.
 *
 * Порядок взятия всегда один: общий → процесс → склад. Вверх по этому порядку
 * не берут никогда, поэтому взаимной блокировки нет по построению.
 *
 * ── Что здесь лучше эталона: очередь готовых, а не перебор ─────────────────
 * Эталон на КАЖДОМ пробеге пересобирает очередь готовых полным перебором всех
 * объявленных процессов (`порядок.filter(…)`), и измерение шага 7 записало это
 * как найденное: переключение стоит O(числа процессов), около 2 мкс + 12–13 нс
 * на процесс. Здесь очередь готовых держится ДЕРЕВОМ ЧАСТИЧНЫХ СУММ над
 * признаком «готов»: и «стал готов / перестал», и «кто k-й по возрастанию
 * номера» стоят O(log P), а «сколько готовых» — O(1).
 *
 * Наблюдаемо это НЕ меняет ничего, и в этом весь смысл. Наружу от очереди видны
 * ровно две вещи, и обе видны в эталоне буквально: СКОЛЬКО готовых (на это
 * умножается число из семени) и КТО k-й ПО ВОЗРАСТАНИЮ среди них. Ни порядок в
 * памяти, ни способ его держать не наблюдаемы ничем. Поэтому семя выбирает тот
 * же процесс, и журнал совпадает побайтово — 4000 совпадений на 289 различных
 * чередованиях. Быстрее стал способ ответить, а не ответ.
 *
 * До этого здесь стоял отсортированный массив, и стоил он `memmove` половины
 * списка на каждое изменение: 88,1 мкс на переключение при 480 000
 * одновременно готовых процессов (`docs/scheduler-benchmark.md`, раздел 5).
 * Это была последняя стенка между «миллион молчащих процессов» и «миллион
 * работающих».
 *
 * ── Память ────────────────────────────────────────────────────────────────
 * Всё живёт в арене вызывающего и умирает вместе с ней. Состояния процессов,
 * сообщения в ящиках и записи журнала — значения flang, а значения flang
 * неизменяемы и лежат в арене; освобождать их по одному нечем и незачем.
 * Отсюда правило для вызывающего: `fl_conc_result` годен до ближайшего
 * `fl_arena_reset`, ровно как и всякое другое значение из рантайма.
 */
#ifndef FLANG_CONC_H
#define FLANG_CONC_H

#include "flang_runtime.h"

/* ───────────────────────────── план программы ─────────────────────────────
   Процессы, надзоры и прогоны печатаются ДАННЫМИ — одной таблицей на программу.
   Ровно так же они печатаются в Elixir (`conc_plan/0`), и по той же причине:
   надзор в flang — объявление, и одно объявление обязано остаться одним местом
   в напечатанном коде. Печать готовых функций-планировщиков размазала бы его по
   файлу и превратила правку одной строки исходника в правку десяти строк
   вывода. */

/** Вызов функции программы по её исходному имени flang (`<префикс>_call`). */
typedef fl_status (*fl_conc_call)(fl_ctx *ctx, const char *name, const fl_value *args, size_t count,
                                  fl_value *result, fl_error *error);

typedef struct fl_conc_process {
  const char *name;    /* имя процесса — оно же адрес в «отправить» */
  const char *handler; /* имя функции-обработчика */
  const char *initial; /* имя функции без параметров: начальное состояние */
  bool total;          /* завершение обработчика доказано */
  /* Запас витков нетотального обработчика; у тотального — 0, и это не «нет
     запаса», а «считать нечего»: планировщик подставит общий предел. */
  size_t budget;
  /* Объявленный размер ящика (`принимает «T» с ящиком N`, шаг А3 карты). Ноль —
     ящик неограничен, как было до А3. Ограниченный ящик делает переполнение
     НАЗВАННЫМ отказом отправителя (`FLANG_MAILBOX_FULL`), а не вторым способом
     исчерпать память узла: наклон «байт на застрявшее сообщение» у
     неограниченного ящика измерен и равен 454,3 байта. */
  size_t mailbox;
} fl_conc_process;

/** Ребёнок надзора: процесс или вложенный надзор со своей стратегией. */
typedef struct fl_conc_child {
  const char *name;
  const char *strategy; /* «перезапустить» | «остановить» | «передать выше» */
} fl_conc_child;

typedef struct fl_conc_supervisor {
  const char *name;
  const fl_conc_child *watch;
  size_t watch_count;
  const fl_conc_child *nested;
  size_t nested_count;
  bool threshold;       /* объявлен ли порог отказов */
  double failures;      /* N в «порог отказов N за M» */
  double window;        /* M, в единицах виртуального времени планировщика */
  const char *otherwise; /* стратегия сверх порога */
} fl_conc_supervisor;

/**
 * Построитель входных сообщений прогона: заполняет `messages[0..count-1]`.
 *
 * Функцией, а не таблицей значений: сообщение — значение flang, а значения
 * flang строятся в арене и до вызова не существуют. Печатается она тем же
 * `emitLiteral`, что и всякий другой литерал программы, — иначе «дано» в
 * исходнике и «дано» в напечатанном коде разошлись бы на первом же варианте с
 * полями.
 */
typedef fl_status (*fl_conc_inbox)(fl_ctx *ctx, fl_value *messages, fl_error *error);

typedef struct fl_conc_run_spec {
  const char *name;
  double seed;    /* «семя N» либо начало отрезка «семя от N до M» */
  double seed_to; /* конец отрезка; равен seed, если семя одно */
  const char *const *targets; /* кому какое сообщение — параллельно messages */
  size_t count;
  fl_conc_inbox build;
} fl_conc_run_spec;

typedef struct fl_conc_plan {
  const fl_conc_process *processes;
  size_t process_count;
  const fl_conc_supervisor *supervisors;
  size_t supervisor_count;
  const fl_conc_run_spec *runs;
  size_t run_count;
  fl_conc_call call;
} fl_conc_plan;

/* ───────────────────────────── итог прогона ─────────────────────────────
   Всё, что наблюдаемо снаружи, — и ничего больше. Числа здесь `double`, потому
   что виртуальное время в модели — число flang, а число flang это IEEE-754:
   считать его в size_t значило бы завести второй вид арифметики рядом с
   первым. */

/** Одна запись журнала доставок: один пробег обработчика. */
typedef struct fl_conc_entry {
  double time;
  size_t process;      /* индекс в plan->processes */
  const char *outcome; /* «обработано» | «отложено» | «продолжено» | … */
  const char *code;    /* NULL, если отказа не было */
  const char *reason;  /* текст отказа; NULL, если отказа не было */
  fl_value message;    /* доставленное сообщение */
} fl_conc_entry;

typedef struct fl_conc_failure {
  size_t process;
  const char *code;
  const char *reason;
  double time;
} fl_conc_failure;

/** Решение надзора: по записи на каждый затронутый процесс, в порядке принятия. */
typedef struct fl_conc_decision {
  double time;
  size_t process;
  const char *supervisor;
  const char *strategy;
  const char *code;
} fl_conc_decision;

typedef struct fl_conc_result {
  /* «покой» | «предел пробегов» | «отказ дошёл доверху» — зависания нет ни в
     одном случае, и это требование контракта, а не удобство. */
  const char *outcome;
  double time;
  size_t turns;
  /* Имена процессов по номеру и сколько их вышло. До `породить` это была та же
     таблица, что в плане, и читатель брал имя прямо оттуда. С порождением
     процессов у прогона больше, чем у плана, и брать имя из плана значило бы
     промолчать про всех, кого прогон завёл. Порядок: сперва объявленные в
     порядке объявления, потом порождённые в порядке рождения. */
  const char *const *names;
  size_t process_count;
  const fl_value *states; /* по процессу, в том же порядке, что `names` */
  const bool *alive;      /* по процессу, в том же порядке, что `names` */
  /* Журнал вели или нет. Признак нужен потому, что «ни одного пробега» и
     «пробеги были, но журнала не вели» дают одинаковые `journal_count == 0`, а
     это разные вещи: по журналу сверяются с эталоном побайтово, и сверка,
     сравнившая пустоту с пустотой, промолчала бы вместо того, чтобы покраснеть. */
  bool journal_kept;
  const fl_conc_entry *journal;
  size_t journal_count;
  const fl_conc_failure *failures;
  size_t failure_count;
  const fl_conc_decision *decisions;
  size_t decision_count;
  /* Сколько потоков вело прогон. Единица — проверочный режим, и тогда журнал
     повторяется по семени. Больше единицы — рабочий, и тогда не повторяется:
     поле стоит здесь именно затем, чтобы читатель итога не мог этого не
     заметить, а прогонщик — не мог промолчать. */
  size_t workers;
} fl_conc_result;

/** Сколько пробегов планировщик делает, прежде чем сдаться (как у эталона). */
#define FL_CONC_MAX_TURNS 10000

/**
 * Сколько процессов прогон может завести всего — объявленных плюс порождённых.
 *
 * Предел объявлен по той же причине, по которой объявлен предел пробегов, и
 * причина эта не «на всякий случай». `породить` в цикле заводит процессы без
 * конца, а слой обязан оставаться ТОТАЛЬНЫМ: у прогона должен быть исход, а не
 * исчерпание памяти узла. Поэтому порождение сверх предела — не смерть
 * программы, а НАЗВАННЫЙ отказ порождающего (`FLANG_PROCESS_LIMIT`), с которым
 * дальше разбирается надзор, ровно как с полным ящиком (шаг А3).
 *
 * Число выбрано так же, как `FL_CONC_MAX_TURNS`: это умолчание, а не потолок, —
 * запрос задаёт своё полем `processes`. Сто тысяч взято не с потолка: замер
 * (`docs/scheduler-benchmark.md`, раздел 2) назвал память работающего процесса,
 * и сто тысяч работающих — это тот порядок, на котором она ещё считается
 * гигабайтами, а не сотнями гигабайт.
 */
#define FL_CONC_MAX_PROCESSES 100000

/**
 * Сколько потоков рабочий режим может завести.
 *
 * Предел объявлен по той же причине, что два предыдущих: `workers` приходит из
 * запроса, а запрос может попросить что угодно, и «завести миллион потоков» —
 * не отказ среды, а зависание узла. Тысяча взята с запасом: замер шага 6 назвал
 * цену заведения потока на большой машине (118 мкс на 256 ядрах), и больше
 * потоков, чем ядер, всё равно не ускоряет ничего.
 */
#define FL_CONC_MAX_WORKERS 1024

/**
 * Сколько пробегов поток забирает из общего счёта за один заход.
 *
 * Это ЕДИНСТВЕННОЕ место, где потоки встречаются на каждом прогоне, и потому
 * ровно оно решает, где будет потолок. Ломоть отвязан от пачки, и вот замером
 * почему: когда ломоть равнялся пачке (64 пробега), потолок стенда «себе» на 64
 * процессах стоял на 10,1 миллиона пробегов в секунду и дальше шестнадцати
 * потоков не двигался; при ломте в 1024 те же шестнадцать потоков дали 22,8
 * миллиона, а тридцать два — 29,7. Мешал общий замок, взятый дважды на каждые
 * шестьдесят четыре пробега, и больше ничего.
 *
 * Тысяча двадцать четыре, а не больше: невыбранный остаток ломтя возвращается
 * только когда потоку становится нечего делать, и слишком щедрый ломоть у
 * одного потока — это ожидание у другого на коротком прогоне.
 */
#define FL_CONC_TURN_SLICE 1024

/**
 * Сколько сообщений поток вычерпывает из ящика, прежде чем отдать процесс
 * обратно на склад. Ровно то, ради чего рабочий режим раздаёт ПРОЦЕССЫ, а не
 * пробеги: передача платится один раз на пачку.
 *
 * От ломтя отвязано намеренно: пачка отвечает за СПРАВЕДЛИВОСТЬ (сколько один
 * процесс держит поток), ломоть — за цену общего замка. Смешав их, пришлось бы
 * выбирать между «долго держит» и «часто встречаются».
 */
#define FL_CONC_BATCH 64

/**
 * Один прогон: разнести входные сообщения, крутить пробеги, снять итог.
 *
 * `max_turns` в 0 означает `FL_CONC_MAX_TURNS`, `max_processes` в 0 —
 * `FL_CONC_MAX_PROCESSES`, `workers` в 0 — один поток (проверочный режим).
 * FL_ERROR возвращается только на
 * том, что к модели отношения не имеет, — нет такого прогона, кончилась память,
 * не построилось начальное состояние. Отказ ПРОЦЕССА ошибкой не является:
 * он записан в итоге, потому что отказ обязан кончаться исходом, а не статусом.
 *
 * `journal` — вести ли журнал доставок. Это НАБЛЮДЕНИЕ, а не работа, и стоит
 * оно памяти на каждом пробеге: арена не возвращает ничего, поэтому долгий
 * процесс с журналом течёт тем быстрее, чем дольше живёт. В режиме прогона
 * журнал обязателен — по нему идёт побайтовая сверка с эталоном, — в рабочем
 * режиме он не нужен вовсе. На наблюдаемое поведение он не влияет ничем: те же
 * исходы, те же состояния, те же решения надзора при том же семени.
 */
fl_status fl_conc_run(fl_ctx *ctx, const fl_conc_plan *plan, const char *run, double seed, size_t max_turns,
                      size_t max_processes, size_t workers, bool journal, fl_conc_result *out,
                      fl_error *error);

/** Сколько потоков рабочий режим завёл бы на этой машине, если о числе не
    сказано. Ноль — многопоточность в этой сборке выключена. */
size_t fl_conc_cores(void);

/** Индекс процесса по имени; SIZE_MAX — нет такого. Нужен прогонщику. */
size_t fl_conc_find(const fl_conc_plan *plan, const char *name);

#endif /* FLANG_CONC_H */
