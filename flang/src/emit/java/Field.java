/**
 * Поле записи или варианта: имя из модели и значение.
 *
 * Именно массив пар, а не Map: порядок полей наблюдаем — он попадает в текст
 * диагностики («запись {цена, количество}») и в размеченный JSON прогонщика, а
 * тексты сверяются с интерпретатором дословно. LinkedHashMap порядок бы
 * сохранил, но полей у записи единицы, и линейный поиск по массиву дешевле
 * хеширования кириллической строки; равенство же записей и так идёт по именам,
 * а не по порядку (Value.fieldsEqual).
 *
 * record, а не класс: неизменяемые данные с равенством по значению — ровно то,
 * для чего record и введён. Собственный equals здесь не нужен и не используется
 * (значения сравнивает Value.equal), но и вреда от него нет.
 */
public record Field(String name, Value value) {}
