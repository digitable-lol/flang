модуль «Цех»

// Пара к `08-supervisor-under-two-supervisors.flang`: то же дерево надзора,
// но каждый надзор стоит под одним. Ни одно правило не срабатывает.

объект «С»
  «н»: число

тип «Т»
  вариант «в» содержит «х»: число

объект «Отклик»
  «состояние»: «С»
  «действия»: список «Действие»

процесс «Работник»
  состояние «С»
  начинает с «нуль»
  принимает «Т»
  обрабатывает «шаг»

процесс «Счётчик»
  состояние «С»
  начинает с «нуль»
  принимает «Т»
  обрабатывает «долгий шаг» с запасом 2000 витков

надзор «Смена»
  процесс «Работник» стратегия «перезапустить»

надзор «Завод»
  надзор «Смена» стратегия «перезапустить»
  процесс «Счётчик» стратегия «перезапустить»

надзор «Дирекция»
  надзор «Завод» стратегия «остановить»

прогон «работник не падает»
  семя 1
  дано «Работник» принимает (вариант «в» с «х» равным 1)
  ожидается «Работник» стратегия «перезапустить» 0 раз

тотальная функция «нуль»
  возвращает «С»
  запись «С» с «н» равным 0

тотальная функция «шаг»
  принимает «т»: «С», «м»: «Т»
  возвращает «Отклик»
  запись «Отклик» с «состояние» равным «т» и «действия» равным []

функция «долгий шаг»
  принимает «т»: «С», «м»: «Т»
  возвращает «Отклик»
  запись «Отклик» с «состояние» равным «т» и «действия» равным []
