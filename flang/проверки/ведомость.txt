длина: кодовые точки, а не единицы UTF-16 (привет)
длина: пустая строка — ноль
длина: эмодзи — одна точка
длина: ё — одна точка
длина: пустой список
длина: список из трёх
символ: индекс от единицы
символ: последний
символ: эмодзи берётся целиком
код символа: латиница
код символа: кириллица
код символа: эмодзи — точка, а не суррогат
код символа: берётся первый символ
код символа: первый символ эмодзи
код символа согласован с символ 1 в …
символ по коду: латиница
символ по коду: кириллица
символ по коду: эмодзи — один символ, а не два суррогата
символ по коду: результат ровно один символ
символ по коду: слева от суррогатной дырки
символ по коду: справа от суррогатной дырки
символ по коду: последняя точка Unicode
символ по коду: круг замкнут на кириллице
символ по коду: минус ноль даёт тот же символ, что ноль
подстрока: границы включительно
подстрока: вся строка
подстрока: пустой промежуток
подстрока: пустая строка
соединить: две строки
соединить: список через разделитель
соединить: пустой список
соединить: список из одного
разделить: три части
разделить: разделителя нет — одна часть
разделить: пустая строка — одна пустая часть
разделить: пустая часть в середине
содержит: подстрока есть
содержит: регистр различается
содержит: элемент списка есть
содержит: элемента списка нет
начинается с: префикс совпал
начинается с: не префикс
начинается с: пустой префикс
к числу: целое
к числу: дробное с пробелами
к числу: показатель степени
к строке: целое
к строке: дробное
к строке: истина по-русски
к строке: ложь по-русски
к строке: строка остаётся собой
пусто: пустой список
пусто: список из нуля непуст
пусто: пустая строка
голова списка из трёх
хвост списка из трёх
хвост списка из одного пуст
элемент: индекс от единицы
элемент: последний
добавить: в конец
добавить: в пустой
элемент: список внутри списка достаётся целым
содержит: список списков сверяется структурно
остаток от: положительные
остаток от: знак берётся от делимого
остаток от: дробный
процентов от: десять от двадцати тысяч
процентов от: порядок действий (percent / 100) * value виден в младшем разряде
довод: файл .flang — это файл, а не корпус
довод: каталог — это корпус
довод: маска — это корпус даже с .flang на конце
корень: каталог остаётся собой
корень: косая в конце снимается
корень: маска обрезается по последней косой
маска: одна звезда не переходит через косую
маска: две звезды переходят через косую
маска: две звезды берут и нулевой каталог
маска: вопрос — ровно один знак, и не косая
отбор: не .flang не берётся
отбор: чужое поддерево не берётся
отбор: порядок не зависит от порядка перечисления
отбор: мимо корпуса идут .git и node_modules
свод: чистый корпус считает файлы и примеры
свод: корпус с ложным примером даёт НЕНУЛЕВОЙ код
свод: упавший файл назван поимённо
свод: назван и упавший пример, и его функция
свод: чистый файл в замечания не попадает
свод: итог считает оба файла и оба провала
СВОИ примеры считаются отдельно от ввезённых
файл без своих примеров назван поимённо, а не спрятан в числе
файл без своих примеров сосчитан отдельным числом
файл без своих примеров НЕ делает корпус красным
JSON несёт своё число примеров у каждого файла
невзятый файл — НЕ чистый корпус
невзятый файл назван словом «не взят», а не нулём примеров
невзятый файл несёт свой код отказа
невзятый файл считается отдельным числом
у отказа названа ЦЕНА: сколько своих примеров потеряно
потерянные примеры сосчитаны в итоге отдельным числом
потерянные НЕ попадают в число прогнанных своих
ведомость: по строке на файл, и своих примеров видно
ведомость: у настоящего слоя своё число не ноль
ведомость: невзятый назван словом и ценой
JSON: чистый корпус помечен valid
JSON: корпус с провалом помечен не-valid
JSON: упавший пример едет обеими сторонами, какие есть
JSON: невзятый файл помечен taken:false
