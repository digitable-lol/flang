# flang · asdf plugin

**[Documentation](https://digitable-lol.github.io/flang/en/install.html#asdf)** ·
**[Документация](https://digitable-lol.github.io/flang/install.html#asdf)**

The asdf and mise plugin: `bin/download`, `bin/install`, `bin/list-all`. Source lives in `packaging/asdf/` of the flang tree; published as `digitable-lol/asdf-flang`.
Плагин asdf и mise: `bin/download`, `bin/install`, `bin/list-all`. Источник — `packaging/asdf/` в дереве flang; публикуется как `digitable-lol/asdf-flang`.
