══ ПАРНЫЙ ЗАМЕР: flang/stdlib, 26 модулей ══

Снято командой `flang check --proof --json` по каждому модулю; «было» — двоичным
из закоммиченного семени ствола (e98eadb9), «стало» — двоичным из семени этой
ветки. Файлов измерено 25 из 26: json.flang не отдаёт ведомость ни до, ни после
(FLANG_RECURSION_LIMIT), и это не следствие правки.

Строк утверждений в ведомостях (обязательство, попавшее в ведомости нескольких
модулей, считается в каждой — так их и печатает `flang check`):

                        было    стало
  proved                 209      172
  proved-conditional       0       37     ← новое слово
  proved-induction        52       52
  grid                   808      808
  declared                 2        2
  ─────────────────────────────────────
  ВСЕГО                 1071     1071     ← сумма сошлась, ничего не пропало

209 − 172 = 37 = ровно столько, сколько встало под новым словом. Ни сетка, ни
индукция, ни отвергнутое не шелохнулись: правка НЕ ТРОГАЕТ правил вывода, она
переименовывает вердикт там, где вывод опёрся на недоказанную посылку.

Разных постусловий за этими 37 строками — СЕМНАДЦАТЬ (остальное — повторы одного
и того же обязательства в ведомостях модулей, которые его импортируют). То же
число независимо посчитано обходом ссылок `по свойству` по БАЗОВЫМ ведомостям
(schet2/sverka.py) и совпадает с замером соседней работы (ветка
u/uslovnaya-dokazannost, 17 из 164 на дереве двухдневной давности).

══ ПОРЧА: вердикт МЕНЯЕТСЯ ══

schet2/proba-a.flang и schet2/proba-b.flang различаются одной строкой — телом
функции «Пара», под которой стоит посылка «в паре два». Вывод «длина пары два»
доказан теоремой `по свойству «в паре два»` в обоих файлах.

  proba-a  посылка доказана       →  вывод: proved
  proba-b  посылка ослаблена      →  вывод: proved-conditional
           (сетка 1 значение)

До правки обе строки были знак в знак одинаковы («proved»). Прогон:
`sh schet2/porcha.sh <двоичный> <куда>`.
