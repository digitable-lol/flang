---
номер: 0048
заголовок: TLS собран целиком — https без чужого curl
статус: свободна (ветка из шапки не существует; сделано ли — НЕ УСТАНОВЛЕНО)
исполнитель: —
ветка: —
команда: первая
карта: Чего в языке нет вовсе
рядом: 0047
---

> **Состояние на 26 августа 2026.** Счётная часть закрыта прогонами: рукопожатие
> TLS 1.3 доходит до ServerHello с настоящего узла, сборка закрытой записи сверена
> с векторами RFC 8448, системная связка корней читается программой. Цельного
> https нет — четыре запора названы в разделе «Чем закрыта», и четвёртый из них
> (цена счёта: 87,93 с на 37 октетов против 3829 у настоящего узла) работой по
> TLS не устраняется вовсе.
>
> Статус оставлен `в работе`, а не `сделана`: условие готовности этой задачи —
> дословный вывод прогона https без curl, и его нет. Прежде здесь стояла строка
> «сделана частью …», от которой задачник числился разъехавшимся: проверка знает
> ровно четыре слова.

# 0048. TLS собран целиком — https без чужого curl

**Части есть, целого нет.** В дереве написаны и доказаны: SHA-256, HMAC, AES-128
в режимах CTR и GCM, X25519, чтение сертификата X.509, список отзыва CRL.
Записи TLS разбираются и печатаются — 233 обещания доказаны ядром.

**А https по-прежнему исполняет внешний `curl`.** То есть язык умеет посчитать
всё, из чего складывается защищённое соединение, но самого соединения не ведёт.

## Зависит от 0047

Пока программа не умеет открыть соединение вовсе, собирать TLS некуда. Сначала
0047, потом эта.

## Что осталось собрать

Сверить по спискам, что именно не написано, а не гадать. Известно, что есть:
рукопожатие частями, шифры, сертификат, отзыв. Известно, что цельного пути
«установить соединение → проверить сертификат → говорить» нет.

Отдельный вопрос, который надо решить и записать: **чему доверять**. Список
корневых удостоверяющих центров — это данные, которые придётся откуда-то взять,
и решение «берём системный список» либо «носим свой» надо назвать вслух.

## Как понять, что сделано

```
flang io <программа> → запрос по https к настоящему узлу, ответ получен,
                       сертификат проверен, curl не участвовал
```

Дословный вывод прогона, а не «поддержка добавлена».

## Что сделано на 26 августа 2026 (ветка `u/tls-end-to-end`)

**Опись снята чтением, а не памятью.** Версия определена чтением кода: это
TLS 1.3, RFC 8446, и только он — на RFC 5246 во всей `flang/stdlib/` нет ни
одной ссылки. Таблица «шаг рукопожатия — что в дереве — чего не хватает» на
двадцать девять шагов клиента лежит в
[`docs/zettel/tls-1-3-reaches-the-first-encrypted-record-and-thirteen-client-steps-are-not-started.md`](../docs/zettel/tls-1-3-reaches-the-first-encrypted-record-and-thirteen-client-steps-are-not-started.md):
готовы целиком 5, сделаны частью 11, не начаты 13.

**Чему доверять — решено и записано.** [`docs/adr/0012-trust-roots.md`](../docs/adr/0012-trust-roots.md)
(ADR-0012): корни доверия — довод программы, а не постоянная библиотеки;
умолчание двоичного хозяина — системный список, названный строкой в программе;
пустой связке не доверяют никому. Довод — замер: поручения
`«Прочитать октеты из файла»` не знают вовсе два хозяина из пяти.

**Шапка задачи отстала от дерева и здесь поправлена.** «Рукопожатие частями» —
это коммит `9cb4505b`: вывод ключей TLS 1.3, сборка своего ClientHello и
расшифровка первой закрытой записи сервера, всё в `flang/stdlib/tls.flang`.
Новых модулей он не заводил.

### Дословные выводы прогонов 26 августа 2026

Все прогоны — через ворота, `PAMYAT=45G`, двоичным `w-predely` 0.6.2.

**1. Соединение ведёт программа, curl не участвовал.** Голый TCP, порт 80:

```
$ flang io /srv/tmp/u-tls-end-to-end/proba-tcp.flang --plan "Проба"
{"plan":"Проба","result":"октетов пришло 825","orders":4,
 "log":[{"поручение":{"variant":"Открыть соединение","fields":{"адрес":"example.com","порт":80}},
         "отклик":{"variant":"Соединение открыто","fields":{"соединение":1}}},
        …"HTTP/1.1 200 OK…"…]}
код 0
```

**2. Наш ClientHello настоящий узел принимает и отвечает приветом, а не тревогой.**

```
$ flang io examples/https/tls-hello-to-a-real-host.flang --plan "Привет узлу"
{"plan":"Привет узлу","result":"октетов принято 3934, целых записей 3; первая
 запись: тип Handshake, версия TLS 1.2, длина 90; ServerHello, версия TLS 1.3,
 набор TLS_AES_128_GCM_SHA256, группа ecdh_x25519, ключ сервера
 9275896B297E7172B4DEBBB1509A8A9740AF167ACB661D9C7526660F1FEC4B55","orders":3,…}
код 0
```

Три поручения: открыть соединение к `example.com:443`, послать 139 октетов
своего ClientHello, прочитать ответ. Всё остальное — счёт на flang.

**3. Счётная часть сверена с тестовыми векторами RFC 8448** — настоящий вход,
настоящий ответ, без единого сокета:

```
$ flang io examples/https/rfc8448-records.flang --plan "Сверка с RFC 8448" --max-steps 4000000000
{"plan":"Сверка с RFC 8448","result":"сборка закрытой записи: сошлось; обратный
 ход: сошлось; сертификатов 1, октетов DER в первом 432, первый октет DER 48",
 "orders":0,"log":[]}
код 0
```

**4. Системная связка корней прочитана самой программой** — улика к ADR-0012:

```
$ flang io examples/https/system-trust-store.flang --plan "Прочитать корни"
{"plan":"Прочитать корни","result":"знаков в связке 182140, корней 121, октетов
 DER в первом корне 2007; доверять некому на пустой связке: да","orders":1,…}
код 0

$ … --no-read
{"error":"хозяину запрещено читать файлы","diagnostics":[{"code":"FLANG_IO_DENIED",…}]}
код 1
```

**5. Оба новых модуля проверены целиком:**

```
модуль «Рукопожатие TLS»: функций 312, из них с доказанным завершением 312; типов 25
flang/stdlib/tls-handshake.flang: проверено — разбор, типы, завершаемость, ядро и примеры; замечаний нет

модуль «Хранилище корней»: функций 136, из них с доказанным завершением 136; типов 12
flang/stdlib/trust-store.flang: проверено — разбор, типы, завершаемость, ядро и примеры; замечаний нет
```

## Докуда НЕ дошло, и почему — названо, а не спрятано

**Цельного `https` к настоящему узлу нет.** Рукопожатие обрывается там же, где
кончается прогон 2: ServerHello разобран, а дальше нужно расшифровать третью
запись — **3829 октетов закрытого текста** (замер того же прогона). Три запора,
каждый достаточен сам по себе:

1. **Обмен ключами не связан с рукопожатием.** `«Умножить точку»` из
   `x25519.flang` в `tls.flang` не ввозится: имя `«Нули»` есть и там, и в
   `sha256.flang`. Значит общий секрет посчитать ВНУТРИ плана нечем, а
   соединение между двумя прогонами не живёт.
2. **Подпись сервера проверить нечем.** CertificateVerify в TLS 1.3
   подписывается RSA-PSS, которого в дереве нет вовсе.
3. **Секрета взять неоткуда.** `«Случайное число»` повторимо; тайный ключ в
   примере взят из RFC 8448, то есть напечатан в открытом документе.
4. **ЦЕНА СЧЁТА, и этот запор работой по TLS не устраняется.** Замер: два
   действия AES-128-GCM над записью в 37 октетов — **87,93 с**
   (`/usr/bin/time` на прогоне 3 выше). Записи в 3829 октетов это даёт больше
   часа, а рукопожатие столько не живёт: трубку кладёт сервер. Разбор —
   [`docs/zettel/tls-one-encrypted-record-of-37-octets-costs-88-seconds-so-a-real-server-hangs-up-first.md`](../docs/zettel/tls-one-encrypted-record-of-37-octets-costs-88-seconds-so-a-real-server-hangs-up-first.md).

Поимённый список того, что для этого надо дописать в ЧУЖИХ файлах, —
[`docs/zettel/tls-six-things-the-client-needs-live-in-foreign-files-and-two-of-them-are-name-collisions.md`](../docs/zettel/tls-six-things-the-client-needs-live-in-foreign-files-and-two-of-them-are-name-collisions.md).

## ⚑ Статус перепроверен 5 сентября 2026

Стояло «в работе», исполнитель `u`, ветка `u/tls-end-to-end`. Ветки НЕТ:
у удалённого всего три ветки (`dev`, `main` и одна рабочая), и ни
одна из тридцати шести веток, названных задачами «в работе», среди
них не значится. Значит за задачей никто не стоит.

Статус переведён в «свободна», НО это не значит «не сделано»:
установлено только то, что работы никто не ведёт. Взявшийся обязан
сперва проверить, не сделано ли уже, и если сделано — закрыть.
