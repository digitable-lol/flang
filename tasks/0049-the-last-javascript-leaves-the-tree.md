---
номер: 0049
заголовок: Последний JavaScript уходит из дерева — язык обслуживает себя сам
статус: в работе (заход 9 сентября: опись 32 файлов оснастки с решением по каждому — ниже; первый ярус снимается)
исполнитель: агент-оснастка
ветка: r/js-osnastka-na-flang
команда: вторая
карта: Чего в языке нет вовсе
рядом: 0016, 0045
---

# 0049. Последний JavaScript уходит из дерева — язык обслуживает себя сам

Цель говорит: **всё написано на flang.** Сегодня в дереве **54 файла
JavaScript, 25 487 строк**, и **долгом из них являются 28 — 13 467 строк**.
Опись пересчитана прогоном 26 августа: `docs/javascript-inventory.md`.

Это не украшение цели: язык, который не может обслужить сам себя, не докажет и
чужую программу.

## Что уже сделано

**Реализации языка на JavaScript больше нет** — снята коммитом `fe8e8a37`. В
`flang/src/` остался один каталог `emit/`. Осталась только оснастка вокруг.

**Сторож жаргона переписан** (задача 0016, 25 августа): 107 функций, у всех
завершение доказано, **сошёлся со старым знак в знак на всём дереве**, трижды.
Снос старого — отдельная задача 0045.

## Что осталось и чем оно трудно

Замер 25 августа опроверг довод описи «цена механическая»:

1. **Движок образцов не годится сторожам по СКОРОСТИ.** Он умеет якоря, группы,
   чередование — больше, чем говорила опись. Но поиск места совпадения
   квадратичен: одно слово в 500 знаках — **35 секунд**, в 5000 — не кончилось за
   120. Страницы сторожа доходят до 270 000 знаков. Значит сторожа пишутся
   делением по литералу, а не образцами.
2. **Образцов не три, а восемнадцать.** Опись считала только объявленные в
   настройке; в самом стороже ещё пятнадцать, и они про очистку — есть замена
   **с функцией**, формы которой в языке нет вовсе.
3. **Цена: в семьсот раз дороже** — 0,3 с против 221 с у переписанного сторожа
   жаргона.

## Порядок

Не «переписать всё», а по одному, и каждый раз — **сверка знак в знак со
старым на всём дереве**. Частичная сверка обманывает: у сторожа жаргона 21
поверхность из 51 давала пустую разницу, а сторож при этом был сломан.

Начинать с тех, где нет просмотров вперёд и назад (их движок не поддерживает и
не будет — это решение в шапке модуля).

## Как понять, что сделано

`git ls-files '*.mjs' | wc -l` — ноль, и `./ярлык тесты` отвечает тем же кодом,
что до сноса.

---

## Опись, снятая 26 августа 2026

`git ls-files '*.mjs' '*.js'` — **54 файла, 25 487 строк**. В шапке задачи стоит
24 445: опись отстала на **1 042 строки**, файлов столько же.

**Но 7 файлов из 54 переписывать не нужно и нельзя** — это 8 180 строк, треть
описи:

| группа | файлов | строк | почему не уйдёт |
|---|---|---|---|
| **напечатано компилятором** | 3 | **6 365** | `flang/conc/{scheduler,wire,link}.js` несут в первой строке «Сгенерировано flang… Не редактировать руками». Это **вывод** языка, а не работа для него. Считать их долгом JavaScript — ошибка разряда: они и есть доказательство, что язык себя обслуживает |
| **рантайм цели js** | 4 | 1 815 | `flang/src/emit/js/*.js` уезжают в вывод ДОСЛОВНО. Уйдут только вместе с целью js |

Остаётся **47 файлов, 17 307 строк** — вот они целиком:

| группа | файлов | строк | что мешает снести |
|---|---|---|---|
| `flang/scripts` — сторожа и оснастка | 11 | 6 654 | четыре сторожа с просмотрами вперёд/назад (`claim` 443, `count` 612, `binary-rules` 779) плюс `name-guard` 530, которому нужны `\p{Script=…}` и разбор `flang ast` в JSON |
| `docs/site` — узел сайта | 11 | 4 102 | заперт дырой 2: точка раскрутки не знает `неотрицательное`, и `flang/scripts/жаргон-образцы.flang` не проверяется ни одним двоичным зоны |
| `scripts` — сборка, журнал, жаргон | 4 | 2 251 | `jargon-guard.mjs` 475 — задача 0045, заперта тремя запорами; `build-changelog*.mjs` 1 711 — журнал изменений |
| `flang/test` — проверки | 9 | 1 991 | ввозят функции, а не зовут команды; `--test` от Node |
| `flang/conc` — оснастка узла | 4 | 1 376 | `bin/node.js` 719 — полный узел на цели js |
| `benchmarks` — замеры | 3 | 579 | ничем; самое дешёвое, что есть |
| прочее (упаковка, редактор, wasm, lsp) | 5 | 354 | крючок установки npm и `extension.js` — npm и VS Code, чужие точки входа |

Самые крупные поимённо: `binary.mjs` 830, `link-collision-guard.mjs` 828,
`binary-rules-guard.mjs` 779, `proof-ledger.mjs` 768, `build.mjs` 958,
`build-changelog.mjs` 878, `build-changelog-page.mjs` 833.

### Что опись меняет в порядке работ

1. **Цель не «54 файла», а 47** — и объявлять надо так. Три файла из описи
   компилятор печатает сам; ставить их в долг значит требовать от языка
   переписать собственный вывод.
2. **Самое дешёвое — `benchmarks`**: 3 файла, 579 строк, ярлыки зовут без
   доводов. Начинать оттуда, а не со сторожей.
3. **Заход 0045 опроверг довод «цена механическая»** для сторожей ещё раз, уже
   на переписанном: вывод сошёлся знак в знак, а файл не ушёл — держат режимы
   `--list`/`--write` и тест, ввозящий пять функций. Сверять надо **поверхность
   (режимы, ключи, ввозимые функции)**, а не вывод одного режима.

Опись `docs/javascript-inventory.md` этими числами пока НЕ перепечатана — там
поправлены только строки про сторожа жаргона.

---

## Заход 26 августа: ветка `u/javascript-leaves-benchmarks`

Взято по разнарядке: `benchmarks/**`, `flang/test/**.mjs`, «прочее»,
`docs/javascript-inventory.md`. **Снесено ноль файлов, и вот чем это доказано.**

### 1. Опись пересчитана: долг не 47 файлов, а 28

`docs/javascript-inventory.md` переписана целиком. Числа снятые, не
переписанные: 54 файла, 25 487 строк, из них

| куча | файлов | строк |
|---|---:|---:|
| не долг: **вывод языка** | 7 | 8 180 |
| не долг: чужая среда и замеряемый материал | 10 | 1 849 |
| каталог тестов, которого «нет и не будет» | 9 | 1 991 |
| **ДОЛГ** | **28** | **13 467** |

Семь файлов вывода языка — `flang/conc/{scheduler,wire,link}.js` (6 365 строк,
шапка «Сгенерировано flang… Не редактировать руками», проверено грепом по всем
54) и `flang/src/emit/js/*.js` (1 815, уезжают в напечатанную программу
дословно).

**Крупнейшая правка сверх разнарядки: `flang/conc/bin/node.js` (719) — тоже не
долг.** Это хозяин узла на цели `js`, восьмой из восьми: рядом `node.c`,
`node.cs`, `node.ex`, `node.go`, `node.java`, `node.py`, `node.rs`, и все восемь
названы в `flang/scripts/node-across-targets.flang`. Убрать — вычеркнуть цель
`js` из счёта работающих целей.

Попутно опись отстала не только числами: **двенадцать имён из неё в дереве не
существуют** — их переименовал `b3167200`, а опись за ним не пошла.

### 2. Совет «начинать с benchmarks» верен, а его довод — нет

Довод описи был: «ярлыки зовут их без доводов». **Ярлык не зовёт их вовсе**
(`grep -c 'work.mjs\|count-library.mjs' ярлыки.flang` → 0), а руками их зовут
**с доводами** — так записано в `docs/benchmark-speed.md:552` и
`docs/emptiness-of-what-is-proved.md:51` («по одному модулю за раз»).

Дальше выяснилось обратное тому, что я сам записал сначала: **дыра 1 их не
держит.** Обход уже придуман и лежит в том же каталоге —
`benchmarks/speed/memory.flang` (299 строк, план на flang) берёт каталог сборки
числом `.sborka`, и `assemble.sh` без довода кладёт сборку именно туда. Значит
`work.flang` пишется по соседу, а не с нуля.

Из трёх файлов `benchmarks` долг — два: `work.mjs` (158) и `count-library.mjs`
(240). `tasks.mjs` (181) — сама замеряемая реализация на JavaScript, ряд «node»
в таблице замера; снять её значит стереть столбец сравнения.

### 3. Цена двойника замерена — и она НЕ семисоткратная

Про сторожа жаргона известно: 255 с на flang против 0,33 с у `node`. Проверено,
относится ли это к замерам. Взяты два счётчика содержательности, делающие одну
работу над одними и теми же двадцатью файлами `docs/benchmark2` и одним
двоичным: `count-20.flang` (на flang, в дереве, `./ярлык доказательства:20`) и
`count-library.mjs` с переставленным каталогом. Оба — через ворота, `PAMYAT=45G`.

| счётчик | стенные часы | пик RSS |
|---|---:|---:|
| на flang | **13,85 с** | 484 МиБ |
| на JavaScript | **4,60 с** | 51 МиБ |

**Втрое, а не в семьсот раз.** Разложено числами обоих прогонов, не прикидкой:
запусков компилятора у flang 77 (поручений 135), у JavaScript 75. Цена одного
запуска, снятая там, где своего счёта почти нет, — 4,60 ÷ 75 ≈ 0,061 с. Значит

| | запуски компилятора | свой счёт | итого |
|---|---:|---:|---:|
| на JavaScript | ≈ 4,6 с | ≈ 0,1 с | 4,60 с |
| на flang | ≈ 4,7 с | ≈ 9,2 с | 13,85 с |

Собственный счёт у flang дороже в десятки раз — и общий итог всё равно втрое.
Считать поэтому надо не «во сколько раз медленнее язык», а **какую долю прогона
язык вообще считает**: сторож жаргона занят своим счётом целиком и платит
семьсот раз, а счётчик две трети прогона ждёт потомка и платит три.

Оба долговых файла `benchmarks` — второго рода. По памяти разрыв
девятикратный (484 МиБ против 51), и у общей машины место у ворот берут по
памяти.

### 4. Тот же прогон нашёл беду в счётчике на JavaScript

Вердикты сличены поштучно: из двадцати утверждений **сошлись восемнадцать**.
Разошлись два, и оба раза прав счётчик на flang:

```
только flang: 11-unique.flang | «уникальные не удлиняют»         | ОСЛАБЛЕННОЕ
только flang: 13-even.flang   | «чётность есть делимость на два» | СОДЕРЖАТЕЛЬНОЕ
```

`count-library.mjs` печатает про эти два файла `доказано 0`. Причина — прогоном:

```
$ flang check docs/benchmark2/13-even.flang --proof
FLANG_EXAMPLE: пример «Минус четыре чётно» функции «Чётное»: … ожидалось true, получено false
docs/benchmark2/13-even.flang: не проверено — ведомость не печатается у программы с замечаниями
$ echo $?
1
```

Один красный пример гасит ВСЮ ведомость файла; счётчик на JavaScript читает
пустую ведомость как «ничего не доказано» и вычитает файл из меры молча.
`count-20.flang` в этом случае берёт вердикт у того же файла без примеров и
ставит отметку `[примеры файла красные]`.

**Это меняет правило сверки для этого файла.** «Сошлось знак в знак» опирается
на то, что старое поведение верно. Здесь оно неверно, и полное совпадение
означало бы, что двойник унаследовал беду. Сверять надо не «сошлось ли», а
«где разошлось и кто прав».

### 5. Что осталось и почему

**`flang/test/**.mjs` (9 файлов, 1 991) переписывать некуда.** Решение владельца
напечатано на странице сайта (`docs/site/learning.ru.md`): «Примеры лежат внутри
функции… **Отдельного каталога тестов нет и не будет**». Двойника у такого
каталога быть не может; девять файлов уходят вместе с теми, кто их зовёт. Четыре
из них (`glob`, `surface-pair`, `tempdir`, `toolchain-guard`) на тесты не
работают вовсе или не только на них — их тянут сторожа и узел сайта.

**«Прочее» (5 файлов, 354) — целиком не долг.** Запускатель и обёртка языкового
сервера стояли в `"bin"` пакета npm (круг: до их работы flang в системе ещё
нет), третий файл был крючком `"postinstall"`,
`editors/vscode/extension.js` — точка входа расширения, которую VS Code грузит в
свой процесс Node, `web/wasm/probe.mjs` ведёт настоящий браузер через Playwright.
Каждый называет свою причину в собственной шапке.

**`benchmarks`: 398 строк долга, ноль снесено.** `work.flang` без сверки не
снесёт `work.mjs`, а сверка у секундомера возможна только по контрольным суммам:
файл печатает медиану, минимум и максимум в миллисекундах, и два прогона одного
и того же `work.mjs` не сойдутся знак в знак друг с другом. Вдобавок сверка
требует `.sborka` — восьми сборок компилятора от `assemble.sh`.
`count-library.flang` требует полного прохода по библиотеке дважды: 3,76 с ушло
на один модуль в 295 строк, а библиотека — 34 476 строк, и `scram.flang` идёт
пятнадцать минут по собственному предупреждению файла.

Ничего не снесено — и ни одна ссылка не переведена: порядок «двойник → сверка →
ссылки → снос» на первом шаге и остановлен. За что 0045 не закрылась в свою
смену, здесь не повторено.

сперва проверить, не сделано ли уже, и если сделано — закрыть.

## Проверено 8 сентября 2026

Стояло «в работе», исполнитель `u`, ветка `u/javascript-leaves-benchmarks`; ветки на удалённом нет.

`git ls-files "*.mjs" | wc -l` = 52 (benchmarks, docs/site, scripts, flang/scripts). Условие «ноль» не выполнено.

---

## Заход 9 сентября 2026, ветка `r/js-osnastka-na-flang`: опись оснастки — 32 файла, решение по каждому

Стек этого захода — только оснастка: `scripts/*.mjs`, `flang/scripts/*.mjs`,
`flang/test/*.mjs`. Разнарядка называла 30 файлов (4 + 11 + 15); прогон
`git ls-files 'scripts/*.mjs' 'flang/scripts/*.mjs' 'flang/test/*.mjs' | wc -l`
на дереве `6dff8a3a9` даёт **32** (6 + 11 + 15), строк — **12 118** (`wc -l`).
Вне стека и не трогаются: `flang/src/emit/js/**`, `flang/conc/**`,
`examples/frameworks/**`, `docs/site/**`, `web/wasm/**`, `benchmarks/**`.

«Кто зовёт» снято грепом по дереву без `docs/zettel`, `tasks/completed`,
`tasks/rejected`, `docs/archive`, `CHANGELOG.md`; живыми считаются вызовы из
`.github/workflows/*.yml`, `scripts/*.sh`, `.githooks`, `ярлыки.flang` и ввозы
из других `.mjs`. Упоминания в прозе (`docs/*.md`, шапки) вызовами не считаются.

Правило решения: **УДАЛИТЬ** — только «ноль вызовов в дереве и CI» или
«превзойдён названным двойником, который уже стоит в точке вызова»; всё
остальное — **ПЕРЕНЕСТИ**. Ярус: 1 — этот заход; 2 — двойник есть или файл
небольшой, нужна сверка и фальсификатор; 3 — двойник частичен или его нет,
либо файл держат чужие каталоги.

### `flang/scripts/*.mjs` — 11 файлов, 6 719 строк

| файл | строк | что делает | кто зовёт (живое) | двойник на flang | решение и довод | ярус |
|---|---:|---|---|---|---|---:|
| `binary.mjs` | 848 | мост к двоичному для Node: запуск, разборы, ведомости, веера | ввозят 10 файлов стека (`target-words`, `word-guard`, `word-occupancy`, `name-guard`, `proof-ledger`, `claim-guard`, `flang-bridge`, `uzel-osnastka`, `planirovshchik-celi.test`, `svyaz-celi.test`) и **вне стека** `docs/site/{surfaces-run,podsvetka}.mjs` (сайт: `pages.yml` зовёт `node docs/site/build.mjs`, тот ввозит `podsvetka.mjs`) | `binary.flang` (497) — библиотека на flang, в Node не ввозится | ПЕРЕНЕСТИ: умирает последним, когда умрут все ввозящие; два ввоза из `docs/site` запирают снос — **решение у координатора** (перенести файл в `docs/site/` или снести вместе с узлом сайта) | 3 |
| `claim-guard.mjs` | 444 | утверждения прозы «в дереве нет X» сверяются с деревом | ярлык `утверждения:проверка` | нет | ПЕРЕНЕСТИ: 12 регулярок с просмотрами → деление по литералу, как в `jargon-guard.flang`; сегодня красен по делу (3 утверждения устарели, замер 8 сентября) | 2 |
| `count-guard.mjs` | 979 | числа прозы сверяются с деревом: потолок, ведомость, таблицы, `--fix`, `--дерево` | ярлык `подсчёты:проверка`; `word-guard.mjs` ввозит `ВЕДОМОСТЬ` | `count-guard.flang` (885) — 1 правило из 10, 8 мин (замер 8 сентября) | ПЕРЕНЕСТИ: дописать 9 правил; примета «СНЯТО 2026-09-09 файлов *.flang = 1313» переезжает в двойник | 3 |
| `direct-run.mjs` | 55 | «запущен напрямую или ввезён?» и раскодирование `file://` | ввозят 8 файлов стека и **вне стека** `docs/site/{surfaces-run,site-numbers}.mjs` | `direct-run.flang` (145) — чистая часть | УДАЛИТЬ после всех ввозящих: у flang вопроса «ввезён ли я» нет; `docs/site` запирает — решение у координатора | 3 |
| `discriminating-search.mjs` | 583 | различающий поиск: пробы подмены тел, 2146 прогонов | ярлык `поиск:различающий`; `binary.yml:1651` | `discriminating-search.flang` (435) — доходит до конца, но «проб запущено 0» | ПЕРЕНЕСТИ: двойник обязан гнать пробы и назвать те же 108 доказанных | 3 |
| `link-collision-guard.mjs` | 1099 | столкновения имён при связывании; `--дерево`, `--порча`, `--debt` | ярлыки `столкновения:проверка` и `:порча`; `binary.yml:1037,1045`; `scripts/identical-declarations.sh:80` ввозит `замыкание` | `link-collision-guard.flang` (789) — вход по умолчанию сходится (0 столкновений), режимов `--дерево`/`--порча` нет, 2 мин 21 с и 5,1 ГиБ; рядом `scripts/name-collision-guard.flang` (ярлык `столкновения:пересчёт`) | ПЕРЕНЕСТИ: режимы → планы; `identical-declarations.sh` — точка переключения сверх разнарядки | 3 |
| `name-guard.mjs` | 532 | имена по правилам Р1–Р7 через разбор дерева; `--debt`, `--list` | ярлык `имена:проверка` | нет | ПЕРЕНЕСТИ: нужен `flang ast` в JSON и классы букв без `\p{Script}`; 9 мин 41 с и 10,7 ГиБ у самого `.mjs`; примета «СНЯТО … = 1313» переезжает | 3 |
| `proof-ledger.mjs` | 786 | ведомость доказательств по корпусу; `--json`, `--files`, `--pustota` | ярлыки `доказательства:ведомость`, `:пустота`; `count-guard.mjs` ввозит; **вне стека** `docs/site/site-numbers.mjs` ввозит `ФАЙЛЫ`, `сводКорпуса` (ярлык `числа`) | нет; `ledger-summary.flang` — верхняя оценка без вычитания подключений | ПЕРЕНЕСТИ: часы прогона под воротами; `docs/site` запирает — решение у координатора | 3 |
| `target-words.mjs` | 383 | занятые слова девяти целей экранированы: подделка на каждую цель | ярлык `слова:проверка` | нет | ПЕРЕНЕСТИ: процессы, а не строки — цена втрое, не в сотни раз; сегодня красен по делу (подделка `cpp` не собирается) | 2 |
| `word-guard.mjs` | 706 | слова о доказанности в прозе; `--porcha`, `--perepis` | ярлыки `слово:проверка`, `слово:подлог`; `binary.yml:575,1533` | `word-guard.flang` (1132) — красен `FLANG_SLOVO_PUSTOTA` там, где `.mjs` зелен; порч нет | ПЕРЕНЕСТИ: сначала свести приговоры, затем шесть порч планом | 2 |
| `word-occupancy.mjs` | 304 | занятость слова в корпусе; слова — доводами командной строки | ярлык `слово:занятость`; `scripts/occupancy-check.flang:227` зовёт его подпроцессом (ярлык `слово:занятость:проверка`) | нет | ПЕРЕНЕСТИ: довод-слово → файл со списком слов рядом с планом (дыра 1 обходится так же, как `.sborka` у `memory.flang`) | 2 |

### `scripts/*.mjs` — 6 файлов, 2 881 строка

| файл | строк | что делает | кто зовёт (живое) | двойник на flang | решение и довод | ярус |
|---|---:|---|---|---|---|---:|
| `build-changelog.mjs` | 924 | печать `CHANGELOG.md` из тегов и коммитов; `--check`, `--self-test` | ярлыки `журнал`, `журнал:проверка`; `pages.yml` (пути) | `build-changelog.flang` (311, планы «Напечатать журнал изменений» и «Журнал не отстал») + `changelog.flang` (1439) | ПЕРЕНЕСТИ: сверить `CHANGELOG.md` знак в знак двумя печатями, фальсификатор — отставший журнал красит `--check` у обоих; шапка файла называет команду — переписать | 2 |
| `build-changelog-page.mjs` | 834 | страница вливаний `docs/site/changelog.md`; `--check` | ярлыки `журнал:страница`, `:проверка`; `pages.yml:101` | `build-changelog-page.flang` (190) + `changelog-page.flang` (1370) | ПЕРЕНЕСТИ: как у соседа; `changelog-page.flang:1056` печатает в шапку страницы имя `.mjs` — переписать | 2 |
| `jargon-guard.mjs` | 478 | внутренние слова на страницах для читателя; `--list`, `--write` | **только** `flang/test/jargon-guard.test.mjs` (ввозит 5 функций); ярлыки `жаргон:проверка/список/долг` уже зовут двойник | `flang/scripts/jargon-guard.flang` (1967, три плана = три режима) | УДАЛИТЬ: превзойдён `flang/scripts/jargon-guard.flang` — ярлыки переключены раньше (0045); сегодня `.mjs` код 1 (2 беды), двойник сверяется прогоном ниже | 1 |
| `latin-collision-guard.mjs` | 257 | два разных имени дают один латинский идентификатор после «Змейки» | `ci.yml:361`, `reprint.yml:239` | `scripts/latin-collision-guard.flang` (460) — никем не зовётся | ПЕРЕНЕСТИ: сверить приговоры, фальсификатор — подложенная пара имён красит оба; переключить два `yml` | 1 |
| `type-collision-guard.mjs` | 323 | одно имя типа объявлено дважды (вид 1) | **никто** (ни yml, ни sh, ни ярлык, ни хук) | `scripts/type-collision-guard.flang` (205) — тоже никем | УДАЛИТЬ: ноль вызовов в дереве и CI; двойник остаётся | 1 |
| `wasm-run.mjs` | 65 | запуск `.wasm` поверх `node:wasi` | **никто** (в `published-vs-tree.sh:538` только в списке «чужая среда»; `docs/wasm-via-c.md` — таблица) | нет; средой WASI служит `wasmtime` | УДАЛИТЬ: ноль вызовов в дереве и CI; в `docs/wasm-via-c.md` назвать `wasmtime`, из списка исключений `published-vs-tree.sh` убрать | 1 |

### `flang/test/*.mjs` — 15 файлов, 2 518 строк

| файл | строк | что делает | кто зовёт (живое) | двойник на flang | решение и довод | ярус |
|---|---:|---|---|---|---|---:|
| `flang-bridge.mjs` | 131 | мост Node → `flang io` для пяти обёрток ниже | только пять `*.flang.mjs` | — | УДАЛИТЬ: ноль вызовов | 1 |
| `glob.flang.mjs` | 27 | обёртка «тот же интерфейс, подкреплённый `glob.flang`» | **никто** | `glob.flang` (175) | УДАЛИТЬ: ноль вызовов | 1 |
| `surface-pair.flang.mjs` | 40 | обёртка над `surface-pair.flang` | **никто** | `surface-pair.flang` (654) | УДАЛИТЬ: ноль вызовов | 1 |
| `tempdir.flang.mjs` | 113 | обёртка над `tempdir.flang` | **никто** | `tempdir.flang` (130) | УДАЛИТЬ: ноль вызовов | 1 |
| `toolchain-guard.flang.mjs` | 86 | обёртка над `toolchain-guard.flang` | **никто** | `toolchain-guard.flang` (171) | УДАЛИТЬ: ноль вызовов | 1 |
| `uzel-osnastka.flang.mjs` | 38 | обёртка над `uzel-osnastka.flang` | **никто** | `uzel-osnastka.flang` (143) | УДАЛИТЬ: ноль вызовов | 1 |
| `jargon-guard.test.mjs` | 349 | 14 проверок проверки жаргона: зелёная сегодня, образцы ловят/не ловят, три порчи страниц, два выдуманных исключения, страницы сайта под маской | `ci.yml:389` (работа `jargon`, только по тегу; помечена красной: 6 из 14), ярлык `тесты` | 3 из 14 уже примерами «Проба страницы» в `jargon-guard.flang`; 19 образцов «ловит/не ловит» — 102 примера в `жаргон-образцы.flang` | ПЕРЕНЕСТИ в `flang/проверки/жаргон.flang` (число проверок до/после — в ячейке); что не переносится без `docs/site/sitemap` на flang — назвать | 1 |
| `glob.mjs` | 137 | обход по образцу | 3 теста узла, `uzel-osnastka.mjs`, `count-guard`, `proof-ledger`, `word-occupancy` | `glob.flang` (175) | УДАЛИТЬ после всех ввозящих | 3 |
| `tempdir.mjs` | 169 | рабочий каталог проб | 3 теста узла, `uzel-osnastka.mjs`; **вне стека** `flang/conc/bench/node-death-targets.mjs` (его самого не зовёт никто) | `tempdir.flang` (130) | УДАЛИТЬ после ввозящих; `node-death-targets.mjs` — решение у координатора | 3 |
| `toolchain-guard.mjs` | 90 | есть ли тулчейн цели | 3 теста узла; **вне стека** `node-death-targets.mjs` | `toolchain-guard.flang` (171) | то же | 3 |
| `uzel-osnastka.mjs` | 257 | сборка и подъём узла для проб | `nadzor-uzla.test.mjs`; **вне стека** `node-death-targets.mjs` | `uzel-osnastka.flang` (143, частью) | то же | 3 |
| `surface-pair.mjs` | 267 | сверка поверхностей пары примеров | **только вне стека** `docs/site/surfaces-run.mjs` (ярлыки `поверхности:*`) | `surface-pair.flang` (654) | в стеке вызовов ноль, но снос ломает узел сайта — решение у координатора | 3 |
| `nadzor-uzla.test.mjs` | 225 | надзор поднимает упавший процесс на девяти целях, три исхода | ярлык `тесты` (`ci.yml:464`); `tempdir-guard.flang:367` называет команду | `supervisor-across-targets.flang` (440) — `dvoyniki.yml` | ПЕРЕНЕСТИ: двойники шире по целям (9 против 8) и уже по случаям — 3 492 из 97 504 у трёх проб вместе (`dvoyniki.yml`, замер 6 сентября); довести до тех же случаев, потом снимать | 3 |
| `planirovshchik-celi.test.mjs` | 327 | планировщик узла на восьми целях, 67 744 сверки | ярлык `тесты` | `scheduler-across-targets.flang` (306) — `dvoyniki.yml` | то же | 3 |
| `svyaz-celi.test.mjs` | 262 | машина связи на восьми целях, 17 472 сверки | ярлык `тесты` | `link-across-targets.flang` (278) — `ci.yml:1645` | то же | 3 |

### Что заперто чужими каталогами и требует решения координатора

Четыре файла стека ввозит узел сайта `docs/site/**` (вне стека): `binary.mjs`
(из `podsvetka.mjs` и `surfaces-run.mjs`), `direct-run.mjs` (`surfaces-run.mjs`,
`site-numbers.mjs`), `proof-ledger.mjs` (`site-numbers.mjs`), `surface-pair.mjs`
(`surfaces-run.mjs`). Снять их — сломать `node docs/site/build.mjs`, который
зовёт `pages.yml`. Ещё три (`tempdir.mjs`, `toolchain-guard.mjs`,
`uzel-osnastka.mjs`) ввозит `flang/conc/bench/node-death-targets.mjs`, которого
не зовёт никто. Пока решения нет, эти семь остаются, и «32 → 0» этим заходом
недостижимо: потолок захода — **25 файлов**.

### Точки переключения сверх разнарядки

`scripts/identical-declarations.sh:80` (ввозит функцию из
`link-collision-guard.mjs`), `scripts/occupancy-check.flang:227` (зовёт
`word-occupancy.mjs` подпроцессом), `flang/scripts/tempdir-guard.flang:367`
(пример называет команду ярлыка `тесты`), `flang/scripts/guards-start.flang`
(обходит `flang/test/*.test.mjs` — при пустом наборе ответит провалом).

### Ход захода (числа — прогоном на дереве ветки, двоичный `bootstrap/flang` 0.7.16 из семени `6dff8a3a9`)

| ячейка | коммит | что | доказательство |
|---|---|---|---|
| 1 | `0daba6279` | сняты `flang-bridge.mjs` и пять обёрток `*.flang.mjs` (435 строк) | ноль вызовов: греп по дереву без каталогов истории называет их только друг из друга |
| 2 | `8fea01c39` | снят `scripts/wasm-run.mjs` (65) | ноль вызовов; `docs/wasm-via-c.md` и список исключений `published-vs-tree.sh` переписаны, WASI-среда — `wasmtime` |
| 3 | `c92244de0` | снят `scripts/type-collision-guard.mjs` (323) | ноль вызовов; двойник `scripts/type-collision-guard.flang` на том же дереве: «файлов 44, типов дважды объявлено 9 (по ведомости 9, новых 0)» = ответ `.mjs`; 140 с и 5,5 ГиБ против 0,22 с и 113 МиБ |
| 4 | этот | заведена `flang/проверки/жаргон.flang` — перенос `flang/test/jargon-guard.test.mjs` | `bootstrap/flang test flang/проверки/жаргон.flang`: «примеров 259, прошло 259, не прошло 0», 10 с (259 = 95 своих + 164 из подключённого `jargon-guard.flang`). Фальсификатор: испорчены два ожидания («дозволено 1» → 999, «Долг упал» → «Долг вырос») — «примеров 259, прошло 257, не прошло 2», код 1, оба примера названы; файл восстановлен байт в байт |
| 6 | этот | снят `scripts/latin-collision-guard.mjs` (257); `ci.yml` и `reprint.yml` зовут `bootstrap/flang io scripts/latin-collision-guard.flang --max-steps 2000000000` после `make -C bootstrap`; работа в `ci.yml` — по тегу и по кнопке (374 с дороже границы «пять минут на пуш») | на дереве оба код 0, «новых столкновений 0» (`.mjs`: 0,36 с, 101 МиБ, замыкание 44 файла, 9192 просьбы; двойник: 374 с, 5,5 ГиБ, 108 файлов четырёх каталогов, 12 022 просьбы). Фальсификатор: в `flang/stdlib/strings.flang` подложена пара «Обойти занятое целью C++» / «Обойти занятое целью C» — `.mjs` код 1 «НОВЫЕ СТОЛКНОВЕНИЯ ЛАТИНСКИХ ИМЁН: 1, oboyti_zanyatoe_celyu_c», двойник код 1 «латинских столкновений: 1 · oboyti_zanyatoe_celyu_c ← «Обойти занятое целью C» … «Обойти занятое целью C++»» за 352 с; подлог откачен. Ведомость `latin-collision-guard-known.tsv` двойник не читает — записей в ней 0, и это записано в её шапке |
| 5 | этот | сняты `scripts/jargon-guard.mjs` (478) и `flang/test/jargon-guard.test.mjs` (349); работа `jargon` в `ci.yml` зовёт `LC_ALL=C.UTF-8 bootstrap/flang test flang/проверки/жаргон.flang` после `make -C bootstrap`, без гейта по тегу и без `setup-node`; задача 0045 закрыта | превзойдён `flang/scripts/jargon-guard.flang`: на дереве обе реализации код 1 с одними и теми же двумя бедами (`docs/memory-and-regions.md:1282` «сторож», `docs/wasm-via-c.md:524` «побайтов»), 0,38 с против 117 с и 3,4 ГиБ; ярлыки `жаргон:*` звали двойник и до этого захода. Пробы — ячейка 4 |

Перенос теста жаргона: из 14 проверок JS перенесены **11** чистыми примерами
(образцы ловят/не ловят и не повторяются; порча чистой страницы, рост долга,
упавший долг; исключение с выдуманной цитатой и объяснение позже употребления;
проза без кода и имён файлов; строки исходника; литерал-образец с кавычкой;
довод у «вне запрета»). **Не перенесены три**, и почему: № 1 «сегодня зелёный»
— это прогон по дереву, его делает ярлык `жаргон:проверка` (`flang io`, тот же
файл сторожа); № 13 «каждая страница карты сайта под маской» — карта сайта
живёт в `docs/site/sitemap.mjs` (вне стека), на flang её читать нечем; хвост
№ 10 «код сторожа не выпущен наружу как строка» проверял исходник самого
`.mjs`, который снимается. Настоящий `docs/jargon.json` (№ 2) примером не
читается — проверена та же функция на встроенном тексте.

### Что измерено, но не снято этим заходом

**`build-changelog.mjs` против `build-changelog.flang`** (план «Журнал не отстал»,
`--max-steps 2000000000`): на дереве ветки оба код 1, но приговоры разные.
Node за 0,4 с: «правило видимости не знает путей: .flangrc, flangtutor», затем
«вышел релиз 0.7.16, а в журнале последний — 0.7.14» и 60 коммитов, которых нет
в журнале. Двойник за 341 с и 988 МиБ: «правило видимости не знает путей»
и **двадцать** имён — `.githooks`, `.gitmodules`, `tasks`, `ROADMAP.md`,
`DESCRIPTION.md`, `flang/ct`, `flangtutor` и ещё тринадцать. Список видимости
у двойника отстал от `.mjs` на восемнадцать путей; пока он не догнан, печать
журнала двумя реализациями сравнивать не с чем, и оба `build-changelog*.mjs`
остаются (ярус 2, точки — `pages.yml:23–24,101` и ярлыки `журнал*`).

**Остаток стека — 21 файл** (`git ls-files 'scripts/*.mjs' 'flang/scripts/*.mjs'
'flang/test/*.mjs' | wc -l`): 11 в `flang/scripts/`, 8 в `flang/test/`, 2 в
`scripts/`. У каждого в таблице выше стоит, кто его зовёт и чего не хватает
двойнику; семь из них держат `docs/site/**` и `flang/conc/bench/**`, и решение
по ним — у координатора, не у этого стека.

## Волна 2 — ветка `r/js-osnastka-volna-2` от `bc1ff161e` (10 сентября 2026)

Числа на входе волны: в трёх каталогах 21 файл `.mjs`, в дереве 55 `*.js,*.mjs`
(`git ls-files`, дерево `bc1ff161e`).

| ячейка | что | доказательство |
|---|---|---|
| 7 | `flang/test/surface-pair.mjs` → `docs/site/lib/surface-pair.mjs` (единственный потребитель — `docs/site/surfaces-run.mjs`) | `node docs/site/surfaces-run.mjs --check` до и после: код 1, «разбивка слотов на странице даёт 76, а прогон — 74» (красен на dev до переезда), 0,40 → 0,38 с |
| 8а | `scripts/changelog.flang` догнал правило видимости Node: +3 видимых корня (`flang/cat`, `flang/ct`, `flang/examples`; `flang/cat` стоял служебным), +17 служебных корней истории, +2 корня, которых не знал и Node (`.flangrc`, `flangtutor`); пример «планы и заметки не видны» переписан | печать: `CHANGELOG.md` и `changelog.json` от `node scripts/build-changelog.mjs` (1,3 с) и от плана «Напечатать журнал изменений» (335 с, 1,7 ГиБ) совпали **байт в байт** (`cmp`). Проверка: план «Журнал не отстал» (345 с, 1,0 ГиБ) и `--check` у Node — оба код 1, те же две причины («правило видимости не знает путей: .flangrc, flangtutor», «вышел релиз 0.7.16, а в журнале последний — 0.7.14»); Node сверх того перечисляет 60 коммитов, которых нет в журнале, двойник — нет |
| 9 | `ci.yml`: работы `id-collisions` и `jargon` возвращены на flang — слияние `a15930b9d` взяло `ci.yml` со стороны dev, и обе работы снова звали снятые `.mjs` | текст из `207972afb`; в шапку `id-collisions` дописан замер скомпилированного пути |
| 10 | `flang/scripts/binary.flang`: код 3 («не проверено») несёт ведомость целиком — как у `binary.mjs`, который разбирает JSON при любом коде | дерево `4b8dbf338`, двоичный собран `make -C bootstrap -B` (sha256 `1311b4b4…`): `flang test flang/scripts/binary.flang` — 126 из 126, 338 с; план «Сторож слов» — код 0, «нарушений 0», 132 с, 3,3 ГиБ; `node flang/scripts/word-guard.mjs` — код 0, «нарушений 0; проверено файлов 1, утверждений 4», 0,84 с. Фальсификатор: то же дерево, строка кода 3 откачена — двойник код 1, `FLANG_SLOVO_PUSTOTA` «ведомость без примеров не сложилась», 87 с |
| 8б | журнал изменений печатает и сверяет flang: ярлыки `журнал` и `журнал:проверка` зовут `scripts/build-changelog.flang`; шапка журнала называет эту команду; `pages.yml` следит за `.flang`-файлами журнала; комментарии `ci.yml`/`release.yml` названы заново | печать двойника — код 0, 324 с, 0,7 ГиБ; сверка «Журнал не отстал» — код 0, 345 с, 1,0 ГиБ. `node scripts/build-changelog.mjs` на том же дереве печатает то же, кроме четырёх строк шапки (старая команда) и поля `unclassified` в `changelog.json` — Node не знает корней `.flangrc` и `flangtutor`, и его `--check` поэтому код 1. Прежний `--self-test` — `flang test` обоих файлов: 160 из 160 (4,2 с) и 172 из 172 (3,6 с); `flang io` примеров плана не гоняет (проба: план с ложным примером — код 0), поэтому тесты стоят в ярлыке `журнал:проверка` отдельной командой. Фальсификатор: из `CHANGELOG.md` вынута строка последнего коммита — сверка двойника код 1, `FLANG_ZHURNAL_OTST…` «журнал разошёлся с историей репозитория» (360 с, 1,5 ГиБ), `--check` у Node код 1 с той же причиной; журнал откачен байт в байт. `build-changelog.mjs` остаётся: его `решение` и `словоПравок` ввозит `build-changelog-page.mjs` |
| 11 | снят `flang/scripts/target-words.mjs` (383): ярлык `слова:проверка` зовёт двойник `flang/scripts/target-words.flang` (ячейка T, 1017 строк); `kernel-forgeries.flang` (правило и пример, постусловие длины 107 → 109), шапка фикстуры `poddelka-slova-celey.flang`, `docs/why-javascript-remains.md` и ведомость перечней (`target-words.mjs:252` → `target-words.flang:34`) названы заново | работник T на `4b8dbf338`: `.mjs` и двойник — код 1, одна находка «подделка: cpp — НАПЕЧАТАННОЕ НЕ СОБИРАЕТСЯ», 51 строка против 51, расходится 0; 56 с/0,27 ГиБ против 100 с/1,6 ГиБ; программы по спискам занятых слов девяти целей совпали байт в байт. Фальсификатор T: обёртка двоичного через `FLANG_BINARY` снимает экранирование `func` у печати Go — оба код 1, те же три находки (cpp + две по go), 62 строки против 62; дерево и двоичный после отката те же по sha256. На дереве ветки после сноса: `./ярлык слова:проверка` — код 1, та же находка cpp, 103 с, 1,5 ГиБ; `flang check kernel-forgeries.flang` — замечаний нет, 103 функции с доказанным завершением; примеров 106 из 106; `ярлыки.flang` 34 из 34, «Целость» — код 0; перепись перечней: новых 24 → 23, мёртвых 19 → 19. Не перенесены режимы `--cel=` и `--tolko-poddelka`: доводов у `flang io` нет, и их не зовёт ни ярлык, ни CI, ни скрипт (греп) |
| 12 | снят `flang/scripts/claim-guard.mjs` (444): ярлык `утверждения:проверка` зовёт двойник `flang/scripts/claim-guard.flang` (ячейка K, 1806 строк, 136 функций, у всех завершение доказано); запись «Место» в `scripts/file-extensions.flang` смотрит в двойник (в нём тот же список расширений); мёртвая строка ведомости перечней `claim-guard.mjs:39` снята; `docs/why-javascript-remains.md` назван заново | работник K на `4b8dbf338`: `.mjs` и двойник — код 1, те же 3 устаревших утверждения («символ по коду»: `docs/ct/spec.md:1073`, `flang/self/parser.flang:6342`, `tasks/completed/1389-…:202`), текст отказа совпал знак в знак (1628 знаков); 1,26 с/113 МиБ против 485 с/1,74 ГиБ; кандидаты до вопроса лексеру — 38 строк у обоих; 4462 фразы против регулярок JavaScript — расхождений 0. Фальсификатор: в `docs/guide/limits.ru.md` подложена ложь «Формы «код символа» в языке нет.» — оба код 1, 4 находки, вывод совпал (1752 знака); правда «Формы «мурзилка» в языке нет.» не поймана ни одним; страница откачена (sha256 `32ca8001…`). Замена `\p{L}` таблицей диапазонов: буквы тайской и деванагари после «нет» читаются иначе — в дереве таких мест 0. На дереве ветки после сноса: `./ярлык утверждения:проверка` — код 1, те же 3 находки, 489 с, 1,7 ГиБ; `file-extensions` — код 0 (26 мест отбора); `ярлыки.flang` 34 из 34, «Целость» 0; перепись перечней новых 23, мёртвых 19 |
| — | ссылки после ячеек 11–12: снятые имена в описи 0049, в задачах 1722 и 6858, в `ROADMAP.md` и двух страницах дорожной карты сайта внесены в `link-guard-known-not-a-path.tsv` как история (так же, как в волне 1); строка-исключение `CHANGELOG.md → flang/test/changelog.test.mjs` снята — её цель ушла из шапки журнала с ячейкой 8б | проверка ссылок до правки: код 1, битых 14 из 6115 (11 — эти снятые имена), устаревших исключений 1; после: код 1, битых 2 из 6119, устаревших 0 (448 с). Остались две битые, которых этот стек не заводил: `docs/design/proof-object-blok-vyvoda.md` (была на dev) и ссылка задачи 1000 на скрипт сверки записи с источником, которого в дереве нет (пришла коммитом `3901cd805`). Проверка подделок ядра (`подделки:проверка`) на ветке отказывает кодом 3 «семя сверить не удалось» — так же на gh/dev `6d1f919c1`; с `SEMYA_OTSTALO_ZNAYU=1` на `ac723eaf1`, `652ea3dc3` и `5359922e2` — одни и те же 20 нарушений строка в строку, то есть ячейки 10–12 её не сдвигают |
| 13 | ярлыки `слово:проверка`, `слово:подлог` и шаги `binary.yml` (работы `build` и `guards-selftest`) зовут двойник `flang/scripts/word-guard.flang` (ячейка W: полный ответ, планы «Подлог слов» и шести порч); `word-guard.mjs` НЕ снят — его `СЛОВАРЬ`, `говоритДоказано`, `пустота`, `словоСтроки` ввозит `proof-ledger.mjs` | работник W на `4b8dbf338`: текст ответа двойника совпал с `.mjs` байт в байт во всех восьми режимах (проверка: код 0/0, 0,82 с против 117 с, 2,2 ГиБ; подлог: код 0/0, 4,5 с против 210 с, 3,3 ГиБ; шесть порч — текст тот же, код у `.mjs` 2 «поймана», у двойника 0: план не может вернуть 2, по контракту `flang io` это «кривой вызов»; отдельные порчи никто не зовёт). Фальсификатор: в фикстуре цель ослаблена «равен 4» → «не меньше 0» — оба код 1, те же две находки; откат по sha256 `518d7b87…`. Изъян `.mjs` повторён нарочно и закреплён примером: `\b` в `/доказан[оы]\b/u` не видит границы после кириллицы — правило о «доказано» у сетки мёртвое. `--perepis` не перенесён: его образцы живут только в `count-guard.mjs`, и его никто не зовёт. Ярлыки из корня отвечают кодом 3 «семя сверить не удалось» — тот же отказ, что у `подделки:проверка` и на gh/dev; шаги CI зовут двоичный напрямую, мимо этой заслонки. Команды шагов CI из корня дерева ветки: «Сторож слов» — код 0, «нарушений 0; проверено файлов 1, утверждений 4», 113 с, 2,2 ГиБ; «Подлог слов» — код 0, «чисто молчит, 6 порч(и) пойманы», 209 с, 3,3 ГиБ — обе под пятиминутной границей пуша, память в пределе раннера (16 ГиБ, хранилище публичное) |

**Скомпилированный путь для сторожа латинских столкновений закрыт языком, а не
временем:** `bootstrap/flang emit scripts/latin-collision-guard.flang --target c
--cli --runtime flang/src/emit/c` → 17,7 с, код 1, `FLANG_PLAN_UNSUPPORTED:
цель «c» не умеет печатать объявление «план» … печатает одна цель — «js»`.
Сторож — план; цель `js` вернула бы Node. Работа остаётся по тегу и по кнопке.
Перемерено 10 сентября своим двоичным (дерево `8591d55ce`, sha256 `1311b4b4…`,
`--out` в рабочий каталог): код 1, тот же `FLANG_PLAN_UNSUPPORTED`-отказ
(«цель «c» не умеет печатать объявление «план»»), 17,6 с, 0,35 ГиБ; ни одного
файла не напечатано — время сборки и прогона снять нечем.

**Сторож жаргона по дереву в CI остался:** работа `neznanye-storozha`
(`uncalled-guards`) в `ci.yml` на каждый пуш в `main`/`dev`, по тегу и по
кнопке зовёт `./ярлык жаргон:проверка` после пробы «краснеет на внутреннем
слове». Перенос проб его не трогал.

**Двойник страницы вливаний непригоден:** план «Напечатать журнал вливаний»
(`scripts/build-changelog-page.flang`, `--max-steps 2000000000`) за 2 ч 34 мин
дошёл до 10 млн шагов и занял **295 ГиБ** (снят по PID вручную; `node
scripts/build-changelog-page.mjs` — 32 с). `build-changelog-page.mjs` остаётся
в ярусе 3 до разбора, что в плане копит память.

**Цена сторожа имён на Node — замер один раз** (дерево `90b02a615`, двоичный
sha256 `1311b4b4…`): `node flang/scripts/name-guard.mjs` — код 1, 601 с,
10,9 ГиБ; охват 294 файла из 1326, «ВЫПОЛНЕНО — 30 имён снято; перепишите
долг: --debt» (код 1 здесь значит «долг упал, ведомость не переписана»).
Двойника пока нет.
