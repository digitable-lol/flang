---
номер: 0052
заголовок: Связывание перестало быть квадратом от размера замыкания
статус: сделана
исполнитель: u
ветка: u/linking-not-quadratic
команда: любая
карта: Цена доказательства
рядом: 0020, 0028, 0051
---

# 0052. Связывание перестало быть квадратом от размера замыкания

Замер записан в шапке `scripts/raskrutka.sh` и снят отдельно:

```
Связать исходники     23 726 585 витков   59 % предела
показатель роста      2,04                то есть КВАДРАТ
```

Вывод оттуда же, дословно: «Убрать ядро из проверки перед печатью НЕ СПАСЛО БЫ.
Связывание — шаг, к ядру отношения не имеющий, — уже съедает 59 % предела…
следующий рост той же величины умножает на 2,3».

## Почему квадрат

Та же причина, что у поиска функции по имени (задача 0020): **перебор списка по
имени, повторённый для каждого имени**. Образец из печатника:

```flang
тотальная функция «Найти функцию»
  «Первая из функций» от (отфильтровать «функции» где «функция» → («функция».«имя») равен «имя»)
```

`отфильтровать` проходит ВЕСЬ список и не останавливается на найденном. Соседняя
`«Есть функция с именем»` строит новый список из всех имён на каждый вызов.

**Таких функций в компиляторе 217**, и «Найти функцию» есть отдельно в каждом
печатнике: C, JS, Java, C#, Elixir, Rust (ввозит из C), плюс `factcheck`.

## Что уже известно про починку

Задача 0020 сделана: словарь в вычислителе дал **в 59 раз меньше витков в
поиске** (874 млн → 14,7 млн на `proof-kernel.flang`).

**И там же названа ловушка:** словарь становится быстрым только после того, как
доказаны его обещания. Первая попытка провалилась именно на этом — недоказанные
обещания едут в код проверками, и вставка обходила весь словарь.
`hashmap.flang` теперь доказан (задача 0035), значит ловушка снята.

## Порядок

По одному месту, каждый раз с замером витков до и после и сверкой вывода **байт
в байт**. Оснастка со счётчиками готова: `/srv/flang-rabota/u-slovar-podklyuchenie`.

## Как понять, что сделано

Показатель роста снят заново и назван числом. Витки связывания до и после.

---

# Замер 26 августа 2026: показатель 2,04 по виткам НЕ подтверждается

Ветка `u/linking-not-quadratic`. Всё ниже снято прогонами через ворота
(`PAMYAT=45G`), двоичным `/srv/flang-rabota/w-predely/bootstrap/flang`.

## Чем мерено, раз перепечатку запускать нельзя

Правка в `flang/self/link.flang` в собранном двоичном не появится без
перепечатки, поэтому сторона «до» снята НАСТОЯЩИМ двоичным на синтетике, а
стороны «до» и «после» сравниваются вычислителем на том же самом файле
`link.flang`, который правится.

**Витки шага названы самим двоичным.** `repl_call` печатает строку «ЗАПАС ШАГОВ
НА ИСХОДЕ: «Связать исходники» съел N витков из L», как только шаг съедает
больше половины предела, и счётчик витков у каждого шага СВОЙ
(`repl_ctx.steps = 0` перед вызовом). Значит достаточно удваивать
`--предел-шагов`, пока прогон не пройдёт: на первой же удаче строка называет
цену связывания и ТОЛЬКО связывания — ни типов, ни завершаемости, ни ядра в
этом числе нет.

Синтетика: входной файл из n тотальных функций и один ввозимый модуль. Ввоз
есть, значит работает и второй проход — тот, ради которого файл перечитывается.

## Таблица роста

| n функций | витки «Связать исходники» | ×  к предыдущей | витков на функцию |
|---:|---:|---:|---:|
| 500  |   194 012 | —    | 387,6 |
| 1000 |   387 732 | 2,00 | 387,3 |
| 2000 |   779 160 | 2,01 | 389,4 |
| 4000 | 1 562 016 | 2,00 | 390,4 |
| 6000 | 2 344 872 | 1,50 при росте входа в 1,5 | 390,7 |

**Показатель роста по виткам — 1,00, а не 2,04.** От 500 к 6000 вход вырос в
12,0 раза, витки — в 12,09. Цена на функцию постоянна с точностью до 0,8 %.

## Почему счётчик витков не видит квадрата

Квадрат в коде ЕСТЬ, и он там, где названо в задаче: `«Есть в таблице»` зовёт
`«Найти в таблице»`, а та делает `отфильтровать «таблица» где …` — проход по
ВСЕМУ списку пар, и делается он на каждом объявлении (`«Слить функцию»`,
`flang/self/link.flang`), да ещё второй раз внутри `«Положить в таблицу»`.

Но **`отфильтровать` печатается в C циклом с встроенным телом, а не вызовом**:
`fl_tick` стоит только на входе в функцию (`fl_enter`) и на витке хвостового
цикла (`«Хвостовой цикл»`, `flang/self/emit-c.flang`). Проход по элементам
списка не тикает НИ РАЗУ. Поэтому счётчик витков считает вызовы, а не работу, и
квадратичный обход для него невидим.

**Зато он видим памяти, и вот чем.** `«Печать цикла дальше»` печатает перед
циклом строку

    FL_TRY(fl_list_alloc(ctx, <список>.as.list.count, &<массив>, error));

то есть КАЖДЫЙ `отфильтровать` заводит массив длиной во ВЕСЬ вход, сколько бы
элементов ни отобралось. У `«Есть в таблице»` отбирается ноль или один, а
заводится n. Арена не отдаёт этого до закрытия области, значит слияние n
объявлений заводит порядка n² ячеек: на 6000 функциях это 6000 × 2 × 6000
значений — гигабайты, которых никто не просил.

А чем тогда заняты 390 витков на функцию? Разбором: `«Связать исходники»`
разбирает файлы САМА (`отобразить «файлы» как «файл» → «Разобрать файл»`) и
файлы с ввозом разбирает второй раз (`«Переразобрать файл»`). Разбор линеен, и
именно он составляет почти всю ту цену, которую видит предел шагов.

**Следствие для перепечатки.** 23 726 585 витков связывания — это в основном
разбор 110 849 строк, и растёт он ЛИНЕЙНО. Рост дерева в полтора раза даст не
×2,3, а ×1,5: около 35 миллионов из 200 000 000, то есть 18 % предела вместо
59 % сегодняшних. По ШАГАМ связывание перепечатку не уронит.

## Время и память того же шага растут КВАДРАТОМ, и вот тут 2,04 подтверждается

`flang ast <файл>` — это разбор, связывание, две отметки анализа и печать: ни
типов, ни завершаемости, ни ядра там нет вовсе. Кривая этой команды — кривая
связывания и есть.

Одиночный файл из n функций плюс ввоз:

| n | время | память |
|---:|---:|---:|
| 500  |  0,86 с |   106 604 КиБ |
| 1000 |  2,48 с |   233 580 КиБ |
| 2000 |  8,02 с |   528 412 КиБ |
| 4000 | 27,41 с | 1 183 760 КиБ |
| 6000 | 70,80 с | 1 794 064 КиБ |

Цепочка из k модулей по 50 функций (каждый ввозит следующий — так устроено
дерево, а не одним файлом):

| n | время | память |
|---:|---:|---:|
| 500  |  0,71 с |   114 700 КиБ |
| 1000 |  1,78 с |   249 256 КиБ |
| 2000 |  5,00 с |   547 372 КиБ |
| 4000 | 16,85 с | 1 286 284 КиБ |
| 6000 | 38,45 с | 1 788 012 КиБ |

**Показатель роста по времени — 1,76 на одном файле и 1,60 на цепочке, и он сам
растёт**: попарно 1,53 → 1,69 → 1,77 → **2,34** и 1,33 → 1,49 → 1,75 → **2,03**.
То есть на последнем шаге замера это уже ровно квадрат, и дальше он только
ближе к двойке. Показатель 2,04 из шапки `scripts/raskrutka.sh` **верен, но
верен про ВРЕМЯ, а не про витки**, и различать это важно: предел шагов
связывание не уронит, а часы и память — да.

## Что и почему исправлено

Три места в `flang/self/link.flang`, и все три — «перебор списка по имени,
повторённый для каждого имени», как и названо в задаче.

1. **Слияние.** `«Слить тип»` и `«Слить функцию»` спрашивали
   `«Есть в таблице»` — проход по всей таблице происхождений, — а затем
   `«Положить в таблицу»` спрашивала то же самое ВТОРОЙ раз. Теперь у связки
   есть `«набор типов»` и `«набор функций»`: имена, склеенные в одну строку и
   каждое в своих ёлочках. Принадлежность — встроенная `содержит`, добавление —
   встроенное `соединить`; обе стоят один виток независимо от длины. Пара в
   таблицу кладётся прямо, без повторного поиска.
   Ложного попадания не бывает: **ёлочек внутри имени не бывает** — проверено на
   всём дереве, 14 709 объявлений, ни одного имени с `«` или `»` внутри.
2. **Второй проход.** `«Шаг подмены»` спрашивал происхождение каждой функции
   через `«Значение по ключу»` — снова проход по всей таблице. Теперь у связки
   есть `«файлы функций»`, список, идущий ноздря в ноздрю с `«функции»`
   (дописываются они всегда вместе — в `«С функцией»` и в
   `«С повтором функции»`), и происхождение берётся по номеру, за один виток.
   Он же убрал проход в `«Постусловия своей»`.
3. **Подмены сведены по файлу.** Искали их в одном плоском списке ВСЕХ файлов
   по тройке «поле, имя, файл», а файл в подмене — тот, из которого она
   приехала. Теперь `«Все подмены»` отдаёт `список «Свод подмен»` — по своду на
   файл, — и просматривается только свой файл. Порядок внутри файла тот же,
   поэтому `«Последняя из подмен»` выбирает то же объявление.

Ещё одна правка не про перебор, а про повтор: `«Постусловия из файла»` стояла
ДОВОДОМ внутри свёртки по теоремам файла и пересчитывалась на каждой теореме,
хотя от теоремы не зависит. Вынесена наружу (`«Донести теоремы файла»`), и файл
без теорем не платит за неё вовсе.

**Ни одного нового слова языка. Ни одного нового ввоза.** Словарь из
`stdlib/hashmap.flang` сюда не годится: он параметрический (`«Узел хеша» от «З»`),
а общий двоичный параметрического типа не понимает вовсе — на этом же сегодня
спотыкается `flang check flang/self/interpret.flang` у всех (`FLANG_TYPE_ARGS`,
разбор задачи 0033). Введи его в `link.flang` — и `link.flang` перестал бы
читаться любым нынешним двоичным.

## Замер до и после — на САМОМ `link.flang`, вычислителем

Правка в `flang/self/**` до собранного двоичного не доезжает без перепечатки,
поэтому обе стороны сняты `flang run` на пробе, которая ввозит НАСТОЯЩИЙ
`flang/self/link.flang` и зовёт `«Связать разобранное»` на построенном входе.
Разбора в пробе нет: программы собраны узлами, мерится ровно связывание.
Двоичный один и тот же, ворота `PAMYAT=45G`, между сторонами меняется только
`link.flang`.

Дверь «слияние» — вход без ввозов, второго прохода нет:

| n | до | после | память |
|---:|---|---|---:|
| 200 | 168,11 с,  5 257 252 КиБ | 170,00 с, 4 589 028 КиБ | −12,7 % |
| 400 | 175,02 с,  7 614 080 КиБ | 170,22 с, 4 988 296 КиБ | −34,5 % |
| 800 | 188,68 с, 13 047 724 КиБ | 172,79 с, 5 770 996 КиБ | −55,8 % |

Прирост памяти на удвоении входа: было ×2,31, стало ×1,96 — то есть линейно.

Дверь «второй проход» — вход с ввозом:

| n | до | после | память |
|---:|---|---|---:|
| 100 | 174,68 с,  5 270 140 КиБ | 173,67 с,  4 990 940 КиБ |  −5,3 % |
| 200 | 180,19 с,  7 664 580 КиБ | 181,55 с,  6 554 500 КиБ | −14,5 % |
| 400 | 190,51 с, 16 941 436 КиБ | 184,36 с, 12 083 592 КиБ | −28,7 % |

Дверь «второй проход и теорема» — то же плюс теорема в ввозимом файле, то есть
работает и донос теорем:

| n | до | после | память |
|---:|---|---|---:|
| 100 | 169,02 с,  5 450 308 КиБ | 169,67 с,  5 005 364 КиБ |  −8,2 % |
| 200 | 174,34 с,  8 228 232 КиБ | 173,11 с,  6 586 432 КиБ | −20,0 % |
| 400 | 191,77 с, 17 373 368 КиБ | 185,55 с, 12 120 224 КиБ | −30,2 % |

**Честная граница.** У двери со вторым проходом квадрат УМЕНЬШИЛСЯ, но не
исчез, и причина в форме пробы: все n функций лежат в ОДНОМ файле, а свод
подмен режет перебор по файлам. В дереве файлов 232 и функций в файле десятки,
поэтому там свод и работает; на пробе из двух файлов резать нечего.

Время в этих числах почти неподвижно: у каждого прогона `flang run` есть
постоянная плата около 168 секунд — связать и отметить замыкание самой пробы, —
и на её фоне видно только память и последние проценты. Отсюда и выбор мерять
памятью.

## Чем доказано, что вывод не поехал

Сверялось три раза, и все три — байт в байт.

1. **Настоящие два модуля, первая дверь** (`«Связать исходники»`: разбор здесь
   свой). `экспортирует` у ввозимого, `только` у ввозящего, два объекта, сумма
   с вариантами, обещание, пример, невывезенная функция. Слепок связанной
   программы печатью `«Печать значения»`: **5 401 байт, `cmp` — совпало**;
   диагностик 0 и до, и после.
2. **Синтетика на 50 функций** со ввозом, теоремой и постусловием: **4 555
   байт, `cmp` — совпало**.
3. **Тот же слепок ДО правки сверен с НАСТОЯЩИМ двоичным.** `flang ast
   glavnoe.flang` печатает связанную программу своим, напечатанным связыванием:
   ключи, порядок типов, порядок функций и всё дерево целиком совпали с тем, что
   даёт вычислитель на `link.flang`, — с точностью до двух отметок анализа,
   которые `flang ast` ставит после связывания, а связывание не ставит. Значит
   проба меряет то самое связывание, а не свою выдумку.

## Чего эта работа НЕ сделала

* Перепечатка не запускалась — шла чужая. Числа сняты вычислителем и на
  синтетике.
* Квадрат во втором проходе уменьшен, а не снят: чтобы снять его до конца,
  нужен настоящий словарь, а он сегодня параметрический и двоичному не по зубам.
* `«Отбросить недостижимое»` (`«Оживить»`, `«Добавить уникальное»`) не тронуто —
  это отдельный шаг, к `«Связать исходники»` отношения не имеющий.

## Проверка правленого файла

```
PAMYAT=45G flang-vorota -- flang check flang/self/link.flang
модуль «Связывание flang»: функций 1201, из них с доказанным завершением 845;
типов 106; файлов вместе с импортами 9
FLANG_MEMORY: недостаточно памяти        ← ядро, а не типы
```

Ни одного замечания о типах: разбор, связывание и типы правленый файл проходит,
до отчёта о завершаемости доходит. Обрывается ЯДРО, и обрывается на памяти — это
не от правки: та же стена стоит и без неё. Проверено печатью пробы на
непра́вленом дереве часом раньше: `«Суд ядра о программе» съел 818 074 887
витков ... FLANG_MEMORY` — против 818 524 818 витков и той же `FLANG_MEMORY`
теперь. Разница в 0,05 %, стена та же.
