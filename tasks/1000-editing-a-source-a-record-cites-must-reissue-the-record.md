---
номер: 1000
заголовок: Правка исходника, на который ссылается запись доказательства, обязана перевыпускать запись
статус: свободна
исполнитель: —
ветка: —
команда: вторая
карта: Как перепроверить всё это самому
рядом: 9612, 9967
---

# 1000. Запись привязана к исходнику отпечатком, а сторожа этой связи в дешёвом хуке нет

## Что случилось

Коммит `6dff8a3a9` (задача 6201) правил ТОЛЬКО комментарии в пяти исходниках:
`corpus-factorial`, `corpus-natural-ceiling`, `four-words`, `precondition`,
`segment` (все в `flang/proof/examples/`). Ни одной строки кода не тронуто.

Записи доказательств привязаны к исходнику числом строк, числом знаков и двумя
отпечатками. Комментарий — тоже строки и тоже знаки, поэтому связь порвалась вся.
Мера: набор проб чекера стал красен (честных отвергнуто **10**, провалов **11**),
доля-проигрыванием упала 567/651 = 87,10 % → 542/638 = 84,95 %, минус 25
доказанных мест. Починка стоила двух коммитов: `f7f2416a0` (пять записей корпуса)
и починка проб Ч56/Ч375/Ч7104, где записи лежат отдельными копиями и слепой
перевыпуск разрушил бы саму пробу.

## Правило

**Правка исходника, на который ссылается запись доказательства, обязана
сопровождаться перевыпуском записи в том же коммите.** Перевыпуск — это
`bootstrap/flang check --proof --записать <запись> <исходник>`; для записи
подложной пробы — перевыпуск честной и повторное нанесение ровно той же порчи
(порча названа в шапке пробы и видна сличением прежней записи с прежней честной).

## Почему сегодня это проходит молча

Ловят это два прибора, и оба вне дешёвого хука:

| прибор | что накрывает | цена | где зовётся |
|---|---|---|---|
| `sh flang/proof/доля-корпуса.sh --вложенность` | 86 записей корпуса | ~37 с | `.github/workflows/binary.yml` |
| `sh flang/proof/чекер/пробы/прогон.sh` | записи проб (Ч56, Ч375, Ч7104 и прочие) | минуты | CI |

`--вложенность` записей ПРОБ не видит вовсе: они не в корпусе. Прогон проб их
видит, но стоит минуты, и в `.githooks/pre-push` его нарочно не взяли.

## Что сделать

1. Написать дешёвого сторожа `scripts/zapis-dognala-istochnik.sh`, который
   работает НЕ по всему дереву, а по диапазону пуша: `git diff --name-only` даёт
   правленые `.flang`; из всех записей дерева (`git grep -h '^исходник ' -- '*.запись'`)
   взять те, что зовут именно эти файлы; для каждой такой пары перепечатать
   запись во временный файл и сравнить `cmp`. При пустом пересечении — мгновенно
   зелено, а это подавляющее большинство пушей.
   Записи проб сравнивать не побайтово: у них честная половина обязана сойтись, а
   внесённая порча — остаться. Проще и честнее для них другое: если правленый
   исходник назван хоть одной записью проб, сторож краснеет со словами «тронут
   исходник пробы — прогони набор проб и пересобери запись руками».
2. Поставить его в `.githooks/pre-push` рядом с `podlog`/`opis` и вписать в
   `scripts/kto-zovet-storozhey.sh` (иначе сторож тихо мертвеет — см. tasks/README.md).
3. В `docs/HANDOFF.md` — одну строку правилом выше.

## Найдено

Ч56/Ч375/Ч7104-починка, 9 сентября 2026; обе меры (падение доли и краснота
набора проб) сняты прогоном на чистом стволе `gh/dev`.
