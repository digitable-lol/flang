---
номер: 1301
заголовок: Передача работы 1 сентября: блокер десяти заплат снят, что делать дальше
статус: в работе
исполнитель: a
ветка: несколько, перечислены ниже
команда: первая
карта: Цена самосборки
рядом: 0311, 1174, 1300, 1302, 1303, 1304, 1305, 1306, 1307
---

# 1301. Что знать тому, кто подхватывает

Файл написан, чтобы работу можно было продолжить, не читая переписку. Всё, что
ниже, снято командами на стволе `ef718648`, а не по памяти.

## Главное: блокер, державший десять заплат ядра, СНЯТ

31 августа в дереве было записано, что печать ходов вывода (derivation trace)
влить нельзя: все **десять** заплат ядра (Ч48, Ч55, Ч56, Ч70, Ч71, Ч75, Ч76,
Ч86, Ч87, Ч91) несут `использует «SHA-256»`, это втягивает
`flang/stdlib/sha256.flang` в замыкание компилятора, а там тип **«Сбор слов»**
уже объявлен в `flang/self/emit-c.flang:454` → `FLANG_DUPLICATE_NAME`.

Имя переименовано в «Сбор слов C», коммит `ef718648`. Проверка сегодня:

    LC_ALL=C.UTF-8 /usr/bin/grep -c 'тип «Сбор слов»' \
      flang/self/emit-c.flang flang/stdlib/sha256.flang
    # emit-c.flang:0   sha256.flang:1

Столкновения больше нет. **Десять заплат надо приложить заново** — раньше они
падали только из-за него. Заплаты лежат в
`/srv/tmp/dokazuemyy/Ч<номер>-<имя>/правка-clon.diff`, опись причин — в
`/srv/tmp/dokazuemyy/Ч123-vlivanie/НЕВЛИТО.tsv` (34 строки, из них 11 про это
столкновение).

Почему это важно: среди десяти лежит линия **Ч56 → Ч71 → Ч87** — печать ходов
вывода. Без неё независимый checker на настоящих записях не подтверждает
самостоятельно **ни одного** утверждения, и гейты Г1, Г2, Г4 стоят.

## Второе столкновение того же вида — ещё не снято

Заплата `C5-totalnost/правка-stvol.diff` (+188/−10) не идёт: функция
**«Связка постусловия»** объявлена и в `flang/self/totality.flang:1974`, и в
`flang/self/proofterm.flang:1292`. Лечение то же, что у «Сбор слов»:
переименовать то из двух, что живёт в более узком месте.

Записано также, что имён, объявленных сразу в `flang/self` и `flang/stdlib`,
**двенадцать**. Число снято 31 августа и на сегодняшнем стволе не перепроверено —
каждое такое имя это будущая поломка на десятом часу пересборки.

## Перестановка проверки: сделана, НЕ проверена

Ветка `a/imya-modulya-cpp` в `/srv/tmp/dokazuemyy/pochinka-imyon`, коммит
`9a11e66f`, +39 строк в двух файлах:

* `flang/self/emit-c.flang` — обёртка `функция «Столкновения имён»`, зовущая
  **существующее** правило `«Проверить столкновения»` (`emit-c.flang:5358`) по
  одной программе и без печати. Второго воплощения правила нет намеренно: иначе
  проверка и компилятор разойдутся и проверка начнёт врать.
* `flang/src/emit/c/flang_repl.c` — вызов между `repl_call("Связать исходники")`
  (~3889) и судом ядра (~4004). `val_text` (:1734) + копия в буфер 512 байт
  (строки рантайма не кончаются нулём) + `bads_say` (:2214).

Собрано: `make -C bootstrap -j8` код 0, чисто под
`-Wall -Wextra -Werror -pedantic -O2 -flto`. Регрессии нет:
`./bootstrap/flang check examples/import-check.flang` код 0.

**Чего не хватает.** Обёртка живёт в исходнике, а двоичный собран из
закоммиченного `bootstrap/compiler_flang.c`, где её ещё нет — значит
`repl_call("Столкновения имён", …)` функции не найдёт. В коде стоит защита по
коду возврата `repl_call`, но она не проверена прогоном. Пока это не проверено,
вливать нельзя.

## Порядок работ, если продолжать с нуля

1. Приложить десять заплат заново (блокера нет).
2. Снять второе столкновение — «Связка постусловия», и перемерить, сколько
   двойных имён в дереве осталось.
3. Проверить перестановку на нарочно внесённом столкновении, оба конца:
   внёс — краснеет, убрал — зеленеет.
4. Приложить контрольные точки Ч134 (+106/−21) — **до** новой пересборки, иначе
   следующий отказ снова стоит десять часов.
5. Только после 1–4 запускать пересборку.

## Ячейки, запущенные 1 сентября

Каждая пишет `ОТЧЁТ.md` в свой каталог `/srv/tmp/dokazuemyy/Ч<номер>-<имя>/`,
разделом «не сделано» — что осталось. Ни одна не пушит: вливание решает старший.

| ячейка | работа |
|---|---|
| Ч143 | приложить Ч56 заново (основание линии ходов вывода) |
| Ч144 | приложить Ч71 — саму печать ходов вывода, поверх Ч56 |
| Ч145 | приложить Ч87 — вершину линии; проверить, что половины checker'а нет в git |
| Ч146 | приложить Ч70 и Ч91 — две отдельные линии ядра |
| Ч147 | приложить Ч48 и Ч55; проверить, не одна ли это правка в двух копиях |
| Ч148 | сличить Ч75/Ч76/Ч86 с Ч71 побайтово, приложить только остатки |
| Ч149 | снять столкновение «Связка постусловия», пересчитать двойные имена |
| Ч150 | замер выноса восьми эмиттеров (см. ниже) |
| Ч151 | проверить перестановку на нарочном столкновении |
| Ч152 | контрольные точки Ч134 |
| Ч153 | линтер, задача 0311 |
| Ч154 | одна линейка ходов вывода вместо трёх счётов, задача 1303 |
| Ч155 | перемерить Г3, Г4, Г7 на сегодняшнем стволе |
| Ч156 | разобрать снимки Ч136, Ч137, Ч138, Ч140 |
| Ч157 | сводка выпуска в docs + README, задача 1305 |
| Ч158 | списки, написанные руками, задача 1306 |

## Отдельный вопрос владельца: вынести эмиттеры в библиотеки

Измерено на `ef718648`:

    for f in flang/self/emit-*.flang; do echo "$(wc -l < $f) $(basename $f)"; done

| | строк |
|---|---:|
| всё в `flang/self` | 123 350 |
| девять `emit-*.flang` | 36 112 (29 %) |
| `emit-c.flang` — уйти не может, им печатается семя | 5 828 |
| восемь остальных | 30 284 |

Разбор по файлам: python 5311, csharp 5265, elixir 4660, js 4603, java 4373,
rust 3113, go 2637, cpp 322.

Чего этот замер **не** решает: Ч107 намерила, что цена суда ядра растёт не от
строк, а от числа обязательств, примерно как обязательства в степени 2,5–3.
Значит выигрыш по времени определяется долей обязательств, а не долей строк.
Это меряет Ч150. Прошлая пересборка: всего 9 ч 56 мин, суд ядра 7 ч 33 мин.

Чего вынос **не** даст: доверенная база эмиттеров и так не содержит, кроме
`emit-c`. Она — parser, лексер, типы, завершаемость, связывание, вычислитель,
ядро доказательств: 26 234 строки flang, из них `parser.flang` 5 287 = 20,2 %.

## Грабли машины, стоившие времени

* Код возврата снимать **до** любой трубы: `kod=$?` отдельной строкой. Ошибка
  повторена за день четырежды.
* `grep` подменён и молча теряет кириллицу — звать `/usr/bin/grep -a` и ставить
  `LC_ALL=C.UTF-8`. В git — `-c core.quotepath=false`.
* Имена в семени **транслитерованы**: «Мера склейки» → `compiler_flang_mera_skleyki`.
  Поиск русской строки в `bootstrap/compiler_flang.c` даёт ноль и лжёт. На этом
  уже ошиблись один раз, объявив что ходов вывода нет нигде.
* `pkill`, `killall`, `pgrep -f` не звать никогда: машина общая, так уже убили
  чужие работы.
* Коммит автором `Marat Zimnurov <the-homeless-god@users.noreply.github.com>`.
  При `rebase` от чужого пользователя нужны `-c user.name` и `-c user.email`,
  иначе код 128 без конфликта.
* `/srv/priyom/stvol` — следящее дерево, оно делает `reset --hard origin/main`
  каждые десять минут. Работать в нём нельзя, только клонировать.

## Разряд ошибок, который стоит держать в голове

Записанное число или список, верные в день записи и лгущие назавтра. Найдено
105 списков, написанных руками, из них 57 разошлись с деревом. Подвиды:
сторож, зелёный по построению; сторож, спрашивающий «есть ли правильное», но не
«есть ли неправильное»; сторож, не зовомый вовсе под кириллическим путём;
самозаверяющий предмет — запись, несущая собственный отпечаток, которым сама
себя и подтверждает.
