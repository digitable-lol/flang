---
номер: 1306
заголовок: Перечни, набранные руками, сосчитаны прибором: 223 перечня, 62 расхождения, у каждого довод
статус: сделана
исполнитель: a
ветка: a/списки-руками
команда: первая
карта: Что мешает больше всего
рядом: 1305
---

# 1306. Список, набранный руками, лжёт назавтра

Перечень имён внутри одного файла, описывающий вещь, живущую в другом. Верен в
день записи, лжёт на следующий.

⚠ **«105 списков, 57 расходятся» — НЕ ЗАМЕР, и это надо знать, читая старые
ссылки на это число.** Оно сложено из двух переписей руками (ячейки Ч121 и Ч127,
31 августа) и своим прогоном не повторялось: прибора не было. Прибор появился
1 сентября — `scripts/hand-written-lists.sh` (коммит `04c42222`), — и своей
меркой считает иначе.

## Различение, по которому их чинить

Разделяющий вопрос: **чтобы внести новое имя, надо ли записать то, чего в дереве
нет вовсе** — причину, цену, разрешение, набор инструментов?

- **нет** → это копия. Брать имена из источника прогоном.
- **да** → это определение. Оставить список, но **краснеть на неизвестное имя,
  называя его**.

Шесть списков не ложатся ни в один разряд целиком: разряд у них не по списку, а
по полю — множество имён выводится из дерева, а привешенные к именам данные нет.

И правило, которого нигде не было: **довод, по которому имя ВЫНУТО из списка,
обязан лежать рядом со списком.** Иначе следующий читатель впишет его обратно.

## Живые расхождения, найденные впервые

- девятая цель печати не доехала до **шестнадцати** списков;
- проверка лицензии не смотрит на `bootstrap/` вовсе; там два порождённых файла
  без шапки (не шесть — счёт по признаку «файл кода» завышал втрое);
- `manpage-guard` ищет коды бед в четырёх файлах рантайма при шести: в двух
  невидимых лежат 22 кода.

## Проба, которую проваливали три проверки подряд

Подсунуть **два** незнакомых имени. Проверка обязана сказать «2» и назвать оба.
Найдены случаи, где ответ на ноль, на одно и на два незнакомых **побайтово
одинаков**, и случай, где ответ был «нарушений: 1» независимо от их числа.

## Чем закрыта

- все 57 расхождений либо устранены, либо названы с доводом, почему остаются;
- каждая проверка разряда «определение» краснеет с именем незнакомца и
  различает их число;
- сама проверка не стала сто шестым списком: имена берёт прогоном из дерева.

## Закрыта 1 сентября 2026 — прибором, а не пересчётом руками

Прибор: `scripts/hand-written-lists.sh` (коммит `04c42222`), ярлыки
`перечни:перепись` и `перечни:проверка`.

Числа своим прогоном, ветка `a/задачник-и-документы` поверх ствола `af791d85`:

| | |
|---|---:|
| перечней набрано руками | **223** (целей 57, путей 166) |
| из них расходятся с деревом | **62** (целей 15, путей 47) |
| перечней целей, сошедшихся с семенем, а не с исходником | 41 |

**Главное, что нашла перепись, и чего перепись руками увидеть не могла:
«дерево» в этом вопросе ДВА.** Слоёв печати в исходнике девять, а у двоичного,
которым дерево судят, целей восемь — девятая (`cpp`) в семя не перепечатана.
Перечень из восьми целей не протух: он описывает двоичный. Мерка на один
источник объявила бы ложью 41 верный перечень.

Три условия закрытия, каждое прогоном:

1. **все расхождения названы с доводом** — `scripts/hand-written-lists-ledger.tsv`,
   62 записи, сверяется В ОБЕ стороны: и новое расхождение красит, и запись,
   переставшая быть расхождением. `sh scripts/hand-written-lists.sh --check` —
   код 0.
2. **проверки краснеют с именем незнакомца** — коммит `04c42222` завёл три
   сторожа и показал каждого краснеющим на нарочно внесённом расхождении;
   ответ на ноль, одно и два незнакомых имени у него разный.
3. **перепись не стала сто шестым списком** — списка мест, куда смотреть, она не
   держит вовсе: файлы берёт `git ls-files`, цели печати — из имён
   `flang/self/emit-*.flang`, справку о целях двоичного — у самого двоичного.
   Руками в ней набрано ровно одно: ПРАВИЛО, по которому строка признаётся
   перечнем.

**Что осталось и лежит в задаче 4717 (вторая стопка):** чинить сами перечни —
брать имена из источника там, где источник есть, — и показать падение числа
разошедшихся разностью.
