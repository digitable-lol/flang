---
номер: 1307
заголовок: Вынести разбор из доверенной базы: кругооборот даёт 99,36 %, целиком сходится 1 файл из 296
статус: свободна
исполнитель:
ветка:
команда: первая
карта: Размер доверия
рядом: 1306
---

# 9790. Кругооборот почти работает

Чтобы поверить доказательству, сегодня надо верить и разбору: он превращает
текст в дерево, и если соврёт — доказано будет не то, что написано.
**`parser.flang` — 5287 строк кода, 20,2 % доверенной базы**, больше проверки
типов и второй после ядра доказательств.

Доверенная база измерена впервые, одной линейкой: **26 234 строки на flang**
(24 669 достижимо) плюс **517 099 строк рантайма на C**.

> Число **5466**, стоявшее в прежней формулировке, **не подтверждается ничем**:
> его не даёт ни одно из двенадцати чисел мерки, `grep` по дереву даёт ноль
> вхождений, а 25 прежних коммитов файла дают полосу 5122…5287 — 5466 не
> достигалось никогда.

## Что уже измерено прогоном

Собиратель текста из потока лексем (50 строк) прогнан на **296 файлах, 155 878
строк**:

| | сколько | собрано байт в байт |
|---|---:|---:|
| строк кода | 112 361 | **111 643 (99,36 %)** |
| строк пустых | 12 986 | 12 986 (100 %) |
| строк комментария | 30 531 | **221 (0,7 %)** |
| файлов целиком байт в байт | 296 | **1** |

`parser.flang` собрался по коду полностью: 6632 из 6632.

**Мешают ровно две вещи, обе в лексере, обе показаны порчей:** комментарии он не
выдаёт вовсе, и форму экранирования забывает (`"Ѐ"` и сам символ в потоке
неразличимы). Поле `text` у строковой лексемы **уже объявлено и пусто** — место
под форму есть.

## Оговорка, без которой работа решается не та

Собиратель идёт через `flang tokens`, поэтому снимает с доверенной базы
**лексер (1018 строк)**, а не разбор (5287). Чтобы снял разбор, печатать надо из
дерева разбора, а позиции у него стоят лишь на **69,9 %** строк корпуса.

## Чем закрыта

- доля файлов, сходящихся **целиком** байт в байт, названа числом и она не 1/296;
- сказано, что именно осталось несходящимся, поимённо;
- цена правки лексера названа меркой `Ч4-merka`, а не предсказанием.

## Где лежит сделанное

`/srv/tmp/dokazuemyy/Ч133-parser-iz-tcb/` — собиратель и числа;
`/srv/tmp/dokazuemyy/Ч136-leksem-kommentarii/` — начатая правка лексера,
остановлена по лимиту, целиком не проверена.
