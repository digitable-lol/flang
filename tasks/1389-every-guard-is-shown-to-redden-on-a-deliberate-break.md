---
номер: 1389
заголовок: У каждого сторожа показано, что он краснеет на нарочной поломке
статус: свободна
исполнитель: —
ветка: —
команда: любая
карта: Что мешает больше всего
рядом: 9350, 4929
---

# 1389. У каждого сторожа показано, что он краснеет на нарочной поломке

За одни сутки три сторожа подряд оказались негодны, и каждый — по своему поводу:

* `коды:проверка` **не бежал вовсе** — отказывал раньше просмотра, потому что
  вывод `git grep` перерастал предел хозяина в 1 МиБ;
* `справка:проверка` был **зелен по везению** — читал обрезанный вход и не
  замечал этого, потому что вердикт совпадал случайно;
* `коды:подлог` **проверял не то, что обещает** — искал подложенное имя простым
  вхождением по всему ответу и краснел от чужой прозы, назвавшей то же имя.

Три из трёх, к которым присмотрелись. **Сколько из остальных зелены по
существу, никто не знает.**

## Сколько сегодня показано

Замер 29 августа 2026 по `ярлыки.flang` (95 ярлыков):

| | сколько |
|---|---|
| ярлыков всего | 95 |
| из них печатники и сборщики (приговора не выносят) | 30 |
| **сторожей** | **65** |
| у которых показана нарочная поломка | **14** |
| **у которых НЕ показана** | **51** |

Показанной считается поломка, у которой есть свой ярлык (`порча`, `подлог`) либо
которую сторож делает сам и говорит об этом в «зачем»: `коды:подлог`,
`октет:порча`, `столкновения:порча`, `спеки:подлог`, `слово:подлог`,
`пустота:порча`, `доказанное:порча`, `поверхности:подлог`, `умения:улики`,
`подделки:проверка`, `запись:проверка`, `сведение:проверка`,
`поиск:различающий`, `слово:занятость:проверка`.

## Что уже проверено руками

Четыре сторожа из 51 проверены нарочной поломкой вручную 29 августа. Все четыре
ведут себя как обязаны — это **отрицательный результат и он полноценный**:

| сторож | чисто | порча | откат | чем ломали |
|---|---|---|---|---|
| `справка:проверка` | 0 | **1** | 0 | вписан код `FLANG_VYDUMKA_SPRAVKI` в `packaging/flang.1` |
| `сторожа:проверка` | 0 | **1** | 0 | ввоз несуществующего файла в `flang/test/nadzor-uzla.test.mjs` |
| `ссылки:проверка` | 0 | **1** | 0 | ссылка на несуществующий файл в `docs/ci-inventory.md` |
| `решения:номера` | 0 | **1** | 0 | второй файл решения с уже занятым номером ADR-0002 |
| `словарь:полнота` | 0 | **1** | 0 | из `docs/glossary.md` убрана строка со словом `это` |
| `расширения:проверка` | 0 | — | — | поломка не придумана дёшево |

Каждый краснеющий назвал беду поимённо: «страница обещает код … которого нет ни
в одном исходнике», «НЕ ЗАВОДИТСЯ flang/test/nadzor-uzla.test.mjs: Error
[ERR_MODULE_NOT_FOUND]», «ссылка в никуда → ./net-takogo-fayla-9350.md»,
«ADR-0002 занят не одним решением», «понятия «alias» нет на странице».

Каждый назвал беду поимённо и с местом, каждый позеленел после отката.

## Чего проверить НЕ удалось, и это отдельная порода

**Сторож, красный на чистом дереве, нарочной поломкой не проверяется вовсе** —
у него нет зелёного состояния, от которого краснеть. Найдено двое:

| сторож | чисто | порча | откат |
|---|---|---|---|
| `задачник:проверка` | **1** | 1 | 1 |
| `лицензии:проверка` | **1** | 1 | 1 |

У обоих беды чужие и давние (`задачник` — пять замечаний в задачах 6094, 6411,
6530; `лицензии` — нет SPDX в `flang/scripts/сличить-двух-сводителей.py`). Но
пока они красны, они ничего не сторожат: краснота от настоящей поломки не
отличается от красноты, которая была вчера. **Это ровно та болезнь, из-за
которой пять суток не слышали сторожа столкновений.**

## Порча, которая правит дерево, при обрыве остаётся в дереве

Найдено не рассуждением, а происшествием: 29 августа прогон нарочной поломки
оборвался на середине и **оставил порчу в дереве** — в `flang/stdlib/base64.flang`
осталась приписанная теорема «знак азбуки — ровно один знак». Восстанавливать
пришлось руками. В тот же день то же самое случилось с `коды:подлог`: ворота
сняли прогон между «подложил» и «убрал», и подложенный код остался в
`docs/what-blocks-1-0.md`.

Это делит нарочные поломки на два образца, и разница не оттеночная:

* **ПРАВЯТ ФАЙЛ ДЕРЕВА** — `коды:подлог`, `доказанное:порча`, `пустота:порча` и
  прочие: пишут порчу на диск, зовут сторожа, снимают порчу. При обрыве по сроку,
  памяти или воротам дерево остаётся грязным, и следующий, кто посмотрит
  `git status`, увидит чужую порчу как свою правку.
* **ПОДМЕНЯЮТ ЧТЕНИЕ** — `столкновения:порча`: порча подкладывается тем же
  способом, каким сторож уже сверяется с выдуманными деревьями
  (`настройки.читать`), файлы на диске не трогаются вовсе. Оборвать такой прогон
  можно в любой миг, и дерево останется чистым.

**Распространять надо второй образец.** Первый годится только там, где сторож
принципиально смотрит на диск чужой программой (`git grep`, `groff`) и подменить
ему чтение нечем; для таких надо хотя бы назвать в «зачем», что прогон правит
дерево, и какой командой откатывать.

## Что сделать

1. **Пройти оставшихся сторожей нарочной поломкой** — по одному, с записанным
   «чисто → порча → откат». Образец, который стоит распространять, —
   `столкновения:порча`: ключ `--порча` у самого сторожа, порча подкладывается
   ЧТЕНИЕМ, дерево не трогается, и три случая разом проверяют, что краснеет он
   на своём и молчит на чужом.
2. **Назвать отдельно тех, у кого поломка дорога или невозможна** — с причиной.
   Это тоже результат: `раскрутка:проверка` и `точка:проверка` печатают заново,
   `установка:npm` ходит в сеть, `выпуск:проверка` — на GitHub.
3. **Разобраться с постоянно красными.** Либо чинить беду, либо записывать долг
   поимённо — как это сделано у сторожа столкновений, где долг судит только
   первый разряд.
4. **Померить, а не предположить, у кого тесно с пределами.** Известное:
   предел хозяина 1 МиБ (`IO_OUT_LIMIT`), у Node без `maxBuffer` тот же 1 МиБ,
   а исчерпание `--max-orders` отвечает `FLANG_IO_LIMIT` — названной бедой, не
   тишиной, значит запас там вопрос удобства.

## Как понять, что сделано

У каждого из 65 сторожей записано одно из двух: либо прогон «чисто → порча →
откат» с тремя кодами возврата, либо причина, по которой поломка невозможна или
дорога. Постоянно красных не осталось или каждый записан долгом с доводом.


## Доля 1—66: что померено 29 августа

Ярлыки 67—95 обойдены прежде и записаны выше. Здесь — ярлыки с 1 по 66:
прочитаны все 66, прогоном померены 14, нарочной поломкой проверены 6.

### Пятеро уличены прогоном: «смотреть было нечем» читается как «всё хорошо»

Болезнь у всех пятерых одна, и она не про отдельного сторожа, а про общее
место: **помощник, который не ответил, читается как помощник, ответивший
«ничего не нашёл»**. Нижней границы — «сколько входов обязано было
просмотреться» — ни у кого из пятерых не было.

| ярлык | чем сломано | было | стало после правки |
|---|---|---|---|
| 4 `утверждения:проверка` | `FLANG_BINARY` на программу с кодом 1 | **код 0**, «чисто — ни одна проза не отрицает формы» | код 1, «лексер не ответил про «…»: код 1. Это не «слова нет в языке» — это невозможность спросить» |
| 23 `слова:проверка` | PATH без сборщиков целей | **код 0**, «собраны всеми целями, что есть на машине (1 из 8)» | код 2, «НЕ ПРОЙДЕНА: целей проверено 1 из 8, не проверено 7 — c (нет cc), go (нет go), …» |
| 42 `доказательства:ведомость` | `FLANG_BINARY` на программу с кодом 1 | **код 0**, все 211 файлов «без ведомости» | код 2, «ВЕДОМОСТЬ НЕ СНЯТА НИ У ОДНОГО ФАЙЛА: файлов 211, без ведомости 211» |
| 43 `доказательства:свод` | PATH без `git` | **код 0**, «файлов в корпусе: 0» | не починен — файл держит сосед |
| 38 `поиск:различающий` | `FLANG_BINARY` в никуда | **код 0** | не починен |

Корень у 4 и 23 общий и назван замером: `bootstrap/flang tokens --keyword`
отвечает **кодом 0 и на настоящее слово, и на выдуманное** — «если» → `"if"`,
«абракадабра» → `null`. Разница в поле ответа, а не в коде возврата. Значит
ненулевой код возврата означает ровно одно: спросить не удалось. А
`ключевоеСлово` в `flang/scripts/binary.mjs` отдавала на него тот же `null`,
что и на честное «такого слова нет», и сторож утверждений принимал молчание
лексера за подтверждение прозы.

### Двое не бегут вовсе, и один из них считался проверенным

`октет:проверка` и `октет:порча` (ярлыки 9 и 10) звались строкой
`sh scripts/bad-octet-guard.sh`, а у файла шапка `#!/usr/bin/env bash` и на
строке 92 стоит `${BASH_SOURCE[0]}`. `/bin/sh` на этой машине — dash. Замер:

    sh scripts/bad-octet-guard.sh --порча
    scripts/bad-octet-guard.sh: 92: Bad substitution
    нет двоичного //bootstrap/flang — соберите: make -C bootstrap
    код 3, 1,5 с

Корень дерева не вычислился, двоичный не нашёлся, ни одна цель не судилась.
Полтора секунды на работу, которая печатает и собирает восемь целей.

**`октет:порча` стоит в списке тех четырнадцати, у кого нарочная поломка
показана.** Показать её было нечем: сторож не заводился. Значит показанных
было тринадцать, а не четырнадцать.

Правка — одно слово в двух строках `ярлыки.flang`: `sh` → `bash`. После неё
сторож судит цели по-настоящему.

### Шесть обещанных самопроверок в дереве отсутствуют

Пять сторожей клянутся прогоном, которого нет:

| файл, которого нет | кто на него ссылается |
|---|---|
| `flang/test/claim-guard.test.mjs` | 4 `утверждения:проверка` |
| `flang/test/count-guard.test.mjs` | 5 `подсчёты:проверка` |
| `flang/test/name-guard.test.mjs` | 14 `имена:проверка` |
| `flang/test/binary-rules.test.mjs` | 19 `правила:проверка` |
| `flang/test/binary-rules-guard.test.mjs` | 19 `правила:проверка` |
| `flang/test/uzel-celi.test.mjs` | 21 `времянки:проверка` |

Последний хуже прочих: `времянки:проверка` не просто ссылается на него в прозе,
а ЗАПУСКАЕТ его — `node --test … flang/test/uzel-celi.test.mjs` вписано в тело
сторожа. Он меряет мусор прогона, которого нет.

### Красные на чистом дереве — их не двое, а больше

К `задачник:проверка` и `лицензии:проверка`, названным выше, прогон добавил:

| ярлык | код на чистом дереве | чем красен |
|---|---|---|
| 4 `утверждения:проверка` | 1 | два утверждения прозы протухли: `flang/cat/SPEC.md:1065` и `flang/self/parser.flang:6181` говорят, что формы «символ по коду» в языке нет, а лексер отдаёт её ключевым словом |
| 6 `коды:проверка` | 1 | три кода названы прозой и отсутствуют в исходниках, `FLANG_ZAMER` — в шести местах |
| 11 `умения:проверка` | 1 | — |
| 13 `закон:проверка` | 1 | — |
| 3 `лицензии:проверка` | 1 | нет метки SPDX в трёх файлах из 74 просмотренных |

Пока сторож красен, он не сторожит: краснота от новой поломки не отличается от
вчерашней. Это та же болезнь, из-за которой пять суток не слышали сторожа
столкновений.

### Заявленное время против настоящего

Примета работает в обе стороны. Померенное:

| ярлык | заявлено | померено | что это значит |
|---|---|---|---|
| 9, 10 `октет:*` | печать и сборка восьми целей | **1,5 с**, код 3 | не бежит вовсе |
| 5 `подсчёты:проверка` | «пересчитать N строк такого-то файла» | **больше 300 с**, вывода 0 байт | обещает счёт строк, а тянет за собой полный свод корпуса доказательств — 211 файлов через `flang check --proof` |
| 13 `закон:проверка` | не заявлено | 74,9 с | — |
| 14 `имена:проверка` | 1048 с в `docs/ci-inventory.md` | больше 300 с | сходится по порядку |
| 8 `печать:проверка` | восемь целей | 5,3 с, 21 поручение | сходится: восемь печатей маленькой программы, разобрано по журналу |

`подсчёты:проверка` — обратный случай к вчерашнему: не «отвечает втрое быстрее
обещанного», а «стоит в сотни раз дороже, чем звучит». Ярлык обещает пересчёт
чисел в прозе, а платит за него полным сводом доказательств; замер цены одного
прогона свода записан в самом дереве — 28,7 ГиБ и 14 минут 30 секунд.

### Порча, правящая дерево, поймана ещё раз — на себе

Первый проход переписи звал `коды:подлог` без объявленной ширины веера. Ворота
сняли прогон между «подложил» и «убрал», и в дереве осталось
`M docs/what-blocks-1-0.md` — ровно то, что записано выше про 29 августа.
Восстановлено `git checkout`. Со вторым проходом (ширина веера объявлена) тот же
ярлык отработал за 36,96 с кодом 0 и дерева не тронул.

Это не новая беда, а её воспроизведение по заказу: **порча, правящая файл,
оставляет грязь при любом снятии прогона, а снять прогон могут ворота, а не
только человек.**

### Предсказания, которые прогон НЕ подтвердил

Записаны нарочно — отрицательный результат тоже результат:

* `решения:номера` (57) на пустом каталоге решений **краснеет** (код 1), а не
  зеленеет. Краснеет, правда, по вздорному поводу: образец `*.md` не раскрылся
  и был принят за имя файла.
* `запись:проверка` (26) с мёртвым сверщиком **краснеет** (код 1), а не отдаёт
  «сошлось всё (0 записей из 0)».
* `имена:проверка` (14) с двоичным, отвечающим пустотой, напечатал
  «ВЫПОЛНЕНО — 761 имён снято» — то есть полную слепоту принял за выполненную
  работу, — но вышел кодом 1 по другому поводу (`Unexpected end of JSON input`).
  Дыра в коде настоящая, зелени на ней показать не удалось.

### Что починено и чем показано

**1. Ярлыки 9 и 10 зовутся `bash`, а не `sh`** (`ярлыки.flang`, две строки).
Показано ключом самого сторожа, на цели `c`:

    bash scripts/bad-octet-guard.sh --цель c            → код 0, «красных: 0»
    bash scripts/bad-octet-guard.sh --цель c --порча    → «подложено: rust отказывать перестал,
                                                          обрывает цикл молча»
                                                          «rust | МОЛЧА бросил: код 0, ответов 1 из 2
                                                           | КРАСНО: отказа нет»
                                                          «сторож покраснел (код 1) — порча видна»

Порча подкладывается в рабочую копию прогонщика во временном каталоге, дерева
не трогает — тот самый образец, который велено распространять.

**2. `ключевоеСлово` в `flang/scripts/binary.mjs` больше не путает «слова нет»
с «спросить не удалось».** Ненулевой код возврата и неразборчивый ответ теперь
поднимают беду с названным словом. Показано:

    node flang/scripts/claim-guard.mjs                       → код 1 (две протухшие прозы)
    FLANG_BINARY=<код 1> node flang/scripts/claim-guard.mjs  → БЫЛО код 0 «чисто»
                                                               СТАЛО код 1 «лексер не ответил
                                                               про «символ уже в окне»: код 1»

Той же правкой перестал врать `слово:занятость` (53): при мёртвом двоичном он
печатал «голым именем: 0 — свободно», теперь называет беду.

**3. У `слова:проверка` (23) появилась нижняя граница.** Пропущенная цель — не
пройденная проверка, код 2 (тот же уговор, что в `scripts/bad-octet-guard.sh`,
строка 15).

**4. У `доказательства:ведомость` (42) появилась нижняя граница.** Ни одной
снятой ведомости — отказ кодом 2, а не «расхождений нет».

### Чего сделать не удалось, и почему

| ярлык | почему |
|---|---|
| 43 `доказательства:свод` | уличён прогоном (без `git` — код 0 и «файлов в корпусе: 0»), но `flang/scripts/ledger-summary.flang` держит ветка `m/what-we-publish-is-true`. Силой не брал. |
| 38 `поиск:различающий` | уличён прогоном (код 0 без двоичного), правка не внесена — не хватило времени |
| 5 `подсчёты:проверка` | дороже 300 с; чтобы померить до конца, нужен полный свод корпуса — часы |
| 20 `узел:цели`, 21 `времянки:проверка` | по получасу каждый по своему же ключу срока |
| 30 `выпуск:проверка` | ходит в сеть и на GitHub четырьмя запросами `curl` |
| 34 `семя:быстро`, 89 `раскрутка` | перепечатка занята: `sh scripts/raskrutka.sh` идёт с 15:49 |
| 36 `столкновения:пересчёт` | 30 минут и 37,8 ГБ по собственной шапке — при пороге сторожа памяти в 45 ГиБ |
| 64 `браузер:собрать`, 66 `браузер:проба` | нужен поднятый стенд и настоящий браузер |
| 11, 12, 25—27, 38—52 (двадцать ярлыков) | заслон отставшего семени: `ярлык` отказывается судить о доказательствах, пока семя не перепечатано. Мерил их с ключом `SEMYA_OTSTALO_ZNAYU=1`, и это надо помнить: ответ такого прогона — про дерево, собранное из СТАРОГО семени. |

### Ещё одна порода грязи: временный каталог в КОРНЕ дерева

Трое сторожей заводят рабочие каталоги не в `/tmp`, а прямо в корне
репозитория, и ни один образец имени не стоит в `.gitignore`:

| кто | что кладёт | где в коде |
|---|---|---|
| 23 `слова:проверка` | `.flang-slova-*`, `.flang-slova-svody`, `.flang-slova-moduli` | `flang/scripts/target-words.mjs:155, 179, 270, 288` |
| 14 `имена:проверка` и всё, что зовёт прогонщик | `.flang-storozh-*` | `flang/scripts/binary.mjs:229, 347, 378` |
| 38 `поиск:различающий` | `.flang-razlichitelnyy-*` | `flang/scripts/discriminating-search.mjs:254` |
| 54 `слово:занятость:проверка` | `proba-zanyatosti.flang` — **в самом корне и с окончанием языка** | `scripts/occupancy-check.flang:193—198` |

Убираются они через `finally`, обработчика сигнала нет ни у кого. Проверено
снятием прогона: `слова:проверка`, снятый воротами на середине, оставил
`?? .flang-slova-cmP3G7/`. Последняя строка таблицы хуже прочих: оставшийся
`proba-zanyatosti.flang` — файл на языке в корне дерева, и его подберут обходы
других сторожей (`git ls-files '*.flang'`, `find … -name '*.flang'`).

В `.gitignore` не стоит ни один из четырёх образцов. Дописывать их туда НЕ надо:
грязь от этого не исчезнет, а перестанет быть видна, и файл на языке в корне
всё равно уедет в чужой корпус. Чинить надо ловушкой на сигнал.

### Четыре риска по ярлыкам 1—66

Отметка стоит там, где риск НАЗВАН местом в коде. Пустая клетка значит «не
нашли», а не «нет». Столбцы: **1** — не бежит вовсе (отказ помощника читается
как чистота); **2** — зелен по везению (смотрит неполный вход); **3** —
проверяет не то, что обещает (вхождение вместо равенства или места); **4** —
нарочной поломкой не проверен.

| № | ярлык | 1 | 2 | 3 | 4 | замечание |
|---|---|---|---|---|---|---|
| 1 | сборка | | | | | не сторож — сборщик |
| 2 | ярлыки | | | • | • | имя плана не проверяется |
| 3 | лицензии:проверка | | • | | • | **красен**: 3 нарушения на 74 просмотренных |
| 4 | утверждения:проверка | **•** | • | • | • | **красен**; уличён прогоном |
| 5 | подсчёты:проверка | • | | • | • | тянет полный свод корпуса; дороже 300 с |
| 6 | коды:проверка | | | | • | **красен**: три кода прозы нет в исходниках |
| 7 | коды:подлог | | | | | правит дерево; при снятии оставляет грязь |
| 8 | печать:проверка | | | | • | сходится: 8 печатей, 21 поручение, 5,3 с |
| 9 | октет:проверка | **•** | | | | **не бежал вовсе** — починено |
| 10 | октет:порча | **•** | | | | **не бежал вовсе** — починено |
| 11 | умения:проверка | | | • | • | **красен** |
| 12 | умения:улики | | | | • | |
| 13 | закон:проверка | • | | | • | **красен**, 74,9 с |
| 14 | имена:проверка | • | • | • | • | «снятые» не красит; 296 файлов из 1142 |
| 15 | жаргон:проверка | | | | • | **красен**, 46,4 с, пик 7,0 ГиБ |
| 16 | жаргон:список | | | | | печать мест |
| 17 | жаргон:долг | | | | | печатник: переписывает долг |
| 18 | память:проверка | • | | **•** | • | прозы не читает вовсе — пороги вписаны в него самого |
| 19 | правила:проверка | | • | • | **•** | обещанных самопроверок нет в дереве |
| 20 | узел:цели | • | | • | • | отказ печати даёт одинаковый хеш пустоты у обоих концов |
| 21 | времянки:проверка | • | | | • | **запускает файл, которого нет** |
| 22 | занятые:проверка | | • | • | • | таблица и обход спрашиваются у одного двоичного |
| 23 | слова:проверка | **•** | • | | • | уличён прогоном — **починено** |
| 24 | расширения:проверка | | • | **•** | • | судит вхождением; список мест рукописный |
| 25 | подделки:проверка | | | • | | у ярлыка нет срока, а умолчание режет |
| 26 | запись:проверка | • | | • | | |
| 27 | сведение:проверка | | | • | | проза ярлыка врёт про свои же числа |
| 28 | срок:проверка | | | | | **лучший в доле**: время заявлено замером, поломка внутри |
| 29 | связь:печать | | | • | • | есть нижняя граница — редкость |
| 30 | выпуск:проверка | **•** | | • | • | нет сети — «НЕ ПРОВЕРЕНО» и код 0 |
| 31 | двоичный:приёмка | | • | • | | судит ЧУЖОЕ дерево по умолчанию |
| 32 | столкновения:проверка | • | • | • | | |
| 33 | столкновения:порча | | | | | **образец для подражания** |
| 34 | семя:быстро | | • | • | • | сторожа можно озеленить, ничего не починив |
| 35 | семя:слова | • | • | • | | ключ подлога есть, но наружу не выведен |
| 36 | столкновения:пересчёт | • | | | • | 30 мин и 37,8 ГБ при пороге сторожа памяти 45 ГиБ |
| 37 | сращивание:проверка | **•** | | | • | 2264 поручения при пределе 2000 |
| 38 | поиск:различающий | **•** | | • | | уличён прогоном; ключ порчи есть, ярлык его не зовёт |
| 39 | сколько-верим | | | | • | **красен**: потолок устарел |
| 40 | спеки:проверка | | | | | крепкий |
| 41 | спеки:подлог | | | • | | |
| 42 | доказательства:ведомость | **•** | • | | • | уличён прогоном — **починено** |
| 43 | доказательства:свод | **•** | • | | • | уличён прогоном; файл держит сосед |
| 44 | слово:проверка | • | • | | | предмет — один файл и четыре утверждения |
| 45 | слово:подлог | | | • | | «поймана» значит любая беда |
| 46 | доказательства:пустота | • | • | | • | код возврата ноль всегда |
| 47 | пустота:проверка | | **•** | | | **красен**: 16 файлов библиотеки не названы нигде |
| 48 | пустота:перепись | | | • | | зелёная по построению |
| 49 | пустота:порча | | | • | | правит `numbers.flang` на диске |
| 50 | доказанное:проверка | • | **•** | | • | судит 12 файлов из 42 |
| 51 | доказанное:порча | | | • | | **это он оставил порчу в `base64.flang`** |
| 52 | доказательства:20 | • | | • | • | красной строки нет вовсе; мусор в дереве не убирает |
| 53 | слово:занятость | **•** | | | • | уличён прогоном — **починено** той же правкой, что 4 |
| 54 | слово:занятость:проверка | | | • | | оставляет `.flang` в корне при обрыве |
| 55 | ссылки:проверка | | • | **•** | • | три прочтения цели вместо одного; 34 ссылки живы только одним из них |
| 56 | задачник:проверка | | | • | • | **красен**: пять замечаний |
| 57 | решения:номера | | • | | | ключ подлога есть, ярлыком не выведен |
| 58 | задачник:доска | | | | | печать, красного исхода нет |
| 59 | задачник:номер | | | | | выдача, не сторож |
| 60 | указатель:печать | | • | **•** | | печатает README из README же |
| 61 | путь:проверка | **•** | • | • | • | отказ перечисления = пустой каталог; **нигде не гоняется** |
| 62 | словарь:полнота | | | • | • | вхождение вместо строки; сегодня зелен по делу |
| 63 | вкладка:проверка | | | • | • | **красен**: хозяин вкладки отстал на 4 поручения |
| 64 | браузер:собрать | | **•** | | • | входов ровно два, и они зашиты |
| 65 | браузер:стенд | | | | | служба, не сторож; отдаёт любой файл дерева |
| 66 | браузер:проба | | | • | | нужен настоящий браузер; без него краснеет честно |

## Чем закрыта

—
