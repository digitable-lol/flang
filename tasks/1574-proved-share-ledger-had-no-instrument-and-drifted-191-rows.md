---
номер: 1574
заголовок: У ведомости доказанного не было прибора, и она разъехалась на 191 строку; знаменатель Г7 не воспроизводится ни одним числом из пяти
статус: сделана (прибор заведён и снабжён тремя пробами порчи; починка описи — отдельное решение)
исполнитель: X2
ветка: x2/vedomost-dokazannogo
команда: любая
карта: Что мешает больше всего
рядом: 8429
---

# 1574 — число 13 347 не воспроизводится, а ведомость под ним разъехалась

## Как найдено

Работник V3 (задача 8429) искал прибор, дающий «13 347 написанных строк» из
таблицы гейтов, и не нашёл: разовый проход, в репозиторий не переехавший.
Порода та же, что в 7298 (сторожей считали рукой, счёт был неверен в обе
стороны) и 7855 (прибор кругооборота лежит во времянке).

## Прибор заведён: `scripts/pol-dokazannogo-sverka.sh`

Идёт 11 секунд, двоичного не требует.

**Что он сверяет точно, а что только датирует** — разница не косметическая:

* **знаменатель** («написано», «без приговора») считается ПО ИСХОДНИКУ:
  строки `обеспечивает`, `требует`, `закон`. Расхождение с описью есть
  ошибка описи здесь и сейчас;
* **числитель** («доказано», «сетка», «объявлено») — приговор ядра. Снять
  его заново значит прогнать двоичный по каждому файлу; на ядре
  доказательств это часы и десятки гигабайт (шапка описи: `sets-oracle` —
  999 с и 16,6 ГиБ на ОДИН файл). Прибор его не пересчитывает и не делает
  вида, что пересчитал. Он сверяет md5 исходника и тем ДАТИРУЕТ строку.

Сверка в обе стороны, по образцу `flang/scripts/cli-keys-debt.json` и
`scripts/storozha-bez-zova.json`.

## Опись разъехалась на 191 строку

```
нет строки        62   файл несёт обязательства, а строки в описи нет
лишних строк      71   строка есть, а обязательств в файле не осталось
не сходится число 58   «написано» в описи не равно счёту по исходнику
                       (в дереве больше на 111, меньше на 134)
```

## Протухлость: три четверти числителя по весу

```
строк протухло по md5     153 из 515
из них несут «доказано»   7182 из 9216   = 77,9 %
и «написано»             10776 из 13607  = 79,2 %
```

Свежих строк 362 из 515, но по **весу** протухли самые крупные — ядро
доказательств. То есть числитель ведомости на три четверти относится к
файлам, которых уже нет в этом виде.

## Знаменатель: пять разных чисел, и дерево не даёт ни одного

```
КРИТЕРИЙ.md, таблица Г7                    13 347
docs (со слов задачи 8429)                 13 399
опись, сумма поля «написано»               13 607  (+97 без приговора = 13 704)
шапка описи, замер 30 августа на 5c0d35c9  13 484  (+97 = 13 581)
ДЕРЕВО СЕГОДНЯ, счёт прибором              13 637  (+102 = 13 739)
```

Разброс от 13 347 до 13 739 — почти четыреста строк. Ни одно из
записанных чисел дерево сегодня не даёт.

## Годится ли 13 347 знаменателем Г7 — НЕТ, и не потому, что число неверно

Число можно поправить за одиннадцать секунд, и это не спасёт. Работник V3
показал: **числитель Г7 равен нулю по устройству разбора** — он достижим
единственной веткой «ядро сказало доказано». Он останется нулём на дереве
пустом, свежем и полном лжи.

Дробь с числителем, который не может быть не нулём, не измеряет ничего —
какой бы точный знаменатель под неё ни подставили. Чинить надо
формулировку гейта (развилка А/Б/В в задаче 8429), а не знаменатель.

Знаменатель при этом всё равно нужен — он входит в долю доказанного, а не
только в Г7, и там его точность имеет цену. Поэтому прибор заведён.

## Проба порчи — три, все с числами

Опись сегодня красна, поэтому проба «зелёное → красное» на ней невозможна.
Построена опись ИЗ ДЕРЕВА (знаменатель точен, числитель не заявлен), на ней
показано зелёное, и от неё сняты три порчи:

```
опись из дерева                          код 0, «опись сходится с деревом»
убрана строка ярлыки.flang               код 1, «НЕТ СТРОКИ: … несёт 17»
вписан README.md (обязательств не несёт) код 1, «ЛИШНЯЯ СТРОКА: README.md»
«написано» 17 → 99                       код 1, «НЕ СХОДИТСЯ: 99 против 17»
откат                                    код 0
```

## Своя ошибка, найденная этой же пробой

Первая редакция прибора звала `git ls-files '*.flang'` без
`core.quotepath=false`. Git отдаёт кириллические пути в кавычках с
восьмеричными кодами, и прибор молча пропускал **508 файлов из 1253** —
считал знаменатель по одним латинским именам и давал 13 082 вместо 13 637.

Поймали это порчи 1 и 3: они не покраснели, потому что `ярлыки.flang`
в опись из дерева не попал вовсе. Порча, которая не краснеет, — не
доказательство исправности прибора, а признак, что мимо цели прошла сама
порча. Ровно об этой ловушке предупреждала задача 7855.

## Не сделано

* **Опись не чинилась.** 191 строка — это не разовая правка, а решение:
  62 недостающие строки требуют прогона двоичного по каждому файлу
  (числитель), а 71 лишнюю надо удалять, разобравшись, почему файл
  перестал нести обязательства — переименован, вычищен или удалён.
* **Числитель не перемерян** и прибором не меряется по устройству.
* **Прибор не в CI.** Ставить сторожа под опись, которая красна на 191
  строку, значит поставить вечно красный шаг. Сперва решение по описи.
* Число 13 347 в КРИТЕРИЙ.md не правилось — файл вне git.
