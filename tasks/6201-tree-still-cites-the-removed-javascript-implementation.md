---
номер: 6201
заголовок: В дереве 783 ссылки на файлы снятой реализации на JavaScript, и на одной из них стоит довод, закрывший целую ветку развития
статус: в работе (заход 9 сентября 2026 в ветке r/6201: рода разложены прибором, действующие доводы переписаны на живые источники, сломанные пути в скриптах починены; не переснято ни одно число снятых проб, поиск других решений-наследников не начат)
исполнитель: Marat
ветка: r/6201
команда: любая
карта: Что мешает больше всего
рядом: 3258, 0049
---

# 6201. Реализации нет год, а доводы всё ещё ссылаются на её строки

20 августа 2026 (`fe8e8a37c`) из дерева удалена вторая реализация языка на
JavaScript: 48 файлов, 56 072 строки. Компилятор остался один.

Ссылки на её файлы остались. Перечисление 9 сентября 2026 — упоминания путей
вида `flang/src/*.mjs` и `flang/bin/*.mjs`, из которых оставлены только те, чей
путь сегодня не существует:

```
вхождений 783 в 201 файле
из них: docs/zettel 145, tasks/completed 32, docs 25, scripts 22,
        tasks 13, flang/scripts 10, docs/adr 7, остальное — по мелочи
```

## Почему это не косметика

Часть ссылок — записи лаборатории и завершённые задачи; им ссылаться на прошлое
законно. Но **часть ссылок несёт действующие доводы**, и вот показательный
случай, найденный при работе над ADR-0025.

`docs/integer-closed-under-addition.md:123` закрывает ветку «числа без верхней
границы» цитатой:

> Честен арифметически и назван невозможным в самом дереве —
> `flang/src/types.mjs:93` и `flang/src/types.mjs:138`

Обоих файлов нет. Довод, которым закрыта ветка развития языка, опирается на
строку, которую сегодня нельзя ни прочесть, ни перепроверить.

Там же, раздел 5 того же документа, лежит число «ноль новых мест из 303»,
снятое пробой `proba-vstrechno.mjs`. Пробы нет. **Число нельзя переснять**, а
оно стоит в доводе против целого направления работы.

И самое дорогое: верхняя граница числового типа 9 007 199 254 740 991 — это
`Number.MAX_SAFE_INTEGER`, константа стандарта ECMAScript. В дерево она пришла
13 августа 2026 (`06e8dd62e`) с доводом «представимостью самого значения
`Number.MAX_SAFE_INTEGER`», реализация ушла через неделю, число осталось.
Разбор — ADR-0025, раздел 2.

## Что сделать

1. **Разделить 783 упоминания на три ведра.** Историческое (записи лаборатории,
   завершённые задачи) — оставить как есть, это летопись. Действующий довод,
   опирающийся на несуществующий файл, — переписать или снять. Указание сделать
   что-то с несуществующим файлом — снять.
2. **Пересчитать каждое число, снятое удалённой пробой.** Числа, которые нечем
   переснять, назвать предположениями прямо в тексте, где они стоят, — либо
   написать прибор заново.
3. **Поискать другие решения, доставшиеся от снятой реализации.** Потолок нашли
   случайно, при работе над другим вопросом. Искать надо там же, где искали его:
   у величин, чьё значение объясняется устройством JavaScript, а не задачей.
   Отправная точка — представление числа: все десять целей печати держат его
   двоичным `double`/`float64`/`f64`/`float`, и ни одна не берёт целое машинное
   слово. Выбор сделан один раз, под реализацию, где выбора не было.
4. **Проверка на повторение.** Упоминание пути, которого в дереве нет, должно
   краснеть само — иначе через полгода их станет больше.

## Как понять, что сделано

- перечисление упоминаний несуществующих путей `flang/src/*.mjs` даёт только
  файлы летописи;
- ни один действующий довод в `docs/` не ссылается на удалённый файл;
- каждое число, снятое удалённой пробой, либо переснято новым прибором, либо
  названо предположением на месте;
- проверка краснеет на новой ссылке на несуществующий путь.

---

## Заход 9 сентября 2026: ветка `r/6201`

### Перечисление — своё, и оно шире шапки

Образец `flang/(src|bin)/…\.mjs` по всему дереву под учётом git, все типы
файлов, включая `emit/*.mjs` и `host/*.mjs`; отсев существующих не нужен — под
`flang/src` и `flang/bin` сегодня нет ни одного `.mjs`. На `4d13d5a88`:
**850 вхождений в 263 файлах**. Шапка называла 783 в 201: её образец был уже,
и повторить его нечем — команда в задаче не записана.

Рода сняты прибором по каталогу и имени файла:

| род | вхождений | файлов | что это |
|---|---:|---:|---|
| (а) летопись | 501 | 150 | `docs/zettel/`, `tasks/completed/`, `docs/adr/`, `docs/archive/`, журналы `CHANGELOG.md` и `docs/site/changelog.md` (печатаются из git), `docs/journal-carried-out-of-sources.md`, `docs/javascript-removal.md`, `docs/javascript-inventory.md`, `docs/javascript-checks-breakdown.tsv`, `docs/branches-2026-08-15.md`, снимки `flang/test/snimok` и `flang/test/zapis`, тексты с явным отрицанием (`docs/what-blocks-1-0.md`, `docs/course/09-eight-targets.md`, `.ai/AGENTS.md`) |
| (б) действующие доводы и указания | 145 | 40 | `docs/*.md` вне летописи, `docs/guide`, `docs/site/contributing*`, `docs/ifl/numbers.md`, `docs/спецификации`, комментарии в `docs/benchmark/*.flang`, `flang/proof/examples/*.flang`, `flang/proof/kernel.flang`, `flang/conc/*.md`, `fspec/policy.flang`, открытые задачи |
| (в) скрипты и код | 204 | 73 | `scripts/`, `flang/scripts/`, `.github/`, `editors/`, `benchmarks/`, `packaging/`, `flang/test/`, `flang/проверки/`, исполняющие части `flang/src/emit/*` и их копии в `bootstrap/` и `examples/*/printed`, напечатанные `flang/conc/*.js`, `flang/self/` |

После захода: 852 вхождения в 260 файлах (а 501 / 150, б 148 / 38, в 203 / 72).
Вхождений рода (б) не стало меньше — переписанный довод называет снятый путь
рядом с живым («в тот день — …; сегодня — …»), а не стирает его: стереть
значило бы стереть и то, чем довод держался. Условие шапки «перечисление даёт
только файлы летописи» этим заходом не выполнено, и для датированных отчётов
оно не нужно: они и есть летопись — только с действующими доводами внутри,
которые теперь подписаны живым источником.

### Род (б): старый довод → чем держится сегодня

**`docs/integer-closed-under-addition.md`** — показательный случай, разобран
целиком против единственного носителя `flang/self/types.flang` и ADR-0025:

- потеря `нат` на `н плюс 1` (раздел 1) — воспроизведена прогоном
  `bootstrap/flang check`, тот же `FLANG_TYPE`; живой путь — «Арифметика
  отрезков» (5110) → «Тип по концам» (243) → «Знак уцелел» (279) → «Тип
  неотрицательного» (311); частичность `элемент` — «Чем частична форма»,
  `flang/self/failures.flang:261`; точный шаг — «Отметить точный шаг»,
  `flang/self/totality.flang:3410`;
- довод «путь Б назван невозможным в самом дереве» (раздел 2, строки 93 и 138
  снятого файла) — **больше ничем в дереве не держится**: в `types.flang` нет
  ни «bignum», ни «Чёрча». Переписан на замер ADR-0025, раздел 3.4 (десять
  операций с машинным двойником, 272 строки исполняющей части на `double`,
  около 75 мест перевода число↔текст, десять целей без единого bignum);
- «ноль мест из 303» и «13 из 303» (раздел 5) — **переснять нечем**, названы
  предположением на месте; проба `proba-vstrechno.mjs` снята с реализацией,
  прибора на живом компиляторе нет;
- обходы раздела 6 перепроверены прогоном: 1 и 2 работают, 3 — нет; довод
  «сужение читает только литерал» устарел наполовину — «Сузить по границе
  номера» (`types.flang:4339`) уже связывает номер с длиной списка, но для
  места `элемент`, не для арифметики; ведомость `--proof --json` у трёх проб
  печатает одно и то же (`partialSites 1`);
- «Как повторить замер» переписан: как написано — никак; что нужно для нового
  прибора, названо, и в этом заходе он не написан.

**Указания на несуществующий двоичный сняты:** `docs/guide/how-to-write-flang.ru.md`
и навык `.ai/.claude/skills/flang-style/SKILL.md` (три команды `node …` →
`bootstrap/flang …`, «`LC_ALL` обязателен» снято — у двоичного локаль только
выбирает язык диагностик), `docs/site/contributing.md` и `.ru.md` («на Node
работает языковой сервер» → сервер в двоичном, `flang lsp --stdio`).

**Живая таблица `docs/ifl/numbers.md`** («Что напечатанный код НЕ держит»):
три строки стояли на снятых файлах и все три сегодня ложны — пределы у JS
есть (`flang/self/emit-js.flang:2362`), конкурентность печатают 7 целей из 10,
видов отказа 11, не 5 (`flang/self/failures.flang:192`). Переписаны с числами.

**Четырнадцать датированных отчётов** получили блок «Сверка с живым деревом»
с живыми адресами и приговором каждому доводу: `memory-and-regions`
(посылка «зеркала `bounded` в `flang/self/` нет вовсе» ложна — оно есть, 1 763
строки; сметы «проход + зеркало» завышены вдвое), `http-boundary` («словарь
знает пять поручений» — их 22, серверный набор заведён; «планировщик — не
программа на flang» — наоборот, `flang/self/conc.flang`), `cost-of-extension`
(таблиц `ЦЕЛИ` и `ВОЗМОЖНОСТИ_ЦЕЛЕЙ` нет вовсе; целей десять; понятий 151),
`modularity-and-packages` (план по снятым файлам: `lock`/`package` есть,
`deps`/`digest` нет, склад и версии отменены владельцем), `lockfile-without-store`,
`self-contained-address-length`, `HANDOFF`, `hacker-news-readiness`,
`body-shapes`, `lemmas-report`, `native-x86-backend` (коэффициент 1,80×
неперемеряем), `wasm-via-c`, `categorical-search-measurement`,
`eight-targets-collision-map`.

**Комментарии в корпусе и спецификациях** переписаны на живые адреса
(`docs/спецификации/*`, `docs/benchmark/05`, `11`, `13`, `docs/zamer-teorkat`,
`flang/conc/DISTRIBUTED.md`, `flang/conc/examples/measure.flang`,
`flang/proof/examples/*`, `flang/proof/kernel.flang`, проба Ч7104,
`fspec/policy.flang`, `flang/проверки/встроенные-формы.flang`,
`web/wasm/demo/index.html`). Два довода оказались **ложными сегодня**, и это
сказано на месте: `docs/benchmark/05-opposite.flang` («ядро тем же приёмом
доказывает ложь» — дыра закрыта, `flang/self/proofterm.flang:666`) и
`flang/proof/examples/precondition.flang` («вход проверяется вычислением на
границе» — проба: `требует н меньше 10` при `--args '{"н":50}'` отвечает 50,
код 0, и пример с нарушением проходит `check`). Третий — общий для семи мест:
списка `АКСИОМЫ` в дереве нет вовсе, «аксиом ноль» стережёт
`flang/scripts/kernel-forgeries.flang` доводом при слове, а не пустотой массива.

### Род (в): что починено, что оставлено

- `benchmarks/model-authoring/grade.py` — звал `node …/flang.mjs` по
  абсолютному пути чужой машины; переписан на `bootstrap/flang` (тотальность —
  из `check --proof --json`, коды — из текста диагностик, `emit --out` во
  временный каталог, код 2 «проверено не до конца» засчитан разбором). Сверка
  эталонов `validate_reference.py` — числа в разделе «Сторожа» ниже; что не
  проходит, не проходит по языку, а не по прибору: `run --args` двоичного
  берёт только скаляры (код `вход-не-скаляр`), поверхность FTS у трёх
  эталонов снята вместе с реализацией;
- `benchmarks/model-authoring/msgstats.py` — читал `KEYWORDS` из снятого
  лексера по чужому абсолютному пути; читает «Куски таблицы»
  `flang/self/lexer.flang`, 187 русских фраз;
- `editors/vim/autoload/flang.vim`, `README.md`, `checks/stream.sh` — ступень
  «`flang/bin/flang-lsp.mjs` в дереве» снята из поиска сервера и из текста
  подсказки; замер на Node печатается строкой «снят, замера нет»;
- `scripts/published-vs-tree.sh` — три несуществующих образца сняты из `case`.
- **Оставлено**, и почему: комментарии-провенанс в исполняющих частях
  `flang/src/emit/*` (72 места) — они копируются в семя `bootstrap/` и в
  напечатанные примеры, правка тянет перепечатку; шапки напечатанных
  `flang/conc/*.js` — вывод компилятора; входы примеров в `scripts/*.flang` и
  `flang/scripts/*.flang` — данные проверок, не пути; `flang/self/` — чужие
  волны; заметки «до 20 августа звали …» в шапках скриптов — явные отрицания.

### Что переснять нечем

«Ноль из 303», «13 из 303», формы номера (проба `proba-vstrechno.mjs` и
соседние), коэффициент 1,80× печатника C, таблица размеров замка, строки
`.mjs`-печатников в цене девятой цели — названы предположениями на месте.
Пункт 3 шапки (другие решения-наследники) не начат: представление числа
двоичным `double` во всех десяти целях — ADR-0025, раздел 2.

### Прибор пункта 4

Сторож ссылок (`scripts/link-guard.flang`, `./ярлык ссылки:проверка`) уже
краснеет на новый несуществующий путь в кавычках — но только в `.md`, вне
цитат `>` и вне «записей о прошлом», а записью о прошлом он считает и файл с
датой в первых восьми строках. Ровно поэтому он не видел ни одного из 145 мест
рода (б): все датированы. Прибора на комментарии `.flang` и на датированные
отчёты нет; заводить его — отдельная работа.

### Сторожа, снятые в этом заходе

- `sh .githooks/pre-push` — **код 0**, все одиннадцать дешёвых сторожей
  зелены (28 с). По дороге: сторож чисел в прозе краснел на восьми приметах,
  сдвинутых этой же правкой (строки `*.sh`, `*.py`, `*.html`, `*.vim`,
  `fspec/policy.flang`, `editors/vim`) — перемерены и датированы
  2026-09-09; задачник краснел на 6191 без исполнителя в базовом коммите —
  взят cherry-pick `d1af7140c` из `gh/dev`, где это уже починено.
- `bootstrap/flang io scripts/link-guard.flang` — **код 0**: файлов 1343,
  ссылок 1690, путей в кавычках 4114, исключений 176, битых 0; в цитатах 323 и
  в записях о прошлом 1655 не проверялись. На базе `4d13d5a88` он был красен:
  23 битых и одно устаревшее исключение — 16 заголовков старых коммитов в двух
  журналах, напечатанных из git (внесены в список известных не-путей как
  история — править их можно только историей), 2 в `docs/site/examples.md`
  (пример на ветке `r/react-ts-pure`, в ствол не влит), по 2 иллюстративных
  имени проб в задачах 3127 и 4271, 1 в этой задаче, и исключение 6130, чья
  цель ожила, — снято. Прогон стоит 7 минут и в хук не входит.
- `benchmarks/model-authoring/validate_reference.py` на переписанном
  `grade.py` — **10 эталонов из 24**: пять задач на числах и три на процессах
  проходят; одиннадцать со списками и записями во входе не проверить
  (`run --args` двоичного берёт только скаляры), три на поверхности FTS —
  снята вместе с реализацией. До правки прибор не запускался вовсе.
