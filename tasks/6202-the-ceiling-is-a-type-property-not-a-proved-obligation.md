---
номер: 6202
заголовок: Потолок числового типа объявлен свойством типа, а не доказан, и на нём стоит каждое восьмое проверенное место
статус: в работе
исполнитель: Marat
ветка: r/6202-6203-cheker
команда: первая
карта: Чего в языке нет вовсе
рядом: 6201, 1238
---

# 6202. Первый шаг ADR-0025: правило семейства П получает посылку

Решение ADR-0025 говорит: верхняя граница числового типа перестаёт быть
свойством типа и становится обязательством, которое доказывают. Задача — первый
шаг, за которым можно снять честное число.

## Что известно из замера ADR-0025

Подмена, снятая 9 сентября 2026: та же независимая проверяющая программа собрана
из копии исходника, где имя правила «ограниченность точным потолком по
построению» заменено на несуществующее.

```
как есть    доля-проигрыванием = 567 / 651 = 87,10 %
без правила доля-проигрыванием = 495 / 646 = 76,63 %
```

**72 проверенных места из 567 стоят на потолке. Прямо этим правилом закрыто 14
утверждений в пяти записях; остальные уходят потому, что одно несошедшееся
утверждение роняет всю запись.**

В таблице правил вывода `flang/proof/ПРАВИЛА-ВЫВОДА.tsv` — 78 правил.
Семейство П (7 строк: П1–П6 и запрет П✗) названо этим правилом целиком.
Семейство Кон (3 строки) называет его одним из двух допустимых. Остальные 68
потолка не касаются.

## Что сделать

1. **Взять ОДНО правило — П3** («ограниченность точным потолком по построению»
   от объявленного типа, `flang/self/proof-kernel.flang:2735` `«Ограниченное по
   типу»`, в проверяющей программе `flang/proof/чекер/сверщик.c:6509–6514`).
   У него сегодня в колонке «посылки» стоит «—»: заключение `Γ ⊢ Т ≤ К` следует
   из объявления и больше ни из чего.
2. **Дать ему посылку.** Заключение то же, но следует оно теперь из доказанного
   факта, а не из имени типа. Написать это надо в трёх местах сразу: строка
   таблицы правил, ядро, независимая проверяющая программа.
3. **Пересчитать долю.** Ожидание: она просядет, а потом вернётся, когда записи
   пяти названных мест будут пересняты с новым ходом. Число снимается прогоном
   `sh flang/proof/доля-корпуса.sh --проигрыванием`, не оценкой.
4. **Завести подделку.** Утверждение о границе, у которого посылки нет, обязано
   быть отвергнуто. Сегодня оно проходит: границу даёт тип.

## Чего эта задача НЕ делает

Не трогает остальные шесть правил семейства П, не трогает представление числа и
не заводит длинную арифметику. Всё это — отдельные работы, и ADR-0025 говорит
прямо: длинная арифметика этим решением не покупается.

## Как понять, что сделано

- у строки П3 колонка «посылки» перестала быть «—»;
- ядро и независимая проверяющая программа согласны в том, что П3 требует
  посылки, — проверено подделкой, которая раньше проходила;
- `sh flang/proof/доля-корпуса.sh --проигрыванием` даёт число не ниже
  567 / 651 после пересъёмки записей;
- `SVERSCHIK=… sh flang/proof/чекер/пробы/прогон.sh` печатает «сошлось всё».

## Ход работы

### 9 сентября 2026 — A-m0, базлайн (Lead-A, учётка a)

Worktree `/srv/tmp/dokazuemyy/wA`, ветка `r/6202-6203-cheker` от `gh/dev` `2a97c8204`;
сверщик собран здесь же `make -C flang/proof/чекер` (md5 `0caa5813c4587cfb693b4e3496d5bae9`),
линейка зовётся из этого дерева — `доля-корпуса.sh` переменную `SVERSCHIK` не читает.

| прибор | результат |
|---|---|
| `sh flang/proof/доля-корпуса.sh --набор корпус --проигрыванием` | **567 / 651 = 87,10 %**; числитель набора 155 (храповик 155) |
| `sh flang/proof/чекер/пробы/прогон.sh` | «сошлось всё»: подделок 265 (принято кодом 0 — 0), честных 142 (отвергнуто 0), набор подделок 31 из 31 |
| `sh scripts/доказуемость.sh` | 1 ДА, 2 НЕТ (87,10 %), 3 ДА, 4 ДА |

Ожидание постановки «237 / 141» снято на голове `4d13d5a88` (проект 6203 §2.5); на
`2a97c8204` набор проб больше — 265 / 142, те же числа стоят в таблице §1.2 проекта
6202 для `gh/dev`. Инварианты (0 принятых подделок, 0 отвергнутых честных) держатся,
доля совпала знак в знак — базлайн принят.

### 9 сентября 2026 — A-c1, И1: П3 получает посылку Пред1 на стороне сверщика

Правки: `flang/proof/чекер/сверщик.c` (образец строки `обязательство вход «» ⟨⟩ строка # на
зовущем`; семейство Пред — шаг `Пред1 ⟨ИМЯ помещается⟩ строка N`; П3 — только `из M`, M — Пред1
о том же имени, основание `строка N` — отказ поимённо; `сверить_обязательства` переигрывает
каждую строку обязательства: строка объявления, формула `ИМЯ не больше 9007199254740991`,
закрытие по вызовам в том же файле и по `дано` примеров); `ПРАВИЛА[]` и фильтр домена
(1606/4291/4330) не тронуты; пробы `пробы/семьи/вывод/` (+`pred-zov*`, `порча/p3-bez-posylki`,
`порча/pred-zov-vyshe`), их вплетение в `пробы/прогон.sh` (+9 подделок, +2 честные).
Сверщик собран `make -C flang/proof/чекер` в этом дереве.

| фальсификатор | ожидание | снято |
|---|---|---|
| (1) доля ПОСЛЕ | ровно 567 / 651 | **567 / 651 = 87,10 %**, сводка `доля-корпуса.sh` побайтно та же, что базлайн |
| (2) abilities / poddelka-dlina / sources / boundaries / corpus-natural-ceiling | коды 3/0/0/3/0 как в базлайне | 3/0/0/3/0, вывод сверщика на всех пяти побайтно тот же, что у бинаря `2a97c8204` |
| (3) подделка «П3 с основанием `строка N` без посылки» (`порча/p3-bez-posylki.запись` = прежняя `vyvod.запись`) | код 1 | **красная до правки**: бинарь `2a97c8204` — код 0; после — код 1, причина «П3 — посылки нет: объявление типа (строка 36) посылкой не является» |
| (4) `прогон.sh` | «сошлось всё» | «сошлось всё»: подделок 274 (принято 0), честных 144 (отвергнуто 0) |
| `доказуемость.sh` | 1/3/4 ДА | 1 ДА, 2 НЕТ (87,10 %), 3 ДА, 4 ДА |

Честная `vyvod.запись` переснята на форму §2.4 (строка обязательства + Пред1 → П3 `из 1` → Разв2):
код 0, «выводов факта о типе проиграно заново 8». На бинаре `2a97c8204` она даёт код 1
(строка не узнана) — это и есть «сверщик раньше ядра». Проба `pred-zov` (форма «Разности пары»
§2.5, два обязательства, два зовущих в файле, `дано` примера) — код 0; `порча/pred-zov-vyshe`
(зовущий подставляет литерал 9007199254740992) — код 1; `pred-zov-nechem` (зовущий подаёт
`длина т`) — код 3, причина названа: «обязательство входа «первое» сверщик закрыть не смог».

Замеры ядром (двоичный `bootstrap/flang` из worktree `r4135`, `120800840`, только чтение):
довод `число` в позицию `нат` — `FLANG_TYPE`, `м плюс 1` — тоже, `длина т` — проходит. Отсюда:
путь закрытия «имя под `требует ≤ К'`» из §2.4 недостижим (аргумент на месте точного довода
типизируется точным) — не написан; довод-выражение — реален, и это третий исход, не отказ.
Не сделано: случай 2 `из_объявленного` (потолок по типу результата на дороге калькулятора) —
не тронут, долю не двигает (§1.2, третий прогон), «либо» в §3.1 оставлено на решение;
`сверщик.flang` блока `вывод` не знает и сегодня — не тронут.

### 9 сентября 2026 — A-c2, И2: тринадцатое семейство «Пред» в ведомости

Правки: `flang/proof/ПРАВИЛА-ВЫВОДА.tsv` — шапка «Н Кон П Пред О Д С СС В Ч Разв Т Р — тринадцать»,
строка П3: посылки «1) Т : предст», категорно с ⟨1⟩, входов 1, сверено «код-шире» (ядро до И3 выводит
без посылки); новая строка Пред1 (семейство Пред, правило_ядра «—», входов 0, опора
`proof-kernel.flang:2746 «Натуральное по типу»`, сверено «нет-в-ADR» — иначе С-2 требует 55 правил по
ADR-0022 §3); `flang/proof/сверка-ведомости.sh:243` — «Пред» в закрытом списке семейств;
`ПРАВИЛА-ВЫВОДА.отпечаток` переснят (`09071bfb…`); `flang/proof/lean/Правила.lean` — строка П3
переписана дословно (тело леммы то же: h1 теперь — заключение Пред1), новая лемма «Пред1»
(тождество на гипотезе-обязательстве), шапка счёта 78 → 79, 75 → 76.

| фальсификатор | снято |
|---|---|
| без «Пред» в списке семейств (строка 243) — красный | **КРАСНО** «правило «Пред1»: семейство «Пред» вне закрытого списка», код 1 |
| отпечаток до пересъёмки — красный | **КРАСНО** «отпечаток ведомости разошёлся», код 1 |
| `сверка-ведомости.sh` после — зелёный | код 0: «сошлось всё: 79 строк ведомости, четыре сторожа зелены»; правил 67 (по ADR §3 — 55), запретов 12 (5); входов у всех стрелок 61 |
| `сверка-ведомости.sh --подлоги` | код 0, «сошлось» |
| `lean/прогон.sh` | **НЕ ПРОВЕРЕНО**: Lean в дереве и в PATH нет (код 2); лемм в файле 85 при 79 строках (как и прежде 84 при 78 — у части строк по две леммы) |
| гейт A-v после c2 | `прогон.sh` «сошлось всё» (274/0, 144/0); доля **567 / 651**, числитель набора 155; `доказуемость.sh` 1 ДА, 3 ДА, 4 ДА |
