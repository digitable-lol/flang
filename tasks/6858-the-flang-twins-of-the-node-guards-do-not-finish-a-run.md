---
номер: 6858
заголовок: flang-двойники сторожей написаны, но не доходят до конца прогона — поэтому node из дерева так и не ушёл
статус: свободна
исполнитель: —
ветка: —
команда: любая
карта: Что мешает больше всего
рядом: 9960
---

# Двойники есть, а node остался — потому что двойники не доходят

## Откуда вопрос

Владелец спросил дословно: «а зачем binary.mjs? перепиши его на flang».

Ответ на поверхности: `flang/scripts/binary.flang` **уже написан** (613 строк,
ячейка Ч382 задачи 9960) и им пользуются flang-сторожи. `binary.mjs` (848
строк) жив не сам по себе, а как библиотека для семи потребителей на node.

Но при разборе выяснилось другое, и оно крупнее заданного вопроса.

## Опись, которой задача 9960 не сделала

Задача 9960 прямо записала в «Не сделано»: «настоящей описи „46 файлов,
сколько реально перенесено" нет». Вот она, по каталогам `flang/scripts/` и
`scripts/` — 20 файлов `.mjs`:

| куча | сколько | файлы |
|---|---:|---|
| двойник ЕСТЬ | **14** | binary, binary-rules-guard, cli-keys-guard, count-guard, direct-run, direct-run-guard, discriminating-search, link-collision-guard, word-guard, build-changelog, build-changelog-page, jargon-guard, latin-collision-guard, type-collision-guard |
| двойника НЕТ | **6** | claim-guard, name-guard, proof-ledger, target-words, word-occupancy, wasm-run |

Правило самой 9960 было: «Старый `.mjs`-файл НЕ удаляется. Новый `.flang`
живёт рядом». Оно соблюдено — но у него оказалось следствие, которого никто
не назвал.

## Находка: ни один ярлык не переключён на двойника

Из `ярлыки.flang` выписаны все ярлыки, чья КОМАНДА реально зовёт `node`.
Среди них — девять, у которых flang-двойник существует:

```
правила:проверка      -> node flang/scripts/binary-rules-guard.mjs
подсчёты:проверка     -> node flang/scripts/count-guard.mjs
ключи:проверка        -> node flang/scripts/cli-keys-guard.mjs
столкновения:проверка -> node flang/scripts/link-collision-guard.mjs --дерево
столкновения:порча    -> node flang/scripts/link-collision-guard.mjs --порча
поиск:различающий     -> node flang/scripts/discriminating-search.mjs
слово:проверка        -> node flang/scripts/word-guard.mjs
слово:подлог          -> node flang/scripts/word-guard.mjs --porcha vse
журнал / журнал:страница -> node scripts/build-changelog*.mjs
```

**Двойники написаны, а зовут по-прежнему node.** Поверхность node не
уменьшилась ни на файл — при том, что на перенос ушло двенадцать ячеек.

## Почему не переключили — проверено прогоном, а не догадкой

Три двойника прогнаны на настоящем дереве. Двоичный — семя дерева
`235c1966`, собранное из него же.

| двойник | node | flang |
|---|---|---|
| `count-guard` | код 124 (не уложился в 300 с) | **код 3**: «Понизить до шага» исчерпала предел шагов (10 млн) на глубине 54 |
| `link-collision-guard` | известно 4,3 с, код 0 (замер координатора) | **код 3**: «Шаг декларации» исчерпала предел шагов (10 млн) на глубине 5 |
| `discriminating-search` | код 0, выдал разбор | **не уложился** в отведённое время при пределе 500 млн шагов |

Предел по умолчанию у `flang io` — 10 000 000 шагов. Его можно поднять
(`--max-steps`, так уже сделано для `задачник:проверка` — 200 млн). Но
дело не в пределе: `link-collision-guard.flang` при `--max-steps
2000000000` и десятиминутном сроке съел **3 211 671 257 шагов**, доходил
до 4,4 ГиБ памяти, полоса хода всё время показывала «0 %» — и был убит
СРОКОМ (код 124), а не пределом. Его node-двойник считает то же самое за
**4,3 секунды**.

Две вещи отсюда, обе стоит назвать:
* три с лишним миллиарда шагов против четырёх секунд — это не «медленнее»,
  это другой порядок задачи;
* **`--max-steps 2000000000` не остановил прогон на двух миллиардах.**
  Ключ либо не дошёл до плана, либо считается не так, как читается. Само
  по себе это отдельная беда: предел, который не держит, хуже, чем
  отсутствие предела, — он создаёт ложное чувство границы.

  **ПОПРАВКА КООРДИНАТОРА, прогоном: ключ работает, но считает НЕ ТО,
  что от него ждут.** Проба:
  `bootstrap/flang io flang/scripts/tasks.flang --plan 'Задачник цел'
  --max-steps 1000` — прогон честно остановился, и вот дословно чем:
  «функция «Разделы карты» исчерпала лимит шагов (1000) **на глубине
  вызовов 3**». То есть предел считается **НА ВЫЗОВ ФУНКЦИИ, а не на
  прогон целиком**. Прогон из многих вызовов может съесть сколько угодно
  суммарно, ни разу не упершись в предел — ровно то, что здесь и
  наблюдалось: 3,2 млрд суммарно при пределе 2 млрд на вызов, и снял его
  срок, а не предел.

  Вывод от этого не слабеет, а становится точнее: «предел, который не
  держит» — не сломанный ключ, а **ключ не той меры**. Общего бюджета на
  прогон в дереве нет вовсе, и всякая сегодняшняя фраза вида «не
  уложился в N шагов» означала «ни один ВЫЗОВ не превысил N», а не «весь
  прогон уложился в N». Это касается многих замеров этих суток.

Разница не в разы. Двойник не медленнее — он не сходится вовсе, и это
надо чинить как беду, а не как «дать больше бюджета».

**Попутно подтверждена задача 1722**: прогон `discriminating-search.mjs`
оставил в дереве временный каталог `flang/scripts/ds-XXXXXX/` с
`.flang`-файлами. Убран руками; беда воспроизводится с первого прогона.

**То есть двойники не «дописаны и ждут переключения». Они не доходят до
конца прогона на настоящем дереве.** Это и есть настоящая причина, по
которой node остался: переключить ярлык было нельзя, а сказать об этом вслух
в отчётах ячеек — не сказали.

## Отдельно про `word-guard`: двойник честно неполон, и это записано

Шапка `word-guard.flang` сама говорит: ключа `--porcha` (шесть встроенных
порч) «здесь НЕТ вовсе», `--perepis` тоже не перенесена. А ярлык
`слово:подлог` зовёт именно `--porcha vse`. Значит `word-guard.mjs` не может
быть удалён даже если основной прогон двойника заработает.

## Что это значит для вопроса владельца

`binary.mjs` нельзя ни удалить, ни «просто переписать»: его держат семь
потребителей.

* четыре из них — прогоны в `flang/test/*.mjs` (`uzel-osnastka`,
  `planirovshchik-celi.test`, `flang-bridge`, `svyaz-celi.test`), это
  отдельная и более крупная работа;
* три — сторожи: `word-guard.mjs` (двойник есть, неполон),
  `target-words.mjs` (339 строк, двойника НЕТ),
  `word-occupancy.mjs` (304 строки, двойника НЕТ).

Из `binary.mjs` потребителями используется девять имён: `КОРЕНЬ`,
`позвать`, `путьДвоичного`, `позватьФункцию`, `ведомость`,
`ведомостьИсходника`, `дерево`, `ключевоеСлово`, `токеныМногих`. Из них в
`binary.flang` есть соответствия для семи; `позватьФункцию` и `токеныМногих`
соответствия не нашли (всего в `binary.mjs` 31 вывод, в `binary.flang` 37
функций — счёт не сходится пофункционально, нужен разбор по одному).

## Порог

1. Взять ОДИН двойник (дешевле всего `link-collision-guard`, у него node-
   двойник считает за 4,3 с) и довести его до конца прогона: понять, почему
   съедаются шаги, и уложиться в бюджет, который не стыдно поставить в
   ярлык. Без этого переключать нечего.
2. Переключить ярлык на двойника и показать прогоном, что он даёт то же
   самое, что node. Это правка `ярлыки.flang`.
3. Только после этого удалять `.mjs` — и по одному, с прогоном на каждый.
4. `target-words.mjs` и `word-occupancy.mjs` (339 и 304 строки) — написать
   двойников; без них `binary.mjs` не умрёт.

## Не сделано

Прогнаны три двойника из четырнадцати — вывод «не доходят» проверен на
трёх, на остальных одиннадцати не проверен и переносить его на них нельзя.
Причина, по которой съедаются шаги, не найдена ни у одного. Ни один `.mjs`
не удалён и ни один ярлык не тронут: удалять, не показав работающую замену,
запрещено порогом самой этой задачи.

## ⚑ 7 сентября 2026, ветка `r/scripts-mjs`: три `.mjs` снесены, ярлыки переключены

Порог задачи выполнен для трёх из четырнадцати — прогоном на дереве `7c4f074d`,
двоичный из его же семени, node v26.7.0:

| файл | двойник | сравнение прогоном | что переключено |
|---|---|---|---|
| `binary-rules-guard` (900) | код 1 у обоих, те же 7 находок; 5 с → 38 с, 0,68 ГиБ | ярлык `правила:проверка` |
| `cli-keys-guard` (372) | код 0 у обоих, те же 8 разрядов долга; 0,2 с → 39 с; проба подлога `--fast` на двойнике красит | ярлык `ключи:проверка`, работа `klyuchi` в `ci.yml` (без `setup-node`) |
| `direct-run-guard` (556) | mjs 3 находки, двойник те же 3 **плюс** `flang/src/emit/js/flang_host_node.js:497` — mjs прощал его списком образцов (строка 338), двойник знает одного изгнанника; 0,3 с → 343 с, 1,9 ГиБ | ничего: не звал никто |

Двойнику ключей нужен `--max-steps 200000000`: без него «Шаг литералов»
упирается в умолчание 10 млн на вызов (код 3), всего же прогон стоит 173 млн
шагов.

Остальные одиннадцать НЕ тронуты, и вот почему (разбор по каждому —
`/srv/tmp/dokazuemyy/ПЕРЕДАЧА/karta-mjs.md`, вне дерева): `binary.mjs` и
`direct-run.mjs` держат узел сайта и тесты на Node; у `link-collision-guard`,
`word-guard`, `count-guard`, `discriminating-search` двойники частичны (нет
`--дерево`/`--порча`, `--porcha`, семи правил из десяти, теоремных семей);
у `claim-guard`, `name-guard`, `proof-ledger`, `target-words`, `word-occupancy`
двойника нет. Отдельно: `word-guard.mjs` на этом стволе ПАДАЕТ трассировкой
(`createHash.update(undefined)`, строка 183) за 0,4 с, а `word-guard.flang`
отвечает `FLANG_SLOVO_PUSTOTA` за 88 с — работы `binary.yml:575` и `:1527`
красны на стволе независимо от этой ветки.

## ⚑ 8 сентября 2026, ветка `r/scripts-dubli`: одиннадцать оставшихся перемерены, снесено ноль

Порог задачи («довести двойник до конца прогона, переключить ярлык, показать
прогоном то же, и только потом удалять») проверен на КАЖДОМ из одиннадцати.
Ни один не прошёл, поэтому ни один `.mjs` не снесён и ни один вызов не
переключён. Дерево `5f679521`, двоичный из его же семени (`make -C bootstrap`),
node v26.7.0, все прогоны `env -u FLANG_MODULE_DIR LC_ALL=C.UTF-8` из корня.

| `.mjs` | двойник на flang | сравнение прогоном сегодня | почему остаётся |
|---|---|---|---|
| `binary` | `binary.flang` — библиотека на flang | — | библиотеку flang нельзя ввезти в Node, а её ввозят 4 файла `flang/test/*.mjs` (ярлык `тесты`), `docs/site/{podsvetka,surfaces-run}.mjs` и семь `.mjs` этого каталога |
| `claim-guard` | нет | код 1 за 1,3 с: 3 утверждения о недостаче больше не правда | переписывать нечего — двойника нет |
| `count-guard` | `count-guard.flang`, частичный | двойник: код 1 за 8 мин 3 с, одна находка — в его же комментарии (`MAX_STEPS` сказано 300000000000, измерено 1400000000000). `.mjs` находил 238 расхождений и идёт 1 ч 55 мин | у двойника нет правил «N строк про путь», таблиц, ведомости, `--fix`, `--дерево` |
| `direct-run` | `direct-run.flang` — порт только чистой части (раскодирование `file://`) | снял файл и прогнал: `node docs/site/site-numbers.mjs --check` падает `ERR_MODULE_NOT_FOUND`; вернул — работает | это Node-библиотека, её ввозят 13 файлов; вопроса «запущен я или ввезён» у flang нет вовсе, заменить нечем |
| `discriminating-search` | `discriminating-search.flang`, частичный | `.mjs`: код 0 за 50 с — 454 утверждения, 2146 прогонов, 108 доказано, расхождений 0. Двойник (`--max-steps 2000000000`): код 0 за 13 мин 53 с и «дел 57, **проб запущено 0**, дыр 0» — ни одной пробы не пущено, «дыр 0» ничего не значит | двойник не гонит пробы |
| `link-collision-guard` | `link-collision-guard.flang`, частичный | вход по умолчанию: оба код 0, «43 файла, столкновений 0» — сошлись; `.mjs` 0,2 с, двойник 2 мин 21 с и 5,1 ГиБ, нужен `--max-steps 2000000000`. Режим `--дерево` у `.mjs`: код 0 за 4,3 с, 54 столкновения в слитой библиотеке (три «НЕ НАЙДЕН модуль» на стволе починены) | ярлыки и `binary.yml` зовут `--дерево` и `--порча`, а их у двойника нет; `scripts/identical-declarations.sh` ввозит из него функцию `замыкание` |
| `name-guard` | нет | не гонял (по прошлому замеру 9 мин 41 с и 10,7 ГиБ) | двойника нет |
| `proof-ledger` | нет; `ledger-summary.flang` — верхняя оценка без вычитания подключений | не гонял (не сходится за 25 мин, под воротами часы) | двойника нет |
| `target-words` | нет | код 1 за 1 мин 0 с: подделка `cpp` не собирается — красен по делу | двойника нет |
| `word-guard` | `word-guard.flang`, частичный | **разошлись**: `.mjs` код 0 за 0,8 с («нарушений 0; файлов 1, утверждений 4»), двойник код 1 за 1 мин 4 с — `FLANG_SLOVO_PUSTOTA`, «ведомость без примеров не сложилась». Починка `89c41f87` дошла до `.mjs` и не дошла до двойника | приговоры разные, и `--porcha` у двойника нет |
| `word-occupancy` | нет | код 1 за 40 с: 942 файла, слово «это» занято, цепочкой сломалось бы 23 места | слово приходит доводом, а доводов у `flang io` нет |

Что из этого новое против замера 7 сентября:

* `word-guard.mjs` больше НЕ падает трассировкой — починен `89c41f87`; теперь
  зелен, а двойник красен, то есть расхождение стало ярче, а не исчезло;
* `link-collision-guard.mjs --дерево` теперь код 0: три «НЕ НАЙДЕН модуль»
  на стволе починены, остались 54 предупреждения о слитой библиотеке;
* обоим двойникам — `link-collision-guard.flang` и `word-guard.flang` — нужен
  `--max-steps 2000000000`: без него они падают кодом 3 на умолчании 10 млн
  шагов на вызов, как двойник ключей;
* `discriminating-search.flang` доходит до конца (13 мин 53 с) и всё равно не
  меряет: 0 проб.

Долг вне flang не изменился — `sh scripts/published-vs-tree.sh --перепись`
код 0: 158 файлов и 49 624 строки долга, «опубликованное сходится с деревом».
Задачи 4838 и 7790 править нечем: снос `.mjs` двигает только перепись, а сноса
не было.
