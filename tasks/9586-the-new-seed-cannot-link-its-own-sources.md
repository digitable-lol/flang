---
номер: 9586
заголовок: Потолок шагов, впечатываемый в новое семя, больше цены связывания собственных исходников
статус: сделана
исполнитель: m
ветка: m/open-work-relisted
команда: любая
карта: Что мешает больше всего
рядом: 0003, 0052, 5563, 6818, 8721
---

# 9586. Потолок шагов нового семени меньше цены его собственного связывания

## Чем измерено

`scripts/raskrutka.sh:179` (снято 29 августа 2026):

```sh
MAX_STEPS=4000000000
```

Это **не предел текущего прогона**. Тот же файл, строки 119 и 424, говорят
прямо, что число уходит в `#define FL_MAX_STEPS` печатаемого рантайма:

```
119:#   2. в копии flang_runtime.h поправить строку «#define FL_MAX_STEPS» на новое
424:#   предел   — MAX_STEPS и MAX_DEPTH: они уходят в `#define FL_MAX_STEPS`.
```

То есть четыре миллиарда впечатываются в НОВОЕ семя как его вшитое умолчание.

Цена одной стадии `«Связать исходники»` на замыкании компилятора названа
владельцем 29 августа: **14,2 млрд шагов**. Я это число не переснимал —
замер стоит десятки минут и место у ворот. Но порядок подтверждается соседним
живым прогоном: `check flang/self/bootstrap/compiler.flang` идёт с ключом
`--предел-шагов 10000000000` и `100000000000`, а не с четырьмя миллиардами.

**Следствие: семя, напечатанное с этим числом, не свяжет собственные
исходники.** Оно соберётся, ответит `--version`, пройдёт мелкие пробы — и
упрётся на первом же настоящем деле.

## Что сделать

1. Переснять цену стадии `«Связать исходники»` на нынешнем замыкании — одним
   прогоном с большим потолком и разбором по стадиям
   (`flang check … --предел-шагов 400000000000`, стадии видны в потоке ошибок).
2. Поставить `MAX_STEPS` с запасом от этого числа, а не от старого.
3. Записать в `scripts/raskrutka.sh` рядом с числом, **от какого замера** оно
   взято и когда. Голое число без замера через месяц снова отстанет.

## Как понять, что сделано

Свежесобранное из нового семени `bootstrap/flang` отвечает без ключа предела:

```sh
make -C bootstrap -j8
./bootstrap/flang check flang/self/bootstrap/compiler.flang
```

и не даёт `FLANG_STEP_LIMIT`. Прогон без ключа — и есть проверка: ключ
ЗАДАЁТ предел, а не поднимает вшитый, и подставленный ключ спрятал бы беду.

## Что от неё зависит

Перепечатка (0003) и всё, что за ней. Заход, кончившийся новым семенем с этим
числом, — это заход, кончившийся семенем, которым нельзя работать.

## Чем закрыта — 30 августа 2026, ствол `f9e67fa6`

Все три пункта «Что сделать» выполнены в стволе, и вот чем.

**Число, на котором стояла задача, из файла исчезло.** `MAX_STEPS=4000000000`
больше нет; сегодня `scripts/raskrutka.sh:322`:

```sh
MEASURED_COST=456857834234
MAX_STEPS=1400000000000
```

**Цена связывания переснята и досчитана** — `scripts/raskrutka.sh:280`: стадия
«Связать исходники» стоит **14 228 379 129** витков, и она досчитана до конца, а
не оборвана. Новый потолок выше неё в 98 раз.

**Довод записан рядом с числом** — большая врезка `scripts/raskrutka.sh:229–330`
(«29 АВГУСТА 2026, ВЕЧЕР: ПИК ИЗМЕРЕН, И ПОЛ ОТМЕНЁН»), с оговоркой, которую
надо читать вместе с числом: двоичный точной цены не называет, вилка — от
455 989 437 934 до 456 857 834 234 при шаге выборки 868 396 300, и
`MEASURED_COST` берёт **верхний** конец нарочно.

**И оно сторожится кодом, а не памятью человека:**

* `scripts/raskrutka.sh:328` — отказ кодом 2, если `MAX_STEPS < MEASURED_COST`;
* `same_numbers_in_second_print` (`:470`) читает три числа из
  `scripts/bootstrap-c.sh` (`:81`, `:93`) и падает кодом 2 при расхождении;
* `:551–554` — клеймо: печать сверяет `#define FL_MAX_STEPS` в напечатанном с
  тем, чем печаталось.

**Живое подтверждение.** Идущий сейчас заход перепечатки
(`/srv/flang-rabota/m-reprint-4`) пущен именно этим числом:

```
flang emit flang/self/bootstrap/compiler.flang --target c … --max-steps 1400000000000
```

**Чего это НЕ закрывает.** Проверка из раздела «Как понять, что сделано» —
собрать `bootstrap/flang` из НОВОГО семени и позвать `check` без ключа — станет
возможна только когда заход дойдёт до конца и семя будет закоммичено. Это
задача **0003**, и остаток лежит там, а не здесь: здесь беда была в числе, и
числа этого больше нет.

**Побочно:** шесть мест дерева всё ещё называют прежний потолок 300 млрд или
прежние 4 млрд (`docs/reprint-cost.md:80–87`, `ярлыки.flang:47–49`,
`flang/proof/сверить.sh:82`, `flang/proof/сличить.sh:23`,
`flang/self/conc.flang:119`, `flang/stdlib/datetime.flang:62`,
`docs/site/kak-dokazat.ru.md:1348`). Это заведено отдельной задачей — сюда оно
не относится: задача была про число в `raskrutka.sh`, а не про прозу о нём.
