---
номер: 9617
заголовок: Кругооборот проверяет печатника, а не разборщика — и потому не снимает с доверия ни строки
статус: открыта
исполнитель: —
ветка: refs/ячейки/Ч332-pechatnik-iz-dereva/*
команда: любая
карта: Что мешает больше всего
рядом: 9616, 9614
---

# 9617. Единственный путь Г5 пройден до конца и НЕ работает

Замер Ч332, 2 сентября 2026. Результат отрицательный, и в этом его цена: он
закрывает направление числом, а не мнением.

## Что сделано

Написана **десятая печать** — `flang/self/emit-flang.flang`, 527 строк,
КОД 363, 33 функции (коммит `e79602f6`, ветка `a/pechatnik-iz-dereva-razbora`).
Это то самое, чего в дереве не было: печать из дерева разбора обратно в текст
flang. `flang check` — код 0, замечаний нет. Печатает **99,80 %** узлов выборки
в 111 266 узлов (43 дерева).

Переиспользовано девятью печатями немногое и намеренно: навигация по дереву из
`emit-c.flang` (код 185) и печать строк/чисел из `core/json.flang` (код 82).
Остальное не годится и измерено `grep`-ом: дефункционализацию зовут 8 печатей из
9 — а она МЕНЯЕТ дерево, кругообороту же надо напечатать разобранное. Отсюда
размер: **363 строки кода против 22 119 у девяти — 1,6 %.**

## Кругооборот на 11 настоящих файлах (533 строки, код 347)

| условие | что значит | итог |
|---|---|---:|
| К1 | напечатанное побайтово равно исходнику | **0 из 11** |
| К2 | дерево цело после разбор→печать→разбор | 11 из 11 |
| К3 | печать — неподвижная точка | 11 из 11 |

Все 11 напечатанных проходят `flang check` с нулём замечаний, как и исходники.

К1 не сходится не по вине печати: **186 строк из 533 (34,9 %) — комментарии и
пустые — в дереве отсутствуют вовсе**. Дословно вернулось 157 строк кода из 347
(45,2 %).

## Главное: снято с доверия НОЛЬ строк, и это ЗАМЕР

**Подделка прогнана.** В настоящем дереве поменяны местами ветви `то` и `иначе`
одного многострочного `если` — **К2 на подделке СОШЛОСЬ.**

Причина резче, чем предполагала Ч323: в К2 участвует не исходный текст, а только
то, что разборщик из него сделал. **Значит К2 есть проверка ПЕЧАТНИКА, а не
разборщика.** Кругооборот не может снять разбор с границы доверия, потому что
никогда не смотрит на исходник.

Проекция Ч323 «5287 → 199» стояла на побайтовой сверке с исходником, а та
измеренно недостижима (0 из 11). **Замены числу нет, и выдумывать её нельзя.**

## Что всё-таки нашлось, и это уже не ноль

Привязка проверяема **сверкой мест дерева с отступами исходника**: 76
многострочных `если` из 76 на шести настоящих файлах, подделка отвергнута 1 из 1.

Покрытие названо честно: **76 из 283 (26,9 %)**. Остальные 207 записаны в строку,
где привязку держат скобки. И отдельно: **155 узлов `if` из 438 вообще не
происходят от слова `если`** — это разложенные `и притом`, `или`, `не`.

## Не сделано

На `parser.flang` печатник не прогонялся ни разу (крупнейший файл выборки — 61
строка). Теоремы, категорная поверхность, планы, наследие FTS (0,20 % узлов) не
печатаются. Импорты не печатаются. Проверка привязки — прототип на питоне, на
flang не переписана, в `сверщик.c` не заведена, цена переноса не замерена, на
кривом ИСХОДНИКЕ не пробовалась. Цель `flang emit --target flang` не заведена —
потребовала бы перепечатки.

## Что это значит для Г5

Из четырёх путей развилки Ч323 три давали ноль по её же замеру, а четвёртый —
единственный, который считался работающим, — теперь тоже даёт **ноль, и это
проверено подделкой**. Значит Г5 в нынешней постановке не берётся ничем из
названного, и либо нужен пятый путь (сверка с отступами — первый кандидат, 26,9 %
покрытия), либо надо переформулировать само условие.
