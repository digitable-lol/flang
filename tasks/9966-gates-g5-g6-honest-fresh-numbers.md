---
номер: 9966
заголовок: Гейты Г5 и Г6 — им нужно только честное свежее число, не полнота
статус: открыта
исполнитель: —
ветка: —
команда: любая
карта: Что мешает больше всего
рядом: 9964, 9965, 9967
---

# 9966 — Г5 (разбор снят с границы доверия) и Г6 (seed воспроизводим бит в бит)

Прочитать разделы «Г5» (896–1084) и «Г6» (1085–1418) `КРИТЕРИЙ.md`
целиком первым делом, и раздел «Приговор» в самом конце файла: у Г5 и
Г6 планка НИЖЕ, чем у остальных пяти — **«Г5 и Г6 названы числом»**, не
«взяты полностью». Это не повод занижать проверку, а повод не гнаться
за 100%, если честное неполное число уже готово.

## Г5 — что уже известно

Слепой сторож (`seed-parses-sources-guard.sh` или его аналог, номер в
дереве мог смениться) не подключён никуда — но его работу уже делает
ДРУГОЙ, зрячий и включённый сторож (`seed-knows-type-words-guard.sh`,
дважды в `ci.yml`, 2,0 с на дерево, ловит подделку). Найдены и починены
две беды разбора предела: (А) предел шагов 300 000 → 2 млрд (кратность
отставания — 5394, не 14 434, как считали раньше — предел применяется
к КАЖДОЙ СТАДИИ отдельно, не к сумме); (Б) правило «досмотрено» должно
читать стадию, а не слово. После обеих починок: **61 из 61 не
досмотрено → 17 из 61**. Остаток — 17 — не закрыт.

**Задача**: проверить, актуальны ли ещё эти 17 (дерево двигалось весь
вечер), и либо закрыть остаток, либо — раз планка Г5 это позволяет —
честно назвать СВЕЖЕЕ число «N из 61 (или из свежего счёта файлов)
недосмотрено» с происхождением, вместо повторения устаревшего.

## Г6 — что уже известно и что изменилось СЕГОДНЯ ВЕЧЕРОМ

Альфа-инвариантность (задача 9958, найдена и починена Ч372 сегодня) не
держит ни одного из 240 доказанных утверждений корпуса — уже измерено,
0 из 240. Отдельно найден код-9621: `check --proof` даёт код 0 и там, где
доказано всё, и там, где не доказано ничего — ведомость внутри честна,
код выхода слеп. Семя на момент последнего замера отставало от
исходников на 124 шага.

**Главное, что изменилось за этот вечер**: печать `pechat-dev` (семя
0.8.0) и трунк-печать Ч352 (`Ч352-kesh-na-stende/клон-ствол`, коммит
`85c8691`, дошла 3 сентября за 12ч05м) — обе печати, о которых
`road-to-1-0.md` пункт 1 говорит «две печати с одного коммита идут
прямо сейчас» — были начаты РАЗНЫМИ коммитами (проверено координатором,
родство не установлено). Значит буквальной пары «одна и та же ревизия,
две независимые печати» сегодня, возможно, НЕТ — и `Г6` своего числа от
`scripts/two-prints-identical.sh` ждать неоткуда, пока такая пара не
случится по-настоящему.

**Задача**:
1. Проверить, закончилась ли `pechat-dev`, и если да — есть ли РЕАЛЬНАЯ
   вторая печать ТОГО ЖЕ коммита где-либо в дереве (`/srv/tmp/dokazuemyy/`),
   или её нужно завести заново — только с явного слова координатора,
   печать 8+ часов не запускать самовольно.
2. Если пары нет и завести её не в бюджете этой ячейки — честно
   зафиксировать: «Г6 не может быть измерен сегодня по формальной паре,
   вот что известно косвенно» (отставание семени, альфа-инвариантность,
   код-9621) — и назвать это как есть, не подменять суррогатом.
3. Проверить код-9621 (`check --proof`, код выхода слеп) — задача 9621
   уже заведена отдельно; если она ещё открыта, эта ячейка может её
   попутно закрыть, раз читает тот же код ради Г6.

## Порог

- Прочитаны оба раздела `КРИТЕРИЙ.md` целиком, не только этот пересказ.
- Г5: свежее число «сколько недосмотрено» названо с происхождением
  (дерево, двоичный, коммит, дата) — не переписано с вечера 31 августа.
- Г6: честно установлено, есть ли сегодня формальная пара «одна ревизия,
  две печати», и если нет — названо прямо, без притворства.
- Ничего из этого не запускает новую многочасовую печать без явного
  слова координатора.
