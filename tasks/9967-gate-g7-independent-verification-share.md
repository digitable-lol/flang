---
номер: 9967
заголовок: Гейт Г7 — из 240 доказанных чекером берётся только 2 полностью, поднять число
статус: открыта
исполнитель: —
ветка: —
команда: любая
карта: Что мешает больше всего
рядом: 9964, 9965, 9966
---

# 9967 — Г7: доказанное без проверки (порог — ноль таких, сегодня «не взят и близко»)

Прочитать раздел «Г7» `КРИТЕРИЙ.md` (1419 — до «Приговор» в конце файла)
целиком первым делом. Последний замер старшего, 2 сентября, по всем 86
записям корпуса с отпечатком третьим доводом:

| величина | число |
|---|---:|
| числятся доказанными | 240 |
| на слово ядра — посылок и утверждений | 211 (87,9%) |
| сведений проиграно заново | 13 |
| строк без привязки к исходнику | 93 |
| **утверждений, проверенных ПОЛНОСТЬЮ** | **2** |

Вывод самого критерия: «Гейт не взят и близко.»

## Прямая связь с сегодняшней работой — проверить ПЕРВЫМ делом

**93 строки без привязки — это ровно те, что шли по ослабленной дороге
задачи 9612** («терм есть, номера строки нет — значит шаг либо не
сверяется ни с чем, либо сверяется вместо исходника, а не против него»).
Сегодня вечером это ЧИНИЛОСЬ ДВАЖДЫ: Ч375 в C-чекере (`sverit_term_bez_nomera`,
коммит `94393713` на `dev`) и Ч388 в чекере на flang (паритет,
влито в `dev`). Раньше 93 строки были ЦЕНОЙ отсутствия этой починки.

**Задача первым шагом — не гадать, а перемерить**: прогнать ТОТ ЖЕ
замер (`доля-корпуса.sh` или чем он сегодня зовётся, отпечаток третьим
доводом — см. задачу 9964, если она уже закрыта к этому моменту) на
СЕГОДНЯШНЕМ `dev` и сравнить с таблицей выше. Гипотеза (не факт, пока
не измерено): часть из 93 строк без привязки теперь ПОЛУЧАЕТ привязку
— сколько именно, скажет только прогон.

## Дальше — поднять долю полностью проверенных выше 2 из 240

После перемера: посмотреть, какие из «211 на слово ядра» ближе всего к
полной проверке (что именно им не хватает — сведение, терм, или
привязка) и закрыть столько, сколько получится за разумное время
ячейки. Не пытаться закрыть все 211 — честно взять понятный кусок,
назвать сколько взято.

## Порог

- Прочитан раздел «Г7» целиком, включая три поправки Ч124, разбор
  «трёх несовместимых чисел», и предупреждение «это не Г2: основание
  по-прежнему проверяет само ядро» — не путать перемер Г7 с Г2.
- Свежий прогон на сегодняшнем `dev` дал НОВОЕ число «на слово
  ядра / полностью проверено», с происхождением (дерево, двоичный,
  коммит, дата) — не переписан старый.
- Если гипотеза про 9612 подтвердилась — названо, на сколько именно
  снизилось число строк без привязки.
- Если удалось поднять число полностью проверенных выше 2 — названо
  как, и что именно проверено.
- Честно: если прогон невозможен в бюджете ячейки (например, дорогая
  сборка/печать не готова) — сказать `?`, а не заявить число на веру.
