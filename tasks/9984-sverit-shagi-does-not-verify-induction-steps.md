---
номер: 9984
заголовок: sverit_shagi сверяет текст шага с исходником, а не его математическую состоятельность — 29 мест в корпусе
статус: сделана — ложный приём закрыт на всех трёх носителях (segment/fold по словарю ядра, algebra по объявлению типа); судится 100 посылок корпуса из 100, остаток назван (тип за импортом)
исполнитель: A1, A4
ветка: a1/9984-reduction, a4/algebra-vid
команда: любая
карта: Что мешает больше всего
рядом: 9616, 9964
---

## ⚑ ПОПРАВКА Ч412 4 сентября: неверная функция, и найдена настоящая — она общая с 9616

`sverit_shagi` — не та функция. Она разбирает только АВТОРСКИЕ блоки
`теорема ... шаг N ... по предположению`; проверено на самом же примере
этой задачи (`corpus-lists.flang::«Длина»`) — там блока `теорема` нет
вовсе, `то по предположению` (строки 401, 424) — голая, безтеоремная,
закрытая ядром форма, которую `sverit_shagi` даже не видит.

**Настоящий пробел — `sverit_polya_posylok`** (сверщик.c, ~2887): для
посылки вида «вид step / закрыта reduction» (ровно эта безтеоремная
форма) чекер проверяет только `шагов == 0` — учёт, не довод.

**Поправка Ч414: «сходятся в одной функции» — неточно.** Маршруты 9616
(«по объявлению», функция «Вердикт по объявлению») и 9984 («по
предположению», функция «Случай сведённый ядром») делят только ТИП
`«Сведение»`, не путь вызова — одна не зовёт другую. Общая починка НЕ
получится даром, каждый маршрут чинится отдельно, хоть и по одной и
той же идее (прокинуть поля «Сведения» вместо того, чтобы их ронять).
9616 уже сдвинута в этом направлении Ч414 (см. её отчёт) — начинать
9984 стоит с чтения ЕЁ работы как образца приёма, не как готового кода.
Также:
`zapis.flang`, «Ходы сведённой посылки записи» печатает ходы только для
`kind=="base"` — случай шага сегодня безусловно даёт пустой список.

Design остаётся в силе (путь б ниже), но метить его — не под
«sverit_shagi:1829», а под связку `proofterm.flang`:«Случай сведённый
ядром» + `sverit_polya_posylok`. Полный отчёт: Ч412,
`/srv/tmp/dokazuemyy/Ч412-sverit-shagi/ОТЧЁТ.md` — там же черновой
дизайн проверки по первой/последней строке следа (короче полной
перереализации правил перезаписи, не проверен на настоящей записи).

# 9984 — «по предположению» проверяется текстом, не смыслом

Найдено расследованием Ч409 (4 сентября, продолжение цепочки Ч405→Ч406→Ч408
по задаче 9616), не реализовано — намеренно, см. «Порог».

## Находка

`sverit_shagi` (`flang/proof/чекер/сверщик.c:1829`) для шагов «по
предположению» (обращение к индукционной гипотезе внутри разбора по
случаям) проверяет ТОЛЬКО, что текст шага в записи совпадает с текстом
шага в исходнике (`strcmp`/`soderzhit`). Он НИКОГДА не проверяет, что сам
шаг математически состоятелен — запись может звать «по предположению» для
любого утверждения, на любом подтерме, и пройдёт, пока слова совпадают.

Это ТОТ ЖЕ КЛАСС уязвимости, что снял Г2 31 августа и что чинила Ч392
вечером 3 сентября — только в другом маршруте чекера.

**Масштаб:** 29 вхождений `по предположению` в 15+ файлах корпуса
(списки, деревья, стеки, факториал, доказательства предусловий) — это
ОСНОВНОЙ механизм закрытия структурной индукции в корпусе, не редкий
случай. Задача 9616 сама называла этот маршрут только мимоходом (33 «шага
допущением» одной ячейкой) — реальный охват органа шире, чем виделось.

## Как ядро на самом деле это доказывает

`flang/self/proofterm.flang:2899` («Индукция по разбору») и сосед
«Сведение посылки допущением» (~2960): ядро строит РОВНО ОДНУ гипотезу на
рекурсивно убывающее поле совпавшего конструктора (например, `хвост` в
`случай голова и хвост`), подставляет её в цель. Шаг, зовущий не тот
подтерм, «не совпадёт с гипотезой — сравнение синтаксическое, и вердикта
не будет» (комментарий самого ядра). Реальные шаги (`corpus-lists.flang`
«длина неотрицательна») пишут голое `то по предположению` БЕЗ видимой
формулы — ядро само выводит связь гипотезы с целью перезаписью выражений
(теми же правилами, что «неотрицательность по построению» в
`proof-kernel.flang`), а НЕ из того, что говорит запись.

## Почему не реализовано этим же вечером

Честный, независимый чекер шага потребовал бы РЕАЛЬНОГО повтора вывода на
уровне выражений (подставить механически построенную гипотезу в тело
случая, подтвердить, что она влечёт цель) — не простого повтора терма, как
у `proigrat_uzel` для других видов шагов. Два честных пути:

(а) переписать значимый срез собственных правил перезаписи ядра в чекере
на C — дорого и само по себе рискованно (чекер снова растёт, снова может
ошибиться в том же классе);

(б) вероятно более дешёвый путь — тот же архитектурный пробел, что уже
назван задачей 9616 для маршрута «по объявлению»: ядро вычисляет
подстановку/влечение по-настоящему и ВЫБРАСЫВАЕТ результат при печати
записи. Прокинуть эту же цепочку (proofterm.flang → zapis.flang →
сверщик.c) для маршрута «по предположению» позволила бы чекеру сверять
НАПЕЧАТАННУЮ цепочку синтаксически, не выводя её заново, — вероятно,
намного меньше строк C, чем (а).

## Порог

- Каждое из 29 мест доказано РЕАЛЬНЫМ прогоном на настоящей корпусной
  записи, не выводом из чтения кода.
- До того, как хоть одно место засчитано: сконструирована минимум одна
  заведомо поддельная запись (индукционный шаг, который структурно не
  тот, что заявлен), и подтверждено, что новый довод её отвергает — та же
  дисциплина, что Ч392 применила для комментарий-вместо-текста.
- Полный набор проб чекера остаётся зелёным (228+ на сегодня, проверить
  текущее число) без единой регрессии.
- Честно: НОЛЬ мест за один присест — приемлемый исход, если дизайн при
  этом доведён до состояния «следующая ячейка может реализовать, не
  передумывая заново» (эта задача уже там, но реализация — нет).

## ⚑ ПОПРАВКА Ч415 4 сентября: сама подпорная находка не воспроизвелась

Прежде чем писать код, Ч415 прогнала `check --proof --json` на 6 живых
файлах, включая ИМЕННО `corpus-lists.flang::«Длина»` — пример, на
который опирались и Ч409, и Ч412. Итог: КАЖДЫЙ шаг «по предположению»
во всех шести вышел `"by":"term"` (уже проверяется независимо путём
term-сравнения), НИ ОДНОГО `"by":"reduction"` (безтеоремная форма,
которую и надо было чинить) не нашлось. `tree.flang`/`hashmap.flang`
(остальные два файла с этим текстом) не досчитались за 200 млн шагов —
не проверены, не дёшевы.

Это НЕ опровергает счёт «29 мест» (тот был текстовым grep, не счётом по
`proof --json`) — значит, либо большинство из 29 уже авторские теоремы
(term-маршрут), либо безтеоремные места живут в непроверенных файлах.
**Прежде чем писать код по этой задаче — сперва найти РЕАЛЬНЫЙ пример
`"by":"reduction"` живым прогоном.** Строить дизайн под непроверенный
пример — та же ошибка, что уже раз поймала Ч405 (задача 9616).

## Не сделано

Реализация (ни путь (а), ни путь (б)) не начата. Выбор между путями (а) и
(б) не сделан координатором — по прикидке Ч409 путь (б) дешевле, но не
проверен. Сам пример, на который опирался весь дизайн, не подтверждён —
см. поправку Ч415 выше; следующий шаг — найти настоящий, не чинить.

## ⚑ ПРИМЕР НАЙДЕН 4 сентября: 7 настоящих `kind:"step"`+`by:"reduction"` в flang/stdlib/tree.flang

Полный обход, не догадка. `check --proof --json` прогнан на всех 16
файлах `flang/proof/examples/*.flang`, чей исходник содержит «по
предположению» (список см. `grep -l 'по предположению'
flang/proof/examples/`, ровно те 15+, что называет находка задачи), плюс
два файла, которые Ч415 не досчитала за 200 млн шагов —
`flang/stdlib/tree.flang` (657 строк) и `flang/stdlib/hashmap.flang`
(1016 строк). Для последних двух хватило бюджета `--предел-шагов
3000000000` (3 млрд) — оба досчитались за секунды, 200 млн оказалось
малым бюджетом, не признаком дороговизны файла.

**Свод `(kind, by)` по всем 18 файлам:**

| kind | by | штук |
|---|---|---:|
| step | term | 19 |
| постусловие | declaration | 71 |
| base | term | 16 |
| base | reduction | 15 |
| **step** | **reduction** | **7** |

Все 7 — в `flang/stdlib/tree.flang`, ноль в `hashmap.flang` и ноль в
остальных 16 файлах `flang/proof/examples`. Ч415 не нашла ни одного,
потому что искала среди `flang/proof/examples` (где их и правда нет) и
не досчитала единственные два файла, где они есть.

**Семь настоящих мест**, все `variant:"Узел"` (структурный случай-шаг
дерева), все `"verdict":"доказано"`, все `"steps":0` — то есть именно
та форма, которую, по находке Ч412, `sverit_polya_posylok` сегодня
принимает по одному учёту «шагов == 0», а не доводом:

| obligations[N] | теорема/обязательство |
|---:|---|
| 21 | узел без левого ребёнка не меняет приоритета корня |
| 28 | узел без правого ребёнка не меняет приоритета корня |
| 43 | размер неотрицателен |
| 44 | дерево с ненулевым приоритетом корня — не пустое |
| 48 | есть ключи — глубина не меньше одного |
| 49 | глубина неотрицательна |
| 52 | у узла глубина — глубже обоих поддеревьев |

**Как повторить, дёшево (секунды, не часы):**

```
make -C bootstrap
./bootstrap/flang check flang/stdlib/tree.flang --proof --json \
  --предел-шагов 3000000000 > tree.json
python3 -c '
import json
d = json.load(open("tree.json", encoding="utf-8"))
for i, ob in enumerate(d["obligations"]):
    s = json.dumps(ob)
    if "\"kind\": \"step\"" in s and "\"by\": \"reduction\"" in s:
        print(i, ob.get("name") or ob.get("says"))
'
```

**Чего это НЕ делает.** Не проба порчи (её всё ещё нет — «Порог» задачи
требует заведомо поддельную запись на этом же примере ДО того, как
место засчитано) и не реализация починки (пути (а)/(б) выше). Только
находка: РЕАЛЬНЫЙ, живой, не выдуманный пример существует, назван
поимённо, воспроизводится за секунды. Следующая ячейка может сразу
строить пробу порчи на `obligations[49]` («глубина неотрицательна» —
самый простой из семи по структуре цели) вместо повторного поиска.

## ⚑ ПРОБА ПОРЧИ 4 сентября: дыра подтверждена, и найдена её граница

Дисциплина «проба порчи ДО того, как считать дырой» (та же, что применена
дважды на Г2 сегодня). Метод дешевле, чем прогонять `flang/stdlib/tree.flang`
через ядро заново: `sverit_polya_posylok` (`сверщик.c:2889-2909`) для
`закрыта reduction` смотрит РОВНО ОДНО поле — `шагов == 0` — и делает это
одинаково для ЛЮБОЙ посылки с этой формой, независимо от файла. Взят уже
существующий в дереве честный образец с той же формой
(`flang/proof/чекер/пробы/записи/корпус/substantive.запись`, утверждение
«С5», посылка «голова и хвост» `закрыта reduction шагов 0`) — не сам
`tree.flang`, но тот же код чекера, та же проверка, тот же класс.

### Проба 1 — принята: реальное имя, ложный вердикт, поддельная посылка

Утверждение «С1» честно стоит в записи как `вердикт нет вердикта`
(ядро НЕ доказало квантор «все элементы результата неотрицательны» —
у ядра просто нет вида цели для кванторов, см. комментарий в исходнике
`flang/proof/map/substantive.flang:9`). Подделка меняет ТОЛЬКО блок
верdikта на честную копию структуры С5 (вердикт `доказано` + `принцип`/
`сведение` + посылка `закрыта reduction шагов 0`), имя и строку не трогает:

```
$ ./сверщик flang/proof/map/substantive.flang проба-порчи/1-c1-lozhnyy-reduction.запись
НЕ ПРОВЕРЕНО ...: Числятся доказанными 4    (было 3 на честной записи)
код=3
```

**Ни слова жалобы про С1.** Подделка молча зачлась в «доказанными 4» —
чекер принял «доказано» утверждение, которое САМО ЯДРО пометило «нет
вердикта», без единого несовпадения. Целый прогон кода не меняется (3,
как на честной записи) ТОЛЬКО потому, что в записи и без того остаются
другие непроверенные утверждения — сам факт принятия С1 ничем не
подсвечен.

**Честная оговорка о степени лжи.** «Все элементы результата
неотрицательны» для «Единицы» (отображает список в список единиц) —
утверждение, скорее всего, ИСТИННОЕ по факту, просто вне вида целей,
которые умеет ядро. Проба доказывает МЕХАНИЗМ (чекер не отличает
«ядро доказало» от «ядро не бралось, а кто-то сказал, что доказало»),
а не подсовывает содержательно ложный факт — таких же честных
постусловий, ложных по факту, в открытом корпусе взять неоткуда (ядро
никогда не печатает заведомо ложное как «доказано»).

### Проба 2 — отклонена: граница дыры

Та же подделка, но с переименованием утверждения в заведомо ложное
(«длина результата равна минус единице»):

```
$ ./сверщик flang/proof/map/substantive.flang проба-порчи/2-imya-ne-sovpadaet.запись
НЕ СОШЛОСЬ: строка 41 исходника не несёт «обеспечивает «С2-ЛОЖЬ: ...»» —
записанное утверждение в исходнике не стоит
код=1
```

Имя утверждения СВЕРЯЕТСЯ с текстом `обеспечивает «...»` по указанной
строке источника — придумать целиком новую ложь нельзя, чекер это ловит.

### Вывод — дыра есть, и она именно там, где предсказывала находка Ч412

**Граница дыры именно такая:** подделыватель не может изобрести
утверждение из ничего (проба 2 это ловит), но МОЖЕТ взять любое честное,
реально написанное в исходнике постусловие, которое ядро НЕ доказало
(«нет вердикта» — таких 119 из 359 по замеру Ч318), и объявить его
доказанным через сфабрикованную посылку `закрыта reduction шагов 0`.
Чекер это примет молча, потому что для этой формы посылки у него ВООБЩЕ
НЕТ проверки, кроме счёта шагов, — не потому что проверка слабая, а
потому что для `reduction` авторских «шаг N» строк не бывает по
конструкции (`шагов 0` и означает «ходов не записано»), и `sverit_shagi`
здесь нечего разбирать.

**Не сделано.** Починка (пути (а)/(б) выше в этой же задаче) — не
начата, по прежнему решению координатора. Проба НЕ пыталась найти
честное постусловие, ложное по факту (см. оговорку выше) — если такое
найдётся в корпусе, проба на нём была бы строго убедительнее.

**Файлы пробы** (не в git, клон `/srv/tmp/dokazuemyy/Ч9984b-proba-porchi/
проба-порчи/`): `1-c1-lozhnyy-reduction.запись` (принята), `2-imya-ne-
sovpadaet.запись` (отклонена) — оба воспроизводятся секундами, чекер
собирается отдельно (`make -C flang/proof/чекер`, не требует
`bootstrap/flang`).

## ⚑ ПОЧИНЕНО 5 сентября 2026 (A1): найден и закрыт НАСТОЯЩИЙ ложный приём — но не тот, что искали

Провенанс: дерево `cdf3d405` (ветка `a1/9984-reduction`), чекер собран
`make -C flang/proof/чекер` (cc -std=c99 -Wall -Wextra -Werror -pedantic -O2),
двоичный ядра `bootstrap/flang` взят из семени дерева.

### Поправка к постановке: «Числятся доказанными» — счётчик ЗАЯВЛЕННОГО

`сверщик.c:3184` — `if (strcmp(verdikt, "доказано") == 0) s->dokazannyh++;`.
То есть «Числятся доказанными» считает, сколько утверждений ЗАЯВЛЕНО
доказанными, а не сколько чекер проверил. Слово «числятся» это и говорит.

Отсюда прежний вывод пробы 9984 («чекер принял молча, 3→4») прочитан
неточно. Прогон на той же паре файлов:

| | честная | подделка С1 |
|---|---:|---:|
| голова | НЕ ПРОВЕРЕНО | НЕ ПРОВЕРЕНО |
| Числятся доказанными | 3 | **4** |
| **НА СЛОВО ЯДРА** | 6 | **9** |
| Узлов «по случаям» проиграно | 0 | 0 |
| …вне приёма сверщика | 2 | **3** |

Подделка не попала ни в одну корзину «проверено»: она ушла в «на слово
ядра» (+3 = само утверждение и две выдуманные посылки) и вдобавок
засчиталась как «вне приёма сверщика». **Сертификата подделка не
получила.** На этой паре файлов ложного приёма нет.

### Настоящая дыра — там, где узел ПРОИГРАН, и его посылки прощены

`сверит_покрытие` (`сверщик.c:2745`): `if (!proigran) s->na_slovo += posylki_na_slovo(svoi);`
— когда узел вердикта проигран заново, его посылки перестают считаться
на слово ядра. Значит запись С проигранным узлом МОЖЕТ быть
сертифицирована, неся при этом посылки `закрыта reduction шагов 0`.
Такая в корпусе есть: **`if-over-a-segment`** даёт
`ПРОВЕРЕНО — запись сошлась с исходником, и всё доказанное проиграно заново`,
код 0.

И вот там слабость ветки `reduction` стоит дорого. Проба порчи на этой
самой сертифицированной записи — поменять местами `вид base` и `вид step`
у посылок «дно» и «спуск» (ложь о строении индукции: база объявлена шагом,
шаг базой):

```
СТАРЫЙ чекер:  код 0   ПРОВЕРЕНО — … и всё доказанное проиграно заново
НОВЫЙ чекер:   код 1   НЕ СОШЛОСЬ: теорема «сумма до неотрицательна»,
                       посылка «дно»: объявлена «вид step», а имя ядра
                       «дно» — это всегда «вид base»
```

**Это ложный приём в чистом виде: сертификат, выданный на ложь.** Он виден
и по коду возврата (0→1), в отличие от исходной пробы 9984, которая по
коду неразличима (обе дают 3) — на это отдельно указал работник A2,
заведший помощник `dolg` для сверки по числу, а не по коду (задача 9992).

### Починка и почему именно такая

Проверять сведение ПО СУЩЕСТВУ нельзя: это второй вычислитель внутри
чекера, и КРИТЕРИЙ.md (раздел Г3) называет это границей — после такого
чекер станет размером с ядро и гейт потеряет смысл. Поэтому проверяется
не сведение, а **согласие записи с собственным словарём ядра**.

Замер по всем 86 записям корпуса: каждое имя посылки несёт РОВНО ОДИН
вид, ни одного исключения ни в одну сторону —
«пусто» base 15/15, «голова и хвост» step 15/15, «дно» base 6/6,
«спуск» step 6/6, «начало свёртки» base 4/4, «шаг свёртки» step 4/4.

Имена «дно»/«спуск»/«начало свёртки»/«шаг свёртки» пишет САМО ЯДРО, а не
автор: `flang/self/proofterm.flang:2805` — «у принципа по отрезку ровно
одна — „дно“». Значит их вид — свойство принципа, а не объявление записи,
и запись, назвавшая «дно» шагом, лжёт. Новая функция
`vid_po_imeni_posylki` возвращает обязательный вид для этих четырёх имён
и NULL для всех прочих; `sverit_polya_posylok` краснеет на расхождении.

**Носитель `algebra` намеренно НЕ входит:** там имена посылок — это имена
вариантов типа, выбранные автором («Узел», «Лист», «Звено»), и вид
определяется не именем, а рекурсивностью варианта в ОБЪЯВЛЕНИИ ТИПА.
Это проверяется по исходнику и здесь не сделано. Оговорка работника A2,
принятая к сведению: на `algebra` подделку второго маршрута ловят два
независимых довода (один читает варианты из исходника), но полагаться на
это как на часть защиты нельзя — покрытие есть не везде.

### Числа до и после, всё прогоном

| замер | до | после |
|---|---|---|
| честный корпус, 86 записей (код + «числятся») | — | **ноль расхождений** со старым чекером |
| `if-over-a-segment`, честная | код 0 ПРОВЕРЕНО | код 0 ПРОВЕРЕНО |
| `if-over-a-segment`, порча вида | **код 0 ПРОВЕРЕНО** | **код 1 НЕ СОШЛОСЬ** |
| Г4 по утверждениям | 18/240 = 7,50 % | **18/240 = 7,50 %** |
| Г4 по записям | 22/86 = 25,58 % | **22/86 = 25,58 %** |
| `сверщик.c`, строк | 3368 | 3397 (+29) |
| КОД по мерке Ч4 | — | 2336 |
| ядро решений (4 файла) | — | 4688 |
| отношение ядро/чекер | — | **2,0×** (порог обещания 1,34×) |

**Г4 не упала.** Ожидание координатора («падение ожидаемо») не
подтвердилось, и причина понятна: правка не переводит закрытое сведением
в «на слово ядра» — она ловит РАСХОЖДЕНИЕ записи со словарём ядра, а на
честных записях расхождений нет. Рост +29 строк куплен не долей корпуса,
а закрытым ложным приёмом — другая валюта, чем та, для которой писалось
правило 4 сентября, и это стоит сказать прямо.

### Что НЕ сделано

* `flang/proof/подделки/прогон.sh` (набор 228 проб на flang) запущен, но
  за отведённое время НЕ ДОСЧИТАН — прогон идёт больше десяти минут.
  Регрессия проверена иначе и сильнее для этой правки: A/B старого и
  нового чекера по всем 86 записям корпуса дал ноль расхождений.
* Носитель `algebra`: вид посылки по рекурсивности варианта из исходника —
  не проверяется. Это и есть остаток дыры, названный числом:
  из 20 различных имён посылок корпуса под новую проверку попадают 4.
* Прочие поля посылки (`вердикт`, `правило`) на согласие со словарём ядра
  не проверяются — здесь закрыт только `вид`.

## ⚑ ОСТАТОК ЗАКРЫТ 5 сентября (A4): носитель `algebra` судится по объявлению типа

Остаток, названный выше числом («под проверку попадают 4»), закрыт: теперь
вид посылки судится и на носителе `algebra`, но знание берётся не из
словаря ядра, а из **объявления типа в исходнике**.

### Правило, и почему оно не догадка

Индукция по алгебраическому типу спускается ровно по тем вариантам, что
содержат поле своего же типа. Значит вариант с таким полем — рекурсивный,
и посылка при нём обязана быть `step`; вариант без полей своего типа
дальше не ведёт — `base`. Вид тут так же не свободен, как «дно» и «спуск»,
просто свойство читается из другого места.

**Правило проверено на всём корпусе ДО того, как писался код**: по 86
записям снято 80 посылок на этом носителе, правило совпало с записью
**80 раз из 80, разошлось 0, объявления не нашлось 0**.

Две тонкости, обе найдены замером, а не рассуждением:

* **варианты пишутся двояко** — `вариант «Лист»` и голое `вариант Лист`
  (39 против 2 по `flang/proof/examples/*.flang`). Первый разбор видел
  только форму с ёлочками и терял `corpus-depth.запись` целиком;
* **рекурсия ищется в хвосте после `содержит `**, а не по всей строке
  варианта: у типа, чей вариант назван именем самого типа, имя в
  заголовке иначе сошло бы за поле и сделало базу шагом.

Встроенные `список` и `строка` объявления не имеют вовсе — для них
словарь: «пусто» → `base`, «голова и хвост` → `step`. Довод тот же, что
у соседней `varianty_tipa`: это суммы самого языка, и читать их чекеру
позволено.

### Проба порчи — и она ВИДНА ПО КОДУ

`пробы/записи/порча/corpus-tree-vid-perevernut.запись`: у честной
`corpus-tree.запись` переставлены местами `вид base` и `вид step` у
посылок «Лист» и «Узел», больше не тронуто ничего (diff — ровно две
строки).

```
СТАРЫЙ чекер (gh/dev 2678b783):  честная код 3  ·  подделка код 3   ← проходит молча
НОВЫЙ чекер:                     честная код 3  ·  подделка код 1
  НЕ СОШЛОСЬ: посылка «Лист»: объявлена «вид step», а вариант «Лист» типа
  «Дерево» — без поля своего типа, значит «вид base»; посылка «Узел»:
  объявлена «вид base», а вариант «Узел» типа «Дерево» — рекурсивный,
  значит «вид step»
```

Учтено предупреждение A2 (задача 9992) о слепоте набора к «тихому
зачёту»: **эта порча меняет код возврата** (3 → 1), поэтому идёт в
обычный набор проб, а не в ведомость долга.

### Попутно: обе пробы вида в наборе не гонялись

Проба предыдущей половины (`if-over-a-segment-vid-perevernut.запись`,
коммит `2678b783`) лежала в дереве, но `прогон.sh` её не звал — то есть
только что закрытая дыра ничем не охранялась. Обе пробы вписаны в набор
отдельным разделом, с доводом, почему нужны обе: их судят РАЗНЫЕ приёмы
(словарь ядра против объявления типа), и отвалившийся приём покажет
ровно та проба, что осталась красной.

### Числа, все прогоном на дереве `2678b783`

| замер | до | после |
|---|---|---|
| посылок корпуса, чей вид судится | 20 из 100 | **100 из 100** |
| различных имён посылок под проверкой | 4 из 32 | **32 из 32** |
| A/B по 86 честным записям, старый↔новый | — | **ноль расхождений** |
| порча вида на `algebra` | код 3, проходит | **код 1, обе беды названы** |
| набор проб: подделок / отвергнуто | 125 / 95 | **127 / 97** |
| подделок принято кодом 0 | 0 | **0** |
| честных отвергнуто | 0 | **0** |
| `сверщик.c` строк | 3397 | 3441 (+44) |
| отношение ядро/чекер (мерка Ч4) | 2,0× | **1,99×** (пол 1,34×) |

Расхождение с прежним счётом стоит назвать: здесь различных имён посылок
**32**, а предыдущая половина писала «20». Считано по всем 86 записям
корпуса разбором строк `посылка «имя» … носитель X`; носителей ровно три
— `algebra` (28 различных имён), `fold` (2), `segment` (2).

### Что НЕ сделано

* **Тип, объявленный в другом файле, не судится.** Если `тип «X»` лежит
  за импортом, `stroka_varianta_tipa` его не находит, и проверка молчит
  (возвращает NULL). В сегодняшнем корпусе таких нет — «объявления не
  нашлось 0», — но правило это не покрывает, и запись из такого файла
  можно переставить безнаказанно.
* Прочие поля посылки (`вердикт`, `правило`) по-прежнему не сверяются со
  словарём ядра — закрыт только `вид`.
* `flang/proof/подделки/прогон.sh` (набор на flang) снова не гонялся: он
  идёт больше десяти минут. Регрессия проверена A/B по всему корпусу.
