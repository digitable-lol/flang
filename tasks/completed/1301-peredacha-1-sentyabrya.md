---
номер: 1301
заголовок: Передача работы 1 сентября: блокер десяти заплат снят, что делать дальше
статус: сделана
исполнитель: a
ветка: —
команда: первая
карта: Цена самосборки
рядом: 0311, 1174, 1300, 1302, 1303, 1304, 1305, 1306, 1307
---

# 1301. Что знать тому, кто подхватывает

Файл написан, чтобы работу можно было продолжить, не читая переписку. Всё, что
ниже, снято командами на стволе `ef718648`, а не по памяти.

⚠ **ЭТО СНИМОК ВЕЧЕРА 1 СЕНТЯБРЯ, И ЧАСТЬ ЕГО УЖЕ УСТАРЕЛА.** За ту же ночь
ствол ушёл с `ef718648` на `af791d85`. Что здесь больше не так — списком, чтобы
не читать весь файл:

| сказано здесь | как сегодня |
|---|---|
| «второе столкновение того же вида — ещё не снято» («Связка постусловия») | снято: имя объявлено только в `flang/self/proofterm.flang`, в `totality.flang` его нет (`grep -c` → 0) |
| «перестановка проверки: сделана, НЕ проверена» | влита коммитом `27789197` и проверена НАСТОЯЩИМ столкновением: третью пару нашла она сама (`cd5a9f26`) |
| «столкновений того же рода два» | их оказалось ТРИ: `de3fef84`, `ef718648`, `cd5a9f26` |
| «за день влито 12 коммитов; ствол `de7067fa`» | ствол `af791d85`; опись невлитого пересобрана коммитом `f8af3a54` — 63 ветки |
| «105 списков руками, 57 разошлись» | не замер, а сумма двух переписей руками; прибор даёт 223 и 62 |
| «показатель Ч107: обязательства в степени 2,5–3» | показателя нет: местный гуляет от 0,07 до 9,51 |
| «половина чекера не возвращалась в ствол» | возвращена: `сверщик.c` 1566 → 2412 строк (`89baa02f`, `fa017ef2`, `5c7a9196`) |

Опись невлитого ниже — та, что снята вечером 1 сентября. Свежая, на 63 ветки, —
коммит `f8af3a54`.

## Главное: блокер, державший десять заплат ядра, СНЯТ

31 августа в дереве было записано, что печать ходов вывода (derivation trace)
влить нельзя: все **десять** заплат ядра (Ч48, Ч55, Ч56, Ч70, Ч71, Ч75, Ч76,
Ч86, Ч87, Ч91) несут `использует «SHA-256»`, это втягивает
`flang/stdlib/sha256.flang` в замыкание компилятора, а там тип **«Сбор слов»**
уже объявлен в `flang/self/emit-c.flang:454` → `FLANG_DUPLICATE_NAME`.

Имя переименовано в «Сбор слов C», коммит `ef718648`. Проверка сегодня:

    LC_ALL=C.UTF-8 /usr/bin/grep -c 'тип «Сбор слов»' \
      flang/self/emit-c.flang flang/stdlib/sha256.flang
    # emit-c.flang:0   sha256.flang:1

Столкновения больше нет. **Десять заплат надо приложить заново** — раньше они
падали только из-за него. Заплаты лежат в
`/srv/tmp/dokazuemyy/Ч<номер>-<имя>/правка-clon.diff`, опись причин — в
`/srv/tmp/dokazuemyy/Ч123-vlivanie/НЕВЛИТО.tsv` (34 строки, из них 11 про это
столкновение).

Почему это важно: среди десяти лежит линия **Ч56 → Ч71 → Ч87** — печать ходов
вывода. Без неё независимый checker на настоящих записях не подтверждает
самостоятельно **ни одного** утверждения, и гейты Г1, Г2, Г4 стоят.

## Второе столкновение того же вида — ещё не снято

Заплата `C5-totalnost/правка-stvol.diff` (+188/−10) не идёт: функция
**«Связка постусловия»** объявлена и в `flang/self/totality.flang:1974`, и в
`flang/self/proofterm.flang:1292`. Лечение то же, что у «Сбор слов»:
переименовать то из двух, что живёт в более узком месте.

Записано также, что имён, объявленных сразу в `flang/self` и `flang/stdlib`,
**двенадцать**. Число снято 31 августа и на сегодняшнем стволе не перепроверено —
каждое такое имя это будущая поломка на десятом часу пересборки.

## Перестановка проверки: сделана, НЕ проверена

Ветка `a/imya-modulya-cpp` в `/srv/tmp/dokazuemyy/pochinka-imyon`, коммит
`9a11e66f`, +39 строк в двух файлах:

* `flang/self/emit-c.flang` — обёртка `функция «Столкновения имён»`, зовущая
  **существующее** правило `«Проверить столкновения»` (`emit-c.flang:5358`) по
  одной программе и без печати. Второго воплощения правила нет намеренно: иначе
  проверка и компилятор разойдутся и проверка начнёт врать.
* `flang/src/emit/c/flang_repl.c` — вызов между `repl_call("Связать исходники")`
  (~3889) и судом ядра (~4004). `val_text` (:1734) + копия в буфер 512 байт
  (строки рантайма не кончаются нулём) + `bads_say` (:2214).

Собрано: `make -C bootstrap -j8` код 0, чисто под
`-Wall -Wextra -Werror -pedantic -O2 -flto`. Регрессии нет:
`./bootstrap/flang check examples/import-check.flang` код 0.

**Чего не хватает.** Обёртка живёт в исходнике, а двоичный собран из
закоммиченного `bootstrap/compiler_flang.c`, где её ещё нет — значит
`repl_call("Столкновения имён", …)` функции не найдёт. В коде стоит защита по
коду возврата `repl_call`, но она не проверена прогоном. Пока это не проверено,
вливать нельзя.

## ⛔ ГЛАВНЫЙ ЗАПРЕТ (замер Ч145, 1 сентября)

**Перепечатывать семя, пока половина чекера не в стволе, НЕЛЬЗЯ.** Ствольный
`flang/proof/чекер/сверщик.c` — 1566 строк и один коммит за всю историю; ячейки
правили свою копию (1477 → 2265), и она не возвращалась. Разрыв 1158 строк.

Пока правка лежит в `flang/self/**`, она до двоичного не доезжает и ничего не
ломает. После перепечатки ствольный чекер даёт на ЧЕСТНОЙ записи код 1.

Порядок: печать → половина чекера (≈ +499 строк) → перепечатка. Подробности и
ступенчатый замер — задача 1311.

## Порядок работ, если продолжать с нуля

1. Приложить десять заплат заново (блокера нет).
2. Снять второе столкновение — «Связка постусловия», и перемерить, сколько
   двойных имён в дереве осталось.
3. Проверить перестановку на нарочно внесённом столкновении, оба конца:
   внёс — краснеет, убрал — зеленеет.
4. Приложить половину чекера (Ч56+Ч71+Ч76+Ч87) — **без неё перепечатка ломает
   дерево**, см. запрет выше и задачу 1311.
5. Только после 1–4 запускать пересборку.

**Поправка к пункту про контрольные точки.** Записанное раньше «сначала точки,
иначе следующий отказ снова стоит десять часов» — НЕВЕРНО, и это замерено
(Ч152, задача 1304). Точка за прогон одна, самый длинный кусок между точками —
вся печать, 8 ч 34 мин, 99,6 % прогона; настоящая пересборка, оборванная на
10-й минуте, оставила в точке 0 файлов и 0 байт. Точки закрывают СОСЕДНИЙ
отказ — «упало после печати», который сносил напечатанное `rm -rf`. Он тоже
настоящий и тоже стоил 8,5 часа, но это другая беда.

## Ячейки, запущенные 1 сентября

Каждая пишет `ОТЧЁТ.md` в свой каталог `/srv/tmp/dokazuemyy/Ч<номер>-<имя>/`,
разделом «не сделано» — что осталось. Ни одна не пушит: вливание решает старший.

| ячейка | работа |
|---|---|
| Ч143 | приложить Ч56 заново (основание линии ходов вывода) |
| Ч144 | приложить Ч71 — саму печать ходов вывода, поверх Ч56 |
| Ч145 | приложить Ч87 — вершину линии; проверить, что половины checker'а нет в git |
| Ч146 | приложить Ч70 и Ч91 — две отдельные линии ядра |
| Ч147 | приложить Ч48 и Ч55; проверить, не одна ли это правка в двух копиях |
| Ч148 | сличить Ч75/Ч76/Ч86 с Ч71 побайтово, приложить только остатки |
| Ч149 | снять столкновение «Связка постусловия», пересчитать двойные имена |
| Ч150 | замер выноса восьми эмиттеров (см. ниже) |
| Ч151 | проверить перестановку на нарочном столкновении |
| Ч152 | контрольные точки Ч134 |
| Ч153 | линтер, задача 0311 |
| Ч154 | одна линейка ходов вывода вместо трёх счётов, задача 1303 |
| Ч155 | перемерить Г3, Г4, Г7 на сегодняшнем стволе |
| Ч156 | разобрать снимки Ч136, Ч137, Ч138, Ч140 |
| Ч157 | сводка выпуска в docs + README, задача 1305 |
| Ч158 | списки, написанные руками, задача 1306 |

## Отдельный вопрос владельца: вынести эмиттеры в библиотеки

Измерено на `ef718648`:

    for f in flang/self/emit-*.flang; do echo "$(wc -l < $f) $(basename $f)"; done

| | строк |
|---|---:|
| всё в `flang/self` | 123 350 |
| девять `emit-*.flang` | 36 112 (29 %) |
| `emit-c.flang` — уйти не может, им печатается семя | 5 828 |
| восемь остальных | 30 284 |

Разбор по файлам: python 5311, csharp 5265, elixir 4660, js 4603, java 4373,
rust 3113, go 2637, cpp 322.

Чего этот замер **не** решает: цена суда ядра растёт не от строк, а от
обязательств — но КАК именно, неизвестно. Показатель Ч107 «обязательства в
степени 2,5–3» 1 сентября отменён: местный показатель гуляет от **0,07 до 9,51**,
то есть цена зависит от содержания условий, а не от их числа. Выигрыш по времени
снимается только прогоном на урезанном замыкании (задача 1309). Прошлая
пересборка: всего 9 ч 56 мин, суд ядра 7 ч 33 мин.

Чего вынос **не** даст: доверенная база эмиттеров и так не содержит, кроме
`emit-c`. Она — parser, лексер, типы, завершаемость, связывание, вычислитель,
ядро доказательств: 26 234 строки flang, из них `parser.flang` 5 287 = 20,2 %.


## ⛔ ОПИСЬ НЕВЛИТОГО, вторая волна (предел сессии, работа цела)

Все агенты, включая обоих ведущих, остановились разом на пределе сессии — не на
ошибке. **Работа НЕ потеряна: 62 отчёта в `/srv/tmp/dokazuemyy/*/ОТЧЁТ.md`,
63 ветки закоммичены в клонах.** Клон: `<ячейка>/клон` (у части — `clon`).

За день влито 12 коммитов; ствол `de7067fa`. Опись ниже — то, что ещё НЕ влито.

| ячейка | ветка | коммитов | размер |
|---|---|---:|---|
| `Ч123-vlivanie` | `a/svod-yacheek-31avg` | 17 | 27ф, 27+, 1726− |
| `Ч129-proza-do-konca` | `main` | 2 | 33ф, 812+, 132− |
| `Ч135-sbor-slov` | `main` | 2 | 5ф, 51+, 12− |
| `Ч136-leksem-kommentarii` | `main` | 2 | 1ф, 118+ |
| `Ч137-changelog-na-flang` | `HEAD` | 1 | 1702ф, 25197+, 197768− |
| `Ч139-nahodki-v-dokumenty` | `a/nahodki-31avg-v-chto-meshaet` | 1 | 1ф, 497+, 6− |
| `Ч141-taski` | `main` | 3 | 2ф, 149+ |
| `Ч143-termy-zanovo` | `a/термы-заново` | 1 | 1ф, 248+, 23− |
| `Ч144-shagi-zanovo` | `a/шаги-вывода-заново` | 2 | 1ф, 371+, 39− |
| `Ч145-platit-zanovo` | `a/платить-в-печати-заново` | 2 | 3ф, 590+, 52− |
| `Ч146-dve-linii-yadra` | `a/след-ядра-заново` | 1 | 2ф, 414+, 38− |
| `Ч147-otpechatok-i-privyazka` | `a/отпечаток-и-привязка` | 1 | 1ф, 36+, 6− |
| `Ч148-troyka-ravnyh` | `a/тройка-остатки` | 1 | 4ф, 156+ |
| `Ч152-kontrolnye-tochki` | `a/контрольные-точки` | 1 | 1ф, 205+, 40− |
| `Ч154-tri-schyota` | `a/линейка-ходов` | 1 | 7ф, 521+, 43− |
| `Ч158-spiski-rukami` | `a/списки-руками` | 7 | 25ф, 2044+, 193− |
| `Ч159-vernut-cheker` | `a/вернуть-чекер` | 1 | 12ф, 486+, 59− |
| `Ч160-semya-vosproizvodimo` | `a/семя-воспроизводимо` | 1 | 2ф, 68+, 30− |
| `Ч161-krugooborot` | `a/кругооборот` | 1 | 4ф, 387+, 2− |
| `Ч162-bez-proverki` | `a/без-проверки` | 1 | 1ф, 375+ |
| `Ч164-pokazatel-tseny` | `a/показатель-цены` | 1 | 7ф, 745+ |
| `Ч165-krasnye-storozha` | `a/красные-сторожа` | 1 | 14ф, 197+, 16− |
| `Ч166-polovina-chekera` | `a/половина-чекера` | 3 | 49ф, 3418+, 54− |
| `Ч168-korpus-86` | `a/корпус-86` | 1 | 1ф, 393+ |
| `Ч169-pochinka-storozha` | `a/починка-сторожа` | 15 | 98ф, 6921+, 244− |
| `Ч171-svesti-godnoe` | `a/свести-годное` | 13 | 94ф, 6684+, 226− |
| `Ч175-proverki-bez-dvoichnogo` | `a/проверки-без-двоичного` | 16 | 102ф, 7023+, 248− |
| `Ч176-vypusk-0-7-0` | `a/выпуск-0-7-0` | 1 | 2ф, 348+ |
| `Ч201-след-ядра-в-записи` | `a/след-ядра-в-записи` | 2 | 2ф, 298+, 15− |
| `Ч202-ведомость-отпечатков` | `a/ведомость-отпечатков` | 2 | 4ф, 436+, 5− |
| `Ч203-приборы-в-дерево` | `a/приборы-в-дерево` | 3 | 3ф, 808+, 5− |
| `Ч204-что-мешает-86` | `a/что-мешает-корпусу` | 1 | 2ф, 379+ |
| `Ч205-слова-условия` | `a/слова-условия` | 7 | 2ф, 586+ |
| `Ч206-связка-постусловия` | `a/связка-постусловия` | 2 | 2ф, 188+, 10− |
| `Ч207-вторая-линия-чекера` | `a/вторая-линия-чекера` | 4 | 49ф, 3796+, 57− |
| `Ч209-атака-на-проигрыватель` | `проверка` | 2 | 2ф, 298+, 15− |
| `Ч210-свести-долю-и-ведомость` | `a/доля-с-ведомостью` | 6 | 6ф, 734+, 20− |
| `Ч211-пробы-на-след` | `a/пробы-на-след` | 5 | 123ф, 7951+, 59− |
| `Ч212-доказанное-вычислением` | `a/доказанное-вычислением` | 13 | 79ф, 5811+, 74− |
| `Ч213-путь-остаётся-бедой` | `a/путь-остаётся-бедой` | 7 | 6ф, 798+, 24− |
| `Ч214-читать-как-язык` | `a/читать-как-язык` | 9 | 122ф, 1638+, 6896− |
| `Ч215-хвост-пример-строка` | `a/хвост-пример-строка` | 1 | 3ф, 134+, 1− |
| `Ч217-номер-версии-записи` | `a/номер-версии-записи` | 2 | 12ф, 471+, 12− |
| `Ч218-переставить-линию-чекера` | `a/линия-чекера-на-новом-стволе` | 2 | 76ф, 4553+, 25− |
| `Ч219-приборы-на-новом-стволе` | `a/приборы-на-новом-стволе` | 9 | 9ф, 1685+, 10− |
| `Ч22-slepye` | `m/kernel-lines-declared-and-reasoning` | 24 | 33ф, 1368+, 193− |
| `Ч220-потолок-чекера` | `a/потолок-чекера` | 1 | 7ф, 261+, 131− |
| `Ч223-свести-две-линии` | `a/две-линии-сведены` | 12 | 106ф, 5808+, 292− |
| `Ч224-перебор-написаний` | `Ч224-перебор-написаний` | 1 | 66ф, 1110+, 291− |
| `Ч227-краснота-вне-корпуса` | `ч227-краснота-вне-корпуса` | 2 | 22ф, 595+, 291− |
| `Ч230-написания-закрыть` | `a/написания-закрыть` | 12 | 109ф, 5808+, 743− |
| `Ч24-hody` | `ch24/hody` | 3 | 5ф, 358+, 16− |
| `Ч303-imena-stadiy` | `kernel-stage-names` | 1 | 2ф, 213+, 71− |
| `Ч305-lekser-kommentarii` | `a/лексер-держит-комментарии-305` | 1 | 1ф, 79+, 18− |
| `Ч306-storozh-chetyre-tseli-rano` | `ч306-storozh-chetyre-tseli-rano` | 1 | 4ф, 53+, 4− |
| `Ч308-vlitoe-rabotaet` | `HEAD` | 2 |  |
| `Ч309-bystryy-storozh-imyon` | `ch309-bystryy-storozh-imyon` | 3 | 2ф, 366+ |
| `Ч312-raznye-bedy-raznye-kody` | `работа/разные-коды-печати` | 1 | 7ф, 20+, 207− |
| `Ч60-svedenie` | `ch60/svedenie` | 4 | 8ф, 532+, 17− |
| `Ч63-chekker-v-derevo` | `main` | 1 | 145ф, 6874+, 7− |
| `Ч67-lozhnye-v-dereve` | `main` | 1 | 3ф, 157+ |
| `Ч73-do-odin-nol` | `main` | 1 | 1ф, 406+, 36− |
| `Ч99-polovina-pravil` | `main` | 2 | 8ф, 576+, 28− |

### Что вливать первым и почему

1. **`Ч177`** — 18 чисел, разошедшихся с деревом после моих же заливок.
   Сторож `scripts/prose-numbers-guard.sh` красный: «примет 116: сошлось 98,
   разошлось 18». Работник умер на середине. Числа врут В ДЕРЕВЕ, это долг.
2. **`Ч303` уже влита** (`de7067fa`), не дублировать.
3. Остальное — по стопкам: `Ч2xx` про доказательства, `Ч3xx` про сборку и
   печать. Границы по файлам описаны в наряде ведущим.

### Чего НЕ вливать

* **Ч201** — её проверяльщик пробивается семью подделками, а заявка
  «0 → 4 записи» держится на отпечатке, снятом с проверяемого файла
  (задача 1314).
* **Ч136** — её собственный прогон дал 295 сломанных файлов из 296.

## Грабли машины, стоившие времени

* Код возврата снимать **до** любой трубы: `kod=$?` отдельной строкой. Ошибка
  повторена за день четырежды.
* `grep` подменён и молча теряет кириллицу — звать `/usr/bin/grep -a` и ставить
  `LC_ALL=C.UTF-8`. В git — `-c core.quotepath=false`.
* Имена в семени **транслитерованы**: «Мера склейки» → `compiler_flang_mera_skleyki`.
  Поиск русской строки в `bootstrap/compiler_flang.c` даёт ноль и лжёт. На этом
  уже ошиблись один раз, объявив что ходов вывода нет нигде.
* `pkill`, `killall`, `pgrep -f` не звать никогда: машина общая, так уже убили
  чужие работы.
* Коммит автором `Marat Zimnurov <the-homeless-god@users.noreply.github.com>`.
  При `rebase` от чужого пользователя нужны `-c user.name` и `-c user.email`,
  иначе код 128 без конфликта.
* `/srv/priyom/stvol` — следящее дерево, оно делает `reset --hard origin/main`
  каждые десять минут. Работать в нём нельзя, только клонировать.

## Разряд ошибок, который стоит держать в голове

Записанное число или список, верные в день записи и лгущие назавтра. Названо
было «105 списков руками, 57 разошлись» — это сумма двух переписей РУКАМИ, а не
замер. Прибор появился 1 сентября (`scripts/hand-written-lists.sh`,
коммит `04c42222`) и своей меркой даёт **223 перечня и 62 расхождения**. Подвиды:
сторож, зелёный по построению; сторож, спрашивающий «есть ли правильное», но не
«есть ли неправильное»; сторож, не зовомый вовсе под кириллическим путём;
самозаверяющий предмет — запись, несущая собственный отпечаток, которым сама
себя и подтверждает.
