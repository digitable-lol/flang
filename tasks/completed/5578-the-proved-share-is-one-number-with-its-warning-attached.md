---
номер: 5578
заголовок: Доля доказанного по дереву названа одним числом, и при ней стоит оговорка о сложении по замыканию
статус: сделана
исполнитель: b
ветка: b/5578-dolya-dokazannogo
команда: первая
карта: Сколько доказано на самом деле
рядом: 0002, 0044, 1606, 2469, 6329
---

# 5578. Доля доказанного по дереву — одно число и одна оговорка

> ℹ **Семя перепечатано 30 августа** (`52996005`) — согласия ведущего заход
> больше не нужно. Но правка в `flang/self/**` доедет до собранного двоичного
> только следующей перепечаткой: до неё прогон по семени её не увидит.

## Чем измерено

Число записано и живёт в ветке `m/sverka-opublikovannogo`, файл
`scripts/proved-share.json`:

```json
"числитель": 8147,
"знаменатель": 13341,
"доля": "61,1 %",
"снято": "2026-08-28",
"коммит": "da47cbdf",
"двоичный": "82f9ac4018e795ec19955a7ce2a9c1a8",
"как снято": "make -C bootstrap -j8, затем ./bootstrap/flang check <файл> --proof
              по одному файлу за раз, 437 прогонов, 12,7 ч."
```

Владелец 29 августа назвал **8237 из 13 341 = 61,7 %**. В дереве стоит **8147 из
13 341 = 61,1 %**. Расхождение — 90 обязательств, и оно не объяснено ни там, ни
здесь. Я ставлю записанное в дереве число и говорю, что второе не сошлось.

Знаменатель на нынешнем стволе уже сдвинулся. Прогон сверки 29 августа:

```
написано обязательств опубликовано 13341     в дереве 13370     РАЗОШЛОСЬ
числитель 8147 — под вопросом на 119 обязательств из 13341
  файлов сдвинулось 4, ушло 0, прибыло 3
```

## Оговорка, без которой число лжёт

`flang check` судит файл **вместе со всем, что тот ввозит**. Складывать строки
«итог:» по файлам нельзя — сумма считает ввезённое по многу раз. Завышение
измерено, 29 августа:

| где | своих приговоров | всего в отчёте | во сколько раз завышает |
|---|---:|---:|---:|
| `flang/self/proof-kernel.flang` | 44 | 1503 | **34,2** |
| `flang/self/proofterm.flang` | 138 | 1779 | **12,9** |
| по всему дереву | — | — | **2,47** (записано в `proved-share.json`) |

Числитель 8147 посчитан ПРАВИЛЬНО — по своим функциям файла, а не сложением
итогов. Это и есть главная ценность числа, и она пропадёт, если кто-нибудь
пересчитает его сложением.

## Что сделать

1. Разрешить расхождение 8147 против 8237: переснять либо признать одно из
   чисел ошибкой и записать почему.
2. Довести знаменатель до нынешнего дерева: 4 файла сдвинулись, 3 прибыли,
   119 обязательств под вопросом. Переснимать надо только их, а не все 437
   прогонов — ведомость `scripts/proved-share-ledger.txt` держит md5 на файл
   именно для этого.
3. Вписать оговорку про завышение в 2,47 раза **рядом с числом**, а не в
   соседний файл: число без оговорки читается как «61 % дерева доказано», а это
   не то, что оно значит.
4. Влить ветку `m/sverka-opublikovannogo` — без неё ни числа, ни ведомости в
   стволе нет вовсе (соседняя задача про сверку опубликованного).

## Переснято 30 августа 2026 на стволе `f9e67fa6`

Ветка `m/sverka-opublikovannogo` **влита** (`07030e90`): и число, и ведомость
теперь в стволе, `scripts/proved-share.json` и
`scripts/proved-share-ledger.txt`. Пункт 4 «Что сделать» этим закрыт.

**Знаменатель ушёл дальше и считается не тем счётом.** Прогон
`./ярлык опубликованное:сверка` сегодня:

```
написано обязательств   опубликовано 13341   в дереве 13477   РАЗОШЛОСЬ
```

Разошлось всего пять мест (при заводе было 22): `библиотека.строк` 34954/34968,
`корпус.строк` 186543/186727, знаменатель 13341/13477, долг и перепись.

**Счёт по первому слову строки теряет обязательства, и это посчитано:**

| | число | проверено |
|---|---:|---|
| знаменатель по первому слову, сегодня | **13 477** | прогоном сверки |
| строк вида `для всех … обеспечивает`, которых счёт не видит | **140** | да |
| файлов, где они лежат | **76** | да |
| файлов, которых счёт по первому слову не видит вовсе (508 − 439) | **69** | да |
| **знаменатель с учётом кванторной формы** | **13 617** | да |

Форма законная и описана в дереве — `docs/site/language.ru.md:133`:
`для всех п обеспечивает «имя» условие`. Значит счёт по первому слову
(`scripts/published-vs-tree.sh:182`) неверен по построению, а не отстал.

Владелец назвал 13 610; сегодня выходит **13 617** — он мерил на день раньше.

**Что оказалось неправдой.** «`git ls-files` без `-z` экранирует кириллицу и
теряет 451 файл из 1154» — **опровергнуто**. Ни одно место в дереве так не
зовёт: `scripts/published-vs-tree.sh:182`, `:249`, `:270` — `git -c
core.quotePath=false ls-files -z`; `flang/scripts/word-guard.mjs:480` и
`scripts/jargon-guard.mjs:231` — с `quotePath=false`; сторожа на flang
(`scripts/tree-inventory.flang:350`, `link-guard.flang:403`,
`error-code-guard.flang:767`, `name-splicing-guard.flang:423`,
`adr-numbers-guard.flang:257`) передают `"-c","core.quotepath=false"` доводом, и
в `link-guard.flang:402` это закреплено примером «Кириллица в путях не должна
приезжать в кавычках». `flang/scripts/per-file-proof-share.py` git не зовёт
вовсе — `os.walk` (строка 87). Арифметика сама по себе верна (451 из 1154 в
восьмеричных экранах), но только если снять `quotePath=false`, чего в дереве
никто не делает.

**Пятый пункт «Что сделать», прибавленный сегодня:** считать знаменатель
разбором программы, а не первым словом строки. Кванторная форма — не
исключение, а вторая законная запись того же обязательства, и первым словом её
не поймать никогда.

## Как понять, что сделано

```sh
sh scripts/published-vs-tree.sh --доля
```

отвечает `числитель … — ЖИВ: ни один файл с обязательствами не сдвинулся` и
кодом 0, и число в `scripts/proved-share.json` совпадает с тем, что называет
сайт.

## Что от неё зависит

Задача 0002 и задача 0044. Это единственное число, которым проект отвечает на
вопрос «сколько у вас доказано», и сегодня оно живёт в неотправленной ветке.

## Чем закрыта

**Одно число: доказано 4028 обязательств из 13 489 — 29,9 %.** Снято прогоном
`sh scripts/published-vs-tree.sh --доля` (14 с) 30 августа 2026 на стволе
`4233c18c`.

**Знаменателей два, и оба названы.** Написано в дереве **13 581** обязательство:
`обеспечивает` 13 484, `требует` 88, `закон` 9 — считая кванторную форму
`для всех … обеспечивает`, которую счёт по первому слову строки не видит (140
штук). Из них **92 не попадут в числитель ни при каком доказательстве** (88
`требует` и 4 `закон` подделок — задача 1729), поэтому знаменатель доли —
**13 489**. По написанному та же доля равна 29,7 %. Знаменатель проверен вторым,
независимым прогоном: 13 484 + 88 + 9 = 13 581, прежним правилом 13 441.

**Пункт 2 задачи (довести знаменатель до дерева) закрыт прогоном.** На стволе
`9190b0be` сдвинувшихся файлов описи было 24 и прибывших 2; все 26 пересняты
двоичным, который даёт `make -C bootstrap -j8`, знаменатель описи сошёлся с
деревом, и сверка отвечала **«числитель — ЖИВ: ни один файл с обязательствами не
сдвинулся»**. Пока работа сводилась со стволом, он ушёл вперёд на сотню
коммитов, и сдвинулось ещё 25 файлов — это `flang/self/emit-*`, `interpret`,
`lsp`, `proof`, `cli`, `stdlib/aes` и соседи, которых двоичный дерева не судит
вовсе. Их 585 доказанных в числитель не вошли.

**Оговорка стоит там же, где число** — тремя ключами внутри того же блока
`scripts/proved-share.json`, в шапке `scripts/proved-share-ledger.txt` и в
`ROADMAP.md` рядом с самим числом, а не в соседнем файле:

> **Первая.** Это НИЖНЯЯ ОЦЕНКА С НЕИЗВЕСТНЫМ ЗАПАСОМ. Файлов с живым
> приговором — 325 из 510; у остальных 185 (7751 обязательство, **57 %
> написанного**) живой ведомости нет вовсе, и в числителе они стоят нулём
> (задача 7205). Пока ведомость не снята, запас над 29,9 % не ограничен ничем,
> кроме 100 %. И складывать строки «итог:» по файлам нельзя: сумма считает
> ввезённое по многу раз и завышает по дереву в 2,47 раза, на
> `proof-kernel.flang` в 34,2, на `proofterm.flang` в 12,9. Направление
> перемерено прогоном на 17 переснятых файлах: приговоров своих 361, ввезённых
> 64 — сложение дало бы 425 вместо 361.
>
> **Вторая.** Числитель снят ДВУМЯ двоичными, и один из них в дереве не
> собирается. Двоичным дерева повторяется **2111 из 13 581 = 15,5 %**; у 77
> файлов, которым он ведомости не даёт вовсе, в описи записано **1825
> доказанных — 45 % числителя**.
>
> **Третья.** Часть числа устарела к часу записи: 25 файлов описи сдвинулись
> (4624 обязательства), и записанных у них 585 доказанных в числитель не вошли.

### Прежние числа: проверены прогоном, оба сняты с учёта

| число | что с ним сегодня |
|---|---|
| **8147 из 13 341 = 61,1 %** (28 августа) | знаменатель отстал от дерева на 240; числитель поимённо не разложен и потому непроверяем — сверка отвечала «числитель 8147 — под вопросом на 1995 обязательств из 13341». Кроме того, тот двоичный не судил 25 файлов (1081 обязательство) вовсе, и 61,1 % — тоже нижняя оценка, только необъявленная |
| **8237** (названо владельцем 29 августа) | в дереве не записано ни строкой; проверить нечем по той же причине — поимённой раскладки нет ни у одного из двух |
| **508 файлов / 13 610** (30 августа, `f9e67fa6`) | правило верное, оно и применяется; разошёлся только час съёмки — сегодня 13 581 в 510 файлах |

**Почему разошлись — разложено по слагаемым.** 13 341 (счёт по первому слову
строки, 28 августа) → 13 441 (то же правило сегодня: +100 приросло в дереве) →
13 581 (+140 кванторной формы, которой счёт по первому слову не видит никогда).
То есть из 240 расхождения **100 — возраст, а 140 — ошибка правила**.

### Опись снята двумя двоичными, и это измерено

Прогон 30 августа перепроверил **все 304 строки живой описи** двоичным
`4e32f9f76d8215c3b5df1b6abe0b6a68`, собранным из этого дерева
(`make -C bootstrap -j8`; каталог `bootstrap/` за это время не менялся), срок
45 с на файл; 26 сдвинувшихся и прибывших — им же со сроком 90 с:

| | число |
|---|---:|
| ведомость снялась | **227** |
| двоичный дерева отказался вовсе | **77** — предел глубины вызовов 20 000 у 63, предел шагов у 1, срок 45 с у 13 |
| у отказавших в описи записано доказанных | **1825** |
| приговор совпал с описью знак в знак | **194** из 227 |
| разошёлся | **33**, сальдо **+93** |

Расхождения идут ВВЕРХ: семя перепечатано 30 августа, и ядро знает больше
правил, чем знало 29-го. Отрицательный вывод при этом полноценный: часть
числителя описи держится на РУЧНОМ гибриде `25605ce2`, которого в дереве нет, —
1825 доказанных нынешним двоичным дерева не воспроизводятся.

### Сверка с опубликованным

`sh scripts/published-vs-tree.sh --доля` до работы: знаменатель опубликован
13 341, в дереве 13 581 — **РАЗОШЛОСЬ**; числитель 8147 — под вопросом на 1995
обязательств. После работы знаменатель сходится, числитель считается поимённо.

**Сайт называет ДРУГОЕ число.** Сайт: **407 из 1045 = 38,9 %**. Дерево: **4028
из 13 489 = 29,9 %**. Знаменатели различаются в **12,9 раза**, числители в
**9,9 раза**. Область у сайта другая и им не названа: `утверждения.*` сняты по
КОРПУСУ (`flang/**/*.flang` без `flang/test/fixtures` и без
`flang/self/bootstrap/compiler.flang` — 213 файлов сегодня). Но и внутри
корпуса числа не сходятся: в его исходниках написано **11 347 обязательств в
206 файлах**, а сайт называет знаменателем **1045** — это **9,2 % написанного**.
Причина в устройстве: `flang/scripts/proof-ledger.mjs:437` —
`if (ведомость === null) continue`, то есть файл, у которого ведомость не
снялась, выпадает и из числителя, и из знаменателя. Сайтовые 38,9 % — доля по
снятому, а не по дереву, и сравнивать её с долей дерева нельзя. Сняты эти ключи
23 августа 2026 на `252606e8`.

### Что осталось красным и почему это не недоделка

`sh scripts/seed-freshness.sh` отвечает отказом: семя отстало от исходников на
5 файлов (`flang/self/bootstrap/compiler.flang`, `emit-js.flang`, `io.flang`,
`parser.flang`, `proof-initial.flang`). Пока семя не перепечатано, раздел
`--доля` красен по построению, и **код 0 недостижим никаким пушем** — это работа
заслона, а не пропущенный шаг. Строка про двоичный красна НАРОЧНО: число снято
двумя двоичными, и назвать один было бы неправдой.

Замечено попутно и не чинено: `./ярлык задачник:проверка` на этом стволе не
доходит до конца — `FLANG_RECURSION_LIMIT` у «Дальше после шага» на глубине
20 001, и с ключом `--предел-глубины 200000` тоже. Проверено на нетронутом
`tasks/` из ствола: беда не от этой правки.

### Число устареет, и это сказано в самом снимке

Над `scripts/proved-share-ledger.txt` одновременно работают несколько
исполнителей (задача 7205, срезы stdlib и core/conc): они дописывают строки,
числитель растёт, доля поднимается. Поэтому в снимке стоит ключ `УСТАРЕВАЕТ`, а
число несёт при себе ствол и час. Их строки в это число не входят.

Ветка: `b/5578-dolya-dokazannogo` (пересобрана от ствола как
`b/5578-dolya-dokazannogo-2` после столкновения по описи).
