---
номер: 6128
заголовок: Приём, которым снята ведомость ядра, записан как рабочий и повторяется кем угодно
статус: сделана
исполнитель: b
ветка: b/6128-priyom-vedomosti
команда: первая
карта: Сколько доказано на самом деле
рядом: 0002, 1943, 2469, 6329, 6715
---

# 6128. Приём снятия ведомости ядра записан как рабочий

**Ведомость ядра снята. Это первое.** До 29 августа она не снималась ничем —
задача 6818, пункт 5, называла это прямо: «ведомость ядра сегодня не снять
ничем».

## Чем измерено

Прогон 29 августа 2026, замеры лежат в `/srv/tmp/m-kernel/yadro/`:

```
$ cat /srv/tmp/m-kernel/yadro/flang_self_proof-kernel.flang.kod
код 0 ZAMER 3138.35 10116324
```

3138,35 с = **52 мин 18 с**; 10 116 324 КиБ = **9,65 ГиБ** пика.

**Приговор по самому файлу — 44 из 44, 100 %** (счёт двумя независимыми
путями, `/srv/tmp/m-kernel/dolya-fajla.py`, 29 августа):

```
функций в файле: 582
обязательств написано в исходнике: 44
строк приговоров в отчёте всего: 1503 (из них своих 44, ввезённых 1459)
СВОИ: {'доказано': 44}
ДВА СЧЁТА СОШЛИСЬ. Доказано 44 из 44 = 100.0 %
```

## Сам приём — вот он, тремя частями

Пусковой скрипт — `/srv/tmp/m-kernel/pusk-yadro.sh`.

**1. Двоичный собирается не из ствола и не общий, а из трёх названных кусков:**

* `compiler_flang.c` — **семя дерева дословно**, md5
  `38aa312a491c411a5bdec0a2d626e74b`, побайтово равен
  `bootstrap/compiler_flang.c`;
* `flang_runtime.c` и `flang_runtime.h` — **рантайм дерева дословно**,
  `flang/src/emit/c/*` (расходятся только шесть строк напечатанной шапки);
* `flang_repl.c` — рантайм дерева плюс заплата ключа предела,
  `/srv/tmp/m-kernel/opyt/predel.patch`.

Собранное лежит в `/srv/flang-rabota/m-kernel-ledger/build-kernel/`.

**2. Ключ предела ставится ОГРОМНЫЙ, а не «с запасом»:**

```sh
"$DVOICHNYY" check "$FAJL" --proof --предел-шагов 400000000000
```

Четыреста миллиардов против вшитых четырёх. Строки хода это подтверждают
(`/srv/tmp/m-kernel/yadro/flang_self_proof-kernel.flang.err`):

```
шагов 70 566 741 085 из 400 000 000 000 (17 %), идёт «Ведомость исходников», 2,8 ГиБ
```

Тут ловушка, названная в `AGENTS.md`: **ключ ЗАДАЁТ предел, а не поднимает его.**
Поставить «с запасом» 400000000 значит опустить предел вдесятеро.

**3. Прогон идёт через ворота с честной памятью:**

```sh
env PAMYAT=45G /srv/flang-rabota/vorota/flang-vorota -- …
```

## Почему это стоит записывать отдельно от прогона

Приём **достаёт то, что раньше не доставалось ничем**. Разница не в терпении:
общий двоичный дерево ядра прочесть не может вовсе
(`FLANG_TYPE_ARGS`, параметрические типы), а собранный так — может, потому что
его рантайм взят из дерева, а не из семени.

## Что сделать

1. Записать приём в дерево — `docs/` или `scripts/` — так, чтобы его повторил
   человек, сегодняшних разговоров не слышавший: три куска двоичного, ключ
   предела с оговоркой «задаёт, а не поднимает», ворота, ожидаемое время и пик.
2. Положить туда же `pusk-yadro.sh` и `dolya-fajla.py`: сегодня они лежат в
   `/srv/tmp/`, то есть исчезнут.
3. Назвать границу приёма: чего он НЕ даёт. Он не заменяет перепечатку — семя
   остаётся старым, и приговор описывает дерево ровно настолько, насколько семя
   ему отвечает.

## Как понять, что сделано

Файл в дереве, по которому чужой человек снимает ведомость `proof-kernel.flang`
заново и получает `44 из 44`, не спрашивая ни у кого ни одной подробности.
Проверка — прогон по записанному, а не чтение записанного.

## Что от неё зависит

Соседняя задача про окрестность ядра (161 обязательство в четырёх файлах) и
задача про двенадцать файлов, упиравшихся в предел шагов: обе снимаются ЭТИМ
приёмом и никаким другим. Пока он не записан, они снимаются только тем, кто его
помнит.

## Чем закрыта

Коммит `f680fc19`, ветка `b/6128-priyom-vedomosti`, 30 августа 2026, ствол
`747fe6ee`. В дереве: `scripts/build-ledger-binary.sh` (двоичный),
`scripts/take-proof-ledger.sh` (ведомость одного файла и доля самого файла),
`docs/kernel-ledger.md` (страница с приёмом, ловушками и границами).
`flang/self/**` и `bootstrap/**` не тронуты.

**Приём проверен прогоном, а не чтением.** Двоичный
`f32d89f599dd6b9f740f42a752dff1ff` — тот самый, которым снята ведомость ядра и
которым 7205 снимает срезы сегодня, — воспроизведён ПОБАЙТОВО одной строкой
`KOMMIT=4a26cf05 sh scripts/build-ledger-binary.sh` (66,64 с, пик 1 087 412 КиБ,
`cmp` с чужой копией — код 0). Все семь его файлов взяты из одного коммита
`4a26cf05`; догадка о двух коммитах опровергнута замером (`614858cd` даёт
14 046 520 байт вместо 14 065 960). Этим воспроизведённым двоичным снята
ведомость `flang/stdlib/aes.flang` в содержимом коммита `326dce24`: код 0,
399,17 с, пик 571 176 КиБ, **доказано 129, сетка 81, объявлено 8 из 218** — знак
в знак со строкой описи `56d52b40…|218|129|81|8|0`.

**Пункт «ведомость ядра переснять и получить 44 из 44» не выполнен, и вот
почему.** `flang/self/proof-kernel.flang` с 29 августа сдвинулся (md5 описи
`84874562…`, в дереве `551dca5c…`), значит тем же двоичным вышло бы ДРУГОЕ число,
а не то же самое: на `aes.flang` сдвиг содержимого дал ровно +14 (129 → 143), и
это измерено. Ядро в час сдачи снимали два чужих прогона (места 19 и 21 у ворот).
Приём доказан на файле, чьё содержимое не сдвинулось.

**Что записано сверх задания.** Приём собран и по нынешней рабочей копии: он даёт
двоичный, ПОБАЙТОВО равный `bootstrap/flang` (23 122 912 байт, md5
`4e32f9f7…`), потому что перепечатка 30 августа свела рантайм дерева и рантайм
семени. Скрипт говорит это первой строкой вывода. Обе причины, по которым приём
был единственным путём, сняты: `FLANG_TYPE_ARGS` на ядре больше не печатается, а
ключ `--предел-шагов` у сборки из семени есть. Осталась третья и новая: предел
ГЛУБИНЫ — `aes.flang` нынешним семенем не снимается ни при 20 000, ни при
200 000 кадров. И `--предел-шагов 400000000000` из этой задачи сегодня ОПУСКАЕТ
вшитый предел (1 400 000 000 000) в 3,5 раза; скрипт сверяет и говорит вслух.
