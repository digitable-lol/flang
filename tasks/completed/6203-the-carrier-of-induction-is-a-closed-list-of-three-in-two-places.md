---
номер: 6203
заголовок: По какому типу можно вести индукцию, решает закрытый перебор из трёх, и он написан дважды в двух независимых программах
статус: сделана
исполнитель: Marat
ветка: r/kvantory
команда: первая
карта: Чего в языке нет вовсе
рядом: 6202
---

# 6203. Первый шаг ADR-0026: перебор из трёх заменяется разбором объявления

Решение ADR-0026 говорит: кванторы над произвольными типами — правка ядра и
независимой проверяющей программы. Задача — первый шаг, самый дешёвый и самый
показательный.

## Что известно из замера ADR-0026

Квантор `для всех` у постусловия связывает **ровно одно имя**, и имя это обязано
быть параметром функции (`flang/self/parser.flang:4204–4215`
`«Квантор постусловия»`, 12 строк; отказ — `flang/proof/SPEC.md:861`).

Поле `forall` в обязательстве несёт **только имя**. Тип берётся из соседнего
поля `vars` (`flang/self/proofterm.flang:1717–1731`). А по типу выбирается
носитель индукции — **закрытым перебором из трёх**:

| функция | файл : строки | носитель |
| --- | --- | --- |
| `«Случаями по имени»` | `proofterm.flang : 2887–2898` | объявленная сумма |
| `«Отрезком по имени»` | `proofterm.flang : 2849–2860` | тип, названный натуральным |
| `«Свёрткой по имени»` | `proofterm.flang : 2874–2886` | свёртка |

**Тот же список из трёх продублирован во второй, независимой программе:**
`flang/proof/чекер/сверщик.c:6641–6644`.

Само поле `forall` читается на всё дерево пять раз
(`obligations.flang:797`, `types.flang:7272` и `:7280`,
`flang/self/bootstrap/compiler.flang:2319`) и записывается в заключение принципа
на `proof-initial.flang:1633`.

## Почему это узкое место, а не мелочь

Пока носитель выбирается перебором из трёх, никакой новый тип квантор не примет —
сколько бы слов ни добавили в язык. И править это надо **дважды**: в ядре и в
проверяющей программе. Разойтись они могут молча — так уже расходились доводы
ядра и записи (ADR-0022, раздел 2).

## Что сделать

1. **Поле `forall` перестаёт быть текстом** и становится узлом с именем И типом.
   Сегодня тип приходится доставать из `vars` — это лишний шаг, на котором
   теряется то, ради чего квантор написан.
2. **Перебор из трёх заменяется разбором объявления типа.** У объявленной суммы —
   начальность (часть значения меньше значения). У отрезка — конечность и строгий
   спуск. У свёртки — её принцип. У всего прочего — **отказ, названный поимённо**:
   «принципа индукции по типу Т нет, и вот почему». Молчаливого «не подошло» быть
   не должно.
3. **То же в независимой проверяющей программе**, `сверщик.c:6641–6644`.
4. **Подделка.** Квантор по типу, у которого принципа нет, обязан быть отвергнут
   с названной причиной, а не принят кодом 0.

## Чего эта задача НЕ делает

Не снимает ограничение «переменная обязана быть параметром функции» — это шаг 2
решения ADR-0026, и у него своя цена. Не заводит квантор существования и
высший порядок — это шаг 3, и у него своя логика.

## Как понять, что сделано

- постусловие `для всех л обеспечивает …` при `л: список числа` проверяется, и
  отчёт называет носитель по объявлению типа;
- `grep -n 'algebra\|segment\|fold' flang/proof/чекер/сверщик.c` не показывает
  закрытого перечисления на 6641–6644;
- новая подделка отвергнута, и причина названа поимённо;
- `sh scripts/доказуемость.sh` печатает «проверка 1 — калькулятор снят? ДА».

## Ход работы

### 9 сентября 2026 — A-c3, Ш0: носитель читается с объявления типа (только сверщик)

Worktree `/srv/tmp/dokazuemyy/wA`, ветка `r/6202-6203-cheker` от `gh/dev` `2a97c8204`; сверщик
собран `make -C flang/proof/чекер` здесь же. Номера строк из проекта устарели (+589 после
`8c98c0311`) — места искались по именам функций.

Правки `flang/proof/чекер/сверщик.c`: `объявление_типа()` читает объявление довода с ИСХОДНИКА
(`тип «Т»` + `вариант` — сумма; `объект «Т»` — запись; `список`/`строка` — встроенные суммы;
`тип «Т» это Х` — псевдоним по цепочке, глубина ≤ 8; имя из одного списка `НАТУРАЛЬНОЕ[]` —
отрезок); четыре списка имён натурального (`довод_с_дном`, `тип_отрезок_с_дном`, `ТИПЫ_С_ДНОМ`/
`ТИПЫ_ТОЧНЫЕ`, тип результата в `из_объявленного`) → один `НАТУРАЛЬНОЕ[]` (шесть имён ядра) плюс
закрытые дополнения `ДНО_СВЕРХ_ОТРЕЗКА` («вес») и `СЕТКА_СВЕРХ_ОТРЕЗКА` («сотых», «тысячных»);
`варианты_типа`/`строка_варианта_типа` — через объявление (псевдоним к сумме разворачивается);
ОДНА точка носителя в `сверить_покрытие`: закрытый список трёх слов + носитель обязан следовать из
объявления довода индукции (сумма/запись → `algebra`|`fold`, отрезок → `segment`, прочее — «принципа
индукции нет», код 1) + тип в строке `принцип` обязан быть объявленным типом довода. Новое:
`flang/proof/ОТРЕЗКИ.tsv` (шесть имён, дно, потолок, целость, откуда) и сторож С-5 в
`сверка-ведомости.sh` (таблица ↔ ядро «Названо натуральным» ↔ `НАТУРАЛЬНОЕ[]`, в обе стороны;
потолок = «Точный потолок» ядра; подлог 9 — имя убрано из таблицы). Семья `пробы/семьи/носитель/`
(README там), `НАБОР.tsv` +2 («лжёт-запись»: `poddelka-nositel-chislo-segment`,
`poddelka-nositel-psevdonim-ne-otrezok`; записи в корпусе, исходники в семье), храповик 31 → 33.

| фальсификатор | снято (старый бинарь `2a97c8204` → новый) |
|---|---|
| честная с `натуральное` | **3 → 0**, «всё доказанное проиграно заново: утверждений 3» |
| подделка П1 (`segment` над `число`) | **3 → 1**: «довод «н» объявлен типом «число» — принципа индукции по нему нет» |
| З2 (`segment` над `тип «Мера» это число`) | **3 → 1**, та же причина по цепочке псевдонима |
| П1б (запись говорит «неотрицательное», довод — `число`) | 3 → 1 |
| П2 (слово вне трёх) | 1 (как и было) |
| псевдоним к отрезку (`тип «Возраст» это неотрицательное`) | 3 → 0 — ядро напечатает после Ш1, сверщик читает уже сейчас |
| корпус 86 записей, старый бинарь против нового | кодов разошлось 0, выводов разошлось 0 (побайтно) |
| `сверка-ведомости.sh` с С-5 | код 0: «79 строк ведомости, пять сторожей зелены»; имён 6/6/6; `--подлоги` код 0; копия сверщика без «натуральное» — С-5 **КРАСНО** («таблица разошлась с НАТУРАЛЬНОЕ[]»), код 1 |
| исходники семьи ядром (`bootstrap/flang` из worktree `r4135`, только чтение) | все три — код 0, «сетка 1», `вердикт нет вердикта`; шапки, посчитанные помощником, равны шапкам ядра знак в знак |
| доверенная база (К9) | `сверщик.c` 8753 → 9079 строк (+326 при мандате +600, потолок 9353); `ПРИМИТИВЫ[]` = 15 |
| гейт A-v после Ш0 | `прогон.sh` «сошлось всё»: подделок 281 (принято кодом 0 — 0), честных 146 (отвергнуто 0); доля **567 / 651 = 87,10 %** (недостижимых вынесено 14 → 16: две новые записи набора по одному месту), числитель набора 155; `доказуемость.sh` 1 ДА, 2 НЕТ, 3 ДА (набор 33 из 33), 4 ДА (33 при храповике 33; 155 при 155) |

Не сделано (по границам Ш0): `сверщик.flang` (третий читатель, 874–901) — не тронут, блока
`вывод` и объявлений он не знает и сегодня; строка `объявление …` в записи — Ш2; ядро — Ш1;
`obekt-odna-posylka`/`psevdonim-otrezok` как записи ядра — после Ш1. Lean не гонялся (в дереве нет).

### 10 сентября 2026 — A-Ш2: сверщик читает и сличает строку `объявление` (только сверщик)

Worktree `/srv/tmp/dokazuemyy/wA3`, ветка `r/a-sh2` от `gh/dev` `71af9d679`; сверщик собран
`make -C flang/proof/чекер` здесь же (sha256 `64d07e4b…`). Спека и входы — эталоны стека B
`ПЕРЕДАЧА/stack-B/golden-sh2/` (печать снята зондом по коду ветки `r/6202-6203-yadro` 147b3e0fc,
не перепечатанным ядром). Сравнение — со сверщиком из `gh/dev` 5359922e2 (sha256 `bd617ec5…`).

Правки `сверщик.c`: `объявление_типа()` (та же одна точка, что у Ш0) дополнена полями `откуда`
(сумма / запись / встроенный / псевдоним по цепочке, со строкой объявления) и `арности` (варианты
в порядке объявления с числом рекурсивных частей, как «Поле рекурсивно» ядра); `сверить_покрытие`
сличает строку знак в знак (algebra — варианты, segment — дно 0 и потолок К_предст, fold — ничего
сверх) и называет первое расходящееся слово; `ОБРАЗЦЫ[]` + «объявление …»; `сверить_строки_записи`
— строка только сразу под принципом. Записи без строки принимаются, как прежде. Семья
`пробы/семьи/объявление/` (README там), помощник `prichina` в `прогон.sh` (код 1 + названная причина
+ ни одной «не узнана»).

| фальсификатор | снято (сверщик dev → новый) |
|---|---|
| эталоны B: честные Ш2 ×3 | **1 → 0** («строка не узнана» → проверено); вывод побайтно равен выводу входа без строки |
| эталоны B: входы ×3 | 0 → 0 |
| эталоны B: подделки ×12 | 1 «не узнана» → **1 с названным расхождением** («Пласт» вместо «Слой», лишнее «Крышка» 0, недостаёт «Слой» 1, «2» вместо «1», «137» вместо «136», «запись» вместо «сумма», носитель не следует, не сразу под принципом, дно, потолок, «сумма» вместо «встроенный» ×2) |
| рукотворные: синоним «натуральное», псевдоним «Возраст» | 0; вывод побайтно равен выводу той же записи без строки; подделки п13 (чужая строка псевдонима), п14 (псевдоним пропущен) → 1 названо |
| мутант: сличение выключено | `прогон.sh`: провалов 24 (12 подделок приняты кодом 0 и без причины); п07/п08 держат Ш0 и место строки |
| 327 записей дерева, dev против нового | расхождений 0 среди прежних 308; 19 — ровно новые записи семьи |
| строки кода (С-6) | 5737 → 5782 (+45), в счёт ADR-0026 +600; строка расхода в `ХРАПОВИК.txt` |
| `прогон.sh` | «сошлось всё»: подделок 299 (принято кодом 0 — 0), честных 153 (отвергнуто 0) |
| доля / вердикт | 567 / 651 = 87,10 % (поручился 85 из 88); `доказуемость.sh` 1 ДА, 2 НЕТ, 3 ДА (299/0, 153/0), 4 ДА |

Не сделано: честной записи с `запись «Т»` (объект) нет — ядро по объекту принципа не даёт (матрица
ADR-0026, ф02), строка для него только грамматикой; встроенная `строка` под algebra — варианты взяты
по коду ядра B, эталона нет; сличение с НАСТОЯЩЕЙ печатью ядра — после перепечатки партии №1.

### 11 сентября 2026 — разбор: что из К1–К12 и Ш0–Ш3 реально в `gh/dev` (по коммитам, не по названиям)

Worktree `/srv/tmp/dokazuemyy/kvantory`, ветка `r/kvantory` от `gh/dev` `2c40752d0`. Шапка задачи
(«Ш1–Ш3 ждут») отстала от ствола: партия №1 влита `f4cf63a4d`, семя перепечатано из `b6f3a4b0f`
(`0ce948bfd`, РЕПРИПТ КОД=0), записи проб пересняты этим семенем (`c887237ff`, `e47be1c03`).
Ниже — что именно в стволе; «семя» = `bootstrap/flang`, собранный в этом дереве
(`make -C bootstrap -j8`), проверено прогоном `bootstrap/flang check --proof` по исходникам семей.

| ячейка | ядро (`flang/self`) | сверщик | в семени `0ce948bfd` | что говорит семя сегодня |
|---|---|---|---|---|
| Ш0 носитель с объявления | — | `fd56f8b4f` | — | `psevdonim-otrezok` без теоремы — код 0, «сетка 1» (как и до Ш1: вердикта нет) |
| Ш1 принцип с объявления | `fdc14f0be`, `2f0f37e5b`, `b6f3a4b0f` (переименования) | — | да | `corpus-length`: `принцип тип «список» по «элементы» носитель algebra`, запись побайтно равна `семьи/объявление/corpus-length.запись` |
| Ш2 строка `объявление` | `fdc14f0be` (`zapis.flang` 1516–1527) | `e4c2c1d10` | да | та же запись: `объявление встроенный «список» варианты «пусто» 0 «голова и хвост» 1` |
| Ш3 тексты | нет | — | — | легенда `proof.flang:262` по-прежнему называет два носителя без `fold` и без объявления; шапка этой задачи и 6202 — «в работе» |
| И3 (6202) `обязательство вход` + Пред1→П3 | `575edb31c` | `9a9df2e3f`…(И1) | да | `pred-zov`: строки `обязательство вход «первое» … на зовущем`, `вывод 1 Пред1 … / вывод 2 П3 … из 1`; запись побайтно равна дереву |
| К1 имя за «для всех» = переменная индукции | `a5f082fb0`, `40f5c0169` | `23c1ff7bf` | да | `квантор/имя-вне-списка` — `FLANG_PROOF_CLAIM_MISMATCH`, код 1 |
| К2/К6 несколько имён, вложенные | `c0f5c5213`, `83d0ec4db`, `272674e16`, `42f2256d8`, `ff380a97d` | `23c1ff7bf` | да | `квантор/два-имени` — код 0 |
| К3 свободное утверждение | `83d0ec4db`, `272674e16`, `f914dea72`, `e945047dc`, `44c5764cf`, `e01d7588f`, `060e44b29` | `b0834b8a4`, `7d13e38b4` | да | `ф05к-утверждение`, `несколько-связываний`, `утверждение-с-ограничением` — код 0; печать `ф05к` побайтно = `ф05к-печать-партии.запись` |
| К3c шаг «по свойству» на утверждение | `eb2deb31d`, `6485ec7ec`, `10a871411`, `a7bfa48a3` | `23579d265`, `a3cbf518e` | да | `к3c-б-база-сведением` — 0; `к3c-применение-утверждения` и `к3c-ход-подстановкой` — `FLANG_PROOF_STEP` (README семьи ждёт 0 — расхождение, см. ниже) |
| К4 постусловие о чужом имени — отказ «напишите утверждение» | `c0f5c5213` (`parser.flang:4285`), `272674e16` (`types.flang:7311`) | — | да | текст отказа в семени есть (`grep -c` по `compiler_flang.c`) |
| К5 «таких что» | `c0f5c5213`, `83d0ec4db`, `272674e16`, `f914dea72` | `37398ea6a`, `8e8b12a79` | да | `ограничение/ф09-ограниченный` — код 0 |
| К7 существование со свидетелем | разбор `c0f5c5213`, `5634975ad`; ядро `f914dea72` (`proofterm.flang:3666`); печать `ff380a97d`, `f2472bde4` (`zapis.flang:5299`) | `5f46157f2`, `796abf27c` | да | **все четыре исходника семьи «существование» — `FLANG_TYPE: неизвестный вид выражения «exists»`**: «Тип узла» (`types.flang:3429`) вида `exists` не знает; `ф10б` — `FLANG_UNKNOWN_NAME`. Записи семьи рукотворные (README семьи), ядро их не печатает |
| К8 индукция по функции — именованный отказ | `a5f082fb0` | — | да | не гонялось (входа в семьях нет) |
| К9 бюджет сверщика | — | `b8e12a3dd`, `1b50ad85f` | — | С-6 зелен (`сверка-ведомости.sh`) |
| К11 ловушка по видам | — | `9f49c2cfa`, `1b50ad85f` | — | `доказуемость.sh` проверка 1: видов 34 из 34 |
| К12 словарь | `147b3e0fc` | — | да | слова `есть такой`/`а именно`/`таких что` разбираются (К5/К7 доходят до типов) |

Вывод: из порядка ADR-0026 §11 в ствол не доехали два места. (1) П.7 — существование: разбор,
ядро, печать и сверщик есть, а типизатор — нет, поэтому ни одна программа с «есть такой» не
проходит `check` (это первый несделанный пункт по нумерации §11 после п.3, который работает).
(2) Ш3 — тексты. Расхождение К3c (`к3c-применение-утверждения`, `к3c-ход-подстановкой` — код 1
`FLANG_PROOF_STEP` при ожидании 0 в README семьи) в этой задаче не разбирается — записано здесь,
чтобы не потерялось.

Прибор на `2c40752d0` (семя и сверщик собраны здесь): `доказуемость.sh` — 1 ДА (видов 34 из 34),
2 ДА (96,01 %: 625 из 651, вынесено 16), 3 ДА (подделок 401, принято 0; честных 197, отвергнуто 0),
4 ДА; `доля-корпуса.sh --набор корпус --проигрыванием` — 625 / 651, на слово ядра 16 + шагов 4,
снято калькулятором 6, знаменатель 651 + 16 = 667 сошёлся; `прогон.sh` — «сошлось всё»;
Lean 4.33.1 — леммы приняты все (85 в файле, 88 строк ведомости), ловушка 8 из 8 отвергнуто.

### 11 сентября 2026 — Ш1 сверен на семени, Ш3 тексты; задача закрыта

Ш1/Ш2 в ядро писал стек B (`fdc14f0be`, `2f0f37e5b`, `b6f3a4b0f`), семя перепечатано из них
(`0ce948bfd`); здесь — сверка «как понять, что сделано» прогоном и Ш3.

| пункт «как понять» | снято |
|---|---|
| `для всех л обеспечивает …` при `л: список числа` | новая проба `семьи/носитель/список-чисел.flang`: семя — код 0, «доказано 1 (из них индукцией 1)»; запись: `принцип тип «список» по «л» носитель algebra база 1 шаг 1` + `объявление встроенный «список» варианты «пусто» 0 «голова и хвост» 1`; сверщик — код 0, «всё доказанное проиграно заново» |
| `grep -n 'algebra\|segment\|fold' сверщик.c` | закрытый список трёх слов остался (`сверить_покрытие`, слова записи — не носители по типу); носитель ВЫВОДИТСЯ из объявления довода (`объявление_типа`), прочее — «принципа индукции по нему нет», код 1 |
| подделка отвергнута поимённо | `poddelka-nositel-chislo-segment`, `poddelka-nositel-psevdonim-ne-otrezok`: «довод «н» объявлен типом «число»/«Мера» — принципа индукции по нему нет: объявление не сумма, не запись, не отрезок» |
| `доказуемость.sh` проверка 1 | ДА (видов 34 из 34) |

Ш3 (тексты): `flang/proof/SPEC.md` §3г-бис (носитель с объявления, пример печати семени),
легенда `flang/self/proof.flang` у объекта «Индукция» (какой из трёх — решает объявление),
`flang/proof/чекер/ЧИТАТЬ.md` (строка про семьи 6202/6203/ADR-0026), ADR-0026 §11 «Исполнение».
Проба вплетена в `прогон.sh`: честных 197 → 198, «сошлось всё»; `доказуемость.sh` — 4 ДА,
доля 625 / 651 (не менялась: семьи в корпус не входят). Правка легенды в `proof.flang` —
комментарий, в семя не едет и поведения не меняет.

Не сделано здесь: `сверщик.flang` (третий читатель) объявлений по-прежнему не знает;
рукотворные записи `imya-naturalnoe`/`psevdonim-otrezok` (без теоремы ядро вердикта не даёт —
«сетка 1», как и до Ш1) остаются рукотворными; расхождение К3c и типизатор К7 — задача 6205.

## Чем закрыта

Ш0 `fd56f8b4f`, Ш2 (сверщик) `e4c2c1d10`, Ш1/Ш2 (ядро) `fdc14f0be` + `2f0f37e5b` + `b6f3a4b0f`,
семя `0ce948bfd`, записи `c887237ff`; сверка на семени, проба `список-чисел` и Ш3-тексты —
коммит этой ветки (`r/kvantory`, см. `git log -- tasks/completed/6203-*.md`).

