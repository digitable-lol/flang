---
номер: 9961
заголовок: `flang new <пакет>` — с нуля до готовой структуры одной командой
статус: сделана (все пять проверок «как проверить» прошли прогоном; см. раздел «Прогон 6 сентября 2026»)
исполнитель: r13
ветка: r/13
команда: любая
карта: Куда идём
рядом: 9962, 9963
---

# 9961 — `flang new <пакет>`: максимально простой старт

Владелец: «Инструмент flang new <package>: максимально простой старт.
Программист пишет команду, и у него создаётся готовая структура папок,
включая настроенный fspec/ и базовые файлы с ключевым словом тотальная
функция.»

## Зачем

Сегодня `flang --help` знает `flang lock <файл>` (замок зависимостей) и
`flang package <файл>` (замок с именем, версией и ведомостью) — но
СОЗДАТЬ новый пакет с нуля нечем. Первый опыт с языком — минуты до первой
`тотальная функция`, не поиск по докам, какие файлы и в каком порядке
завести руками. > **Замер устарел.** Абзац ниже был верен, когда задачу заводили, и неверен
> с 3 сентября 2026: команда в дереве ЕСТЬ — она в
> `flang/src/emit/c/flang_repl.c` (ячейка Ч396), а её ключ `--force` записан
> долгом в `flang/scripts/cli-keys-debt.json`. Абзац оставлен, чтобы видно
> было, от чего отсчитывали:
>
> ~~Подкоманды `flang new` в дереве нет вовсе (проверено:
> `grep -n "new" flang/self/cli.flang`, `grep -rn "flang new" docs/ tasks/`
> — пусто).~~

## Что должна делать команда

`flang new <имя>` создаёт каталог `<имя>/` и кладёт в него:

1. **Базовый модуль** — файл вида `<имя>.flang` (или по принятой в дереве
   раскладке модулей, см. `docs/guide/naming.ru.md`) с одной настоящей
   `тотальная функция`, не заглушкой: с примером (`пример «…» ожидается
   …`), чтобы `flang check` на свежесозданном пакете сразу отвечал «функций
   1, из них с доказанным завершением 1», а не пустотой. Первое, что видит
   программист после `flang new` — зелёный ответ на непустую программу.
2. **`fspec/`, настроенный и рабочий** — по образцу корневого `fspec/`
   (`fspec/README.md`, `fspec/guard.flang`, `fspec/spec/*.flang`): каталог
   `spec/` с одной спекой-примером (доказанное правило предметной
   области, не декоративная), и `guard.flang`, который её проверяет.
   `bootstrap/flang io <пакет>/fspec/guard.flang` на свежесозданном
   пакете обязан отвечать «спеки согласны» сразу после `flang new`, без
   правок руками.
3. **Манифест пакета** — что-то совместимое с уже существующим `flang
   package <файл>` (прочитать его код первым делом, не гадать формат:
   `flang/self/cli.flang` или где он на самом деле реализован — команда
   в `--help` есть, реализацию найти и прочитать). Имя, версия, пустая
   ведомость.
4. **README** — не шаблонная вода, а то же самое «minutes to first total
   function», что даёт сама команда: как собрать, как проверить, как
   добавить спеку.

## Как проверить, что сделано, а не «выглядит сделанным»

- `flang new проба` в чистом временном каталоге → код 0.
- `flang check проба/проба.flang` (или как файл назовётся по конвенции)
  сразу после — код 0, функций ≥1, доказанных ≥1, БЕЗ правок вручную.
- `bootstrap/flang io проба/fspec/guard.flang` сразу после — «спеки
  согласны», БЕЗ правок вручную.
- Повторный `flang new проба` в тот же каталог — не молча перезаписывает
  чужую работу (спросить, что тут правильно: отказ с кодом и причиной,
  или явный ключ `--силой`/`--force`).
- Проба порчей: `flang new` с недопустимым именем пакета (пробел,
  зарезервированное слово, уже занятое системное имя) — понятная беда, не
  полусозданный каталог.

## Не входит в объём этой задачи

Публикация пакета, разрешение версий чужих зависимостей, реестр — это
`flang lock`/`flang package`/`scripts/registry-example/` (см. пункт 6
`docs/road-to-1-0.md`, «Полноценная история пакетов и модулей», ещё почти
ничего не сделано и отдельная, более крупная задача). `flang new` — это
только генератор стартовой структуры для уже существующего однопакетного
мира, не менеджер зависимостей.

## Прогон 6 сентября 2026 (работник r13, двоичный `bootstrap/flang` 0.7.11)

Задача числилась свободной, а работа была сделана. Ниже КАЖДЫЙ пункт раздела
«Как проверить» против своего прогона; ничего не зачтено по рассуждению —
каждая строка получена командой во временном каталоге.

| пункт «как проверить» | прогон | сделано |
|---|---|---|
| `flang new проба` → код 0 | код 0, создано 9 файлов | да |
| `flang check проба/проба.flang` без правок | код 0, «функций 1, из них с доказанным завершением 1» | да |
| `flang io проба/fspec/guard.flang` без правок | код 0, «спеки согласны: спек 1, утверждений 2, и каждое доказано из нуля аксиом» | да |
| повторный `new` не переписывает молча | код 1, `FLANG_NEW`, «не переписывает чужую работу молча»; ключ `--force` / `--силой` есть и назван в `flang new --help` | да |
| порча имени — понятная беда, не полукаталог | код 2, `FLANG_NEW`, «Пакет не создан»; проверено на имени с пробелом, на занятом слове `check` и на `..` — каталога не остаётся ни в одном случае | да |

Девять созданных файлов: `проба.flang`, `flang.package`, `README.md`,
`fspec/README.md`, `fspec/guard.flang`, `fspec/policy.flang`,
`fspec/settings.txt`, `fspec/snapshot.txt`, `fspec/spec/01-order-steps.flang`
— то есть все четыре пункта раздела «Что должна делать команда»: базовый
модуль, настроенный `fspec/`, манифест и README. Сверх пунктов проверено:
`flang test проба.flang` — код 0, «примеров 1, прошло 1»; `flang lock` — код 0.

## Не закрыто этой задачей

**`flang package` на манифесте отвечает кодом 1** — но беда не в манифесте и
не в `flang new`:

```
flang package flang.package  → код 1, FLANG_PACKAGE: не проходит проверку
flang package проба.flang    → код 1, FLANG_TYPE: функция «Прогоны для ядра»
                                принимает 2 аргум., получено 1
```

Та же `FLANG_TYPE` выходит на ОБРАЗЦЕ ИЗ ДЕРЕВА
(`docs/examples/package/discount.flang` с манифестом того же вида
`{"имя": …, "версия": …}`), значит спотыкается сама команда `flang package` в
двоичном 0.7.11, а формат манифеста, который кладёт `flang new`, совпадает с
образцом дерева. Это отдельная беда и отдельная задача; в разделе «Как
проверить» этой задачи такого пункта нет.

**Ключи `flang new` не переехали в эталон.** `--force` и `--силой` есть
только в C; `flang/self/cli.flang` знает саму команду `new`, но её ключей не
заводил. Расхождение уже названо долгом в `flang/scripts/cli-keys-debt.json`
(раздел «разряд-1-только-в-C»), сторож `ключи:проверка` его держит.
