---
номер: 9990
заголовок: «задачник:проверка» красен 220 замечаниями — не CI, никто не смотрел; и он требует статус НЕ того вида, каким весь вечер пишут задачи
статус: сделана (сторож ослаблен под честные статусы, словарь сведён к канону, 221 → 14; остаток поимённый)
исполнитель: C1
ветка: c1/9990-zadachnik
команда: любая
карта: Что мешает больше всего
рядом: —
---

# 9990 — сторож задачника молчал, потому что его никто не звал

## Как найдено

Тихий цикл обхода (гейты Г1-Г7 без движения несколько часов подряд):
вместо простого ожидания решила прогнать существующие быстрые сторожи
по накопленным за вечер правкам. `./ярлык задачник:проверка`
(`flang/scripts/tasks.flang --plan 'Задачник цел'`) дал:

```
задачник разъехался, замечаний 220
```

**Этого ярлыка нет НИ В ОДНОМ workflow** (`grep -rn 'задачник:проверка'
.github/workflows/*.yml` — пусто). Только ручной прогон. Отсюда и накопление —
не потому что кто-то сломал, а потому что смотреть было некому.

## Разбор 220 замечаний — две РАЗНЫЕ беды, не одна

```
163  ссылка в никуда           — задача ссылается на номер, которого нет в tasks/
 31  статус «открыта» не из четырёх
  5  команда «—» не из трёх
  3  статус «заведена» не из четырёх
 15  статус <развёрнутая строка> не из четырёх (разные, поимённо ниже)
  3  другое (поле не названо и т.п., по одному)
```

### Беда 1 (163 из 220) — вероятно, эхо архивации `tasks/completed/`

Задачи вроде `0003-seed-reprint-finishes.md` ссылаются полем «рядом» на
`0001`/`0002` — тех нет в `tasks/`, потому что 79 задач были перенесены в
`tasks/completed/` (задача 9960, ячейка Ч419, слито координатором как
`84e97cbf`, было раньше этого вечера). Сторож `tasks.flang`, похоже, ищет
номер только в `tasks/`, не в `tasks/completed/`. **Не проверено окончательно
чтением кода сторожа** — гипотеза, не установленный факт.

### Беда 2 — статус НЕ ОДНОГО СЛОВА ИЗ ЧЕТЫРЁХ, и это касается СЕГОДНЯШНИХ задач тоже

Канон сторожа (`flang/scripts/tasks.flang:463`): статус обязан быть РОВНО
одним из четырёх слов — **`свободна`, `в работе`, `сделана`, `отменена`**.
Ни «не начата», ни «открыта», ни «заведена» сюда не входят.

**Это не только старые задачи.** Пять статусов из сегодняшних, написанных
этим же вечером мной и ячейками, тоже красные, ПОТОМУ ЧТО несут честную
пояснительную приписку в скобках, а не голое слово:

- 9983: `сделана — путь 2 (сторож), путь 1 отклонён обоснованно`
- 9984: `не начата (реализации нет; пример найден И проба порчи ПОДТВЕРДИЛА дыру...)`
- 9985: `сделана (частично) — span-утечка починена (d621bda9)...`
- 9986: `сделана (частично) — полный обход и корень обоих регрессов найдены...`
- 9987: `сделана (полный передний край измерен честно...)`

## Почему это НЕ починено прямо сейчас координатором

Две причины, обе весомые:

1. **Масштаб.** 220 замечаний, разброс от задачи `0003` до `9989` — это не
   точечная правка, а вопрос конвенции для ВСЕГО каталога `tasks/`. Решение
   такого масштаба ночью, в тихом цикле, без владельца — не по чину.
2. **Направление решения не очевидно и не мне выбирать.** Два честных пути,
   и они противоречат друг другу:
   - **(а) Ужесточить практику** — статус всегда одно из четырёх слов,
     пояснение уезжает в тело задачи (например, отдельный раздел «Чем
     закрыта», как уже делают многие задачи). Сторож остаётся как есть.
   - **(б) Ослабить сторожа** — разрешить статусу нести короткую пометку
     через тире/скобки ПОСЛЕ одного из четырёх канонических слов, проверяя
     только префикс. Практика вечера (богатые, честные статусы) остаётся.

   Путь (б) ближе к духу этого же вечера — КРИТЕРИЙ.md весь построен на
   «названо число с происхождением, а не голое слово». Но это ЧЕЙ-ТО выбор
   конвенции для всего дерева, не разовая правка кода.

## Как понять, что сделано

1. Подтвердить или опровергнуть гипотезу беды 1 (чтение `tasks.flang`:
   ищет ли он `tasks/completed/`, и должен ли).
2. Владелец выбирает путь (а) или (б) для беды 2.
3. По выбранному пути — либо переписать статусы задач на голое слово +
   раздел в теле, либо расширить сторож на «слово + необязательный
   постфикс», с пробой порчи (статус вообще не по протоколу — обязан
   остаться красным).
4. `./ярлык задачник:проверка` — код 0 или явно названный, кому доверенный,
   остаток.

## Не сделано (на момент заведения задачи)

Ничего из «Как понять» не сделано — эта задача только называет находку и
оба честных пути, как и просит установившаяся сегодня дисциплина для
подобных развилок (см. Г2/Г4 записи в КРИТЕРИЙ.md этого же вечера).

## ⚑ СДЕЛАНО 5 сентября (C1): 221 → 14, и обе беды разошлись по разным починкам

Замер снят своим прогоном на `dev cdf3d405`, двоичный собран из этого же
дерева (`make -C bootstrap`, код 0): **было 221 замечание**, не 220 — с
момента прошлого счёта в дереве прибавилась задача 9991.

### Беда 1 — гипотеза подтверждена чтением кода

`«Каталог задач»` (`tasks.flang:36`) возвращает `"../../tasks"`, и это
единственный каталог, который план перечисляет. `tasks/completed/` не
читался вовсе — значит всякая ссылка на закрытую задачу висела.

Починено: заведён `«Каталог архива»`, план получил шаг `«Ждём архив»`
между списком задач и их чтением. Архив участвует ТОЛЬКО как набор
номеров, на которые позволено ссылаться: его шапки никто не сверяет
(поздно требовать сегодняшних правил от закрытого), поэтому файлы архива
не читаются — хватает имён (`«Задача архива»` заполняет один «номер»).
Отсутствие каталога — не беда, а пустой архив: иначе задачник нельзя было
бы проверить в дереве, где ещё ничего не закрывали.

Проверено, что номера не сталкиваются: пересечение номеров `tasks/` и
`tasks/completed/` — **0**, значит правило «номер занят не одной задачей»
от слияния наборов не краснеет ложно.

### Беда 2 — разделилась на ДВЕ разные, и лечатся они по-разному

**Сначала счёт слов** (свой прогон по 173 живым файлам):

| слово статуса | сколько | исполнитель | что это |
|---|---:|---|---|
| `свободна` | 68 | — | канон |
| `в работе` | 36 | назван | канон |
| `открыта` | **31** | все `—` | СИНОНИМ «свободна» |
| `сделана` | 26 | назван | канон |
| `отменена` | 4 | — | канон |
| `заведена` | **3** | все `—` | СИНОНИМ «свободна» |
| `не начата` | **3** | `нет`/`A3` | СИНОНИМ «свободна» |
| `первая половина сделана` | 1 | `—` | одиночка |
| `причина найдена…` | 1 | `Ч403` | одиночка |

Отсюда вывод, которого в постановке задачи не было: **неканонические
слова — не новые состояния, а синонимы уже существующих.** Значит расширять
канон нечем — расширение развело бы ОДНО состояние по четырём ярлыкам.

**Поэтому починки две, и они независимы:**

1. **Приписка после слова — ослабить сторож** (решение владельца, путь б).
   Заведено `«Слово состояния»`: статус равен каноническому слову ЛИБО
   начинается с него И пробела. Пробел обязателен — без него `сделанаЧТОТО`
   прошло бы как «сделана» с припиской.
2. **Синонимы — привести к канону в файлах** (путь а). 37 файлов: `открыта`
   и `заведена` → `свободна` дословно; три одиночки — по смыслу, с
   сохранением пояснения в скобках (`первая половина сделана` →
   `свободна (первая половина сделана)`; `причина найдена…` при названном
   исполнителе → `сделана (…)`; `не начата (…)` → `свободна (…)`, а
   `исполнитель: нет` → прочерк, это тот же разнобой словаря).

### Побочная находка, которой никто не искал: доска теряла 46 задач из 172

`«Со статусом»` сверяла статус ТОЧНЫМ равенством, поэтому ни задача со
словом `открыта`, ни задача с честной припиской в столбец не попадали —
они считались в «всего» и не считались нигде больше. Замерено обоими
прогонами:

```
ДО:     всего задач 172: свободно 67,  в работе 36, сделано 19, отменено 4   (видно 126)
ПОСЛЕ:  всего задач 172: свободно 104, в работе 36, сделано 28, отменено 4   (видно 172)
```

Починено тем же `«Слово состояния»` — им теперь считают и `«Со статусом»`,
и `«Закрытые»`, и три правила согласия в `«Беды согласия»` (иначе правки
статуса молча отключили бы проверку «сделана, а исполнитель не назван»).

### Проба порчи — обе красные

Без неё ослабление сторожа неотличимо от его выключения:

```
статус: сделана (сторож ослаблен под честные статусы, словарь сведён к канону, 221 → 14; остаток поимённый)
статус: сделана (сторож ослаблен под честные статусы, словарь сведён к канону, 221 → 14; остаток поимённый)
```

Правки проб отменены, в дереве их нет.

### Что осталось: 14 замечаний, все поимённые и настоящие

Это уже не разнобой, а отдельные дефекты, каждый со своей причиной:

* `команда «—» не из трёх` — 5 задач (1734, 2481, 3172, 5406, 6913). Поле
  `команда` не терпит прочерка. Вопрос отдельный: либо дописать команду,
  либо разрешить прочерк — не решала, это не разнобой словаря;
* `в дорожной карте нет раздела` — 3 задачи (5406, 6913, 9621) называют
  разделы ROADMAP.md, которых там нет;
* `1315` — не названы поля «исполнитель» и «ветка» (3 замечания);
* `ссылка в никуда` — 2 настоящие: задача 8651 ссылается на 8650, которой
  нет НИГДЕ; задача 9983 — на 9959, которая лежит в `release-versions/`,
  то есть в третьем каталоге, ни в живых, ни в архиве;
* `9987` — названо четыре исполнителя (Ч9987, Ч9987b, Ч9987c, Ч9987d):
  задачу вели четыре ячейки подряд, а правило требует одного.

### Проверено

`flang check flang/scripts/tasks.flang` — «функций 101, из них с доказанным
завершением 101; типов 8; замечаний нет». Все новые функции тотальны,
примеры проходят.

### Не сделано

* 14 замечаний выше не чинились — каждое требует своего решения, а не
  общего правила, и часть из них (команда-прочерк, разделы карты) — вопрос
  конвенции, а не кода;
* `задачник:проверка` по-прежнему НЕ в CI. Пока его никто не зовёт
  автоматикой, он разъедется снова — предложение добавить его шагом в
  workflow оставлено координатору, сама не трогала.
