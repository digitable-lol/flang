# Ч180 — можно ли считать суд ядра на многих ядрах

## Обстановка
256 ядер, 499 ГБ (`nproc`, `free -g`). Печать компилятора самим собой — 53,5 млрд шагов за час на ОДНОМ ядре: стадия «Суд ядра о программе» съедает 7 ч 33 мин из 9 ч 56 мин при `Percent of CPU: 99%`. Обязательств в замыкании компилятора 6369.

## Задача
Числами: независимы ли обязательства, какая доля стадии делимая, каков потолок при 256 и при 16–64 ядрах, во что обойдётся правка. Саму правку не делать.

## Действия
Клон `a/все-ядра` от `af791d85`, `make -C bootstrap -j8`. Написан прибор (`benchmarks/суд-ядра-по-ядрам/`, коммиты `cb2b675b` и `b68a29d4`): вклеивается в копию семени (`bash встроить.sh <семя>`), зовёт части стадии по имени и печатает по каждой секунды и шаги. Замер: `FLANG_KERNEL_SPLIT=16 FLANG_KERNEL_SNAPSHOT=1 <семя>/flang check <файл>`.

## Результат

**1. Независимы — но не те, что дороги.** `«Пройти обязательства»` (`flang/self/proofterm.flang:3201`) свёрткой не является: накопитель `«свод»` — только сток, вердикт считает `«Проверить терм»` (`proofterm.flang:2387`) от трёх неизменных доводов. Опыт: список обязательств `base64.flang` разрезан на 16 долей — **77 строк из 77 совпали построчно** (`fl_equal`); на `lexer.flang` 94 из 94. Зависимость лежит в `«Проход без теорем»`/`«Попытка без теоремы»` (`proofterm.flang:3241–3256`): каждая попытка читает `«закрытые»`, накопленные соседями в том же проходе. Последовательны также `«Снять предусловия»` (3613), `«Твёрдые ключи»`/`«Пометить условные»` (3350+), `«Свести теоремы»` (`obligations.flang`). Независимо по функциям и построение списка (`«Обязательства функций»`, `obligations.flang:354`), но это тысячные доли секунды.

**2. Делимое сегодня — доли процента.** `check flang/stdlib/base64.flang`: `«Закрыть без теорем»` 33,676 с (538 752 612 шагов) против `«Пройти обязательства»` 0,012 с (126 344 шага) — **99,97 % шагов стадии в последовательной части**; `json.flang` — 636,678 с (7 345 855 530 шагов) против 0,013 с, 99,998 %; `lexer.flang` — 1,086 с против 0,018 с, 98,4 %. Неделимая доля 99,96 % и выше, потолок по Амдалю **1,0004×**, то есть ничего.

**3. Потолок после переделки — 2×, и берётся он шестнадцатью ядрами.** Неподвижную точку можно взять «снимком»: все попытки прохода смотрят в один снимок прошлого, а не в накопленное соседями. Итог тот же — прибор считает обе формы и сличает: закрыто 77 из 77 (`base64`), 94 из 94 (`lexer`). Цена: `base64` — 4 прохода, работы 69,330 с (вдвое больше, чем спуском), критический путь 16,617 с. Против сегодняшних 33,676 с это **2,03×**. Уже при 16 ядрах 23,903/16 = 1,49 с меньше 6,813 с самой долгой попытки прохода — значит 64 и 256 ядер не дают сверх шестнадцати НИЧЕГО. На `lexer` — 1,78×, и потолок берётся двумя ядрами (одна попытка 0,519 с из 0,967 с прохода).

**4. Делить поровну нельзя, и это померено.** У `lexer` 100 обязательств и 1,1 с, у `base64` 103 и 33,7 с, у `json` 111 и 636,7 с — при разнице в счёте на 11 % цена больше в 580 раз. Делить список поровну по числу обязательств бессмысленно.

**5. Цена.** Правка — в рантайме на C, `flang/src/emit/c/flang_repl.c`: драйвер с `de7067fa` уже зовёт части ядра по имени, петля неподвижной точки переносится туда же — 126 строк (столько занял прибор) плюс пул потоков со своими `fl_arena`/`fl_ctx`, около 300 строк итого; ядро на flang не меняется ни строкой. Но `flang_repl.c` — рукописный источник, в семя попадает перепечаткой (`flang/self/emit-c.flang:5818`, `scripts/bootstrap-c.sh`), поэтому до двоичного доезжает либо перепечаткой, либо ручной вклейкой в `bootstrap/` — как делал прибор. Путь через `flang/self/**` дороже: чтобы петля жила на flang, компилятор должен объявить `процесс` (сегодня их в `flang/self` ноль), и только тогда `emit-c` подключит уже написанный движок потоков (`flang/src/emit/c/flang_conc.c`, 4638 строк) — это правка самого компилятора и вся раскрутка.

**Ответ.** Разложить можно, но выигрыш — 2× потолком, а не 16× и не 256×, и весь он берётся шестнадцатью ядрами. Дешевле не раскладывать, а убирать работу: одна попытка на `base64` стоит 6,8 с из 23,9 с прохода.

## Чего не сделано
Не мерен сам компилятор (6369 обязательств, час на прогон; машина общая — в это время на ней шло до двенадцати чужих `flang`). Числа сняты на `lexer.flang` и `base64.flang`; на компилятор форма переносится по признаку «теорем во всём `flang/self` девять» — то есть почти все его обязательства идут тем же путём, через `«Закрыть без теорем»`. Замер `json.flang` оборван на одиннадцатой минуте. Ускорения не написано ни строки: работа была разведкой.
