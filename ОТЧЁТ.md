**Цель.** Ключ кеша приговоров ядра, который не отдаёт ложных доказательств.

| Ключевой результат | порог | факт | |
|---|---|---|---|
| ложных у полного ключа, проба порчей | 0 из 12; у наивного 4 | 0 из 12; наивный 4 | ✓ |
| смена правила проверяльщика | ключ иной у всех 49 затронутых | иной у 103 из 103; по дереву — 49 ложных | ✓ |
| места в ключе | 0 расхождений из 4 | 0; ключ Ч183 на паре коммитов — 0 из 6023 | ✓ |
| ключ дважды на дереве | 0 расхождений из 103 | 0, три прогона подряд | ✓ |
| пары коммитов | сойтись с 99,90 % Ч183 | 99,90 % на её окне, 99,78 % на dev | ✓ |
| `flang check` тронутых | расхождений 0 | 0, ведомость побайтово та же | ✓ |

**Вывод.** Ключ без замыкания даёт 4 ложных доказательства, без отпечатка проверяльщика — 49, полный на тех же опытах ни одного: обе составляющие несущие.
**Затык.** Нет.
**Нужно от тебя.** Ничего: хранилище — следующей ячейке.

---

# Ч351 — ключ кеша доказанного

**Действия.** Клон от `dev`, ветка `a/klyuch-kesha-dokazannogo`. Прибор Ч183
перенесён без правок отдельным коммитом (`2d7a14b0`), чтобы всё дальнейшее
читалось диффом; ключ Ч351 — `704fb6a7`. Собрано четыре семени: целое, целое с
прибором Ч183 (для воспроизведения её чисел), целое с прибором Ч351 и порченое
(«Предел ветвления» 4 → 0). Прогнаны четыре пробы, два замера по парам коммитов
и прогон по замыканию компилятора. Хранилища не написано ни строки. Перепечатка
не запускалась.

## Ключ: девять слагаемых и место, откуда ядро каждое читает

Строки — `flang/self`, ветка `dev`.

| | что | откуда ядро читает |
|---|---|---|
| ① | узел обязательства (цель, посылки, `vars`, `assumptions`, `grid`, `bind`) | `obligations.flang:223` |
| ② | тело своей функции | `proofterm.flang:2632` |
| ③ | тела ВСЕХ вызванных по замыканию | `proof-kernel.flang:1873`, определения — `proofterm.flang:198` |
| ④ | постусловия вызванных | `proofterm.flang:2455` |
| ⑤ | предусловия вызванных и глобальный список неоплаченных | `proofterm.flang:2482`, `3503` |
| ⑥ | объявления типов | `proofterm.flang:203`, принцип — `proof-initial.flang:1649` |
| ⑦ | объявления законов, ПЯТЬ полей (запас — см. «Не сделано») | `proofterm.flang:228`—`243` |
| ⑧ | множество уже доказанных фактов | `proofterm.flang:2493` |
| ⑨ | отпечаток ПРОВЕРЯЛЬЩИКА — двоичного | `/proc/self/exe`, 0,030 с на 21,8 МБ |

## Что изменено против ключа Ч183 и чем это оправдано

### 1. Места (`span`) из ключа изъяты — оговорка Ч183 закрыта

Ч183: одна пустая строка вверху файла — приговоры все те же, ключи все иные
(4 из 4). Кеш с таким ключом не попадёт никогда. Просто выбросить места нельзя:
кешированный отказ назовёт устаревшую строку.

**Почему изъятие законно.** Это читается в самом ядре, а не берётся на веру:
всякое правило, которое ходит по узлу, поле `span` ПРОПУСКАЕТ явной веткой —
подстановка (`proof-initial.flang:278`, и там стоит постусловие «поле места
проходится нетронутым»), сбор имён (`proof-kernel.flang:2095`), сбор вызовов
(`proofterm.flang:3385`), связыватели (`proof-kernel.flang:2188`), условие цели
(`proof-kernel.flang:6946`). Место читается РОВНО В ОДНОМ месте: когда его
приписывают к отказу — `«Отказ терма»` (`proofterm.flang:427`) и `«Диагностика
отказа»` (`:439`), и оба принимают место ОТДЕЛЬНЫМ аргументом, последним полем.

**Что делать с отказом.** Хранить его БЕЗ поля `span`, а место приписывать при
чтении из кеша — из СЕГОДНЯШНЕГО узла обязательства, тем же `«Первое непустое»`
от постусловия и функции, каким его берёт `obligations.flang:228`. Форма ядра к
этому готова, дописывать в него нечего.

**Замер** (`проба-местами.sh`, все строки обоих файлов съезжают на одну вниз,
файлы лежат в разных каталогах под одним именем):

| что спрошено | ответ |
|---|---|
| полный ключ Ч351 | совпал у 4 из 4 |
| ключ «с местами» (Ч183) | совпал у 0 из 4 — опыт поставлен |
| приговоры | изменились у 0 из 4 |
| ведомость `check --proof` | не изменилась ни байтом (4999 байт) |
| отказ `FLANG_PROOF_INDUCTION_STEP` | строка 28 → 29, текст ТОТ ЖЕ побайтово (853 байта) |

Последняя строка и есть доказательство того, что хранить отказ без места
безопасно: место стоит СНАРУЖИ текста, в отдельном поле.

**А ЗАПИСЬ ДОКАЗАТЕЛЬСТВА? Спрошено отдельно, потому что она места печатает.**
`check --записать` кладёт запись, которую читает `flang/proof/сверщик.flang`, и
номера строк в ней есть (`«Строка узла записи»`, `zapis.flang:148`). Сличение
записи целого дерева со сдвинутым: расходятся **8 строк из 37**, и все они
двух названных видов — шапка (`исходник`, `строк`, `знаков`, `отпечаток`: она
считается по файлу заново каждым прогоном) и по ОДНОЙ строке на утверждение —
`утверждение «…» функции «…» строка N`. Место в этой строке берётся из САМОГО
УЗЛА ОБЯЗАТЕЛЬСТВА, а он при чтении из кеша сегодняшний. Внутри вердикта
(`посылка …`, `сведение …`, `принцип …`) номеров строк нет ни одного.

Значит договор с хранилищем такой, и он полон:

| что | где живёт |
|---|---|
| ключ | считается без мест |
| вердикт, отказ | хранятся без поля `span` |
| место отказа и место утверждения в записи | приписываются ПРИ ЧТЕНИИ, из сегодняшнего узла обязательства |

### 2. Отпечаток снимается с ДВОИЧНОГО, и снимает его сам прибор

У Ч183 отпечаток приезжал переменной среды `FLANG_KESH_KERNEL` как sha256
четырёх файлов ядра В ДЕРЕВЕ. Дерево и двоичный — разные вещи, и забыть
переменную было можно. Теперь прибор читает `/proc/self/exe` сам; не прочёл —
ключи НЕ ПЕЧАТАЮТСЯ вовсе.

**Замер** (`проба-ядром.sh`; два двоичных ОДНОГО РАЗМЕРА, 21 828 128 байт,
дерево `flang/stdlib/base64.flang` побайтово одно, sha256
`9c501db8…`; в порченом переписано одно правило — `«Предел ветвления»` с 4 на 0,
`proof-kernel.flang:6800`):

| | совпал | ЛОЖНЫХ |
|---|---|---|
| полный ключ | 0 из 103 | **0** |
| ключ по дереву (без ⑨) | 103 из 103 | **49** |

Приговоров сменилось 49 из 103. `«Версия ядра»` (`proofterm.flang:73`) у обоих
двоичных равна 2 — отпечатком ПРАВИЛ она не годится, там версия ФОРМАТА термов.
Отпечатки: `5813530c1662dbf1` и `6a85675d93edb50e`.

**Почему НЕ `scripts/otpechatok-semeni`, на который указывало задание.** Этот
файл — отпечаток ВХОДОВ ПЕЧАТИ (sha256 сорока пяти `.flang` замыкания плюс
рантайм на C, плюс коммит), и он лежит В ДЕРЕВЕ. Он отвечает на вопрос «из чего
семя объявлено напечатанным», а ключу нужен другой — «чем сейчас проверяют».
Разойтись эти два ответа могут ровно так, как в пробе ядром: `встроить.sh` и
`порченое-семя.sh` правят `bootstrap/*.c` НАПРЯМУЮ, файл отпечатка при этом не
меняется ни байтом, а двоичные выходят разные. Отпечаток по дереву — это ровно
столбец «по дереву», и его цена замерена: **49 ложных доказательств из 103**.
`/proc/self/exe` к тому же вшестеро быстрее (0,030 с против названных 0,19 с).

**Цена этого решения названа прямо.** Ключ расходится не у 49, а у ВСЕХ 103:
отпечаток стоит в общей части, и «затронуто ли ИМЕННО ЭТО обязательство» ключ
различать не умеет. Значит всякая пересборка проверяльщика опустошает кеш
целиком. Так и надо: отличить затронутое от незатронутого можно было бы только
разобрав, какие правила ядра участвовали в каждом приговоре, — а это ровно то
доверие к ядру, ради недопущения которого слой доказательств и написан. Плата
берётся редко: проверяльщик меняется перепечаткой, а дерево — каждым коммитом.

### 3. Законы-пересечения добавлены, замыкание считается по всему узлу

`«Есть закон»` (`proofterm.flang:228`—`243`) читает ПЯТЬ полей — `monoids`,
`monads`, `isomorphisms`, `embeddings`, `intersections`. Ч183 хешировала
четыре. Перечисление всех обращений вида `от «программа» и "…"` в четырёх
файлах ядра даёт ровно семь полей: `functions`, `types` и эти пять — больше
ядро с программы не читает ничего. (Оговорка о том, что на пути «без теорем»
законы не читаются вовсе, — в «Не сделано».)

Замыкание вызовов считается по ВСЕМУ узлу функции, а не по одному `body`, как у
Ч183: цель — это постусловие с подставленным телом (`proofterm.flang:2637`), и
вызов, стоящий только в постусловии или в `требует`, из `body` не виден.
Пропущенное ребро замыкания — это ложное доказательство.

**Единственное ребро, которое прибор бросает нарочно** — вызов имени, у которого
в программе нет определения, то есть встроенного. Развернуть его нечем:
`«Функция терма по имени»` (`proofterm.flang:198`) отдаёт ничто, и ни
нормализация, ни `«Постусловия вызванных»` из программы о нём ничего не берут.
Что он делает, записано в самом проверяльщике — и потому уже стоит в ключе
девятой составляющей. Прочие ненайденные рёбра прибор не бросает молча: если
функции самого обязательства в программе нет, он говорит «ключ НЕ ПОЛОН» и
пробы краснеют кодом 3.

### 4. Почему ⑧ не отдельное слагаемое

`«Доказанные»` спрашивают не вообще, а по одному ключу
`«Ключ факта»(кого, имя)`, где «кого» — имя ВЫЗВАННОЙ (`proofterm.flang:2493`).
Доказана ли она, решает её собственное обязательство, а оно стоит на её
замыкании — подмножестве нашего. Индукцией по замыканию: множество доказанных
фактов, которое видит обязательство, определено содержимым его замыкания и общей
частью ключа. Класть в ключ сам список доказанных НЕЛЬЗЯ: он растёт по ходу
неподвижной точки, и ключ не сошёлся бы сам с собой на втором проходе.

Тем же рассуждением оправданы две нормализации, каждая только поднимает долю
попаданий и ни одна не теряет различия: список неоплаченных сортируется (его
читают одним `«Есть строка»`, то есть как МНОЖЕСТВО, `proofterm.flang:2482`), и
хеши членов замыкания сортируются (перестановка объявлений приговора не меняет).

## Что ключ стоит

Ключ обязан быть дешевле того, что он экономит, иначе он не кеш, а вторая
работа. На ЗАМЫКАНИИ КОМПИЛЯТОРА (7645 функций, 6038 обязательств, 6032 из них
без теоремы) прибор считает:

| что | сколько |
|---|---|
| отпечаток проверяльщика (21,8 МБ двоичного) | **0,030 с** |
| граф вызовов и хеши всех 7645 узлов | **2,206 с** |
| стадия суда ядра, которую кеш и снимает (замер Ч180) | **7 ч 33 мин** |

Весь прогон занял 560 с, но остальные 558 — это разбор, типы и связывание,
которые проверяльщик делает и без всякого кеша. Собственная цена ключа — секунды
против часов.

Дёшево ключ обходится потому, что хеш узла считается СТРУКТУРНЫМ обходом
`«Значение»` напрямую, а не через `«Печать значения»`, как у Ч183: печать нельзя
попросить пропустить место, и она идёт через толкователь, то есть стоит шагов
ядра.

## Пробы: числа

### Проба на промах — `проба-на-промах.sh`

Три порчи по одной зависимости, 4 обязательства, 12 сличений; приговор менялся
5 раз.

| ключ | ложных | напрасных промахов | промахов |
|---|---|---|---|
| **полный** | **0** | 5 | 10 |
| наивный (только своя функция) | **4** | 1 | 2 |
| без замыкания (③④ выброшены) | 1 | 5 | 9 |
| по дереву (⑨ выброшен) | 0 | 5 | 10 |
| с местами (ключ Ч183) | 0 | 5 | 10 |

Наивный ключ дал ровно 4 ложных, как и у Ч183: «Двойной вес» терял
доказательство трижды, а его собственный текст не менялся ни разу; в П2 к нему
добавился «Вес мешка». Счёт разделён на ЛОЖНЫЕ и НАПРАСНЫЕ нарочно: напрасный
промах — это ЦЕНА, ложное доказательство — ЛОЖЬ, и путать их значит выбрасывать
из ключа составляющие.

### Опыт, вышедший сам собой: комментарии в ключ не входят

Вынося комментарии из `проба.flang` (их требовал сторож), я получил тот же опыт,
что и со сдвигом строк, только жёстче: убрано 10 строк из 46. Оба файла положены
в разные каталоги под одним именем и прогнаны ОДНИМ двоичным:

| ключ | совпал |
|---|---|
| **полный** | **4 из 4** |
| наивный | 4 из 4 |
| по дереву | 4 из 4 |
| без замыкания | 4 из 4 |
| с местами (ключ Ч183) | **0 из 4** |

Приговоры совпали у 4 из 4. Комментарии разбор выбрасывает, ядру они не видны, и
в ключе Ч351 их нет. Ключ Ч183 от них ехал — через места.

### Ключи не сталкиваются

На `base64.flang`: 103 обязательства, **103 разных полных ключа**, столкновений
ноль. Размеры замыканий там: 54 обязательства с замыканием в одну функцию, 21 —
в две, дальше по 5—10, максимум 10.

### Проба повтора — `проба-повтора.sh`

`base64.flang`, три прогона подряд одним двоичным: **ключи и приговоры совпали
побайтово, 103 из 103 строк, 22 053 байта**.

**Находка, попутная.** Столбец ЦЕНЫ (`repl_ctx.steps`) воспроизводим НЕ до шага:
на одной из троек прогонов он разошёлся на 4 строках из 103, худшее расхождение
11 050 706 против 11 050 724 (0,000163 %). Ключа это не касается — он в других
столбцах, — но числа цены Ч183 и Ч180 надо называть с этой оговоркой. Моё
`base64` даёт 538 704 632 шага против 538 704 690 у Ч183: расхождение
того же порядка.

### По парам соседних коммитов — `по-парам-коммитов.sh`

Ключи считаны по ТЕКСТУ (`ключи-по-дереву.py`, обновлён под ключ Ч351: добавлены
законы). Ядро во всех прогонах одно, отпечаток постоянен.

| окно истории | пар | обязательств | полный совпал | доля |
|---|---|---|---|---|
| от `499edf26` — то же, что мерила Ч183 | 30 | 197 280 | 197 080 | **99,90 %** |
| от `2d7a14b0` (сегодняшний `dev`) | 30 | 197 379 | 196 935 | **99,78 %** |

**Сходится.** На ОКНЕ Ч183 мой ключ даёт 197 080 из 197 280 — те же числа до
единицы, и худшая пара та же: `a7ea8518` — 6408 из 6576 (97,44 %). На окне
`dev` доля ниже (99,78 %) не оттого, что ключ хуже, а оттого, что окно другое:
29 пар из 30 дают ровно 100 %, а вся недостача — одна пара `8430911c`
(`flang/self/lexer.flang`), 6141 из 6585 (93,26 %).

### Та же худшая пара, снятая ПРИБОРОМ, — и оговорка Ч183 закрыта числом

Тот же коммит `8430911c` против `86cc5c86`, но ключи считаны НЕ по тексту,
а самим прибором — по разобранной программе, замыканию вызовов и общей части:

| ключ | совпал | доля |
|---|---|---|
| **полный** | **5627 из 6023** | **93,43 %** |
| наивный | 6016 из 6023 | 99,88 % |
| по дереву | 5627 из 6023 | 93,43 % |
| с местами (ключ Ч183) | 0 из 6023 | **0,00 %** |
| без замыкания | 6016 из 6023 | 99,88 % |

**Вот ради чего это считалось.**

* **Ключ Ч183 на реальной паре коммитов попадает НОЛЬ РАЗ из 6023.** Не «редко»
  — ни разу: места съезжают у всякого обязательства всякого тронутого файла, а
  общая часть ключа съезжает у всех остальных. Кеш на таком ключе — пустая
  трата, и это то самое, о чём Ч183 написала оговоркой, а числом не показала.
* **Оговорка «разрыв между текстом и прибором не измерен» закрыта.** Текст на
  этой паре дал 93,26 % (6141 из 6585), прибор — **93,43 %** (5627 из 6023).
  Расхождение **0,17 процентного пункта**. Верхняя оценка и нижняя сошлись, и
  текстовой оценке по остальным парам можно верить.
* Обязательства у двух счётчиков не одни и те же (6023 против 6585, семя
  раскрутки старше дерева), поэтому сходятся ДОЛИ, а не списки.

### `flang check` на тронутых файлах

Тронут один `.flang` в дереве — `benchmarks/кеш-доказанного/проба.flang`
(вынесены комментарии). Ведомость `check --proof`, снятая ЦЕЛЫМ двоичным
(собранным из нетронутого дерева, `semena/tseloe-baza`), до и после —
**побайтово та же, расхождений 0**. `scripts/no-comments-guard.sh` — код 0
(«комментариев не прибавилось: 57 009 строк в 741 файле, долг был 58 553»); до
выноса он краснел кодом 1 на `проба.flang` (+9).

Косвенная, но веская проверка: `flang/self` и `flang/src` не тронуты ни одной
строкой (`git diff --stat dev...HEAD` — только `benchmarks/кеш-доказанного/`),
значит двоичный, собираемый из дерева, тот же.

## Как это звать

```
bash benchmarks/кеш-доказанного/встроить.sh      <семя>            собрать прибор
bash benchmarks/кеш-доказанного/порченое-семя.sh <семя>            собрать порченое
bash benchmarks/кеш-доказанного/замер.sh         <семя> <файл> <куда> [ключи|цена|без-ядра]
bash benchmarks/кеш-доказанного/проба-на-промах.sh <семя> <куда>
bash benchmarks/кеш-доказанного/проба-местами.sh   <семя> <куда>
bash benchmarks/кеш-доказанного/проба-повтора.sh   <семя> <файл> <куда> [прогонов]
bash benchmarks/кеш-доказанного/проба-ядром.sh     <целое> <порченое> <файл> <куда>
```

Все четыре пробы отвечают 0, если ложных доказательств нет, 1 — если нашлись,
3 — если опыт НЕ ПОСТАВЛЕН (порча не применилась, прибор сорвался). Третий код
отдельный нарочно: «не проверено» и «проверено и хорошо» — разные факты.

### Обе пробы видены красными

Сторож, которого никто не видел красным, — тот, на который не стоит смотреть.
Оба нерабочих исхода воспроизведены:

* **код 1.** Копия `проба-на-промах.sh` (в черновике, не в дереве) с одной
  подменой — столбцом «полного» ключа объявлен наивный — отвечает **1** и
  называет каждое ложное доказательство поимённо: «Двойной вес» трижды, «Вес
  мешка» однажды;
* **код 3.** `проба-ядром.sh`, которому вместо порченого семени подсунут тот же
  самый двоичный, отвечает **3**: «ОПЫТ НЕ ПОСТАВЛЕН: приговоры не разошлись».
  Отпечатки у обоих совпали (`5813530c1662dbf1`), приговор не сменился ни у
  одного — и проба не сказала «зелено».

## Не сделано

**Ключ по парам коммитов на замыкании компилятора прибором не снят целиком.**
Ради него в прибор заведён режим `FLANG_KESH_TOLKO_KLYUCHI=1` — ядро не зовётся
вовсе, считаются одни ключи. Он работает: `flang check
flang/self/bootstrap/compiler.flang` отдал **6032 ключа за 560 с** (9 мин 20 с,
пик 11 ГиБ). Тридцать одно дерево стоило бы ≈ 4,8 ч, а машина занята печатью,
поэтому прибором снята ОДНА пара — та, на которой доля попаданий хуже всего, и
на ней текст с прибором разошлись на 0,17 п. п. (см. выше). На прочих 29 парах
разрыв «текст против прибора» не измерен:
у текста рёбра вызовов берутся по вхождению имени, а не разбором (завышает
замыкание, то есть ЗАНИЖАЕТ долю), и вместо списка неоплаченных стоит признак
«есть `требует`» — единственное место, где оценка может оказаться ВЫШЕ правды.

Оговорка при этом: семя раскрутки — `flang 0.6.2`, и на сегодняшнем дереве оно
даёт `FLANG_NOT_TOTAL` на функциях, зовущих `«Печать значения»`. Ключи считаются
всё равно (обязательств 6032 против 6585 у текстовой оценки), но обязательства
у двух счётчиков не одни и те же, и сличать их доли лоб в лоб нельзя.

**Законы (⑦) в ключе стоят ПО ЧТЕНИЮ ЯДРА, и замером не проверены — а на пути
«без теорем» они и не читаются вовсе.** `«Есть закон»` зовётся из ОДНОГО места —
`«По закону»` (`proofterm.flang:1430`), а это обработчик АВТОРСКОГО шага
`по закону «имя»` внутри теоремы. Обязательства без теоремы, которые и закрывает
кеш, туда не попадают: у них нет `proof`, и посылки порождённого принципа
закрывает `«Свести счётом»`, а не шаги автора. Значит ⑦ в кеше приговоров
БЕЗ ТЕОРЕМ — запас, а не необходимость: он ничего не покупает и стоит
промахов на всяком коммите, трогающем объявление закона. Я его оставил, потому
что кеш, расширенный на обязательства С теоремами, без него солжёт, а стоит
запас здесь ровно ноль: объявленных законов в замыкании компилятора нет ни
одного (`grep -E '^(моноид|монада|изоморфизм|вложение|пересечение) '` по
`flang/self`, `flang/core`, `flang/stdlib` — пусто). Тем же объясняется, почему
на окне Ч183 моё число совпало с её числом до единицы: добавленное слагаемое там
пусто у всех тридцати одного дерева. Подделки на объявление закона я не ставил.

**Цена не мерена.** Сколько кеш приговоров сберегает — вопрос Ч183, а не мой; её
числа (59,27 % на `base64` при кеше только доказанного, 76—80 % команды при кеше
приговоров) я не переснимал. Замер повтора показал, что счётчик шагов дрожит на
1,6·10⁻⁶, — на выводы Ч183 это не влияет, но числа цены точны не до шага.

**Хранилища нет ни строки** — так и было заказано.

**Столкновение хешей не мерено.** Прибор считает FNV-1a 64: сличаются свои же
прогоны, подделывать некому. В настоящем хранилище хеш обязан быть
криптостойким — там столкновение означает ложное доказательство, и подбирать
его будет тот, кому это выгодно. Это сказано в шапке `прибор.c` и остаётся
задачей следующей ячейке.

## Провенанс

* дерево: клон `/srv/tmp/dokazuemyy/peredacha`, ветка `a/klyuch-kesha-dokazannogo`
  от `dev` (`c17c565c`); коммиты `2d7a14b0`, `704fb6a7`;
* разведка Ч183: `refs/ячейки/Ч183-kesh-dokazannogo/a/кеш-доказанного`,
  `ОТЧЁТ.md` и `benchmarks/кеш-доказанного/`;
* семена: `semena/tseloe-baza` (нетронутое дерево), `semena/183` (прибор Ч183),
  `semena/351` (прибор Ч351, отпечаток `5813530c1662dbf1`),
  `semena/351-porchenoe` (то же плюс «Предел ветвления» 0, отпечаток
  `6a85675d93edb50e`);
* вывод проб — всё в `/srv/tmp/dokazuemyy/Ч351-klyuch-kesha/`: `f-promah.txt`,
  `f-mesta.txt`, `f-povtor.txt`, `f-yadro.txt`; по парам —
  `vyhod-351-pary.txt` (окно `dev`) и `vyhod-351-pary-183.txt` (окно Ч183);
  прибором по паре — `vyhod/pribor-para/`; по замыканию компилятора —
  `vyhod/komp-klyuchi.txt`; самопроверки проб — `vyhod/krasnaya.txt` (код 1) и
  `vyhod/yadro-samoproverka/` (код 3); воспроизведение Ч183 её же прибором —
  `vyhod-183-promah.txt`;
* сборка: `cc -std=c99 -Wall -Wextra -Werror -pedantic -O2 -flto`, без единого
  предупреждения; `flang 0.6.2` (семя раскрутки), ствол заморожен под 0.7.0 и не
  тронут;
* СБОРКА ВОСПРОИЗВОДИМА, и это проверено: семя, собранное заново из
  ЗАКОММИЧЕННОГО исходника (после того как в `прибор.c` правились комментарии),
  вышло ПОБАЙТОВО ТЕМ ЖЕ — тот же `sha256`, тот же отпечаток
  `5813530c1662dbf1`, те же ключи у всех четырёх обязательств пробы. Значит
  названные в отчёте отпечатки и ключи снимаются с того самого исходника, что
  лежит в ветке. Отпечаток меняет правка ПРАВИЛ, а не комментария, — и это ровно
  то, что проба ядром и показывает.
