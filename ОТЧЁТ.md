**Цель.** Куда уходит печать связанного и делится ли она по модулям.

| Ключевой результат | порог | факт | |
|---|---|---|---|
| разложение стадии печати | доли числом, сумма ±5 % | сумма частей 100,00 %; 73,2 % — «Развязать постусловия» | ✓ |
| файлы, байты, дорогой | названы числом | 7 файлов, 38 958 006 Б; `compiler_flang.c` — 83,1 % | ✓ |
| цена файла зависит от прочих | да/нет с замером | да: один файл 53,54 с, все шесть 55,35 с — общего 96,7 % | ✓ |
| печать по модулям | да/нет, довод из кода | нет: свёртка с общим счётчиком; функция врозь и вместе — 31 строка из 66 | ✓ |

**Вывод.** 73 % стадии уходит не на печать, а на поиск петель контракта, и вывод нумеруется общим счётчиком: цена не в объёме, а в фикс-пойнте и общем состоянии.
**Затык.** Нет.
**Нужно от тебя.** Что первым: фикс-пойнт (десятки строк, режет 73 % стадии) или модульность (сотни строк, меняет формат вывода).

---

**Действия.** Прочитал путь печати в семени и в дереве; собрал стенд с отметкой
стадий (сброс выборки по времени на каждой смене шага) и снял точное разложение
стадии «Напечатать связанное»; напечатал шесть модулей поодиночке и сравнил с
целым; сравнил «один файл» против «все файлы»; сличил одну и ту же функцию,
напечатанную врозь и вместе; собрал список поздних проверок.

**Не сделано.** Разложение стадии ВНУТРИ снято на `parser.flang` (238 с), а не
на самом компиляторе. Два захода с прибором на компиляторе сняты мной же по
скорости: первый шёл 8–9 млн шагов/с против 37 млн у стенда без приборов, и
печать компилятора при такой скорости заняла бы 8,4 ч (замер скорости в 22:23
на двух пробах через 120 с). Держать четвёртую печать ради этого рядом с
выпуском 0.7.0 я не стал. Что от заходов ОСТАЛОСЬ и в отчёте есть: стадии
компилятора до печати, снятые прибором, — связывание 503,7 с, типы 17,5 с,
тотальность 14,2 с (сходится с 480 / 30 с по отметкам соседнего стенда, разница
5 %). Разложение ВНУТРИ стадии печати на самом компиляторе — `не мерил: заход
с прибором вчетверо медленнее, 8,4 ч против часа отчёта`.

**Провенанс.** Стенд `/srv/tmp/dokazuemyy/stend-bez-yadra/клон`, коммит
`c17c565c`, `kernel=false` в `emit_file`. Свой клон и ветка:
`/srv/tmp/dokazuemyy/Ч353-tsena-pechati-koda/клон`, ветка `a/tsena-pechati-koda`
от `dev` (`85f4febd`). Замеры: `zamery/`. Приборы рантайма — `FLANG_TIME_OUT`
(выборка 100 Гц), `FLANG_TICKS_OUT` (ведомость шагов), плюс моя отметка стадий
`FLANG_STAGES_OUT` — правка `bootstrap/flang_runtime.c` и `flang_repl.c`
(66 добавленных строк), лежит в `zamery/otmetka-stadiy.patch`, в рабочей копии
клона НЕ закоммичена и в семя не идёт: `bootstrap/` сверяется байт в байт.
Все журналы, ведомости и напечатанные каталоги — в `zamery/`.

## 0. Что стадия делает по шагам

`«Напечатать связанное»` (`flang/self/bootstrap/compiler.flang:394`) →
`«Напечатать к печати»` (там же, 208) → `«Печать программы»`
(`flang/self/emit-c.flang:5685`). Порядок:

| # | шаг | где | что делает |
|---|---|---|---|
| 1 | «Отказ по плану» | `compiler.flang:303` | отказ, если объявлен `план`: печатает его одна цель `js` |
| 2 | «Отметить меры» | | сторожа меры на функции |
| 3 | «Отметить доказанные» | | снятие сторожей с доказанного |
| 4 | **«Развязать постусловия»** | `compiler.flang:1359` | двойники «без проверок» для функций в петле контракта — **73 % стадии** |
| 5 | «Таблица входа» | `types.flang` | объявленные типы параметров данными |
| 6 | «Дефункционализировать» | `defunc.flang:2366` | высший порядок в варианты |
| 7 | «Собрать общее» | `emit-c.flang:2021` | ОДНА таблица на всю программу: приставка, функции, записи, варианты, суммы, ид, хвосты, циклы, рекурсия |
| 8 | «Имя файла программы» | `emit-c.flang` | имя файла из имени модуля, занятое целью обходится суффиксом `_flang` |
| 9 | «Проверить столкновения» | `emit-c.flang:5693` | столкновения после транслитерации — кладёт беду в «Состояние» и **печать НЕ останавливает** |
| 10 | «Печать тел» | `emit-c.flang:5190` | свёртка записи → суммы → функции, через все тела тянется одно «Состояние» |
| 11 | «Печать диспетчера» | | таблица вызова по имени |
| 12 | «Печать конкурентности C» | | только если объявлен хоть один `процесс` |
| 13 | «Печать границы входа» | | типы параметров данными |
| 14 | сборка списка файлов | | 2 файла рантайма дословно + 2 напечатанных + прогонщик + оболочка + `Makefile` |
| 15 | «Итог печати» | | `«ошибка»` берётся из последнего «Состояния» — то есть беда шага 9 доезжает сюда, оплатив всю печать |

## 1. Ответ владельцу: печатать по модулям сегодня НЕЛЬЗЯ

Не «дорого», а **невыразимо**: трёх вещей нет в коде.

**1.1. Печать берёт программу, а не список модулей.** `emit_call`
(`flang/src/emit/c/flang_repl.c:9382`) делает РОВНО ОДИН
`repl_call(«Напечатать связанное», {программа, настройки})` и получает назад
готовый список файлов. `«Печать программы»` (`flang/self/emit-c.flang:5685`)
кладёт ВСЮ связанную программу в ОДИН `<модуль>.c` и ОДИН `<модуль>.h`.
Единицы трансляции меньше программы у бэкенда нет.

**1.2. После связывания неизвестно, из какого файла функция.** У связанной
программы поле `module` ОДНО на всю программу; узел функции несёт
`name/total/params/returns/body/examples/span`, а `span` — это строка и столбец
БЕЗ имени файла (проверено `flang ast`). Запись `«Функция»`, которую читает
печатник (`emit-c.flang:1047`), — семь полей, происхождения среди них нет.
В напечатанном `lekser_flang.c` имя `.flang`-файла встречается РОВНО ОДИН раз —
и это `flang/self/emit-c.flang` в шапке, то есть имя печатника, а не источника.
Сказать «перепечатай только функции этого файла» не на чем.

**1.3. Текст функции — не функция от этой функции.** `«Печать тел»`
(`emit-c.flang:5190`) — это `свёртка` по записям → суммам → функциям, тянущая
через все тела одно `«Состояние»`: общий счётчик временных (`fl_t<N>`), общий
пул литералов (`ключи`, `статика`) и список занятых имён (`взято`).

Замер. Одна и та же функция «Читать имя» из `lexer.flang`:

| напечатана | временное | литерал |
|---|---|---|
| `lexer.flang` в одиночку | `fl_t389`, `fl_t388` | `lekser_flang_text_12` |
| внутри компилятора | `fl_t244`, `fl_t243` | `compiler_flang_text_507` |

Тела сличены построчно с вычтенной приставкой: 66 строк с каждой стороны,
**расходится 31, совпадает 35** — почти половина текста функции задана не ею.
Кеш «этот модуль уже напечатан» был бы бесполезен: его содержимое зависит от
того, сколько временных истратили ДО него.

## 2. Разложение стадии «Напечатать связанное»

Прибор: своя отметка стадий + `FLANG_TIME_OUT` (SIGPROF, 100 Гц, точка = 10 мс).
Вход `flang/self/parser.flang`, ключи как у перепечатки семени
(`--cli --repl`), стенд без суда ядра. Прогон 238,41 с.

| стадия | точек | секунд | доля прогона |
|---|---:|---:|---:|
| Разбор исходника (6 вызовов) | 1988 | 19,9 | 8,4 % |
| Связать исходники | 4570 | 45,7 | 19,2 % |
| Проверить типы | 42 | 0,4 | 0,2 % |
| Проверить тотальность | 22 | 0,2 | 0,1 % |
| Суд над категориями | 0 | 0,0 | 0,0 % |
| Чего печать не судила | 0 | 0,0 | 0,0 % |
| **Напечатать связанное** | **17 163** | **171,6** | **72,2 %** |

Внутри стадии (17 163 точки = 100 %):

| часть | секунд | доля |
|---|---:|---:|
| **«Развязать постусловия»: поиск петель контракта** | **125,6** | **73,20 %** |
| печать литералов («Печать литерала») | 16,5 | 9,64 % |
| печать значений выражений («Печать значения в C») | 14,0 | 8,15 % |
| печать вызовов и аргументов | 9,2 | 5,37 % |
| разметка типов («Пометка вида типа») | 2,1 | 1,20 % |
| граф хвостов и циклов («Собрать общее») | 1,2 | 0,71 % |
| прочее, 69 функций | 3,0 | 1,74 % |
| **сумма** | **171,6** | **100,00 %** |

**Главное число отчёта: 73 % стадии «печать» — не печать.** Это
`«Имена двойников» → «Постусловие зациклено» → «Замкнуть кругом»`
(`flang/self/bootstrap/compiler.flang:1234–1305`): для КАЖДОЙ функции с
постусловием считается транзитивное замыкание по всей программе, и внутри него
`«Имена тела и постусловий»` делает
`отфильтровать «функции» где имя равен имя` — полный линейный проход по всем
функциям программы на каждое имя каждого круга, а `«Дописать имя двойника»` —
`отфильтровать` по всему уже собранному списку. Словаря нет ни там, ни там,
хотя `flang/stdlib/hashmap.flang` и `dictionary.flang` в дереве лежат.

Отсюда и рост быстрее размера. Стадия печати, замерена тем же прибором:

| модуль | функций в выводе | тело, Б | стадия печати, с | Б/с |
|---|---:|---:|---:|---:|
| lexer | 133 | 518 363 | 3,6 | 144 000 |
| totality | 638 | 2 230 051 | 7,9 | 282 000 |
| types | 966 | 3 681 651 | 17,6 | 209 000 |
| emit-c | 920 | 3 783 102 | 41,4 | 91 000 |
| parser | 1525 | 6 751 531 | 171,6 | 39 000 |
| link | 1943 | 8 252 929 | 230,7 | 36 000 |

От `totality` к `link` тело растёт в 3,70 раза, а стадия — в 29,2 раза:
показатель `log 29,2 / log 3,70 = 2,58`. Печать не линейна по размеру.

Разделив стадию на «поиск петель» и «собственно печать», видно, что взрывается
ровно первая половина:

| модуль | тело, Б | стадия, с | поиск петель, с | печать, с | доля петель |
|---|---:|---:|---:|---:|---:|
| lexer | 518 363 | 3,6 | 0,2 | 3,4 | 5,3 % |
| totality | 2 230 051 | 7,9 | 1,6 | 6,2 | 20,6 % |
| types | 3 681 651 | 17,6 | 1,2 | 16,4 | 6,8 % |
| emit-c | 3 783 102 | 41,4 | 24,0 | 17,5 | 57,8 % |
| parser | 6 751 531 | 171,6 | 125,6 | 46,0 | 73,2 % |
| link | 8 252 929 | 230,7 | 166,1 | 64,6 | 72,0 % |

От `totality` к `link` (тело ×3,70): собственно печать растёт в **10,4 раза**
(показатель 1,79), поиск петель — в **104 раза** (показатель 3,55). `types`
выпадает из ряда (6,8 %) и этим же подтверждает довод: цена поиска зависит не от
размера, а от того, сколько функций несут постусловия, дотягивающиеся до петли.

## 2а. То же на самом компиляторе: стадии по часам

Соседний стенд (тот же коммит, суд ядра выключен) дошёл до конца в 22:08:49:
**7084,43 с = 1 ч 58 мин**, пик 18,3 ГиБ, `ПЕЧАТЬ КОД=0`, семя перепечатано,
собрано и спрошено — годное. Строка хода печатается раз в полминуты, поэтому
отметки — это прямо секунды:

| стадия | отметок | секунд | доля | тем же прибором, что выше |
|---|---:|---:|---:|---:|
| Связать исходники | 16 | 480 | 6,9 % | 503,7 с |
| Проверить типы | 1 | 30 | 0,4 % | 17,5 с |
| Проверить тотальность | 0 | <30 | — | 14,2 с |
| **Напечатать связанное** | **215** | **6450** | **92,7 %** | не домерено |
| сумма отметок | 232 | 6960 | 98,2 % от 7084 с | |

Правый столбец — мой заход по стадиям на самом компиляторе (выборка 100 Гц),
снятый до печати: связывание 503,7 с против 480 с по отметкам — расхождение 5 %,
то есть два независимых прибора о стадиях говорят одно.

Наряд называл 141 / 16 / 1 (89 / 10 / 1 %) с прошлого захода; сегодня печать
дала 215 отметок вместо 141 — машина держала три-пять печатей разом. Вывод от
этого только твёрже: **на стадию печати уходит 9 из каждых 10 оставшихся минут**, и «останется
около часа» после кеша приговоров сегодня меряется как **1 ч 47 мин** одной
печати.

## 3. Что именно печатается

Семь файлов, 38 958 006 байт (`bootstrap/` = ровно то, что даёт печать):

| файл | байт | доля | откуда |
|---|---:|---:|---|
| `compiler_flang.c` | 32 383 400 | 83,12 % | напечатано |
| `compiler_flang.h` | 5 463 380 | 14,02 % | напечатано |
| `flang_repl.c` | 702 832 | 1,80 % | дословная копия |
| `flang_runtime.c` | 275 184 | 0,71 % | дословная копия |
| `flang_runtime.h` | 71 688 | 0,18 % | копия + блок настроек |
| `flang_cli.c` | 58 629 | 0,15 % | дословная копия |
| `Makefile` | 2 893 | 0,01 % | напечатано |

Самый дорогой — `compiler_flang.c`. Четыре файла (1 108 333 Б, 2,84 %) не
печатаются вовсе: `emit_call` читает их с диска шестью `repl_read_file` ДО
вызова печати, и они уезжают в вывод дословно — у `flang_runtime.h` сверху
дописан только блок настроек. Их же, по замеру задачи 9619, не судил никто.

**Времени на файл нет, и это не пропуск замера, а устройство.** Печать —
один вызов на всю программу, файлы разбирает уже C: `emit_files_out` просто
раскладывает готовый список.

## 4. Цена одного файла зависит от остальных: ДА

`flang emit … --target c --file X` печатает ВСЁ и потом выбирает один файл.

| вход | что просили | вышло байт | секунд |
|---|---|---:|---:|
| `types.flang` | все 6 файлов (`--out`) | 4 088 669 | **55,35** |
| `types.flang` | один `Makefile` (`--file`) | 1 492 | **53,54** |
| `utf8.flang` | все 6 файлов | 615 223 | 4,10 |
| `utf8.flang` | один `Makefile` | 1 456 | 3,60 |

Попросив 1492 байта вместо 4,09 МБ, платишь **96,7 %** прежней цены.

Второй замер, про повтор работы. Шесть модулей компилятора, напечатанных
поодиночке: 703 с суммарно, **6125 напечатанных функций, различных 3667** —
каждая функция печатается в среднем **1,67 раза**, потому что каждый модуль
тянет за собой всё своё замыкание. Это шесть модулей из примерно сорока; чем
глубже граф ввозов, тем больше повтор. Пофайловая печать работу не делит, а
повторяет — тот же вывод, что у задачи 9619 про пофайловую ПРОВЕРКУ.

## 5. Дешёвые проверки, стоящие поздно

Порядок в ДЕРЕВЕ: связывание → типы → тотальность → процессы → **столкновения
имён** → суд ядра → категории → ПЕЧАТЬ → создание каталога → запись файлов.
В СЕМЕНИ, которым печатают сегодня, двух средних шагов нет вовсе.

| # | проверка | где стоит | где могла бы | цена правки |
|---|---|---|---|---|
| 1 | каталог `--out` создаётся | `flang/src/emit/c/flang_repl.c:9390` — ПОСЛЕ печати | до связывания | ~3 строки: перенести вызов |
| 2 | «такого файла печать не даёт» (`--file X`) | `flang_repl.c:8877` — после печати, код 2 | сразу после связывания: набор имён известен из строки цели и имени модуля | ~15 строк |
| 3 | пять из шести исходников рантайма | читаются в `emit_call`, после суда ядра; пробуется только `flang_runtime.h` (`flang_repl.c:9155`) | в той же пробе | ~10 строк |
| 4 | «Отказ по плану» — у цели нет планировщика для объявленной конкурентности | первая строка `«Напечатать связанное»` (`compiler.flang:397`), то есть после всей проверки | рядом со «Столкновения имён»: нужны только поля `processes/supervisors/runs` | ~5 строк, строкой таблицы целей |
| 5 | «Проверить столкновения» ВНУТРИ печати печать НЕ останавливает | `emit-c.flang:5693`: ошибка кладётся в `«Состояние»`, `«Печать тел»` идёт дальше, ошибка всплывает в `«Итог печати»` | там же, но с выходом | ~4 строки: `если не («начало».«ошибка») равно "" то «Сборка отказала»` |
| 6 | место на диске / отказ записи | `emit_write`, самый конец, 39 МБ | до печати | ~10 строк, ценность малая |

**Уже починено в дереве, но ещё не в семени.** `«Столкновения имён»` поднято
из печати в `repl_check_sources` ПЕРЕД судом ядра
(`flang/src/emit/c/flang_repl.c:4112`), и повод записан там же: 31 августа
пересборка семени шла **9 ч 56 мин** и упала на печати на паре
«Занято именем модуля C++» / «Занято именем модуля C» — оба дают
`zanyato_imenem_modulya_c`. В СЕМЕНИ этого нет:
`grep -c "Столкновения имён" bootstrap/flang_repl.c` = **0**, а
`bootstrap/flang_repl.c` короче своего исходника на **905 строк**. Значит
двоичный, которым идёт печать 0.7.0, проверку столкновений по-прежнему делает
последней. Она приедет ровно той перепечаткой, которая её застанет.

## 6. Что делать, по цене

**Сначала дешёвое — фикс-пойнт.** Два независимых промаха, и первый чинится
почти даром:

1. **Круг раскрывает уже раскрытое.** `«Замкнуть кругом»`
   (`compiler.flang:1282`) на каждом круге сворачивает по ВСЕМУ списку имён:
   `свёртка «имена» начиная с «имена» … → «Имена тела и постусловий» от «имя»`.
   Имена, раскрытые на прошлом круге, раскрываются снова. Классическая правка —
   вести фронт (только новые имена) и складывать его с уже собранным: **~6
   строк**, без новых ввозов, и она убирает множитель «число кругов».
   То же у `«Замкнуть по телам»` (1270).
2. **Поиск по спискам линейный.** `«Имена тела и постусловий»` (1262) на каждое
   имя делает `отфильтровать «функции» где имя равен имя` — полный проход по
   всем функциям; `«Дописать имя двойника»` (1219) — `отфильтровать` по всему
   собранному. Здесь нужен словарь: `flang/stdlib/hashmap.flang` даёт
   `«Положить в словарь»`, `«Найти в словаре»`, `«Есть ключ в словаре»`.
   Оговорка честная: модуль обобщённый (`от «З»`), а `compiler.flang` из
   stdlib ввозит сегодня только `«Strings»` — ввоз расширит замыкание, и цену
   расширения надо померить отдельно, прежде чем обещать выигрыш.

Первая правка формата вывода не меняет и семени не трогает; вторая тоже, но
дороже. Вместе они бьют по 73 % стадии и по бо́льшей части роста `n^2,58`.

**Потом дорогое — модульность.** Ей мешают ровно три вещи из раздела 1:

| что менять | где | порядок цены |
|---|---|---|
| нести имя файла-источника через связывание | `flang/self/link.flang` | 30–60 строк |
| счётчик временных и пул литералов сделать пофункционными (имя от содержимого, а не от порядка) | `emit-c.flang`: `«счётчик»` встречается 129 раз, `«ключи»` 129, `«статика»` 105 | 300–500 строк |
| один `.c` на модуль вместо одного на программу + общий заголовок + `Makefile` на много единиц | `«Печать программы»`, `«Печать Makefile»` | 100–200 строк |

И это ещё не всё: сквозные проходы `«Дефункционализировать»`,
`«Отметить меры»`, `«Отметить доказанные»`, `«Развязать постусловия»` и
`«Собрать общее»` смотрят всю программу целиком; пока они такие, «тронул один
файл — перепечатал один файл» не выйдет, даже когда печать тел разделится.
